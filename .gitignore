node_modules
.DS_Store
dist
.wand
.tmp
.codex
output
.playwright-cli
.playwright-mcp
debug-*.mjs
*.png
!docs/screenshots/*.png
.wand-uploads/
.wand-worktrees/
.dev-pack/

# 前端内嵌兜底资产：由 scripts/generate-web-assets.js 在 build/check/dev 时自动生成，
# 是构建产物而非源码，不入库（避免 2MB base64 逐版本提交撑大 .git 仓库）。
src/web-ui/embedded-assets.ts
# 浏览器层 esbuild 打包产物（scripts/bundle-browser.js 生成），同理不入库
src/web-ui/content/scripts.js

# IDE / 工具本地状态，不该入库
.qoder/
.claude/
