# wand

wand 是一个通过浏览器访问的本地终端工具，支持 Claude 等命令行工具。

A browser-accessible local terminal for running CLI tools like Claude.

## 启动 / Start

```bash
npm install -g @co0ontty/wand && wand init && wand web
```

配置文件 / Config: `~/.wand/config.json`
