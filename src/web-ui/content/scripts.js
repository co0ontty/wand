"use strict";
(() => {
  var __create = Object.create;
  var __defProp = Object.defineProperty;
  var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
  var __getOwnPropNames = Object.getOwnPropertyNames;
  var __getProtoOf = Object.getPrototypeOf;
  var __hasOwnProp = Object.prototype.hasOwnProperty;
  var __defNormalProp = (obj, key, value) => key in obj ? __defProp(obj, key, { enumerable: true, configurable: true, writable: true, value }) : obj[key] = value;
  var __commonJS = (cb, mod) => function __require() {
    return mod || (0, cb[__getOwnPropNames(cb)[0]])((mod = { exports: {} }).exports, mod), mod.exports;
  };
  var __copyProps = (to, from, except, desc) => {
    if (from && typeof from === "object" || typeof from === "function") {
      for (let key of __getOwnPropNames(from))
        if (!__hasOwnProp.call(to, key) && key !== except)
          __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
    }
    return to;
  };
  var __toESM = (mod, isNodeMode, target) => (target = mod != null ? __create(__getProtoOf(mod)) : {}, __copyProps(
    // If the importer is in node compatibility mode or this is not an ESM
    // file that has been converted to a CommonJS file using a Babel-
    // compatible transform (i.e. "__esModule" has not been set), then set
    // "default" to the CommonJS "module.exports" for node compatibility.
    isNodeMode || !mod || !mod.__esModule ? __defProp(target, "default", { value: mod, enumerable: true }) : target,
    mod
  ));
  var __publicField = (obj, key, value) => __defNormalProp(obj, typeof key !== "symbol" ? key + "" : key, value);

  // node_modules/react/cjs/react.production.js
  var require_react_production = __commonJS({
    "node_modules/react/cjs/react.production.js"(exports) {
      "use strict";
      var REACT_ELEMENT_TYPE = /* @__PURE__ */ Symbol.for("react.transitional.element");
      var REACT_PORTAL_TYPE = /* @__PURE__ */ Symbol.for("react.portal");
      var REACT_FRAGMENT_TYPE = /* @__PURE__ */ Symbol.for("react.fragment");
      var REACT_STRICT_MODE_TYPE = /* @__PURE__ */ Symbol.for("react.strict_mode");
      var REACT_PROFILER_TYPE = /* @__PURE__ */ Symbol.for("react.profiler");
      var REACT_CONSUMER_TYPE = /* @__PURE__ */ Symbol.for("react.consumer");
      var REACT_CONTEXT_TYPE = /* @__PURE__ */ Symbol.for("react.context");
      var REACT_FORWARD_REF_TYPE = /* @__PURE__ */ Symbol.for("react.forward_ref");
      var REACT_SUSPENSE_TYPE = /* @__PURE__ */ Symbol.for("react.suspense");
      var REACT_MEMO_TYPE = /* @__PURE__ */ Symbol.for("react.memo");
      var REACT_LAZY_TYPE2 = /* @__PURE__ */ Symbol.for("react.lazy");
      var REACT_ACTIVITY_TYPE = /* @__PURE__ */ Symbol.for("react.activity");
      var MAYBE_ITERATOR_SYMBOL = Symbol.iterator;
      function getIteratorFn(maybeIterable) {
        if (null === maybeIterable || "object" !== typeof maybeIterable) return null;
        maybeIterable = MAYBE_ITERATOR_SYMBOL && maybeIterable[MAYBE_ITERATOR_SYMBOL] || maybeIterable["@@iterator"];
        return "function" === typeof maybeIterable ? maybeIterable : null;
      }
      var ReactNoopUpdateQueue = {
        isMounted: function() {
          return false;
        },
        enqueueForceUpdate: function() {
        },
        enqueueReplaceState: function() {
        },
        enqueueSetState: function() {
        }
      };
      var assign = Object.assign;
      var emptyObject = {};
      function Component(props, context, updater) {
        this.props = props;
        this.context = context;
        this.refs = emptyObject;
        this.updater = updater || ReactNoopUpdateQueue;
      }
      Component.prototype.isReactComponent = {};
      Component.prototype.setState = function(partialState, callback) {
        if ("object" !== typeof partialState && "function" !== typeof partialState && null != partialState)
          throw Error(
            "takes an object of state variables to update or a function which returns an object of state variables."
          );
        this.updater.enqueueSetState(this, partialState, callback, "setState");
      };
      Component.prototype.forceUpdate = function(callback) {
        this.updater.enqueueForceUpdate(this, callback, "forceUpdate");
      };
      function ComponentDummy() {
      }
      ComponentDummy.prototype = Component.prototype;
      function PureComponent(props, context, updater) {
        this.props = props;
        this.context = context;
        this.refs = emptyObject;
        this.updater = updater || ReactNoopUpdateQueue;
      }
      var pureComponentPrototype = PureComponent.prototype = new ComponentDummy();
      pureComponentPrototype.constructor = PureComponent;
      assign(pureComponentPrototype, Component.prototype);
      pureComponentPrototype.isPureReactComponent = true;
      var isArrayImpl = Array.isArray;
      function noop3() {
      }
      var ReactSharedInternals = { H: null, A: null, T: null, S: null };
      var hasOwnProperty = Object.prototype.hasOwnProperty;
      function ReactElement(type, key, props) {
        var refProp = props.ref;
        return {
          $$typeof: REACT_ELEMENT_TYPE,
          type,
          key,
          ref: void 0 !== refProp ? refProp : null,
          props
        };
      }
      function cloneAndReplaceKey(oldElement, newKey) {
        return ReactElement(oldElement.type, newKey, oldElement.props);
      }
      function isValidElement2(object) {
        return "object" === typeof object && null !== object && object.$$typeof === REACT_ELEMENT_TYPE;
      }
      function escape(key) {
        var escaperLookup = { "=": "=0", ":": "=2" };
        return "$" + key.replace(/[=:]/g, function(match) {
          return escaperLookup[match];
        });
      }
      var userProvidedKeyEscapeRegex = /\/+/g;
      function getElementKey(element, index2) {
        return "object" === typeof element && null !== element && null != element.key ? escape("" + element.key) : index2.toString(36);
      }
      function resolveThenable(thenable) {
        switch (thenable.status) {
          case "fulfilled":
            return thenable.value;
          case "rejected":
            throw thenable.reason;
          default:
            switch ("string" === typeof thenable.status ? thenable.then(noop3, noop3) : (thenable.status = "pending", thenable.then(
              function(fulfilledValue) {
                "pending" === thenable.status && (thenable.status = "fulfilled", thenable.value = fulfilledValue);
              },
              function(error) {
                "pending" === thenable.status && (thenable.status = "rejected", thenable.reason = error);
              }
            )), thenable.status) {
              case "fulfilled":
                return thenable.value;
              case "rejected":
                throw thenable.reason;
            }
        }
        throw thenable;
      }
      function mapIntoArray(children, array, escapedPrefix, nameSoFar, callback) {
        var type = typeof children;
        if ("undefined" === type || "boolean" === type) children = null;
        var invokeCallback = false;
        if (null === children) invokeCallback = true;
        else
          switch (type) {
            case "bigint":
            case "string":
            case "number":
              invokeCallback = true;
              break;
            case "object":
              switch (children.$$typeof) {
                case REACT_ELEMENT_TYPE:
                case REACT_PORTAL_TYPE:
                  invokeCallback = true;
                  break;
                case REACT_LAZY_TYPE2:
                  return invokeCallback = children._init, mapIntoArray(
                    invokeCallback(children._payload),
                    array,
                    escapedPrefix,
                    nameSoFar,
                    callback
                  );
              }
          }
        if (invokeCallback)
          return callback = callback(children), invokeCallback = "" === nameSoFar ? "." + getElementKey(children, 0) : nameSoFar, isArrayImpl(callback) ? (escapedPrefix = "", null != invokeCallback && (escapedPrefix = invokeCallback.replace(userProvidedKeyEscapeRegex, "$&/") + "/"), mapIntoArray(callback, array, escapedPrefix, "", function(c) {
            return c;
          })) : null != callback && (isValidElement2(callback) && (callback = cloneAndReplaceKey(
            callback,
            escapedPrefix + (null == callback.key || children && children.key === callback.key ? "" : ("" + callback.key).replace(
              userProvidedKeyEscapeRegex,
              "$&/"
            ) + "/") + invokeCallback
          )), array.push(callback)), 1;
        invokeCallback = 0;
        var nextNamePrefix = "" === nameSoFar ? "." : nameSoFar + ":";
        if (isArrayImpl(children))
          for (var i = 0; i < children.length; i++)
            nameSoFar = children[i], type = nextNamePrefix + getElementKey(nameSoFar, i), invokeCallback += mapIntoArray(
              nameSoFar,
              array,
              escapedPrefix,
              type,
              callback
            );
        else if (i = getIteratorFn(children), "function" === typeof i)
          for (children = i.call(children), i = 0; !(nameSoFar = children.next()).done; )
            nameSoFar = nameSoFar.value, type = nextNamePrefix + getElementKey(nameSoFar, i++), invokeCallback += mapIntoArray(
              nameSoFar,
              array,
              escapedPrefix,
              type,
              callback
            );
        else if ("object" === type) {
          if ("function" === typeof children.then)
            return mapIntoArray(
              resolveThenable(children),
              array,
              escapedPrefix,
              nameSoFar,
              callback
            );
          array = String(children);
          throw Error(
            "Objects are not valid as a React child (found: " + ("[object Object]" === array ? "object with keys {" + Object.keys(children).join(", ") + "}" : array) + "). If you meant to render a collection of children, use an array instead."
          );
        }
        return invokeCallback;
      }
      function mapChildren(children, func, context) {
        if (null == children) return children;
        var result = [], count3 = 0;
        mapIntoArray(children, result, "", "", function(child) {
          return func.call(context, child, count3++);
        });
        return result;
      }
      function lazyInitializer(payload) {
        if (-1 === payload._status) {
          var ctor = payload._result;
          ctor = ctor();
          ctor.then(
            function(moduleObject) {
              if (0 === payload._status || -1 === payload._status)
                payload._status = 1, payload._result = moduleObject;
            },
            function(error) {
              if (0 === payload._status || -1 === payload._status)
                payload._status = 2, payload._result = error;
            }
          );
          -1 === payload._status && (payload._status = 0, payload._result = ctor);
        }
        if (1 === payload._status) return payload._result.default;
        throw payload._result;
      }
      var reportGlobalError = "function" === typeof reportError ? reportError : function(error) {
        if ("object" === typeof window && "function" === typeof window.ErrorEvent) {
          var event = new window.ErrorEvent("error", {
            bubbles: true,
            cancelable: true,
            message: "object" === typeof error && null !== error && "string" === typeof error.message ? String(error.message) : String(error),
            error
          });
          if (!window.dispatchEvent(event)) return;
        } else if ("object" === typeof process && "function" === typeof process.emit) {
          process.emit("uncaughtException", error);
          return;
        }
        console.error(error);
      };
      var Children5 = {
        map: mapChildren,
        forEach: function(children, forEachFunc, forEachContext) {
          mapChildren(
            children,
            function() {
              forEachFunc.apply(this, arguments);
            },
            forEachContext
          );
        },
        count: function(children) {
          var n = 0;
          mapChildren(children, function() {
            n++;
          });
          return n;
        },
        toArray: function(children) {
          return mapChildren(children, function(child) {
            return child;
          }) || [];
        },
        only: function(children) {
          if (!isValidElement2(children))
            throw Error(
              "React.Children.only expected to receive a single React element child."
            );
          return children;
        }
      };
      exports.Activity = REACT_ACTIVITY_TYPE;
      exports.Children = Children5;
      exports.Component = Component;
      exports.Fragment = REACT_FRAGMENT_TYPE;
      exports.Profiler = REACT_PROFILER_TYPE;
      exports.PureComponent = PureComponent;
      exports.StrictMode = REACT_STRICT_MODE_TYPE;
      exports.Suspense = REACT_SUSPENSE_TYPE;
      exports.__CLIENT_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE = ReactSharedInternals;
      exports.__COMPILER_RUNTIME = {
        __proto__: null,
        c: function(size4) {
          return ReactSharedInternals.H.useMemoCache(size4);
        }
      };
      exports.cache = function(fn) {
        return function() {
          return fn.apply(null, arguments);
        };
      };
      exports.cacheSignal = function() {
        return null;
      };
      exports.cloneElement = function(element, config, children) {
        if (null === element || void 0 === element)
          throw Error(
            "The argument must be a React element, but you passed " + element + "."
          );
        var props = assign({}, element.props), key = element.key;
        if (null != config)
          for (propName in void 0 !== config.key && (key = "" + config.key), config)
            !hasOwnProperty.call(config, propName) || "key" === propName || "__self" === propName || "__source" === propName || "ref" === propName && void 0 === config.ref || (props[propName] = config[propName]);
        var propName = arguments.length - 2;
        if (1 === propName) props.children = children;
        else if (1 < propName) {
          for (var childArray = Array(propName), i = 0; i < propName; i++)
            childArray[i] = arguments[i + 2];
          props.children = childArray;
        }
        return ReactElement(element.type, key, props);
      };
      exports.createContext = function(defaultValue) {
        defaultValue = {
          $$typeof: REACT_CONTEXT_TYPE,
          _currentValue: defaultValue,
          _currentValue2: defaultValue,
          _threadCount: 0,
          Provider: null,
          Consumer: null
        };
        defaultValue.Provider = defaultValue;
        defaultValue.Consumer = {
          $$typeof: REACT_CONSUMER_TYPE,
          _context: defaultValue
        };
        return defaultValue;
      };
      exports.createElement = function(type, config, children) {
        var propName, props = {}, key = null;
        if (null != config)
          for (propName in void 0 !== config.key && (key = "" + config.key), config)
            hasOwnProperty.call(config, propName) && "key" !== propName && "__self" !== propName && "__source" !== propName && (props[propName] = config[propName]);
        var childrenLength = arguments.length - 2;
        if (1 === childrenLength) props.children = children;
        else if (1 < childrenLength) {
          for (var childArray = Array(childrenLength), i = 0; i < childrenLength; i++)
            childArray[i] = arguments[i + 2];
          props.children = childArray;
        }
        if (type && type.defaultProps)
          for (propName in childrenLength = type.defaultProps, childrenLength)
            void 0 === props[propName] && (props[propName] = childrenLength[propName]);
        return ReactElement(type, key, props);
      };
      exports.createRef = function() {
        return { current: null };
      };
      exports.forwardRef = function(render3) {
        return { $$typeof: REACT_FORWARD_REF_TYPE, render: render3 };
      };
      exports.isValidElement = isValidElement2;
      exports.lazy = function(ctor) {
        return {
          $$typeof: REACT_LAZY_TYPE2,
          _payload: { _status: -1, _result: ctor },
          _init: lazyInitializer
        };
      };
      exports.memo = function(type, compare) {
        return {
          $$typeof: REACT_MEMO_TYPE,
          type,
          compare: void 0 === compare ? null : compare
        };
      };
      exports.startTransition = function(scope) {
        var prevTransition = ReactSharedInternals.T, currentTransition = {};
        ReactSharedInternals.T = currentTransition;
        try {
          var returnValue = scope(), onStartTransitionFinish = ReactSharedInternals.S;
          null !== onStartTransitionFinish && onStartTransitionFinish(currentTransition, returnValue);
          "object" === typeof returnValue && null !== returnValue && "function" === typeof returnValue.then && returnValue.then(noop3, reportGlobalError);
        } catch (error) {
          reportGlobalError(error);
        } finally {
          null !== prevTransition && null !== currentTransition.types && (prevTransition.types = currentTransition.types), ReactSharedInternals.T = prevTransition;
        }
      };
      exports.unstable_useCacheRefresh = function() {
        return ReactSharedInternals.H.useCacheRefresh();
      };
      exports.use = function(usable) {
        return ReactSharedInternals.H.use(usable);
      };
      exports.useActionState = function(action, initialState, permalink) {
        return ReactSharedInternals.H.useActionState(action, initialState, permalink);
      };
      exports.useCallback = function(callback, deps) {
        return ReactSharedInternals.H.useCallback(callback, deps);
      };
      exports.useContext = function(Context) {
        return ReactSharedInternals.H.useContext(Context);
      };
      exports.useDebugValue = function() {
      };
      exports.useDeferredValue = function(value, initialValue) {
        return ReactSharedInternals.H.useDeferredValue(value, initialValue);
      };
      exports.useEffect = function(create, deps) {
        return ReactSharedInternals.H.useEffect(create, deps);
      };
      exports.useEffectEvent = function(callback) {
        return ReactSharedInternals.H.useEffectEvent(callback);
      };
      exports.useId = function() {
        return ReactSharedInternals.H.useId();
      };
      exports.useImperativeHandle = function(ref, create, deps) {
        return ReactSharedInternals.H.useImperativeHandle(ref, create, deps);
      };
      exports.useInsertionEffect = function(create, deps) {
        return ReactSharedInternals.H.useInsertionEffect(create, deps);
      };
      exports.useLayoutEffect = function(create, deps) {
        return ReactSharedInternals.H.useLayoutEffect(create, deps);
      };
      exports.useMemo = function(create, deps) {
        return ReactSharedInternals.H.useMemo(create, deps);
      };
      exports.useOptimistic = function(passthrough, reducer) {
        return ReactSharedInternals.H.useOptimistic(passthrough, reducer);
      };
      exports.useReducer = function(reducer, initialArg, init) {
        return ReactSharedInternals.H.useReducer(reducer, initialArg, init);
      };
      exports.useRef = function(initialValue) {
        return ReactSharedInternals.H.useRef(initialValue);
      };
      exports.useState = function(initialState) {
        return ReactSharedInternals.H.useState(initialState);
      };
      exports.useSyncExternalStore = function(subscribe2, getSnapshot, getServerSnapshot) {
        return ReactSharedInternals.H.useSyncExternalStore(
          subscribe2,
          getSnapshot,
          getServerSnapshot
        );
      };
      exports.useTransition = function() {
        return ReactSharedInternals.H.useTransition();
      };
      exports.version = "19.2.7";
    }
  });

  // node_modules/react/index.js
  var require_react = __commonJS({
    "node_modules/react/index.js"(exports, module) {
      "use strict";
      if (true) {
        module.exports = require_react_production();
      } else {
        module.exports = null;
      }
    }
  });

  // node_modules/react-dom/cjs/react-dom.production.js
  var require_react_dom_production = __commonJS({
    "node_modules/react-dom/cjs/react-dom.production.js"(exports) {
      "use strict";
      var React42 = require_react();
      function formatProdErrorMessage(code) {
        var url = "https://react.dev/errors/" + code;
        if (1 < arguments.length) {
          url += "?args[]=" + encodeURIComponent(arguments[1]);
          for (var i = 2; i < arguments.length; i++)
            url += "&args[]=" + encodeURIComponent(arguments[i]);
        }
        return "Minified React error #" + code + "; visit " + url + " for the full message or use the non-minified dev environment for full errors and additional helpful warnings.";
      }
      function noop3() {
      }
      var Internals = {
        d: {
          f: noop3,
          r: function() {
            throw Error(formatProdErrorMessage(522));
          },
          D: noop3,
          C: noop3,
          L: noop3,
          m: noop3,
          X: noop3,
          S: noop3,
          M: noop3
        },
        p: 0,
        findDOMNode: null
      };
      var REACT_PORTAL_TYPE = /* @__PURE__ */ Symbol.for("react.portal");
      function createPortal$1(children, containerInfo, implementation) {
        var key = 3 < arguments.length && void 0 !== arguments[3] ? arguments[3] : null;
        return {
          $$typeof: REACT_PORTAL_TYPE,
          key: null == key ? null : "" + key,
          children,
          containerInfo,
          implementation
        };
      }
      var ReactSharedInternals = React42.__CLIENT_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE;
      function getCrossOriginStringAs(as, input) {
        if ("font" === as) return "";
        if ("string" === typeof input)
          return "use-credentials" === input ? input : "";
      }
      exports.__DOM_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE = Internals;
      exports.createPortal = function(children, container) {
        var key = 2 < arguments.length && void 0 !== arguments[2] ? arguments[2] : null;
        if (!container || 1 !== container.nodeType && 9 !== container.nodeType && 11 !== container.nodeType)
          throw Error(formatProdErrorMessage(299));
        return createPortal$1(children, container, null, key);
      };
      exports.flushSync = function(fn) {
        var previousTransition = ReactSharedInternals.T, previousUpdatePriority = Internals.p;
        try {
          if (ReactSharedInternals.T = null, Internals.p = 2, fn) return fn();
        } finally {
          ReactSharedInternals.T = previousTransition, Internals.p = previousUpdatePriority, Internals.d.f();
        }
      };
      exports.preconnect = function(href, options) {
        "string" === typeof href && (options ? (options = options.crossOrigin, options = "string" === typeof options ? "use-credentials" === options ? options : "" : void 0) : options = null, Internals.d.C(href, options));
      };
      exports.prefetchDNS = function(href) {
        "string" === typeof href && Internals.d.D(href);
      };
      exports.preinit = function(href, options) {
        if ("string" === typeof href && options && "string" === typeof options.as) {
          var as = options.as, crossOrigin = getCrossOriginStringAs(as, options.crossOrigin), integrity = "string" === typeof options.integrity ? options.integrity : void 0, fetchPriority = "string" === typeof options.fetchPriority ? options.fetchPriority : void 0;
          "style" === as ? Internals.d.S(
            href,
            "string" === typeof options.precedence ? options.precedence : void 0,
            {
              crossOrigin,
              integrity,
              fetchPriority
            }
          ) : "script" === as && Internals.d.X(href, {
            crossOrigin,
            integrity,
            fetchPriority,
            nonce: "string" === typeof options.nonce ? options.nonce : void 0
          });
        }
      };
      exports.preinitModule = function(href, options) {
        if ("string" === typeof href)
          if ("object" === typeof options && null !== options) {
            if (null == options.as || "script" === options.as) {
              var crossOrigin = getCrossOriginStringAs(
                options.as,
                options.crossOrigin
              );
              Internals.d.M(href, {
                crossOrigin,
                integrity: "string" === typeof options.integrity ? options.integrity : void 0,
                nonce: "string" === typeof options.nonce ? options.nonce : void 0
              });
            }
          } else null == options && Internals.d.M(href);
      };
      exports.preload = function(href, options) {
        if ("string" === typeof href && "object" === typeof options && null !== options && "string" === typeof options.as) {
          var as = options.as, crossOrigin = getCrossOriginStringAs(as, options.crossOrigin);
          Internals.d.L(href, as, {
            crossOrigin,
            integrity: "string" === typeof options.integrity ? options.integrity : void 0,
            nonce: "string" === typeof options.nonce ? options.nonce : void 0,
            type: "string" === typeof options.type ? options.type : void 0,
            fetchPriority: "string" === typeof options.fetchPriority ? options.fetchPriority : void 0,
            referrerPolicy: "string" === typeof options.referrerPolicy ? options.referrerPolicy : void 0,
            imageSrcSet: "string" === typeof options.imageSrcSet ? options.imageSrcSet : void 0,
            imageSizes: "string" === typeof options.imageSizes ? options.imageSizes : void 0,
            media: "string" === typeof options.media ? options.media : void 0
          });
        }
      };
      exports.preloadModule = function(href, options) {
        if ("string" === typeof href)
          if (options) {
            var crossOrigin = getCrossOriginStringAs(options.as, options.crossOrigin);
            Internals.d.m(href, {
              as: "string" === typeof options.as && "script" !== options.as ? options.as : void 0,
              crossOrigin,
              integrity: "string" === typeof options.integrity ? options.integrity : void 0
            });
          } else Internals.d.m(href);
      };
      exports.requestFormReset = function(form) {
        Internals.d.r(form);
      };
      exports.unstable_batchedUpdates = function(fn, a) {
        return fn(a);
      };
      exports.useFormState = function(action, initialState, permalink) {
        return ReactSharedInternals.H.useFormState(action, initialState, permalink);
      };
      exports.useFormStatus = function() {
        return ReactSharedInternals.H.useHostTransitionStatus();
      };
      exports.version = "19.2.7";
    }
  });

  // node_modules/react-dom/index.js
  var require_react_dom = __commonJS({
    "node_modules/react-dom/index.js"(exports, module) {
      "use strict";
      function checkDCE() {
        if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ === "undefined" || typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE !== "function") {
          return;
        }
        if (false) {
          throw new Error("^_^");
        }
        try {
          __REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE(checkDCE);
        } catch (err) {
          console.error(err);
        }
      }
      if (true) {
        checkDCE();
        module.exports = require_react_dom_production();
      } else {
        module.exports = null;
      }
    }
  });

  // node_modules/scheduler/cjs/scheduler.production.js
  var require_scheduler_production = __commonJS({
    "node_modules/scheduler/cjs/scheduler.production.js"(exports) {
      "use strict";
      function push(heap, node) {
        var index2 = heap.length;
        heap.push(node);
        a: for (; 0 < index2; ) {
          var parentIndex = index2 - 1 >>> 1, parent = heap[parentIndex];
          if (0 < compare(parent, node))
            heap[parentIndex] = node, heap[index2] = parent, index2 = parentIndex;
          else break a;
        }
      }
      function peek(heap) {
        return 0 === heap.length ? null : heap[0];
      }
      function pop(heap) {
        if (0 === heap.length) return null;
        var first = heap[0], last = heap.pop();
        if (last !== first) {
          heap[0] = last;
          a: for (var index2 = 0, length = heap.length, halfLength = length >>> 1; index2 < halfLength; ) {
            var leftIndex = 2 * (index2 + 1) - 1, left = heap[leftIndex], rightIndex = leftIndex + 1, right = heap[rightIndex];
            if (0 > compare(left, last))
              rightIndex < length && 0 > compare(right, left) ? (heap[index2] = right, heap[rightIndex] = last, index2 = rightIndex) : (heap[index2] = left, heap[leftIndex] = last, index2 = leftIndex);
            else if (rightIndex < length && 0 > compare(right, last))
              heap[index2] = right, heap[rightIndex] = last, index2 = rightIndex;
            else break a;
          }
        }
        return first;
      }
      function compare(a, b) {
        var diff = a.sortIndex - b.sortIndex;
        return 0 !== diff ? diff : a.id - b.id;
      }
      exports.unstable_now = void 0;
      if ("object" === typeof performance && "function" === typeof performance.now) {
        localPerformance = performance;
        exports.unstable_now = function() {
          return localPerformance.now();
        };
      } else {
        localDate = Date, initialTime = localDate.now();
        exports.unstable_now = function() {
          return localDate.now() - initialTime;
        };
      }
      var localPerformance;
      var localDate;
      var initialTime;
      var taskQueue = [];
      var timerQueue = [];
      var taskIdCounter = 1;
      var currentTask = null;
      var currentPriorityLevel = 3;
      var isPerformingWork = false;
      var isHostCallbackScheduled = false;
      var isHostTimeoutScheduled = false;
      var needsPaint = false;
      var localSetTimeout = "function" === typeof setTimeout ? setTimeout : null;
      var localClearTimeout = "function" === typeof clearTimeout ? clearTimeout : null;
      var localSetImmediate = "undefined" !== typeof setImmediate ? setImmediate : null;
      function advanceTimers(currentTime) {
        for (var timer = peek(timerQueue); null !== timer; ) {
          if (null === timer.callback) pop(timerQueue);
          else if (timer.startTime <= currentTime)
            pop(timerQueue), timer.sortIndex = timer.expirationTime, push(taskQueue, timer);
          else break;
          timer = peek(timerQueue);
        }
      }
      function handleTimeout(currentTime) {
        isHostTimeoutScheduled = false;
        advanceTimers(currentTime);
        if (!isHostCallbackScheduled)
          if (null !== peek(taskQueue))
            isHostCallbackScheduled = true, isMessageLoopRunning || (isMessageLoopRunning = true, schedulePerformWorkUntilDeadline());
          else {
            var firstTimer = peek(timerQueue);
            null !== firstTimer && requestHostTimeout(handleTimeout, firstTimer.startTime - currentTime);
          }
      }
      var isMessageLoopRunning = false;
      var taskTimeoutID = -1;
      var frameInterval = 5;
      var startTime = -1;
      function shouldYieldToHost() {
        return needsPaint ? true : exports.unstable_now() - startTime < frameInterval ? false : true;
      }
      function performWorkUntilDeadline() {
        needsPaint = false;
        if (isMessageLoopRunning) {
          var currentTime = exports.unstable_now();
          startTime = currentTime;
          var hasMoreWork = true;
          try {
            a: {
              isHostCallbackScheduled = false;
              isHostTimeoutScheduled && (isHostTimeoutScheduled = false, localClearTimeout(taskTimeoutID), taskTimeoutID = -1);
              isPerformingWork = true;
              var previousPriorityLevel = currentPriorityLevel;
              try {
                b: {
                  advanceTimers(currentTime);
                  for (currentTask = peek(taskQueue); null !== currentTask && !(currentTask.expirationTime > currentTime && shouldYieldToHost()); ) {
                    var callback = currentTask.callback;
                    if ("function" === typeof callback) {
                      currentTask.callback = null;
                      currentPriorityLevel = currentTask.priorityLevel;
                      var continuationCallback = callback(
                        currentTask.expirationTime <= currentTime
                      );
                      currentTime = exports.unstable_now();
                      if ("function" === typeof continuationCallback) {
                        currentTask.callback = continuationCallback;
                        advanceTimers(currentTime);
                        hasMoreWork = true;
                        break b;
                      }
                      currentTask === peek(taskQueue) && pop(taskQueue);
                      advanceTimers(currentTime);
                    } else pop(taskQueue);
                    currentTask = peek(taskQueue);
                  }
                  if (null !== currentTask) hasMoreWork = true;
                  else {
                    var firstTimer = peek(timerQueue);
                    null !== firstTimer && requestHostTimeout(
                      handleTimeout,
                      firstTimer.startTime - currentTime
                    );
                    hasMoreWork = false;
                  }
                }
                break a;
              } finally {
                currentTask = null, currentPriorityLevel = previousPriorityLevel, isPerformingWork = false;
              }
              hasMoreWork = void 0;
            }
          } finally {
            hasMoreWork ? schedulePerformWorkUntilDeadline() : isMessageLoopRunning = false;
          }
        }
      }
      var schedulePerformWorkUntilDeadline;
      if ("function" === typeof localSetImmediate)
        schedulePerformWorkUntilDeadline = function() {
          localSetImmediate(performWorkUntilDeadline);
        };
      else if ("undefined" !== typeof MessageChannel) {
        channel = new MessageChannel(), port = channel.port2;
        channel.port1.onmessage = performWorkUntilDeadline;
        schedulePerformWorkUntilDeadline = function() {
          port.postMessage(null);
        };
      } else
        schedulePerformWorkUntilDeadline = function() {
          localSetTimeout(performWorkUntilDeadline, 0);
        };
      var channel;
      var port;
      function requestHostTimeout(callback, ms) {
        taskTimeoutID = localSetTimeout(function() {
          callback(exports.unstable_now());
        }, ms);
      }
      exports.unstable_IdlePriority = 5;
      exports.unstable_ImmediatePriority = 1;
      exports.unstable_LowPriority = 4;
      exports.unstable_NormalPriority = 3;
      exports.unstable_Profiling = null;
      exports.unstable_UserBlockingPriority = 2;
      exports.unstable_cancelCallback = function(task) {
        task.callback = null;
      };
      exports.unstable_forceFrameRate = function(fps) {
        0 > fps || 125 < fps ? console.error(
          "forceFrameRate takes a positive int between 0 and 125, forcing frame rates higher than 125 fps is not supported"
        ) : frameInterval = 0 < fps ? Math.floor(1e3 / fps) : 5;
      };
      exports.unstable_getCurrentPriorityLevel = function() {
        return currentPriorityLevel;
      };
      exports.unstable_next = function(eventHandler) {
        switch (currentPriorityLevel) {
          case 1:
          case 2:
          case 3:
            var priorityLevel = 3;
            break;
          default:
            priorityLevel = currentPriorityLevel;
        }
        var previousPriorityLevel = currentPriorityLevel;
        currentPriorityLevel = priorityLevel;
        try {
          return eventHandler();
        } finally {
          currentPriorityLevel = previousPriorityLevel;
        }
      };
      exports.unstable_requestPaint = function() {
        needsPaint = true;
      };
      exports.unstable_runWithPriority = function(priorityLevel, eventHandler) {
        switch (priorityLevel) {
          case 1:
          case 2:
          case 3:
          case 4:
          case 5:
            break;
          default:
            priorityLevel = 3;
        }
        var previousPriorityLevel = currentPriorityLevel;
        currentPriorityLevel = priorityLevel;
        try {
          return eventHandler();
        } finally {
          currentPriorityLevel = previousPriorityLevel;
        }
      };
      exports.unstable_scheduleCallback = function(priorityLevel, callback, options) {
        var currentTime = exports.unstable_now();
        "object" === typeof options && null !== options ? (options = options.delay, options = "number" === typeof options && 0 < options ? currentTime + options : currentTime) : options = currentTime;
        switch (priorityLevel) {
          case 1:
            var timeout = -1;
            break;
          case 2:
            timeout = 250;
            break;
          case 5:
            timeout = 1073741823;
            break;
          case 4:
            timeout = 1e4;
            break;
          default:
            timeout = 5e3;
        }
        timeout = options + timeout;
        priorityLevel = {
          id: taskIdCounter++,
          callback,
          priorityLevel,
          startTime: options,
          expirationTime: timeout,
          sortIndex: -1
        };
        options > currentTime ? (priorityLevel.sortIndex = options, push(timerQueue, priorityLevel), null === peek(taskQueue) && priorityLevel === peek(timerQueue) && (isHostTimeoutScheduled ? (localClearTimeout(taskTimeoutID), taskTimeoutID = -1) : isHostTimeoutScheduled = true, requestHostTimeout(handleTimeout, options - currentTime))) : (priorityLevel.sortIndex = timeout, push(taskQueue, priorityLevel), isHostCallbackScheduled || isPerformingWork || (isHostCallbackScheduled = true, isMessageLoopRunning || (isMessageLoopRunning = true, schedulePerformWorkUntilDeadline())));
        return priorityLevel;
      };
      exports.unstable_shouldYield = shouldYieldToHost;
      exports.unstable_wrapCallback = function(callback) {
        var parentPriorityLevel = currentPriorityLevel;
        return function() {
          var previousPriorityLevel = currentPriorityLevel;
          currentPriorityLevel = parentPriorityLevel;
          try {
            return callback.apply(this, arguments);
          } finally {
            currentPriorityLevel = previousPriorityLevel;
          }
        };
      };
    }
  });

  // node_modules/scheduler/index.js
  var require_scheduler = __commonJS({
    "node_modules/scheduler/index.js"(exports, module) {
      "use strict";
      if (true) {
        module.exports = require_scheduler_production();
      } else {
        module.exports = null;
      }
    }
  });

  // node_modules/react-dom/cjs/react-dom-client.production.js
  var require_react_dom_client_production = __commonJS({
    "node_modules/react-dom/cjs/react-dom-client.production.js"(exports) {
      "use strict";
      var Scheduler = require_scheduler();
      var React42 = require_react();
      var ReactDOM6 = require_react_dom();
      function formatProdErrorMessage(code) {
        var url = "https://react.dev/errors/" + code;
        if (1 < arguments.length) {
          url += "?args[]=" + encodeURIComponent(arguments[1]);
          for (var i = 2; i < arguments.length; i++)
            url += "&args[]=" + encodeURIComponent(arguments[i]);
        }
        return "Minified React error #" + code + "; visit " + url + " for the full message or use the non-minified dev environment for full errors and additional helpful warnings.";
      }
      function isValidContainer(node) {
        return !(!node || 1 !== node.nodeType && 9 !== node.nodeType && 11 !== node.nodeType);
      }
      function getNearestMountedFiber(fiber) {
        var node = fiber, nearestMounted = fiber;
        if (fiber.alternate) for (; node.return; ) node = node.return;
        else {
          fiber = node;
          do
            node = fiber, 0 !== (node.flags & 4098) && (nearestMounted = node.return), fiber = node.return;
          while (fiber);
        }
        return 3 === node.tag ? nearestMounted : null;
      }
      function getSuspenseInstanceFromFiber(fiber) {
        if (13 === fiber.tag) {
          var suspenseState = fiber.memoizedState;
          null === suspenseState && (fiber = fiber.alternate, null !== fiber && (suspenseState = fiber.memoizedState));
          if (null !== suspenseState) return suspenseState.dehydrated;
        }
        return null;
      }
      function getActivityInstanceFromFiber(fiber) {
        if (31 === fiber.tag) {
          var activityState = fiber.memoizedState;
          null === activityState && (fiber = fiber.alternate, null !== fiber && (activityState = fiber.memoizedState));
          if (null !== activityState) return activityState.dehydrated;
        }
        return null;
      }
      function assertIsMounted(fiber) {
        if (getNearestMountedFiber(fiber) !== fiber)
          throw Error(formatProdErrorMessage(188));
      }
      function findCurrentFiberUsingSlowPath(fiber) {
        var alternate = fiber.alternate;
        if (!alternate) {
          alternate = getNearestMountedFiber(fiber);
          if (null === alternate) throw Error(formatProdErrorMessage(188));
          return alternate !== fiber ? null : fiber;
        }
        for (var a = fiber, b = alternate; ; ) {
          var parentA = a.return;
          if (null === parentA) break;
          var parentB = parentA.alternate;
          if (null === parentB) {
            b = parentA.return;
            if (null !== b) {
              a = b;
              continue;
            }
            break;
          }
          if (parentA.child === parentB.child) {
            for (parentB = parentA.child; parentB; ) {
              if (parentB === a) return assertIsMounted(parentA), fiber;
              if (parentB === b) return assertIsMounted(parentA), alternate;
              parentB = parentB.sibling;
            }
            throw Error(formatProdErrorMessage(188));
          }
          if (a.return !== b.return) a = parentA, b = parentB;
          else {
            for (var didFindChild = false, child$0 = parentA.child; child$0; ) {
              if (child$0 === a) {
                didFindChild = true;
                a = parentA;
                b = parentB;
                break;
              }
              if (child$0 === b) {
                didFindChild = true;
                b = parentA;
                a = parentB;
                break;
              }
              child$0 = child$0.sibling;
            }
            if (!didFindChild) {
              for (child$0 = parentB.child; child$0; ) {
                if (child$0 === a) {
                  didFindChild = true;
                  a = parentB;
                  b = parentA;
                  break;
                }
                if (child$0 === b) {
                  didFindChild = true;
                  b = parentB;
                  a = parentA;
                  break;
                }
                child$0 = child$0.sibling;
              }
              if (!didFindChild) throw Error(formatProdErrorMessage(189));
            }
          }
          if (a.alternate !== b) throw Error(formatProdErrorMessage(190));
        }
        if (3 !== a.tag) throw Error(formatProdErrorMessage(188));
        return a.stateNode.current === a ? fiber : alternate;
      }
      function findCurrentHostFiberImpl(node) {
        var tag = node.tag;
        if (5 === tag || 26 === tag || 27 === tag || 6 === tag) return node;
        for (node = node.child; null !== node; ) {
          tag = findCurrentHostFiberImpl(node);
          if (null !== tag) return tag;
          node = node.sibling;
        }
        return null;
      }
      var assign = Object.assign;
      var REACT_LEGACY_ELEMENT_TYPE = /* @__PURE__ */ Symbol.for("react.element");
      var REACT_ELEMENT_TYPE = /* @__PURE__ */ Symbol.for("react.transitional.element");
      var REACT_PORTAL_TYPE = /* @__PURE__ */ Symbol.for("react.portal");
      var REACT_FRAGMENT_TYPE = /* @__PURE__ */ Symbol.for("react.fragment");
      var REACT_STRICT_MODE_TYPE = /* @__PURE__ */ Symbol.for("react.strict_mode");
      var REACT_PROFILER_TYPE = /* @__PURE__ */ Symbol.for("react.profiler");
      var REACT_CONSUMER_TYPE = /* @__PURE__ */ Symbol.for("react.consumer");
      var REACT_CONTEXT_TYPE = /* @__PURE__ */ Symbol.for("react.context");
      var REACT_FORWARD_REF_TYPE = /* @__PURE__ */ Symbol.for("react.forward_ref");
      var REACT_SUSPENSE_TYPE = /* @__PURE__ */ Symbol.for("react.suspense");
      var REACT_SUSPENSE_LIST_TYPE = /* @__PURE__ */ Symbol.for("react.suspense_list");
      var REACT_MEMO_TYPE = /* @__PURE__ */ Symbol.for("react.memo");
      var REACT_LAZY_TYPE2 = /* @__PURE__ */ Symbol.for("react.lazy");
      var REACT_ACTIVITY_TYPE = /* @__PURE__ */ Symbol.for("react.activity");
      var REACT_MEMO_CACHE_SENTINEL = /* @__PURE__ */ Symbol.for("react.memo_cache_sentinel");
      var MAYBE_ITERATOR_SYMBOL = Symbol.iterator;
      function getIteratorFn(maybeIterable) {
        if (null === maybeIterable || "object" !== typeof maybeIterable) return null;
        maybeIterable = MAYBE_ITERATOR_SYMBOL && maybeIterable[MAYBE_ITERATOR_SYMBOL] || maybeIterable["@@iterator"];
        return "function" === typeof maybeIterable ? maybeIterable : null;
      }
      var REACT_CLIENT_REFERENCE = /* @__PURE__ */ Symbol.for("react.client.reference");
      function getComponentNameFromType(type) {
        if (null == type) return null;
        if ("function" === typeof type)
          return type.$$typeof === REACT_CLIENT_REFERENCE ? null : type.displayName || type.name || null;
        if ("string" === typeof type) return type;
        switch (type) {
          case REACT_FRAGMENT_TYPE:
            return "Fragment";
          case REACT_PROFILER_TYPE:
            return "Profiler";
          case REACT_STRICT_MODE_TYPE:
            return "StrictMode";
          case REACT_SUSPENSE_TYPE:
            return "Suspense";
          case REACT_SUSPENSE_LIST_TYPE:
            return "SuspenseList";
          case REACT_ACTIVITY_TYPE:
            return "Activity";
        }
        if ("object" === typeof type)
          switch (type.$$typeof) {
            case REACT_PORTAL_TYPE:
              return "Portal";
            case REACT_CONTEXT_TYPE:
              return type.displayName || "Context";
            case REACT_CONSUMER_TYPE:
              return (type._context.displayName || "Context") + ".Consumer";
            case REACT_FORWARD_REF_TYPE:
              var innerType = type.render;
              type = type.displayName;
              type || (type = innerType.displayName || innerType.name || "", type = "" !== type ? "ForwardRef(" + type + ")" : "ForwardRef");
              return type;
            case REACT_MEMO_TYPE:
              return innerType = type.displayName || null, null !== innerType ? innerType : getComponentNameFromType(type.type) || "Memo";
            case REACT_LAZY_TYPE2:
              innerType = type._payload;
              type = type._init;
              try {
                return getComponentNameFromType(type(innerType));
              } catch (x) {
              }
          }
        return null;
      }
      var isArrayImpl = Array.isArray;
      var ReactSharedInternals = React42.__CLIENT_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE;
      var ReactDOMSharedInternals = ReactDOM6.__DOM_INTERNALS_DO_NOT_USE_OR_WARN_USERS_THEY_CANNOT_UPGRADE;
      var sharedNotPendingObject = {
        pending: false,
        data: null,
        method: null,
        action: null
      };
      var valueStack = [];
      var index2 = -1;
      function createCursor(defaultValue) {
        return { current: defaultValue };
      }
      function pop(cursor) {
        0 > index2 || (cursor.current = valueStack[index2], valueStack[index2] = null, index2--);
      }
      function push(cursor, value) {
        index2++;
        valueStack[index2] = cursor.current;
        cursor.current = value;
      }
      var contextStackCursor = createCursor(null);
      var contextFiberStackCursor = createCursor(null);
      var rootInstanceStackCursor = createCursor(null);
      var hostTransitionProviderCursor = createCursor(null);
      function pushHostContainer(fiber, nextRootInstance) {
        push(rootInstanceStackCursor, nextRootInstance);
        push(contextFiberStackCursor, fiber);
        push(contextStackCursor, null);
        switch (nextRootInstance.nodeType) {
          case 9:
          case 11:
            fiber = (fiber = nextRootInstance.documentElement) ? (fiber = fiber.namespaceURI) ? getOwnHostContext(fiber) : 0 : 0;
            break;
          default:
            if (fiber = nextRootInstance.tagName, nextRootInstance = nextRootInstance.namespaceURI)
              nextRootInstance = getOwnHostContext(nextRootInstance), fiber = getChildHostContextProd(nextRootInstance, fiber);
            else
              switch (fiber) {
                case "svg":
                  fiber = 1;
                  break;
                case "math":
                  fiber = 2;
                  break;
                default:
                  fiber = 0;
              }
        }
        pop(contextStackCursor);
        push(contextStackCursor, fiber);
      }
      function popHostContainer() {
        pop(contextStackCursor);
        pop(contextFiberStackCursor);
        pop(rootInstanceStackCursor);
      }
      function pushHostContext(fiber) {
        null !== fiber.memoizedState && push(hostTransitionProviderCursor, fiber);
        var context = contextStackCursor.current;
        var JSCompiler_inline_result = getChildHostContextProd(context, fiber.type);
        context !== JSCompiler_inline_result && (push(contextFiberStackCursor, fiber), push(contextStackCursor, JSCompiler_inline_result));
      }
      function popHostContext(fiber) {
        contextFiberStackCursor.current === fiber && (pop(contextStackCursor), pop(contextFiberStackCursor));
        hostTransitionProviderCursor.current === fiber && (pop(hostTransitionProviderCursor), HostTransitionContext._currentValue = sharedNotPendingObject);
      }
      var prefix;
      var suffix;
      function describeBuiltInComponentFrame(name) {
        if (void 0 === prefix)
          try {
            throw Error();
          } catch (x) {
            var match = x.stack.trim().match(/\n( *(at )?)/);
            prefix = match && match[1] || "";
            suffix = -1 < x.stack.indexOf("\n    at") ? " (<anonymous>)" : -1 < x.stack.indexOf("@") ? "@unknown:0:0" : "";
          }
        return "\n" + prefix + name + suffix;
      }
      var reentry = false;
      function describeNativeComponentFrame(fn, construct) {
        if (!fn || reentry) return "";
        reentry = true;
        var previousPrepareStackTrace = Error.prepareStackTrace;
        Error.prepareStackTrace = void 0;
        try {
          var RunInRootFrame = {
            DetermineComponentFrameRoot: function() {
              try {
                if (construct) {
                  var Fake = function() {
                    throw Error();
                  };
                  Object.defineProperty(Fake.prototype, "props", {
                    set: function() {
                      throw Error();
                    }
                  });
                  if ("object" === typeof Reflect && Reflect.construct) {
                    try {
                      Reflect.construct(Fake, []);
                    } catch (x) {
                      var control = x;
                    }
                    Reflect.construct(fn, [], Fake);
                  } else {
                    try {
                      Fake.call();
                    } catch (x$1) {
                      control = x$1;
                    }
                    fn.call(Fake.prototype);
                  }
                } else {
                  try {
                    throw Error();
                  } catch (x$2) {
                    control = x$2;
                  }
                  (Fake = fn()) && "function" === typeof Fake.catch && Fake.catch(function() {
                  });
                }
              } catch (sample) {
                if (sample && control && "string" === typeof sample.stack)
                  return [sample.stack, control.stack];
              }
              return [null, null];
            }
          };
          RunInRootFrame.DetermineComponentFrameRoot.displayName = "DetermineComponentFrameRoot";
          var namePropDescriptor = Object.getOwnPropertyDescriptor(
            RunInRootFrame.DetermineComponentFrameRoot,
            "name"
          );
          namePropDescriptor && namePropDescriptor.configurable && Object.defineProperty(
            RunInRootFrame.DetermineComponentFrameRoot,
            "name",
            { value: "DetermineComponentFrameRoot" }
          );
          var _RunInRootFrame$Deter = RunInRootFrame.DetermineComponentFrameRoot(), sampleStack = _RunInRootFrame$Deter[0], controlStack = _RunInRootFrame$Deter[1];
          if (sampleStack && controlStack) {
            var sampleLines = sampleStack.split("\n"), controlLines = controlStack.split("\n");
            for (namePropDescriptor = RunInRootFrame = 0; RunInRootFrame < sampleLines.length && !sampleLines[RunInRootFrame].includes("DetermineComponentFrameRoot"); )
              RunInRootFrame++;
            for (; namePropDescriptor < controlLines.length && !controlLines[namePropDescriptor].includes(
              "DetermineComponentFrameRoot"
            ); )
              namePropDescriptor++;
            if (RunInRootFrame === sampleLines.length || namePropDescriptor === controlLines.length)
              for (RunInRootFrame = sampleLines.length - 1, namePropDescriptor = controlLines.length - 1; 1 <= RunInRootFrame && 0 <= namePropDescriptor && sampleLines[RunInRootFrame] !== controlLines[namePropDescriptor]; )
                namePropDescriptor--;
            for (; 1 <= RunInRootFrame && 0 <= namePropDescriptor; RunInRootFrame--, namePropDescriptor--)
              if (sampleLines[RunInRootFrame] !== controlLines[namePropDescriptor]) {
                if (1 !== RunInRootFrame || 1 !== namePropDescriptor) {
                  do
                    if (RunInRootFrame--, namePropDescriptor--, 0 > namePropDescriptor || sampleLines[RunInRootFrame] !== controlLines[namePropDescriptor]) {
                      var frame = "\n" + sampleLines[RunInRootFrame].replace(" at new ", " at ");
                      fn.displayName && frame.includes("<anonymous>") && (frame = frame.replace("<anonymous>", fn.displayName));
                      return frame;
                    }
                  while (1 <= RunInRootFrame && 0 <= namePropDescriptor);
                }
                break;
              }
          }
        } finally {
          reentry = false, Error.prepareStackTrace = previousPrepareStackTrace;
        }
        return (previousPrepareStackTrace = fn ? fn.displayName || fn.name : "") ? describeBuiltInComponentFrame(previousPrepareStackTrace) : "";
      }
      function describeFiber(fiber, childFiber) {
        switch (fiber.tag) {
          case 26:
          case 27:
          case 5:
            return describeBuiltInComponentFrame(fiber.type);
          case 16:
            return describeBuiltInComponentFrame("Lazy");
          case 13:
            return fiber.child !== childFiber && null !== childFiber ? describeBuiltInComponentFrame("Suspense Fallback") : describeBuiltInComponentFrame("Suspense");
          case 19:
            return describeBuiltInComponentFrame("SuspenseList");
          case 0:
          case 15:
            return describeNativeComponentFrame(fiber.type, false);
          case 11:
            return describeNativeComponentFrame(fiber.type.render, false);
          case 1:
            return describeNativeComponentFrame(fiber.type, true);
          case 31:
            return describeBuiltInComponentFrame("Activity");
          default:
            return "";
        }
      }
      function getStackByFiberInDevAndProd(workInProgress2) {
        try {
          var info = "", previous = null;
          do
            info += describeFiber(workInProgress2, previous), previous = workInProgress2, workInProgress2 = workInProgress2.return;
          while (workInProgress2);
          return info;
        } catch (x) {
          return "\nError generating stack: " + x.message + "\n" + x.stack;
        }
      }
      var hasOwnProperty = Object.prototype.hasOwnProperty;
      var scheduleCallback$3 = Scheduler.unstable_scheduleCallback;
      var cancelCallback$1 = Scheduler.unstable_cancelCallback;
      var shouldYield = Scheduler.unstable_shouldYield;
      var requestPaint = Scheduler.unstable_requestPaint;
      var now = Scheduler.unstable_now;
      var getCurrentPriorityLevel = Scheduler.unstable_getCurrentPriorityLevel;
      var ImmediatePriority = Scheduler.unstable_ImmediatePriority;
      var UserBlockingPriority = Scheduler.unstable_UserBlockingPriority;
      var NormalPriority$1 = Scheduler.unstable_NormalPriority;
      var LowPriority = Scheduler.unstable_LowPriority;
      var IdlePriority = Scheduler.unstable_IdlePriority;
      var log$1 = Scheduler.log;
      var unstable_setDisableYieldValue = Scheduler.unstable_setDisableYieldValue;
      var rendererID = null;
      var injectedHook = null;
      function setIsStrictModeForDevtools(newIsStrictMode) {
        "function" === typeof log$1 && unstable_setDisableYieldValue(newIsStrictMode);
        if (injectedHook && "function" === typeof injectedHook.setStrictMode)
          try {
            injectedHook.setStrictMode(rendererID, newIsStrictMode);
          } catch (err) {
          }
      }
      var clz32 = Math.clz32 ? Math.clz32 : clz32Fallback;
      var log = Math.log;
      var LN2 = Math.LN2;
      function clz32Fallback(x) {
        x >>>= 0;
        return 0 === x ? 32 : 31 - (log(x) / LN2 | 0) | 0;
      }
      var nextTransitionUpdateLane = 256;
      var nextTransitionDeferredLane = 262144;
      var nextRetryLane = 4194304;
      function getHighestPriorityLanes(lanes) {
        var pendingSyncLanes = lanes & 42;
        if (0 !== pendingSyncLanes) return pendingSyncLanes;
        switch (lanes & -lanes) {
          case 1:
            return 1;
          case 2:
            return 2;
          case 4:
            return 4;
          case 8:
            return 8;
          case 16:
            return 16;
          case 32:
            return 32;
          case 64:
            return 64;
          case 128:
            return 128;
          case 256:
          case 512:
          case 1024:
          case 2048:
          case 4096:
          case 8192:
          case 16384:
          case 32768:
          case 65536:
          case 131072:
            return lanes & 261888;
          case 262144:
          case 524288:
          case 1048576:
          case 2097152:
            return lanes & 3932160;
          case 4194304:
          case 8388608:
          case 16777216:
          case 33554432:
            return lanes & 62914560;
          case 67108864:
            return 67108864;
          case 134217728:
            return 134217728;
          case 268435456:
            return 268435456;
          case 536870912:
            return 536870912;
          case 1073741824:
            return 0;
          default:
            return lanes;
        }
      }
      function getNextLanes(root2, wipLanes, rootHasPendingCommit) {
        var pendingLanes = root2.pendingLanes;
        if (0 === pendingLanes) return 0;
        var nextLanes = 0, suspendedLanes = root2.suspendedLanes, pingedLanes = root2.pingedLanes;
        root2 = root2.warmLanes;
        var nonIdlePendingLanes = pendingLanes & 134217727;
        0 !== nonIdlePendingLanes ? (pendingLanes = nonIdlePendingLanes & ~suspendedLanes, 0 !== pendingLanes ? nextLanes = getHighestPriorityLanes(pendingLanes) : (pingedLanes &= nonIdlePendingLanes, 0 !== pingedLanes ? nextLanes = getHighestPriorityLanes(pingedLanes) : rootHasPendingCommit || (rootHasPendingCommit = nonIdlePendingLanes & ~root2, 0 !== rootHasPendingCommit && (nextLanes = getHighestPriorityLanes(rootHasPendingCommit))))) : (nonIdlePendingLanes = pendingLanes & ~suspendedLanes, 0 !== nonIdlePendingLanes ? nextLanes = getHighestPriorityLanes(nonIdlePendingLanes) : 0 !== pingedLanes ? nextLanes = getHighestPriorityLanes(pingedLanes) : rootHasPendingCommit || (rootHasPendingCommit = pendingLanes & ~root2, 0 !== rootHasPendingCommit && (nextLanes = getHighestPriorityLanes(rootHasPendingCommit))));
        return 0 === nextLanes ? 0 : 0 !== wipLanes && wipLanes !== nextLanes && 0 === (wipLanes & suspendedLanes) && (suspendedLanes = nextLanes & -nextLanes, rootHasPendingCommit = wipLanes & -wipLanes, suspendedLanes >= rootHasPendingCommit || 32 === suspendedLanes && 0 !== (rootHasPendingCommit & 4194048)) ? wipLanes : nextLanes;
      }
      function checkIfRootIsPrerendering(root2, renderLanes2) {
        return 0 === (root2.pendingLanes & ~(root2.suspendedLanes & ~root2.pingedLanes) & renderLanes2);
      }
      function computeExpirationTime(lane, currentTime) {
        switch (lane) {
          case 1:
          case 2:
          case 4:
          case 8:
          case 64:
            return currentTime + 250;
          case 16:
          case 32:
          case 128:
          case 256:
          case 512:
          case 1024:
          case 2048:
          case 4096:
          case 8192:
          case 16384:
          case 32768:
          case 65536:
          case 131072:
          case 262144:
          case 524288:
          case 1048576:
          case 2097152:
            return currentTime + 5e3;
          case 4194304:
          case 8388608:
          case 16777216:
          case 33554432:
            return -1;
          case 67108864:
          case 134217728:
          case 268435456:
          case 536870912:
          case 1073741824:
            return -1;
          default:
            return -1;
        }
      }
      function claimNextRetryLane() {
        var lane = nextRetryLane;
        nextRetryLane <<= 1;
        0 === (nextRetryLane & 62914560) && (nextRetryLane = 4194304);
        return lane;
      }
      function createLaneMap(initial) {
        for (var laneMap = [], i = 0; 31 > i; i++) laneMap.push(initial);
        return laneMap;
      }
      function markRootUpdated$1(root2, updateLane) {
        root2.pendingLanes |= updateLane;
        268435456 !== updateLane && (root2.suspendedLanes = 0, root2.pingedLanes = 0, root2.warmLanes = 0);
      }
      function markRootFinished(root2, finishedLanes, remainingLanes, spawnedLane, updatedLanes, suspendedRetryLanes) {
        var previouslyPendingLanes = root2.pendingLanes;
        root2.pendingLanes = remainingLanes;
        root2.suspendedLanes = 0;
        root2.pingedLanes = 0;
        root2.warmLanes = 0;
        root2.expiredLanes &= remainingLanes;
        root2.entangledLanes &= remainingLanes;
        root2.errorRecoveryDisabledLanes &= remainingLanes;
        root2.shellSuspendCounter = 0;
        var entanglements = root2.entanglements, expirationTimes = root2.expirationTimes, hiddenUpdates = root2.hiddenUpdates;
        for (remainingLanes = previouslyPendingLanes & ~remainingLanes; 0 < remainingLanes; ) {
          var index$7 = 31 - clz32(remainingLanes), lane = 1 << index$7;
          entanglements[index$7] = 0;
          expirationTimes[index$7] = -1;
          var hiddenUpdatesForLane = hiddenUpdates[index$7];
          if (null !== hiddenUpdatesForLane)
            for (hiddenUpdates[index$7] = null, index$7 = 0; index$7 < hiddenUpdatesForLane.length; index$7++) {
              var update = hiddenUpdatesForLane[index$7];
              null !== update && (update.lane &= -536870913);
            }
          remainingLanes &= ~lane;
        }
        0 !== spawnedLane && markSpawnedDeferredLane(root2, spawnedLane, 0);
        0 !== suspendedRetryLanes && 0 === updatedLanes && 0 !== root2.tag && (root2.suspendedLanes |= suspendedRetryLanes & ~(previouslyPendingLanes & ~finishedLanes));
      }
      function markSpawnedDeferredLane(root2, spawnedLane, entangledLanes) {
        root2.pendingLanes |= spawnedLane;
        root2.suspendedLanes &= ~spawnedLane;
        var spawnedLaneIndex = 31 - clz32(spawnedLane);
        root2.entangledLanes |= spawnedLane;
        root2.entanglements[spawnedLaneIndex] = root2.entanglements[spawnedLaneIndex] | 1073741824 | entangledLanes & 261930;
      }
      function markRootEntangled(root2, entangledLanes) {
        var rootEntangledLanes = root2.entangledLanes |= entangledLanes;
        for (root2 = root2.entanglements; rootEntangledLanes; ) {
          var index$8 = 31 - clz32(rootEntangledLanes), lane = 1 << index$8;
          lane & entangledLanes | root2[index$8] & entangledLanes && (root2[index$8] |= entangledLanes);
          rootEntangledLanes &= ~lane;
        }
      }
      function getBumpedLaneForHydration(root2, renderLanes2) {
        var renderLane = renderLanes2 & -renderLanes2;
        renderLane = 0 !== (renderLane & 42) ? 1 : getBumpedLaneForHydrationByLane(renderLane);
        return 0 !== (renderLane & (root2.suspendedLanes | renderLanes2)) ? 0 : renderLane;
      }
      function getBumpedLaneForHydrationByLane(lane) {
        switch (lane) {
          case 2:
            lane = 1;
            break;
          case 8:
            lane = 4;
            break;
          case 32:
            lane = 16;
            break;
          case 256:
          case 512:
          case 1024:
          case 2048:
          case 4096:
          case 8192:
          case 16384:
          case 32768:
          case 65536:
          case 131072:
          case 262144:
          case 524288:
          case 1048576:
          case 2097152:
          case 4194304:
          case 8388608:
          case 16777216:
          case 33554432:
            lane = 128;
            break;
          case 268435456:
            lane = 134217728;
            break;
          default:
            lane = 0;
        }
        return lane;
      }
      function lanesToEventPriority(lanes) {
        lanes &= -lanes;
        return 2 < lanes ? 8 < lanes ? 0 !== (lanes & 134217727) ? 32 : 268435456 : 8 : 2;
      }
      function resolveUpdatePriority() {
        var updatePriority = ReactDOMSharedInternals.p;
        if (0 !== updatePriority) return updatePriority;
        updatePriority = window.event;
        return void 0 === updatePriority ? 32 : getEventPriority(updatePriority.type);
      }
      function runWithPriority(priority, fn) {
        var previousPriority = ReactDOMSharedInternals.p;
        try {
          return ReactDOMSharedInternals.p = priority, fn();
        } finally {
          ReactDOMSharedInternals.p = previousPriority;
        }
      }
      var randomKey = Math.random().toString(36).slice(2);
      var internalInstanceKey = "__reactFiber$" + randomKey;
      var internalPropsKey = "__reactProps$" + randomKey;
      var internalContainerInstanceKey = "__reactContainer$" + randomKey;
      var internalEventHandlersKey = "__reactEvents$" + randomKey;
      var internalEventHandlerListenersKey = "__reactListeners$" + randomKey;
      var internalEventHandlesSetKey = "__reactHandles$" + randomKey;
      var internalRootNodeResourcesKey = "__reactResources$" + randomKey;
      var internalHoistableMarker = "__reactMarker$" + randomKey;
      function detachDeletedInstance(node) {
        delete node[internalInstanceKey];
        delete node[internalPropsKey];
        delete node[internalEventHandlersKey];
        delete node[internalEventHandlerListenersKey];
        delete node[internalEventHandlesSetKey];
      }
      function getClosestInstanceFromNode(targetNode) {
        var targetInst = targetNode[internalInstanceKey];
        if (targetInst) return targetInst;
        for (var parentNode = targetNode.parentNode; parentNode; ) {
          if (targetInst = parentNode[internalContainerInstanceKey] || parentNode[internalInstanceKey]) {
            parentNode = targetInst.alternate;
            if (null !== targetInst.child || null !== parentNode && null !== parentNode.child)
              for (targetNode = getParentHydrationBoundary(targetNode); null !== targetNode; ) {
                if (parentNode = targetNode[internalInstanceKey]) return parentNode;
                targetNode = getParentHydrationBoundary(targetNode);
              }
            return targetInst;
          }
          targetNode = parentNode;
          parentNode = targetNode.parentNode;
        }
        return null;
      }
      function getInstanceFromNode(node) {
        if (node = node[internalInstanceKey] || node[internalContainerInstanceKey]) {
          var tag = node.tag;
          if (5 === tag || 6 === tag || 13 === tag || 31 === tag || 26 === tag || 27 === tag || 3 === tag)
            return node;
        }
        return null;
      }
      function getNodeFromInstance(inst) {
        var tag = inst.tag;
        if (5 === tag || 26 === tag || 27 === tag || 6 === tag) return inst.stateNode;
        throw Error(formatProdErrorMessage(33));
      }
      function getResourcesFromRoot(root2) {
        var resources = root2[internalRootNodeResourcesKey];
        resources || (resources = root2[internalRootNodeResourcesKey] = { hoistableStyles: /* @__PURE__ */ new Map(), hoistableScripts: /* @__PURE__ */ new Map() });
        return resources;
      }
      function markNodeAsHoistable(node) {
        node[internalHoistableMarker] = true;
      }
      var allNativeEvents = /* @__PURE__ */ new Set();
      var registrationNameDependencies = {};
      function registerTwoPhaseEvent(registrationName, dependencies) {
        registerDirectEvent(registrationName, dependencies);
        registerDirectEvent(registrationName + "Capture", dependencies);
      }
      function registerDirectEvent(registrationName, dependencies) {
        registrationNameDependencies[registrationName] = dependencies;
        for (registrationName = 0; registrationName < dependencies.length; registrationName++)
          allNativeEvents.add(dependencies[registrationName]);
      }
      var VALID_ATTRIBUTE_NAME_REGEX = RegExp(
        "^[:A-Z_a-z\\u00C0-\\u00D6\\u00D8-\\u00F6\\u00F8-\\u02FF\\u0370-\\u037D\\u037F-\\u1FFF\\u200C-\\u200D\\u2070-\\u218F\\u2C00-\\u2FEF\\u3001-\\uD7FF\\uF900-\\uFDCF\\uFDF0-\\uFFFD][:A-Z_a-z\\u00C0-\\u00D6\\u00D8-\\u00F6\\u00F8-\\u02FF\\u0370-\\u037D\\u037F-\\u1FFF\\u200C-\\u200D\\u2070-\\u218F\\u2C00-\\u2FEF\\u3001-\\uD7FF\\uF900-\\uFDCF\\uFDF0-\\uFFFD\\-.0-9\\u00B7\\u0300-\\u036F\\u203F-\\u2040]*$"
      );
      var illegalAttributeNameCache = {};
      var validatedAttributeNameCache = {};
      function isAttributeNameSafe(attributeName) {
        if (hasOwnProperty.call(validatedAttributeNameCache, attributeName))
          return true;
        if (hasOwnProperty.call(illegalAttributeNameCache, attributeName)) return false;
        if (VALID_ATTRIBUTE_NAME_REGEX.test(attributeName))
          return validatedAttributeNameCache[attributeName] = true;
        illegalAttributeNameCache[attributeName] = true;
        return false;
      }
      function setValueForAttribute(node, name, value) {
        if (isAttributeNameSafe(name))
          if (null === value) node.removeAttribute(name);
          else {
            switch (typeof value) {
              case "undefined":
              case "function":
              case "symbol":
                node.removeAttribute(name);
                return;
              case "boolean":
                var prefix$10 = name.toLowerCase().slice(0, 5);
                if ("data-" !== prefix$10 && "aria-" !== prefix$10) {
                  node.removeAttribute(name);
                  return;
                }
            }
            node.setAttribute(name, "" + value);
          }
      }
      function setValueForKnownAttribute(node, name, value) {
        if (null === value) node.removeAttribute(name);
        else {
          switch (typeof value) {
            case "undefined":
            case "function":
            case "symbol":
            case "boolean":
              node.removeAttribute(name);
              return;
          }
          node.setAttribute(name, "" + value);
        }
      }
      function setValueForNamespacedAttribute(node, namespace, name, value) {
        if (null === value) node.removeAttribute(name);
        else {
          switch (typeof value) {
            case "undefined":
            case "function":
            case "symbol":
            case "boolean":
              node.removeAttribute(name);
              return;
          }
          node.setAttributeNS(namespace, name, "" + value);
        }
      }
      function getToStringValue(value) {
        switch (typeof value) {
          case "bigint":
          case "boolean":
          case "number":
          case "string":
          case "undefined":
            return value;
          case "object":
            return value;
          default:
            return "";
        }
      }
      function isCheckable(elem) {
        var type = elem.type;
        return (elem = elem.nodeName) && "input" === elem.toLowerCase() && ("checkbox" === type || "radio" === type);
      }
      function trackValueOnNode(node, valueField, currentValue) {
        var descriptor = Object.getOwnPropertyDescriptor(
          node.constructor.prototype,
          valueField
        );
        if (!node.hasOwnProperty(valueField) && "undefined" !== typeof descriptor && "function" === typeof descriptor.get && "function" === typeof descriptor.set) {
          var get = descriptor.get, set = descriptor.set;
          Object.defineProperty(node, valueField, {
            configurable: true,
            get: function() {
              return get.call(this);
            },
            set: function(value) {
              currentValue = "" + value;
              set.call(this, value);
            }
          });
          Object.defineProperty(node, valueField, {
            enumerable: descriptor.enumerable
          });
          return {
            getValue: function() {
              return currentValue;
            },
            setValue: function(value) {
              currentValue = "" + value;
            },
            stopTracking: function() {
              node._valueTracker = null;
              delete node[valueField];
            }
          };
        }
      }
      function track(node) {
        if (!node._valueTracker) {
          var valueField = isCheckable(node) ? "checked" : "value";
          node._valueTracker = trackValueOnNode(
            node,
            valueField,
            "" + node[valueField]
          );
        }
      }
      function updateValueIfChanged(node) {
        if (!node) return false;
        var tracker = node._valueTracker;
        if (!tracker) return true;
        var lastValue = tracker.getValue();
        var value = "";
        node && (value = isCheckable(node) ? node.checked ? "true" : "false" : node.value);
        node = value;
        return node !== lastValue ? (tracker.setValue(node), true) : false;
      }
      function getActiveElement(doc) {
        doc = doc || ("undefined" !== typeof document ? document : void 0);
        if ("undefined" === typeof doc) return null;
        try {
          return doc.activeElement || doc.body;
        } catch (e) {
          return doc.body;
        }
      }
      var escapeSelectorAttributeValueInsideDoubleQuotesRegex = /[\n"\\]/g;
      function escapeSelectorAttributeValueInsideDoubleQuotes(value) {
        return value.replace(
          escapeSelectorAttributeValueInsideDoubleQuotesRegex,
          function(ch) {
            return "\\" + ch.charCodeAt(0).toString(16) + " ";
          }
        );
      }
      function updateInput(element, value, defaultValue, lastDefaultValue, checked, defaultChecked, type, name) {
        element.name = "";
        null != type && "function" !== typeof type && "symbol" !== typeof type && "boolean" !== typeof type ? element.type = type : element.removeAttribute("type");
        if (null != value)
          if ("number" === type) {
            if (0 === value && "" === element.value || element.value != value)
              element.value = "" + getToStringValue(value);
          } else
            element.value !== "" + getToStringValue(value) && (element.value = "" + getToStringValue(value));
        else
          "submit" !== type && "reset" !== type || element.removeAttribute("value");
        null != value ? setDefaultValue(element, type, getToStringValue(value)) : null != defaultValue ? setDefaultValue(element, type, getToStringValue(defaultValue)) : null != lastDefaultValue && element.removeAttribute("value");
        null == checked && null != defaultChecked && (element.defaultChecked = !!defaultChecked);
        null != checked && (element.checked = checked && "function" !== typeof checked && "symbol" !== typeof checked);
        null != name && "function" !== typeof name && "symbol" !== typeof name && "boolean" !== typeof name ? element.name = "" + getToStringValue(name) : element.removeAttribute("name");
      }
      function initInput(element, value, defaultValue, checked, defaultChecked, type, name, isHydrating2) {
        null != type && "function" !== typeof type && "symbol" !== typeof type && "boolean" !== typeof type && (element.type = type);
        if (null != value || null != defaultValue) {
          if (!("submit" !== type && "reset" !== type || void 0 !== value && null !== value)) {
            track(element);
            return;
          }
          defaultValue = null != defaultValue ? "" + getToStringValue(defaultValue) : "";
          value = null != value ? "" + getToStringValue(value) : defaultValue;
          isHydrating2 || value === element.value || (element.value = value);
          element.defaultValue = value;
        }
        checked = null != checked ? checked : defaultChecked;
        checked = "function" !== typeof checked && "symbol" !== typeof checked && !!checked;
        element.checked = isHydrating2 ? element.checked : !!checked;
        element.defaultChecked = !!checked;
        null != name && "function" !== typeof name && "symbol" !== typeof name && "boolean" !== typeof name && (element.name = name);
        track(element);
      }
      function setDefaultValue(node, type, value) {
        "number" === type && getActiveElement(node.ownerDocument) === node || node.defaultValue === "" + value || (node.defaultValue = "" + value);
      }
      function updateOptions(node, multiple, propValue, setDefaultSelected) {
        node = node.options;
        if (multiple) {
          multiple = {};
          for (var i = 0; i < propValue.length; i++)
            multiple["$" + propValue[i]] = true;
          for (propValue = 0; propValue < node.length; propValue++)
            i = multiple.hasOwnProperty("$" + node[propValue].value), node[propValue].selected !== i && (node[propValue].selected = i), i && setDefaultSelected && (node[propValue].defaultSelected = true);
        } else {
          propValue = "" + getToStringValue(propValue);
          multiple = null;
          for (i = 0; i < node.length; i++) {
            if (node[i].value === propValue) {
              node[i].selected = true;
              setDefaultSelected && (node[i].defaultSelected = true);
              return;
            }
            null !== multiple || node[i].disabled || (multiple = node[i]);
          }
          null !== multiple && (multiple.selected = true);
        }
      }
      function updateTextarea(element, value, defaultValue) {
        if (null != value && (value = "" + getToStringValue(value), value !== element.value && (element.value = value), null == defaultValue)) {
          element.defaultValue !== value && (element.defaultValue = value);
          return;
        }
        element.defaultValue = null != defaultValue ? "" + getToStringValue(defaultValue) : "";
      }
      function initTextarea(element, value, defaultValue, children) {
        if (null == value) {
          if (null != children) {
            if (null != defaultValue) throw Error(formatProdErrorMessage(92));
            if (isArrayImpl(children)) {
              if (1 < children.length) throw Error(formatProdErrorMessage(93));
              children = children[0];
            }
            defaultValue = children;
          }
          null == defaultValue && (defaultValue = "");
          value = defaultValue;
        }
        defaultValue = getToStringValue(value);
        element.defaultValue = defaultValue;
        children = element.textContent;
        children === defaultValue && "" !== children && null !== children && (element.value = children);
        track(element);
      }
      function setTextContent(node, text5) {
        if (text5) {
          var firstChild = node.firstChild;
          if (firstChild && firstChild === node.lastChild && 3 === firstChild.nodeType) {
            firstChild.nodeValue = text5;
            return;
          }
        }
        node.textContent = text5;
      }
      var unitlessNumbers = new Set(
        "animationIterationCount aspectRatio borderImageOutset borderImageSlice borderImageWidth boxFlex boxFlexGroup boxOrdinalGroup columnCount columns flex flexGrow flexPositive flexShrink flexNegative flexOrder gridArea gridRow gridRowEnd gridRowSpan gridRowStart gridColumn gridColumnEnd gridColumnSpan gridColumnStart fontWeight lineClamp lineHeight opacity order orphans scale tabSize widows zIndex zoom fillOpacity floodOpacity stopOpacity strokeDasharray strokeDashoffset strokeMiterlimit strokeOpacity strokeWidth MozAnimationIterationCount MozBoxFlex MozBoxFlexGroup MozLineClamp msAnimationIterationCount msFlex msZoom msFlexGrow msFlexNegative msFlexOrder msFlexPositive msFlexShrink msGridColumn msGridColumnSpan msGridRow msGridRowSpan WebkitAnimationIterationCount WebkitBoxFlex WebKitBoxFlexGroup WebkitBoxOrdinalGroup WebkitColumnCount WebkitColumns WebkitFlex WebkitFlexGrow WebkitFlexPositive WebkitFlexShrink WebkitLineClamp".split(
          " "
        )
      );
      function setValueForStyle(style2, styleName, value) {
        var isCustomProperty = 0 === styleName.indexOf("--");
        null == value || "boolean" === typeof value || "" === value ? isCustomProperty ? style2.setProperty(styleName, "") : "float" === styleName ? style2.cssFloat = "" : style2[styleName] = "" : isCustomProperty ? style2.setProperty(styleName, value) : "number" !== typeof value || 0 === value || unitlessNumbers.has(styleName) ? "float" === styleName ? style2.cssFloat = value : style2[styleName] = ("" + value).trim() : style2[styleName] = value + "px";
      }
      function setValueForStyles(node, styles, prevStyles) {
        if (null != styles && "object" !== typeof styles)
          throw Error(formatProdErrorMessage(62));
        node = node.style;
        if (null != prevStyles) {
          for (var styleName in prevStyles)
            !prevStyles.hasOwnProperty(styleName) || null != styles && styles.hasOwnProperty(styleName) || (0 === styleName.indexOf("--") ? node.setProperty(styleName, "") : "float" === styleName ? node.cssFloat = "" : node[styleName] = "");
          for (var styleName$16 in styles)
            styleName = styles[styleName$16], styles.hasOwnProperty(styleName$16) && prevStyles[styleName$16] !== styleName && setValueForStyle(node, styleName$16, styleName);
        } else
          for (var styleName$17 in styles)
            styles.hasOwnProperty(styleName$17) && setValueForStyle(node, styleName$17, styles[styleName$17]);
      }
      function isCustomElement(tagName) {
        if (-1 === tagName.indexOf("-")) return false;
        switch (tagName) {
          case "annotation-xml":
          case "color-profile":
          case "font-face":
          case "font-face-src":
          case "font-face-uri":
          case "font-face-format":
          case "font-face-name":
          case "missing-glyph":
            return false;
          default:
            return true;
        }
      }
      var aliases = /* @__PURE__ */ new Map([
        ["acceptCharset", "accept-charset"],
        ["htmlFor", "for"],
        ["httpEquiv", "http-equiv"],
        ["crossOrigin", "crossorigin"],
        ["accentHeight", "accent-height"],
        ["alignmentBaseline", "alignment-baseline"],
        ["arabicForm", "arabic-form"],
        ["baselineShift", "baseline-shift"],
        ["capHeight", "cap-height"],
        ["clipPath", "clip-path"],
        ["clipRule", "clip-rule"],
        ["colorInterpolation", "color-interpolation"],
        ["colorInterpolationFilters", "color-interpolation-filters"],
        ["colorProfile", "color-profile"],
        ["colorRendering", "color-rendering"],
        ["dominantBaseline", "dominant-baseline"],
        ["enableBackground", "enable-background"],
        ["fillOpacity", "fill-opacity"],
        ["fillRule", "fill-rule"],
        ["floodColor", "flood-color"],
        ["floodOpacity", "flood-opacity"],
        ["fontFamily", "font-family"],
        ["fontSize", "font-size"],
        ["fontSizeAdjust", "font-size-adjust"],
        ["fontStretch", "font-stretch"],
        ["fontStyle", "font-style"],
        ["fontVariant", "font-variant"],
        ["fontWeight", "font-weight"],
        ["glyphName", "glyph-name"],
        ["glyphOrientationHorizontal", "glyph-orientation-horizontal"],
        ["glyphOrientationVertical", "glyph-orientation-vertical"],
        ["horizAdvX", "horiz-adv-x"],
        ["horizOriginX", "horiz-origin-x"],
        ["imageRendering", "image-rendering"],
        ["letterSpacing", "letter-spacing"],
        ["lightingColor", "lighting-color"],
        ["markerEnd", "marker-end"],
        ["markerMid", "marker-mid"],
        ["markerStart", "marker-start"],
        ["overlinePosition", "overline-position"],
        ["overlineThickness", "overline-thickness"],
        ["paintOrder", "paint-order"],
        ["panose-1", "panose-1"],
        ["pointerEvents", "pointer-events"],
        ["renderingIntent", "rendering-intent"],
        ["shapeRendering", "shape-rendering"],
        ["stopColor", "stop-color"],
        ["stopOpacity", "stop-opacity"],
        ["strikethroughPosition", "strikethrough-position"],
        ["strikethroughThickness", "strikethrough-thickness"],
        ["strokeDasharray", "stroke-dasharray"],
        ["strokeDashoffset", "stroke-dashoffset"],
        ["strokeLinecap", "stroke-linecap"],
        ["strokeLinejoin", "stroke-linejoin"],
        ["strokeMiterlimit", "stroke-miterlimit"],
        ["strokeOpacity", "stroke-opacity"],
        ["strokeWidth", "stroke-width"],
        ["textAnchor", "text-anchor"],
        ["textDecoration", "text-decoration"],
        ["textRendering", "text-rendering"],
        ["transformOrigin", "transform-origin"],
        ["underlinePosition", "underline-position"],
        ["underlineThickness", "underline-thickness"],
        ["unicodeBidi", "unicode-bidi"],
        ["unicodeRange", "unicode-range"],
        ["unitsPerEm", "units-per-em"],
        ["vAlphabetic", "v-alphabetic"],
        ["vHanging", "v-hanging"],
        ["vIdeographic", "v-ideographic"],
        ["vMathematical", "v-mathematical"],
        ["vectorEffect", "vector-effect"],
        ["vertAdvY", "vert-adv-y"],
        ["vertOriginX", "vert-origin-x"],
        ["vertOriginY", "vert-origin-y"],
        ["wordSpacing", "word-spacing"],
        ["writingMode", "writing-mode"],
        ["xmlnsXlink", "xmlns:xlink"],
        ["xHeight", "x-height"]
      ]);
      var isJavaScriptProtocol = /^[\u0000-\u001F ]*j[\r\n\t]*a[\r\n\t]*v[\r\n\t]*a[\r\n\t]*s[\r\n\t]*c[\r\n\t]*r[\r\n\t]*i[\r\n\t]*p[\r\n\t]*t[\r\n\t]*:/i;
      function sanitizeURL(url) {
        return isJavaScriptProtocol.test("" + url) ? "javascript:throw new Error('React has blocked a javascript: URL as a security precaution.')" : url;
      }
      function noop$1() {
      }
      var currentReplayingEvent = null;
      function getEventTarget(nativeEvent) {
        nativeEvent = nativeEvent.target || nativeEvent.srcElement || window;
        nativeEvent.correspondingUseElement && (nativeEvent = nativeEvent.correspondingUseElement);
        return 3 === nativeEvent.nodeType ? nativeEvent.parentNode : nativeEvent;
      }
      var restoreTarget = null;
      var restoreQueue = null;
      function restoreStateOfTarget(target) {
        var internalInstance = getInstanceFromNode(target);
        if (internalInstance && (target = internalInstance.stateNode)) {
          var props = target[internalPropsKey] || null;
          a: switch (target = internalInstance.stateNode, internalInstance.type) {
            case "input":
              updateInput(
                target,
                props.value,
                props.defaultValue,
                props.defaultValue,
                props.checked,
                props.defaultChecked,
                props.type,
                props.name
              );
              internalInstance = props.name;
              if ("radio" === props.type && null != internalInstance) {
                for (props = target; props.parentNode; ) props = props.parentNode;
                props = props.querySelectorAll(
                  'input[name="' + escapeSelectorAttributeValueInsideDoubleQuotes(
                    "" + internalInstance
                  ) + '"][type="radio"]'
                );
                for (internalInstance = 0; internalInstance < props.length; internalInstance++) {
                  var otherNode = props[internalInstance];
                  if (otherNode !== target && otherNode.form === target.form) {
                    var otherProps = otherNode[internalPropsKey] || null;
                    if (!otherProps) throw Error(formatProdErrorMessage(90));
                    updateInput(
                      otherNode,
                      otherProps.value,
                      otherProps.defaultValue,
                      otherProps.defaultValue,
                      otherProps.checked,
                      otherProps.defaultChecked,
                      otherProps.type,
                      otherProps.name
                    );
                  }
                }
                for (internalInstance = 0; internalInstance < props.length; internalInstance++)
                  otherNode = props[internalInstance], otherNode.form === target.form && updateValueIfChanged(otherNode);
              }
              break a;
            case "textarea":
              updateTextarea(target, props.value, props.defaultValue);
              break a;
            case "select":
              internalInstance = props.value, null != internalInstance && updateOptions(target, !!props.multiple, internalInstance, false);
          }
        }
      }
      var isInsideEventHandler = false;
      function batchedUpdates$1(fn, a, b) {
        if (isInsideEventHandler) return fn(a, b);
        isInsideEventHandler = true;
        try {
          var JSCompiler_inline_result = fn(a);
          return JSCompiler_inline_result;
        } finally {
          if (isInsideEventHandler = false, null !== restoreTarget || null !== restoreQueue) {
            if (flushSyncWork$1(), restoreTarget && (a = restoreTarget, fn = restoreQueue, restoreQueue = restoreTarget = null, restoreStateOfTarget(a), fn))
              for (a = 0; a < fn.length; a++) restoreStateOfTarget(fn[a]);
          }
        }
      }
      function getListener(inst, registrationName) {
        var stateNode = inst.stateNode;
        if (null === stateNode) return null;
        var props = stateNode[internalPropsKey] || null;
        if (null === props) return null;
        stateNode = props[registrationName];
        a: switch (registrationName) {
          case "onClick":
          case "onClickCapture":
          case "onDoubleClick":
          case "onDoubleClickCapture":
          case "onMouseDown":
          case "onMouseDownCapture":
          case "onMouseMove":
          case "onMouseMoveCapture":
          case "onMouseUp":
          case "onMouseUpCapture":
          case "onMouseEnter":
            (props = !props.disabled) || (inst = inst.type, props = !("button" === inst || "input" === inst || "select" === inst || "textarea" === inst));
            inst = !props;
            break a;
          default:
            inst = false;
        }
        if (inst) return null;
        if (stateNode && "function" !== typeof stateNode)
          throw Error(
            formatProdErrorMessage(231, registrationName, typeof stateNode)
          );
        return stateNode;
      }
      var canUseDOM2 = !("undefined" === typeof window || "undefined" === typeof window.document || "undefined" === typeof window.document.createElement);
      var passiveBrowserEventsSupported = false;
      if (canUseDOM2)
        try {
          options = {};
          Object.defineProperty(options, "passive", {
            get: function() {
              passiveBrowserEventsSupported = true;
            }
          });
          window.addEventListener("test", options, options);
          window.removeEventListener("test", options, options);
        } catch (e) {
          passiveBrowserEventsSupported = false;
        }
      var options;
      var root = null;
      var startText = null;
      var fallbackText = null;
      function getData() {
        if (fallbackText) return fallbackText;
        var start, startValue = startText, startLength = startValue.length, end, endValue = "value" in root ? root.value : root.textContent, endLength = endValue.length;
        for (start = 0; start < startLength && startValue[start] === endValue[start]; start++) ;
        var minEnd = startLength - start;
        for (end = 1; end <= minEnd && startValue[startLength - end] === endValue[endLength - end]; end++) ;
        return fallbackText = endValue.slice(start, 1 < end ? 1 - end : void 0);
      }
      function getEventCharCode(nativeEvent) {
        var keyCode = nativeEvent.keyCode;
        "charCode" in nativeEvent ? (nativeEvent = nativeEvent.charCode, 0 === nativeEvent && 13 === keyCode && (nativeEvent = 13)) : nativeEvent = keyCode;
        10 === nativeEvent && (nativeEvent = 13);
        return 32 <= nativeEvent || 13 === nativeEvent ? nativeEvent : 0;
      }
      function functionThatReturnsTrue() {
        return true;
      }
      function functionThatReturnsFalse() {
        return false;
      }
      function createSyntheticEvent(Interface) {
        function SyntheticBaseEvent(reactName, reactEventType, targetInst, nativeEvent, nativeEventTarget) {
          this._reactName = reactName;
          this._targetInst = targetInst;
          this.type = reactEventType;
          this.nativeEvent = nativeEvent;
          this.target = nativeEventTarget;
          this.currentTarget = null;
          for (var propName in Interface)
            Interface.hasOwnProperty(propName) && (reactName = Interface[propName], this[propName] = reactName ? reactName(nativeEvent) : nativeEvent[propName]);
          this.isDefaultPrevented = (null != nativeEvent.defaultPrevented ? nativeEvent.defaultPrevented : false === nativeEvent.returnValue) ? functionThatReturnsTrue : functionThatReturnsFalse;
          this.isPropagationStopped = functionThatReturnsFalse;
          return this;
        }
        assign(SyntheticBaseEvent.prototype, {
          preventDefault: function() {
            this.defaultPrevented = true;
            var event = this.nativeEvent;
            event && (event.preventDefault ? event.preventDefault() : "unknown" !== typeof event.returnValue && (event.returnValue = false), this.isDefaultPrevented = functionThatReturnsTrue);
          },
          stopPropagation: function() {
            var event = this.nativeEvent;
            event && (event.stopPropagation ? event.stopPropagation() : "unknown" !== typeof event.cancelBubble && (event.cancelBubble = true), this.isPropagationStopped = functionThatReturnsTrue);
          },
          persist: function() {
          },
          isPersistent: functionThatReturnsTrue
        });
        return SyntheticBaseEvent;
      }
      var EventInterface = {
        eventPhase: 0,
        bubbles: 0,
        cancelable: 0,
        timeStamp: function(event) {
          return event.timeStamp || Date.now();
        },
        defaultPrevented: 0,
        isTrusted: 0
      };
      var SyntheticEvent = createSyntheticEvent(EventInterface);
      var UIEventInterface = assign({}, EventInterface, { view: 0, detail: 0 });
      var SyntheticUIEvent = createSyntheticEvent(UIEventInterface);
      var lastMovementX;
      var lastMovementY;
      var lastMouseEvent;
      var MouseEventInterface = assign({}, UIEventInterface, {
        screenX: 0,
        screenY: 0,
        clientX: 0,
        clientY: 0,
        pageX: 0,
        pageY: 0,
        ctrlKey: 0,
        shiftKey: 0,
        altKey: 0,
        metaKey: 0,
        getModifierState: getEventModifierState,
        button: 0,
        buttons: 0,
        relatedTarget: function(event) {
          return void 0 === event.relatedTarget ? event.fromElement === event.srcElement ? event.toElement : event.fromElement : event.relatedTarget;
        },
        movementX: function(event) {
          if ("movementX" in event) return event.movementX;
          event !== lastMouseEvent && (lastMouseEvent && "mousemove" === event.type ? (lastMovementX = event.screenX - lastMouseEvent.screenX, lastMovementY = event.screenY - lastMouseEvent.screenY) : lastMovementY = lastMovementX = 0, lastMouseEvent = event);
          return lastMovementX;
        },
        movementY: function(event) {
          return "movementY" in event ? event.movementY : lastMovementY;
        }
      });
      var SyntheticMouseEvent = createSyntheticEvent(MouseEventInterface);
      var DragEventInterface = assign({}, MouseEventInterface, { dataTransfer: 0 });
      var SyntheticDragEvent = createSyntheticEvent(DragEventInterface);
      var FocusEventInterface = assign({}, UIEventInterface, { relatedTarget: 0 });
      var SyntheticFocusEvent = createSyntheticEvent(FocusEventInterface);
      var AnimationEventInterface = assign({}, EventInterface, {
        animationName: 0,
        elapsedTime: 0,
        pseudoElement: 0
      });
      var SyntheticAnimationEvent = createSyntheticEvent(AnimationEventInterface);
      var ClipboardEventInterface = assign({}, EventInterface, {
        clipboardData: function(event) {
          return "clipboardData" in event ? event.clipboardData : window.clipboardData;
        }
      });
      var SyntheticClipboardEvent = createSyntheticEvent(ClipboardEventInterface);
      var CompositionEventInterface = assign({}, EventInterface, { data: 0 });
      var SyntheticCompositionEvent = createSyntheticEvent(CompositionEventInterface);
      var normalizeKey = {
        Esc: "Escape",
        Spacebar: " ",
        Left: "ArrowLeft",
        Up: "ArrowUp",
        Right: "ArrowRight",
        Down: "ArrowDown",
        Del: "Delete",
        Win: "OS",
        Menu: "ContextMenu",
        Apps: "ContextMenu",
        Scroll: "ScrollLock",
        MozPrintableKey: "Unidentified"
      };
      var translateToKey = {
        8: "Backspace",
        9: "Tab",
        12: "Clear",
        13: "Enter",
        16: "Shift",
        17: "Control",
        18: "Alt",
        19: "Pause",
        20: "CapsLock",
        27: "Escape",
        32: " ",
        33: "PageUp",
        34: "PageDown",
        35: "End",
        36: "Home",
        37: "ArrowLeft",
        38: "ArrowUp",
        39: "ArrowRight",
        40: "ArrowDown",
        45: "Insert",
        46: "Delete",
        112: "F1",
        113: "F2",
        114: "F3",
        115: "F4",
        116: "F5",
        117: "F6",
        118: "F7",
        119: "F8",
        120: "F9",
        121: "F10",
        122: "F11",
        123: "F12",
        144: "NumLock",
        145: "ScrollLock",
        224: "Meta"
      };
      var modifierKeyToProp = {
        Alt: "altKey",
        Control: "ctrlKey",
        Meta: "metaKey",
        Shift: "shiftKey"
      };
      function modifierStateGetter(keyArg) {
        var nativeEvent = this.nativeEvent;
        return nativeEvent.getModifierState ? nativeEvent.getModifierState(keyArg) : (keyArg = modifierKeyToProp[keyArg]) ? !!nativeEvent[keyArg] : false;
      }
      function getEventModifierState() {
        return modifierStateGetter;
      }
      var KeyboardEventInterface = assign({}, UIEventInterface, {
        key: function(nativeEvent) {
          if (nativeEvent.key) {
            var key = normalizeKey[nativeEvent.key] || nativeEvent.key;
            if ("Unidentified" !== key) return key;
          }
          return "keypress" === nativeEvent.type ? (nativeEvent = getEventCharCode(nativeEvent), 13 === nativeEvent ? "Enter" : String.fromCharCode(nativeEvent)) : "keydown" === nativeEvent.type || "keyup" === nativeEvent.type ? translateToKey[nativeEvent.keyCode] || "Unidentified" : "";
        },
        code: 0,
        location: 0,
        ctrlKey: 0,
        shiftKey: 0,
        altKey: 0,
        metaKey: 0,
        repeat: 0,
        locale: 0,
        getModifierState: getEventModifierState,
        charCode: function(event) {
          return "keypress" === event.type ? getEventCharCode(event) : 0;
        },
        keyCode: function(event) {
          return "keydown" === event.type || "keyup" === event.type ? event.keyCode : 0;
        },
        which: function(event) {
          return "keypress" === event.type ? getEventCharCode(event) : "keydown" === event.type || "keyup" === event.type ? event.keyCode : 0;
        }
      });
      var SyntheticKeyboardEvent = createSyntheticEvent(KeyboardEventInterface);
      var PointerEventInterface = assign({}, MouseEventInterface, {
        pointerId: 0,
        width: 0,
        height: 0,
        pressure: 0,
        tangentialPressure: 0,
        tiltX: 0,
        tiltY: 0,
        twist: 0,
        pointerType: 0,
        isPrimary: 0
      });
      var SyntheticPointerEvent = createSyntheticEvent(PointerEventInterface);
      var TouchEventInterface = assign({}, UIEventInterface, {
        touches: 0,
        targetTouches: 0,
        changedTouches: 0,
        altKey: 0,
        metaKey: 0,
        ctrlKey: 0,
        shiftKey: 0,
        getModifierState: getEventModifierState
      });
      var SyntheticTouchEvent = createSyntheticEvent(TouchEventInterface);
      var TransitionEventInterface = assign({}, EventInterface, {
        propertyName: 0,
        elapsedTime: 0,
        pseudoElement: 0
      });
      var SyntheticTransitionEvent = createSyntheticEvent(TransitionEventInterface);
      var WheelEventInterface = assign({}, MouseEventInterface, {
        deltaX: function(event) {
          return "deltaX" in event ? event.deltaX : "wheelDeltaX" in event ? -event.wheelDeltaX : 0;
        },
        deltaY: function(event) {
          return "deltaY" in event ? event.deltaY : "wheelDeltaY" in event ? -event.wheelDeltaY : "wheelDelta" in event ? -event.wheelDelta : 0;
        },
        deltaZ: 0,
        deltaMode: 0
      });
      var SyntheticWheelEvent = createSyntheticEvent(WheelEventInterface);
      var ToggleEventInterface = assign({}, EventInterface, {
        newState: 0,
        oldState: 0
      });
      var SyntheticToggleEvent = createSyntheticEvent(ToggleEventInterface);
      var END_KEYCODES = [9, 13, 27, 32];
      var canUseCompositionEvent = canUseDOM2 && "CompositionEvent" in window;
      var documentMode = null;
      canUseDOM2 && "documentMode" in document && (documentMode = document.documentMode);
      var canUseTextInputEvent = canUseDOM2 && "TextEvent" in window && !documentMode;
      var useFallbackCompositionData = canUseDOM2 && (!canUseCompositionEvent || documentMode && 8 < documentMode && 11 >= documentMode);
      var SPACEBAR_CHAR = String.fromCharCode(32);
      var hasSpaceKeypress = false;
      function isFallbackCompositionEnd(domEventName, nativeEvent) {
        switch (domEventName) {
          case "keyup":
            return -1 !== END_KEYCODES.indexOf(nativeEvent.keyCode);
          case "keydown":
            return 229 !== nativeEvent.keyCode;
          case "keypress":
          case "mousedown":
          case "focusout":
            return true;
          default:
            return false;
        }
      }
      function getDataFromCustomEvent(nativeEvent) {
        nativeEvent = nativeEvent.detail;
        return "object" === typeof nativeEvent && "data" in nativeEvent ? nativeEvent.data : null;
      }
      var isComposing = false;
      function getNativeBeforeInputChars(domEventName, nativeEvent) {
        switch (domEventName) {
          case "compositionend":
            return getDataFromCustomEvent(nativeEvent);
          case "keypress":
            if (32 !== nativeEvent.which) return null;
            hasSpaceKeypress = true;
            return SPACEBAR_CHAR;
          case "textInput":
            return domEventName = nativeEvent.data, domEventName === SPACEBAR_CHAR && hasSpaceKeypress ? null : domEventName;
          default:
            return null;
        }
      }
      function getFallbackBeforeInputChars(domEventName, nativeEvent) {
        if (isComposing)
          return "compositionend" === domEventName || !canUseCompositionEvent && isFallbackCompositionEnd(domEventName, nativeEvent) ? (domEventName = getData(), fallbackText = startText = root = null, isComposing = false, domEventName) : null;
        switch (domEventName) {
          case "paste":
            return null;
          case "keypress":
            if (!(nativeEvent.ctrlKey || nativeEvent.altKey || nativeEvent.metaKey) || nativeEvent.ctrlKey && nativeEvent.altKey) {
              if (nativeEvent.char && 1 < nativeEvent.char.length)
                return nativeEvent.char;
              if (nativeEvent.which) return String.fromCharCode(nativeEvent.which);
            }
            return null;
          case "compositionend":
            return useFallbackCompositionData && "ko" !== nativeEvent.locale ? null : nativeEvent.data;
          default:
            return null;
        }
      }
      var supportedInputTypes = {
        color: true,
        date: true,
        datetime: true,
        "datetime-local": true,
        email: true,
        month: true,
        number: true,
        password: true,
        range: true,
        search: true,
        tel: true,
        text: true,
        time: true,
        url: true,
        week: true
      };
      function isTextInputElement(elem) {
        var nodeName = elem && elem.nodeName && elem.nodeName.toLowerCase();
        return "input" === nodeName ? !!supportedInputTypes[elem.type] : "textarea" === nodeName ? true : false;
      }
      function createAndAccumulateChangeEvent(dispatchQueue, inst, nativeEvent, target) {
        restoreTarget ? restoreQueue ? restoreQueue.push(target) : restoreQueue = [target] : restoreTarget = target;
        inst = accumulateTwoPhaseListeners(inst, "onChange");
        0 < inst.length && (nativeEvent = new SyntheticEvent(
          "onChange",
          "change",
          null,
          nativeEvent,
          target
        ), dispatchQueue.push({ event: nativeEvent, listeners: inst }));
      }
      var activeElement$1 = null;
      var activeElementInst$1 = null;
      function runEventInBatch(dispatchQueue) {
        processDispatchQueue(dispatchQueue, 0);
      }
      function getInstIfValueChanged(targetInst) {
        var targetNode = getNodeFromInstance(targetInst);
        if (updateValueIfChanged(targetNode)) return targetInst;
      }
      function getTargetInstForChangeEvent(domEventName, targetInst) {
        if ("change" === domEventName) return targetInst;
      }
      var isInputEventSupported = false;
      if (canUseDOM2) {
        if (canUseDOM2) {
          isSupported$jscomp$inline_427 = "oninput" in document;
          if (!isSupported$jscomp$inline_427) {
            element$jscomp$inline_428 = document.createElement("div");
            element$jscomp$inline_428.setAttribute("oninput", "return;");
            isSupported$jscomp$inline_427 = "function" === typeof element$jscomp$inline_428.oninput;
          }
          JSCompiler_inline_result$jscomp$286 = isSupported$jscomp$inline_427;
        } else JSCompiler_inline_result$jscomp$286 = false;
        isInputEventSupported = JSCompiler_inline_result$jscomp$286 && (!document.documentMode || 9 < document.documentMode);
      }
      var JSCompiler_inline_result$jscomp$286;
      var isSupported$jscomp$inline_427;
      var element$jscomp$inline_428;
      function stopWatchingForValueChange() {
        activeElement$1 && (activeElement$1.detachEvent("onpropertychange", handlePropertyChange), activeElementInst$1 = activeElement$1 = null);
      }
      function handlePropertyChange(nativeEvent) {
        if ("value" === nativeEvent.propertyName && getInstIfValueChanged(activeElementInst$1)) {
          var dispatchQueue = [];
          createAndAccumulateChangeEvent(
            dispatchQueue,
            activeElementInst$1,
            nativeEvent,
            getEventTarget(nativeEvent)
          );
          batchedUpdates$1(runEventInBatch, dispatchQueue);
        }
      }
      function handleEventsForInputEventPolyfill(domEventName, target, targetInst) {
        "focusin" === domEventName ? (stopWatchingForValueChange(), activeElement$1 = target, activeElementInst$1 = targetInst, activeElement$1.attachEvent("onpropertychange", handlePropertyChange)) : "focusout" === domEventName && stopWatchingForValueChange();
      }
      function getTargetInstForInputEventPolyfill(domEventName) {
        if ("selectionchange" === domEventName || "keyup" === domEventName || "keydown" === domEventName)
          return getInstIfValueChanged(activeElementInst$1);
      }
      function getTargetInstForClickEvent(domEventName, targetInst) {
        if ("click" === domEventName) return getInstIfValueChanged(targetInst);
      }
      function getTargetInstForInputOrChangeEvent(domEventName, targetInst) {
        if ("input" === domEventName || "change" === domEventName)
          return getInstIfValueChanged(targetInst);
      }
      function is(x, y) {
        return x === y && (0 !== x || 1 / x === 1 / y) || x !== x && y !== y;
      }
      var objectIs = "function" === typeof Object.is ? Object.is : is;
      function shallowEqual(objA, objB) {
        if (objectIs(objA, objB)) return true;
        if ("object" !== typeof objA || null === objA || "object" !== typeof objB || null === objB)
          return false;
        var keysA = Object.keys(objA), keysB = Object.keys(objB);
        if (keysA.length !== keysB.length) return false;
        for (keysB = 0; keysB < keysA.length; keysB++) {
          var currentKey = keysA[keysB];
          if (!hasOwnProperty.call(objB, currentKey) || !objectIs(objA[currentKey], objB[currentKey]))
            return false;
        }
        return true;
      }
      function getLeafNode(node) {
        for (; node && node.firstChild; ) node = node.firstChild;
        return node;
      }
      function getNodeForCharacterOffset(root2, offset4) {
        var node = getLeafNode(root2);
        root2 = 0;
        for (var nodeEnd; node; ) {
          if (3 === node.nodeType) {
            nodeEnd = root2 + node.textContent.length;
            if (root2 <= offset4 && nodeEnd >= offset4)
              return { node, offset: offset4 - root2 };
            root2 = nodeEnd;
          }
          a: {
            for (; node; ) {
              if (node.nextSibling) {
                node = node.nextSibling;
                break a;
              }
              node = node.parentNode;
            }
            node = void 0;
          }
          node = getLeafNode(node);
        }
      }
      function containsNode(outerNode, innerNode) {
        return outerNode && innerNode ? outerNode === innerNode ? true : outerNode && 3 === outerNode.nodeType ? false : innerNode && 3 === innerNode.nodeType ? containsNode(outerNode, innerNode.parentNode) : "contains" in outerNode ? outerNode.contains(innerNode) : outerNode.compareDocumentPosition ? !!(outerNode.compareDocumentPosition(innerNode) & 16) : false : false;
      }
      function getActiveElementDeep(containerInfo) {
        containerInfo = null != containerInfo && null != containerInfo.ownerDocument && null != containerInfo.ownerDocument.defaultView ? containerInfo.ownerDocument.defaultView : window;
        for (var element = getActiveElement(containerInfo.document); element instanceof containerInfo.HTMLIFrameElement; ) {
          try {
            var JSCompiler_inline_result = "string" === typeof element.contentWindow.location.href;
          } catch (err) {
            JSCompiler_inline_result = false;
          }
          if (JSCompiler_inline_result) containerInfo = element.contentWindow;
          else break;
          element = getActiveElement(containerInfo.document);
        }
        return element;
      }
      function hasSelectionCapabilities(elem) {
        var nodeName = elem && elem.nodeName && elem.nodeName.toLowerCase();
        return nodeName && ("input" === nodeName && ("text" === elem.type || "search" === elem.type || "tel" === elem.type || "url" === elem.type || "password" === elem.type) || "textarea" === nodeName || "true" === elem.contentEditable);
      }
      var skipSelectionChangeEvent = canUseDOM2 && "documentMode" in document && 11 >= document.documentMode;
      var activeElement = null;
      var activeElementInst = null;
      var lastSelection = null;
      var mouseDown = false;
      function constructSelectEvent(dispatchQueue, nativeEvent, nativeEventTarget) {
        var doc = nativeEventTarget.window === nativeEventTarget ? nativeEventTarget.document : 9 === nativeEventTarget.nodeType ? nativeEventTarget : nativeEventTarget.ownerDocument;
        mouseDown || null == activeElement || activeElement !== getActiveElement(doc) || (doc = activeElement, "selectionStart" in doc && hasSelectionCapabilities(doc) ? doc = { start: doc.selectionStart, end: doc.selectionEnd } : (doc = (doc.ownerDocument && doc.ownerDocument.defaultView || window).getSelection(), doc = {
          anchorNode: doc.anchorNode,
          anchorOffset: doc.anchorOffset,
          focusNode: doc.focusNode,
          focusOffset: doc.focusOffset
        }), lastSelection && shallowEqual(lastSelection, doc) || (lastSelection = doc, doc = accumulateTwoPhaseListeners(activeElementInst, "onSelect"), 0 < doc.length && (nativeEvent = new SyntheticEvent(
          "onSelect",
          "select",
          null,
          nativeEvent,
          nativeEventTarget
        ), dispatchQueue.push({ event: nativeEvent, listeners: doc }), nativeEvent.target = activeElement)));
      }
      function makePrefixMap(styleProp, eventName) {
        var prefixes = {};
        prefixes[styleProp.toLowerCase()] = eventName.toLowerCase();
        prefixes["Webkit" + styleProp] = "webkit" + eventName;
        prefixes["Moz" + styleProp] = "moz" + eventName;
        return prefixes;
      }
      var vendorPrefixes = {
        animationend: makePrefixMap("Animation", "AnimationEnd"),
        animationiteration: makePrefixMap("Animation", "AnimationIteration"),
        animationstart: makePrefixMap("Animation", "AnimationStart"),
        transitionrun: makePrefixMap("Transition", "TransitionRun"),
        transitionstart: makePrefixMap("Transition", "TransitionStart"),
        transitioncancel: makePrefixMap("Transition", "TransitionCancel"),
        transitionend: makePrefixMap("Transition", "TransitionEnd")
      };
      var prefixedEventNames = {};
      var style = {};
      canUseDOM2 && (style = document.createElement("div").style, "AnimationEvent" in window || (delete vendorPrefixes.animationend.animation, delete vendorPrefixes.animationiteration.animation, delete vendorPrefixes.animationstart.animation), "TransitionEvent" in window || delete vendorPrefixes.transitionend.transition);
      function getVendorPrefixedEventName(eventName) {
        if (prefixedEventNames[eventName]) return prefixedEventNames[eventName];
        if (!vendorPrefixes[eventName]) return eventName;
        var prefixMap = vendorPrefixes[eventName], styleProp;
        for (styleProp in prefixMap)
          if (prefixMap.hasOwnProperty(styleProp) && styleProp in style)
            return prefixedEventNames[eventName] = prefixMap[styleProp];
        return eventName;
      }
      var ANIMATION_END = getVendorPrefixedEventName("animationend");
      var ANIMATION_ITERATION = getVendorPrefixedEventName("animationiteration");
      var ANIMATION_START = getVendorPrefixedEventName("animationstart");
      var TRANSITION_RUN = getVendorPrefixedEventName("transitionrun");
      var TRANSITION_START = getVendorPrefixedEventName("transitionstart");
      var TRANSITION_CANCEL = getVendorPrefixedEventName("transitioncancel");
      var TRANSITION_END = getVendorPrefixedEventName("transitionend");
      var topLevelEventsToReactNames = /* @__PURE__ */ new Map();
      var simpleEventPluginEvents = "abort auxClick beforeToggle cancel canPlay canPlayThrough click close contextMenu copy cut drag dragEnd dragEnter dragExit dragLeave dragOver dragStart drop durationChange emptied encrypted ended error gotPointerCapture input invalid keyDown keyPress keyUp load loadedData loadedMetadata loadStart lostPointerCapture mouseDown mouseMove mouseOut mouseOver mouseUp paste pause play playing pointerCancel pointerDown pointerMove pointerOut pointerOver pointerUp progress rateChange reset resize seeked seeking stalled submit suspend timeUpdate touchCancel touchEnd touchStart volumeChange scroll toggle touchMove waiting wheel".split(
        " "
      );
      simpleEventPluginEvents.push("scrollEnd");
      function registerSimpleEvent(domEventName, reactName) {
        topLevelEventsToReactNames.set(domEventName, reactName);
        registerTwoPhaseEvent(reactName, [domEventName]);
      }
      var reportGlobalError = "function" === typeof reportError ? reportError : function(error) {
        if ("object" === typeof window && "function" === typeof window.ErrorEvent) {
          var event = new window.ErrorEvent("error", {
            bubbles: true,
            cancelable: true,
            message: "object" === typeof error && null !== error && "string" === typeof error.message ? String(error.message) : String(error),
            error
          });
          if (!window.dispatchEvent(event)) return;
        } else if ("object" === typeof process && "function" === typeof process.emit) {
          process.emit("uncaughtException", error);
          return;
        }
        console.error(error);
      };
      var concurrentQueues = [];
      var concurrentQueuesIndex = 0;
      var concurrentlyUpdatedLanes = 0;
      function finishQueueingConcurrentUpdates() {
        for (var endIndex = concurrentQueuesIndex, i = concurrentlyUpdatedLanes = concurrentQueuesIndex = 0; i < endIndex; ) {
          var fiber = concurrentQueues[i];
          concurrentQueues[i++] = null;
          var queue = concurrentQueues[i];
          concurrentQueues[i++] = null;
          var update = concurrentQueues[i];
          concurrentQueues[i++] = null;
          var lane = concurrentQueues[i];
          concurrentQueues[i++] = null;
          if (null !== queue && null !== update) {
            var pending = queue.pending;
            null === pending ? update.next = update : (update.next = pending.next, pending.next = update);
            queue.pending = update;
          }
          0 !== lane && markUpdateLaneFromFiberToRoot(fiber, update, lane);
        }
      }
      function enqueueUpdate$1(fiber, queue, update, lane) {
        concurrentQueues[concurrentQueuesIndex++] = fiber;
        concurrentQueues[concurrentQueuesIndex++] = queue;
        concurrentQueues[concurrentQueuesIndex++] = update;
        concurrentQueues[concurrentQueuesIndex++] = lane;
        concurrentlyUpdatedLanes |= lane;
        fiber.lanes |= lane;
        fiber = fiber.alternate;
        null !== fiber && (fiber.lanes |= lane);
      }
      function enqueueConcurrentHookUpdate(fiber, queue, update, lane) {
        enqueueUpdate$1(fiber, queue, update, lane);
        return getRootForUpdatedFiber(fiber);
      }
      function enqueueConcurrentRenderForLane(fiber, lane) {
        enqueueUpdate$1(fiber, null, null, lane);
        return getRootForUpdatedFiber(fiber);
      }
      function markUpdateLaneFromFiberToRoot(sourceFiber, update, lane) {
        sourceFiber.lanes |= lane;
        var alternate = sourceFiber.alternate;
        null !== alternate && (alternate.lanes |= lane);
        for (var isHidden2 = false, parent = sourceFiber.return; null !== parent; )
          parent.childLanes |= lane, alternate = parent.alternate, null !== alternate && (alternate.childLanes |= lane), 22 === parent.tag && (sourceFiber = parent.stateNode, null === sourceFiber || sourceFiber._visibility & 1 || (isHidden2 = true)), sourceFiber = parent, parent = parent.return;
        return 3 === sourceFiber.tag ? (parent = sourceFiber.stateNode, isHidden2 && null !== update && (isHidden2 = 31 - clz32(lane), sourceFiber = parent.hiddenUpdates, alternate = sourceFiber[isHidden2], null === alternate ? sourceFiber[isHidden2] = [update] : alternate.push(update), update.lane = lane | 536870912), parent) : null;
      }
      function getRootForUpdatedFiber(sourceFiber) {
        if (50 < nestedUpdateCount)
          throw nestedUpdateCount = 0, rootWithNestedUpdates = null, Error(formatProdErrorMessage(185));
        for (var parent = sourceFiber.return; null !== parent; )
          sourceFiber = parent, parent = sourceFiber.return;
        return 3 === sourceFiber.tag ? sourceFiber.stateNode : null;
      }
      var emptyContextObject = {};
      function FiberNode(tag, pendingProps, key, mode) {
        this.tag = tag;
        this.key = key;
        this.sibling = this.child = this.return = this.stateNode = this.type = this.elementType = null;
        this.index = 0;
        this.refCleanup = this.ref = null;
        this.pendingProps = pendingProps;
        this.dependencies = this.memoizedState = this.updateQueue = this.memoizedProps = null;
        this.mode = mode;
        this.subtreeFlags = this.flags = 0;
        this.deletions = null;
        this.childLanes = this.lanes = 0;
        this.alternate = null;
      }
      function createFiberImplClass(tag, pendingProps, key, mode) {
        return new FiberNode(tag, pendingProps, key, mode);
      }
      function shouldConstruct(Component) {
        Component = Component.prototype;
        return !(!Component || !Component.isReactComponent);
      }
      function createWorkInProgress(current, pendingProps) {
        var workInProgress2 = current.alternate;
        null === workInProgress2 ? (workInProgress2 = createFiberImplClass(
          current.tag,
          pendingProps,
          current.key,
          current.mode
        ), workInProgress2.elementType = current.elementType, workInProgress2.type = current.type, workInProgress2.stateNode = current.stateNode, workInProgress2.alternate = current, current.alternate = workInProgress2) : (workInProgress2.pendingProps = pendingProps, workInProgress2.type = current.type, workInProgress2.flags = 0, workInProgress2.subtreeFlags = 0, workInProgress2.deletions = null);
        workInProgress2.flags = current.flags & 65011712;
        workInProgress2.childLanes = current.childLanes;
        workInProgress2.lanes = current.lanes;
        workInProgress2.child = current.child;
        workInProgress2.memoizedProps = current.memoizedProps;
        workInProgress2.memoizedState = current.memoizedState;
        workInProgress2.updateQueue = current.updateQueue;
        pendingProps = current.dependencies;
        workInProgress2.dependencies = null === pendingProps ? null : { lanes: pendingProps.lanes, firstContext: pendingProps.firstContext };
        workInProgress2.sibling = current.sibling;
        workInProgress2.index = current.index;
        workInProgress2.ref = current.ref;
        workInProgress2.refCleanup = current.refCleanup;
        return workInProgress2;
      }
      function resetWorkInProgress(workInProgress2, renderLanes2) {
        workInProgress2.flags &= 65011714;
        var current = workInProgress2.alternate;
        null === current ? (workInProgress2.childLanes = 0, workInProgress2.lanes = renderLanes2, workInProgress2.child = null, workInProgress2.subtreeFlags = 0, workInProgress2.memoizedProps = null, workInProgress2.memoizedState = null, workInProgress2.updateQueue = null, workInProgress2.dependencies = null, workInProgress2.stateNode = null) : (workInProgress2.childLanes = current.childLanes, workInProgress2.lanes = current.lanes, workInProgress2.child = current.child, workInProgress2.subtreeFlags = 0, workInProgress2.deletions = null, workInProgress2.memoizedProps = current.memoizedProps, workInProgress2.memoizedState = current.memoizedState, workInProgress2.updateQueue = current.updateQueue, workInProgress2.type = current.type, renderLanes2 = current.dependencies, workInProgress2.dependencies = null === renderLanes2 ? null : {
          lanes: renderLanes2.lanes,
          firstContext: renderLanes2.firstContext
        });
        return workInProgress2;
      }
      function createFiberFromTypeAndProps(type, key, pendingProps, owner, mode, lanes) {
        var fiberTag = 0;
        owner = type;
        if ("function" === typeof type) shouldConstruct(type) && (fiberTag = 1);
        else if ("string" === typeof type)
          fiberTag = isHostHoistableType(
            type,
            pendingProps,
            contextStackCursor.current
          ) ? 26 : "html" === type || "head" === type || "body" === type ? 27 : 5;
        else
          a: switch (type) {
            case REACT_ACTIVITY_TYPE:
              return type = createFiberImplClass(31, pendingProps, key, mode), type.elementType = REACT_ACTIVITY_TYPE, type.lanes = lanes, type;
            case REACT_FRAGMENT_TYPE:
              return createFiberFromFragment(pendingProps.children, mode, lanes, key);
            case REACT_STRICT_MODE_TYPE:
              fiberTag = 8;
              mode |= 24;
              break;
            case REACT_PROFILER_TYPE:
              return type = createFiberImplClass(12, pendingProps, key, mode | 2), type.elementType = REACT_PROFILER_TYPE, type.lanes = lanes, type;
            case REACT_SUSPENSE_TYPE:
              return type = createFiberImplClass(13, pendingProps, key, mode), type.elementType = REACT_SUSPENSE_TYPE, type.lanes = lanes, type;
            case REACT_SUSPENSE_LIST_TYPE:
              return type = createFiberImplClass(19, pendingProps, key, mode), type.elementType = REACT_SUSPENSE_LIST_TYPE, type.lanes = lanes, type;
            default:
              if ("object" === typeof type && null !== type)
                switch (type.$$typeof) {
                  case REACT_CONTEXT_TYPE:
                    fiberTag = 10;
                    break a;
                  case REACT_CONSUMER_TYPE:
                    fiberTag = 9;
                    break a;
                  case REACT_FORWARD_REF_TYPE:
                    fiberTag = 11;
                    break a;
                  case REACT_MEMO_TYPE:
                    fiberTag = 14;
                    break a;
                  case REACT_LAZY_TYPE2:
                    fiberTag = 16;
                    owner = null;
                    break a;
                }
              fiberTag = 29;
              pendingProps = Error(
                formatProdErrorMessage(130, null === type ? "null" : typeof type, "")
              );
              owner = null;
          }
        key = createFiberImplClass(fiberTag, pendingProps, key, mode);
        key.elementType = type;
        key.type = owner;
        key.lanes = lanes;
        return key;
      }
      function createFiberFromFragment(elements, mode, lanes, key) {
        elements = createFiberImplClass(7, elements, key, mode);
        elements.lanes = lanes;
        return elements;
      }
      function createFiberFromText(content, mode, lanes) {
        content = createFiberImplClass(6, content, null, mode);
        content.lanes = lanes;
        return content;
      }
      function createFiberFromDehydratedFragment(dehydratedNode) {
        var fiber = createFiberImplClass(18, null, null, 0);
        fiber.stateNode = dehydratedNode;
        return fiber;
      }
      function createFiberFromPortal(portal, mode, lanes) {
        mode = createFiberImplClass(
          4,
          null !== portal.children ? portal.children : [],
          portal.key,
          mode
        );
        mode.lanes = lanes;
        mode.stateNode = {
          containerInfo: portal.containerInfo,
          pendingChildren: null,
          implementation: portal.implementation
        };
        return mode;
      }
      var CapturedStacks = /* @__PURE__ */ new WeakMap();
      function createCapturedValueAtFiber(value, source) {
        if ("object" === typeof value && null !== value) {
          var existing = CapturedStacks.get(value);
          if (void 0 !== existing) return existing;
          source = {
            value,
            source,
            stack: getStackByFiberInDevAndProd(source)
          };
          CapturedStacks.set(value, source);
          return source;
        }
        return {
          value,
          source,
          stack: getStackByFiberInDevAndProd(source)
        };
      }
      var forkStack = [];
      var forkStackIndex = 0;
      var treeForkProvider = null;
      var treeForkCount = 0;
      var idStack = [];
      var idStackIndex = 0;
      var treeContextProvider = null;
      var treeContextId = 1;
      var treeContextOverflow = "";
      function pushTreeFork(workInProgress2, totalChildren) {
        forkStack[forkStackIndex++] = treeForkCount;
        forkStack[forkStackIndex++] = treeForkProvider;
        treeForkProvider = workInProgress2;
        treeForkCount = totalChildren;
      }
      function pushTreeId(workInProgress2, totalChildren, index3) {
        idStack[idStackIndex++] = treeContextId;
        idStack[idStackIndex++] = treeContextOverflow;
        idStack[idStackIndex++] = treeContextProvider;
        treeContextProvider = workInProgress2;
        var baseIdWithLeadingBit = treeContextId;
        workInProgress2 = treeContextOverflow;
        var baseLength = 32 - clz32(baseIdWithLeadingBit) - 1;
        baseIdWithLeadingBit &= ~(1 << baseLength);
        index3 += 1;
        var length = 32 - clz32(totalChildren) + baseLength;
        if (30 < length) {
          var numberOfOverflowBits = baseLength - baseLength % 5;
          length = (baseIdWithLeadingBit & (1 << numberOfOverflowBits) - 1).toString(32);
          baseIdWithLeadingBit >>= numberOfOverflowBits;
          baseLength -= numberOfOverflowBits;
          treeContextId = 1 << 32 - clz32(totalChildren) + baseLength | index3 << baseLength | baseIdWithLeadingBit;
          treeContextOverflow = length + workInProgress2;
        } else
          treeContextId = 1 << length | index3 << baseLength | baseIdWithLeadingBit, treeContextOverflow = workInProgress2;
      }
      function pushMaterializedTreeId(workInProgress2) {
        null !== workInProgress2.return && (pushTreeFork(workInProgress2, 1), pushTreeId(workInProgress2, 1, 0));
      }
      function popTreeContext(workInProgress2) {
        for (; workInProgress2 === treeForkProvider; )
          treeForkProvider = forkStack[--forkStackIndex], forkStack[forkStackIndex] = null, treeForkCount = forkStack[--forkStackIndex], forkStack[forkStackIndex] = null;
        for (; workInProgress2 === treeContextProvider; )
          treeContextProvider = idStack[--idStackIndex], idStack[idStackIndex] = null, treeContextOverflow = idStack[--idStackIndex], idStack[idStackIndex] = null, treeContextId = idStack[--idStackIndex], idStack[idStackIndex] = null;
      }
      function restoreSuspendedTreeContext(workInProgress2, suspendedContext) {
        idStack[idStackIndex++] = treeContextId;
        idStack[idStackIndex++] = treeContextOverflow;
        idStack[idStackIndex++] = treeContextProvider;
        treeContextId = suspendedContext.id;
        treeContextOverflow = suspendedContext.overflow;
        treeContextProvider = workInProgress2;
      }
      var hydrationParentFiber = null;
      var nextHydratableInstance = null;
      var isHydrating = false;
      var hydrationErrors = null;
      var rootOrSingletonContext = false;
      var HydrationMismatchException = Error(formatProdErrorMessage(519));
      function throwOnHydrationMismatch(fiber) {
        var error = Error(
          formatProdErrorMessage(
            418,
            1 < arguments.length && void 0 !== arguments[1] && arguments[1] ? "text" : "HTML",
            ""
          )
        );
        queueHydrationError(createCapturedValueAtFiber(error, fiber));
        throw HydrationMismatchException;
      }
      function prepareToHydrateHostInstance(fiber) {
        var instance = fiber.stateNode, type = fiber.type, props = fiber.memoizedProps;
        instance[internalInstanceKey] = fiber;
        instance[internalPropsKey] = props;
        switch (type) {
          case "dialog":
            listenToNonDelegatedEvent("cancel", instance);
            listenToNonDelegatedEvent("close", instance);
            break;
          case "iframe":
          case "object":
          case "embed":
            listenToNonDelegatedEvent("load", instance);
            break;
          case "video":
          case "audio":
            for (type = 0; type < mediaEventTypes.length; type++)
              listenToNonDelegatedEvent(mediaEventTypes[type], instance);
            break;
          case "source":
            listenToNonDelegatedEvent("error", instance);
            break;
          case "img":
          case "image":
          case "link":
            listenToNonDelegatedEvent("error", instance);
            listenToNonDelegatedEvent("load", instance);
            break;
          case "details":
            listenToNonDelegatedEvent("toggle", instance);
            break;
          case "input":
            listenToNonDelegatedEvent("invalid", instance);
            initInput(
              instance,
              props.value,
              props.defaultValue,
              props.checked,
              props.defaultChecked,
              props.type,
              props.name,
              true
            );
            break;
          case "select":
            listenToNonDelegatedEvent("invalid", instance);
            break;
          case "textarea":
            listenToNonDelegatedEvent("invalid", instance), initTextarea(instance, props.value, props.defaultValue, props.children);
        }
        type = props.children;
        "string" !== typeof type && "number" !== typeof type && "bigint" !== typeof type || instance.textContent === "" + type || true === props.suppressHydrationWarning || checkForUnmatchedText(instance.textContent, type) ? (null != props.popover && (listenToNonDelegatedEvent("beforetoggle", instance), listenToNonDelegatedEvent("toggle", instance)), null != props.onScroll && listenToNonDelegatedEvent("scroll", instance), null != props.onScrollEnd && listenToNonDelegatedEvent("scrollend", instance), null != props.onClick && (instance.onclick = noop$1), instance = true) : instance = false;
        instance || throwOnHydrationMismatch(fiber, true);
      }
      function popToNextHostParent(fiber) {
        for (hydrationParentFiber = fiber.return; hydrationParentFiber; )
          switch (hydrationParentFiber.tag) {
            case 5:
            case 31:
            case 13:
              rootOrSingletonContext = false;
              return;
            case 27:
            case 3:
              rootOrSingletonContext = true;
              return;
            default:
              hydrationParentFiber = hydrationParentFiber.return;
          }
      }
      function popHydrationState(fiber) {
        if (fiber !== hydrationParentFiber) return false;
        if (!isHydrating) return popToNextHostParent(fiber), isHydrating = true, false;
        var tag = fiber.tag, JSCompiler_temp;
        if (JSCompiler_temp = 3 !== tag && 27 !== tag) {
          if (JSCompiler_temp = 5 === tag)
            JSCompiler_temp = fiber.type, JSCompiler_temp = !("form" !== JSCompiler_temp && "button" !== JSCompiler_temp) || shouldSetTextContent(fiber.type, fiber.memoizedProps);
          JSCompiler_temp = !JSCompiler_temp;
        }
        JSCompiler_temp && nextHydratableInstance && throwOnHydrationMismatch(fiber);
        popToNextHostParent(fiber);
        if (13 === tag) {
          fiber = fiber.memoizedState;
          fiber = null !== fiber ? fiber.dehydrated : null;
          if (!fiber) throw Error(formatProdErrorMessage(317));
          nextHydratableInstance = getNextHydratableInstanceAfterHydrationBoundary(fiber);
        } else if (31 === tag) {
          fiber = fiber.memoizedState;
          fiber = null !== fiber ? fiber.dehydrated : null;
          if (!fiber) throw Error(formatProdErrorMessage(317));
          nextHydratableInstance = getNextHydratableInstanceAfterHydrationBoundary(fiber);
        } else
          27 === tag ? (tag = nextHydratableInstance, isSingletonScope(fiber.type) ? (fiber = previousHydratableOnEnteringScopedSingleton, previousHydratableOnEnteringScopedSingleton = null, nextHydratableInstance = fiber) : nextHydratableInstance = tag) : nextHydratableInstance = hydrationParentFiber ? getNextHydratable(fiber.stateNode.nextSibling) : null;
        return true;
      }
      function resetHydrationState() {
        nextHydratableInstance = hydrationParentFiber = null;
        isHydrating = false;
      }
      function upgradeHydrationErrorsToRecoverable() {
        var queuedErrors = hydrationErrors;
        null !== queuedErrors && (null === workInProgressRootRecoverableErrors ? workInProgressRootRecoverableErrors = queuedErrors : workInProgressRootRecoverableErrors.push.apply(
          workInProgressRootRecoverableErrors,
          queuedErrors
        ), hydrationErrors = null);
        return queuedErrors;
      }
      function queueHydrationError(error) {
        null === hydrationErrors ? hydrationErrors = [error] : hydrationErrors.push(error);
      }
      var valueCursor = createCursor(null);
      var currentlyRenderingFiber$1 = null;
      var lastContextDependency = null;
      function pushProvider(providerFiber, context, nextValue) {
        push(valueCursor, context._currentValue);
        context._currentValue = nextValue;
      }
      function popProvider(context) {
        context._currentValue = valueCursor.current;
        pop(valueCursor);
      }
      function scheduleContextWorkOnParentPath(parent, renderLanes2, propagationRoot) {
        for (; null !== parent; ) {
          var alternate = parent.alternate;
          (parent.childLanes & renderLanes2) !== renderLanes2 ? (parent.childLanes |= renderLanes2, null !== alternate && (alternate.childLanes |= renderLanes2)) : null !== alternate && (alternate.childLanes & renderLanes2) !== renderLanes2 && (alternate.childLanes |= renderLanes2);
          if (parent === propagationRoot) break;
          parent = parent.return;
        }
      }
      function propagateContextChanges(workInProgress2, contexts, renderLanes2, forcePropagateEntireTree) {
        var fiber = workInProgress2.child;
        null !== fiber && (fiber.return = workInProgress2);
        for (; null !== fiber; ) {
          var list = fiber.dependencies;
          if (null !== list) {
            var nextFiber = fiber.child;
            list = list.firstContext;
            a: for (; null !== list; ) {
              var dependency = list;
              list = fiber;
              for (var i = 0; i < contexts.length; i++)
                if (dependency.context === contexts[i]) {
                  list.lanes |= renderLanes2;
                  dependency = list.alternate;
                  null !== dependency && (dependency.lanes |= renderLanes2);
                  scheduleContextWorkOnParentPath(
                    list.return,
                    renderLanes2,
                    workInProgress2
                  );
                  forcePropagateEntireTree || (nextFiber = null);
                  break a;
                }
              list = dependency.next;
            }
          } else if (18 === fiber.tag) {
            nextFiber = fiber.return;
            if (null === nextFiber) throw Error(formatProdErrorMessage(341));
            nextFiber.lanes |= renderLanes2;
            list = nextFiber.alternate;
            null !== list && (list.lanes |= renderLanes2);
            scheduleContextWorkOnParentPath(nextFiber, renderLanes2, workInProgress2);
            nextFiber = null;
          } else nextFiber = fiber.child;
          if (null !== nextFiber) nextFiber.return = fiber;
          else
            for (nextFiber = fiber; null !== nextFiber; ) {
              if (nextFiber === workInProgress2) {
                nextFiber = null;
                break;
              }
              fiber = nextFiber.sibling;
              if (null !== fiber) {
                fiber.return = nextFiber.return;
                nextFiber = fiber;
                break;
              }
              nextFiber = nextFiber.return;
            }
          fiber = nextFiber;
        }
      }
      function propagateParentContextChanges(current, workInProgress2, renderLanes2, forcePropagateEntireTree) {
        current = null;
        for (var parent = workInProgress2, isInsidePropagationBailout = false; null !== parent; ) {
          if (!isInsidePropagationBailout) {
            if (0 !== (parent.flags & 524288)) isInsidePropagationBailout = true;
            else if (0 !== (parent.flags & 262144)) break;
          }
          if (10 === parent.tag) {
            var currentParent = parent.alternate;
            if (null === currentParent) throw Error(formatProdErrorMessage(387));
            currentParent = currentParent.memoizedProps;
            if (null !== currentParent) {
              var context = parent.type;
              objectIs(parent.pendingProps.value, currentParent.value) || (null !== current ? current.push(context) : current = [context]);
            }
          } else if (parent === hostTransitionProviderCursor.current) {
            currentParent = parent.alternate;
            if (null === currentParent) throw Error(formatProdErrorMessage(387));
            currentParent.memoizedState.memoizedState !== parent.memoizedState.memoizedState && (null !== current ? current.push(HostTransitionContext) : current = [HostTransitionContext]);
          }
          parent = parent.return;
        }
        null !== current && propagateContextChanges(
          workInProgress2,
          current,
          renderLanes2,
          forcePropagateEntireTree
        );
        workInProgress2.flags |= 262144;
      }
      function checkIfContextChanged(currentDependencies) {
        for (currentDependencies = currentDependencies.firstContext; null !== currentDependencies; ) {
          if (!objectIs(
            currentDependencies.context._currentValue,
            currentDependencies.memoizedValue
          ))
            return true;
          currentDependencies = currentDependencies.next;
        }
        return false;
      }
      function prepareToReadContext(workInProgress2) {
        currentlyRenderingFiber$1 = workInProgress2;
        lastContextDependency = null;
        workInProgress2 = workInProgress2.dependencies;
        null !== workInProgress2 && (workInProgress2.firstContext = null);
      }
      function readContext(context) {
        return readContextForConsumer(currentlyRenderingFiber$1, context);
      }
      function readContextDuringReconciliation(consumer, context) {
        null === currentlyRenderingFiber$1 && prepareToReadContext(consumer);
        return readContextForConsumer(consumer, context);
      }
      function readContextForConsumer(consumer, context) {
        var value = context._currentValue;
        context = { context, memoizedValue: value, next: null };
        if (null === lastContextDependency) {
          if (null === consumer) throw Error(formatProdErrorMessage(308));
          lastContextDependency = context;
          consumer.dependencies = { lanes: 0, firstContext: context };
          consumer.flags |= 524288;
        } else lastContextDependency = lastContextDependency.next = context;
        return value;
      }
      var AbortControllerLocal = "undefined" !== typeof AbortController ? AbortController : function() {
        var listeners7 = [], signal = this.signal = {
          aborted: false,
          addEventListener: function(type, listener) {
            listeners7.push(listener);
          }
        };
        this.abort = function() {
          signal.aborted = true;
          listeners7.forEach(function(listener) {
            return listener();
          });
        };
      };
      var scheduleCallback$2 = Scheduler.unstable_scheduleCallback;
      var NormalPriority = Scheduler.unstable_NormalPriority;
      var CacheContext = {
        $$typeof: REACT_CONTEXT_TYPE,
        Consumer: null,
        Provider: null,
        _currentValue: null,
        _currentValue2: null,
        _threadCount: 0
      };
      function createCache() {
        return {
          controller: new AbortControllerLocal(),
          data: /* @__PURE__ */ new Map(),
          refCount: 0
        };
      }
      function releaseCache(cache) {
        cache.refCount--;
        0 === cache.refCount && scheduleCallback$2(NormalPriority, function() {
          cache.controller.abort();
        });
      }
      var currentEntangledListeners = null;
      var currentEntangledPendingCount = 0;
      var currentEntangledLane = 0;
      var currentEntangledActionThenable = null;
      function entangleAsyncAction(transition, thenable) {
        if (null === currentEntangledListeners) {
          var entangledListeners = currentEntangledListeners = [];
          currentEntangledPendingCount = 0;
          currentEntangledLane = requestTransitionLane();
          currentEntangledActionThenable = {
            status: "pending",
            value: void 0,
            then: function(resolve) {
              entangledListeners.push(resolve);
            }
          };
        }
        currentEntangledPendingCount++;
        thenable.then(pingEngtangledActionScope, pingEngtangledActionScope);
        return thenable;
      }
      function pingEngtangledActionScope() {
        if (0 === --currentEntangledPendingCount && null !== currentEntangledListeners) {
          null !== currentEntangledActionThenable && (currentEntangledActionThenable.status = "fulfilled");
          var listeners7 = currentEntangledListeners;
          currentEntangledListeners = null;
          currentEntangledLane = 0;
          currentEntangledActionThenable = null;
          for (var i = 0; i < listeners7.length; i++) (0, listeners7[i])();
        }
      }
      function chainThenableValue(thenable, result) {
        var listeners7 = [], thenableWithOverride = {
          status: "pending",
          value: null,
          reason: null,
          then: function(resolve) {
            listeners7.push(resolve);
          }
        };
        thenable.then(
          function() {
            thenableWithOverride.status = "fulfilled";
            thenableWithOverride.value = result;
            for (var i = 0; i < listeners7.length; i++) (0, listeners7[i])(result);
          },
          function(error) {
            thenableWithOverride.status = "rejected";
            thenableWithOverride.reason = error;
            for (error = 0; error < listeners7.length; error++)
              (0, listeners7[error])(void 0);
          }
        );
        return thenableWithOverride;
      }
      var prevOnStartTransitionFinish = ReactSharedInternals.S;
      ReactSharedInternals.S = function(transition, returnValue) {
        globalMostRecentTransitionTime = now();
        "object" === typeof returnValue && null !== returnValue && "function" === typeof returnValue.then && entangleAsyncAction(transition, returnValue);
        null !== prevOnStartTransitionFinish && prevOnStartTransitionFinish(transition, returnValue);
      };
      var resumedCache = createCursor(null);
      function peekCacheFromPool() {
        var cacheResumedFromPreviousRender = resumedCache.current;
        return null !== cacheResumedFromPreviousRender ? cacheResumedFromPreviousRender : workInProgressRoot.pooledCache;
      }
      function pushTransition(offscreenWorkInProgress, prevCachePool) {
        null === prevCachePool ? push(resumedCache, resumedCache.current) : push(resumedCache, prevCachePool.pool);
      }
      function getSuspendedCache() {
        var cacheFromPool = peekCacheFromPool();
        return null === cacheFromPool ? null : { parent: CacheContext._currentValue, pool: cacheFromPool };
      }
      var SuspenseException = Error(formatProdErrorMessage(460));
      var SuspenseyCommitException = Error(formatProdErrorMessage(474));
      var SuspenseActionException = Error(formatProdErrorMessage(542));
      var noopSuspenseyCommitThenable = { then: function() {
      } };
      function isThenableResolved(thenable) {
        thenable = thenable.status;
        return "fulfilled" === thenable || "rejected" === thenable;
      }
      function trackUsedThenable(thenableState2, thenable, index3) {
        index3 = thenableState2[index3];
        void 0 === index3 ? thenableState2.push(thenable) : index3 !== thenable && (thenable.then(noop$1, noop$1), thenable = index3);
        switch (thenable.status) {
          case "fulfilled":
            return thenable.value;
          case "rejected":
            throw thenableState2 = thenable.reason, checkIfUseWrappedInAsyncCatch(thenableState2), thenableState2;
          default:
            if ("string" === typeof thenable.status) thenable.then(noop$1, noop$1);
            else {
              thenableState2 = workInProgressRoot;
              if (null !== thenableState2 && 100 < thenableState2.shellSuspendCounter)
                throw Error(formatProdErrorMessage(482));
              thenableState2 = thenable;
              thenableState2.status = "pending";
              thenableState2.then(
                function(fulfilledValue) {
                  if ("pending" === thenable.status) {
                    var fulfilledThenable = thenable;
                    fulfilledThenable.status = "fulfilled";
                    fulfilledThenable.value = fulfilledValue;
                  }
                },
                function(error) {
                  if ("pending" === thenable.status) {
                    var rejectedThenable = thenable;
                    rejectedThenable.status = "rejected";
                    rejectedThenable.reason = error;
                  }
                }
              );
            }
            switch (thenable.status) {
              case "fulfilled":
                return thenable.value;
              case "rejected":
                throw thenableState2 = thenable.reason, checkIfUseWrappedInAsyncCatch(thenableState2), thenableState2;
            }
            suspendedThenable = thenable;
            throw SuspenseException;
        }
      }
      function resolveLazy(lazyType) {
        try {
          var init = lazyType._init;
          return init(lazyType._payload);
        } catch (x) {
          if (null !== x && "object" === typeof x && "function" === typeof x.then)
            throw suspendedThenable = x, SuspenseException;
          throw x;
        }
      }
      var suspendedThenable = null;
      function getSuspendedThenable() {
        if (null === suspendedThenable) throw Error(formatProdErrorMessage(459));
        var thenable = suspendedThenable;
        suspendedThenable = null;
        return thenable;
      }
      function checkIfUseWrappedInAsyncCatch(rejectedReason) {
        if (rejectedReason === SuspenseException || rejectedReason === SuspenseActionException)
          throw Error(formatProdErrorMessage(483));
      }
      var thenableState$1 = null;
      var thenableIndexCounter$1 = 0;
      function unwrapThenable(thenable) {
        var index3 = thenableIndexCounter$1;
        thenableIndexCounter$1 += 1;
        null === thenableState$1 && (thenableState$1 = []);
        return trackUsedThenable(thenableState$1, thenable, index3);
      }
      function coerceRef(workInProgress2, element) {
        element = element.props.ref;
        workInProgress2.ref = void 0 !== element ? element : null;
      }
      function throwOnInvalidObjectTypeImpl(returnFiber, newChild) {
        if (newChild.$$typeof === REACT_LEGACY_ELEMENT_TYPE)
          throw Error(formatProdErrorMessage(525));
        returnFiber = Object.prototype.toString.call(newChild);
        throw Error(
          formatProdErrorMessage(
            31,
            "[object Object]" === returnFiber ? "object with keys {" + Object.keys(newChild).join(", ") + "}" : returnFiber
          )
        );
      }
      function createChildReconciler(shouldTrackSideEffects) {
        function deleteChild(returnFiber, childToDelete) {
          if (shouldTrackSideEffects) {
            var deletions = returnFiber.deletions;
            null === deletions ? (returnFiber.deletions = [childToDelete], returnFiber.flags |= 16) : deletions.push(childToDelete);
          }
        }
        function deleteRemainingChildren(returnFiber, currentFirstChild) {
          if (!shouldTrackSideEffects) return null;
          for (; null !== currentFirstChild; )
            deleteChild(returnFiber, currentFirstChild), currentFirstChild = currentFirstChild.sibling;
          return null;
        }
        function mapRemainingChildren(currentFirstChild) {
          for (var existingChildren = /* @__PURE__ */ new Map(); null !== currentFirstChild; )
            null !== currentFirstChild.key ? existingChildren.set(currentFirstChild.key, currentFirstChild) : existingChildren.set(currentFirstChild.index, currentFirstChild), currentFirstChild = currentFirstChild.sibling;
          return existingChildren;
        }
        function useFiber(fiber, pendingProps) {
          fiber = createWorkInProgress(fiber, pendingProps);
          fiber.index = 0;
          fiber.sibling = null;
          return fiber;
        }
        function placeChild(newFiber, lastPlacedIndex, newIndex) {
          newFiber.index = newIndex;
          if (!shouldTrackSideEffects)
            return newFiber.flags |= 1048576, lastPlacedIndex;
          newIndex = newFiber.alternate;
          if (null !== newIndex)
            return newIndex = newIndex.index, newIndex < lastPlacedIndex ? (newFiber.flags |= 67108866, lastPlacedIndex) : newIndex;
          newFiber.flags |= 67108866;
          return lastPlacedIndex;
        }
        function placeSingleChild(newFiber) {
          shouldTrackSideEffects && null === newFiber.alternate && (newFiber.flags |= 67108866);
          return newFiber;
        }
        function updateTextNode(returnFiber, current, textContent, lanes) {
          if (null === current || 6 !== current.tag)
            return current = createFiberFromText(textContent, returnFiber.mode, lanes), current.return = returnFiber, current;
          current = useFiber(current, textContent);
          current.return = returnFiber;
          return current;
        }
        function updateElement(returnFiber, current, element, lanes) {
          var elementType = element.type;
          if (elementType === REACT_FRAGMENT_TYPE)
            return updateFragment(
              returnFiber,
              current,
              element.props.children,
              lanes,
              element.key
            );
          if (null !== current && (current.elementType === elementType || "object" === typeof elementType && null !== elementType && elementType.$$typeof === REACT_LAZY_TYPE2 && resolveLazy(elementType) === current.type))
            return current = useFiber(current, element.props), coerceRef(current, element), current.return = returnFiber, current;
          current = createFiberFromTypeAndProps(
            element.type,
            element.key,
            element.props,
            null,
            returnFiber.mode,
            lanes
          );
          coerceRef(current, element);
          current.return = returnFiber;
          return current;
        }
        function updatePortal(returnFiber, current, portal, lanes) {
          if (null === current || 4 !== current.tag || current.stateNode.containerInfo !== portal.containerInfo || current.stateNode.implementation !== portal.implementation)
            return current = createFiberFromPortal(portal, returnFiber.mode, lanes), current.return = returnFiber, current;
          current = useFiber(current, portal.children || []);
          current.return = returnFiber;
          return current;
        }
        function updateFragment(returnFiber, current, fragment, lanes, key) {
          if (null === current || 7 !== current.tag)
            return current = createFiberFromFragment(
              fragment,
              returnFiber.mode,
              lanes,
              key
            ), current.return = returnFiber, current;
          current = useFiber(current, fragment);
          current.return = returnFiber;
          return current;
        }
        function createChild(returnFiber, newChild, lanes) {
          if ("string" === typeof newChild && "" !== newChild || "number" === typeof newChild || "bigint" === typeof newChild)
            return newChild = createFiberFromText(
              "" + newChild,
              returnFiber.mode,
              lanes
            ), newChild.return = returnFiber, newChild;
          if ("object" === typeof newChild && null !== newChild) {
            switch (newChild.$$typeof) {
              case REACT_ELEMENT_TYPE:
                return lanes = createFiberFromTypeAndProps(
                  newChild.type,
                  newChild.key,
                  newChild.props,
                  null,
                  returnFiber.mode,
                  lanes
                ), coerceRef(lanes, newChild), lanes.return = returnFiber, lanes;
              case REACT_PORTAL_TYPE:
                return newChild = createFiberFromPortal(
                  newChild,
                  returnFiber.mode,
                  lanes
                ), newChild.return = returnFiber, newChild;
              case REACT_LAZY_TYPE2:
                return newChild = resolveLazy(newChild), createChild(returnFiber, newChild, lanes);
            }
            if (isArrayImpl(newChild) || getIteratorFn(newChild))
              return newChild = createFiberFromFragment(
                newChild,
                returnFiber.mode,
                lanes,
                null
              ), newChild.return = returnFiber, newChild;
            if ("function" === typeof newChild.then)
              return createChild(returnFiber, unwrapThenable(newChild), lanes);
            if (newChild.$$typeof === REACT_CONTEXT_TYPE)
              return createChild(
                returnFiber,
                readContextDuringReconciliation(returnFiber, newChild),
                lanes
              );
            throwOnInvalidObjectTypeImpl(returnFiber, newChild);
          }
          return null;
        }
        function updateSlot(returnFiber, oldFiber, newChild, lanes) {
          var key = null !== oldFiber ? oldFiber.key : null;
          if ("string" === typeof newChild && "" !== newChild || "number" === typeof newChild || "bigint" === typeof newChild)
            return null !== key ? null : updateTextNode(returnFiber, oldFiber, "" + newChild, lanes);
          if ("object" === typeof newChild && null !== newChild) {
            switch (newChild.$$typeof) {
              case REACT_ELEMENT_TYPE:
                return newChild.key === key ? updateElement(returnFiber, oldFiber, newChild, lanes) : null;
              case REACT_PORTAL_TYPE:
                return newChild.key === key ? updatePortal(returnFiber, oldFiber, newChild, lanes) : null;
              case REACT_LAZY_TYPE2:
                return newChild = resolveLazy(newChild), updateSlot(returnFiber, oldFiber, newChild, lanes);
            }
            if (isArrayImpl(newChild) || getIteratorFn(newChild))
              return null !== key ? null : updateFragment(returnFiber, oldFiber, newChild, lanes, null);
            if ("function" === typeof newChild.then)
              return updateSlot(
                returnFiber,
                oldFiber,
                unwrapThenable(newChild),
                lanes
              );
            if (newChild.$$typeof === REACT_CONTEXT_TYPE)
              return updateSlot(
                returnFiber,
                oldFiber,
                readContextDuringReconciliation(returnFiber, newChild),
                lanes
              );
            throwOnInvalidObjectTypeImpl(returnFiber, newChild);
          }
          return null;
        }
        function updateFromMap(existingChildren, returnFiber, newIdx, newChild, lanes) {
          if ("string" === typeof newChild && "" !== newChild || "number" === typeof newChild || "bigint" === typeof newChild)
            return existingChildren = existingChildren.get(newIdx) || null, updateTextNode(returnFiber, existingChildren, "" + newChild, lanes);
          if ("object" === typeof newChild && null !== newChild) {
            switch (newChild.$$typeof) {
              case REACT_ELEMENT_TYPE:
                return existingChildren = existingChildren.get(
                  null === newChild.key ? newIdx : newChild.key
                ) || null, updateElement(returnFiber, existingChildren, newChild, lanes);
              case REACT_PORTAL_TYPE:
                return existingChildren = existingChildren.get(
                  null === newChild.key ? newIdx : newChild.key
                ) || null, updatePortal(returnFiber, existingChildren, newChild, lanes);
              case REACT_LAZY_TYPE2:
                return newChild = resolveLazy(newChild), updateFromMap(
                  existingChildren,
                  returnFiber,
                  newIdx,
                  newChild,
                  lanes
                );
            }
            if (isArrayImpl(newChild) || getIteratorFn(newChild))
              return existingChildren = existingChildren.get(newIdx) || null, updateFragment(returnFiber, existingChildren, newChild, lanes, null);
            if ("function" === typeof newChild.then)
              return updateFromMap(
                existingChildren,
                returnFiber,
                newIdx,
                unwrapThenable(newChild),
                lanes
              );
            if (newChild.$$typeof === REACT_CONTEXT_TYPE)
              return updateFromMap(
                existingChildren,
                returnFiber,
                newIdx,
                readContextDuringReconciliation(returnFiber, newChild),
                lanes
              );
            throwOnInvalidObjectTypeImpl(returnFiber, newChild);
          }
          return null;
        }
        function reconcileChildrenArray(returnFiber, currentFirstChild, newChildren, lanes) {
          for (var resultingFirstChild = null, previousNewFiber = null, oldFiber = currentFirstChild, newIdx = currentFirstChild = 0, nextOldFiber = null; null !== oldFiber && newIdx < newChildren.length; newIdx++) {
            oldFiber.index > newIdx ? (nextOldFiber = oldFiber, oldFiber = null) : nextOldFiber = oldFiber.sibling;
            var newFiber = updateSlot(
              returnFiber,
              oldFiber,
              newChildren[newIdx],
              lanes
            );
            if (null === newFiber) {
              null === oldFiber && (oldFiber = nextOldFiber);
              break;
            }
            shouldTrackSideEffects && oldFiber && null === newFiber.alternate && deleteChild(returnFiber, oldFiber);
            currentFirstChild = placeChild(newFiber, currentFirstChild, newIdx);
            null === previousNewFiber ? resultingFirstChild = newFiber : previousNewFiber.sibling = newFiber;
            previousNewFiber = newFiber;
            oldFiber = nextOldFiber;
          }
          if (newIdx === newChildren.length)
            return deleteRemainingChildren(returnFiber, oldFiber), isHydrating && pushTreeFork(returnFiber, newIdx), resultingFirstChild;
          if (null === oldFiber) {
            for (; newIdx < newChildren.length; newIdx++)
              oldFiber = createChild(returnFiber, newChildren[newIdx], lanes), null !== oldFiber && (currentFirstChild = placeChild(
                oldFiber,
                currentFirstChild,
                newIdx
              ), null === previousNewFiber ? resultingFirstChild = oldFiber : previousNewFiber.sibling = oldFiber, previousNewFiber = oldFiber);
            isHydrating && pushTreeFork(returnFiber, newIdx);
            return resultingFirstChild;
          }
          for (oldFiber = mapRemainingChildren(oldFiber); newIdx < newChildren.length; newIdx++)
            nextOldFiber = updateFromMap(
              oldFiber,
              returnFiber,
              newIdx,
              newChildren[newIdx],
              lanes
            ), null !== nextOldFiber && (shouldTrackSideEffects && null !== nextOldFiber.alternate && oldFiber.delete(
              null === nextOldFiber.key ? newIdx : nextOldFiber.key
            ), currentFirstChild = placeChild(
              nextOldFiber,
              currentFirstChild,
              newIdx
            ), null === previousNewFiber ? resultingFirstChild = nextOldFiber : previousNewFiber.sibling = nextOldFiber, previousNewFiber = nextOldFiber);
          shouldTrackSideEffects && oldFiber.forEach(function(child) {
            return deleteChild(returnFiber, child);
          });
          isHydrating && pushTreeFork(returnFiber, newIdx);
          return resultingFirstChild;
        }
        function reconcileChildrenIterator(returnFiber, currentFirstChild, newChildren, lanes) {
          if (null == newChildren) throw Error(formatProdErrorMessage(151));
          for (var resultingFirstChild = null, previousNewFiber = null, oldFiber = currentFirstChild, newIdx = currentFirstChild = 0, nextOldFiber = null, step = newChildren.next(); null !== oldFiber && !step.done; newIdx++, step = newChildren.next()) {
            oldFiber.index > newIdx ? (nextOldFiber = oldFiber, oldFiber = null) : nextOldFiber = oldFiber.sibling;
            var newFiber = updateSlot(returnFiber, oldFiber, step.value, lanes);
            if (null === newFiber) {
              null === oldFiber && (oldFiber = nextOldFiber);
              break;
            }
            shouldTrackSideEffects && oldFiber && null === newFiber.alternate && deleteChild(returnFiber, oldFiber);
            currentFirstChild = placeChild(newFiber, currentFirstChild, newIdx);
            null === previousNewFiber ? resultingFirstChild = newFiber : previousNewFiber.sibling = newFiber;
            previousNewFiber = newFiber;
            oldFiber = nextOldFiber;
          }
          if (step.done)
            return deleteRemainingChildren(returnFiber, oldFiber), isHydrating && pushTreeFork(returnFiber, newIdx), resultingFirstChild;
          if (null === oldFiber) {
            for (; !step.done; newIdx++, step = newChildren.next())
              step = createChild(returnFiber, step.value, lanes), null !== step && (currentFirstChild = placeChild(step, currentFirstChild, newIdx), null === previousNewFiber ? resultingFirstChild = step : previousNewFiber.sibling = step, previousNewFiber = step);
            isHydrating && pushTreeFork(returnFiber, newIdx);
            return resultingFirstChild;
          }
          for (oldFiber = mapRemainingChildren(oldFiber); !step.done; newIdx++, step = newChildren.next())
            step = updateFromMap(oldFiber, returnFiber, newIdx, step.value, lanes), null !== step && (shouldTrackSideEffects && null !== step.alternate && oldFiber.delete(null === step.key ? newIdx : step.key), currentFirstChild = placeChild(step, currentFirstChild, newIdx), null === previousNewFiber ? resultingFirstChild = step : previousNewFiber.sibling = step, previousNewFiber = step);
          shouldTrackSideEffects && oldFiber.forEach(function(child) {
            return deleteChild(returnFiber, child);
          });
          isHydrating && pushTreeFork(returnFiber, newIdx);
          return resultingFirstChild;
        }
        function reconcileChildFibersImpl(returnFiber, currentFirstChild, newChild, lanes) {
          "object" === typeof newChild && null !== newChild && newChild.type === REACT_FRAGMENT_TYPE && null === newChild.key && (newChild = newChild.props.children);
          if ("object" === typeof newChild && null !== newChild) {
            switch (newChild.$$typeof) {
              case REACT_ELEMENT_TYPE:
                a: {
                  for (var key = newChild.key; null !== currentFirstChild; ) {
                    if (currentFirstChild.key === key) {
                      key = newChild.type;
                      if (key === REACT_FRAGMENT_TYPE) {
                        if (7 === currentFirstChild.tag) {
                          deleteRemainingChildren(
                            returnFiber,
                            currentFirstChild.sibling
                          );
                          lanes = useFiber(
                            currentFirstChild,
                            newChild.props.children
                          );
                          lanes.return = returnFiber;
                          returnFiber = lanes;
                          break a;
                        }
                      } else if (currentFirstChild.elementType === key || "object" === typeof key && null !== key && key.$$typeof === REACT_LAZY_TYPE2 && resolveLazy(key) === currentFirstChild.type) {
                        deleteRemainingChildren(
                          returnFiber,
                          currentFirstChild.sibling
                        );
                        lanes = useFiber(currentFirstChild, newChild.props);
                        coerceRef(lanes, newChild);
                        lanes.return = returnFiber;
                        returnFiber = lanes;
                        break a;
                      }
                      deleteRemainingChildren(returnFiber, currentFirstChild);
                      break;
                    } else deleteChild(returnFiber, currentFirstChild);
                    currentFirstChild = currentFirstChild.sibling;
                  }
                  newChild.type === REACT_FRAGMENT_TYPE ? (lanes = createFiberFromFragment(
                    newChild.props.children,
                    returnFiber.mode,
                    lanes,
                    newChild.key
                  ), lanes.return = returnFiber, returnFiber = lanes) : (lanes = createFiberFromTypeAndProps(
                    newChild.type,
                    newChild.key,
                    newChild.props,
                    null,
                    returnFiber.mode,
                    lanes
                  ), coerceRef(lanes, newChild), lanes.return = returnFiber, returnFiber = lanes);
                }
                return placeSingleChild(returnFiber);
              case REACT_PORTAL_TYPE:
                a: {
                  for (key = newChild.key; null !== currentFirstChild; ) {
                    if (currentFirstChild.key === key)
                      if (4 === currentFirstChild.tag && currentFirstChild.stateNode.containerInfo === newChild.containerInfo && currentFirstChild.stateNode.implementation === newChild.implementation) {
                        deleteRemainingChildren(
                          returnFiber,
                          currentFirstChild.sibling
                        );
                        lanes = useFiber(currentFirstChild, newChild.children || []);
                        lanes.return = returnFiber;
                        returnFiber = lanes;
                        break a;
                      } else {
                        deleteRemainingChildren(returnFiber, currentFirstChild);
                        break;
                      }
                    else deleteChild(returnFiber, currentFirstChild);
                    currentFirstChild = currentFirstChild.sibling;
                  }
                  lanes = createFiberFromPortal(newChild, returnFiber.mode, lanes);
                  lanes.return = returnFiber;
                  returnFiber = lanes;
                }
                return placeSingleChild(returnFiber);
              case REACT_LAZY_TYPE2:
                return newChild = resolveLazy(newChild), reconcileChildFibersImpl(
                  returnFiber,
                  currentFirstChild,
                  newChild,
                  lanes
                );
            }
            if (isArrayImpl(newChild))
              return reconcileChildrenArray(
                returnFiber,
                currentFirstChild,
                newChild,
                lanes
              );
            if (getIteratorFn(newChild)) {
              key = getIteratorFn(newChild);
              if ("function" !== typeof key) throw Error(formatProdErrorMessage(150));
              newChild = key.call(newChild);
              return reconcileChildrenIterator(
                returnFiber,
                currentFirstChild,
                newChild,
                lanes
              );
            }
            if ("function" === typeof newChild.then)
              return reconcileChildFibersImpl(
                returnFiber,
                currentFirstChild,
                unwrapThenable(newChild),
                lanes
              );
            if (newChild.$$typeof === REACT_CONTEXT_TYPE)
              return reconcileChildFibersImpl(
                returnFiber,
                currentFirstChild,
                readContextDuringReconciliation(returnFiber, newChild),
                lanes
              );
            throwOnInvalidObjectTypeImpl(returnFiber, newChild);
          }
          return "string" === typeof newChild && "" !== newChild || "number" === typeof newChild || "bigint" === typeof newChild ? (newChild = "" + newChild, null !== currentFirstChild && 6 === currentFirstChild.tag ? (deleteRemainingChildren(returnFiber, currentFirstChild.sibling), lanes = useFiber(currentFirstChild, newChild), lanes.return = returnFiber, returnFiber = lanes) : (deleteRemainingChildren(returnFiber, currentFirstChild), lanes = createFiberFromText(newChild, returnFiber.mode, lanes), lanes.return = returnFiber, returnFiber = lanes), placeSingleChild(returnFiber)) : deleteRemainingChildren(returnFiber, currentFirstChild);
        }
        return function(returnFiber, currentFirstChild, newChild, lanes) {
          try {
            thenableIndexCounter$1 = 0;
            var firstChildFiber = reconcileChildFibersImpl(
              returnFiber,
              currentFirstChild,
              newChild,
              lanes
            );
            thenableState$1 = null;
            return firstChildFiber;
          } catch (x) {
            if (x === SuspenseException || x === SuspenseActionException) throw x;
            var fiber = createFiberImplClass(29, x, null, returnFiber.mode);
            fiber.lanes = lanes;
            fiber.return = returnFiber;
            return fiber;
          } finally {
          }
        };
      }
      var reconcileChildFibers = createChildReconciler(true);
      var mountChildFibers = createChildReconciler(false);
      var hasForceUpdate = false;
      function initializeUpdateQueue(fiber) {
        fiber.updateQueue = {
          baseState: fiber.memoizedState,
          firstBaseUpdate: null,
          lastBaseUpdate: null,
          shared: { pending: null, lanes: 0, hiddenCallbacks: null },
          callbacks: null
        };
      }
      function cloneUpdateQueue(current, workInProgress2) {
        current = current.updateQueue;
        workInProgress2.updateQueue === current && (workInProgress2.updateQueue = {
          baseState: current.baseState,
          firstBaseUpdate: current.firstBaseUpdate,
          lastBaseUpdate: current.lastBaseUpdate,
          shared: current.shared,
          callbacks: null
        });
      }
      function createUpdate(lane) {
        return { lane, tag: 0, payload: null, callback: null, next: null };
      }
      function enqueueUpdate(fiber, update, lane) {
        var updateQueue = fiber.updateQueue;
        if (null === updateQueue) return null;
        updateQueue = updateQueue.shared;
        if (0 !== (executionContext & 2)) {
          var pending = updateQueue.pending;
          null === pending ? update.next = update : (update.next = pending.next, pending.next = update);
          updateQueue.pending = update;
          update = getRootForUpdatedFiber(fiber);
          markUpdateLaneFromFiberToRoot(fiber, null, lane);
          return update;
        }
        enqueueUpdate$1(fiber, updateQueue, update, lane);
        return getRootForUpdatedFiber(fiber);
      }
      function entangleTransitions(root2, fiber, lane) {
        fiber = fiber.updateQueue;
        if (null !== fiber && (fiber = fiber.shared, 0 !== (lane & 4194048))) {
          var queueLanes = fiber.lanes;
          queueLanes &= root2.pendingLanes;
          lane |= queueLanes;
          fiber.lanes = lane;
          markRootEntangled(root2, lane);
        }
      }
      function enqueueCapturedUpdate(workInProgress2, capturedUpdate) {
        var queue = workInProgress2.updateQueue, current = workInProgress2.alternate;
        if (null !== current && (current = current.updateQueue, queue === current)) {
          var newFirst = null, newLast = null;
          queue = queue.firstBaseUpdate;
          if (null !== queue) {
            do {
              var clone = {
                lane: queue.lane,
                tag: queue.tag,
                payload: queue.payload,
                callback: null,
                next: null
              };
              null === newLast ? newFirst = newLast = clone : newLast = newLast.next = clone;
              queue = queue.next;
            } while (null !== queue);
            null === newLast ? newFirst = newLast = capturedUpdate : newLast = newLast.next = capturedUpdate;
          } else newFirst = newLast = capturedUpdate;
          queue = {
            baseState: current.baseState,
            firstBaseUpdate: newFirst,
            lastBaseUpdate: newLast,
            shared: current.shared,
            callbacks: current.callbacks
          };
          workInProgress2.updateQueue = queue;
          return;
        }
        workInProgress2 = queue.lastBaseUpdate;
        null === workInProgress2 ? queue.firstBaseUpdate = capturedUpdate : workInProgress2.next = capturedUpdate;
        queue.lastBaseUpdate = capturedUpdate;
      }
      var didReadFromEntangledAsyncAction = false;
      function suspendIfUpdateReadFromEntangledAsyncAction() {
        if (didReadFromEntangledAsyncAction) {
          var entangledActionThenable = currentEntangledActionThenable;
          if (null !== entangledActionThenable) throw entangledActionThenable;
        }
      }
      function processUpdateQueue(workInProgress$jscomp$0, props, instance$jscomp$0, renderLanes2) {
        didReadFromEntangledAsyncAction = false;
        var queue = workInProgress$jscomp$0.updateQueue;
        hasForceUpdate = false;
        var firstBaseUpdate = queue.firstBaseUpdate, lastBaseUpdate = queue.lastBaseUpdate, pendingQueue = queue.shared.pending;
        if (null !== pendingQueue) {
          queue.shared.pending = null;
          var lastPendingUpdate = pendingQueue, firstPendingUpdate = lastPendingUpdate.next;
          lastPendingUpdate.next = null;
          null === lastBaseUpdate ? firstBaseUpdate = firstPendingUpdate : lastBaseUpdate.next = firstPendingUpdate;
          lastBaseUpdate = lastPendingUpdate;
          var current = workInProgress$jscomp$0.alternate;
          null !== current && (current = current.updateQueue, pendingQueue = current.lastBaseUpdate, pendingQueue !== lastBaseUpdate && (null === pendingQueue ? current.firstBaseUpdate = firstPendingUpdate : pendingQueue.next = firstPendingUpdate, current.lastBaseUpdate = lastPendingUpdate));
        }
        if (null !== firstBaseUpdate) {
          var newState = queue.baseState;
          lastBaseUpdate = 0;
          current = firstPendingUpdate = lastPendingUpdate = null;
          pendingQueue = firstBaseUpdate;
          do {
            var updateLane = pendingQueue.lane & -536870913, isHiddenUpdate = updateLane !== pendingQueue.lane;
            if (isHiddenUpdate ? (workInProgressRootRenderLanes & updateLane) === updateLane : (renderLanes2 & updateLane) === updateLane) {
              0 !== updateLane && updateLane === currentEntangledLane && (didReadFromEntangledAsyncAction = true);
              null !== current && (current = current.next = {
                lane: 0,
                tag: pendingQueue.tag,
                payload: pendingQueue.payload,
                callback: null,
                next: null
              });
              a: {
                var workInProgress2 = workInProgress$jscomp$0, update = pendingQueue;
                updateLane = props;
                var instance = instance$jscomp$0;
                switch (update.tag) {
                  case 1:
                    workInProgress2 = update.payload;
                    if ("function" === typeof workInProgress2) {
                      newState = workInProgress2.call(instance, newState, updateLane);
                      break a;
                    }
                    newState = workInProgress2;
                    break a;
                  case 3:
                    workInProgress2.flags = workInProgress2.flags & -65537 | 128;
                  case 0:
                    workInProgress2 = update.payload;
                    updateLane = "function" === typeof workInProgress2 ? workInProgress2.call(instance, newState, updateLane) : workInProgress2;
                    if (null === updateLane || void 0 === updateLane) break a;
                    newState = assign({}, newState, updateLane);
                    break a;
                  case 2:
                    hasForceUpdate = true;
                }
              }
              updateLane = pendingQueue.callback;
              null !== updateLane && (workInProgress$jscomp$0.flags |= 64, isHiddenUpdate && (workInProgress$jscomp$0.flags |= 8192), isHiddenUpdate = queue.callbacks, null === isHiddenUpdate ? queue.callbacks = [updateLane] : isHiddenUpdate.push(updateLane));
            } else
              isHiddenUpdate = {
                lane: updateLane,
                tag: pendingQueue.tag,
                payload: pendingQueue.payload,
                callback: pendingQueue.callback,
                next: null
              }, null === current ? (firstPendingUpdate = current = isHiddenUpdate, lastPendingUpdate = newState) : current = current.next = isHiddenUpdate, lastBaseUpdate |= updateLane;
            pendingQueue = pendingQueue.next;
            if (null === pendingQueue)
              if (pendingQueue = queue.shared.pending, null === pendingQueue)
                break;
              else
                isHiddenUpdate = pendingQueue, pendingQueue = isHiddenUpdate.next, isHiddenUpdate.next = null, queue.lastBaseUpdate = isHiddenUpdate, queue.shared.pending = null;
          } while (1);
          null === current && (lastPendingUpdate = newState);
          queue.baseState = lastPendingUpdate;
          queue.firstBaseUpdate = firstPendingUpdate;
          queue.lastBaseUpdate = current;
          null === firstBaseUpdate && (queue.shared.lanes = 0);
          workInProgressRootSkippedLanes |= lastBaseUpdate;
          workInProgress$jscomp$0.lanes = lastBaseUpdate;
          workInProgress$jscomp$0.memoizedState = newState;
        }
      }
      function callCallback(callback, context) {
        if ("function" !== typeof callback)
          throw Error(formatProdErrorMessage(191, callback));
        callback.call(context);
      }
      function commitCallbacks(updateQueue, context) {
        var callbacks = updateQueue.callbacks;
        if (null !== callbacks)
          for (updateQueue.callbacks = null, updateQueue = 0; updateQueue < callbacks.length; updateQueue++)
            callCallback(callbacks[updateQueue], context);
      }
      var currentTreeHiddenStackCursor = createCursor(null);
      var prevEntangledRenderLanesCursor = createCursor(0);
      function pushHiddenContext(fiber, context) {
        fiber = entangledRenderLanes;
        push(prevEntangledRenderLanesCursor, fiber);
        push(currentTreeHiddenStackCursor, context);
        entangledRenderLanes = fiber | context.baseLanes;
      }
      function reuseHiddenContextOnStack() {
        push(prevEntangledRenderLanesCursor, entangledRenderLanes);
        push(currentTreeHiddenStackCursor, currentTreeHiddenStackCursor.current);
      }
      function popHiddenContext() {
        entangledRenderLanes = prevEntangledRenderLanesCursor.current;
        pop(currentTreeHiddenStackCursor);
        pop(prevEntangledRenderLanesCursor);
      }
      var suspenseHandlerStackCursor = createCursor(null);
      var shellBoundary = null;
      function pushPrimaryTreeSuspenseHandler(handler) {
        var current = handler.alternate;
        push(suspenseStackCursor, suspenseStackCursor.current & 1);
        push(suspenseHandlerStackCursor, handler);
        null === shellBoundary && (null === current || null !== currentTreeHiddenStackCursor.current ? shellBoundary = handler : null !== current.memoizedState && (shellBoundary = handler));
      }
      function pushDehydratedActivitySuspenseHandler(fiber) {
        push(suspenseStackCursor, suspenseStackCursor.current);
        push(suspenseHandlerStackCursor, fiber);
        null === shellBoundary && (shellBoundary = fiber);
      }
      function pushOffscreenSuspenseHandler(fiber) {
        22 === fiber.tag ? (push(suspenseStackCursor, suspenseStackCursor.current), push(suspenseHandlerStackCursor, fiber), null === shellBoundary && (shellBoundary = fiber)) : reuseSuspenseHandlerOnStack(fiber);
      }
      function reuseSuspenseHandlerOnStack() {
        push(suspenseStackCursor, suspenseStackCursor.current);
        push(suspenseHandlerStackCursor, suspenseHandlerStackCursor.current);
      }
      function popSuspenseHandler(fiber) {
        pop(suspenseHandlerStackCursor);
        shellBoundary === fiber && (shellBoundary = null);
        pop(suspenseStackCursor);
      }
      var suspenseStackCursor = createCursor(0);
      function findFirstSuspended(row) {
        for (var node = row; null !== node; ) {
          if (13 === node.tag) {
            var state2 = node.memoizedState;
            if (null !== state2 && (state2 = state2.dehydrated, null === state2 || isSuspenseInstancePending(state2) || isSuspenseInstanceFallback(state2)))
              return node;
          } else if (19 === node.tag && ("forwards" === node.memoizedProps.revealOrder || "backwards" === node.memoizedProps.revealOrder || "unstable_legacy-backwards" === node.memoizedProps.revealOrder || "together" === node.memoizedProps.revealOrder)) {
            if (0 !== (node.flags & 128)) return node;
          } else if (null !== node.child) {
            node.child.return = node;
            node = node.child;
            continue;
          }
          if (node === row) break;
          for (; null === node.sibling; ) {
            if (null === node.return || node.return === row) return null;
            node = node.return;
          }
          node.sibling.return = node.return;
          node = node.sibling;
        }
        return null;
      }
      var renderLanes = 0;
      var currentlyRenderingFiber = null;
      var currentHook = null;
      var workInProgressHook = null;
      var didScheduleRenderPhaseUpdate = false;
      var didScheduleRenderPhaseUpdateDuringThisPass = false;
      var shouldDoubleInvokeUserFnsInHooksDEV = false;
      var localIdCounter = 0;
      var thenableIndexCounter = 0;
      var thenableState = null;
      var globalClientIdCounter = 0;
      function throwInvalidHookError() {
        throw Error(formatProdErrorMessage(321));
      }
      function areHookInputsEqual(nextDeps, prevDeps) {
        if (null === prevDeps) return false;
        for (var i = 0; i < prevDeps.length && i < nextDeps.length; i++)
          if (!objectIs(nextDeps[i], prevDeps[i])) return false;
        return true;
      }
      function renderWithHooks(current, workInProgress2, Component, props, secondArg, nextRenderLanes) {
        renderLanes = nextRenderLanes;
        currentlyRenderingFiber = workInProgress2;
        workInProgress2.memoizedState = null;
        workInProgress2.updateQueue = null;
        workInProgress2.lanes = 0;
        ReactSharedInternals.H = null === current || null === current.memoizedState ? HooksDispatcherOnMount : HooksDispatcherOnUpdate;
        shouldDoubleInvokeUserFnsInHooksDEV = false;
        nextRenderLanes = Component(props, secondArg);
        shouldDoubleInvokeUserFnsInHooksDEV = false;
        didScheduleRenderPhaseUpdateDuringThisPass && (nextRenderLanes = renderWithHooksAgain(
          workInProgress2,
          Component,
          props,
          secondArg
        ));
        finishRenderingHooks(current);
        return nextRenderLanes;
      }
      function finishRenderingHooks(current) {
        ReactSharedInternals.H = ContextOnlyDispatcher;
        var didRenderTooFewHooks = null !== currentHook && null !== currentHook.next;
        renderLanes = 0;
        workInProgressHook = currentHook = currentlyRenderingFiber = null;
        didScheduleRenderPhaseUpdate = false;
        thenableIndexCounter = 0;
        thenableState = null;
        if (didRenderTooFewHooks) throw Error(formatProdErrorMessage(300));
        null === current || didReceiveUpdate || (current = current.dependencies, null !== current && checkIfContextChanged(current) && (didReceiveUpdate = true));
      }
      function renderWithHooksAgain(workInProgress2, Component, props, secondArg) {
        currentlyRenderingFiber = workInProgress2;
        var numberOfReRenders = 0;
        do {
          didScheduleRenderPhaseUpdateDuringThisPass && (thenableState = null);
          thenableIndexCounter = 0;
          didScheduleRenderPhaseUpdateDuringThisPass = false;
          if (25 <= numberOfReRenders) throw Error(formatProdErrorMessage(301));
          numberOfReRenders += 1;
          workInProgressHook = currentHook = null;
          if (null != workInProgress2.updateQueue) {
            var children = workInProgress2.updateQueue;
            children.lastEffect = null;
            children.events = null;
            children.stores = null;
            null != children.memoCache && (children.memoCache.index = 0);
          }
          ReactSharedInternals.H = HooksDispatcherOnRerender;
          children = Component(props, secondArg);
        } while (didScheduleRenderPhaseUpdateDuringThisPass);
        return children;
      }
      function TransitionAwareHostComponent() {
        var dispatcher = ReactSharedInternals.H, maybeThenable = dispatcher.useState()[0];
        maybeThenable = "function" === typeof maybeThenable.then ? useThenable(maybeThenable) : maybeThenable;
        dispatcher = dispatcher.useState()[0];
        (null !== currentHook ? currentHook.memoizedState : null) !== dispatcher && (currentlyRenderingFiber.flags |= 1024);
        return maybeThenable;
      }
      function checkDidRenderIdHook() {
        var didRenderIdHook = 0 !== localIdCounter;
        localIdCounter = 0;
        return didRenderIdHook;
      }
      function bailoutHooks(current, workInProgress2, lanes) {
        workInProgress2.updateQueue = current.updateQueue;
        workInProgress2.flags &= -2053;
        current.lanes &= ~lanes;
      }
      function resetHooksOnUnwind(workInProgress2) {
        if (didScheduleRenderPhaseUpdate) {
          for (workInProgress2 = workInProgress2.memoizedState; null !== workInProgress2; ) {
            var queue = workInProgress2.queue;
            null !== queue && (queue.pending = null);
            workInProgress2 = workInProgress2.next;
          }
          didScheduleRenderPhaseUpdate = false;
        }
        renderLanes = 0;
        workInProgressHook = currentHook = currentlyRenderingFiber = null;
        didScheduleRenderPhaseUpdateDuringThisPass = false;
        thenableIndexCounter = localIdCounter = 0;
        thenableState = null;
      }
      function mountWorkInProgressHook() {
        var hook = {
          memoizedState: null,
          baseState: null,
          baseQueue: null,
          queue: null,
          next: null
        };
        null === workInProgressHook ? currentlyRenderingFiber.memoizedState = workInProgressHook = hook : workInProgressHook = workInProgressHook.next = hook;
        return workInProgressHook;
      }
      function updateWorkInProgressHook() {
        if (null === currentHook) {
          var nextCurrentHook = currentlyRenderingFiber.alternate;
          nextCurrentHook = null !== nextCurrentHook ? nextCurrentHook.memoizedState : null;
        } else nextCurrentHook = currentHook.next;
        var nextWorkInProgressHook = null === workInProgressHook ? currentlyRenderingFiber.memoizedState : workInProgressHook.next;
        if (null !== nextWorkInProgressHook)
          workInProgressHook = nextWorkInProgressHook, currentHook = nextCurrentHook;
        else {
          if (null === nextCurrentHook) {
            if (null === currentlyRenderingFiber.alternate)
              throw Error(formatProdErrorMessage(467));
            throw Error(formatProdErrorMessage(310));
          }
          currentHook = nextCurrentHook;
          nextCurrentHook = {
            memoizedState: currentHook.memoizedState,
            baseState: currentHook.baseState,
            baseQueue: currentHook.baseQueue,
            queue: currentHook.queue,
            next: null
          };
          null === workInProgressHook ? currentlyRenderingFiber.memoizedState = workInProgressHook = nextCurrentHook : workInProgressHook = workInProgressHook.next = nextCurrentHook;
        }
        return workInProgressHook;
      }
      function createFunctionComponentUpdateQueue() {
        return { lastEffect: null, events: null, stores: null, memoCache: null };
      }
      function useThenable(thenable) {
        var index3 = thenableIndexCounter;
        thenableIndexCounter += 1;
        null === thenableState && (thenableState = []);
        thenable = trackUsedThenable(thenableState, thenable, index3);
        index3 = currentlyRenderingFiber;
        null === (null === workInProgressHook ? index3.memoizedState : workInProgressHook.next) && (index3 = index3.alternate, ReactSharedInternals.H = null === index3 || null === index3.memoizedState ? HooksDispatcherOnMount : HooksDispatcherOnUpdate);
        return thenable;
      }
      function use2(usable) {
        if (null !== usable && "object" === typeof usable) {
          if ("function" === typeof usable.then) return useThenable(usable);
          if (usable.$$typeof === REACT_CONTEXT_TYPE) return readContext(usable);
        }
        throw Error(formatProdErrorMessage(438, String(usable)));
      }
      function useMemoCache(size4) {
        var memoCache = null, updateQueue = currentlyRenderingFiber.updateQueue;
        null !== updateQueue && (memoCache = updateQueue.memoCache);
        if (null == memoCache) {
          var current = currentlyRenderingFiber.alternate;
          null !== current && (current = current.updateQueue, null !== current && (current = current.memoCache, null != current && (memoCache = {
            data: current.data.map(function(array) {
              return array.slice();
            }),
            index: 0
          })));
        }
        null == memoCache && (memoCache = { data: [], index: 0 });
        null === updateQueue && (updateQueue = createFunctionComponentUpdateQueue(), currentlyRenderingFiber.updateQueue = updateQueue);
        updateQueue.memoCache = memoCache;
        updateQueue = memoCache.data[memoCache.index];
        if (void 0 === updateQueue)
          for (updateQueue = memoCache.data[memoCache.index] = Array(size4), current = 0; current < size4; current++)
            updateQueue[current] = REACT_MEMO_CACHE_SENTINEL;
        memoCache.index++;
        return updateQueue;
      }
      function basicStateReducer(state2, action) {
        return "function" === typeof action ? action(state2) : action;
      }
      function updateReducer(reducer) {
        var hook = updateWorkInProgressHook();
        return updateReducerImpl(hook, currentHook, reducer);
      }
      function updateReducerImpl(hook, current, reducer) {
        var queue = hook.queue;
        if (null === queue) throw Error(formatProdErrorMessage(311));
        queue.lastRenderedReducer = reducer;
        var baseQueue = hook.baseQueue, pendingQueue = queue.pending;
        if (null !== pendingQueue) {
          if (null !== baseQueue) {
            var baseFirst = baseQueue.next;
            baseQueue.next = pendingQueue.next;
            pendingQueue.next = baseFirst;
          }
          current.baseQueue = baseQueue = pendingQueue;
          queue.pending = null;
        }
        pendingQueue = hook.baseState;
        if (null === baseQueue) hook.memoizedState = pendingQueue;
        else {
          current = baseQueue.next;
          var newBaseQueueFirst = baseFirst = null, newBaseQueueLast = null, update = current, didReadFromEntangledAsyncAction$60 = false;
          do {
            var updateLane = update.lane & -536870913;
            if (updateLane !== update.lane ? (workInProgressRootRenderLanes & updateLane) === updateLane : (renderLanes & updateLane) === updateLane) {
              var revertLane = update.revertLane;
              if (0 === revertLane)
                null !== newBaseQueueLast && (newBaseQueueLast = newBaseQueueLast.next = {
                  lane: 0,
                  revertLane: 0,
                  gesture: null,
                  action: update.action,
                  hasEagerState: update.hasEagerState,
                  eagerState: update.eagerState,
                  next: null
                }), updateLane === currentEntangledLane && (didReadFromEntangledAsyncAction$60 = true);
              else if ((renderLanes & revertLane) === revertLane) {
                update = update.next;
                revertLane === currentEntangledLane && (didReadFromEntangledAsyncAction$60 = true);
                continue;
              } else
                updateLane = {
                  lane: 0,
                  revertLane: update.revertLane,
                  gesture: null,
                  action: update.action,
                  hasEagerState: update.hasEagerState,
                  eagerState: update.eagerState,
                  next: null
                }, null === newBaseQueueLast ? (newBaseQueueFirst = newBaseQueueLast = updateLane, baseFirst = pendingQueue) : newBaseQueueLast = newBaseQueueLast.next = updateLane, currentlyRenderingFiber.lanes |= revertLane, workInProgressRootSkippedLanes |= revertLane;
              updateLane = update.action;
              shouldDoubleInvokeUserFnsInHooksDEV && reducer(pendingQueue, updateLane);
              pendingQueue = update.hasEagerState ? update.eagerState : reducer(pendingQueue, updateLane);
            } else
              revertLane = {
                lane: updateLane,
                revertLane: update.revertLane,
                gesture: update.gesture,
                action: update.action,
                hasEagerState: update.hasEagerState,
                eagerState: update.eagerState,
                next: null
              }, null === newBaseQueueLast ? (newBaseQueueFirst = newBaseQueueLast = revertLane, baseFirst = pendingQueue) : newBaseQueueLast = newBaseQueueLast.next = revertLane, currentlyRenderingFiber.lanes |= updateLane, workInProgressRootSkippedLanes |= updateLane;
            update = update.next;
          } while (null !== update && update !== current);
          null === newBaseQueueLast ? baseFirst = pendingQueue : newBaseQueueLast.next = newBaseQueueFirst;
          if (!objectIs(pendingQueue, hook.memoizedState) && (didReceiveUpdate = true, didReadFromEntangledAsyncAction$60 && (reducer = currentEntangledActionThenable, null !== reducer)))
            throw reducer;
          hook.memoizedState = pendingQueue;
          hook.baseState = baseFirst;
          hook.baseQueue = newBaseQueueLast;
          queue.lastRenderedState = pendingQueue;
        }
        null === baseQueue && (queue.lanes = 0);
        return [hook.memoizedState, queue.dispatch];
      }
      function rerenderReducer(reducer) {
        var hook = updateWorkInProgressHook(), queue = hook.queue;
        if (null === queue) throw Error(formatProdErrorMessage(311));
        queue.lastRenderedReducer = reducer;
        var dispatch = queue.dispatch, lastRenderPhaseUpdate = queue.pending, newState = hook.memoizedState;
        if (null !== lastRenderPhaseUpdate) {
          queue.pending = null;
          var update = lastRenderPhaseUpdate = lastRenderPhaseUpdate.next;
          do
            newState = reducer(newState, update.action), update = update.next;
          while (update !== lastRenderPhaseUpdate);
          objectIs(newState, hook.memoizedState) || (didReceiveUpdate = true);
          hook.memoizedState = newState;
          null === hook.baseQueue && (hook.baseState = newState);
          queue.lastRenderedState = newState;
        }
        return [newState, dispatch];
      }
      function updateSyncExternalStore(subscribe2, getSnapshot, getServerSnapshot) {
        var fiber = currentlyRenderingFiber, hook = updateWorkInProgressHook(), isHydrating$jscomp$0 = isHydrating;
        if (isHydrating$jscomp$0) {
          if (void 0 === getServerSnapshot) throw Error(formatProdErrorMessage(407));
          getServerSnapshot = getServerSnapshot();
        } else getServerSnapshot = getSnapshot();
        var snapshotChanged = !objectIs(
          (currentHook || hook).memoizedState,
          getServerSnapshot
        );
        snapshotChanged && (hook.memoizedState = getServerSnapshot, didReceiveUpdate = true);
        hook = hook.queue;
        updateEffect(subscribeToStore.bind(null, fiber, hook, subscribe2), [
          subscribe2
        ]);
        if (hook.getSnapshot !== getSnapshot || snapshotChanged || null !== workInProgressHook && workInProgressHook.memoizedState.tag & 1) {
          fiber.flags |= 2048;
          pushSimpleEffect(
            9,
            { destroy: void 0 },
            updateStoreInstance.bind(
              null,
              fiber,
              hook,
              getServerSnapshot,
              getSnapshot
            ),
            null
          );
          if (null === workInProgressRoot) throw Error(formatProdErrorMessage(349));
          isHydrating$jscomp$0 || 0 !== (renderLanes & 127) || pushStoreConsistencyCheck(fiber, getSnapshot, getServerSnapshot);
        }
        return getServerSnapshot;
      }
      function pushStoreConsistencyCheck(fiber, getSnapshot, renderedSnapshot) {
        fiber.flags |= 16384;
        fiber = { getSnapshot, value: renderedSnapshot };
        getSnapshot = currentlyRenderingFiber.updateQueue;
        null === getSnapshot ? (getSnapshot = createFunctionComponentUpdateQueue(), currentlyRenderingFiber.updateQueue = getSnapshot, getSnapshot.stores = [fiber]) : (renderedSnapshot = getSnapshot.stores, null === renderedSnapshot ? getSnapshot.stores = [fiber] : renderedSnapshot.push(fiber));
      }
      function updateStoreInstance(fiber, inst, nextSnapshot, getSnapshot) {
        inst.value = nextSnapshot;
        inst.getSnapshot = getSnapshot;
        checkIfSnapshotChanged(inst) && forceStoreRerender(fiber);
      }
      function subscribeToStore(fiber, inst, subscribe2) {
        return subscribe2(function() {
          checkIfSnapshotChanged(inst) && forceStoreRerender(fiber);
        });
      }
      function checkIfSnapshotChanged(inst) {
        var latestGetSnapshot = inst.getSnapshot;
        inst = inst.value;
        try {
          var nextValue = latestGetSnapshot();
          return !objectIs(inst, nextValue);
        } catch (error) {
          return true;
        }
      }
      function forceStoreRerender(fiber) {
        var root2 = enqueueConcurrentRenderForLane(fiber, 2);
        null !== root2 && scheduleUpdateOnFiber(root2, fiber, 2);
      }
      function mountStateImpl(initialState) {
        var hook = mountWorkInProgressHook();
        if ("function" === typeof initialState) {
          var initialStateInitializer = initialState;
          initialState = initialStateInitializer();
          if (shouldDoubleInvokeUserFnsInHooksDEV) {
            setIsStrictModeForDevtools(true);
            try {
              initialStateInitializer();
            } finally {
              setIsStrictModeForDevtools(false);
            }
          }
        }
        hook.memoizedState = hook.baseState = initialState;
        hook.queue = {
          pending: null,
          lanes: 0,
          dispatch: null,
          lastRenderedReducer: basicStateReducer,
          lastRenderedState: initialState
        };
        return hook;
      }
      function updateOptimisticImpl(hook, current, passthrough, reducer) {
        hook.baseState = passthrough;
        return updateReducerImpl(
          hook,
          currentHook,
          "function" === typeof reducer ? reducer : basicStateReducer
        );
      }
      function dispatchActionState(fiber, actionQueue, setPendingState, setState, payload) {
        if (isRenderPhaseUpdate(fiber)) throw Error(formatProdErrorMessage(485));
        fiber = actionQueue.action;
        if (null !== fiber) {
          var actionNode = {
            payload,
            action: fiber,
            next: null,
            isTransition: true,
            status: "pending",
            value: null,
            reason: null,
            listeners: [],
            then: function(listener) {
              actionNode.listeners.push(listener);
            }
          };
          null !== ReactSharedInternals.T ? setPendingState(true) : actionNode.isTransition = false;
          setState(actionNode);
          setPendingState = actionQueue.pending;
          null === setPendingState ? (actionNode.next = actionQueue.pending = actionNode, runActionStateAction(actionQueue, actionNode)) : (actionNode.next = setPendingState.next, actionQueue.pending = setPendingState.next = actionNode);
        }
      }
      function runActionStateAction(actionQueue, node) {
        var action = node.action, payload = node.payload, prevState = actionQueue.state;
        if (node.isTransition) {
          var prevTransition = ReactSharedInternals.T, currentTransition = {};
          ReactSharedInternals.T = currentTransition;
          try {
            var returnValue = action(prevState, payload), onStartTransitionFinish = ReactSharedInternals.S;
            null !== onStartTransitionFinish && onStartTransitionFinish(currentTransition, returnValue);
            handleActionReturnValue(actionQueue, node, returnValue);
          } catch (error) {
            onActionError(actionQueue, node, error);
          } finally {
            null !== prevTransition && null !== currentTransition.types && (prevTransition.types = currentTransition.types), ReactSharedInternals.T = prevTransition;
          }
        } else
          try {
            prevTransition = action(prevState, payload), handleActionReturnValue(actionQueue, node, prevTransition);
          } catch (error$66) {
            onActionError(actionQueue, node, error$66);
          }
      }
      function handleActionReturnValue(actionQueue, node, returnValue) {
        null !== returnValue && "object" === typeof returnValue && "function" === typeof returnValue.then ? returnValue.then(
          function(nextState) {
            onActionSuccess(actionQueue, node, nextState);
          },
          function(error) {
            return onActionError(actionQueue, node, error);
          }
        ) : onActionSuccess(actionQueue, node, returnValue);
      }
      function onActionSuccess(actionQueue, actionNode, nextState) {
        actionNode.status = "fulfilled";
        actionNode.value = nextState;
        notifyActionListeners(actionNode);
        actionQueue.state = nextState;
        actionNode = actionQueue.pending;
        null !== actionNode && (nextState = actionNode.next, nextState === actionNode ? actionQueue.pending = null : (nextState = nextState.next, actionNode.next = nextState, runActionStateAction(actionQueue, nextState)));
      }
      function onActionError(actionQueue, actionNode, error) {
        var last = actionQueue.pending;
        actionQueue.pending = null;
        if (null !== last) {
          last = last.next;
          do
            actionNode.status = "rejected", actionNode.reason = error, notifyActionListeners(actionNode), actionNode = actionNode.next;
          while (actionNode !== last);
        }
        actionQueue.action = null;
      }
      function notifyActionListeners(actionNode) {
        actionNode = actionNode.listeners;
        for (var i = 0; i < actionNode.length; i++) (0, actionNode[i])();
      }
      function actionStateReducer(oldState, newState) {
        return newState;
      }
      function mountActionState(action, initialStateProp) {
        if (isHydrating) {
          var ssrFormState = workInProgressRoot.formState;
          if (null !== ssrFormState) {
            a: {
              var JSCompiler_inline_result = currentlyRenderingFiber;
              if (isHydrating) {
                if (nextHydratableInstance) {
                  b: {
                    var JSCompiler_inline_result$jscomp$0 = nextHydratableInstance;
                    for (var inRootOrSingleton = rootOrSingletonContext; 8 !== JSCompiler_inline_result$jscomp$0.nodeType; ) {
                      if (!inRootOrSingleton) {
                        JSCompiler_inline_result$jscomp$0 = null;
                        break b;
                      }
                      JSCompiler_inline_result$jscomp$0 = getNextHydratable(
                        JSCompiler_inline_result$jscomp$0.nextSibling
                      );
                      if (null === JSCompiler_inline_result$jscomp$0) {
                        JSCompiler_inline_result$jscomp$0 = null;
                        break b;
                      }
                    }
                    inRootOrSingleton = JSCompiler_inline_result$jscomp$0.data;
                    JSCompiler_inline_result$jscomp$0 = "F!" === inRootOrSingleton || "F" === inRootOrSingleton ? JSCompiler_inline_result$jscomp$0 : null;
                  }
                  if (JSCompiler_inline_result$jscomp$0) {
                    nextHydratableInstance = getNextHydratable(
                      JSCompiler_inline_result$jscomp$0.nextSibling
                    );
                    JSCompiler_inline_result = "F!" === JSCompiler_inline_result$jscomp$0.data;
                    break a;
                  }
                }
                throwOnHydrationMismatch(JSCompiler_inline_result);
              }
              JSCompiler_inline_result = false;
            }
            JSCompiler_inline_result && (initialStateProp = ssrFormState[0]);
          }
        }
        ssrFormState = mountWorkInProgressHook();
        ssrFormState.memoizedState = ssrFormState.baseState = initialStateProp;
        JSCompiler_inline_result = {
          pending: null,
          lanes: 0,
          dispatch: null,
          lastRenderedReducer: actionStateReducer,
          lastRenderedState: initialStateProp
        };
        ssrFormState.queue = JSCompiler_inline_result;
        ssrFormState = dispatchSetState.bind(
          null,
          currentlyRenderingFiber,
          JSCompiler_inline_result
        );
        JSCompiler_inline_result.dispatch = ssrFormState;
        JSCompiler_inline_result = mountStateImpl(false);
        inRootOrSingleton = dispatchOptimisticSetState.bind(
          null,
          currentlyRenderingFiber,
          false,
          JSCompiler_inline_result.queue
        );
        JSCompiler_inline_result = mountWorkInProgressHook();
        JSCompiler_inline_result$jscomp$0 = {
          state: initialStateProp,
          dispatch: null,
          action,
          pending: null
        };
        JSCompiler_inline_result.queue = JSCompiler_inline_result$jscomp$0;
        ssrFormState = dispatchActionState.bind(
          null,
          currentlyRenderingFiber,
          JSCompiler_inline_result$jscomp$0,
          inRootOrSingleton,
          ssrFormState
        );
        JSCompiler_inline_result$jscomp$0.dispatch = ssrFormState;
        JSCompiler_inline_result.memoizedState = action;
        return [initialStateProp, ssrFormState, false];
      }
      function updateActionState(action) {
        var stateHook = updateWorkInProgressHook();
        return updateActionStateImpl(stateHook, currentHook, action);
      }
      function updateActionStateImpl(stateHook, currentStateHook, action) {
        currentStateHook = updateReducerImpl(
          stateHook,
          currentStateHook,
          actionStateReducer
        )[0];
        stateHook = updateReducer(basicStateReducer)[0];
        if ("object" === typeof currentStateHook && null !== currentStateHook && "function" === typeof currentStateHook.then)
          try {
            var state2 = useThenable(currentStateHook);
          } catch (x) {
            if (x === SuspenseException) throw SuspenseActionException;
            throw x;
          }
        else state2 = currentStateHook;
        currentStateHook = updateWorkInProgressHook();
        var actionQueue = currentStateHook.queue, dispatch = actionQueue.dispatch;
        action !== currentStateHook.memoizedState && (currentlyRenderingFiber.flags |= 2048, pushSimpleEffect(
          9,
          { destroy: void 0 },
          actionStateActionEffect.bind(null, actionQueue, action),
          null
        ));
        return [state2, dispatch, stateHook];
      }
      function actionStateActionEffect(actionQueue, action) {
        actionQueue.action = action;
      }
      function rerenderActionState(action) {
        var stateHook = updateWorkInProgressHook(), currentStateHook = currentHook;
        if (null !== currentStateHook)
          return updateActionStateImpl(stateHook, currentStateHook, action);
        updateWorkInProgressHook();
        stateHook = stateHook.memoizedState;
        currentStateHook = updateWorkInProgressHook();
        var dispatch = currentStateHook.queue.dispatch;
        currentStateHook.memoizedState = action;
        return [stateHook, dispatch, false];
      }
      function pushSimpleEffect(tag, inst, create, deps) {
        tag = { tag, create, deps, inst, next: null };
        inst = currentlyRenderingFiber.updateQueue;
        null === inst && (inst = createFunctionComponentUpdateQueue(), currentlyRenderingFiber.updateQueue = inst);
        create = inst.lastEffect;
        null === create ? inst.lastEffect = tag.next = tag : (deps = create.next, create.next = tag, tag.next = deps, inst.lastEffect = tag);
        return tag;
      }
      function updateRef() {
        return updateWorkInProgressHook().memoizedState;
      }
      function mountEffectImpl(fiberFlags, hookFlags, create, deps) {
        var hook = mountWorkInProgressHook();
        currentlyRenderingFiber.flags |= fiberFlags;
        hook.memoizedState = pushSimpleEffect(
          1 | hookFlags,
          { destroy: void 0 },
          create,
          void 0 === deps ? null : deps
        );
      }
      function updateEffectImpl(fiberFlags, hookFlags, create, deps) {
        var hook = updateWorkInProgressHook();
        deps = void 0 === deps ? null : deps;
        var inst = hook.memoizedState.inst;
        null !== currentHook && null !== deps && areHookInputsEqual(deps, currentHook.memoizedState.deps) ? hook.memoizedState = pushSimpleEffect(hookFlags, inst, create, deps) : (currentlyRenderingFiber.flags |= fiberFlags, hook.memoizedState = pushSimpleEffect(
          1 | hookFlags,
          inst,
          create,
          deps
        ));
      }
      function mountEffect(create, deps) {
        mountEffectImpl(8390656, 8, create, deps);
      }
      function updateEffect(create, deps) {
        updateEffectImpl(2048, 8, create, deps);
      }
      function useEffectEventImpl(payload) {
        currentlyRenderingFiber.flags |= 4;
        var componentUpdateQueue = currentlyRenderingFiber.updateQueue;
        if (null === componentUpdateQueue)
          componentUpdateQueue = createFunctionComponentUpdateQueue(), currentlyRenderingFiber.updateQueue = componentUpdateQueue, componentUpdateQueue.events = [payload];
        else {
          var events = componentUpdateQueue.events;
          null === events ? componentUpdateQueue.events = [payload] : events.push(payload);
        }
      }
      function updateEvent(callback) {
        var ref = updateWorkInProgressHook().memoizedState;
        useEffectEventImpl({ ref, nextImpl: callback });
        return function() {
          if (0 !== (executionContext & 2)) throw Error(formatProdErrorMessage(440));
          return ref.impl.apply(void 0, arguments);
        };
      }
      function updateInsertionEffect(create, deps) {
        return updateEffectImpl(4, 2, create, deps);
      }
      function updateLayoutEffect(create, deps) {
        return updateEffectImpl(4, 4, create, deps);
      }
      function imperativeHandleEffect(create, ref) {
        if ("function" === typeof ref) {
          create = create();
          var refCleanup = ref(create);
          return function() {
            "function" === typeof refCleanup ? refCleanup() : ref(null);
          };
        }
        if (null !== ref && void 0 !== ref)
          return create = create(), ref.current = create, function() {
            ref.current = null;
          };
      }
      function updateImperativeHandle(ref, create, deps) {
        deps = null !== deps && void 0 !== deps ? deps.concat([ref]) : null;
        updateEffectImpl(4, 4, imperativeHandleEffect.bind(null, create, ref), deps);
      }
      function mountDebugValue() {
      }
      function updateCallback(callback, deps) {
        var hook = updateWorkInProgressHook();
        deps = void 0 === deps ? null : deps;
        var prevState = hook.memoizedState;
        if (null !== deps && areHookInputsEqual(deps, prevState[1]))
          return prevState[0];
        hook.memoizedState = [callback, deps];
        return callback;
      }
      function updateMemo(nextCreate, deps) {
        var hook = updateWorkInProgressHook();
        deps = void 0 === deps ? null : deps;
        var prevState = hook.memoizedState;
        if (null !== deps && areHookInputsEqual(deps, prevState[1]))
          return prevState[0];
        prevState = nextCreate();
        if (shouldDoubleInvokeUserFnsInHooksDEV) {
          setIsStrictModeForDevtools(true);
          try {
            nextCreate();
          } finally {
            setIsStrictModeForDevtools(false);
          }
        }
        hook.memoizedState = [prevState, deps];
        return prevState;
      }
      function mountDeferredValueImpl(hook, value, initialValue) {
        if (void 0 === initialValue || 0 !== (renderLanes & 1073741824) && 0 === (workInProgressRootRenderLanes & 261930))
          return hook.memoizedState = value;
        hook.memoizedState = initialValue;
        hook = requestDeferredLane();
        currentlyRenderingFiber.lanes |= hook;
        workInProgressRootSkippedLanes |= hook;
        return initialValue;
      }
      function updateDeferredValueImpl(hook, prevValue, value, initialValue) {
        if (objectIs(value, prevValue)) return value;
        if (null !== currentTreeHiddenStackCursor.current)
          return hook = mountDeferredValueImpl(hook, value, initialValue), objectIs(hook, prevValue) || (didReceiveUpdate = true), hook;
        if (0 === (renderLanes & 42) || 0 !== (renderLanes & 1073741824) && 0 === (workInProgressRootRenderLanes & 261930))
          return didReceiveUpdate = true, hook.memoizedState = value;
        hook = requestDeferredLane();
        currentlyRenderingFiber.lanes |= hook;
        workInProgressRootSkippedLanes |= hook;
        return prevValue;
      }
      function startTransition(fiber, queue, pendingState, finishedState, callback) {
        var previousPriority = ReactDOMSharedInternals.p;
        ReactDOMSharedInternals.p = 0 !== previousPriority && 8 > previousPriority ? previousPriority : 8;
        var prevTransition = ReactSharedInternals.T, currentTransition = {};
        ReactSharedInternals.T = currentTransition;
        dispatchOptimisticSetState(fiber, false, queue, pendingState);
        try {
          var returnValue = callback(), onStartTransitionFinish = ReactSharedInternals.S;
          null !== onStartTransitionFinish && onStartTransitionFinish(currentTransition, returnValue);
          if (null !== returnValue && "object" === typeof returnValue && "function" === typeof returnValue.then) {
            var thenableForFinishedState = chainThenableValue(
              returnValue,
              finishedState
            );
            dispatchSetStateInternal(
              fiber,
              queue,
              thenableForFinishedState,
              requestUpdateLane(fiber)
            );
          } else
            dispatchSetStateInternal(
              fiber,
              queue,
              finishedState,
              requestUpdateLane(fiber)
            );
        } catch (error) {
          dispatchSetStateInternal(
            fiber,
            queue,
            { then: function() {
            }, status: "rejected", reason: error },
            requestUpdateLane()
          );
        } finally {
          ReactDOMSharedInternals.p = previousPriority, null !== prevTransition && null !== currentTransition.types && (prevTransition.types = currentTransition.types), ReactSharedInternals.T = prevTransition;
        }
      }
      function noop3() {
      }
      function startHostTransition(formFiber, pendingState, action, formData) {
        if (5 !== formFiber.tag) throw Error(formatProdErrorMessage(476));
        var queue = ensureFormComponentIsStateful(formFiber).queue;
        startTransition(
          formFiber,
          queue,
          pendingState,
          sharedNotPendingObject,
          null === action ? noop3 : function() {
            requestFormReset$1(formFiber);
            return action(formData);
          }
        );
      }
      function ensureFormComponentIsStateful(formFiber) {
        var existingStateHook = formFiber.memoizedState;
        if (null !== existingStateHook) return existingStateHook;
        existingStateHook = {
          memoizedState: sharedNotPendingObject,
          baseState: sharedNotPendingObject,
          baseQueue: null,
          queue: {
            pending: null,
            lanes: 0,
            dispatch: null,
            lastRenderedReducer: basicStateReducer,
            lastRenderedState: sharedNotPendingObject
          },
          next: null
        };
        var initialResetState = {};
        existingStateHook.next = {
          memoizedState: initialResetState,
          baseState: initialResetState,
          baseQueue: null,
          queue: {
            pending: null,
            lanes: 0,
            dispatch: null,
            lastRenderedReducer: basicStateReducer,
            lastRenderedState: initialResetState
          },
          next: null
        };
        formFiber.memoizedState = existingStateHook;
        formFiber = formFiber.alternate;
        null !== formFiber && (formFiber.memoizedState = existingStateHook);
        return existingStateHook;
      }
      function requestFormReset$1(formFiber) {
        var stateHook = ensureFormComponentIsStateful(formFiber);
        null === stateHook.next && (stateHook = formFiber.alternate.memoizedState);
        dispatchSetStateInternal(
          formFiber,
          stateHook.next.queue,
          {},
          requestUpdateLane()
        );
      }
      function useHostTransitionStatus() {
        return readContext(HostTransitionContext);
      }
      function updateId() {
        return updateWorkInProgressHook().memoizedState;
      }
      function updateRefresh() {
        return updateWorkInProgressHook().memoizedState;
      }
      function refreshCache(fiber) {
        for (var provider = fiber.return; null !== provider; ) {
          switch (provider.tag) {
            case 24:
            case 3:
              var lane = requestUpdateLane();
              fiber = createUpdate(lane);
              var root$69 = enqueueUpdate(provider, fiber, lane);
              null !== root$69 && (scheduleUpdateOnFiber(root$69, provider, lane), entangleTransitions(root$69, provider, lane));
              provider = { cache: createCache() };
              fiber.payload = provider;
              return;
          }
          provider = provider.return;
        }
      }
      function dispatchReducerAction(fiber, queue, action) {
        var lane = requestUpdateLane();
        action = {
          lane,
          revertLane: 0,
          gesture: null,
          action,
          hasEagerState: false,
          eagerState: null,
          next: null
        };
        isRenderPhaseUpdate(fiber) ? enqueueRenderPhaseUpdate(queue, action) : (action = enqueueConcurrentHookUpdate(fiber, queue, action, lane), null !== action && (scheduleUpdateOnFiber(action, fiber, lane), entangleTransitionUpdate(action, queue, lane)));
      }
      function dispatchSetState(fiber, queue, action) {
        var lane = requestUpdateLane();
        dispatchSetStateInternal(fiber, queue, action, lane);
      }
      function dispatchSetStateInternal(fiber, queue, action, lane) {
        var update = {
          lane,
          revertLane: 0,
          gesture: null,
          action,
          hasEagerState: false,
          eagerState: null,
          next: null
        };
        if (isRenderPhaseUpdate(fiber)) enqueueRenderPhaseUpdate(queue, update);
        else {
          var alternate = fiber.alternate;
          if (0 === fiber.lanes && (null === alternate || 0 === alternate.lanes) && (alternate = queue.lastRenderedReducer, null !== alternate))
            try {
              var currentState = queue.lastRenderedState, eagerState = alternate(currentState, action);
              update.hasEagerState = true;
              update.eagerState = eagerState;
              if (objectIs(eagerState, currentState))
                return enqueueUpdate$1(fiber, queue, update, 0), null === workInProgressRoot && finishQueueingConcurrentUpdates(), false;
            } catch (error) {
            } finally {
            }
          action = enqueueConcurrentHookUpdate(fiber, queue, update, lane);
          if (null !== action)
            return scheduleUpdateOnFiber(action, fiber, lane), entangleTransitionUpdate(action, queue, lane), true;
        }
        return false;
      }
      function dispatchOptimisticSetState(fiber, throwIfDuringRender, queue, action) {
        action = {
          lane: 2,
          revertLane: requestTransitionLane(),
          gesture: null,
          action,
          hasEagerState: false,
          eagerState: null,
          next: null
        };
        if (isRenderPhaseUpdate(fiber)) {
          if (throwIfDuringRender) throw Error(formatProdErrorMessage(479));
        } else
          throwIfDuringRender = enqueueConcurrentHookUpdate(
            fiber,
            queue,
            action,
            2
          ), null !== throwIfDuringRender && scheduleUpdateOnFiber(throwIfDuringRender, fiber, 2);
      }
      function isRenderPhaseUpdate(fiber) {
        var alternate = fiber.alternate;
        return fiber === currentlyRenderingFiber || null !== alternate && alternate === currentlyRenderingFiber;
      }
      function enqueueRenderPhaseUpdate(queue, update) {
        didScheduleRenderPhaseUpdateDuringThisPass = didScheduleRenderPhaseUpdate = true;
        var pending = queue.pending;
        null === pending ? update.next = update : (update.next = pending.next, pending.next = update);
        queue.pending = update;
      }
      function entangleTransitionUpdate(root2, queue, lane) {
        if (0 !== (lane & 4194048)) {
          var queueLanes = queue.lanes;
          queueLanes &= root2.pendingLanes;
          lane |= queueLanes;
          queue.lanes = lane;
          markRootEntangled(root2, lane);
        }
      }
      var ContextOnlyDispatcher = {
        readContext,
        use: use2,
        useCallback: throwInvalidHookError,
        useContext: throwInvalidHookError,
        useEffect: throwInvalidHookError,
        useImperativeHandle: throwInvalidHookError,
        useLayoutEffect: throwInvalidHookError,
        useInsertionEffect: throwInvalidHookError,
        useMemo: throwInvalidHookError,
        useReducer: throwInvalidHookError,
        useRef: throwInvalidHookError,
        useState: throwInvalidHookError,
        useDebugValue: throwInvalidHookError,
        useDeferredValue: throwInvalidHookError,
        useTransition: throwInvalidHookError,
        useSyncExternalStore: throwInvalidHookError,
        useId: throwInvalidHookError,
        useHostTransitionStatus: throwInvalidHookError,
        useFormState: throwInvalidHookError,
        useActionState: throwInvalidHookError,
        useOptimistic: throwInvalidHookError,
        useMemoCache: throwInvalidHookError,
        useCacheRefresh: throwInvalidHookError
      };
      ContextOnlyDispatcher.useEffectEvent = throwInvalidHookError;
      var HooksDispatcherOnMount = {
        readContext,
        use: use2,
        useCallback: function(callback, deps) {
          mountWorkInProgressHook().memoizedState = [
            callback,
            void 0 === deps ? null : deps
          ];
          return callback;
        },
        useContext: readContext,
        useEffect: mountEffect,
        useImperativeHandle: function(ref, create, deps) {
          deps = null !== deps && void 0 !== deps ? deps.concat([ref]) : null;
          mountEffectImpl(
            4194308,
            4,
            imperativeHandleEffect.bind(null, create, ref),
            deps
          );
        },
        useLayoutEffect: function(create, deps) {
          return mountEffectImpl(4194308, 4, create, deps);
        },
        useInsertionEffect: function(create, deps) {
          mountEffectImpl(4, 2, create, deps);
        },
        useMemo: function(nextCreate, deps) {
          var hook = mountWorkInProgressHook();
          deps = void 0 === deps ? null : deps;
          var nextValue = nextCreate();
          if (shouldDoubleInvokeUserFnsInHooksDEV) {
            setIsStrictModeForDevtools(true);
            try {
              nextCreate();
            } finally {
              setIsStrictModeForDevtools(false);
            }
          }
          hook.memoizedState = [nextValue, deps];
          return nextValue;
        },
        useReducer: function(reducer, initialArg, init) {
          var hook = mountWorkInProgressHook();
          if (void 0 !== init) {
            var initialState = init(initialArg);
            if (shouldDoubleInvokeUserFnsInHooksDEV) {
              setIsStrictModeForDevtools(true);
              try {
                init(initialArg);
              } finally {
                setIsStrictModeForDevtools(false);
              }
            }
          } else initialState = initialArg;
          hook.memoizedState = hook.baseState = initialState;
          reducer = {
            pending: null,
            lanes: 0,
            dispatch: null,
            lastRenderedReducer: reducer,
            lastRenderedState: initialState
          };
          hook.queue = reducer;
          reducer = reducer.dispatch = dispatchReducerAction.bind(
            null,
            currentlyRenderingFiber,
            reducer
          );
          return [hook.memoizedState, reducer];
        },
        useRef: function(initialValue) {
          var hook = mountWorkInProgressHook();
          initialValue = { current: initialValue };
          return hook.memoizedState = initialValue;
        },
        useState: function(initialState) {
          initialState = mountStateImpl(initialState);
          var queue = initialState.queue, dispatch = dispatchSetState.bind(null, currentlyRenderingFiber, queue);
          queue.dispatch = dispatch;
          return [initialState.memoizedState, dispatch];
        },
        useDebugValue: mountDebugValue,
        useDeferredValue: function(value, initialValue) {
          var hook = mountWorkInProgressHook();
          return mountDeferredValueImpl(hook, value, initialValue);
        },
        useTransition: function() {
          var stateHook = mountStateImpl(false);
          stateHook = startTransition.bind(
            null,
            currentlyRenderingFiber,
            stateHook.queue,
            true,
            false
          );
          mountWorkInProgressHook().memoizedState = stateHook;
          return [false, stateHook];
        },
        useSyncExternalStore: function(subscribe2, getSnapshot, getServerSnapshot) {
          var fiber = currentlyRenderingFiber, hook = mountWorkInProgressHook();
          if (isHydrating) {
            if (void 0 === getServerSnapshot)
              throw Error(formatProdErrorMessage(407));
            getServerSnapshot = getServerSnapshot();
          } else {
            getServerSnapshot = getSnapshot();
            if (null === workInProgressRoot)
              throw Error(formatProdErrorMessage(349));
            0 !== (workInProgressRootRenderLanes & 127) || pushStoreConsistencyCheck(fiber, getSnapshot, getServerSnapshot);
          }
          hook.memoizedState = getServerSnapshot;
          var inst = { value: getServerSnapshot, getSnapshot };
          hook.queue = inst;
          mountEffect(subscribeToStore.bind(null, fiber, inst, subscribe2), [
            subscribe2
          ]);
          fiber.flags |= 2048;
          pushSimpleEffect(
            9,
            { destroy: void 0 },
            updateStoreInstance.bind(
              null,
              fiber,
              inst,
              getServerSnapshot,
              getSnapshot
            ),
            null
          );
          return getServerSnapshot;
        },
        useId: function() {
          var hook = mountWorkInProgressHook(), identifierPrefix = workInProgressRoot.identifierPrefix;
          if (isHydrating) {
            var JSCompiler_inline_result = treeContextOverflow;
            var idWithLeadingBit = treeContextId;
            JSCompiler_inline_result = (idWithLeadingBit & ~(1 << 32 - clz32(idWithLeadingBit) - 1)).toString(32) + JSCompiler_inline_result;
            identifierPrefix = "_" + identifierPrefix + "R_" + JSCompiler_inline_result;
            JSCompiler_inline_result = localIdCounter++;
            0 < JSCompiler_inline_result && (identifierPrefix += "H" + JSCompiler_inline_result.toString(32));
            identifierPrefix += "_";
          } else
            JSCompiler_inline_result = globalClientIdCounter++, identifierPrefix = "_" + identifierPrefix + "r_" + JSCompiler_inline_result.toString(32) + "_";
          return hook.memoizedState = identifierPrefix;
        },
        useHostTransitionStatus,
        useFormState: mountActionState,
        useActionState: mountActionState,
        useOptimistic: function(passthrough) {
          var hook = mountWorkInProgressHook();
          hook.memoizedState = hook.baseState = passthrough;
          var queue = {
            pending: null,
            lanes: 0,
            dispatch: null,
            lastRenderedReducer: null,
            lastRenderedState: null
          };
          hook.queue = queue;
          hook = dispatchOptimisticSetState.bind(
            null,
            currentlyRenderingFiber,
            true,
            queue
          );
          queue.dispatch = hook;
          return [passthrough, hook];
        },
        useMemoCache,
        useCacheRefresh: function() {
          return mountWorkInProgressHook().memoizedState = refreshCache.bind(
            null,
            currentlyRenderingFiber
          );
        },
        useEffectEvent: function(callback) {
          var hook = mountWorkInProgressHook(), ref = { impl: callback };
          hook.memoizedState = ref;
          return function() {
            if (0 !== (executionContext & 2))
              throw Error(formatProdErrorMessage(440));
            return ref.impl.apply(void 0, arguments);
          };
        }
      };
      var HooksDispatcherOnUpdate = {
        readContext,
        use: use2,
        useCallback: updateCallback,
        useContext: readContext,
        useEffect: updateEffect,
        useImperativeHandle: updateImperativeHandle,
        useInsertionEffect: updateInsertionEffect,
        useLayoutEffect: updateLayoutEffect,
        useMemo: updateMemo,
        useReducer: updateReducer,
        useRef: updateRef,
        useState: function() {
          return updateReducer(basicStateReducer);
        },
        useDebugValue: mountDebugValue,
        useDeferredValue: function(value, initialValue) {
          var hook = updateWorkInProgressHook();
          return updateDeferredValueImpl(
            hook,
            currentHook.memoizedState,
            value,
            initialValue
          );
        },
        useTransition: function() {
          var booleanOrThenable = updateReducer(basicStateReducer)[0], start = updateWorkInProgressHook().memoizedState;
          return [
            "boolean" === typeof booleanOrThenable ? booleanOrThenable : useThenable(booleanOrThenable),
            start
          ];
        },
        useSyncExternalStore: updateSyncExternalStore,
        useId: updateId,
        useHostTransitionStatus,
        useFormState: updateActionState,
        useActionState: updateActionState,
        useOptimistic: function(passthrough, reducer) {
          var hook = updateWorkInProgressHook();
          return updateOptimisticImpl(hook, currentHook, passthrough, reducer);
        },
        useMemoCache,
        useCacheRefresh: updateRefresh
      };
      HooksDispatcherOnUpdate.useEffectEvent = updateEvent;
      var HooksDispatcherOnRerender = {
        readContext,
        use: use2,
        useCallback: updateCallback,
        useContext: readContext,
        useEffect: updateEffect,
        useImperativeHandle: updateImperativeHandle,
        useInsertionEffect: updateInsertionEffect,
        useLayoutEffect: updateLayoutEffect,
        useMemo: updateMemo,
        useReducer: rerenderReducer,
        useRef: updateRef,
        useState: function() {
          return rerenderReducer(basicStateReducer);
        },
        useDebugValue: mountDebugValue,
        useDeferredValue: function(value, initialValue) {
          var hook = updateWorkInProgressHook();
          return null === currentHook ? mountDeferredValueImpl(hook, value, initialValue) : updateDeferredValueImpl(
            hook,
            currentHook.memoizedState,
            value,
            initialValue
          );
        },
        useTransition: function() {
          var booleanOrThenable = rerenderReducer(basicStateReducer)[0], start = updateWorkInProgressHook().memoizedState;
          return [
            "boolean" === typeof booleanOrThenable ? booleanOrThenable : useThenable(booleanOrThenable),
            start
          ];
        },
        useSyncExternalStore: updateSyncExternalStore,
        useId: updateId,
        useHostTransitionStatus,
        useFormState: rerenderActionState,
        useActionState: rerenderActionState,
        useOptimistic: function(passthrough, reducer) {
          var hook = updateWorkInProgressHook();
          if (null !== currentHook)
            return updateOptimisticImpl(hook, currentHook, passthrough, reducer);
          hook.baseState = passthrough;
          return [passthrough, hook.queue.dispatch];
        },
        useMemoCache,
        useCacheRefresh: updateRefresh
      };
      HooksDispatcherOnRerender.useEffectEvent = updateEvent;
      function applyDerivedStateFromProps(workInProgress2, ctor, getDerivedStateFromProps, nextProps) {
        ctor = workInProgress2.memoizedState;
        getDerivedStateFromProps = getDerivedStateFromProps(nextProps, ctor);
        getDerivedStateFromProps = null === getDerivedStateFromProps || void 0 === getDerivedStateFromProps ? ctor : assign({}, ctor, getDerivedStateFromProps);
        workInProgress2.memoizedState = getDerivedStateFromProps;
        0 === workInProgress2.lanes && (workInProgress2.updateQueue.baseState = getDerivedStateFromProps);
      }
      var classComponentUpdater = {
        enqueueSetState: function(inst, payload, callback) {
          inst = inst._reactInternals;
          var lane = requestUpdateLane(), update = createUpdate(lane);
          update.payload = payload;
          void 0 !== callback && null !== callback && (update.callback = callback);
          payload = enqueueUpdate(inst, update, lane);
          null !== payload && (scheduleUpdateOnFiber(payload, inst, lane), entangleTransitions(payload, inst, lane));
        },
        enqueueReplaceState: function(inst, payload, callback) {
          inst = inst._reactInternals;
          var lane = requestUpdateLane(), update = createUpdate(lane);
          update.tag = 1;
          update.payload = payload;
          void 0 !== callback && null !== callback && (update.callback = callback);
          payload = enqueueUpdate(inst, update, lane);
          null !== payload && (scheduleUpdateOnFiber(payload, inst, lane), entangleTransitions(payload, inst, lane));
        },
        enqueueForceUpdate: function(inst, callback) {
          inst = inst._reactInternals;
          var lane = requestUpdateLane(), update = createUpdate(lane);
          update.tag = 2;
          void 0 !== callback && null !== callback && (update.callback = callback);
          callback = enqueueUpdate(inst, update, lane);
          null !== callback && (scheduleUpdateOnFiber(callback, inst, lane), entangleTransitions(callback, inst, lane));
        }
      };
      function checkShouldComponentUpdate(workInProgress2, ctor, oldProps, newProps, oldState, newState, nextContext) {
        workInProgress2 = workInProgress2.stateNode;
        return "function" === typeof workInProgress2.shouldComponentUpdate ? workInProgress2.shouldComponentUpdate(newProps, newState, nextContext) : ctor.prototype && ctor.prototype.isPureReactComponent ? !shallowEqual(oldProps, newProps) || !shallowEqual(oldState, newState) : true;
      }
      function callComponentWillReceiveProps(workInProgress2, instance, newProps, nextContext) {
        workInProgress2 = instance.state;
        "function" === typeof instance.componentWillReceiveProps && instance.componentWillReceiveProps(newProps, nextContext);
        "function" === typeof instance.UNSAFE_componentWillReceiveProps && instance.UNSAFE_componentWillReceiveProps(newProps, nextContext);
        instance.state !== workInProgress2 && classComponentUpdater.enqueueReplaceState(instance, instance.state, null);
      }
      function resolveClassComponentProps(Component, baseProps) {
        var newProps = baseProps;
        if ("ref" in baseProps) {
          newProps = {};
          for (var propName in baseProps)
            "ref" !== propName && (newProps[propName] = baseProps[propName]);
        }
        if (Component = Component.defaultProps) {
          newProps === baseProps && (newProps = assign({}, newProps));
          for (var propName$73 in Component)
            void 0 === newProps[propName$73] && (newProps[propName$73] = Component[propName$73]);
        }
        return newProps;
      }
      function defaultOnUncaughtError(error) {
        reportGlobalError(error);
      }
      function defaultOnCaughtError(error) {
        console.error(error);
      }
      function defaultOnRecoverableError(error) {
        reportGlobalError(error);
      }
      function logUncaughtError(root2, errorInfo) {
        try {
          var onUncaughtError = root2.onUncaughtError;
          onUncaughtError(errorInfo.value, { componentStack: errorInfo.stack });
        } catch (e$74) {
          setTimeout(function() {
            throw e$74;
          });
        }
      }
      function logCaughtError(root2, boundary, errorInfo) {
        try {
          var onCaughtError = root2.onCaughtError;
          onCaughtError(errorInfo.value, {
            componentStack: errorInfo.stack,
            errorBoundary: 1 === boundary.tag ? boundary.stateNode : null
          });
        } catch (e$75) {
          setTimeout(function() {
            throw e$75;
          });
        }
      }
      function createRootErrorUpdate(root2, errorInfo, lane) {
        lane = createUpdate(lane);
        lane.tag = 3;
        lane.payload = { element: null };
        lane.callback = function() {
          logUncaughtError(root2, errorInfo);
        };
        return lane;
      }
      function createClassErrorUpdate(lane) {
        lane = createUpdate(lane);
        lane.tag = 3;
        return lane;
      }
      function initializeClassErrorUpdate(update, root2, fiber, errorInfo) {
        var getDerivedStateFromError = fiber.type.getDerivedStateFromError;
        if ("function" === typeof getDerivedStateFromError) {
          var error = errorInfo.value;
          update.payload = function() {
            return getDerivedStateFromError(error);
          };
          update.callback = function() {
            logCaughtError(root2, fiber, errorInfo);
          };
        }
        var inst = fiber.stateNode;
        null !== inst && "function" === typeof inst.componentDidCatch && (update.callback = function() {
          logCaughtError(root2, fiber, errorInfo);
          "function" !== typeof getDerivedStateFromError && (null === legacyErrorBoundariesThatAlreadyFailed ? legacyErrorBoundariesThatAlreadyFailed = /* @__PURE__ */ new Set([this]) : legacyErrorBoundariesThatAlreadyFailed.add(this));
          var stack = errorInfo.stack;
          this.componentDidCatch(errorInfo.value, {
            componentStack: null !== stack ? stack : ""
          });
        });
      }
      function throwException(root2, returnFiber, sourceFiber, value, rootRenderLanes) {
        sourceFiber.flags |= 32768;
        if (null !== value && "object" === typeof value && "function" === typeof value.then) {
          returnFiber = sourceFiber.alternate;
          null !== returnFiber && propagateParentContextChanges(
            returnFiber,
            sourceFiber,
            rootRenderLanes,
            true
          );
          sourceFiber = suspenseHandlerStackCursor.current;
          if (null !== sourceFiber) {
            switch (sourceFiber.tag) {
              case 31:
              case 13:
                return null === shellBoundary ? renderDidSuspendDelayIfPossible() : null === sourceFiber.alternate && 0 === workInProgressRootExitStatus && (workInProgressRootExitStatus = 3), sourceFiber.flags &= -257, sourceFiber.flags |= 65536, sourceFiber.lanes = rootRenderLanes, value === noopSuspenseyCommitThenable ? sourceFiber.flags |= 16384 : (returnFiber = sourceFiber.updateQueue, null === returnFiber ? sourceFiber.updateQueue = /* @__PURE__ */ new Set([value]) : returnFiber.add(value), attachPingListener(root2, value, rootRenderLanes)), false;
              case 22:
                return sourceFiber.flags |= 65536, value === noopSuspenseyCommitThenable ? sourceFiber.flags |= 16384 : (returnFiber = sourceFiber.updateQueue, null === returnFiber ? (returnFiber = {
                  transitions: null,
                  markerInstances: null,
                  retryQueue: /* @__PURE__ */ new Set([value])
                }, sourceFiber.updateQueue = returnFiber) : (sourceFiber = returnFiber.retryQueue, null === sourceFiber ? returnFiber.retryQueue = /* @__PURE__ */ new Set([value]) : sourceFiber.add(value)), attachPingListener(root2, value, rootRenderLanes)), false;
            }
            throw Error(formatProdErrorMessage(435, sourceFiber.tag));
          }
          attachPingListener(root2, value, rootRenderLanes);
          renderDidSuspendDelayIfPossible();
          return false;
        }
        if (isHydrating)
          return returnFiber = suspenseHandlerStackCursor.current, null !== returnFiber ? (0 === (returnFiber.flags & 65536) && (returnFiber.flags |= 256), returnFiber.flags |= 65536, returnFiber.lanes = rootRenderLanes, value !== HydrationMismatchException && (root2 = Error(formatProdErrorMessage(422), { cause: value }), queueHydrationError(createCapturedValueAtFiber(root2, sourceFiber)))) : (value !== HydrationMismatchException && (returnFiber = Error(formatProdErrorMessage(423), {
            cause: value
          }), queueHydrationError(
            createCapturedValueAtFiber(returnFiber, sourceFiber)
          )), root2 = root2.current.alternate, root2.flags |= 65536, rootRenderLanes &= -rootRenderLanes, root2.lanes |= rootRenderLanes, value = createCapturedValueAtFiber(value, sourceFiber), rootRenderLanes = createRootErrorUpdate(
            root2.stateNode,
            value,
            rootRenderLanes
          ), enqueueCapturedUpdate(root2, rootRenderLanes), 4 !== workInProgressRootExitStatus && (workInProgressRootExitStatus = 2)), false;
        var wrapperError = Error(formatProdErrorMessage(520), { cause: value });
        wrapperError = createCapturedValueAtFiber(wrapperError, sourceFiber);
        null === workInProgressRootConcurrentErrors ? workInProgressRootConcurrentErrors = [wrapperError] : workInProgressRootConcurrentErrors.push(wrapperError);
        4 !== workInProgressRootExitStatus && (workInProgressRootExitStatus = 2);
        if (null === returnFiber) return true;
        value = createCapturedValueAtFiber(value, sourceFiber);
        sourceFiber = returnFiber;
        do {
          switch (sourceFiber.tag) {
            case 3:
              return sourceFiber.flags |= 65536, root2 = rootRenderLanes & -rootRenderLanes, sourceFiber.lanes |= root2, root2 = createRootErrorUpdate(sourceFiber.stateNode, value, root2), enqueueCapturedUpdate(sourceFiber, root2), false;
            case 1:
              if (returnFiber = sourceFiber.type, wrapperError = sourceFiber.stateNode, 0 === (sourceFiber.flags & 128) && ("function" === typeof returnFiber.getDerivedStateFromError || null !== wrapperError && "function" === typeof wrapperError.componentDidCatch && (null === legacyErrorBoundariesThatAlreadyFailed || !legacyErrorBoundariesThatAlreadyFailed.has(wrapperError))))
                return sourceFiber.flags |= 65536, rootRenderLanes &= -rootRenderLanes, sourceFiber.lanes |= rootRenderLanes, rootRenderLanes = createClassErrorUpdate(rootRenderLanes), initializeClassErrorUpdate(
                  rootRenderLanes,
                  root2,
                  sourceFiber,
                  value
                ), enqueueCapturedUpdate(sourceFiber, rootRenderLanes), false;
          }
          sourceFiber = sourceFiber.return;
        } while (null !== sourceFiber);
        return false;
      }
      var SelectiveHydrationException = Error(formatProdErrorMessage(461));
      var didReceiveUpdate = false;
      function reconcileChildren(current, workInProgress2, nextChildren, renderLanes2) {
        workInProgress2.child = null === current ? mountChildFibers(workInProgress2, null, nextChildren, renderLanes2) : reconcileChildFibers(
          workInProgress2,
          current.child,
          nextChildren,
          renderLanes2
        );
      }
      function updateForwardRef(current, workInProgress2, Component, nextProps, renderLanes2) {
        Component = Component.render;
        var ref = workInProgress2.ref;
        if ("ref" in nextProps) {
          var propsWithoutRef = {};
          for (var key in nextProps)
            "ref" !== key && (propsWithoutRef[key] = nextProps[key]);
        } else propsWithoutRef = nextProps;
        prepareToReadContext(workInProgress2);
        nextProps = renderWithHooks(
          current,
          workInProgress2,
          Component,
          propsWithoutRef,
          ref,
          renderLanes2
        );
        key = checkDidRenderIdHook();
        if (null !== current && !didReceiveUpdate)
          return bailoutHooks(current, workInProgress2, renderLanes2), bailoutOnAlreadyFinishedWork(current, workInProgress2, renderLanes2);
        isHydrating && key && pushMaterializedTreeId(workInProgress2);
        workInProgress2.flags |= 1;
        reconcileChildren(current, workInProgress2, nextProps, renderLanes2);
        return workInProgress2.child;
      }
      function updateMemoComponent(current, workInProgress2, Component, nextProps, renderLanes2) {
        if (null === current) {
          var type = Component.type;
          if ("function" === typeof type && !shouldConstruct(type) && void 0 === type.defaultProps && null === Component.compare)
            return workInProgress2.tag = 15, workInProgress2.type = type, updateSimpleMemoComponent(
              current,
              workInProgress2,
              type,
              nextProps,
              renderLanes2
            );
          current = createFiberFromTypeAndProps(
            Component.type,
            null,
            nextProps,
            workInProgress2,
            workInProgress2.mode,
            renderLanes2
          );
          current.ref = workInProgress2.ref;
          current.return = workInProgress2;
          return workInProgress2.child = current;
        }
        type = current.child;
        if (!checkScheduledUpdateOrContext(current, renderLanes2)) {
          var prevProps = type.memoizedProps;
          Component = Component.compare;
          Component = null !== Component ? Component : shallowEqual;
          if (Component(prevProps, nextProps) && current.ref === workInProgress2.ref)
            return bailoutOnAlreadyFinishedWork(current, workInProgress2, renderLanes2);
        }
        workInProgress2.flags |= 1;
        current = createWorkInProgress(type, nextProps);
        current.ref = workInProgress2.ref;
        current.return = workInProgress2;
        return workInProgress2.child = current;
      }
      function updateSimpleMemoComponent(current, workInProgress2, Component, nextProps, renderLanes2) {
        if (null !== current) {
          var prevProps = current.memoizedProps;
          if (shallowEqual(prevProps, nextProps) && current.ref === workInProgress2.ref)
            if (didReceiveUpdate = false, workInProgress2.pendingProps = nextProps = prevProps, checkScheduledUpdateOrContext(current, renderLanes2))
              0 !== (current.flags & 131072) && (didReceiveUpdate = true);
            else
              return workInProgress2.lanes = current.lanes, bailoutOnAlreadyFinishedWork(current, workInProgress2, renderLanes2);
        }
        return updateFunctionComponent(
          current,
          workInProgress2,
          Component,
          nextProps,
          renderLanes2
        );
      }
      function updateOffscreenComponent(current, workInProgress2, renderLanes2, nextProps) {
        var nextChildren = nextProps.children, prevState = null !== current ? current.memoizedState : null;
        null === current && null === workInProgress2.stateNode && (workInProgress2.stateNode = {
          _visibility: 1,
          _pendingMarkers: null,
          _retryCache: null,
          _transitions: null
        });
        if ("hidden" === nextProps.mode) {
          if (0 !== (workInProgress2.flags & 128)) {
            prevState = null !== prevState ? prevState.baseLanes | renderLanes2 : renderLanes2;
            if (null !== current) {
              nextProps = workInProgress2.child = current.child;
              for (nextChildren = 0; null !== nextProps; )
                nextChildren = nextChildren | nextProps.lanes | nextProps.childLanes, nextProps = nextProps.sibling;
              nextProps = nextChildren & ~prevState;
            } else nextProps = 0, workInProgress2.child = null;
            return deferHiddenOffscreenComponent(
              current,
              workInProgress2,
              prevState,
              renderLanes2,
              nextProps
            );
          }
          if (0 !== (renderLanes2 & 536870912))
            workInProgress2.memoizedState = { baseLanes: 0, cachePool: null }, null !== current && pushTransition(
              workInProgress2,
              null !== prevState ? prevState.cachePool : null
            ), null !== prevState ? pushHiddenContext(workInProgress2, prevState) : reuseHiddenContextOnStack(), pushOffscreenSuspenseHandler(workInProgress2);
          else
            return nextProps = workInProgress2.lanes = 536870912, deferHiddenOffscreenComponent(
              current,
              workInProgress2,
              null !== prevState ? prevState.baseLanes | renderLanes2 : renderLanes2,
              renderLanes2,
              nextProps
            );
        } else
          null !== prevState ? (pushTransition(workInProgress2, prevState.cachePool), pushHiddenContext(workInProgress2, prevState), reuseSuspenseHandlerOnStack(workInProgress2), workInProgress2.memoizedState = null) : (null !== current && pushTransition(workInProgress2, null), reuseHiddenContextOnStack(), reuseSuspenseHandlerOnStack(workInProgress2));
        reconcileChildren(current, workInProgress2, nextChildren, renderLanes2);
        return workInProgress2.child;
      }
      function bailoutOffscreenComponent(current, workInProgress2) {
        null !== current && 22 === current.tag || null !== workInProgress2.stateNode || (workInProgress2.stateNode = {
          _visibility: 1,
          _pendingMarkers: null,
          _retryCache: null,
          _transitions: null
        });
        return workInProgress2.sibling;
      }
      function deferHiddenOffscreenComponent(current, workInProgress2, nextBaseLanes, renderLanes2, remainingChildLanes) {
        var JSCompiler_inline_result = peekCacheFromPool();
        JSCompiler_inline_result = null === JSCompiler_inline_result ? null : { parent: CacheContext._currentValue, pool: JSCompiler_inline_result };
        workInProgress2.memoizedState = {
          baseLanes: nextBaseLanes,
          cachePool: JSCompiler_inline_result
        };
        null !== current && pushTransition(workInProgress2, null);
        reuseHiddenContextOnStack();
        pushOffscreenSuspenseHandler(workInProgress2);
        null !== current && propagateParentContextChanges(current, workInProgress2, renderLanes2, true);
        workInProgress2.childLanes = remainingChildLanes;
        return null;
      }
      function mountActivityChildren(workInProgress2, nextProps) {
        nextProps = mountWorkInProgressOffscreenFiber(
          { mode: nextProps.mode, children: nextProps.children },
          workInProgress2.mode
        );
        nextProps.ref = workInProgress2.ref;
        workInProgress2.child = nextProps;
        nextProps.return = workInProgress2;
        return nextProps;
      }
      function retryActivityComponentWithoutHydrating(current, workInProgress2, renderLanes2) {
        reconcileChildFibers(workInProgress2, current.child, null, renderLanes2);
        current = mountActivityChildren(workInProgress2, workInProgress2.pendingProps);
        current.flags |= 2;
        popSuspenseHandler(workInProgress2);
        workInProgress2.memoizedState = null;
        return current;
      }
      function updateActivityComponent(current, workInProgress2, renderLanes2) {
        var nextProps = workInProgress2.pendingProps, didSuspend = 0 !== (workInProgress2.flags & 128);
        workInProgress2.flags &= -129;
        if (null === current) {
          if (isHydrating) {
            if ("hidden" === nextProps.mode)
              return current = mountActivityChildren(workInProgress2, nextProps), workInProgress2.lanes = 536870912, bailoutOffscreenComponent(null, current);
            pushDehydratedActivitySuspenseHandler(workInProgress2);
            (current = nextHydratableInstance) ? (current = canHydrateHydrationBoundary(
              current,
              rootOrSingletonContext
            ), current = null !== current && "&" === current.data ? current : null, null !== current && (workInProgress2.memoizedState = {
              dehydrated: current,
              treeContext: null !== treeContextProvider ? { id: treeContextId, overflow: treeContextOverflow } : null,
              retryLane: 536870912,
              hydrationErrors: null
            }, renderLanes2 = createFiberFromDehydratedFragment(current), renderLanes2.return = workInProgress2, workInProgress2.child = renderLanes2, hydrationParentFiber = workInProgress2, nextHydratableInstance = null)) : current = null;
            if (null === current) throw throwOnHydrationMismatch(workInProgress2);
            workInProgress2.lanes = 536870912;
            return null;
          }
          return mountActivityChildren(workInProgress2, nextProps);
        }
        var prevState = current.memoizedState;
        if (null !== prevState) {
          var dehydrated = prevState.dehydrated;
          pushDehydratedActivitySuspenseHandler(workInProgress2);
          if (didSuspend)
            if (workInProgress2.flags & 256)
              workInProgress2.flags &= -257, workInProgress2 = retryActivityComponentWithoutHydrating(
                current,
                workInProgress2,
                renderLanes2
              );
            else if (null !== workInProgress2.memoizedState)
              workInProgress2.child = current.child, workInProgress2.flags |= 128, workInProgress2 = null;
            else throw Error(formatProdErrorMessage(558));
          else if (didReceiveUpdate || propagateParentContextChanges(current, workInProgress2, renderLanes2, false), didSuspend = 0 !== (renderLanes2 & current.childLanes), didReceiveUpdate || didSuspend) {
            nextProps = workInProgressRoot;
            if (null !== nextProps && (dehydrated = getBumpedLaneForHydration(nextProps, renderLanes2), 0 !== dehydrated && dehydrated !== prevState.retryLane))
              throw prevState.retryLane = dehydrated, enqueueConcurrentRenderForLane(current, dehydrated), scheduleUpdateOnFiber(nextProps, current, dehydrated), SelectiveHydrationException;
            renderDidSuspendDelayIfPossible();
            workInProgress2 = retryActivityComponentWithoutHydrating(
              current,
              workInProgress2,
              renderLanes2
            );
          } else
            current = prevState.treeContext, nextHydratableInstance = getNextHydratable(dehydrated.nextSibling), hydrationParentFiber = workInProgress2, isHydrating = true, hydrationErrors = null, rootOrSingletonContext = false, null !== current && restoreSuspendedTreeContext(workInProgress2, current), workInProgress2 = mountActivityChildren(workInProgress2, nextProps), workInProgress2.flags |= 4096;
          return workInProgress2;
        }
        current = createWorkInProgress(current.child, {
          mode: nextProps.mode,
          children: nextProps.children
        });
        current.ref = workInProgress2.ref;
        workInProgress2.child = current;
        current.return = workInProgress2;
        return current;
      }
      function markRef(current, workInProgress2) {
        var ref = workInProgress2.ref;
        if (null === ref)
          null !== current && null !== current.ref && (workInProgress2.flags |= 4194816);
        else {
          if ("function" !== typeof ref && "object" !== typeof ref)
            throw Error(formatProdErrorMessage(284));
          if (null === current || current.ref !== ref)
            workInProgress2.flags |= 4194816;
        }
      }
      function updateFunctionComponent(current, workInProgress2, Component, nextProps, renderLanes2) {
        prepareToReadContext(workInProgress2);
        Component = renderWithHooks(
          current,
          workInProgress2,
          Component,
          nextProps,
          void 0,
          renderLanes2
        );
        nextProps = checkDidRenderIdHook();
        if (null !== current && !didReceiveUpdate)
          return bailoutHooks(current, workInProgress2, renderLanes2), bailoutOnAlreadyFinishedWork(current, workInProgress2, renderLanes2);
        isHydrating && nextProps && pushMaterializedTreeId(workInProgress2);
        workInProgress2.flags |= 1;
        reconcileChildren(current, workInProgress2, Component, renderLanes2);
        return workInProgress2.child;
      }
      function replayFunctionComponent(current, workInProgress2, nextProps, Component, secondArg, renderLanes2) {
        prepareToReadContext(workInProgress2);
        workInProgress2.updateQueue = null;
        nextProps = renderWithHooksAgain(
          workInProgress2,
          Component,
          nextProps,
          secondArg
        );
        finishRenderingHooks(current);
        Component = checkDidRenderIdHook();
        if (null !== current && !didReceiveUpdate)
          return bailoutHooks(current, workInProgress2, renderLanes2), bailoutOnAlreadyFinishedWork(current, workInProgress2, renderLanes2);
        isHydrating && Component && pushMaterializedTreeId(workInProgress2);
        workInProgress2.flags |= 1;
        reconcileChildren(current, workInProgress2, nextProps, renderLanes2);
        return workInProgress2.child;
      }
      function updateClassComponent(current, workInProgress2, Component, nextProps, renderLanes2) {
        prepareToReadContext(workInProgress2);
        if (null === workInProgress2.stateNode) {
          var context = emptyContextObject, contextType = Component.contextType;
          "object" === typeof contextType && null !== contextType && (context = readContext(contextType));
          context = new Component(nextProps, context);
          workInProgress2.memoizedState = null !== context.state && void 0 !== context.state ? context.state : null;
          context.updater = classComponentUpdater;
          workInProgress2.stateNode = context;
          context._reactInternals = workInProgress2;
          context = workInProgress2.stateNode;
          context.props = nextProps;
          context.state = workInProgress2.memoizedState;
          context.refs = {};
          initializeUpdateQueue(workInProgress2);
          contextType = Component.contextType;
          context.context = "object" === typeof contextType && null !== contextType ? readContext(contextType) : emptyContextObject;
          context.state = workInProgress2.memoizedState;
          contextType = Component.getDerivedStateFromProps;
          "function" === typeof contextType && (applyDerivedStateFromProps(
            workInProgress2,
            Component,
            contextType,
            nextProps
          ), context.state = workInProgress2.memoizedState);
          "function" === typeof Component.getDerivedStateFromProps || "function" === typeof context.getSnapshotBeforeUpdate || "function" !== typeof context.UNSAFE_componentWillMount && "function" !== typeof context.componentWillMount || (contextType = context.state, "function" === typeof context.componentWillMount && context.componentWillMount(), "function" === typeof context.UNSAFE_componentWillMount && context.UNSAFE_componentWillMount(), contextType !== context.state && classComponentUpdater.enqueueReplaceState(context, context.state, null), processUpdateQueue(workInProgress2, nextProps, context, renderLanes2), suspendIfUpdateReadFromEntangledAsyncAction(), context.state = workInProgress2.memoizedState);
          "function" === typeof context.componentDidMount && (workInProgress2.flags |= 4194308);
          nextProps = true;
        } else if (null === current) {
          context = workInProgress2.stateNode;
          var unresolvedOldProps = workInProgress2.memoizedProps, oldProps = resolveClassComponentProps(Component, unresolvedOldProps);
          context.props = oldProps;
          var oldContext = context.context, contextType$jscomp$0 = Component.contextType;
          contextType = emptyContextObject;
          "object" === typeof contextType$jscomp$0 && null !== contextType$jscomp$0 && (contextType = readContext(contextType$jscomp$0));
          var getDerivedStateFromProps = Component.getDerivedStateFromProps;
          contextType$jscomp$0 = "function" === typeof getDerivedStateFromProps || "function" === typeof context.getSnapshotBeforeUpdate;
          unresolvedOldProps = workInProgress2.pendingProps !== unresolvedOldProps;
          contextType$jscomp$0 || "function" !== typeof context.UNSAFE_componentWillReceiveProps && "function" !== typeof context.componentWillReceiveProps || (unresolvedOldProps || oldContext !== contextType) && callComponentWillReceiveProps(
            workInProgress2,
            context,
            nextProps,
            contextType
          );
          hasForceUpdate = false;
          var oldState = workInProgress2.memoizedState;
          context.state = oldState;
          processUpdateQueue(workInProgress2, nextProps, context, renderLanes2);
          suspendIfUpdateReadFromEntangledAsyncAction();
          oldContext = workInProgress2.memoizedState;
          unresolvedOldProps || oldState !== oldContext || hasForceUpdate ? ("function" === typeof getDerivedStateFromProps && (applyDerivedStateFromProps(
            workInProgress2,
            Component,
            getDerivedStateFromProps,
            nextProps
          ), oldContext = workInProgress2.memoizedState), (oldProps = hasForceUpdate || checkShouldComponentUpdate(
            workInProgress2,
            Component,
            oldProps,
            nextProps,
            oldState,
            oldContext,
            contextType
          )) ? (contextType$jscomp$0 || "function" !== typeof context.UNSAFE_componentWillMount && "function" !== typeof context.componentWillMount || ("function" === typeof context.componentWillMount && context.componentWillMount(), "function" === typeof context.UNSAFE_componentWillMount && context.UNSAFE_componentWillMount()), "function" === typeof context.componentDidMount && (workInProgress2.flags |= 4194308)) : ("function" === typeof context.componentDidMount && (workInProgress2.flags |= 4194308), workInProgress2.memoizedProps = nextProps, workInProgress2.memoizedState = oldContext), context.props = nextProps, context.state = oldContext, context.context = contextType, nextProps = oldProps) : ("function" === typeof context.componentDidMount && (workInProgress2.flags |= 4194308), nextProps = false);
        } else {
          context = workInProgress2.stateNode;
          cloneUpdateQueue(current, workInProgress2);
          contextType = workInProgress2.memoizedProps;
          contextType$jscomp$0 = resolveClassComponentProps(Component, contextType);
          context.props = contextType$jscomp$0;
          getDerivedStateFromProps = workInProgress2.pendingProps;
          oldState = context.context;
          oldContext = Component.contextType;
          oldProps = emptyContextObject;
          "object" === typeof oldContext && null !== oldContext && (oldProps = readContext(oldContext));
          unresolvedOldProps = Component.getDerivedStateFromProps;
          (oldContext = "function" === typeof unresolvedOldProps || "function" === typeof context.getSnapshotBeforeUpdate) || "function" !== typeof context.UNSAFE_componentWillReceiveProps && "function" !== typeof context.componentWillReceiveProps || (contextType !== getDerivedStateFromProps || oldState !== oldProps) && callComponentWillReceiveProps(
            workInProgress2,
            context,
            nextProps,
            oldProps
          );
          hasForceUpdate = false;
          oldState = workInProgress2.memoizedState;
          context.state = oldState;
          processUpdateQueue(workInProgress2, nextProps, context, renderLanes2);
          suspendIfUpdateReadFromEntangledAsyncAction();
          var newState = workInProgress2.memoizedState;
          contextType !== getDerivedStateFromProps || oldState !== newState || hasForceUpdate || null !== current && null !== current.dependencies && checkIfContextChanged(current.dependencies) ? ("function" === typeof unresolvedOldProps && (applyDerivedStateFromProps(
            workInProgress2,
            Component,
            unresolvedOldProps,
            nextProps
          ), newState = workInProgress2.memoizedState), (contextType$jscomp$0 = hasForceUpdate || checkShouldComponentUpdate(
            workInProgress2,
            Component,
            contextType$jscomp$0,
            nextProps,
            oldState,
            newState,
            oldProps
          ) || null !== current && null !== current.dependencies && checkIfContextChanged(current.dependencies)) ? (oldContext || "function" !== typeof context.UNSAFE_componentWillUpdate && "function" !== typeof context.componentWillUpdate || ("function" === typeof context.componentWillUpdate && context.componentWillUpdate(nextProps, newState, oldProps), "function" === typeof context.UNSAFE_componentWillUpdate && context.UNSAFE_componentWillUpdate(
            nextProps,
            newState,
            oldProps
          )), "function" === typeof context.componentDidUpdate && (workInProgress2.flags |= 4), "function" === typeof context.getSnapshotBeforeUpdate && (workInProgress2.flags |= 1024)) : ("function" !== typeof context.componentDidUpdate || contextType === current.memoizedProps && oldState === current.memoizedState || (workInProgress2.flags |= 4), "function" !== typeof context.getSnapshotBeforeUpdate || contextType === current.memoizedProps && oldState === current.memoizedState || (workInProgress2.flags |= 1024), workInProgress2.memoizedProps = nextProps, workInProgress2.memoizedState = newState), context.props = nextProps, context.state = newState, context.context = oldProps, nextProps = contextType$jscomp$0) : ("function" !== typeof context.componentDidUpdate || contextType === current.memoizedProps && oldState === current.memoizedState || (workInProgress2.flags |= 4), "function" !== typeof context.getSnapshotBeforeUpdate || contextType === current.memoizedProps && oldState === current.memoizedState || (workInProgress2.flags |= 1024), nextProps = false);
        }
        context = nextProps;
        markRef(current, workInProgress2);
        nextProps = 0 !== (workInProgress2.flags & 128);
        context || nextProps ? (context = workInProgress2.stateNode, Component = nextProps && "function" !== typeof Component.getDerivedStateFromError ? null : context.render(), workInProgress2.flags |= 1, null !== current && nextProps ? (workInProgress2.child = reconcileChildFibers(
          workInProgress2,
          current.child,
          null,
          renderLanes2
        ), workInProgress2.child = reconcileChildFibers(
          workInProgress2,
          null,
          Component,
          renderLanes2
        )) : reconcileChildren(current, workInProgress2, Component, renderLanes2), workInProgress2.memoizedState = context.state, current = workInProgress2.child) : current = bailoutOnAlreadyFinishedWork(
          current,
          workInProgress2,
          renderLanes2
        );
        return current;
      }
      function mountHostRootWithoutHydrating(current, workInProgress2, nextChildren, renderLanes2) {
        resetHydrationState();
        workInProgress2.flags |= 256;
        reconcileChildren(current, workInProgress2, nextChildren, renderLanes2);
        return workInProgress2.child;
      }
      var SUSPENDED_MARKER = {
        dehydrated: null,
        treeContext: null,
        retryLane: 0,
        hydrationErrors: null
      };
      function mountSuspenseOffscreenState(renderLanes2) {
        return { baseLanes: renderLanes2, cachePool: getSuspendedCache() };
      }
      function getRemainingWorkInPrimaryTree(current, primaryTreeDidDefer, renderLanes2) {
        current = null !== current ? current.childLanes & ~renderLanes2 : 0;
        primaryTreeDidDefer && (current |= workInProgressDeferredLane);
        return current;
      }
      function updateSuspenseComponent(current, workInProgress2, renderLanes2) {
        var nextProps = workInProgress2.pendingProps, showFallback = false, didSuspend = 0 !== (workInProgress2.flags & 128), JSCompiler_temp;
        (JSCompiler_temp = didSuspend) || (JSCompiler_temp = null !== current && null === current.memoizedState ? false : 0 !== (suspenseStackCursor.current & 2));
        JSCompiler_temp && (showFallback = true, workInProgress2.flags &= -129);
        JSCompiler_temp = 0 !== (workInProgress2.flags & 32);
        workInProgress2.flags &= -33;
        if (null === current) {
          if (isHydrating) {
            showFallback ? pushPrimaryTreeSuspenseHandler(workInProgress2) : reuseSuspenseHandlerOnStack(workInProgress2);
            (current = nextHydratableInstance) ? (current = canHydrateHydrationBoundary(
              current,
              rootOrSingletonContext
            ), current = null !== current && "&" !== current.data ? current : null, null !== current && (workInProgress2.memoizedState = {
              dehydrated: current,
              treeContext: null !== treeContextProvider ? { id: treeContextId, overflow: treeContextOverflow } : null,
              retryLane: 536870912,
              hydrationErrors: null
            }, renderLanes2 = createFiberFromDehydratedFragment(current), renderLanes2.return = workInProgress2, workInProgress2.child = renderLanes2, hydrationParentFiber = workInProgress2, nextHydratableInstance = null)) : current = null;
            if (null === current) throw throwOnHydrationMismatch(workInProgress2);
            isSuspenseInstanceFallback(current) ? workInProgress2.lanes = 32 : workInProgress2.lanes = 536870912;
            return null;
          }
          var nextPrimaryChildren = nextProps.children;
          nextProps = nextProps.fallback;
          if (showFallback)
            return reuseSuspenseHandlerOnStack(workInProgress2), showFallback = workInProgress2.mode, nextPrimaryChildren = mountWorkInProgressOffscreenFiber(
              { mode: "hidden", children: nextPrimaryChildren },
              showFallback
            ), nextProps = createFiberFromFragment(
              nextProps,
              showFallback,
              renderLanes2,
              null
            ), nextPrimaryChildren.return = workInProgress2, nextProps.return = workInProgress2, nextPrimaryChildren.sibling = nextProps, workInProgress2.child = nextPrimaryChildren, nextProps = workInProgress2.child, nextProps.memoizedState = mountSuspenseOffscreenState(renderLanes2), nextProps.childLanes = getRemainingWorkInPrimaryTree(
              current,
              JSCompiler_temp,
              renderLanes2
            ), workInProgress2.memoizedState = SUSPENDED_MARKER, bailoutOffscreenComponent(null, nextProps);
          pushPrimaryTreeSuspenseHandler(workInProgress2);
          return mountSuspensePrimaryChildren(workInProgress2, nextPrimaryChildren);
        }
        var prevState = current.memoizedState;
        if (null !== prevState && (nextPrimaryChildren = prevState.dehydrated, null !== nextPrimaryChildren)) {
          if (didSuspend)
            workInProgress2.flags & 256 ? (pushPrimaryTreeSuspenseHandler(workInProgress2), workInProgress2.flags &= -257, workInProgress2 = retrySuspenseComponentWithoutHydrating(
              current,
              workInProgress2,
              renderLanes2
            )) : null !== workInProgress2.memoizedState ? (reuseSuspenseHandlerOnStack(workInProgress2), workInProgress2.child = current.child, workInProgress2.flags |= 128, workInProgress2 = null) : (reuseSuspenseHandlerOnStack(workInProgress2), nextPrimaryChildren = nextProps.fallback, showFallback = workInProgress2.mode, nextProps = mountWorkInProgressOffscreenFiber(
              { mode: "visible", children: nextProps.children },
              showFallback
            ), nextPrimaryChildren = createFiberFromFragment(
              nextPrimaryChildren,
              showFallback,
              renderLanes2,
              null
            ), nextPrimaryChildren.flags |= 2, nextProps.return = workInProgress2, nextPrimaryChildren.return = workInProgress2, nextProps.sibling = nextPrimaryChildren, workInProgress2.child = nextProps, reconcileChildFibers(
              workInProgress2,
              current.child,
              null,
              renderLanes2
            ), nextProps = workInProgress2.child, nextProps.memoizedState = mountSuspenseOffscreenState(renderLanes2), nextProps.childLanes = getRemainingWorkInPrimaryTree(
              current,
              JSCompiler_temp,
              renderLanes2
            ), workInProgress2.memoizedState = SUSPENDED_MARKER, workInProgress2 = bailoutOffscreenComponent(null, nextProps));
          else if (pushPrimaryTreeSuspenseHandler(workInProgress2), isSuspenseInstanceFallback(nextPrimaryChildren)) {
            JSCompiler_temp = nextPrimaryChildren.nextSibling && nextPrimaryChildren.nextSibling.dataset;
            if (JSCompiler_temp) var digest = JSCompiler_temp.dgst;
            JSCompiler_temp = digest;
            nextProps = Error(formatProdErrorMessage(419));
            nextProps.stack = "";
            nextProps.digest = JSCompiler_temp;
            queueHydrationError({ value: nextProps, source: null, stack: null });
            workInProgress2 = retrySuspenseComponentWithoutHydrating(
              current,
              workInProgress2,
              renderLanes2
            );
          } else if (didReceiveUpdate || propagateParentContextChanges(current, workInProgress2, renderLanes2, false), JSCompiler_temp = 0 !== (renderLanes2 & current.childLanes), didReceiveUpdate || JSCompiler_temp) {
            JSCompiler_temp = workInProgressRoot;
            if (null !== JSCompiler_temp && (nextProps = getBumpedLaneForHydration(JSCompiler_temp, renderLanes2), 0 !== nextProps && nextProps !== prevState.retryLane))
              throw prevState.retryLane = nextProps, enqueueConcurrentRenderForLane(current, nextProps), scheduleUpdateOnFiber(JSCompiler_temp, current, nextProps), SelectiveHydrationException;
            isSuspenseInstancePending(nextPrimaryChildren) || renderDidSuspendDelayIfPossible();
            workInProgress2 = retrySuspenseComponentWithoutHydrating(
              current,
              workInProgress2,
              renderLanes2
            );
          } else
            isSuspenseInstancePending(nextPrimaryChildren) ? (workInProgress2.flags |= 192, workInProgress2.child = current.child, workInProgress2 = null) : (current = prevState.treeContext, nextHydratableInstance = getNextHydratable(
              nextPrimaryChildren.nextSibling
            ), hydrationParentFiber = workInProgress2, isHydrating = true, hydrationErrors = null, rootOrSingletonContext = false, null !== current && restoreSuspendedTreeContext(workInProgress2, current), workInProgress2 = mountSuspensePrimaryChildren(
              workInProgress2,
              nextProps.children
            ), workInProgress2.flags |= 4096);
          return workInProgress2;
        }
        if (showFallback)
          return reuseSuspenseHandlerOnStack(workInProgress2), nextPrimaryChildren = nextProps.fallback, showFallback = workInProgress2.mode, prevState = current.child, digest = prevState.sibling, nextProps = createWorkInProgress(prevState, {
            mode: "hidden",
            children: nextProps.children
          }), nextProps.subtreeFlags = prevState.subtreeFlags & 65011712, null !== digest ? nextPrimaryChildren = createWorkInProgress(
            digest,
            nextPrimaryChildren
          ) : (nextPrimaryChildren = createFiberFromFragment(
            nextPrimaryChildren,
            showFallback,
            renderLanes2,
            null
          ), nextPrimaryChildren.flags |= 2), nextPrimaryChildren.return = workInProgress2, nextProps.return = workInProgress2, nextProps.sibling = nextPrimaryChildren, workInProgress2.child = nextProps, bailoutOffscreenComponent(null, nextProps), nextProps = workInProgress2.child, nextPrimaryChildren = current.child.memoizedState, null === nextPrimaryChildren ? nextPrimaryChildren = mountSuspenseOffscreenState(renderLanes2) : (showFallback = nextPrimaryChildren.cachePool, null !== showFallback ? (prevState = CacheContext._currentValue, showFallback = showFallback.parent !== prevState ? { parent: prevState, pool: prevState } : showFallback) : showFallback = getSuspendedCache(), nextPrimaryChildren = {
            baseLanes: nextPrimaryChildren.baseLanes | renderLanes2,
            cachePool: showFallback
          }), nextProps.memoizedState = nextPrimaryChildren, nextProps.childLanes = getRemainingWorkInPrimaryTree(
            current,
            JSCompiler_temp,
            renderLanes2
          ), workInProgress2.memoizedState = SUSPENDED_MARKER, bailoutOffscreenComponent(current.child, nextProps);
        pushPrimaryTreeSuspenseHandler(workInProgress2);
        renderLanes2 = current.child;
        current = renderLanes2.sibling;
        renderLanes2 = createWorkInProgress(renderLanes2, {
          mode: "visible",
          children: nextProps.children
        });
        renderLanes2.return = workInProgress2;
        renderLanes2.sibling = null;
        null !== current && (JSCompiler_temp = workInProgress2.deletions, null === JSCompiler_temp ? (workInProgress2.deletions = [current], workInProgress2.flags |= 16) : JSCompiler_temp.push(current));
        workInProgress2.child = renderLanes2;
        workInProgress2.memoizedState = null;
        return renderLanes2;
      }
      function mountSuspensePrimaryChildren(workInProgress2, primaryChildren) {
        primaryChildren = mountWorkInProgressOffscreenFiber(
          { mode: "visible", children: primaryChildren },
          workInProgress2.mode
        );
        primaryChildren.return = workInProgress2;
        return workInProgress2.child = primaryChildren;
      }
      function mountWorkInProgressOffscreenFiber(offscreenProps, mode) {
        offscreenProps = createFiberImplClass(22, offscreenProps, null, mode);
        offscreenProps.lanes = 0;
        return offscreenProps;
      }
      function retrySuspenseComponentWithoutHydrating(current, workInProgress2, renderLanes2) {
        reconcileChildFibers(workInProgress2, current.child, null, renderLanes2);
        current = mountSuspensePrimaryChildren(
          workInProgress2,
          workInProgress2.pendingProps.children
        );
        current.flags |= 2;
        workInProgress2.memoizedState = null;
        return current;
      }
      function scheduleSuspenseWorkOnFiber(fiber, renderLanes2, propagationRoot) {
        fiber.lanes |= renderLanes2;
        var alternate = fiber.alternate;
        null !== alternate && (alternate.lanes |= renderLanes2);
        scheduleContextWorkOnParentPath(fiber.return, renderLanes2, propagationRoot);
      }
      function initSuspenseListRenderState(workInProgress2, isBackwards, tail, lastContentRow, tailMode, treeForkCount2) {
        var renderState = workInProgress2.memoizedState;
        null === renderState ? workInProgress2.memoizedState = {
          isBackwards,
          rendering: null,
          renderingStartTime: 0,
          last: lastContentRow,
          tail,
          tailMode,
          treeForkCount: treeForkCount2
        } : (renderState.isBackwards = isBackwards, renderState.rendering = null, renderState.renderingStartTime = 0, renderState.last = lastContentRow, renderState.tail = tail, renderState.tailMode = tailMode, renderState.treeForkCount = treeForkCount2);
      }
      function updateSuspenseListComponent(current, workInProgress2, renderLanes2) {
        var nextProps = workInProgress2.pendingProps, revealOrder = nextProps.revealOrder, tailMode = nextProps.tail;
        nextProps = nextProps.children;
        var suspenseContext = suspenseStackCursor.current, shouldForceFallback = 0 !== (suspenseContext & 2);
        shouldForceFallback ? (suspenseContext = suspenseContext & 1 | 2, workInProgress2.flags |= 128) : suspenseContext &= 1;
        push(suspenseStackCursor, suspenseContext);
        reconcileChildren(current, workInProgress2, nextProps, renderLanes2);
        nextProps = isHydrating ? treeForkCount : 0;
        if (!shouldForceFallback && null !== current && 0 !== (current.flags & 128))
          a: for (current = workInProgress2.child; null !== current; ) {
            if (13 === current.tag)
              null !== current.memoizedState && scheduleSuspenseWorkOnFiber(current, renderLanes2, workInProgress2);
            else if (19 === current.tag)
              scheduleSuspenseWorkOnFiber(current, renderLanes2, workInProgress2);
            else if (null !== current.child) {
              current.child.return = current;
              current = current.child;
              continue;
            }
            if (current === workInProgress2) break a;
            for (; null === current.sibling; ) {
              if (null === current.return || current.return === workInProgress2)
                break a;
              current = current.return;
            }
            current.sibling.return = current.return;
            current = current.sibling;
          }
        switch (revealOrder) {
          case "forwards":
            renderLanes2 = workInProgress2.child;
            for (revealOrder = null; null !== renderLanes2; )
              current = renderLanes2.alternate, null !== current && null === findFirstSuspended(current) && (revealOrder = renderLanes2), renderLanes2 = renderLanes2.sibling;
            renderLanes2 = revealOrder;
            null === renderLanes2 ? (revealOrder = workInProgress2.child, workInProgress2.child = null) : (revealOrder = renderLanes2.sibling, renderLanes2.sibling = null);
            initSuspenseListRenderState(
              workInProgress2,
              false,
              revealOrder,
              renderLanes2,
              tailMode,
              nextProps
            );
            break;
          case "backwards":
          case "unstable_legacy-backwards":
            renderLanes2 = null;
            revealOrder = workInProgress2.child;
            for (workInProgress2.child = null; null !== revealOrder; ) {
              current = revealOrder.alternate;
              if (null !== current && null === findFirstSuspended(current)) {
                workInProgress2.child = revealOrder;
                break;
              }
              current = revealOrder.sibling;
              revealOrder.sibling = renderLanes2;
              renderLanes2 = revealOrder;
              revealOrder = current;
            }
            initSuspenseListRenderState(
              workInProgress2,
              true,
              renderLanes2,
              null,
              tailMode,
              nextProps
            );
            break;
          case "together":
            initSuspenseListRenderState(
              workInProgress2,
              false,
              null,
              null,
              void 0,
              nextProps
            );
            break;
          default:
            workInProgress2.memoizedState = null;
        }
        return workInProgress2.child;
      }
      function bailoutOnAlreadyFinishedWork(current, workInProgress2, renderLanes2) {
        null !== current && (workInProgress2.dependencies = current.dependencies);
        workInProgressRootSkippedLanes |= workInProgress2.lanes;
        if (0 === (renderLanes2 & workInProgress2.childLanes))
          if (null !== current) {
            if (propagateParentContextChanges(
              current,
              workInProgress2,
              renderLanes2,
              false
            ), 0 === (renderLanes2 & workInProgress2.childLanes))
              return null;
          } else return null;
        if (null !== current && workInProgress2.child !== current.child)
          throw Error(formatProdErrorMessage(153));
        if (null !== workInProgress2.child) {
          current = workInProgress2.child;
          renderLanes2 = createWorkInProgress(current, current.pendingProps);
          workInProgress2.child = renderLanes2;
          for (renderLanes2.return = workInProgress2; null !== current.sibling; )
            current = current.sibling, renderLanes2 = renderLanes2.sibling = createWorkInProgress(current, current.pendingProps), renderLanes2.return = workInProgress2;
          renderLanes2.sibling = null;
        }
        return workInProgress2.child;
      }
      function checkScheduledUpdateOrContext(current, renderLanes2) {
        if (0 !== (current.lanes & renderLanes2)) return true;
        current = current.dependencies;
        return null !== current && checkIfContextChanged(current) ? true : false;
      }
      function attemptEarlyBailoutIfNoScheduledUpdate(current, workInProgress2, renderLanes2) {
        switch (workInProgress2.tag) {
          case 3:
            pushHostContainer(workInProgress2, workInProgress2.stateNode.containerInfo);
            pushProvider(workInProgress2, CacheContext, current.memoizedState.cache);
            resetHydrationState();
            break;
          case 27:
          case 5:
            pushHostContext(workInProgress2);
            break;
          case 4:
            pushHostContainer(workInProgress2, workInProgress2.stateNode.containerInfo);
            break;
          case 10:
            pushProvider(
              workInProgress2,
              workInProgress2.type,
              workInProgress2.memoizedProps.value
            );
            break;
          case 31:
            if (null !== workInProgress2.memoizedState)
              return workInProgress2.flags |= 128, pushDehydratedActivitySuspenseHandler(workInProgress2), null;
            break;
          case 13:
            var state$102 = workInProgress2.memoizedState;
            if (null !== state$102) {
              if (null !== state$102.dehydrated)
                return pushPrimaryTreeSuspenseHandler(workInProgress2), workInProgress2.flags |= 128, null;
              if (0 !== (renderLanes2 & workInProgress2.child.childLanes))
                return updateSuspenseComponent(current, workInProgress2, renderLanes2);
              pushPrimaryTreeSuspenseHandler(workInProgress2);
              current = bailoutOnAlreadyFinishedWork(
                current,
                workInProgress2,
                renderLanes2
              );
              return null !== current ? current.sibling : null;
            }
            pushPrimaryTreeSuspenseHandler(workInProgress2);
            break;
          case 19:
            var didSuspendBefore = 0 !== (current.flags & 128);
            state$102 = 0 !== (renderLanes2 & workInProgress2.childLanes);
            state$102 || (propagateParentContextChanges(
              current,
              workInProgress2,
              renderLanes2,
              false
            ), state$102 = 0 !== (renderLanes2 & workInProgress2.childLanes));
            if (didSuspendBefore) {
              if (state$102)
                return updateSuspenseListComponent(
                  current,
                  workInProgress2,
                  renderLanes2
                );
              workInProgress2.flags |= 128;
            }
            didSuspendBefore = workInProgress2.memoizedState;
            null !== didSuspendBefore && (didSuspendBefore.rendering = null, didSuspendBefore.tail = null, didSuspendBefore.lastEffect = null);
            push(suspenseStackCursor, suspenseStackCursor.current);
            if (state$102) break;
            else return null;
          case 22:
            return workInProgress2.lanes = 0, updateOffscreenComponent(
              current,
              workInProgress2,
              renderLanes2,
              workInProgress2.pendingProps
            );
          case 24:
            pushProvider(workInProgress2, CacheContext, current.memoizedState.cache);
        }
        return bailoutOnAlreadyFinishedWork(current, workInProgress2, renderLanes2);
      }
      function beginWork(current, workInProgress2, renderLanes2) {
        if (null !== current)
          if (current.memoizedProps !== workInProgress2.pendingProps)
            didReceiveUpdate = true;
          else {
            if (!checkScheduledUpdateOrContext(current, renderLanes2) && 0 === (workInProgress2.flags & 128))
              return didReceiveUpdate = false, attemptEarlyBailoutIfNoScheduledUpdate(
                current,
                workInProgress2,
                renderLanes2
              );
            didReceiveUpdate = 0 !== (current.flags & 131072) ? true : false;
          }
        else
          didReceiveUpdate = false, isHydrating && 0 !== (workInProgress2.flags & 1048576) && pushTreeId(workInProgress2, treeForkCount, workInProgress2.index);
        workInProgress2.lanes = 0;
        switch (workInProgress2.tag) {
          case 16:
            a: {
              var props = workInProgress2.pendingProps;
              current = resolveLazy(workInProgress2.elementType);
              workInProgress2.type = current;
              if ("function" === typeof current)
                shouldConstruct(current) ? (props = resolveClassComponentProps(current, props), workInProgress2.tag = 1, workInProgress2 = updateClassComponent(
                  null,
                  workInProgress2,
                  current,
                  props,
                  renderLanes2
                )) : (workInProgress2.tag = 0, workInProgress2 = updateFunctionComponent(
                  null,
                  workInProgress2,
                  current,
                  props,
                  renderLanes2
                ));
              else {
                if (void 0 !== current && null !== current) {
                  var $$typeof = current.$$typeof;
                  if ($$typeof === REACT_FORWARD_REF_TYPE) {
                    workInProgress2.tag = 11;
                    workInProgress2 = updateForwardRef(
                      null,
                      workInProgress2,
                      current,
                      props,
                      renderLanes2
                    );
                    break a;
                  } else if ($$typeof === REACT_MEMO_TYPE) {
                    workInProgress2.tag = 14;
                    workInProgress2 = updateMemoComponent(
                      null,
                      workInProgress2,
                      current,
                      props,
                      renderLanes2
                    );
                    break a;
                  }
                }
                workInProgress2 = getComponentNameFromType(current) || current;
                throw Error(formatProdErrorMessage(306, workInProgress2, ""));
              }
            }
            return workInProgress2;
          case 0:
            return updateFunctionComponent(
              current,
              workInProgress2,
              workInProgress2.type,
              workInProgress2.pendingProps,
              renderLanes2
            );
          case 1:
            return props = workInProgress2.type, $$typeof = resolveClassComponentProps(
              props,
              workInProgress2.pendingProps
            ), updateClassComponent(
              current,
              workInProgress2,
              props,
              $$typeof,
              renderLanes2
            );
          case 3:
            a: {
              pushHostContainer(
                workInProgress2,
                workInProgress2.stateNode.containerInfo
              );
              if (null === current) throw Error(formatProdErrorMessage(387));
              props = workInProgress2.pendingProps;
              var prevState = workInProgress2.memoizedState;
              $$typeof = prevState.element;
              cloneUpdateQueue(current, workInProgress2);
              processUpdateQueue(workInProgress2, props, null, renderLanes2);
              var nextState = workInProgress2.memoizedState;
              props = nextState.cache;
              pushProvider(workInProgress2, CacheContext, props);
              props !== prevState.cache && propagateContextChanges(
                workInProgress2,
                [CacheContext],
                renderLanes2,
                true
              );
              suspendIfUpdateReadFromEntangledAsyncAction();
              props = nextState.element;
              if (prevState.isDehydrated)
                if (prevState = {
                  element: props,
                  isDehydrated: false,
                  cache: nextState.cache
                }, workInProgress2.updateQueue.baseState = prevState, workInProgress2.memoizedState = prevState, workInProgress2.flags & 256) {
                  workInProgress2 = mountHostRootWithoutHydrating(
                    current,
                    workInProgress2,
                    props,
                    renderLanes2
                  );
                  break a;
                } else if (props !== $$typeof) {
                  $$typeof = createCapturedValueAtFiber(
                    Error(formatProdErrorMessage(424)),
                    workInProgress2
                  );
                  queueHydrationError($$typeof);
                  workInProgress2 = mountHostRootWithoutHydrating(
                    current,
                    workInProgress2,
                    props,
                    renderLanes2
                  );
                  break a;
                } else {
                  current = workInProgress2.stateNode.containerInfo;
                  switch (current.nodeType) {
                    case 9:
                      current = current.body;
                      break;
                    default:
                      current = "HTML" === current.nodeName ? current.ownerDocument.body : current;
                  }
                  nextHydratableInstance = getNextHydratable(current.firstChild);
                  hydrationParentFiber = workInProgress2;
                  isHydrating = true;
                  hydrationErrors = null;
                  rootOrSingletonContext = true;
                  renderLanes2 = mountChildFibers(
                    workInProgress2,
                    null,
                    props,
                    renderLanes2
                  );
                  for (workInProgress2.child = renderLanes2; renderLanes2; )
                    renderLanes2.flags = renderLanes2.flags & -3 | 4096, renderLanes2 = renderLanes2.sibling;
                }
              else {
                resetHydrationState();
                if (props === $$typeof) {
                  workInProgress2 = bailoutOnAlreadyFinishedWork(
                    current,
                    workInProgress2,
                    renderLanes2
                  );
                  break a;
                }
                reconcileChildren(current, workInProgress2, props, renderLanes2);
              }
              workInProgress2 = workInProgress2.child;
            }
            return workInProgress2;
          case 26:
            return markRef(current, workInProgress2), null === current ? (renderLanes2 = getResource(
              workInProgress2.type,
              null,
              workInProgress2.pendingProps,
              null
            )) ? workInProgress2.memoizedState = renderLanes2 : isHydrating || (renderLanes2 = workInProgress2.type, current = workInProgress2.pendingProps, props = getOwnerDocumentFromRootContainer(
              rootInstanceStackCursor.current
            ).createElement(renderLanes2), props[internalInstanceKey] = workInProgress2, props[internalPropsKey] = current, setInitialProperties(props, renderLanes2, current), markNodeAsHoistable(props), workInProgress2.stateNode = props) : workInProgress2.memoizedState = getResource(
              workInProgress2.type,
              current.memoizedProps,
              workInProgress2.pendingProps,
              current.memoizedState
            ), null;
          case 27:
            return pushHostContext(workInProgress2), null === current && isHydrating && (props = workInProgress2.stateNode = resolveSingletonInstance(
              workInProgress2.type,
              workInProgress2.pendingProps,
              rootInstanceStackCursor.current
            ), hydrationParentFiber = workInProgress2, rootOrSingletonContext = true, $$typeof = nextHydratableInstance, isSingletonScope(workInProgress2.type) ? (previousHydratableOnEnteringScopedSingleton = $$typeof, nextHydratableInstance = getNextHydratable(props.firstChild)) : nextHydratableInstance = $$typeof), reconcileChildren(
              current,
              workInProgress2,
              workInProgress2.pendingProps.children,
              renderLanes2
            ), markRef(current, workInProgress2), null === current && (workInProgress2.flags |= 4194304), workInProgress2.child;
          case 5:
            if (null === current && isHydrating) {
              if ($$typeof = props = nextHydratableInstance)
                props = canHydrateInstance(
                  props,
                  workInProgress2.type,
                  workInProgress2.pendingProps,
                  rootOrSingletonContext
                ), null !== props ? (workInProgress2.stateNode = props, hydrationParentFiber = workInProgress2, nextHydratableInstance = getNextHydratable(props.firstChild), rootOrSingletonContext = false, $$typeof = true) : $$typeof = false;
              $$typeof || throwOnHydrationMismatch(workInProgress2);
            }
            pushHostContext(workInProgress2);
            $$typeof = workInProgress2.type;
            prevState = workInProgress2.pendingProps;
            nextState = null !== current ? current.memoizedProps : null;
            props = prevState.children;
            shouldSetTextContent($$typeof, prevState) ? props = null : null !== nextState && shouldSetTextContent($$typeof, nextState) && (workInProgress2.flags |= 32);
            null !== workInProgress2.memoizedState && ($$typeof = renderWithHooks(
              current,
              workInProgress2,
              TransitionAwareHostComponent,
              null,
              null,
              renderLanes2
            ), HostTransitionContext._currentValue = $$typeof);
            markRef(current, workInProgress2);
            reconcileChildren(current, workInProgress2, props, renderLanes2);
            return workInProgress2.child;
          case 6:
            if (null === current && isHydrating) {
              if (current = renderLanes2 = nextHydratableInstance)
                renderLanes2 = canHydrateTextInstance(
                  renderLanes2,
                  workInProgress2.pendingProps,
                  rootOrSingletonContext
                ), null !== renderLanes2 ? (workInProgress2.stateNode = renderLanes2, hydrationParentFiber = workInProgress2, nextHydratableInstance = null, current = true) : current = false;
              current || throwOnHydrationMismatch(workInProgress2);
            }
            return null;
          case 13:
            return updateSuspenseComponent(current, workInProgress2, renderLanes2);
          case 4:
            return pushHostContainer(
              workInProgress2,
              workInProgress2.stateNode.containerInfo
            ), props = workInProgress2.pendingProps, null === current ? workInProgress2.child = reconcileChildFibers(
              workInProgress2,
              null,
              props,
              renderLanes2
            ) : reconcileChildren(current, workInProgress2, props, renderLanes2), workInProgress2.child;
          case 11:
            return updateForwardRef(
              current,
              workInProgress2,
              workInProgress2.type,
              workInProgress2.pendingProps,
              renderLanes2
            );
          case 7:
            return reconcileChildren(
              current,
              workInProgress2,
              workInProgress2.pendingProps,
              renderLanes2
            ), workInProgress2.child;
          case 8:
            return reconcileChildren(
              current,
              workInProgress2,
              workInProgress2.pendingProps.children,
              renderLanes2
            ), workInProgress2.child;
          case 12:
            return reconcileChildren(
              current,
              workInProgress2,
              workInProgress2.pendingProps.children,
              renderLanes2
            ), workInProgress2.child;
          case 10:
            return props = workInProgress2.pendingProps, pushProvider(workInProgress2, workInProgress2.type, props.value), reconcileChildren(current, workInProgress2, props.children, renderLanes2), workInProgress2.child;
          case 9:
            return $$typeof = workInProgress2.type._context, props = workInProgress2.pendingProps.children, prepareToReadContext(workInProgress2), $$typeof = readContext($$typeof), props = props($$typeof), workInProgress2.flags |= 1, reconcileChildren(current, workInProgress2, props, renderLanes2), workInProgress2.child;
          case 14:
            return updateMemoComponent(
              current,
              workInProgress2,
              workInProgress2.type,
              workInProgress2.pendingProps,
              renderLanes2
            );
          case 15:
            return updateSimpleMemoComponent(
              current,
              workInProgress2,
              workInProgress2.type,
              workInProgress2.pendingProps,
              renderLanes2
            );
          case 19:
            return updateSuspenseListComponent(current, workInProgress2, renderLanes2);
          case 31:
            return updateActivityComponent(current, workInProgress2, renderLanes2);
          case 22:
            return updateOffscreenComponent(
              current,
              workInProgress2,
              renderLanes2,
              workInProgress2.pendingProps
            );
          case 24:
            return prepareToReadContext(workInProgress2), props = readContext(CacheContext), null === current ? ($$typeof = peekCacheFromPool(), null === $$typeof && ($$typeof = workInProgressRoot, prevState = createCache(), $$typeof.pooledCache = prevState, prevState.refCount++, null !== prevState && ($$typeof.pooledCacheLanes |= renderLanes2), $$typeof = prevState), workInProgress2.memoizedState = { parent: props, cache: $$typeof }, initializeUpdateQueue(workInProgress2), pushProvider(workInProgress2, CacheContext, $$typeof)) : (0 !== (current.lanes & renderLanes2) && (cloneUpdateQueue(current, workInProgress2), processUpdateQueue(workInProgress2, null, null, renderLanes2), suspendIfUpdateReadFromEntangledAsyncAction()), $$typeof = current.memoizedState, prevState = workInProgress2.memoizedState, $$typeof.parent !== props ? ($$typeof = { parent: props, cache: props }, workInProgress2.memoizedState = $$typeof, 0 === workInProgress2.lanes && (workInProgress2.memoizedState = workInProgress2.updateQueue.baseState = $$typeof), pushProvider(workInProgress2, CacheContext, props)) : (props = prevState.cache, pushProvider(workInProgress2, CacheContext, props), props !== $$typeof.cache && propagateContextChanges(
              workInProgress2,
              [CacheContext],
              renderLanes2,
              true
            ))), reconcileChildren(
              current,
              workInProgress2,
              workInProgress2.pendingProps.children,
              renderLanes2
            ), workInProgress2.child;
          case 29:
            throw workInProgress2.pendingProps;
        }
        throw Error(formatProdErrorMessage(156, workInProgress2.tag));
      }
      function markUpdate(workInProgress2) {
        workInProgress2.flags |= 4;
      }
      function preloadInstanceAndSuspendIfNeeded(workInProgress2, type, oldProps, newProps, renderLanes2) {
        if (type = 0 !== (workInProgress2.mode & 32)) type = false;
        if (type) {
          if (workInProgress2.flags |= 16777216, (renderLanes2 & 335544128) === renderLanes2)
            if (workInProgress2.stateNode.complete) workInProgress2.flags |= 8192;
            else if (shouldRemainOnPreviousScreen()) workInProgress2.flags |= 8192;
            else
              throw suspendedThenable = noopSuspenseyCommitThenable, SuspenseyCommitException;
        } else workInProgress2.flags &= -16777217;
      }
      function preloadResourceAndSuspendIfNeeded(workInProgress2, resource) {
        if ("stylesheet" !== resource.type || 0 !== (resource.state.loading & 4))
          workInProgress2.flags &= -16777217;
        else if (workInProgress2.flags |= 16777216, !preloadResource(resource))
          if (shouldRemainOnPreviousScreen()) workInProgress2.flags |= 8192;
          else
            throw suspendedThenable = noopSuspenseyCommitThenable, SuspenseyCommitException;
      }
      function scheduleRetryEffect(workInProgress2, retryQueue) {
        null !== retryQueue && (workInProgress2.flags |= 4);
        workInProgress2.flags & 16384 && (retryQueue = 22 !== workInProgress2.tag ? claimNextRetryLane() : 536870912, workInProgress2.lanes |= retryQueue, workInProgressSuspendedRetryLanes |= retryQueue);
      }
      function cutOffTailIfNeeded(renderState, hasRenderedATailFallback) {
        if (!isHydrating)
          switch (renderState.tailMode) {
            case "hidden":
              hasRenderedATailFallback = renderState.tail;
              for (var lastTailNode = null; null !== hasRenderedATailFallback; )
                null !== hasRenderedATailFallback.alternate && (lastTailNode = hasRenderedATailFallback), hasRenderedATailFallback = hasRenderedATailFallback.sibling;
              null === lastTailNode ? renderState.tail = null : lastTailNode.sibling = null;
              break;
            case "collapsed":
              lastTailNode = renderState.tail;
              for (var lastTailNode$106 = null; null !== lastTailNode; )
                null !== lastTailNode.alternate && (lastTailNode$106 = lastTailNode), lastTailNode = lastTailNode.sibling;
              null === lastTailNode$106 ? hasRenderedATailFallback || null === renderState.tail ? renderState.tail = null : renderState.tail.sibling = null : lastTailNode$106.sibling = null;
          }
      }
      function bubbleProperties(completedWork) {
        var didBailout = null !== completedWork.alternate && completedWork.alternate.child === completedWork.child, newChildLanes = 0, subtreeFlags = 0;
        if (didBailout)
          for (var child$107 = completedWork.child; null !== child$107; )
            newChildLanes |= child$107.lanes | child$107.childLanes, subtreeFlags |= child$107.subtreeFlags & 65011712, subtreeFlags |= child$107.flags & 65011712, child$107.return = completedWork, child$107 = child$107.sibling;
        else
          for (child$107 = completedWork.child; null !== child$107; )
            newChildLanes |= child$107.lanes | child$107.childLanes, subtreeFlags |= child$107.subtreeFlags, subtreeFlags |= child$107.flags, child$107.return = completedWork, child$107 = child$107.sibling;
        completedWork.subtreeFlags |= subtreeFlags;
        completedWork.childLanes = newChildLanes;
        return didBailout;
      }
      function completeWork(current, workInProgress2, renderLanes2) {
        var newProps = workInProgress2.pendingProps;
        popTreeContext(workInProgress2);
        switch (workInProgress2.tag) {
          case 16:
          case 15:
          case 0:
          case 11:
          case 7:
          case 8:
          case 12:
          case 9:
          case 14:
            return bubbleProperties(workInProgress2), null;
          case 1:
            return bubbleProperties(workInProgress2), null;
          case 3:
            renderLanes2 = workInProgress2.stateNode;
            newProps = null;
            null !== current && (newProps = current.memoizedState.cache);
            workInProgress2.memoizedState.cache !== newProps && (workInProgress2.flags |= 2048);
            popProvider(CacheContext);
            popHostContainer();
            renderLanes2.pendingContext && (renderLanes2.context = renderLanes2.pendingContext, renderLanes2.pendingContext = null);
            if (null === current || null === current.child)
              popHydrationState(workInProgress2) ? markUpdate(workInProgress2) : null === current || current.memoizedState.isDehydrated && 0 === (workInProgress2.flags & 256) || (workInProgress2.flags |= 1024, upgradeHydrationErrorsToRecoverable());
            bubbleProperties(workInProgress2);
            return null;
          case 26:
            var type = workInProgress2.type, nextResource = workInProgress2.memoizedState;
            null === current ? (markUpdate(workInProgress2), null !== nextResource ? (bubbleProperties(workInProgress2), preloadResourceAndSuspendIfNeeded(workInProgress2, nextResource)) : (bubbleProperties(workInProgress2), preloadInstanceAndSuspendIfNeeded(
              workInProgress2,
              type,
              null,
              newProps,
              renderLanes2
            ))) : nextResource ? nextResource !== current.memoizedState ? (markUpdate(workInProgress2), bubbleProperties(workInProgress2), preloadResourceAndSuspendIfNeeded(workInProgress2, nextResource)) : (bubbleProperties(workInProgress2), workInProgress2.flags &= -16777217) : (current = current.memoizedProps, current !== newProps && markUpdate(workInProgress2), bubbleProperties(workInProgress2), preloadInstanceAndSuspendIfNeeded(
              workInProgress2,
              type,
              current,
              newProps,
              renderLanes2
            ));
            return null;
          case 27:
            popHostContext(workInProgress2);
            renderLanes2 = rootInstanceStackCursor.current;
            type = workInProgress2.type;
            if (null !== current && null != workInProgress2.stateNode)
              current.memoizedProps !== newProps && markUpdate(workInProgress2);
            else {
              if (!newProps) {
                if (null === workInProgress2.stateNode)
                  throw Error(formatProdErrorMessage(166));
                bubbleProperties(workInProgress2);
                return null;
              }
              current = contextStackCursor.current;
              popHydrationState(workInProgress2) ? prepareToHydrateHostInstance(workInProgress2, current) : (current = resolveSingletonInstance(type, newProps, renderLanes2), workInProgress2.stateNode = current, markUpdate(workInProgress2));
            }
            bubbleProperties(workInProgress2);
            return null;
          case 5:
            popHostContext(workInProgress2);
            type = workInProgress2.type;
            if (null !== current && null != workInProgress2.stateNode)
              current.memoizedProps !== newProps && markUpdate(workInProgress2);
            else {
              if (!newProps) {
                if (null === workInProgress2.stateNode)
                  throw Error(formatProdErrorMessage(166));
                bubbleProperties(workInProgress2);
                return null;
              }
              nextResource = contextStackCursor.current;
              if (popHydrationState(workInProgress2))
                prepareToHydrateHostInstance(workInProgress2, nextResource);
              else {
                var ownerDocument = getOwnerDocumentFromRootContainer(
                  rootInstanceStackCursor.current
                );
                switch (nextResource) {
                  case 1:
                    nextResource = ownerDocument.createElementNS(
                      "http://www.w3.org/2000/svg",
                      type
                    );
                    break;
                  case 2:
                    nextResource = ownerDocument.createElementNS(
                      "http://www.w3.org/1998/Math/MathML",
                      type
                    );
                    break;
                  default:
                    switch (type) {
                      case "svg":
                        nextResource = ownerDocument.createElementNS(
                          "http://www.w3.org/2000/svg",
                          type
                        );
                        break;
                      case "math":
                        nextResource = ownerDocument.createElementNS(
                          "http://www.w3.org/1998/Math/MathML",
                          type
                        );
                        break;
                      case "script":
                        nextResource = ownerDocument.createElement("div");
                        nextResource.innerHTML = "<script><\/script>";
                        nextResource = nextResource.removeChild(
                          nextResource.firstChild
                        );
                        break;
                      case "select":
                        nextResource = "string" === typeof newProps.is ? ownerDocument.createElement("select", {
                          is: newProps.is
                        }) : ownerDocument.createElement("select");
                        newProps.multiple ? nextResource.multiple = true : newProps.size && (nextResource.size = newProps.size);
                        break;
                      default:
                        nextResource = "string" === typeof newProps.is ? ownerDocument.createElement(type, { is: newProps.is }) : ownerDocument.createElement(type);
                    }
                }
                nextResource[internalInstanceKey] = workInProgress2;
                nextResource[internalPropsKey] = newProps;
                a: for (ownerDocument = workInProgress2.child; null !== ownerDocument; ) {
                  if (5 === ownerDocument.tag || 6 === ownerDocument.tag)
                    nextResource.appendChild(ownerDocument.stateNode);
                  else if (4 !== ownerDocument.tag && 27 !== ownerDocument.tag && null !== ownerDocument.child) {
                    ownerDocument.child.return = ownerDocument;
                    ownerDocument = ownerDocument.child;
                    continue;
                  }
                  if (ownerDocument === workInProgress2) break a;
                  for (; null === ownerDocument.sibling; ) {
                    if (null === ownerDocument.return || ownerDocument.return === workInProgress2)
                      break a;
                    ownerDocument = ownerDocument.return;
                  }
                  ownerDocument.sibling.return = ownerDocument.return;
                  ownerDocument = ownerDocument.sibling;
                }
                workInProgress2.stateNode = nextResource;
                a: switch (setInitialProperties(nextResource, type, newProps), type) {
                  case "button":
                  case "input":
                  case "select":
                  case "textarea":
                    newProps = !!newProps.autoFocus;
                    break a;
                  case "img":
                    newProps = true;
                    break a;
                  default:
                    newProps = false;
                }
                newProps && markUpdate(workInProgress2);
              }
            }
            bubbleProperties(workInProgress2);
            preloadInstanceAndSuspendIfNeeded(
              workInProgress2,
              workInProgress2.type,
              null === current ? null : current.memoizedProps,
              workInProgress2.pendingProps,
              renderLanes2
            );
            return null;
          case 6:
            if (current && null != workInProgress2.stateNode)
              current.memoizedProps !== newProps && markUpdate(workInProgress2);
            else {
              if ("string" !== typeof newProps && null === workInProgress2.stateNode)
                throw Error(formatProdErrorMessage(166));
              current = rootInstanceStackCursor.current;
              if (popHydrationState(workInProgress2)) {
                current = workInProgress2.stateNode;
                renderLanes2 = workInProgress2.memoizedProps;
                newProps = null;
                type = hydrationParentFiber;
                if (null !== type)
                  switch (type.tag) {
                    case 27:
                    case 5:
                      newProps = type.memoizedProps;
                  }
                current[internalInstanceKey] = workInProgress2;
                current = current.nodeValue === renderLanes2 || null !== newProps && true === newProps.suppressHydrationWarning || checkForUnmatchedText(current.nodeValue, renderLanes2) ? true : false;
                current || throwOnHydrationMismatch(workInProgress2, true);
              } else
                current = getOwnerDocumentFromRootContainer(current).createTextNode(
                  newProps
                ), current[internalInstanceKey] = workInProgress2, workInProgress2.stateNode = current;
            }
            bubbleProperties(workInProgress2);
            return null;
          case 31:
            renderLanes2 = workInProgress2.memoizedState;
            if (null === current || null !== current.memoizedState) {
              newProps = popHydrationState(workInProgress2);
              if (null !== renderLanes2) {
                if (null === current) {
                  if (!newProps) throw Error(formatProdErrorMessage(318));
                  current = workInProgress2.memoizedState;
                  current = null !== current ? current.dehydrated : null;
                  if (!current) throw Error(formatProdErrorMessage(557));
                  current[internalInstanceKey] = workInProgress2;
                } else
                  resetHydrationState(), 0 === (workInProgress2.flags & 128) && (workInProgress2.memoizedState = null), workInProgress2.flags |= 4;
                bubbleProperties(workInProgress2);
                current = false;
              } else
                renderLanes2 = upgradeHydrationErrorsToRecoverable(), null !== current && null !== current.memoizedState && (current.memoizedState.hydrationErrors = renderLanes2), current = true;
              if (!current) {
                if (workInProgress2.flags & 256)
                  return popSuspenseHandler(workInProgress2), workInProgress2;
                popSuspenseHandler(workInProgress2);
                return null;
              }
              if (0 !== (workInProgress2.flags & 128))
                throw Error(formatProdErrorMessage(558));
            }
            bubbleProperties(workInProgress2);
            return null;
          case 13:
            newProps = workInProgress2.memoizedState;
            if (null === current || null !== current.memoizedState && null !== current.memoizedState.dehydrated) {
              type = popHydrationState(workInProgress2);
              if (null !== newProps && null !== newProps.dehydrated) {
                if (null === current) {
                  if (!type) throw Error(formatProdErrorMessage(318));
                  type = workInProgress2.memoizedState;
                  type = null !== type ? type.dehydrated : null;
                  if (!type) throw Error(formatProdErrorMessage(317));
                  type[internalInstanceKey] = workInProgress2;
                } else
                  resetHydrationState(), 0 === (workInProgress2.flags & 128) && (workInProgress2.memoizedState = null), workInProgress2.flags |= 4;
                bubbleProperties(workInProgress2);
                type = false;
              } else
                type = upgradeHydrationErrorsToRecoverable(), null !== current && null !== current.memoizedState && (current.memoizedState.hydrationErrors = type), type = true;
              if (!type) {
                if (workInProgress2.flags & 256)
                  return popSuspenseHandler(workInProgress2), workInProgress2;
                popSuspenseHandler(workInProgress2);
                return null;
              }
            }
            popSuspenseHandler(workInProgress2);
            if (0 !== (workInProgress2.flags & 128))
              return workInProgress2.lanes = renderLanes2, workInProgress2;
            renderLanes2 = null !== newProps;
            current = null !== current && null !== current.memoizedState;
            renderLanes2 && (newProps = workInProgress2.child, type = null, null !== newProps.alternate && null !== newProps.alternate.memoizedState && null !== newProps.alternate.memoizedState.cachePool && (type = newProps.alternate.memoizedState.cachePool.pool), nextResource = null, null !== newProps.memoizedState && null !== newProps.memoizedState.cachePool && (nextResource = newProps.memoizedState.cachePool.pool), nextResource !== type && (newProps.flags |= 2048));
            renderLanes2 !== current && renderLanes2 && (workInProgress2.child.flags |= 8192);
            scheduleRetryEffect(workInProgress2, workInProgress2.updateQueue);
            bubbleProperties(workInProgress2);
            return null;
          case 4:
            return popHostContainer(), null === current && listenToAllSupportedEvents(workInProgress2.stateNode.containerInfo), bubbleProperties(workInProgress2), null;
          case 10:
            return popProvider(workInProgress2.type), bubbleProperties(workInProgress2), null;
          case 19:
            pop(suspenseStackCursor);
            newProps = workInProgress2.memoizedState;
            if (null === newProps) return bubbleProperties(workInProgress2), null;
            type = 0 !== (workInProgress2.flags & 128);
            nextResource = newProps.rendering;
            if (null === nextResource)
              if (type) cutOffTailIfNeeded(newProps, false);
              else {
                if (0 !== workInProgressRootExitStatus || null !== current && 0 !== (current.flags & 128))
                  for (current = workInProgress2.child; null !== current; ) {
                    nextResource = findFirstSuspended(current);
                    if (null !== nextResource) {
                      workInProgress2.flags |= 128;
                      cutOffTailIfNeeded(newProps, false);
                      current = nextResource.updateQueue;
                      workInProgress2.updateQueue = current;
                      scheduleRetryEffect(workInProgress2, current);
                      workInProgress2.subtreeFlags = 0;
                      current = renderLanes2;
                      for (renderLanes2 = workInProgress2.child; null !== renderLanes2; )
                        resetWorkInProgress(renderLanes2, current), renderLanes2 = renderLanes2.sibling;
                      push(
                        suspenseStackCursor,
                        suspenseStackCursor.current & 1 | 2
                      );
                      isHydrating && pushTreeFork(workInProgress2, newProps.treeForkCount);
                      return workInProgress2.child;
                    }
                    current = current.sibling;
                  }
                null !== newProps.tail && now() > workInProgressRootRenderTargetTime && (workInProgress2.flags |= 128, type = true, cutOffTailIfNeeded(newProps, false), workInProgress2.lanes = 4194304);
              }
            else {
              if (!type)
                if (current = findFirstSuspended(nextResource), null !== current) {
                  if (workInProgress2.flags |= 128, type = true, current = current.updateQueue, workInProgress2.updateQueue = current, scheduleRetryEffect(workInProgress2, current), cutOffTailIfNeeded(newProps, true), null === newProps.tail && "hidden" === newProps.tailMode && !nextResource.alternate && !isHydrating)
                    return bubbleProperties(workInProgress2), null;
                } else
                  2 * now() - newProps.renderingStartTime > workInProgressRootRenderTargetTime && 536870912 !== renderLanes2 && (workInProgress2.flags |= 128, type = true, cutOffTailIfNeeded(newProps, false), workInProgress2.lanes = 4194304);
              newProps.isBackwards ? (nextResource.sibling = workInProgress2.child, workInProgress2.child = nextResource) : (current = newProps.last, null !== current ? current.sibling = nextResource : workInProgress2.child = nextResource, newProps.last = nextResource);
            }
            if (null !== newProps.tail)
              return current = newProps.tail, newProps.rendering = current, newProps.tail = current.sibling, newProps.renderingStartTime = now(), current.sibling = null, renderLanes2 = suspenseStackCursor.current, push(
                suspenseStackCursor,
                type ? renderLanes2 & 1 | 2 : renderLanes2 & 1
              ), isHydrating && pushTreeFork(workInProgress2, newProps.treeForkCount), current;
            bubbleProperties(workInProgress2);
            return null;
          case 22:
          case 23:
            return popSuspenseHandler(workInProgress2), popHiddenContext(), newProps = null !== workInProgress2.memoizedState, null !== current ? null !== current.memoizedState !== newProps && (workInProgress2.flags |= 8192) : newProps && (workInProgress2.flags |= 8192), newProps ? 0 !== (renderLanes2 & 536870912) && 0 === (workInProgress2.flags & 128) && (bubbleProperties(workInProgress2), workInProgress2.subtreeFlags & 6 && (workInProgress2.flags |= 8192)) : bubbleProperties(workInProgress2), renderLanes2 = workInProgress2.updateQueue, null !== renderLanes2 && scheduleRetryEffect(workInProgress2, renderLanes2.retryQueue), renderLanes2 = null, null !== current && null !== current.memoizedState && null !== current.memoizedState.cachePool && (renderLanes2 = current.memoizedState.cachePool.pool), newProps = null, null !== workInProgress2.memoizedState && null !== workInProgress2.memoizedState.cachePool && (newProps = workInProgress2.memoizedState.cachePool.pool), newProps !== renderLanes2 && (workInProgress2.flags |= 2048), null !== current && pop(resumedCache), null;
          case 24:
            return renderLanes2 = null, null !== current && (renderLanes2 = current.memoizedState.cache), workInProgress2.memoizedState.cache !== renderLanes2 && (workInProgress2.flags |= 2048), popProvider(CacheContext), bubbleProperties(workInProgress2), null;
          case 25:
            return null;
          case 30:
            return null;
        }
        throw Error(formatProdErrorMessage(156, workInProgress2.tag));
      }
      function unwindWork(current, workInProgress2) {
        popTreeContext(workInProgress2);
        switch (workInProgress2.tag) {
          case 1:
            return current = workInProgress2.flags, current & 65536 ? (workInProgress2.flags = current & -65537 | 128, workInProgress2) : null;
          case 3:
            return popProvider(CacheContext), popHostContainer(), current = workInProgress2.flags, 0 !== (current & 65536) && 0 === (current & 128) ? (workInProgress2.flags = current & -65537 | 128, workInProgress2) : null;
          case 26:
          case 27:
          case 5:
            return popHostContext(workInProgress2), null;
          case 31:
            if (null !== workInProgress2.memoizedState) {
              popSuspenseHandler(workInProgress2);
              if (null === workInProgress2.alternate)
                throw Error(formatProdErrorMessage(340));
              resetHydrationState();
            }
            current = workInProgress2.flags;
            return current & 65536 ? (workInProgress2.flags = current & -65537 | 128, workInProgress2) : null;
          case 13:
            popSuspenseHandler(workInProgress2);
            current = workInProgress2.memoizedState;
            if (null !== current && null !== current.dehydrated) {
              if (null === workInProgress2.alternate)
                throw Error(formatProdErrorMessage(340));
              resetHydrationState();
            }
            current = workInProgress2.flags;
            return current & 65536 ? (workInProgress2.flags = current & -65537 | 128, workInProgress2) : null;
          case 19:
            return pop(suspenseStackCursor), null;
          case 4:
            return popHostContainer(), null;
          case 10:
            return popProvider(workInProgress2.type), null;
          case 22:
          case 23:
            return popSuspenseHandler(workInProgress2), popHiddenContext(), null !== current && pop(resumedCache), current = workInProgress2.flags, current & 65536 ? (workInProgress2.flags = current & -65537 | 128, workInProgress2) : null;
          case 24:
            return popProvider(CacheContext), null;
          case 25:
            return null;
          default:
            return null;
        }
      }
      function unwindInterruptedWork(current, interruptedWork) {
        popTreeContext(interruptedWork);
        switch (interruptedWork.tag) {
          case 3:
            popProvider(CacheContext);
            popHostContainer();
            break;
          case 26:
          case 27:
          case 5:
            popHostContext(interruptedWork);
            break;
          case 4:
            popHostContainer();
            break;
          case 31:
            null !== interruptedWork.memoizedState && popSuspenseHandler(interruptedWork);
            break;
          case 13:
            popSuspenseHandler(interruptedWork);
            break;
          case 19:
            pop(suspenseStackCursor);
            break;
          case 10:
            popProvider(interruptedWork.type);
            break;
          case 22:
          case 23:
            popSuspenseHandler(interruptedWork);
            popHiddenContext();
            null !== current && pop(resumedCache);
            break;
          case 24:
            popProvider(CacheContext);
        }
      }
      function commitHookEffectListMount(flags, finishedWork) {
        try {
          var updateQueue = finishedWork.updateQueue, lastEffect = null !== updateQueue ? updateQueue.lastEffect : null;
          if (null !== lastEffect) {
            var firstEffect = lastEffect.next;
            updateQueue = firstEffect;
            do {
              if ((updateQueue.tag & flags) === flags) {
                lastEffect = void 0;
                var create = updateQueue.create, inst = updateQueue.inst;
                lastEffect = create();
                inst.destroy = lastEffect;
              }
              updateQueue = updateQueue.next;
            } while (updateQueue !== firstEffect);
          }
        } catch (error) {
          captureCommitPhaseError(finishedWork, finishedWork.return, error);
        }
      }
      function commitHookEffectListUnmount(flags, finishedWork, nearestMountedAncestor$jscomp$0) {
        try {
          var updateQueue = finishedWork.updateQueue, lastEffect = null !== updateQueue ? updateQueue.lastEffect : null;
          if (null !== lastEffect) {
            var firstEffect = lastEffect.next;
            updateQueue = firstEffect;
            do {
              if ((updateQueue.tag & flags) === flags) {
                var inst = updateQueue.inst, destroy = inst.destroy;
                if (void 0 !== destroy) {
                  inst.destroy = void 0;
                  lastEffect = finishedWork;
                  var nearestMountedAncestor = nearestMountedAncestor$jscomp$0, destroy_ = destroy;
                  try {
                    destroy_();
                  } catch (error) {
                    captureCommitPhaseError(
                      lastEffect,
                      nearestMountedAncestor,
                      error
                    );
                  }
                }
              }
              updateQueue = updateQueue.next;
            } while (updateQueue !== firstEffect);
          }
        } catch (error) {
          captureCommitPhaseError(finishedWork, finishedWork.return, error);
        }
      }
      function commitClassCallbacks(finishedWork) {
        var updateQueue = finishedWork.updateQueue;
        if (null !== updateQueue) {
          var instance = finishedWork.stateNode;
          try {
            commitCallbacks(updateQueue, instance);
          } catch (error) {
            captureCommitPhaseError(finishedWork, finishedWork.return, error);
          }
        }
      }
      function safelyCallComponentWillUnmount(current, nearestMountedAncestor, instance) {
        instance.props = resolveClassComponentProps(
          current.type,
          current.memoizedProps
        );
        instance.state = current.memoizedState;
        try {
          instance.componentWillUnmount();
        } catch (error) {
          captureCommitPhaseError(current, nearestMountedAncestor, error);
        }
      }
      function safelyAttachRef(current, nearestMountedAncestor) {
        try {
          var ref = current.ref;
          if (null !== ref) {
            switch (current.tag) {
              case 26:
              case 27:
              case 5:
                var instanceToUse = current.stateNode;
                break;
              case 30:
                instanceToUse = current.stateNode;
                break;
              default:
                instanceToUse = current.stateNode;
            }
            "function" === typeof ref ? current.refCleanup = ref(instanceToUse) : ref.current = instanceToUse;
          }
        } catch (error) {
          captureCommitPhaseError(current, nearestMountedAncestor, error);
        }
      }
      function safelyDetachRef(current, nearestMountedAncestor) {
        var ref = current.ref, refCleanup = current.refCleanup;
        if (null !== ref)
          if ("function" === typeof refCleanup)
            try {
              refCleanup();
            } catch (error) {
              captureCommitPhaseError(current, nearestMountedAncestor, error);
            } finally {
              current.refCleanup = null, current = current.alternate, null != current && (current.refCleanup = null);
            }
          else if ("function" === typeof ref)
            try {
              ref(null);
            } catch (error$140) {
              captureCommitPhaseError(current, nearestMountedAncestor, error$140);
            }
          else ref.current = null;
      }
      function commitHostMount(finishedWork) {
        var type = finishedWork.type, props = finishedWork.memoizedProps, instance = finishedWork.stateNode;
        try {
          a: switch (type) {
            case "button":
            case "input":
            case "select":
            case "textarea":
              props.autoFocus && instance.focus();
              break a;
            case "img":
              props.src ? instance.src = props.src : props.srcSet && (instance.srcset = props.srcSet);
          }
        } catch (error) {
          captureCommitPhaseError(finishedWork, finishedWork.return, error);
        }
      }
      function commitHostUpdate(finishedWork, newProps, oldProps) {
        try {
          var domElement = finishedWork.stateNode;
          updateProperties(domElement, finishedWork.type, oldProps, newProps);
          domElement[internalPropsKey] = newProps;
        } catch (error) {
          captureCommitPhaseError(finishedWork, finishedWork.return, error);
        }
      }
      function isHostParent(fiber) {
        return 5 === fiber.tag || 3 === fiber.tag || 26 === fiber.tag || 27 === fiber.tag && isSingletonScope(fiber.type) || 4 === fiber.tag;
      }
      function getHostSibling(fiber) {
        a: for (; ; ) {
          for (; null === fiber.sibling; ) {
            if (null === fiber.return || isHostParent(fiber.return)) return null;
            fiber = fiber.return;
          }
          fiber.sibling.return = fiber.return;
          for (fiber = fiber.sibling; 5 !== fiber.tag && 6 !== fiber.tag && 18 !== fiber.tag; ) {
            if (27 === fiber.tag && isSingletonScope(fiber.type)) continue a;
            if (fiber.flags & 2) continue a;
            if (null === fiber.child || 4 === fiber.tag) continue a;
            else fiber.child.return = fiber, fiber = fiber.child;
          }
          if (!(fiber.flags & 2)) return fiber.stateNode;
        }
      }
      function insertOrAppendPlacementNodeIntoContainer(node, before, parent) {
        var tag = node.tag;
        if (5 === tag || 6 === tag)
          node = node.stateNode, before ? (9 === parent.nodeType ? parent.body : "HTML" === parent.nodeName ? parent.ownerDocument.body : parent).insertBefore(node, before) : (before = 9 === parent.nodeType ? parent.body : "HTML" === parent.nodeName ? parent.ownerDocument.body : parent, before.appendChild(node), parent = parent._reactRootContainer, null !== parent && void 0 !== parent || null !== before.onclick || (before.onclick = noop$1));
        else if (4 !== tag && (27 === tag && isSingletonScope(node.type) && (parent = node.stateNode, before = null), node = node.child, null !== node))
          for (insertOrAppendPlacementNodeIntoContainer(node, before, parent), node = node.sibling; null !== node; )
            insertOrAppendPlacementNodeIntoContainer(node, before, parent), node = node.sibling;
      }
      function insertOrAppendPlacementNode(node, before, parent) {
        var tag = node.tag;
        if (5 === tag || 6 === tag)
          node = node.stateNode, before ? parent.insertBefore(node, before) : parent.appendChild(node);
        else if (4 !== tag && (27 === tag && isSingletonScope(node.type) && (parent = node.stateNode), node = node.child, null !== node))
          for (insertOrAppendPlacementNode(node, before, parent), node = node.sibling; null !== node; )
            insertOrAppendPlacementNode(node, before, parent), node = node.sibling;
      }
      function commitHostSingletonAcquisition(finishedWork) {
        var singleton2 = finishedWork.stateNode, props = finishedWork.memoizedProps;
        try {
          for (var type = finishedWork.type, attributes = singleton2.attributes; attributes.length; )
            singleton2.removeAttributeNode(attributes[0]);
          setInitialProperties(singleton2, type, props);
          singleton2[internalInstanceKey] = finishedWork;
          singleton2[internalPropsKey] = props;
        } catch (error) {
          captureCommitPhaseError(finishedWork, finishedWork.return, error);
        }
      }
      var offscreenSubtreeIsHidden = false;
      var offscreenSubtreeWasHidden = false;
      var needsFormReset = false;
      var PossiblyWeakSet = "function" === typeof WeakSet ? WeakSet : Set;
      var nextEffect = null;
      function commitBeforeMutationEffects(root2, firstChild) {
        root2 = root2.containerInfo;
        eventsEnabled = _enabled;
        root2 = getActiveElementDeep(root2);
        if (hasSelectionCapabilities(root2)) {
          if ("selectionStart" in root2)
            var JSCompiler_temp = {
              start: root2.selectionStart,
              end: root2.selectionEnd
            };
          else
            a: {
              JSCompiler_temp = (JSCompiler_temp = root2.ownerDocument) && JSCompiler_temp.defaultView || window;
              var selection = JSCompiler_temp.getSelection && JSCompiler_temp.getSelection();
              if (selection && 0 !== selection.rangeCount) {
                JSCompiler_temp = selection.anchorNode;
                var anchorOffset = selection.anchorOffset, focusNode = selection.focusNode;
                selection = selection.focusOffset;
                try {
                  JSCompiler_temp.nodeType, focusNode.nodeType;
                } catch (e$20) {
                  JSCompiler_temp = null;
                  break a;
                }
                var length = 0, start = -1, end = -1, indexWithinAnchor = 0, indexWithinFocus = 0, node = root2, parentNode = null;
                b: for (; ; ) {
                  for (var next; ; ) {
                    node !== JSCompiler_temp || 0 !== anchorOffset && 3 !== node.nodeType || (start = length + anchorOffset);
                    node !== focusNode || 0 !== selection && 3 !== node.nodeType || (end = length + selection);
                    3 === node.nodeType && (length += node.nodeValue.length);
                    if (null === (next = node.firstChild)) break;
                    parentNode = node;
                    node = next;
                  }
                  for (; ; ) {
                    if (node === root2) break b;
                    parentNode === JSCompiler_temp && ++indexWithinAnchor === anchorOffset && (start = length);
                    parentNode === focusNode && ++indexWithinFocus === selection && (end = length);
                    if (null !== (next = node.nextSibling)) break;
                    node = parentNode;
                    parentNode = node.parentNode;
                  }
                  node = next;
                }
                JSCompiler_temp = -1 === start || -1 === end ? null : { start, end };
              } else JSCompiler_temp = null;
            }
          JSCompiler_temp = JSCompiler_temp || { start: 0, end: 0 };
        } else JSCompiler_temp = null;
        selectionInformation = { focusedElem: root2, selectionRange: JSCompiler_temp };
        _enabled = false;
        for (nextEffect = firstChild; null !== nextEffect; )
          if (firstChild = nextEffect, root2 = firstChild.child, 0 !== (firstChild.subtreeFlags & 1028) && null !== root2)
            root2.return = firstChild, nextEffect = root2;
          else
            for (; null !== nextEffect; ) {
              firstChild = nextEffect;
              focusNode = firstChild.alternate;
              root2 = firstChild.flags;
              switch (firstChild.tag) {
                case 0:
                  if (0 !== (root2 & 4) && (root2 = firstChild.updateQueue, root2 = null !== root2 ? root2.events : null, null !== root2))
                    for (JSCompiler_temp = 0; JSCompiler_temp < root2.length; JSCompiler_temp++)
                      anchorOffset = root2[JSCompiler_temp], anchorOffset.ref.impl = anchorOffset.nextImpl;
                  break;
                case 11:
                case 15:
                  break;
                case 1:
                  if (0 !== (root2 & 1024) && null !== focusNode) {
                    root2 = void 0;
                    JSCompiler_temp = firstChild;
                    anchorOffset = focusNode.memoizedProps;
                    focusNode = focusNode.memoizedState;
                    selection = JSCompiler_temp.stateNode;
                    try {
                      var resolvedPrevProps = resolveClassComponentProps(
                        JSCompiler_temp.type,
                        anchorOffset
                      );
                      root2 = selection.getSnapshotBeforeUpdate(
                        resolvedPrevProps,
                        focusNode
                      );
                      selection.__reactInternalSnapshotBeforeUpdate = root2;
                    } catch (error) {
                      captureCommitPhaseError(
                        JSCompiler_temp,
                        JSCompiler_temp.return,
                        error
                      );
                    }
                  }
                  break;
                case 3:
                  if (0 !== (root2 & 1024)) {
                    if (root2 = firstChild.stateNode.containerInfo, JSCompiler_temp = root2.nodeType, 9 === JSCompiler_temp)
                      clearContainerSparingly(root2);
                    else if (1 === JSCompiler_temp)
                      switch (root2.nodeName) {
                        case "HEAD":
                        case "HTML":
                        case "BODY":
                          clearContainerSparingly(root2);
                          break;
                        default:
                          root2.textContent = "";
                      }
                  }
                  break;
                case 5:
                case 26:
                case 27:
                case 6:
                case 4:
                case 17:
                  break;
                default:
                  if (0 !== (root2 & 1024)) throw Error(formatProdErrorMessage(163));
              }
              root2 = firstChild.sibling;
              if (null !== root2) {
                root2.return = firstChild.return;
                nextEffect = root2;
                break;
              }
              nextEffect = firstChild.return;
            }
      }
      function commitLayoutEffectOnFiber(finishedRoot, current, finishedWork) {
        var flags = finishedWork.flags;
        switch (finishedWork.tag) {
          case 0:
          case 11:
          case 15:
            recursivelyTraverseLayoutEffects(finishedRoot, finishedWork);
            flags & 4 && commitHookEffectListMount(5, finishedWork);
            break;
          case 1:
            recursivelyTraverseLayoutEffects(finishedRoot, finishedWork);
            if (flags & 4)
              if (finishedRoot = finishedWork.stateNode, null === current)
                try {
                  finishedRoot.componentDidMount();
                } catch (error) {
                  captureCommitPhaseError(finishedWork, finishedWork.return, error);
                }
              else {
                var prevProps = resolveClassComponentProps(
                  finishedWork.type,
                  current.memoizedProps
                );
                current = current.memoizedState;
                try {
                  finishedRoot.componentDidUpdate(
                    prevProps,
                    current,
                    finishedRoot.__reactInternalSnapshotBeforeUpdate
                  );
                } catch (error$139) {
                  captureCommitPhaseError(
                    finishedWork,
                    finishedWork.return,
                    error$139
                  );
                }
              }
            flags & 64 && commitClassCallbacks(finishedWork);
            flags & 512 && safelyAttachRef(finishedWork, finishedWork.return);
            break;
          case 3:
            recursivelyTraverseLayoutEffects(finishedRoot, finishedWork);
            if (flags & 64 && (finishedRoot = finishedWork.updateQueue, null !== finishedRoot)) {
              current = null;
              if (null !== finishedWork.child)
                switch (finishedWork.child.tag) {
                  case 27:
                  case 5:
                    current = finishedWork.child.stateNode;
                    break;
                  case 1:
                    current = finishedWork.child.stateNode;
                }
              try {
                commitCallbacks(finishedRoot, current);
              } catch (error) {
                captureCommitPhaseError(finishedWork, finishedWork.return, error);
              }
            }
            break;
          case 27:
            null === current && flags & 4 && commitHostSingletonAcquisition(finishedWork);
          case 26:
          case 5:
            recursivelyTraverseLayoutEffects(finishedRoot, finishedWork);
            null === current && flags & 4 && commitHostMount(finishedWork);
            flags & 512 && safelyAttachRef(finishedWork, finishedWork.return);
            break;
          case 12:
            recursivelyTraverseLayoutEffects(finishedRoot, finishedWork);
            break;
          case 31:
            recursivelyTraverseLayoutEffects(finishedRoot, finishedWork);
            flags & 4 && commitActivityHydrationCallbacks(finishedRoot, finishedWork);
            break;
          case 13:
            recursivelyTraverseLayoutEffects(finishedRoot, finishedWork);
            flags & 4 && commitSuspenseHydrationCallbacks(finishedRoot, finishedWork);
            flags & 64 && (finishedRoot = finishedWork.memoizedState, null !== finishedRoot && (finishedRoot = finishedRoot.dehydrated, null !== finishedRoot && (finishedWork = retryDehydratedSuspenseBoundary.bind(
              null,
              finishedWork
            ), registerSuspenseInstanceRetry(finishedRoot, finishedWork))));
            break;
          case 22:
            flags = null !== finishedWork.memoizedState || offscreenSubtreeIsHidden;
            if (!flags) {
              current = null !== current && null !== current.memoizedState || offscreenSubtreeWasHidden;
              prevProps = offscreenSubtreeIsHidden;
              var prevOffscreenSubtreeWasHidden = offscreenSubtreeWasHidden;
              offscreenSubtreeIsHidden = flags;
              (offscreenSubtreeWasHidden = current) && !prevOffscreenSubtreeWasHidden ? recursivelyTraverseReappearLayoutEffects(
                finishedRoot,
                finishedWork,
                0 !== (finishedWork.subtreeFlags & 8772)
              ) : recursivelyTraverseLayoutEffects(finishedRoot, finishedWork);
              offscreenSubtreeIsHidden = prevProps;
              offscreenSubtreeWasHidden = prevOffscreenSubtreeWasHidden;
            }
            break;
          case 30:
            break;
          default:
            recursivelyTraverseLayoutEffects(finishedRoot, finishedWork);
        }
      }
      function detachFiberAfterEffects(fiber) {
        var alternate = fiber.alternate;
        null !== alternate && (fiber.alternate = null, detachFiberAfterEffects(alternate));
        fiber.child = null;
        fiber.deletions = null;
        fiber.sibling = null;
        5 === fiber.tag && (alternate = fiber.stateNode, null !== alternate && detachDeletedInstance(alternate));
        fiber.stateNode = null;
        fiber.return = null;
        fiber.dependencies = null;
        fiber.memoizedProps = null;
        fiber.memoizedState = null;
        fiber.pendingProps = null;
        fiber.stateNode = null;
        fiber.updateQueue = null;
      }
      var hostParent = null;
      var hostParentIsContainer = false;
      function recursivelyTraverseDeletionEffects(finishedRoot, nearestMountedAncestor, parent) {
        for (parent = parent.child; null !== parent; )
          commitDeletionEffectsOnFiber(finishedRoot, nearestMountedAncestor, parent), parent = parent.sibling;
      }
      function commitDeletionEffectsOnFiber(finishedRoot, nearestMountedAncestor, deletedFiber) {
        if (injectedHook && "function" === typeof injectedHook.onCommitFiberUnmount)
          try {
            injectedHook.onCommitFiberUnmount(rendererID, deletedFiber);
          } catch (err) {
          }
        switch (deletedFiber.tag) {
          case 26:
            offscreenSubtreeWasHidden || safelyDetachRef(deletedFiber, nearestMountedAncestor);
            recursivelyTraverseDeletionEffects(
              finishedRoot,
              nearestMountedAncestor,
              deletedFiber
            );
            deletedFiber.memoizedState ? deletedFiber.memoizedState.count-- : deletedFiber.stateNode && (deletedFiber = deletedFiber.stateNode, deletedFiber.parentNode.removeChild(deletedFiber));
            break;
          case 27:
            offscreenSubtreeWasHidden || safelyDetachRef(deletedFiber, nearestMountedAncestor);
            var prevHostParent = hostParent, prevHostParentIsContainer = hostParentIsContainer;
            isSingletonScope(deletedFiber.type) && (hostParent = deletedFiber.stateNode, hostParentIsContainer = false);
            recursivelyTraverseDeletionEffects(
              finishedRoot,
              nearestMountedAncestor,
              deletedFiber
            );
            releaseSingletonInstance(deletedFiber.stateNode);
            hostParent = prevHostParent;
            hostParentIsContainer = prevHostParentIsContainer;
            break;
          case 5:
            offscreenSubtreeWasHidden || safelyDetachRef(deletedFiber, nearestMountedAncestor);
          case 6:
            prevHostParent = hostParent;
            prevHostParentIsContainer = hostParentIsContainer;
            hostParent = null;
            recursivelyTraverseDeletionEffects(
              finishedRoot,
              nearestMountedAncestor,
              deletedFiber
            );
            hostParent = prevHostParent;
            hostParentIsContainer = prevHostParentIsContainer;
            if (null !== hostParent)
              if (hostParentIsContainer)
                try {
                  (9 === hostParent.nodeType ? hostParent.body : "HTML" === hostParent.nodeName ? hostParent.ownerDocument.body : hostParent).removeChild(deletedFiber.stateNode);
                } catch (error) {
                  captureCommitPhaseError(
                    deletedFiber,
                    nearestMountedAncestor,
                    error
                  );
                }
              else
                try {
                  hostParent.removeChild(deletedFiber.stateNode);
                } catch (error) {
                  captureCommitPhaseError(
                    deletedFiber,
                    nearestMountedAncestor,
                    error
                  );
                }
            break;
          case 18:
            null !== hostParent && (hostParentIsContainer ? (finishedRoot = hostParent, clearHydrationBoundary(
              9 === finishedRoot.nodeType ? finishedRoot.body : "HTML" === finishedRoot.nodeName ? finishedRoot.ownerDocument.body : finishedRoot,
              deletedFiber.stateNode
            ), retryIfBlockedOn(finishedRoot)) : clearHydrationBoundary(hostParent, deletedFiber.stateNode));
            break;
          case 4:
            prevHostParent = hostParent;
            prevHostParentIsContainer = hostParentIsContainer;
            hostParent = deletedFiber.stateNode.containerInfo;
            hostParentIsContainer = true;
            recursivelyTraverseDeletionEffects(
              finishedRoot,
              nearestMountedAncestor,
              deletedFiber
            );
            hostParent = prevHostParent;
            hostParentIsContainer = prevHostParentIsContainer;
            break;
          case 0:
          case 11:
          case 14:
          case 15:
            commitHookEffectListUnmount(2, deletedFiber, nearestMountedAncestor);
            offscreenSubtreeWasHidden || commitHookEffectListUnmount(4, deletedFiber, nearestMountedAncestor);
            recursivelyTraverseDeletionEffects(
              finishedRoot,
              nearestMountedAncestor,
              deletedFiber
            );
            break;
          case 1:
            offscreenSubtreeWasHidden || (safelyDetachRef(deletedFiber, nearestMountedAncestor), prevHostParent = deletedFiber.stateNode, "function" === typeof prevHostParent.componentWillUnmount && safelyCallComponentWillUnmount(
              deletedFiber,
              nearestMountedAncestor,
              prevHostParent
            ));
            recursivelyTraverseDeletionEffects(
              finishedRoot,
              nearestMountedAncestor,
              deletedFiber
            );
            break;
          case 21:
            recursivelyTraverseDeletionEffects(
              finishedRoot,
              nearestMountedAncestor,
              deletedFiber
            );
            break;
          case 22:
            offscreenSubtreeWasHidden = (prevHostParent = offscreenSubtreeWasHidden) || null !== deletedFiber.memoizedState;
            recursivelyTraverseDeletionEffects(
              finishedRoot,
              nearestMountedAncestor,
              deletedFiber
            );
            offscreenSubtreeWasHidden = prevHostParent;
            break;
          default:
            recursivelyTraverseDeletionEffects(
              finishedRoot,
              nearestMountedAncestor,
              deletedFiber
            );
        }
      }
      function commitActivityHydrationCallbacks(finishedRoot, finishedWork) {
        if (null === finishedWork.memoizedState && (finishedRoot = finishedWork.alternate, null !== finishedRoot && (finishedRoot = finishedRoot.memoizedState, null !== finishedRoot))) {
          finishedRoot = finishedRoot.dehydrated;
          try {
            retryIfBlockedOn(finishedRoot);
          } catch (error) {
            captureCommitPhaseError(finishedWork, finishedWork.return, error);
          }
        }
      }
      function commitSuspenseHydrationCallbacks(finishedRoot, finishedWork) {
        if (null === finishedWork.memoizedState && (finishedRoot = finishedWork.alternate, null !== finishedRoot && (finishedRoot = finishedRoot.memoizedState, null !== finishedRoot && (finishedRoot = finishedRoot.dehydrated, null !== finishedRoot))))
          try {
            retryIfBlockedOn(finishedRoot);
          } catch (error) {
            captureCommitPhaseError(finishedWork, finishedWork.return, error);
          }
      }
      function getRetryCache(finishedWork) {
        switch (finishedWork.tag) {
          case 31:
          case 13:
          case 19:
            var retryCache = finishedWork.stateNode;
            null === retryCache && (retryCache = finishedWork.stateNode = new PossiblyWeakSet());
            return retryCache;
          case 22:
            return finishedWork = finishedWork.stateNode, retryCache = finishedWork._retryCache, null === retryCache && (retryCache = finishedWork._retryCache = new PossiblyWeakSet()), retryCache;
          default:
            throw Error(formatProdErrorMessage(435, finishedWork.tag));
        }
      }
      function attachSuspenseRetryListeners(finishedWork, wakeables) {
        var retryCache = getRetryCache(finishedWork);
        wakeables.forEach(function(wakeable) {
          if (!retryCache.has(wakeable)) {
            retryCache.add(wakeable);
            var retry = resolveRetryWakeable.bind(null, finishedWork, wakeable);
            wakeable.then(retry, retry);
          }
        });
      }
      function recursivelyTraverseMutationEffects(root$jscomp$0, parentFiber) {
        var deletions = parentFiber.deletions;
        if (null !== deletions)
          for (var i = 0; i < deletions.length; i++) {
            var childToDelete = deletions[i], root2 = root$jscomp$0, returnFiber = parentFiber, parent = returnFiber;
            a: for (; null !== parent; ) {
              switch (parent.tag) {
                case 27:
                  if (isSingletonScope(parent.type)) {
                    hostParent = parent.stateNode;
                    hostParentIsContainer = false;
                    break a;
                  }
                  break;
                case 5:
                  hostParent = parent.stateNode;
                  hostParentIsContainer = false;
                  break a;
                case 3:
                case 4:
                  hostParent = parent.stateNode.containerInfo;
                  hostParentIsContainer = true;
                  break a;
              }
              parent = parent.return;
            }
            if (null === hostParent) throw Error(formatProdErrorMessage(160));
            commitDeletionEffectsOnFiber(root2, returnFiber, childToDelete);
            hostParent = null;
            hostParentIsContainer = false;
            root2 = childToDelete.alternate;
            null !== root2 && (root2.return = null);
            childToDelete.return = null;
          }
        if (parentFiber.subtreeFlags & 13886)
          for (parentFiber = parentFiber.child; null !== parentFiber; )
            commitMutationEffectsOnFiber(parentFiber, root$jscomp$0), parentFiber = parentFiber.sibling;
      }
      var currentHoistableRoot = null;
      function commitMutationEffectsOnFiber(finishedWork, root2) {
        var current = finishedWork.alternate, flags = finishedWork.flags;
        switch (finishedWork.tag) {
          case 0:
          case 11:
          case 14:
          case 15:
            recursivelyTraverseMutationEffects(root2, finishedWork);
            commitReconciliationEffects(finishedWork);
            flags & 4 && (commitHookEffectListUnmount(3, finishedWork, finishedWork.return), commitHookEffectListMount(3, finishedWork), commitHookEffectListUnmount(5, finishedWork, finishedWork.return));
            break;
          case 1:
            recursivelyTraverseMutationEffects(root2, finishedWork);
            commitReconciliationEffects(finishedWork);
            flags & 512 && (offscreenSubtreeWasHidden || null === current || safelyDetachRef(current, current.return));
            flags & 64 && offscreenSubtreeIsHidden && (finishedWork = finishedWork.updateQueue, null !== finishedWork && (flags = finishedWork.callbacks, null !== flags && (current = finishedWork.shared.hiddenCallbacks, finishedWork.shared.hiddenCallbacks = null === current ? flags : current.concat(flags))));
            break;
          case 26:
            var hoistableRoot = currentHoistableRoot;
            recursivelyTraverseMutationEffects(root2, finishedWork);
            commitReconciliationEffects(finishedWork);
            flags & 512 && (offscreenSubtreeWasHidden || null === current || safelyDetachRef(current, current.return));
            if (flags & 4) {
              var currentResource = null !== current ? current.memoizedState : null;
              flags = finishedWork.memoizedState;
              if (null === current)
                if (null === flags)
                  if (null === finishedWork.stateNode) {
                    a: {
                      flags = finishedWork.type;
                      current = finishedWork.memoizedProps;
                      hoistableRoot = hoistableRoot.ownerDocument || hoistableRoot;
                      b: switch (flags) {
                        case "title":
                          currentResource = hoistableRoot.getElementsByTagName("title")[0];
                          if (!currentResource || currentResource[internalHoistableMarker] || currentResource[internalInstanceKey] || "http://www.w3.org/2000/svg" === currentResource.namespaceURI || currentResource.hasAttribute("itemprop"))
                            currentResource = hoistableRoot.createElement(flags), hoistableRoot.head.insertBefore(
                              currentResource,
                              hoistableRoot.querySelector("head > title")
                            );
                          setInitialProperties(currentResource, flags, current);
                          currentResource[internalInstanceKey] = finishedWork;
                          markNodeAsHoistable(currentResource);
                          flags = currentResource;
                          break a;
                        case "link":
                          var maybeNodes = getHydratableHoistableCache(
                            "link",
                            "href",
                            hoistableRoot
                          ).get(flags + (current.href || ""));
                          if (maybeNodes) {
                            for (var i = 0; i < maybeNodes.length; i++)
                              if (currentResource = maybeNodes[i], currentResource.getAttribute("href") === (null == current.href || "" === current.href ? null : current.href) && currentResource.getAttribute("rel") === (null == current.rel ? null : current.rel) && currentResource.getAttribute("title") === (null == current.title ? null : current.title) && currentResource.getAttribute("crossorigin") === (null == current.crossOrigin ? null : current.crossOrigin)) {
                                maybeNodes.splice(i, 1);
                                break b;
                              }
                          }
                          currentResource = hoistableRoot.createElement(flags);
                          setInitialProperties(currentResource, flags, current);
                          hoistableRoot.head.appendChild(currentResource);
                          break;
                        case "meta":
                          if (maybeNodes = getHydratableHoistableCache(
                            "meta",
                            "content",
                            hoistableRoot
                          ).get(flags + (current.content || ""))) {
                            for (i = 0; i < maybeNodes.length; i++)
                              if (currentResource = maybeNodes[i], currentResource.getAttribute("content") === (null == current.content ? null : "" + current.content) && currentResource.getAttribute("name") === (null == current.name ? null : current.name) && currentResource.getAttribute("property") === (null == current.property ? null : current.property) && currentResource.getAttribute("http-equiv") === (null == current.httpEquiv ? null : current.httpEquiv) && currentResource.getAttribute("charset") === (null == current.charSet ? null : current.charSet)) {
                                maybeNodes.splice(i, 1);
                                break b;
                              }
                          }
                          currentResource = hoistableRoot.createElement(flags);
                          setInitialProperties(currentResource, flags, current);
                          hoistableRoot.head.appendChild(currentResource);
                          break;
                        default:
                          throw Error(formatProdErrorMessage(468, flags));
                      }
                      currentResource[internalInstanceKey] = finishedWork;
                      markNodeAsHoistable(currentResource);
                      flags = currentResource;
                    }
                    finishedWork.stateNode = flags;
                  } else
                    mountHoistable(
                      hoistableRoot,
                      finishedWork.type,
                      finishedWork.stateNode
                    );
                else
                  finishedWork.stateNode = acquireResource(
                    hoistableRoot,
                    flags,
                    finishedWork.memoizedProps
                  );
              else
                currentResource !== flags ? (null === currentResource ? null !== current.stateNode && (current = current.stateNode, current.parentNode.removeChild(current)) : currentResource.count--, null === flags ? mountHoistable(
                  hoistableRoot,
                  finishedWork.type,
                  finishedWork.stateNode
                ) : acquireResource(
                  hoistableRoot,
                  flags,
                  finishedWork.memoizedProps
                )) : null === flags && null !== finishedWork.stateNode && commitHostUpdate(
                  finishedWork,
                  finishedWork.memoizedProps,
                  current.memoizedProps
                );
            }
            break;
          case 27:
            recursivelyTraverseMutationEffects(root2, finishedWork);
            commitReconciliationEffects(finishedWork);
            flags & 512 && (offscreenSubtreeWasHidden || null === current || safelyDetachRef(current, current.return));
            null !== current && flags & 4 && commitHostUpdate(
              finishedWork,
              finishedWork.memoizedProps,
              current.memoizedProps
            );
            break;
          case 5:
            recursivelyTraverseMutationEffects(root2, finishedWork);
            commitReconciliationEffects(finishedWork);
            flags & 512 && (offscreenSubtreeWasHidden || null === current || safelyDetachRef(current, current.return));
            if (finishedWork.flags & 32) {
              hoistableRoot = finishedWork.stateNode;
              try {
                setTextContent(hoistableRoot, "");
              } catch (error) {
                captureCommitPhaseError(finishedWork, finishedWork.return, error);
              }
            }
            flags & 4 && null != finishedWork.stateNode && (hoistableRoot = finishedWork.memoizedProps, commitHostUpdate(
              finishedWork,
              hoistableRoot,
              null !== current ? current.memoizedProps : hoistableRoot
            ));
            flags & 1024 && (needsFormReset = true);
            break;
          case 6:
            recursivelyTraverseMutationEffects(root2, finishedWork);
            commitReconciliationEffects(finishedWork);
            if (flags & 4) {
              if (null === finishedWork.stateNode)
                throw Error(formatProdErrorMessage(162));
              flags = finishedWork.memoizedProps;
              current = finishedWork.stateNode;
              try {
                current.nodeValue = flags;
              } catch (error) {
                captureCommitPhaseError(finishedWork, finishedWork.return, error);
              }
            }
            break;
          case 3:
            tagCaches = null;
            hoistableRoot = currentHoistableRoot;
            currentHoistableRoot = getHoistableRoot(root2.containerInfo);
            recursivelyTraverseMutationEffects(root2, finishedWork);
            currentHoistableRoot = hoistableRoot;
            commitReconciliationEffects(finishedWork);
            if (flags & 4 && null !== current && current.memoizedState.isDehydrated)
              try {
                retryIfBlockedOn(root2.containerInfo);
              } catch (error) {
                captureCommitPhaseError(finishedWork, finishedWork.return, error);
              }
            needsFormReset && (needsFormReset = false, recursivelyResetForms(finishedWork));
            break;
          case 4:
            flags = currentHoistableRoot;
            currentHoistableRoot = getHoistableRoot(
              finishedWork.stateNode.containerInfo
            );
            recursivelyTraverseMutationEffects(root2, finishedWork);
            commitReconciliationEffects(finishedWork);
            currentHoistableRoot = flags;
            break;
          case 12:
            recursivelyTraverseMutationEffects(root2, finishedWork);
            commitReconciliationEffects(finishedWork);
            break;
          case 31:
            recursivelyTraverseMutationEffects(root2, finishedWork);
            commitReconciliationEffects(finishedWork);
            flags & 4 && (flags = finishedWork.updateQueue, null !== flags && (finishedWork.updateQueue = null, attachSuspenseRetryListeners(finishedWork, flags)));
            break;
          case 13:
            recursivelyTraverseMutationEffects(root2, finishedWork);
            commitReconciliationEffects(finishedWork);
            finishedWork.child.flags & 8192 && null !== finishedWork.memoizedState !== (null !== current && null !== current.memoizedState) && (globalMostRecentFallbackTime = now());
            flags & 4 && (flags = finishedWork.updateQueue, null !== flags && (finishedWork.updateQueue = null, attachSuspenseRetryListeners(finishedWork, flags)));
            break;
          case 22:
            hoistableRoot = null !== finishedWork.memoizedState;
            var wasHidden = null !== current && null !== current.memoizedState, prevOffscreenSubtreeIsHidden = offscreenSubtreeIsHidden, prevOffscreenSubtreeWasHidden = offscreenSubtreeWasHidden;
            offscreenSubtreeIsHidden = prevOffscreenSubtreeIsHidden || hoistableRoot;
            offscreenSubtreeWasHidden = prevOffscreenSubtreeWasHidden || wasHidden;
            recursivelyTraverseMutationEffects(root2, finishedWork);
            offscreenSubtreeWasHidden = prevOffscreenSubtreeWasHidden;
            offscreenSubtreeIsHidden = prevOffscreenSubtreeIsHidden;
            commitReconciliationEffects(finishedWork);
            if (flags & 8192)
              a: for (root2 = finishedWork.stateNode, root2._visibility = hoistableRoot ? root2._visibility & -2 : root2._visibility | 1, hoistableRoot && (null === current || wasHidden || offscreenSubtreeIsHidden || offscreenSubtreeWasHidden || recursivelyTraverseDisappearLayoutEffects(finishedWork)), current = null, root2 = finishedWork; ; ) {
                if (5 === root2.tag || 26 === root2.tag) {
                  if (null === current) {
                    wasHidden = current = root2;
                    try {
                      if (currentResource = wasHidden.stateNode, hoistableRoot)
                        maybeNodes = currentResource.style, "function" === typeof maybeNodes.setProperty ? maybeNodes.setProperty("display", "none", "important") : maybeNodes.display = "none";
                      else {
                        i = wasHidden.stateNode;
                        var styleProp = wasHidden.memoizedProps.style, display = void 0 !== styleProp && null !== styleProp && styleProp.hasOwnProperty("display") ? styleProp.display : null;
                        i.style.display = null == display || "boolean" === typeof display ? "" : ("" + display).trim();
                      }
                    } catch (error) {
                      captureCommitPhaseError(wasHidden, wasHidden.return, error);
                    }
                  }
                } else if (6 === root2.tag) {
                  if (null === current) {
                    wasHidden = root2;
                    try {
                      wasHidden.stateNode.nodeValue = hoistableRoot ? "" : wasHidden.memoizedProps;
                    } catch (error) {
                      captureCommitPhaseError(wasHidden, wasHidden.return, error);
                    }
                  }
                } else if (18 === root2.tag) {
                  if (null === current) {
                    wasHidden = root2;
                    try {
                      var instance = wasHidden.stateNode;
                      hoistableRoot ? hideOrUnhideDehydratedBoundary(instance, true) : hideOrUnhideDehydratedBoundary(wasHidden.stateNode, false);
                    } catch (error) {
                      captureCommitPhaseError(wasHidden, wasHidden.return, error);
                    }
                  }
                } else if ((22 !== root2.tag && 23 !== root2.tag || null === root2.memoizedState || root2 === finishedWork) && null !== root2.child) {
                  root2.child.return = root2;
                  root2 = root2.child;
                  continue;
                }
                if (root2 === finishedWork) break a;
                for (; null === root2.sibling; ) {
                  if (null === root2.return || root2.return === finishedWork) break a;
                  current === root2 && (current = null);
                  root2 = root2.return;
                }
                current === root2 && (current = null);
                root2.sibling.return = root2.return;
                root2 = root2.sibling;
              }
            flags & 4 && (flags = finishedWork.updateQueue, null !== flags && (current = flags.retryQueue, null !== current && (flags.retryQueue = null, attachSuspenseRetryListeners(finishedWork, current))));
            break;
          case 19:
            recursivelyTraverseMutationEffects(root2, finishedWork);
            commitReconciliationEffects(finishedWork);
            flags & 4 && (flags = finishedWork.updateQueue, null !== flags && (finishedWork.updateQueue = null, attachSuspenseRetryListeners(finishedWork, flags)));
            break;
          case 30:
            break;
          case 21:
            break;
          default:
            recursivelyTraverseMutationEffects(root2, finishedWork), commitReconciliationEffects(finishedWork);
        }
      }
      function commitReconciliationEffects(finishedWork) {
        var flags = finishedWork.flags;
        if (flags & 2) {
          try {
            for (var hostParentFiber, parentFiber = finishedWork.return; null !== parentFiber; ) {
              if (isHostParent(parentFiber)) {
                hostParentFiber = parentFiber;
                break;
              }
              parentFiber = parentFiber.return;
            }
            if (null == hostParentFiber) throw Error(formatProdErrorMessage(160));
            switch (hostParentFiber.tag) {
              case 27:
                var parent = hostParentFiber.stateNode, before = getHostSibling(finishedWork);
                insertOrAppendPlacementNode(finishedWork, before, parent);
                break;
              case 5:
                var parent$141 = hostParentFiber.stateNode;
                hostParentFiber.flags & 32 && (setTextContent(parent$141, ""), hostParentFiber.flags &= -33);
                var before$142 = getHostSibling(finishedWork);
                insertOrAppendPlacementNode(finishedWork, before$142, parent$141);
                break;
              case 3:
              case 4:
                var parent$143 = hostParentFiber.stateNode.containerInfo, before$144 = getHostSibling(finishedWork);
                insertOrAppendPlacementNodeIntoContainer(
                  finishedWork,
                  before$144,
                  parent$143
                );
                break;
              default:
                throw Error(formatProdErrorMessage(161));
            }
          } catch (error) {
            captureCommitPhaseError(finishedWork, finishedWork.return, error);
          }
          finishedWork.flags &= -3;
        }
        flags & 4096 && (finishedWork.flags &= -4097);
      }
      function recursivelyResetForms(parentFiber) {
        if (parentFiber.subtreeFlags & 1024)
          for (parentFiber = parentFiber.child; null !== parentFiber; ) {
            var fiber = parentFiber;
            recursivelyResetForms(fiber);
            5 === fiber.tag && fiber.flags & 1024 && fiber.stateNode.reset();
            parentFiber = parentFiber.sibling;
          }
      }
      function recursivelyTraverseLayoutEffects(root2, parentFiber) {
        if (parentFiber.subtreeFlags & 8772)
          for (parentFiber = parentFiber.child; null !== parentFiber; )
            commitLayoutEffectOnFiber(root2, parentFiber.alternate, parentFiber), parentFiber = parentFiber.sibling;
      }
      function recursivelyTraverseDisappearLayoutEffects(parentFiber) {
        for (parentFiber = parentFiber.child; null !== parentFiber; ) {
          var finishedWork = parentFiber;
          switch (finishedWork.tag) {
            case 0:
            case 11:
            case 14:
            case 15:
              commitHookEffectListUnmount(4, finishedWork, finishedWork.return);
              recursivelyTraverseDisappearLayoutEffects(finishedWork);
              break;
            case 1:
              safelyDetachRef(finishedWork, finishedWork.return);
              var instance = finishedWork.stateNode;
              "function" === typeof instance.componentWillUnmount && safelyCallComponentWillUnmount(
                finishedWork,
                finishedWork.return,
                instance
              );
              recursivelyTraverseDisappearLayoutEffects(finishedWork);
              break;
            case 27:
              releaseSingletonInstance(finishedWork.stateNode);
            case 26:
            case 5:
              safelyDetachRef(finishedWork, finishedWork.return);
              recursivelyTraverseDisappearLayoutEffects(finishedWork);
              break;
            case 22:
              null === finishedWork.memoizedState && recursivelyTraverseDisappearLayoutEffects(finishedWork);
              break;
            case 30:
              recursivelyTraverseDisappearLayoutEffects(finishedWork);
              break;
            default:
              recursivelyTraverseDisappearLayoutEffects(finishedWork);
          }
          parentFiber = parentFiber.sibling;
        }
      }
      function recursivelyTraverseReappearLayoutEffects(finishedRoot$jscomp$0, parentFiber, includeWorkInProgressEffects) {
        includeWorkInProgressEffects = includeWorkInProgressEffects && 0 !== (parentFiber.subtreeFlags & 8772);
        for (parentFiber = parentFiber.child; null !== parentFiber; ) {
          var current = parentFiber.alternate, finishedRoot = finishedRoot$jscomp$0, finishedWork = parentFiber, flags = finishedWork.flags;
          switch (finishedWork.tag) {
            case 0:
            case 11:
            case 15:
              recursivelyTraverseReappearLayoutEffects(
                finishedRoot,
                finishedWork,
                includeWorkInProgressEffects
              );
              commitHookEffectListMount(4, finishedWork);
              break;
            case 1:
              recursivelyTraverseReappearLayoutEffects(
                finishedRoot,
                finishedWork,
                includeWorkInProgressEffects
              );
              current = finishedWork;
              finishedRoot = current.stateNode;
              if ("function" === typeof finishedRoot.componentDidMount)
                try {
                  finishedRoot.componentDidMount();
                } catch (error) {
                  captureCommitPhaseError(current, current.return, error);
                }
              current = finishedWork;
              finishedRoot = current.updateQueue;
              if (null !== finishedRoot) {
                var instance = current.stateNode;
                try {
                  var hiddenCallbacks = finishedRoot.shared.hiddenCallbacks;
                  if (null !== hiddenCallbacks)
                    for (finishedRoot.shared.hiddenCallbacks = null, finishedRoot = 0; finishedRoot < hiddenCallbacks.length; finishedRoot++)
                      callCallback(hiddenCallbacks[finishedRoot], instance);
                } catch (error) {
                  captureCommitPhaseError(current, current.return, error);
                }
              }
              includeWorkInProgressEffects && flags & 64 && commitClassCallbacks(finishedWork);
              safelyAttachRef(finishedWork, finishedWork.return);
              break;
            case 27:
              commitHostSingletonAcquisition(finishedWork);
            case 26:
            case 5:
              recursivelyTraverseReappearLayoutEffects(
                finishedRoot,
                finishedWork,
                includeWorkInProgressEffects
              );
              includeWorkInProgressEffects && null === current && flags & 4 && commitHostMount(finishedWork);
              safelyAttachRef(finishedWork, finishedWork.return);
              break;
            case 12:
              recursivelyTraverseReappearLayoutEffects(
                finishedRoot,
                finishedWork,
                includeWorkInProgressEffects
              );
              break;
            case 31:
              recursivelyTraverseReappearLayoutEffects(
                finishedRoot,
                finishedWork,
                includeWorkInProgressEffects
              );
              includeWorkInProgressEffects && flags & 4 && commitActivityHydrationCallbacks(finishedRoot, finishedWork);
              break;
            case 13:
              recursivelyTraverseReappearLayoutEffects(
                finishedRoot,
                finishedWork,
                includeWorkInProgressEffects
              );
              includeWorkInProgressEffects && flags & 4 && commitSuspenseHydrationCallbacks(finishedRoot, finishedWork);
              break;
            case 22:
              null === finishedWork.memoizedState && recursivelyTraverseReappearLayoutEffects(
                finishedRoot,
                finishedWork,
                includeWorkInProgressEffects
              );
              safelyAttachRef(finishedWork, finishedWork.return);
              break;
            case 30:
              break;
            default:
              recursivelyTraverseReappearLayoutEffects(
                finishedRoot,
                finishedWork,
                includeWorkInProgressEffects
              );
          }
          parentFiber = parentFiber.sibling;
        }
      }
      function commitOffscreenPassiveMountEffects(current, finishedWork) {
        var previousCache = null;
        null !== current && null !== current.memoizedState && null !== current.memoizedState.cachePool && (previousCache = current.memoizedState.cachePool.pool);
        current = null;
        null !== finishedWork.memoizedState && null !== finishedWork.memoizedState.cachePool && (current = finishedWork.memoizedState.cachePool.pool);
        current !== previousCache && (null != current && current.refCount++, null != previousCache && releaseCache(previousCache));
      }
      function commitCachePassiveMountEffect(current, finishedWork) {
        current = null;
        null !== finishedWork.alternate && (current = finishedWork.alternate.memoizedState.cache);
        finishedWork = finishedWork.memoizedState.cache;
        finishedWork !== current && (finishedWork.refCount++, null != current && releaseCache(current));
      }
      function recursivelyTraversePassiveMountEffects(root2, parentFiber, committedLanes, committedTransitions) {
        if (parentFiber.subtreeFlags & 10256)
          for (parentFiber = parentFiber.child; null !== parentFiber; )
            commitPassiveMountOnFiber(
              root2,
              parentFiber,
              committedLanes,
              committedTransitions
            ), parentFiber = parentFiber.sibling;
      }
      function commitPassiveMountOnFiber(finishedRoot, finishedWork, committedLanes, committedTransitions) {
        var flags = finishedWork.flags;
        switch (finishedWork.tag) {
          case 0:
          case 11:
          case 15:
            recursivelyTraversePassiveMountEffects(
              finishedRoot,
              finishedWork,
              committedLanes,
              committedTransitions
            );
            flags & 2048 && commitHookEffectListMount(9, finishedWork);
            break;
          case 1:
            recursivelyTraversePassiveMountEffects(
              finishedRoot,
              finishedWork,
              committedLanes,
              committedTransitions
            );
            break;
          case 3:
            recursivelyTraversePassiveMountEffects(
              finishedRoot,
              finishedWork,
              committedLanes,
              committedTransitions
            );
            flags & 2048 && (finishedRoot = null, null !== finishedWork.alternate && (finishedRoot = finishedWork.alternate.memoizedState.cache), finishedWork = finishedWork.memoizedState.cache, finishedWork !== finishedRoot && (finishedWork.refCount++, null != finishedRoot && releaseCache(finishedRoot)));
            break;
          case 12:
            if (flags & 2048) {
              recursivelyTraversePassiveMountEffects(
                finishedRoot,
                finishedWork,
                committedLanes,
                committedTransitions
              );
              finishedRoot = finishedWork.stateNode;
              try {
                var _finishedWork$memoize2 = finishedWork.memoizedProps, id = _finishedWork$memoize2.id, onPostCommit = _finishedWork$memoize2.onPostCommit;
                "function" === typeof onPostCommit && onPostCommit(
                  id,
                  null === finishedWork.alternate ? "mount" : "update",
                  finishedRoot.passiveEffectDuration,
                  -0
                );
              } catch (error) {
                captureCommitPhaseError(finishedWork, finishedWork.return, error);
              }
            } else
              recursivelyTraversePassiveMountEffects(
                finishedRoot,
                finishedWork,
                committedLanes,
                committedTransitions
              );
            break;
          case 31:
            recursivelyTraversePassiveMountEffects(
              finishedRoot,
              finishedWork,
              committedLanes,
              committedTransitions
            );
            break;
          case 13:
            recursivelyTraversePassiveMountEffects(
              finishedRoot,
              finishedWork,
              committedLanes,
              committedTransitions
            );
            break;
          case 23:
            break;
          case 22:
            _finishedWork$memoize2 = finishedWork.stateNode;
            id = finishedWork.alternate;
            null !== finishedWork.memoizedState ? _finishedWork$memoize2._visibility & 2 ? recursivelyTraversePassiveMountEffects(
              finishedRoot,
              finishedWork,
              committedLanes,
              committedTransitions
            ) : recursivelyTraverseAtomicPassiveEffects(finishedRoot, finishedWork) : _finishedWork$memoize2._visibility & 2 ? recursivelyTraversePassiveMountEffects(
              finishedRoot,
              finishedWork,
              committedLanes,
              committedTransitions
            ) : (_finishedWork$memoize2._visibility |= 2, recursivelyTraverseReconnectPassiveEffects(
              finishedRoot,
              finishedWork,
              committedLanes,
              committedTransitions,
              0 !== (finishedWork.subtreeFlags & 10256) || false
            ));
            flags & 2048 && commitOffscreenPassiveMountEffects(id, finishedWork);
            break;
          case 24:
            recursivelyTraversePassiveMountEffects(
              finishedRoot,
              finishedWork,
              committedLanes,
              committedTransitions
            );
            flags & 2048 && commitCachePassiveMountEffect(finishedWork.alternate, finishedWork);
            break;
          default:
            recursivelyTraversePassiveMountEffects(
              finishedRoot,
              finishedWork,
              committedLanes,
              committedTransitions
            );
        }
      }
      function recursivelyTraverseReconnectPassiveEffects(finishedRoot$jscomp$0, parentFiber, committedLanes$jscomp$0, committedTransitions$jscomp$0, includeWorkInProgressEffects) {
        includeWorkInProgressEffects = includeWorkInProgressEffects && (0 !== (parentFiber.subtreeFlags & 10256) || false);
        for (parentFiber = parentFiber.child; null !== parentFiber; ) {
          var finishedRoot = finishedRoot$jscomp$0, finishedWork = parentFiber, committedLanes = committedLanes$jscomp$0, committedTransitions = committedTransitions$jscomp$0, flags = finishedWork.flags;
          switch (finishedWork.tag) {
            case 0:
            case 11:
            case 15:
              recursivelyTraverseReconnectPassiveEffects(
                finishedRoot,
                finishedWork,
                committedLanes,
                committedTransitions,
                includeWorkInProgressEffects
              );
              commitHookEffectListMount(8, finishedWork);
              break;
            case 23:
              break;
            case 22:
              var instance = finishedWork.stateNode;
              null !== finishedWork.memoizedState ? instance._visibility & 2 ? recursivelyTraverseReconnectPassiveEffects(
                finishedRoot,
                finishedWork,
                committedLanes,
                committedTransitions,
                includeWorkInProgressEffects
              ) : recursivelyTraverseAtomicPassiveEffects(
                finishedRoot,
                finishedWork
              ) : (instance._visibility |= 2, recursivelyTraverseReconnectPassiveEffects(
                finishedRoot,
                finishedWork,
                committedLanes,
                committedTransitions,
                includeWorkInProgressEffects
              ));
              includeWorkInProgressEffects && flags & 2048 && commitOffscreenPassiveMountEffects(
                finishedWork.alternate,
                finishedWork
              );
              break;
            case 24:
              recursivelyTraverseReconnectPassiveEffects(
                finishedRoot,
                finishedWork,
                committedLanes,
                committedTransitions,
                includeWorkInProgressEffects
              );
              includeWorkInProgressEffects && flags & 2048 && commitCachePassiveMountEffect(finishedWork.alternate, finishedWork);
              break;
            default:
              recursivelyTraverseReconnectPassiveEffects(
                finishedRoot,
                finishedWork,
                committedLanes,
                committedTransitions,
                includeWorkInProgressEffects
              );
          }
          parentFiber = parentFiber.sibling;
        }
      }
      function recursivelyTraverseAtomicPassiveEffects(finishedRoot$jscomp$0, parentFiber) {
        if (parentFiber.subtreeFlags & 10256)
          for (parentFiber = parentFiber.child; null !== parentFiber; ) {
            var finishedRoot = finishedRoot$jscomp$0, finishedWork = parentFiber, flags = finishedWork.flags;
            switch (finishedWork.tag) {
              case 22:
                recursivelyTraverseAtomicPassiveEffects(finishedRoot, finishedWork);
                flags & 2048 && commitOffscreenPassiveMountEffects(
                  finishedWork.alternate,
                  finishedWork
                );
                break;
              case 24:
                recursivelyTraverseAtomicPassiveEffects(finishedRoot, finishedWork);
                flags & 2048 && commitCachePassiveMountEffect(finishedWork.alternate, finishedWork);
                break;
              default:
                recursivelyTraverseAtomicPassiveEffects(finishedRoot, finishedWork);
            }
            parentFiber = parentFiber.sibling;
          }
      }
      var suspenseyCommitFlag = 8192;
      function recursivelyAccumulateSuspenseyCommit(parentFiber, committedLanes, suspendedState) {
        if (parentFiber.subtreeFlags & suspenseyCommitFlag)
          for (parentFiber = parentFiber.child; null !== parentFiber; )
            accumulateSuspenseyCommitOnFiber(
              parentFiber,
              committedLanes,
              suspendedState
            ), parentFiber = parentFiber.sibling;
      }
      function accumulateSuspenseyCommitOnFiber(fiber, committedLanes, suspendedState) {
        switch (fiber.tag) {
          case 26:
            recursivelyAccumulateSuspenseyCommit(
              fiber,
              committedLanes,
              suspendedState
            );
            fiber.flags & suspenseyCommitFlag && null !== fiber.memoizedState && suspendResource(
              suspendedState,
              currentHoistableRoot,
              fiber.memoizedState,
              fiber.memoizedProps
            );
            break;
          case 5:
            recursivelyAccumulateSuspenseyCommit(
              fiber,
              committedLanes,
              suspendedState
            );
            break;
          case 3:
          case 4:
            var previousHoistableRoot = currentHoistableRoot;
            currentHoistableRoot = getHoistableRoot(fiber.stateNode.containerInfo);
            recursivelyAccumulateSuspenseyCommit(
              fiber,
              committedLanes,
              suspendedState
            );
            currentHoistableRoot = previousHoistableRoot;
            break;
          case 22:
            null === fiber.memoizedState && (previousHoistableRoot = fiber.alternate, null !== previousHoistableRoot && null !== previousHoistableRoot.memoizedState ? (previousHoistableRoot = suspenseyCommitFlag, suspenseyCommitFlag = 16777216, recursivelyAccumulateSuspenseyCommit(
              fiber,
              committedLanes,
              suspendedState
            ), suspenseyCommitFlag = previousHoistableRoot) : recursivelyAccumulateSuspenseyCommit(
              fiber,
              committedLanes,
              suspendedState
            ));
            break;
          default:
            recursivelyAccumulateSuspenseyCommit(
              fiber,
              committedLanes,
              suspendedState
            );
        }
      }
      function detachAlternateSiblings(parentFiber) {
        var previousFiber = parentFiber.alternate;
        if (null !== previousFiber && (parentFiber = previousFiber.child, null !== parentFiber)) {
          previousFiber.child = null;
          do
            previousFiber = parentFiber.sibling, parentFiber.sibling = null, parentFiber = previousFiber;
          while (null !== parentFiber);
        }
      }
      function recursivelyTraversePassiveUnmountEffects(parentFiber) {
        var deletions = parentFiber.deletions;
        if (0 !== (parentFiber.flags & 16)) {
          if (null !== deletions)
            for (var i = 0; i < deletions.length; i++) {
              var childToDelete = deletions[i];
              nextEffect = childToDelete;
              commitPassiveUnmountEffectsInsideOfDeletedTree_begin(
                childToDelete,
                parentFiber
              );
            }
          detachAlternateSiblings(parentFiber);
        }
        if (parentFiber.subtreeFlags & 10256)
          for (parentFiber = parentFiber.child; null !== parentFiber; )
            commitPassiveUnmountOnFiber(parentFiber), parentFiber = parentFiber.sibling;
      }
      function commitPassiveUnmountOnFiber(finishedWork) {
        switch (finishedWork.tag) {
          case 0:
          case 11:
          case 15:
            recursivelyTraversePassiveUnmountEffects(finishedWork);
            finishedWork.flags & 2048 && commitHookEffectListUnmount(9, finishedWork, finishedWork.return);
            break;
          case 3:
            recursivelyTraversePassiveUnmountEffects(finishedWork);
            break;
          case 12:
            recursivelyTraversePassiveUnmountEffects(finishedWork);
            break;
          case 22:
            var instance = finishedWork.stateNode;
            null !== finishedWork.memoizedState && instance._visibility & 2 && (null === finishedWork.return || 13 !== finishedWork.return.tag) ? (instance._visibility &= -3, recursivelyTraverseDisconnectPassiveEffects(finishedWork)) : recursivelyTraversePassiveUnmountEffects(finishedWork);
            break;
          default:
            recursivelyTraversePassiveUnmountEffects(finishedWork);
        }
      }
      function recursivelyTraverseDisconnectPassiveEffects(parentFiber) {
        var deletions = parentFiber.deletions;
        if (0 !== (parentFiber.flags & 16)) {
          if (null !== deletions)
            for (var i = 0; i < deletions.length; i++) {
              var childToDelete = deletions[i];
              nextEffect = childToDelete;
              commitPassiveUnmountEffectsInsideOfDeletedTree_begin(
                childToDelete,
                parentFiber
              );
            }
          detachAlternateSiblings(parentFiber);
        }
        for (parentFiber = parentFiber.child; null !== parentFiber; ) {
          deletions = parentFiber;
          switch (deletions.tag) {
            case 0:
            case 11:
            case 15:
              commitHookEffectListUnmount(8, deletions, deletions.return);
              recursivelyTraverseDisconnectPassiveEffects(deletions);
              break;
            case 22:
              i = deletions.stateNode;
              i._visibility & 2 && (i._visibility &= -3, recursivelyTraverseDisconnectPassiveEffects(deletions));
              break;
            default:
              recursivelyTraverseDisconnectPassiveEffects(deletions);
          }
          parentFiber = parentFiber.sibling;
        }
      }
      function commitPassiveUnmountEffectsInsideOfDeletedTree_begin(deletedSubtreeRoot, nearestMountedAncestor) {
        for (; null !== nextEffect; ) {
          var fiber = nextEffect;
          switch (fiber.tag) {
            case 0:
            case 11:
            case 15:
              commitHookEffectListUnmount(8, fiber, nearestMountedAncestor);
              break;
            case 23:
            case 22:
              if (null !== fiber.memoizedState && null !== fiber.memoizedState.cachePool) {
                var cache = fiber.memoizedState.cachePool.pool;
                null != cache && cache.refCount++;
              }
              break;
            case 24:
              releaseCache(fiber.memoizedState.cache);
          }
          cache = fiber.child;
          if (null !== cache) cache.return = fiber, nextEffect = cache;
          else
            a: for (fiber = deletedSubtreeRoot; null !== nextEffect; ) {
              cache = nextEffect;
              var sibling = cache.sibling, returnFiber = cache.return;
              detachFiberAfterEffects(cache);
              if (cache === fiber) {
                nextEffect = null;
                break a;
              }
              if (null !== sibling) {
                sibling.return = returnFiber;
                nextEffect = sibling;
                break a;
              }
              nextEffect = returnFiber;
            }
        }
      }
      var DefaultAsyncDispatcher = {
        getCacheForType: function(resourceType) {
          var cache = readContext(CacheContext), cacheForType = cache.data.get(resourceType);
          void 0 === cacheForType && (cacheForType = resourceType(), cache.data.set(resourceType, cacheForType));
          return cacheForType;
        },
        cacheSignal: function() {
          return readContext(CacheContext).controller.signal;
        }
      };
      var PossiblyWeakMap = "function" === typeof WeakMap ? WeakMap : Map;
      var executionContext = 0;
      var workInProgressRoot = null;
      var workInProgress = null;
      var workInProgressRootRenderLanes = 0;
      var workInProgressSuspendedReason = 0;
      var workInProgressThrownValue = null;
      var workInProgressRootDidSkipSuspendedSiblings = false;
      var workInProgressRootIsPrerendering = false;
      var workInProgressRootDidAttachPingListener = false;
      var entangledRenderLanes = 0;
      var workInProgressRootExitStatus = 0;
      var workInProgressRootSkippedLanes = 0;
      var workInProgressRootInterleavedUpdatedLanes = 0;
      var workInProgressRootPingedLanes = 0;
      var workInProgressDeferredLane = 0;
      var workInProgressSuspendedRetryLanes = 0;
      var workInProgressRootConcurrentErrors = null;
      var workInProgressRootRecoverableErrors = null;
      var workInProgressRootDidIncludeRecursiveRenderUpdate = false;
      var globalMostRecentFallbackTime = 0;
      var globalMostRecentTransitionTime = 0;
      var workInProgressRootRenderTargetTime = Infinity;
      var workInProgressTransitions = null;
      var legacyErrorBoundariesThatAlreadyFailed = null;
      var pendingEffectsStatus = 0;
      var pendingEffectsRoot = null;
      var pendingFinishedWork = null;
      var pendingEffectsLanes = 0;
      var pendingEffectsRemainingLanes = 0;
      var pendingPassiveTransitions = null;
      var pendingRecoverableErrors = null;
      var nestedUpdateCount = 0;
      var rootWithNestedUpdates = null;
      function requestUpdateLane() {
        return 0 !== (executionContext & 2) && 0 !== workInProgressRootRenderLanes ? workInProgressRootRenderLanes & -workInProgressRootRenderLanes : null !== ReactSharedInternals.T ? requestTransitionLane() : resolveUpdatePriority();
      }
      function requestDeferredLane() {
        if (0 === workInProgressDeferredLane)
          if (0 === (workInProgressRootRenderLanes & 536870912) || isHydrating) {
            var lane = nextTransitionDeferredLane;
            nextTransitionDeferredLane <<= 1;
            0 === (nextTransitionDeferredLane & 3932160) && (nextTransitionDeferredLane = 262144);
            workInProgressDeferredLane = lane;
          } else workInProgressDeferredLane = 536870912;
        lane = suspenseHandlerStackCursor.current;
        null !== lane && (lane.flags |= 32);
        return workInProgressDeferredLane;
      }
      function scheduleUpdateOnFiber(root2, fiber, lane) {
        if (root2 === workInProgressRoot && (2 === workInProgressSuspendedReason || 9 === workInProgressSuspendedReason) || null !== root2.cancelPendingCommit)
          prepareFreshStack(root2, 0), markRootSuspended(
            root2,
            workInProgressRootRenderLanes,
            workInProgressDeferredLane,
            false
          );
        markRootUpdated$1(root2, lane);
        if (0 === (executionContext & 2) || root2 !== workInProgressRoot)
          root2 === workInProgressRoot && (0 === (executionContext & 2) && (workInProgressRootInterleavedUpdatedLanes |= lane), 4 === workInProgressRootExitStatus && markRootSuspended(
            root2,
            workInProgressRootRenderLanes,
            workInProgressDeferredLane,
            false
          )), ensureRootIsScheduled(root2);
      }
      function performWorkOnRoot(root$jscomp$0, lanes, forceSync) {
        if (0 !== (executionContext & 6)) throw Error(formatProdErrorMessage(327));
        var shouldTimeSlice = !forceSync && 0 === (lanes & 127) && 0 === (lanes & root$jscomp$0.expiredLanes) || checkIfRootIsPrerendering(root$jscomp$0, lanes), exitStatus = shouldTimeSlice ? renderRootConcurrent(root$jscomp$0, lanes) : renderRootSync(root$jscomp$0, lanes, true), renderWasConcurrent = shouldTimeSlice;
        do {
          if (0 === exitStatus) {
            workInProgressRootIsPrerendering && !shouldTimeSlice && markRootSuspended(root$jscomp$0, lanes, 0, false);
            break;
          } else {
            forceSync = root$jscomp$0.current.alternate;
            if (renderWasConcurrent && !isRenderConsistentWithExternalStores(forceSync)) {
              exitStatus = renderRootSync(root$jscomp$0, lanes, false);
              renderWasConcurrent = false;
              continue;
            }
            if (2 === exitStatus) {
              renderWasConcurrent = lanes;
              if (root$jscomp$0.errorRecoveryDisabledLanes & renderWasConcurrent)
                var JSCompiler_inline_result = 0;
              else
                JSCompiler_inline_result = root$jscomp$0.pendingLanes & -536870913, JSCompiler_inline_result = 0 !== JSCompiler_inline_result ? JSCompiler_inline_result : JSCompiler_inline_result & 536870912 ? 536870912 : 0;
              if (0 !== JSCompiler_inline_result) {
                lanes = JSCompiler_inline_result;
                a: {
                  var root2 = root$jscomp$0;
                  exitStatus = workInProgressRootConcurrentErrors;
                  var wasRootDehydrated = root2.current.memoizedState.isDehydrated;
                  wasRootDehydrated && (prepareFreshStack(root2, JSCompiler_inline_result).flags |= 256);
                  JSCompiler_inline_result = renderRootSync(
                    root2,
                    JSCompiler_inline_result,
                    false
                  );
                  if (2 !== JSCompiler_inline_result) {
                    if (workInProgressRootDidAttachPingListener && !wasRootDehydrated) {
                      root2.errorRecoveryDisabledLanes |= renderWasConcurrent;
                      workInProgressRootInterleavedUpdatedLanes |= renderWasConcurrent;
                      exitStatus = 4;
                      break a;
                    }
                    renderWasConcurrent = workInProgressRootRecoverableErrors;
                    workInProgressRootRecoverableErrors = exitStatus;
                    null !== renderWasConcurrent && (null === workInProgressRootRecoverableErrors ? workInProgressRootRecoverableErrors = renderWasConcurrent : workInProgressRootRecoverableErrors.push.apply(
                      workInProgressRootRecoverableErrors,
                      renderWasConcurrent
                    ));
                  }
                  exitStatus = JSCompiler_inline_result;
                }
                renderWasConcurrent = false;
                if (2 !== exitStatus) continue;
              }
            }
            if (1 === exitStatus) {
              prepareFreshStack(root$jscomp$0, 0);
              markRootSuspended(root$jscomp$0, lanes, 0, true);
              break;
            }
            a: {
              shouldTimeSlice = root$jscomp$0;
              renderWasConcurrent = exitStatus;
              switch (renderWasConcurrent) {
                case 0:
                case 1:
                  throw Error(formatProdErrorMessage(345));
                case 4:
                  if ((lanes & 4194048) !== lanes) break;
                case 6:
                  markRootSuspended(
                    shouldTimeSlice,
                    lanes,
                    workInProgressDeferredLane,
                    !workInProgressRootDidSkipSuspendedSiblings
                  );
                  break a;
                case 2:
                  workInProgressRootRecoverableErrors = null;
                  break;
                case 3:
                case 5:
                  break;
                default:
                  throw Error(formatProdErrorMessage(329));
              }
              if ((lanes & 62914560) === lanes && (exitStatus = globalMostRecentFallbackTime + 300 - now(), 10 < exitStatus)) {
                markRootSuspended(
                  shouldTimeSlice,
                  lanes,
                  workInProgressDeferredLane,
                  !workInProgressRootDidSkipSuspendedSiblings
                );
                if (0 !== getNextLanes(shouldTimeSlice, 0, true)) break a;
                pendingEffectsLanes = lanes;
                shouldTimeSlice.timeoutHandle = scheduleTimeout(
                  commitRootWhenReady.bind(
                    null,
                    shouldTimeSlice,
                    forceSync,
                    workInProgressRootRecoverableErrors,
                    workInProgressTransitions,
                    workInProgressRootDidIncludeRecursiveRenderUpdate,
                    lanes,
                    workInProgressDeferredLane,
                    workInProgressRootInterleavedUpdatedLanes,
                    workInProgressSuspendedRetryLanes,
                    workInProgressRootDidSkipSuspendedSiblings,
                    renderWasConcurrent,
                    "Throttled",
                    -0,
                    0
                  ),
                  exitStatus
                );
                break a;
              }
              commitRootWhenReady(
                shouldTimeSlice,
                forceSync,
                workInProgressRootRecoverableErrors,
                workInProgressTransitions,
                workInProgressRootDidIncludeRecursiveRenderUpdate,
                lanes,
                workInProgressDeferredLane,
                workInProgressRootInterleavedUpdatedLanes,
                workInProgressSuspendedRetryLanes,
                workInProgressRootDidSkipSuspendedSiblings,
                renderWasConcurrent,
                null,
                -0,
                0
              );
            }
          }
          break;
        } while (1);
        ensureRootIsScheduled(root$jscomp$0);
      }
      function commitRootWhenReady(root2, finishedWork, recoverableErrors, transitions, didIncludeRenderPhaseUpdate, lanes, spawnedLane, updatedLanes, suspendedRetryLanes, didSkipSuspendedSiblings, exitStatus, suspendedCommitReason, completedRenderStartTime, completedRenderEndTime) {
        root2.timeoutHandle = -1;
        suspendedCommitReason = finishedWork.subtreeFlags;
        if (suspendedCommitReason & 8192 || 16785408 === (suspendedCommitReason & 16785408)) {
          suspendedCommitReason = {
            stylesheets: null,
            count: 0,
            imgCount: 0,
            imgBytes: 0,
            suspenseyImages: [],
            waitingForImages: true,
            waitingForViewTransition: false,
            unsuspend: noop$1
          };
          accumulateSuspenseyCommitOnFiber(
            finishedWork,
            lanes,
            suspendedCommitReason
          );
          var timeoutOffset = (lanes & 62914560) === lanes ? globalMostRecentFallbackTime - now() : (lanes & 4194048) === lanes ? globalMostRecentTransitionTime - now() : 0;
          timeoutOffset = waitForCommitToBeReady(
            suspendedCommitReason,
            timeoutOffset
          );
          if (null !== timeoutOffset) {
            pendingEffectsLanes = lanes;
            root2.cancelPendingCommit = timeoutOffset(
              commitRoot.bind(
                null,
                root2,
                finishedWork,
                lanes,
                recoverableErrors,
                transitions,
                didIncludeRenderPhaseUpdate,
                spawnedLane,
                updatedLanes,
                suspendedRetryLanes,
                exitStatus,
                suspendedCommitReason,
                null,
                completedRenderStartTime,
                completedRenderEndTime
              )
            );
            markRootSuspended(root2, lanes, spawnedLane, !didSkipSuspendedSiblings);
            return;
          }
        }
        commitRoot(
          root2,
          finishedWork,
          lanes,
          recoverableErrors,
          transitions,
          didIncludeRenderPhaseUpdate,
          spawnedLane,
          updatedLanes,
          suspendedRetryLanes
        );
      }
      function isRenderConsistentWithExternalStores(finishedWork) {
        for (var node = finishedWork; ; ) {
          var tag = node.tag;
          if ((0 === tag || 11 === tag || 15 === tag) && node.flags & 16384 && (tag = node.updateQueue, null !== tag && (tag = tag.stores, null !== tag)))
            for (var i = 0; i < tag.length; i++) {
              var check = tag[i], getSnapshot = check.getSnapshot;
              check = check.value;
              try {
                if (!objectIs(getSnapshot(), check)) return false;
              } catch (error) {
                return false;
              }
            }
          tag = node.child;
          if (node.subtreeFlags & 16384 && null !== tag)
            tag.return = node, node = tag;
          else {
            if (node === finishedWork) break;
            for (; null === node.sibling; ) {
              if (null === node.return || node.return === finishedWork) return true;
              node = node.return;
            }
            node.sibling.return = node.return;
            node = node.sibling;
          }
        }
        return true;
      }
      function markRootSuspended(root2, suspendedLanes, spawnedLane, didAttemptEntireTree) {
        suspendedLanes &= ~workInProgressRootPingedLanes;
        suspendedLanes &= ~workInProgressRootInterleavedUpdatedLanes;
        root2.suspendedLanes |= suspendedLanes;
        root2.pingedLanes &= ~suspendedLanes;
        didAttemptEntireTree && (root2.warmLanes |= suspendedLanes);
        didAttemptEntireTree = root2.expirationTimes;
        for (var lanes = suspendedLanes; 0 < lanes; ) {
          var index$6 = 31 - clz32(lanes), lane = 1 << index$6;
          didAttemptEntireTree[index$6] = -1;
          lanes &= ~lane;
        }
        0 !== spawnedLane && markSpawnedDeferredLane(root2, spawnedLane, suspendedLanes);
      }
      function flushSyncWork$1() {
        return 0 === (executionContext & 6) ? (flushSyncWorkAcrossRoots_impl(0, false), false) : true;
      }
      function resetWorkInProgressStack() {
        if (null !== workInProgress) {
          if (0 === workInProgressSuspendedReason)
            var interruptedWork = workInProgress.return;
          else
            interruptedWork = workInProgress, lastContextDependency = currentlyRenderingFiber$1 = null, resetHooksOnUnwind(interruptedWork), thenableState$1 = null, thenableIndexCounter$1 = 0, interruptedWork = workInProgress;
          for (; null !== interruptedWork; )
            unwindInterruptedWork(interruptedWork.alternate, interruptedWork), interruptedWork = interruptedWork.return;
          workInProgress = null;
        }
      }
      function prepareFreshStack(root2, lanes) {
        var timeoutHandle = root2.timeoutHandle;
        -1 !== timeoutHandle && (root2.timeoutHandle = -1, cancelTimeout(timeoutHandle));
        timeoutHandle = root2.cancelPendingCommit;
        null !== timeoutHandle && (root2.cancelPendingCommit = null, timeoutHandle());
        pendingEffectsLanes = 0;
        resetWorkInProgressStack();
        workInProgressRoot = root2;
        workInProgress = timeoutHandle = createWorkInProgress(root2.current, null);
        workInProgressRootRenderLanes = lanes;
        workInProgressSuspendedReason = 0;
        workInProgressThrownValue = null;
        workInProgressRootDidSkipSuspendedSiblings = false;
        workInProgressRootIsPrerendering = checkIfRootIsPrerendering(root2, lanes);
        workInProgressRootDidAttachPingListener = false;
        workInProgressSuspendedRetryLanes = workInProgressDeferredLane = workInProgressRootPingedLanes = workInProgressRootInterleavedUpdatedLanes = workInProgressRootSkippedLanes = workInProgressRootExitStatus = 0;
        workInProgressRootRecoverableErrors = workInProgressRootConcurrentErrors = null;
        workInProgressRootDidIncludeRecursiveRenderUpdate = false;
        0 !== (lanes & 8) && (lanes |= lanes & 32);
        var allEntangledLanes = root2.entangledLanes;
        if (0 !== allEntangledLanes)
          for (root2 = root2.entanglements, allEntangledLanes &= lanes; 0 < allEntangledLanes; ) {
            var index$4 = 31 - clz32(allEntangledLanes), lane = 1 << index$4;
            lanes |= root2[index$4];
            allEntangledLanes &= ~lane;
          }
        entangledRenderLanes = lanes;
        finishQueueingConcurrentUpdates();
        return timeoutHandle;
      }
      function handleThrow(root2, thrownValue) {
        currentlyRenderingFiber = null;
        ReactSharedInternals.H = ContextOnlyDispatcher;
        thrownValue === SuspenseException || thrownValue === SuspenseActionException ? (thrownValue = getSuspendedThenable(), workInProgressSuspendedReason = 3) : thrownValue === SuspenseyCommitException ? (thrownValue = getSuspendedThenable(), workInProgressSuspendedReason = 4) : workInProgressSuspendedReason = thrownValue === SelectiveHydrationException ? 8 : null !== thrownValue && "object" === typeof thrownValue && "function" === typeof thrownValue.then ? 6 : 1;
        workInProgressThrownValue = thrownValue;
        null === workInProgress && (workInProgressRootExitStatus = 1, logUncaughtError(
          root2,
          createCapturedValueAtFiber(thrownValue, root2.current)
        ));
      }
      function shouldRemainOnPreviousScreen() {
        var handler = suspenseHandlerStackCursor.current;
        return null === handler ? true : (workInProgressRootRenderLanes & 4194048) === workInProgressRootRenderLanes ? null === shellBoundary ? true : false : (workInProgressRootRenderLanes & 62914560) === workInProgressRootRenderLanes || 0 !== (workInProgressRootRenderLanes & 536870912) ? handler === shellBoundary : false;
      }
      function pushDispatcher() {
        var prevDispatcher = ReactSharedInternals.H;
        ReactSharedInternals.H = ContextOnlyDispatcher;
        return null === prevDispatcher ? ContextOnlyDispatcher : prevDispatcher;
      }
      function pushAsyncDispatcher() {
        var prevAsyncDispatcher = ReactSharedInternals.A;
        ReactSharedInternals.A = DefaultAsyncDispatcher;
        return prevAsyncDispatcher;
      }
      function renderDidSuspendDelayIfPossible() {
        workInProgressRootExitStatus = 4;
        workInProgressRootDidSkipSuspendedSiblings || (workInProgressRootRenderLanes & 4194048) !== workInProgressRootRenderLanes && null !== suspenseHandlerStackCursor.current || (workInProgressRootIsPrerendering = true);
        0 === (workInProgressRootSkippedLanes & 134217727) && 0 === (workInProgressRootInterleavedUpdatedLanes & 134217727) || null === workInProgressRoot || markRootSuspended(
          workInProgressRoot,
          workInProgressRootRenderLanes,
          workInProgressDeferredLane,
          false
        );
      }
      function renderRootSync(root2, lanes, shouldYieldForPrerendering) {
        var prevExecutionContext = executionContext;
        executionContext |= 2;
        var prevDispatcher = pushDispatcher(), prevAsyncDispatcher = pushAsyncDispatcher();
        if (workInProgressRoot !== root2 || workInProgressRootRenderLanes !== lanes)
          workInProgressTransitions = null, prepareFreshStack(root2, lanes);
        lanes = false;
        var exitStatus = workInProgressRootExitStatus;
        a: do
          try {
            if (0 !== workInProgressSuspendedReason && null !== workInProgress) {
              var unitOfWork = workInProgress, thrownValue = workInProgressThrownValue;
              switch (workInProgressSuspendedReason) {
                case 8:
                  resetWorkInProgressStack();
                  exitStatus = 6;
                  break a;
                case 3:
                case 2:
                case 9:
                case 6:
                  null === suspenseHandlerStackCursor.current && (lanes = true);
                  var reason = workInProgressSuspendedReason;
                  workInProgressSuspendedReason = 0;
                  workInProgressThrownValue = null;
                  throwAndUnwindWorkLoop(root2, unitOfWork, thrownValue, reason);
                  if (shouldYieldForPrerendering && workInProgressRootIsPrerendering) {
                    exitStatus = 0;
                    break a;
                  }
                  break;
                default:
                  reason = workInProgressSuspendedReason, workInProgressSuspendedReason = 0, workInProgressThrownValue = null, throwAndUnwindWorkLoop(root2, unitOfWork, thrownValue, reason);
              }
            }
            workLoopSync();
            exitStatus = workInProgressRootExitStatus;
            break;
          } catch (thrownValue$165) {
            handleThrow(root2, thrownValue$165);
          }
        while (1);
        lanes && root2.shellSuspendCounter++;
        lastContextDependency = currentlyRenderingFiber$1 = null;
        executionContext = prevExecutionContext;
        ReactSharedInternals.H = prevDispatcher;
        ReactSharedInternals.A = prevAsyncDispatcher;
        null === workInProgress && (workInProgressRoot = null, workInProgressRootRenderLanes = 0, finishQueueingConcurrentUpdates());
        return exitStatus;
      }
      function workLoopSync() {
        for (; null !== workInProgress; ) performUnitOfWork(workInProgress);
      }
      function renderRootConcurrent(root2, lanes) {
        var prevExecutionContext = executionContext;
        executionContext |= 2;
        var prevDispatcher = pushDispatcher(), prevAsyncDispatcher = pushAsyncDispatcher();
        workInProgressRoot !== root2 || workInProgressRootRenderLanes !== lanes ? (workInProgressTransitions = null, workInProgressRootRenderTargetTime = now() + 500, prepareFreshStack(root2, lanes)) : workInProgressRootIsPrerendering = checkIfRootIsPrerendering(
          root2,
          lanes
        );
        a: do
          try {
            if (0 !== workInProgressSuspendedReason && null !== workInProgress) {
              lanes = workInProgress;
              var thrownValue = workInProgressThrownValue;
              b: switch (workInProgressSuspendedReason) {
                case 1:
                  workInProgressSuspendedReason = 0;
                  workInProgressThrownValue = null;
                  throwAndUnwindWorkLoop(root2, lanes, thrownValue, 1);
                  break;
                case 2:
                case 9:
                  if (isThenableResolved(thrownValue)) {
                    workInProgressSuspendedReason = 0;
                    workInProgressThrownValue = null;
                    replaySuspendedUnitOfWork(lanes);
                    break;
                  }
                  lanes = function() {
                    2 !== workInProgressSuspendedReason && 9 !== workInProgressSuspendedReason || workInProgressRoot !== root2 || (workInProgressSuspendedReason = 7);
                    ensureRootIsScheduled(root2);
                  };
                  thrownValue.then(lanes, lanes);
                  break a;
                case 3:
                  workInProgressSuspendedReason = 7;
                  break a;
                case 4:
                  workInProgressSuspendedReason = 5;
                  break a;
                case 7:
                  isThenableResolved(thrownValue) ? (workInProgressSuspendedReason = 0, workInProgressThrownValue = null, replaySuspendedUnitOfWork(lanes)) : (workInProgressSuspendedReason = 0, workInProgressThrownValue = null, throwAndUnwindWorkLoop(root2, lanes, thrownValue, 7));
                  break;
                case 5:
                  var resource = null;
                  switch (workInProgress.tag) {
                    case 26:
                      resource = workInProgress.memoizedState;
                    case 5:
                    case 27:
                      var hostFiber = workInProgress;
                      if (resource ? preloadResource(resource) : hostFiber.stateNode.complete) {
                        workInProgressSuspendedReason = 0;
                        workInProgressThrownValue = null;
                        var sibling = hostFiber.sibling;
                        if (null !== sibling) workInProgress = sibling;
                        else {
                          var returnFiber = hostFiber.return;
                          null !== returnFiber ? (workInProgress = returnFiber, completeUnitOfWork(returnFiber)) : workInProgress = null;
                        }
                        break b;
                      }
                  }
                  workInProgressSuspendedReason = 0;
                  workInProgressThrownValue = null;
                  throwAndUnwindWorkLoop(root2, lanes, thrownValue, 5);
                  break;
                case 6:
                  workInProgressSuspendedReason = 0;
                  workInProgressThrownValue = null;
                  throwAndUnwindWorkLoop(root2, lanes, thrownValue, 6);
                  break;
                case 8:
                  resetWorkInProgressStack();
                  workInProgressRootExitStatus = 6;
                  break a;
                default:
                  throw Error(formatProdErrorMessage(462));
              }
            }
            workLoopConcurrentByScheduler();
            break;
          } catch (thrownValue$167) {
            handleThrow(root2, thrownValue$167);
          }
        while (1);
        lastContextDependency = currentlyRenderingFiber$1 = null;
        ReactSharedInternals.H = prevDispatcher;
        ReactSharedInternals.A = prevAsyncDispatcher;
        executionContext = prevExecutionContext;
        if (null !== workInProgress) return 0;
        workInProgressRoot = null;
        workInProgressRootRenderLanes = 0;
        finishQueueingConcurrentUpdates();
        return workInProgressRootExitStatus;
      }
      function workLoopConcurrentByScheduler() {
        for (; null !== workInProgress && !shouldYield(); )
          performUnitOfWork(workInProgress);
      }
      function performUnitOfWork(unitOfWork) {
        var next = beginWork(unitOfWork.alternate, unitOfWork, entangledRenderLanes);
        unitOfWork.memoizedProps = unitOfWork.pendingProps;
        null === next ? completeUnitOfWork(unitOfWork) : workInProgress = next;
      }
      function replaySuspendedUnitOfWork(unitOfWork) {
        var next = unitOfWork;
        var current = next.alternate;
        switch (next.tag) {
          case 15:
          case 0:
            next = replayFunctionComponent(
              current,
              next,
              next.pendingProps,
              next.type,
              void 0,
              workInProgressRootRenderLanes
            );
            break;
          case 11:
            next = replayFunctionComponent(
              current,
              next,
              next.pendingProps,
              next.type.render,
              next.ref,
              workInProgressRootRenderLanes
            );
            break;
          case 5:
            resetHooksOnUnwind(next);
          default:
            unwindInterruptedWork(current, next), next = workInProgress = resetWorkInProgress(next, entangledRenderLanes), next = beginWork(current, next, entangledRenderLanes);
        }
        unitOfWork.memoizedProps = unitOfWork.pendingProps;
        null === next ? completeUnitOfWork(unitOfWork) : workInProgress = next;
      }
      function throwAndUnwindWorkLoop(root2, unitOfWork, thrownValue, suspendedReason) {
        lastContextDependency = currentlyRenderingFiber$1 = null;
        resetHooksOnUnwind(unitOfWork);
        thenableState$1 = null;
        thenableIndexCounter$1 = 0;
        var returnFiber = unitOfWork.return;
        try {
          if (throwException(
            root2,
            returnFiber,
            unitOfWork,
            thrownValue,
            workInProgressRootRenderLanes
          )) {
            workInProgressRootExitStatus = 1;
            logUncaughtError(
              root2,
              createCapturedValueAtFiber(thrownValue, root2.current)
            );
            workInProgress = null;
            return;
          }
        } catch (error) {
          if (null !== returnFiber) throw workInProgress = returnFiber, error;
          workInProgressRootExitStatus = 1;
          logUncaughtError(
            root2,
            createCapturedValueAtFiber(thrownValue, root2.current)
          );
          workInProgress = null;
          return;
        }
        if (unitOfWork.flags & 32768) {
          if (isHydrating || 1 === suspendedReason) root2 = true;
          else if (workInProgressRootIsPrerendering || 0 !== (workInProgressRootRenderLanes & 536870912))
            root2 = false;
          else if (workInProgressRootDidSkipSuspendedSiblings = root2 = true, 2 === suspendedReason || 9 === suspendedReason || 3 === suspendedReason || 6 === suspendedReason)
            suspendedReason = suspenseHandlerStackCursor.current, null !== suspendedReason && 13 === suspendedReason.tag && (suspendedReason.flags |= 16384);
          unwindUnitOfWork(unitOfWork, root2);
        } else completeUnitOfWork(unitOfWork);
      }
      function completeUnitOfWork(unitOfWork) {
        var completedWork = unitOfWork;
        do {
          if (0 !== (completedWork.flags & 32768)) {
            unwindUnitOfWork(
              completedWork,
              workInProgressRootDidSkipSuspendedSiblings
            );
            return;
          }
          unitOfWork = completedWork.return;
          var next = completeWork(
            completedWork.alternate,
            completedWork,
            entangledRenderLanes
          );
          if (null !== next) {
            workInProgress = next;
            return;
          }
          completedWork = completedWork.sibling;
          if (null !== completedWork) {
            workInProgress = completedWork;
            return;
          }
          workInProgress = completedWork = unitOfWork;
        } while (null !== completedWork);
        0 === workInProgressRootExitStatus && (workInProgressRootExitStatus = 5);
      }
      function unwindUnitOfWork(unitOfWork, skipSiblings) {
        do {
          var next = unwindWork(unitOfWork.alternate, unitOfWork);
          if (null !== next) {
            next.flags &= 32767;
            workInProgress = next;
            return;
          }
          next = unitOfWork.return;
          null !== next && (next.flags |= 32768, next.subtreeFlags = 0, next.deletions = null);
          if (!skipSiblings && (unitOfWork = unitOfWork.sibling, null !== unitOfWork)) {
            workInProgress = unitOfWork;
            return;
          }
          workInProgress = unitOfWork = next;
        } while (null !== unitOfWork);
        workInProgressRootExitStatus = 6;
        workInProgress = null;
      }
      function commitRoot(root2, finishedWork, lanes, recoverableErrors, transitions, didIncludeRenderPhaseUpdate, spawnedLane, updatedLanes, suspendedRetryLanes) {
        root2.cancelPendingCommit = null;
        do
          flushPendingEffects();
        while (0 !== pendingEffectsStatus);
        if (0 !== (executionContext & 6)) throw Error(formatProdErrorMessage(327));
        if (null !== finishedWork) {
          if (finishedWork === root2.current) throw Error(formatProdErrorMessage(177));
          didIncludeRenderPhaseUpdate = finishedWork.lanes | finishedWork.childLanes;
          didIncludeRenderPhaseUpdate |= concurrentlyUpdatedLanes;
          markRootFinished(
            root2,
            lanes,
            didIncludeRenderPhaseUpdate,
            spawnedLane,
            updatedLanes,
            suspendedRetryLanes
          );
          root2 === workInProgressRoot && (workInProgress = workInProgressRoot = null, workInProgressRootRenderLanes = 0);
          pendingFinishedWork = finishedWork;
          pendingEffectsRoot = root2;
          pendingEffectsLanes = lanes;
          pendingEffectsRemainingLanes = didIncludeRenderPhaseUpdate;
          pendingPassiveTransitions = transitions;
          pendingRecoverableErrors = recoverableErrors;
          0 !== (finishedWork.subtreeFlags & 10256) || 0 !== (finishedWork.flags & 10256) ? (root2.callbackNode = null, root2.callbackPriority = 0, scheduleCallback$1(NormalPriority$1, function() {
            flushPassiveEffects();
            return null;
          })) : (root2.callbackNode = null, root2.callbackPriority = 0);
          recoverableErrors = 0 !== (finishedWork.flags & 13878);
          if (0 !== (finishedWork.subtreeFlags & 13878) || recoverableErrors) {
            recoverableErrors = ReactSharedInternals.T;
            ReactSharedInternals.T = null;
            transitions = ReactDOMSharedInternals.p;
            ReactDOMSharedInternals.p = 2;
            spawnedLane = executionContext;
            executionContext |= 4;
            try {
              commitBeforeMutationEffects(root2, finishedWork, lanes);
            } finally {
              executionContext = spawnedLane, ReactDOMSharedInternals.p = transitions, ReactSharedInternals.T = recoverableErrors;
            }
          }
          pendingEffectsStatus = 1;
          flushMutationEffects();
          flushLayoutEffects();
          flushSpawnedWork();
        }
      }
      function flushMutationEffects() {
        if (1 === pendingEffectsStatus) {
          pendingEffectsStatus = 0;
          var root2 = pendingEffectsRoot, finishedWork = pendingFinishedWork, rootMutationHasEffect = 0 !== (finishedWork.flags & 13878);
          if (0 !== (finishedWork.subtreeFlags & 13878) || rootMutationHasEffect) {
            rootMutationHasEffect = ReactSharedInternals.T;
            ReactSharedInternals.T = null;
            var previousPriority = ReactDOMSharedInternals.p;
            ReactDOMSharedInternals.p = 2;
            var prevExecutionContext = executionContext;
            executionContext |= 4;
            try {
              commitMutationEffectsOnFiber(finishedWork, root2);
              var priorSelectionInformation = selectionInformation, curFocusedElem = getActiveElementDeep(root2.containerInfo), priorFocusedElem = priorSelectionInformation.focusedElem, priorSelectionRange = priorSelectionInformation.selectionRange;
              if (curFocusedElem !== priorFocusedElem && priorFocusedElem && priorFocusedElem.ownerDocument && containsNode(
                priorFocusedElem.ownerDocument.documentElement,
                priorFocusedElem
              )) {
                if (null !== priorSelectionRange && hasSelectionCapabilities(priorFocusedElem)) {
                  var start = priorSelectionRange.start, end = priorSelectionRange.end;
                  void 0 === end && (end = start);
                  if ("selectionStart" in priorFocusedElem)
                    priorFocusedElem.selectionStart = start, priorFocusedElem.selectionEnd = Math.min(
                      end,
                      priorFocusedElem.value.length
                    );
                  else {
                    var doc = priorFocusedElem.ownerDocument || document, win = doc && doc.defaultView || window;
                    if (win.getSelection) {
                      var selection = win.getSelection(), length = priorFocusedElem.textContent.length, start$jscomp$0 = Math.min(priorSelectionRange.start, length), end$jscomp$0 = void 0 === priorSelectionRange.end ? start$jscomp$0 : Math.min(priorSelectionRange.end, length);
                      !selection.extend && start$jscomp$0 > end$jscomp$0 && (curFocusedElem = end$jscomp$0, end$jscomp$0 = start$jscomp$0, start$jscomp$0 = curFocusedElem);
                      var startMarker = getNodeForCharacterOffset(
                        priorFocusedElem,
                        start$jscomp$0
                      ), endMarker = getNodeForCharacterOffset(
                        priorFocusedElem,
                        end$jscomp$0
                      );
                      if (startMarker && endMarker && (1 !== selection.rangeCount || selection.anchorNode !== startMarker.node || selection.anchorOffset !== startMarker.offset || selection.focusNode !== endMarker.node || selection.focusOffset !== endMarker.offset)) {
                        var range = doc.createRange();
                        range.setStart(startMarker.node, startMarker.offset);
                        selection.removeAllRanges();
                        start$jscomp$0 > end$jscomp$0 ? (selection.addRange(range), selection.extend(endMarker.node, endMarker.offset)) : (range.setEnd(endMarker.node, endMarker.offset), selection.addRange(range));
                      }
                    }
                  }
                }
                doc = [];
                for (selection = priorFocusedElem; selection = selection.parentNode; )
                  1 === selection.nodeType && doc.push({
                    element: selection,
                    left: selection.scrollLeft,
                    top: selection.scrollTop
                  });
                "function" === typeof priorFocusedElem.focus && priorFocusedElem.focus();
                for (priorFocusedElem = 0; priorFocusedElem < doc.length; priorFocusedElem++) {
                  var info = doc[priorFocusedElem];
                  info.element.scrollLeft = info.left;
                  info.element.scrollTop = info.top;
                }
              }
              _enabled = !!eventsEnabled;
              selectionInformation = eventsEnabled = null;
            } finally {
              executionContext = prevExecutionContext, ReactDOMSharedInternals.p = previousPriority, ReactSharedInternals.T = rootMutationHasEffect;
            }
          }
          root2.current = finishedWork;
          pendingEffectsStatus = 2;
        }
      }
      function flushLayoutEffects() {
        if (2 === pendingEffectsStatus) {
          pendingEffectsStatus = 0;
          var root2 = pendingEffectsRoot, finishedWork = pendingFinishedWork, rootHasLayoutEffect = 0 !== (finishedWork.flags & 8772);
          if (0 !== (finishedWork.subtreeFlags & 8772) || rootHasLayoutEffect) {
            rootHasLayoutEffect = ReactSharedInternals.T;
            ReactSharedInternals.T = null;
            var previousPriority = ReactDOMSharedInternals.p;
            ReactDOMSharedInternals.p = 2;
            var prevExecutionContext = executionContext;
            executionContext |= 4;
            try {
              commitLayoutEffectOnFiber(root2, finishedWork.alternate, finishedWork);
            } finally {
              executionContext = prevExecutionContext, ReactDOMSharedInternals.p = previousPriority, ReactSharedInternals.T = rootHasLayoutEffect;
            }
          }
          pendingEffectsStatus = 3;
        }
      }
      function flushSpawnedWork() {
        if (4 === pendingEffectsStatus || 3 === pendingEffectsStatus) {
          pendingEffectsStatus = 0;
          requestPaint();
          var root2 = pendingEffectsRoot, finishedWork = pendingFinishedWork, lanes = pendingEffectsLanes, recoverableErrors = pendingRecoverableErrors;
          0 !== (finishedWork.subtreeFlags & 10256) || 0 !== (finishedWork.flags & 10256) ? pendingEffectsStatus = 5 : (pendingEffectsStatus = 0, pendingFinishedWork = pendingEffectsRoot = null, releaseRootPooledCache(root2, root2.pendingLanes));
          var remainingLanes = root2.pendingLanes;
          0 === remainingLanes && (legacyErrorBoundariesThatAlreadyFailed = null);
          lanesToEventPriority(lanes);
          finishedWork = finishedWork.stateNode;
          if (injectedHook && "function" === typeof injectedHook.onCommitFiberRoot)
            try {
              injectedHook.onCommitFiberRoot(
                rendererID,
                finishedWork,
                void 0,
                128 === (finishedWork.current.flags & 128)
              );
            } catch (err) {
            }
          if (null !== recoverableErrors) {
            finishedWork = ReactSharedInternals.T;
            remainingLanes = ReactDOMSharedInternals.p;
            ReactDOMSharedInternals.p = 2;
            ReactSharedInternals.T = null;
            try {
              for (var onRecoverableError = root2.onRecoverableError, i = 0; i < recoverableErrors.length; i++) {
                var recoverableError = recoverableErrors[i];
                onRecoverableError(recoverableError.value, {
                  componentStack: recoverableError.stack
                });
              }
            } finally {
              ReactSharedInternals.T = finishedWork, ReactDOMSharedInternals.p = remainingLanes;
            }
          }
          0 !== (pendingEffectsLanes & 3) && flushPendingEffects();
          ensureRootIsScheduled(root2);
          remainingLanes = root2.pendingLanes;
          0 !== (lanes & 261930) && 0 !== (remainingLanes & 42) ? root2 === rootWithNestedUpdates ? nestedUpdateCount++ : (nestedUpdateCount = 0, rootWithNestedUpdates = root2) : nestedUpdateCount = 0;
          flushSyncWorkAcrossRoots_impl(0, false);
        }
      }
      function releaseRootPooledCache(root2, remainingLanes) {
        0 === (root2.pooledCacheLanes &= remainingLanes) && (remainingLanes = root2.pooledCache, null != remainingLanes && (root2.pooledCache = null, releaseCache(remainingLanes)));
      }
      function flushPendingEffects() {
        flushMutationEffects();
        flushLayoutEffects();
        flushSpawnedWork();
        return flushPassiveEffects();
      }
      function flushPassiveEffects() {
        if (5 !== pendingEffectsStatus) return false;
        var root2 = pendingEffectsRoot, remainingLanes = pendingEffectsRemainingLanes;
        pendingEffectsRemainingLanes = 0;
        var renderPriority = lanesToEventPriority(pendingEffectsLanes), prevTransition = ReactSharedInternals.T, previousPriority = ReactDOMSharedInternals.p;
        try {
          ReactDOMSharedInternals.p = 32 > renderPriority ? 32 : renderPriority;
          ReactSharedInternals.T = null;
          renderPriority = pendingPassiveTransitions;
          pendingPassiveTransitions = null;
          var root$jscomp$0 = pendingEffectsRoot, lanes = pendingEffectsLanes;
          pendingEffectsStatus = 0;
          pendingFinishedWork = pendingEffectsRoot = null;
          pendingEffectsLanes = 0;
          if (0 !== (executionContext & 6)) throw Error(formatProdErrorMessage(331));
          var prevExecutionContext = executionContext;
          executionContext |= 4;
          commitPassiveUnmountOnFiber(root$jscomp$0.current);
          commitPassiveMountOnFiber(
            root$jscomp$0,
            root$jscomp$0.current,
            lanes,
            renderPriority
          );
          executionContext = prevExecutionContext;
          flushSyncWorkAcrossRoots_impl(0, false);
          if (injectedHook && "function" === typeof injectedHook.onPostCommitFiberRoot)
            try {
              injectedHook.onPostCommitFiberRoot(rendererID, root$jscomp$0);
            } catch (err) {
            }
          return true;
        } finally {
          ReactDOMSharedInternals.p = previousPriority, ReactSharedInternals.T = prevTransition, releaseRootPooledCache(root2, remainingLanes);
        }
      }
      function captureCommitPhaseErrorOnRoot(rootFiber, sourceFiber, error) {
        sourceFiber = createCapturedValueAtFiber(error, sourceFiber);
        sourceFiber = createRootErrorUpdate(rootFiber.stateNode, sourceFiber, 2);
        rootFiber = enqueueUpdate(rootFiber, sourceFiber, 2);
        null !== rootFiber && (markRootUpdated$1(rootFiber, 2), ensureRootIsScheduled(rootFiber));
      }
      function captureCommitPhaseError(sourceFiber, nearestMountedAncestor, error) {
        if (3 === sourceFiber.tag)
          captureCommitPhaseErrorOnRoot(sourceFiber, sourceFiber, error);
        else
          for (; null !== nearestMountedAncestor; ) {
            if (3 === nearestMountedAncestor.tag) {
              captureCommitPhaseErrorOnRoot(
                nearestMountedAncestor,
                sourceFiber,
                error
              );
              break;
            } else if (1 === nearestMountedAncestor.tag) {
              var instance = nearestMountedAncestor.stateNode;
              if ("function" === typeof nearestMountedAncestor.type.getDerivedStateFromError || "function" === typeof instance.componentDidCatch && (null === legacyErrorBoundariesThatAlreadyFailed || !legacyErrorBoundariesThatAlreadyFailed.has(instance))) {
                sourceFiber = createCapturedValueAtFiber(error, sourceFiber);
                error = createClassErrorUpdate(2);
                instance = enqueueUpdate(nearestMountedAncestor, error, 2);
                null !== instance && (initializeClassErrorUpdate(
                  error,
                  instance,
                  nearestMountedAncestor,
                  sourceFiber
                ), markRootUpdated$1(instance, 2), ensureRootIsScheduled(instance));
                break;
              }
            }
            nearestMountedAncestor = nearestMountedAncestor.return;
          }
      }
      function attachPingListener(root2, wakeable, lanes) {
        var pingCache = root2.pingCache;
        if (null === pingCache) {
          pingCache = root2.pingCache = new PossiblyWeakMap();
          var threadIDs = /* @__PURE__ */ new Set();
          pingCache.set(wakeable, threadIDs);
        } else
          threadIDs = pingCache.get(wakeable), void 0 === threadIDs && (threadIDs = /* @__PURE__ */ new Set(), pingCache.set(wakeable, threadIDs));
        threadIDs.has(lanes) || (workInProgressRootDidAttachPingListener = true, threadIDs.add(lanes), root2 = pingSuspendedRoot.bind(null, root2, wakeable, lanes), wakeable.then(root2, root2));
      }
      function pingSuspendedRoot(root2, wakeable, pingedLanes) {
        var pingCache = root2.pingCache;
        null !== pingCache && pingCache.delete(wakeable);
        root2.pingedLanes |= root2.suspendedLanes & pingedLanes;
        root2.warmLanes &= ~pingedLanes;
        workInProgressRoot === root2 && (workInProgressRootRenderLanes & pingedLanes) === pingedLanes && (4 === workInProgressRootExitStatus || 3 === workInProgressRootExitStatus && (workInProgressRootRenderLanes & 62914560) === workInProgressRootRenderLanes && 300 > now() - globalMostRecentFallbackTime ? 0 === (executionContext & 2) && prepareFreshStack(root2, 0) : workInProgressRootPingedLanes |= pingedLanes, workInProgressSuspendedRetryLanes === workInProgressRootRenderLanes && (workInProgressSuspendedRetryLanes = 0));
        ensureRootIsScheduled(root2);
      }
      function retryTimedOutBoundary(boundaryFiber, retryLane) {
        0 === retryLane && (retryLane = claimNextRetryLane());
        boundaryFiber = enqueueConcurrentRenderForLane(boundaryFiber, retryLane);
        null !== boundaryFiber && (markRootUpdated$1(boundaryFiber, retryLane), ensureRootIsScheduled(boundaryFiber));
      }
      function retryDehydratedSuspenseBoundary(boundaryFiber) {
        var suspenseState = boundaryFiber.memoizedState, retryLane = 0;
        null !== suspenseState && (retryLane = suspenseState.retryLane);
        retryTimedOutBoundary(boundaryFiber, retryLane);
      }
      function resolveRetryWakeable(boundaryFiber, wakeable) {
        var retryLane = 0;
        switch (boundaryFiber.tag) {
          case 31:
          case 13:
            var retryCache = boundaryFiber.stateNode;
            var suspenseState = boundaryFiber.memoizedState;
            null !== suspenseState && (retryLane = suspenseState.retryLane);
            break;
          case 19:
            retryCache = boundaryFiber.stateNode;
            break;
          case 22:
            retryCache = boundaryFiber.stateNode._retryCache;
            break;
          default:
            throw Error(formatProdErrorMessage(314));
        }
        null !== retryCache && retryCache.delete(wakeable);
        retryTimedOutBoundary(boundaryFiber, retryLane);
      }
      function scheduleCallback$1(priorityLevel, callback) {
        return scheduleCallback$3(priorityLevel, callback);
      }
      var firstScheduledRoot = null;
      var lastScheduledRoot = null;
      var didScheduleMicrotask = false;
      var mightHavePendingSyncWork = false;
      var isFlushingWork = false;
      var currentEventTransitionLane = 0;
      function ensureRootIsScheduled(root2) {
        root2 !== lastScheduledRoot && null === root2.next && (null === lastScheduledRoot ? firstScheduledRoot = lastScheduledRoot = root2 : lastScheduledRoot = lastScheduledRoot.next = root2);
        mightHavePendingSyncWork = true;
        didScheduleMicrotask || (didScheduleMicrotask = true, scheduleImmediateRootScheduleTask());
      }
      function flushSyncWorkAcrossRoots_impl(syncTransitionLanes, onlyLegacy) {
        if (!isFlushingWork && mightHavePendingSyncWork) {
          isFlushingWork = true;
          do {
            var didPerformSomeWork = false;
            for (var root$170 = firstScheduledRoot; null !== root$170; ) {
              if (!onlyLegacy)
                if (0 !== syncTransitionLanes) {
                  var pendingLanes = root$170.pendingLanes;
                  if (0 === pendingLanes) var JSCompiler_inline_result = 0;
                  else {
                    var suspendedLanes = root$170.suspendedLanes, pingedLanes = root$170.pingedLanes;
                    JSCompiler_inline_result = (1 << 31 - clz32(42 | syncTransitionLanes) + 1) - 1;
                    JSCompiler_inline_result &= pendingLanes & ~(suspendedLanes & ~pingedLanes);
                    JSCompiler_inline_result = JSCompiler_inline_result & 201326741 ? JSCompiler_inline_result & 201326741 | 1 : JSCompiler_inline_result ? JSCompiler_inline_result | 2 : 0;
                  }
                  0 !== JSCompiler_inline_result && (didPerformSomeWork = true, performSyncWorkOnRoot(root$170, JSCompiler_inline_result));
                } else
                  JSCompiler_inline_result = workInProgressRootRenderLanes, JSCompiler_inline_result = getNextLanes(
                    root$170,
                    root$170 === workInProgressRoot ? JSCompiler_inline_result : 0,
                    null !== root$170.cancelPendingCommit || -1 !== root$170.timeoutHandle
                  ), 0 === (JSCompiler_inline_result & 3) || checkIfRootIsPrerendering(root$170, JSCompiler_inline_result) || (didPerformSomeWork = true, performSyncWorkOnRoot(root$170, JSCompiler_inline_result));
              root$170 = root$170.next;
            }
          } while (didPerformSomeWork);
          isFlushingWork = false;
        }
      }
      function processRootScheduleInImmediateTask() {
        processRootScheduleInMicrotask();
      }
      function processRootScheduleInMicrotask() {
        mightHavePendingSyncWork = didScheduleMicrotask = false;
        var syncTransitionLanes = 0;
        0 !== currentEventTransitionLane && shouldAttemptEagerTransition() && (syncTransitionLanes = currentEventTransitionLane);
        for (var currentTime = now(), prev = null, root2 = firstScheduledRoot; null !== root2; ) {
          var next = root2.next, nextLanes = scheduleTaskForRootDuringMicrotask(root2, currentTime);
          if (0 === nextLanes)
            root2.next = null, null === prev ? firstScheduledRoot = next : prev.next = next, null === next && (lastScheduledRoot = prev);
          else if (prev = root2, 0 !== syncTransitionLanes || 0 !== (nextLanes & 3))
            mightHavePendingSyncWork = true;
          root2 = next;
        }
        0 !== pendingEffectsStatus && 5 !== pendingEffectsStatus || flushSyncWorkAcrossRoots_impl(syncTransitionLanes, false);
        0 !== currentEventTransitionLane && (currentEventTransitionLane = 0);
      }
      function scheduleTaskForRootDuringMicrotask(root2, currentTime) {
        for (var suspendedLanes = root2.suspendedLanes, pingedLanes = root2.pingedLanes, expirationTimes = root2.expirationTimes, lanes = root2.pendingLanes & -62914561; 0 < lanes; ) {
          var index$5 = 31 - clz32(lanes), lane = 1 << index$5, expirationTime = expirationTimes[index$5];
          if (-1 === expirationTime) {
            if (0 === (lane & suspendedLanes) || 0 !== (lane & pingedLanes))
              expirationTimes[index$5] = computeExpirationTime(lane, currentTime);
          } else expirationTime <= currentTime && (root2.expiredLanes |= lane);
          lanes &= ~lane;
        }
        currentTime = workInProgressRoot;
        suspendedLanes = workInProgressRootRenderLanes;
        suspendedLanes = getNextLanes(
          root2,
          root2 === currentTime ? suspendedLanes : 0,
          null !== root2.cancelPendingCommit || -1 !== root2.timeoutHandle
        );
        pingedLanes = root2.callbackNode;
        if (0 === suspendedLanes || root2 === currentTime && (2 === workInProgressSuspendedReason || 9 === workInProgressSuspendedReason) || null !== root2.cancelPendingCommit)
          return null !== pingedLanes && null !== pingedLanes && cancelCallback$1(pingedLanes), root2.callbackNode = null, root2.callbackPriority = 0;
        if (0 === (suspendedLanes & 3) || checkIfRootIsPrerendering(root2, suspendedLanes)) {
          currentTime = suspendedLanes & -suspendedLanes;
          if (currentTime === root2.callbackPriority) return currentTime;
          null !== pingedLanes && cancelCallback$1(pingedLanes);
          switch (lanesToEventPriority(suspendedLanes)) {
            case 2:
            case 8:
              suspendedLanes = UserBlockingPriority;
              break;
            case 32:
              suspendedLanes = NormalPriority$1;
              break;
            case 268435456:
              suspendedLanes = IdlePriority;
              break;
            default:
              suspendedLanes = NormalPriority$1;
          }
          pingedLanes = performWorkOnRootViaSchedulerTask.bind(null, root2);
          suspendedLanes = scheduleCallback$3(suspendedLanes, pingedLanes);
          root2.callbackPriority = currentTime;
          root2.callbackNode = suspendedLanes;
          return currentTime;
        }
        null !== pingedLanes && null !== pingedLanes && cancelCallback$1(pingedLanes);
        root2.callbackPriority = 2;
        root2.callbackNode = null;
        return 2;
      }
      function performWorkOnRootViaSchedulerTask(root2, didTimeout) {
        if (0 !== pendingEffectsStatus && 5 !== pendingEffectsStatus)
          return root2.callbackNode = null, root2.callbackPriority = 0, null;
        var originalCallbackNode = root2.callbackNode;
        if (flushPendingEffects() && root2.callbackNode !== originalCallbackNode)
          return null;
        var workInProgressRootRenderLanes$jscomp$0 = workInProgressRootRenderLanes;
        workInProgressRootRenderLanes$jscomp$0 = getNextLanes(
          root2,
          root2 === workInProgressRoot ? workInProgressRootRenderLanes$jscomp$0 : 0,
          null !== root2.cancelPendingCommit || -1 !== root2.timeoutHandle
        );
        if (0 === workInProgressRootRenderLanes$jscomp$0) return null;
        performWorkOnRoot(root2, workInProgressRootRenderLanes$jscomp$0, didTimeout);
        scheduleTaskForRootDuringMicrotask(root2, now());
        return null != root2.callbackNode && root2.callbackNode === originalCallbackNode ? performWorkOnRootViaSchedulerTask.bind(null, root2) : null;
      }
      function performSyncWorkOnRoot(root2, lanes) {
        if (flushPendingEffects()) return null;
        performWorkOnRoot(root2, lanes, true);
      }
      function scheduleImmediateRootScheduleTask() {
        scheduleMicrotask(function() {
          0 !== (executionContext & 6) ? scheduleCallback$3(
            ImmediatePriority,
            processRootScheduleInImmediateTask
          ) : processRootScheduleInMicrotask();
        });
      }
      function requestTransitionLane() {
        if (0 === currentEventTransitionLane) {
          var actionScopeLane = currentEntangledLane;
          0 === actionScopeLane && (actionScopeLane = nextTransitionUpdateLane, nextTransitionUpdateLane <<= 1, 0 === (nextTransitionUpdateLane & 261888) && (nextTransitionUpdateLane = 256));
          currentEventTransitionLane = actionScopeLane;
        }
        return currentEventTransitionLane;
      }
      function coerceFormActionProp(actionProp) {
        return null == actionProp || "symbol" === typeof actionProp || "boolean" === typeof actionProp ? null : "function" === typeof actionProp ? actionProp : sanitizeURL("" + actionProp);
      }
      function createFormDataWithSubmitter(form, submitter) {
        var temp = submitter.ownerDocument.createElement("input");
        temp.name = submitter.name;
        temp.value = submitter.value;
        form.id && temp.setAttribute("form", form.id);
        submitter.parentNode.insertBefore(temp, submitter);
        form = new FormData(form);
        temp.parentNode.removeChild(temp);
        return form;
      }
      function extractEvents$1(dispatchQueue, domEventName, maybeTargetInst, nativeEvent, nativeEventTarget) {
        if ("submit" === domEventName && maybeTargetInst && maybeTargetInst.stateNode === nativeEventTarget) {
          var action = coerceFormActionProp(
            (nativeEventTarget[internalPropsKey] || null).action
          ), submitter = nativeEvent.submitter;
          submitter && (domEventName = (domEventName = submitter[internalPropsKey] || null) ? coerceFormActionProp(domEventName.formAction) : submitter.getAttribute("formAction"), null !== domEventName && (action = domEventName, submitter = null));
          var event = new SyntheticEvent(
            "action",
            "action",
            null,
            nativeEvent,
            nativeEventTarget
          );
          dispatchQueue.push({
            event,
            listeners: [
              {
                instance: null,
                listener: function() {
                  if (nativeEvent.defaultPrevented) {
                    if (0 !== currentEventTransitionLane) {
                      var formData = submitter ? createFormDataWithSubmitter(nativeEventTarget, submitter) : new FormData(nativeEventTarget);
                      startHostTransition(
                        maybeTargetInst,
                        {
                          pending: true,
                          data: formData,
                          method: nativeEventTarget.method,
                          action
                        },
                        null,
                        formData
                      );
                    }
                  } else
                    "function" === typeof action && (event.preventDefault(), formData = submitter ? createFormDataWithSubmitter(nativeEventTarget, submitter) : new FormData(nativeEventTarget), startHostTransition(
                      maybeTargetInst,
                      {
                        pending: true,
                        data: formData,
                        method: nativeEventTarget.method,
                        action
                      },
                      action,
                      formData
                    ));
                },
                currentTarget: nativeEventTarget
              }
            ]
          });
        }
      }
      for (i$jscomp$inline_1577 = 0; i$jscomp$inline_1577 < simpleEventPluginEvents.length; i$jscomp$inline_1577++) {
        eventName$jscomp$inline_1578 = simpleEventPluginEvents[i$jscomp$inline_1577], domEventName$jscomp$inline_1579 = eventName$jscomp$inline_1578.toLowerCase(), capitalizedEvent$jscomp$inline_1580 = eventName$jscomp$inline_1578[0].toUpperCase() + eventName$jscomp$inline_1578.slice(1);
        registerSimpleEvent(
          domEventName$jscomp$inline_1579,
          "on" + capitalizedEvent$jscomp$inline_1580
        );
      }
      var eventName$jscomp$inline_1578;
      var domEventName$jscomp$inline_1579;
      var capitalizedEvent$jscomp$inline_1580;
      var i$jscomp$inline_1577;
      registerSimpleEvent(ANIMATION_END, "onAnimationEnd");
      registerSimpleEvent(ANIMATION_ITERATION, "onAnimationIteration");
      registerSimpleEvent(ANIMATION_START, "onAnimationStart");
      registerSimpleEvent("dblclick", "onDoubleClick");
      registerSimpleEvent("focusin", "onFocus");
      registerSimpleEvent("focusout", "onBlur");
      registerSimpleEvent(TRANSITION_RUN, "onTransitionRun");
      registerSimpleEvent(TRANSITION_START, "onTransitionStart");
      registerSimpleEvent(TRANSITION_CANCEL, "onTransitionCancel");
      registerSimpleEvent(TRANSITION_END, "onTransitionEnd");
      registerDirectEvent("onMouseEnter", ["mouseout", "mouseover"]);
      registerDirectEvent("onMouseLeave", ["mouseout", "mouseover"]);
      registerDirectEvent("onPointerEnter", ["pointerout", "pointerover"]);
      registerDirectEvent("onPointerLeave", ["pointerout", "pointerover"]);
      registerTwoPhaseEvent(
        "onChange",
        "change click focusin focusout input keydown keyup selectionchange".split(" ")
      );
      registerTwoPhaseEvent(
        "onSelect",
        "focusout contextmenu dragend focusin keydown keyup mousedown mouseup selectionchange".split(
          " "
        )
      );
      registerTwoPhaseEvent("onBeforeInput", [
        "compositionend",
        "keypress",
        "textInput",
        "paste"
      ]);
      registerTwoPhaseEvent(
        "onCompositionEnd",
        "compositionend focusout keydown keypress keyup mousedown".split(" ")
      );
      registerTwoPhaseEvent(
        "onCompositionStart",
        "compositionstart focusout keydown keypress keyup mousedown".split(" ")
      );
      registerTwoPhaseEvent(
        "onCompositionUpdate",
        "compositionupdate focusout keydown keypress keyup mousedown".split(" ")
      );
      var mediaEventTypes = "abort canplay canplaythrough durationchange emptied encrypted ended error loadeddata loadedmetadata loadstart pause play playing progress ratechange resize seeked seeking stalled suspend timeupdate volumechange waiting".split(
        " "
      );
      var nonDelegatedEvents = new Set(
        "beforetoggle cancel close invalid load scroll scrollend toggle".split(" ").concat(mediaEventTypes)
      );
      function processDispatchQueue(dispatchQueue, eventSystemFlags) {
        eventSystemFlags = 0 !== (eventSystemFlags & 4);
        for (var i = 0; i < dispatchQueue.length; i++) {
          var _dispatchQueue$i = dispatchQueue[i], event = _dispatchQueue$i.event;
          _dispatchQueue$i = _dispatchQueue$i.listeners;
          a: {
            var previousInstance = void 0;
            if (eventSystemFlags)
              for (var i$jscomp$0 = _dispatchQueue$i.length - 1; 0 <= i$jscomp$0; i$jscomp$0--) {
                var _dispatchListeners$i = _dispatchQueue$i[i$jscomp$0], instance = _dispatchListeners$i.instance, currentTarget = _dispatchListeners$i.currentTarget;
                _dispatchListeners$i = _dispatchListeners$i.listener;
                if (instance !== previousInstance && event.isPropagationStopped())
                  break a;
                previousInstance = _dispatchListeners$i;
                event.currentTarget = currentTarget;
                try {
                  previousInstance(event);
                } catch (error) {
                  reportGlobalError(error);
                }
                event.currentTarget = null;
                previousInstance = instance;
              }
            else
              for (i$jscomp$0 = 0; i$jscomp$0 < _dispatchQueue$i.length; i$jscomp$0++) {
                _dispatchListeners$i = _dispatchQueue$i[i$jscomp$0];
                instance = _dispatchListeners$i.instance;
                currentTarget = _dispatchListeners$i.currentTarget;
                _dispatchListeners$i = _dispatchListeners$i.listener;
                if (instance !== previousInstance && event.isPropagationStopped())
                  break a;
                previousInstance = _dispatchListeners$i;
                event.currentTarget = currentTarget;
                try {
                  previousInstance(event);
                } catch (error) {
                  reportGlobalError(error);
                }
                event.currentTarget = null;
                previousInstance = instance;
              }
          }
        }
      }
      function listenToNonDelegatedEvent(domEventName, targetElement) {
        var JSCompiler_inline_result = targetElement[internalEventHandlersKey];
        void 0 === JSCompiler_inline_result && (JSCompiler_inline_result = targetElement[internalEventHandlersKey] = /* @__PURE__ */ new Set());
        var listenerSetKey = domEventName + "__bubble";
        JSCompiler_inline_result.has(listenerSetKey) || (addTrappedEventListener(targetElement, domEventName, 2, false), JSCompiler_inline_result.add(listenerSetKey));
      }
      function listenToNativeEvent(domEventName, isCapturePhaseListener, target) {
        var eventSystemFlags = 0;
        isCapturePhaseListener && (eventSystemFlags |= 4);
        addTrappedEventListener(
          target,
          domEventName,
          eventSystemFlags,
          isCapturePhaseListener
        );
      }
      var listeningMarker = "_reactListening" + Math.random().toString(36).slice(2);
      function listenToAllSupportedEvents(rootContainerElement) {
        if (!rootContainerElement[listeningMarker]) {
          rootContainerElement[listeningMarker] = true;
          allNativeEvents.forEach(function(domEventName) {
            "selectionchange" !== domEventName && (nonDelegatedEvents.has(domEventName) || listenToNativeEvent(domEventName, false, rootContainerElement), listenToNativeEvent(domEventName, true, rootContainerElement));
          });
          var ownerDocument = 9 === rootContainerElement.nodeType ? rootContainerElement : rootContainerElement.ownerDocument;
          null === ownerDocument || ownerDocument[listeningMarker] || (ownerDocument[listeningMarker] = true, listenToNativeEvent("selectionchange", false, ownerDocument));
        }
      }
      function addTrappedEventListener(targetContainer, domEventName, eventSystemFlags, isCapturePhaseListener) {
        switch (getEventPriority(domEventName)) {
          case 2:
            var listenerWrapper = dispatchDiscreteEvent;
            break;
          case 8:
            listenerWrapper = dispatchContinuousEvent;
            break;
          default:
            listenerWrapper = dispatchEvent;
        }
        eventSystemFlags = listenerWrapper.bind(
          null,
          domEventName,
          eventSystemFlags,
          targetContainer
        );
        listenerWrapper = void 0;
        !passiveBrowserEventsSupported || "touchstart" !== domEventName && "touchmove" !== domEventName && "wheel" !== domEventName || (listenerWrapper = true);
        isCapturePhaseListener ? void 0 !== listenerWrapper ? targetContainer.addEventListener(domEventName, eventSystemFlags, {
          capture: true,
          passive: listenerWrapper
        }) : targetContainer.addEventListener(domEventName, eventSystemFlags, true) : void 0 !== listenerWrapper ? targetContainer.addEventListener(domEventName, eventSystemFlags, {
          passive: listenerWrapper
        }) : targetContainer.addEventListener(domEventName, eventSystemFlags, false);
      }
      function dispatchEventForPluginEventSystem(domEventName, eventSystemFlags, nativeEvent, targetInst$jscomp$0, targetContainer) {
        var ancestorInst = targetInst$jscomp$0;
        if (0 === (eventSystemFlags & 1) && 0 === (eventSystemFlags & 2) && null !== targetInst$jscomp$0)
          a: for (; ; ) {
            if (null === targetInst$jscomp$0) return;
            var nodeTag = targetInst$jscomp$0.tag;
            if (3 === nodeTag || 4 === nodeTag) {
              var container = targetInst$jscomp$0.stateNode.containerInfo;
              if (container === targetContainer) break;
              if (4 === nodeTag)
                for (nodeTag = targetInst$jscomp$0.return; null !== nodeTag; ) {
                  var grandTag = nodeTag.tag;
                  if ((3 === grandTag || 4 === grandTag) && nodeTag.stateNode.containerInfo === targetContainer)
                    return;
                  nodeTag = nodeTag.return;
                }
              for (; null !== container; ) {
                nodeTag = getClosestInstanceFromNode(container);
                if (null === nodeTag) return;
                grandTag = nodeTag.tag;
                if (5 === grandTag || 6 === grandTag || 26 === grandTag || 27 === grandTag) {
                  targetInst$jscomp$0 = ancestorInst = nodeTag;
                  continue a;
                }
                container = container.parentNode;
              }
            }
            targetInst$jscomp$0 = targetInst$jscomp$0.return;
          }
        batchedUpdates$1(function() {
          var targetInst = ancestorInst, nativeEventTarget = getEventTarget(nativeEvent), dispatchQueue = [];
          a: {
            var reactName = topLevelEventsToReactNames.get(domEventName);
            if (void 0 !== reactName) {
              var SyntheticEventCtor = SyntheticEvent, reactEventType = domEventName;
              switch (domEventName) {
                case "keypress":
                  if (0 === getEventCharCode(nativeEvent)) break a;
                case "keydown":
                case "keyup":
                  SyntheticEventCtor = SyntheticKeyboardEvent;
                  break;
                case "focusin":
                  reactEventType = "focus";
                  SyntheticEventCtor = SyntheticFocusEvent;
                  break;
                case "focusout":
                  reactEventType = "blur";
                  SyntheticEventCtor = SyntheticFocusEvent;
                  break;
                case "beforeblur":
                case "afterblur":
                  SyntheticEventCtor = SyntheticFocusEvent;
                  break;
                case "click":
                  if (2 === nativeEvent.button) break a;
                case "auxclick":
                case "dblclick":
                case "mousedown":
                case "mousemove":
                case "mouseup":
                case "mouseout":
                case "mouseover":
                case "contextmenu":
                  SyntheticEventCtor = SyntheticMouseEvent;
                  break;
                case "drag":
                case "dragend":
                case "dragenter":
                case "dragexit":
                case "dragleave":
                case "dragover":
                case "dragstart":
                case "drop":
                  SyntheticEventCtor = SyntheticDragEvent;
                  break;
                case "touchcancel":
                case "touchend":
                case "touchmove":
                case "touchstart":
                  SyntheticEventCtor = SyntheticTouchEvent;
                  break;
                case ANIMATION_END:
                case ANIMATION_ITERATION:
                case ANIMATION_START:
                  SyntheticEventCtor = SyntheticAnimationEvent;
                  break;
                case TRANSITION_END:
                  SyntheticEventCtor = SyntheticTransitionEvent;
                  break;
                case "scroll":
                case "scrollend":
                  SyntheticEventCtor = SyntheticUIEvent;
                  break;
                case "wheel":
                  SyntheticEventCtor = SyntheticWheelEvent;
                  break;
                case "copy":
                case "cut":
                case "paste":
                  SyntheticEventCtor = SyntheticClipboardEvent;
                  break;
                case "gotpointercapture":
                case "lostpointercapture":
                case "pointercancel":
                case "pointerdown":
                case "pointermove":
                case "pointerout":
                case "pointerover":
                case "pointerup":
                  SyntheticEventCtor = SyntheticPointerEvent;
                  break;
                case "toggle":
                case "beforetoggle":
                  SyntheticEventCtor = SyntheticToggleEvent;
              }
              var inCapturePhase = 0 !== (eventSystemFlags & 4), accumulateTargetOnly = !inCapturePhase && ("scroll" === domEventName || "scrollend" === domEventName), reactEventName = inCapturePhase ? null !== reactName ? reactName + "Capture" : null : reactName;
              inCapturePhase = [];
              for (var instance = targetInst, lastHostComponent; null !== instance; ) {
                var _instance = instance;
                lastHostComponent = _instance.stateNode;
                _instance = _instance.tag;
                5 !== _instance && 26 !== _instance && 27 !== _instance || null === lastHostComponent || null === reactEventName || (_instance = getListener(instance, reactEventName), null != _instance && inCapturePhase.push(
                  createDispatchListener(instance, _instance, lastHostComponent)
                ));
                if (accumulateTargetOnly) break;
                instance = instance.return;
              }
              0 < inCapturePhase.length && (reactName = new SyntheticEventCtor(
                reactName,
                reactEventType,
                null,
                nativeEvent,
                nativeEventTarget
              ), dispatchQueue.push({ event: reactName, listeners: inCapturePhase }));
            }
          }
          if (0 === (eventSystemFlags & 7)) {
            a: {
              reactName = "mouseover" === domEventName || "pointerover" === domEventName;
              SyntheticEventCtor = "mouseout" === domEventName || "pointerout" === domEventName;
              if (reactName && nativeEvent !== currentReplayingEvent && (reactEventType = nativeEvent.relatedTarget || nativeEvent.fromElement) && (getClosestInstanceFromNode(reactEventType) || reactEventType[internalContainerInstanceKey]))
                break a;
              if (SyntheticEventCtor || reactName) {
                reactName = nativeEventTarget.window === nativeEventTarget ? nativeEventTarget : (reactName = nativeEventTarget.ownerDocument) ? reactName.defaultView || reactName.parentWindow : window;
                if (SyntheticEventCtor) {
                  if (reactEventType = nativeEvent.relatedTarget || nativeEvent.toElement, SyntheticEventCtor = targetInst, reactEventType = reactEventType ? getClosestInstanceFromNode(reactEventType) : null, null !== reactEventType && (accumulateTargetOnly = getNearestMountedFiber(reactEventType), inCapturePhase = reactEventType.tag, reactEventType !== accumulateTargetOnly || 5 !== inCapturePhase && 27 !== inCapturePhase && 6 !== inCapturePhase))
                    reactEventType = null;
                } else SyntheticEventCtor = null, reactEventType = targetInst;
                if (SyntheticEventCtor !== reactEventType) {
                  inCapturePhase = SyntheticMouseEvent;
                  _instance = "onMouseLeave";
                  reactEventName = "onMouseEnter";
                  instance = "mouse";
                  if ("pointerout" === domEventName || "pointerover" === domEventName)
                    inCapturePhase = SyntheticPointerEvent, _instance = "onPointerLeave", reactEventName = "onPointerEnter", instance = "pointer";
                  accumulateTargetOnly = null == SyntheticEventCtor ? reactName : getNodeFromInstance(SyntheticEventCtor);
                  lastHostComponent = null == reactEventType ? reactName : getNodeFromInstance(reactEventType);
                  reactName = new inCapturePhase(
                    _instance,
                    instance + "leave",
                    SyntheticEventCtor,
                    nativeEvent,
                    nativeEventTarget
                  );
                  reactName.target = accumulateTargetOnly;
                  reactName.relatedTarget = lastHostComponent;
                  _instance = null;
                  getClosestInstanceFromNode(nativeEventTarget) === targetInst && (inCapturePhase = new inCapturePhase(
                    reactEventName,
                    instance + "enter",
                    reactEventType,
                    nativeEvent,
                    nativeEventTarget
                  ), inCapturePhase.target = lastHostComponent, inCapturePhase.relatedTarget = accumulateTargetOnly, _instance = inCapturePhase);
                  accumulateTargetOnly = _instance;
                  if (SyntheticEventCtor && reactEventType)
                    b: {
                      inCapturePhase = getParent;
                      reactEventName = SyntheticEventCtor;
                      instance = reactEventType;
                      lastHostComponent = 0;
                      for (_instance = reactEventName; _instance; _instance = inCapturePhase(_instance))
                        lastHostComponent++;
                      _instance = 0;
                      for (var tempB = instance; tempB; tempB = inCapturePhase(tempB))
                        _instance++;
                      for (; 0 < lastHostComponent - _instance; )
                        reactEventName = inCapturePhase(reactEventName), lastHostComponent--;
                      for (; 0 < _instance - lastHostComponent; )
                        instance = inCapturePhase(instance), _instance--;
                      for (; lastHostComponent--; ) {
                        if (reactEventName === instance || null !== instance && reactEventName === instance.alternate) {
                          inCapturePhase = reactEventName;
                          break b;
                        }
                        reactEventName = inCapturePhase(reactEventName);
                        instance = inCapturePhase(instance);
                      }
                      inCapturePhase = null;
                    }
                  else inCapturePhase = null;
                  null !== SyntheticEventCtor && accumulateEnterLeaveListenersForEvent(
                    dispatchQueue,
                    reactName,
                    SyntheticEventCtor,
                    inCapturePhase,
                    false
                  );
                  null !== reactEventType && null !== accumulateTargetOnly && accumulateEnterLeaveListenersForEvent(
                    dispatchQueue,
                    accumulateTargetOnly,
                    reactEventType,
                    inCapturePhase,
                    true
                  );
                }
              }
            }
            a: {
              reactName = targetInst ? getNodeFromInstance(targetInst) : window;
              SyntheticEventCtor = reactName.nodeName && reactName.nodeName.toLowerCase();
              if ("select" === SyntheticEventCtor || "input" === SyntheticEventCtor && "file" === reactName.type)
                var getTargetInstFunc = getTargetInstForChangeEvent;
              else if (isTextInputElement(reactName))
                if (isInputEventSupported)
                  getTargetInstFunc = getTargetInstForInputOrChangeEvent;
                else {
                  getTargetInstFunc = getTargetInstForInputEventPolyfill;
                  var handleEventFunc = handleEventsForInputEventPolyfill;
                }
              else
                SyntheticEventCtor = reactName.nodeName, !SyntheticEventCtor || "input" !== SyntheticEventCtor.toLowerCase() || "checkbox" !== reactName.type && "radio" !== reactName.type ? targetInst && isCustomElement(targetInst.elementType) && (getTargetInstFunc = getTargetInstForChangeEvent) : getTargetInstFunc = getTargetInstForClickEvent;
              if (getTargetInstFunc && (getTargetInstFunc = getTargetInstFunc(domEventName, targetInst))) {
                createAndAccumulateChangeEvent(
                  dispatchQueue,
                  getTargetInstFunc,
                  nativeEvent,
                  nativeEventTarget
                );
                break a;
              }
              handleEventFunc && handleEventFunc(domEventName, reactName, targetInst);
              "focusout" === domEventName && targetInst && "number" === reactName.type && null != targetInst.memoizedProps.value && setDefaultValue(reactName, "number", reactName.value);
            }
            handleEventFunc = targetInst ? getNodeFromInstance(targetInst) : window;
            switch (domEventName) {
              case "focusin":
                if (isTextInputElement(handleEventFunc) || "true" === handleEventFunc.contentEditable)
                  activeElement = handleEventFunc, activeElementInst = targetInst, lastSelection = null;
                break;
              case "focusout":
                lastSelection = activeElementInst = activeElement = null;
                break;
              case "mousedown":
                mouseDown = true;
                break;
              case "contextmenu":
              case "mouseup":
              case "dragend":
                mouseDown = false;
                constructSelectEvent(dispatchQueue, nativeEvent, nativeEventTarget);
                break;
              case "selectionchange":
                if (skipSelectionChangeEvent) break;
              case "keydown":
              case "keyup":
                constructSelectEvent(dispatchQueue, nativeEvent, nativeEventTarget);
            }
            var fallbackData;
            if (canUseCompositionEvent)
              b: {
                switch (domEventName) {
                  case "compositionstart":
                    var eventType = "onCompositionStart";
                    break b;
                  case "compositionend":
                    eventType = "onCompositionEnd";
                    break b;
                  case "compositionupdate":
                    eventType = "onCompositionUpdate";
                    break b;
                }
                eventType = void 0;
              }
            else
              isComposing ? isFallbackCompositionEnd(domEventName, nativeEvent) && (eventType = "onCompositionEnd") : "keydown" === domEventName && 229 === nativeEvent.keyCode && (eventType = "onCompositionStart");
            eventType && (useFallbackCompositionData && "ko" !== nativeEvent.locale && (isComposing || "onCompositionStart" !== eventType ? "onCompositionEnd" === eventType && isComposing && (fallbackData = getData()) : (root = nativeEventTarget, startText = "value" in root ? root.value : root.textContent, isComposing = true)), handleEventFunc = accumulateTwoPhaseListeners(targetInst, eventType), 0 < handleEventFunc.length && (eventType = new SyntheticCompositionEvent(
              eventType,
              domEventName,
              null,
              nativeEvent,
              nativeEventTarget
            ), dispatchQueue.push({ event: eventType, listeners: handleEventFunc }), fallbackData ? eventType.data = fallbackData : (fallbackData = getDataFromCustomEvent(nativeEvent), null !== fallbackData && (eventType.data = fallbackData))));
            if (fallbackData = canUseTextInputEvent ? getNativeBeforeInputChars(domEventName, nativeEvent) : getFallbackBeforeInputChars(domEventName, nativeEvent))
              eventType = accumulateTwoPhaseListeners(targetInst, "onBeforeInput"), 0 < eventType.length && (handleEventFunc = new SyntheticCompositionEvent(
                "onBeforeInput",
                "beforeinput",
                null,
                nativeEvent,
                nativeEventTarget
              ), dispatchQueue.push({
                event: handleEventFunc,
                listeners: eventType
              }), handleEventFunc.data = fallbackData);
            extractEvents$1(
              dispatchQueue,
              domEventName,
              targetInst,
              nativeEvent,
              nativeEventTarget
            );
          }
          processDispatchQueue(dispatchQueue, eventSystemFlags);
        });
      }
      function createDispatchListener(instance, listener, currentTarget) {
        return {
          instance,
          listener,
          currentTarget
        };
      }
      function accumulateTwoPhaseListeners(targetFiber, reactName) {
        for (var captureName = reactName + "Capture", listeners7 = []; null !== targetFiber; ) {
          var _instance2 = targetFiber, stateNode = _instance2.stateNode;
          _instance2 = _instance2.tag;
          5 !== _instance2 && 26 !== _instance2 && 27 !== _instance2 || null === stateNode || (_instance2 = getListener(targetFiber, captureName), null != _instance2 && listeners7.unshift(
            createDispatchListener(targetFiber, _instance2, stateNode)
          ), _instance2 = getListener(targetFiber, reactName), null != _instance2 && listeners7.push(
            createDispatchListener(targetFiber, _instance2, stateNode)
          ));
          if (3 === targetFiber.tag) return listeners7;
          targetFiber = targetFiber.return;
        }
        return [];
      }
      function getParent(inst) {
        if (null === inst) return null;
        do
          inst = inst.return;
        while (inst && 5 !== inst.tag && 27 !== inst.tag);
        return inst ? inst : null;
      }
      function accumulateEnterLeaveListenersForEvent(dispatchQueue, event, target, common, inCapturePhase) {
        for (var registrationName = event._reactName, listeners7 = []; null !== target && target !== common; ) {
          var _instance3 = target, alternate = _instance3.alternate, stateNode = _instance3.stateNode;
          _instance3 = _instance3.tag;
          if (null !== alternate && alternate === common) break;
          5 !== _instance3 && 26 !== _instance3 && 27 !== _instance3 || null === stateNode || (alternate = stateNode, inCapturePhase ? (stateNode = getListener(target, registrationName), null != stateNode && listeners7.unshift(
            createDispatchListener(target, stateNode, alternate)
          )) : inCapturePhase || (stateNode = getListener(target, registrationName), null != stateNode && listeners7.push(
            createDispatchListener(target, stateNode, alternate)
          )));
          target = target.return;
        }
        0 !== listeners7.length && dispatchQueue.push({ event, listeners: listeners7 });
      }
      var NORMALIZE_NEWLINES_REGEX = /\r\n?/g;
      var NORMALIZE_NULL_AND_REPLACEMENT_REGEX = /\u0000|\uFFFD/g;
      function normalizeMarkupForTextOrAttribute(markup) {
        return ("string" === typeof markup ? markup : "" + markup).replace(NORMALIZE_NEWLINES_REGEX, "\n").replace(NORMALIZE_NULL_AND_REPLACEMENT_REGEX, "");
      }
      function checkForUnmatchedText(serverText, clientText) {
        clientText = normalizeMarkupForTextOrAttribute(clientText);
        return normalizeMarkupForTextOrAttribute(serverText) === clientText ? true : false;
      }
      function setProp(domElement, tag, key, value, props, prevValue) {
        switch (key) {
          case "children":
            "string" === typeof value ? "body" === tag || "textarea" === tag && "" === value || setTextContent(domElement, value) : ("number" === typeof value || "bigint" === typeof value) && "body" !== tag && setTextContent(domElement, "" + value);
            break;
          case "className":
            setValueForKnownAttribute(domElement, "class", value);
            break;
          case "tabIndex":
            setValueForKnownAttribute(domElement, "tabindex", value);
            break;
          case "dir":
          case "role":
          case "viewBox":
          case "width":
          case "height":
            setValueForKnownAttribute(domElement, key, value);
            break;
          case "style":
            setValueForStyles(domElement, value, prevValue);
            break;
          case "data":
            if ("object" !== tag) {
              setValueForKnownAttribute(domElement, "data", value);
              break;
            }
          case "src":
          case "href":
            if ("" === value && ("a" !== tag || "href" !== key)) {
              domElement.removeAttribute(key);
              break;
            }
            if (null == value || "function" === typeof value || "symbol" === typeof value || "boolean" === typeof value) {
              domElement.removeAttribute(key);
              break;
            }
            value = sanitizeURL("" + value);
            domElement.setAttribute(key, value);
            break;
          case "action":
          case "formAction":
            if ("function" === typeof value) {
              domElement.setAttribute(
                key,
                "javascript:throw new Error('A React form was unexpectedly submitted. If you called form.submit() manually, consider using form.requestSubmit() instead. If you\\'re trying to use event.stopPropagation() in a submit event handler, consider also calling event.preventDefault().')"
              );
              break;
            } else
              "function" === typeof prevValue && ("formAction" === key ? ("input" !== tag && setProp(domElement, tag, "name", props.name, props, null), setProp(
                domElement,
                tag,
                "formEncType",
                props.formEncType,
                props,
                null
              ), setProp(
                domElement,
                tag,
                "formMethod",
                props.formMethod,
                props,
                null
              ), setProp(
                domElement,
                tag,
                "formTarget",
                props.formTarget,
                props,
                null
              )) : (setProp(domElement, tag, "encType", props.encType, props, null), setProp(domElement, tag, "method", props.method, props, null), setProp(domElement, tag, "target", props.target, props, null)));
            if (null == value || "symbol" === typeof value || "boolean" === typeof value) {
              domElement.removeAttribute(key);
              break;
            }
            value = sanitizeURL("" + value);
            domElement.setAttribute(key, value);
            break;
          case "onClick":
            null != value && (domElement.onclick = noop$1);
            break;
          case "onScroll":
            null != value && listenToNonDelegatedEvent("scroll", domElement);
            break;
          case "onScrollEnd":
            null != value && listenToNonDelegatedEvent("scrollend", domElement);
            break;
          case "dangerouslySetInnerHTML":
            if (null != value) {
              if ("object" !== typeof value || !("__html" in value))
                throw Error(formatProdErrorMessage(61));
              key = value.__html;
              if (null != key) {
                if (null != props.children) throw Error(formatProdErrorMessage(60));
                domElement.innerHTML = key;
              }
            }
            break;
          case "multiple":
            domElement.multiple = value && "function" !== typeof value && "symbol" !== typeof value;
            break;
          case "muted":
            domElement.muted = value && "function" !== typeof value && "symbol" !== typeof value;
            break;
          case "suppressContentEditableWarning":
          case "suppressHydrationWarning":
          case "defaultValue":
          case "defaultChecked":
          case "innerHTML":
          case "ref":
            break;
          case "autoFocus":
            break;
          case "xlinkHref":
            if (null == value || "function" === typeof value || "boolean" === typeof value || "symbol" === typeof value) {
              domElement.removeAttribute("xlink:href");
              break;
            }
            key = sanitizeURL("" + value);
            domElement.setAttributeNS(
              "http://www.w3.org/1999/xlink",
              "xlink:href",
              key
            );
            break;
          case "contentEditable":
          case "spellCheck":
          case "draggable":
          case "value":
          case "autoReverse":
          case "externalResourcesRequired":
          case "focusable":
          case "preserveAlpha":
            null != value && "function" !== typeof value && "symbol" !== typeof value ? domElement.setAttribute(key, "" + value) : domElement.removeAttribute(key);
            break;
          case "inert":
          case "allowFullScreen":
          case "async":
          case "autoPlay":
          case "controls":
          case "default":
          case "defer":
          case "disabled":
          case "disablePictureInPicture":
          case "disableRemotePlayback":
          case "formNoValidate":
          case "hidden":
          case "loop":
          case "noModule":
          case "noValidate":
          case "open":
          case "playsInline":
          case "readOnly":
          case "required":
          case "reversed":
          case "scoped":
          case "seamless":
          case "itemScope":
            value && "function" !== typeof value && "symbol" !== typeof value ? domElement.setAttribute(key, "") : domElement.removeAttribute(key);
            break;
          case "capture":
          case "download":
            true === value ? domElement.setAttribute(key, "") : false !== value && null != value && "function" !== typeof value && "symbol" !== typeof value ? domElement.setAttribute(key, value) : domElement.removeAttribute(key);
            break;
          case "cols":
          case "rows":
          case "size":
          case "span":
            null != value && "function" !== typeof value && "symbol" !== typeof value && !isNaN(value) && 1 <= value ? domElement.setAttribute(key, value) : domElement.removeAttribute(key);
            break;
          case "rowSpan":
          case "start":
            null == value || "function" === typeof value || "symbol" === typeof value || isNaN(value) ? domElement.removeAttribute(key) : domElement.setAttribute(key, value);
            break;
          case "popover":
            listenToNonDelegatedEvent("beforetoggle", domElement);
            listenToNonDelegatedEvent("toggle", domElement);
            setValueForAttribute(domElement, "popover", value);
            break;
          case "xlinkActuate":
            setValueForNamespacedAttribute(
              domElement,
              "http://www.w3.org/1999/xlink",
              "xlink:actuate",
              value
            );
            break;
          case "xlinkArcrole":
            setValueForNamespacedAttribute(
              domElement,
              "http://www.w3.org/1999/xlink",
              "xlink:arcrole",
              value
            );
            break;
          case "xlinkRole":
            setValueForNamespacedAttribute(
              domElement,
              "http://www.w3.org/1999/xlink",
              "xlink:role",
              value
            );
            break;
          case "xlinkShow":
            setValueForNamespacedAttribute(
              domElement,
              "http://www.w3.org/1999/xlink",
              "xlink:show",
              value
            );
            break;
          case "xlinkTitle":
            setValueForNamespacedAttribute(
              domElement,
              "http://www.w3.org/1999/xlink",
              "xlink:title",
              value
            );
            break;
          case "xlinkType":
            setValueForNamespacedAttribute(
              domElement,
              "http://www.w3.org/1999/xlink",
              "xlink:type",
              value
            );
            break;
          case "xmlBase":
            setValueForNamespacedAttribute(
              domElement,
              "http://www.w3.org/XML/1998/namespace",
              "xml:base",
              value
            );
            break;
          case "xmlLang":
            setValueForNamespacedAttribute(
              domElement,
              "http://www.w3.org/XML/1998/namespace",
              "xml:lang",
              value
            );
            break;
          case "xmlSpace":
            setValueForNamespacedAttribute(
              domElement,
              "http://www.w3.org/XML/1998/namespace",
              "xml:space",
              value
            );
            break;
          case "is":
            setValueForAttribute(domElement, "is", value);
            break;
          case "innerText":
          case "textContent":
            break;
          default:
            if (!(2 < key.length) || "o" !== key[0] && "O" !== key[0] || "n" !== key[1] && "N" !== key[1])
              key = aliases.get(key) || key, setValueForAttribute(domElement, key, value);
        }
      }
      function setPropOnCustomElement(domElement, tag, key, value, props, prevValue) {
        switch (key) {
          case "style":
            setValueForStyles(domElement, value, prevValue);
            break;
          case "dangerouslySetInnerHTML":
            if (null != value) {
              if ("object" !== typeof value || !("__html" in value))
                throw Error(formatProdErrorMessage(61));
              key = value.__html;
              if (null != key) {
                if (null != props.children) throw Error(formatProdErrorMessage(60));
                domElement.innerHTML = key;
              }
            }
            break;
          case "children":
            "string" === typeof value ? setTextContent(domElement, value) : ("number" === typeof value || "bigint" === typeof value) && setTextContent(domElement, "" + value);
            break;
          case "onScroll":
            null != value && listenToNonDelegatedEvent("scroll", domElement);
            break;
          case "onScrollEnd":
            null != value && listenToNonDelegatedEvent("scrollend", domElement);
            break;
          case "onClick":
            null != value && (domElement.onclick = noop$1);
            break;
          case "suppressContentEditableWarning":
          case "suppressHydrationWarning":
          case "innerHTML":
          case "ref":
            break;
          case "innerText":
          case "textContent":
            break;
          default:
            if (!registrationNameDependencies.hasOwnProperty(key))
              a: {
                if ("o" === key[0] && "n" === key[1] && (props = key.endsWith("Capture"), tag = key.slice(2, props ? key.length - 7 : void 0), prevValue = domElement[internalPropsKey] || null, prevValue = null != prevValue ? prevValue[key] : null, "function" === typeof prevValue && domElement.removeEventListener(tag, prevValue, props), "function" === typeof value)) {
                  "function" !== typeof prevValue && null !== prevValue && (key in domElement ? domElement[key] = null : domElement.hasAttribute(key) && domElement.removeAttribute(key));
                  domElement.addEventListener(tag, value, props);
                  break a;
                }
                key in domElement ? domElement[key] = value : true === value ? domElement.setAttribute(key, "") : setValueForAttribute(domElement, key, value);
              }
        }
      }
      function setInitialProperties(domElement, tag, props) {
        switch (tag) {
          case "div":
          case "span":
          case "svg":
          case "path":
          case "a":
          case "g":
          case "p":
          case "li":
            break;
          case "img":
            listenToNonDelegatedEvent("error", domElement);
            listenToNonDelegatedEvent("load", domElement);
            var hasSrc = false, hasSrcSet = false, propKey;
            for (propKey in props)
              if (props.hasOwnProperty(propKey)) {
                var propValue = props[propKey];
                if (null != propValue)
                  switch (propKey) {
                    case "src":
                      hasSrc = true;
                      break;
                    case "srcSet":
                      hasSrcSet = true;
                      break;
                    case "children":
                    case "dangerouslySetInnerHTML":
                      throw Error(formatProdErrorMessage(137, tag));
                    default:
                      setProp(domElement, tag, propKey, propValue, props, null);
                  }
              }
            hasSrcSet && setProp(domElement, tag, "srcSet", props.srcSet, props, null);
            hasSrc && setProp(domElement, tag, "src", props.src, props, null);
            return;
          case "input":
            listenToNonDelegatedEvent("invalid", domElement);
            var defaultValue = propKey = propValue = hasSrcSet = null, checked = null, defaultChecked = null;
            for (hasSrc in props)
              if (props.hasOwnProperty(hasSrc)) {
                var propValue$184 = props[hasSrc];
                if (null != propValue$184)
                  switch (hasSrc) {
                    case "name":
                      hasSrcSet = propValue$184;
                      break;
                    case "type":
                      propValue = propValue$184;
                      break;
                    case "checked":
                      checked = propValue$184;
                      break;
                    case "defaultChecked":
                      defaultChecked = propValue$184;
                      break;
                    case "value":
                      propKey = propValue$184;
                      break;
                    case "defaultValue":
                      defaultValue = propValue$184;
                      break;
                    case "children":
                    case "dangerouslySetInnerHTML":
                      if (null != propValue$184)
                        throw Error(formatProdErrorMessage(137, tag));
                      break;
                    default:
                      setProp(domElement, tag, hasSrc, propValue$184, props, null);
                  }
              }
            initInput(
              domElement,
              propKey,
              defaultValue,
              checked,
              defaultChecked,
              propValue,
              hasSrcSet,
              false
            );
            return;
          case "select":
            listenToNonDelegatedEvent("invalid", domElement);
            hasSrc = propValue = propKey = null;
            for (hasSrcSet in props)
              if (props.hasOwnProperty(hasSrcSet) && (defaultValue = props[hasSrcSet], null != defaultValue))
                switch (hasSrcSet) {
                  case "value":
                    propKey = defaultValue;
                    break;
                  case "defaultValue":
                    propValue = defaultValue;
                    break;
                  case "multiple":
                    hasSrc = defaultValue;
                  default:
                    setProp(domElement, tag, hasSrcSet, defaultValue, props, null);
                }
            tag = propKey;
            props = propValue;
            domElement.multiple = !!hasSrc;
            null != tag ? updateOptions(domElement, !!hasSrc, tag, false) : null != props && updateOptions(domElement, !!hasSrc, props, true);
            return;
          case "textarea":
            listenToNonDelegatedEvent("invalid", domElement);
            propKey = hasSrcSet = hasSrc = null;
            for (propValue in props)
              if (props.hasOwnProperty(propValue) && (defaultValue = props[propValue], null != defaultValue))
                switch (propValue) {
                  case "value":
                    hasSrc = defaultValue;
                    break;
                  case "defaultValue":
                    hasSrcSet = defaultValue;
                    break;
                  case "children":
                    propKey = defaultValue;
                    break;
                  case "dangerouslySetInnerHTML":
                    if (null != defaultValue) throw Error(formatProdErrorMessage(91));
                    break;
                  default:
                    setProp(domElement, tag, propValue, defaultValue, props, null);
                }
            initTextarea(domElement, hasSrc, hasSrcSet, propKey);
            return;
          case "option":
            for (checked in props)
              if (props.hasOwnProperty(checked) && (hasSrc = props[checked], null != hasSrc))
                switch (checked) {
                  case "selected":
                    domElement.selected = hasSrc && "function" !== typeof hasSrc && "symbol" !== typeof hasSrc;
                    break;
                  default:
                    setProp(domElement, tag, checked, hasSrc, props, null);
                }
            return;
          case "dialog":
            listenToNonDelegatedEvent("beforetoggle", domElement);
            listenToNonDelegatedEvent("toggle", domElement);
            listenToNonDelegatedEvent("cancel", domElement);
            listenToNonDelegatedEvent("close", domElement);
            break;
          case "iframe":
          case "object":
            listenToNonDelegatedEvent("load", domElement);
            break;
          case "video":
          case "audio":
            for (hasSrc = 0; hasSrc < mediaEventTypes.length; hasSrc++)
              listenToNonDelegatedEvent(mediaEventTypes[hasSrc], domElement);
            break;
          case "image":
            listenToNonDelegatedEvent("error", domElement);
            listenToNonDelegatedEvent("load", domElement);
            break;
          case "details":
            listenToNonDelegatedEvent("toggle", domElement);
            break;
          case "embed":
          case "source":
          case "link":
            listenToNonDelegatedEvent("error", domElement), listenToNonDelegatedEvent("load", domElement);
          case "area":
          case "base":
          case "br":
          case "col":
          case "hr":
          case "keygen":
          case "meta":
          case "param":
          case "track":
          case "wbr":
          case "menuitem":
            for (defaultChecked in props)
              if (props.hasOwnProperty(defaultChecked) && (hasSrc = props[defaultChecked], null != hasSrc))
                switch (defaultChecked) {
                  case "children":
                  case "dangerouslySetInnerHTML":
                    throw Error(formatProdErrorMessage(137, tag));
                  default:
                    setProp(domElement, tag, defaultChecked, hasSrc, props, null);
                }
            return;
          default:
            if (isCustomElement(tag)) {
              for (propValue$184 in props)
                props.hasOwnProperty(propValue$184) && (hasSrc = props[propValue$184], void 0 !== hasSrc && setPropOnCustomElement(
                  domElement,
                  tag,
                  propValue$184,
                  hasSrc,
                  props,
                  void 0
                ));
              return;
            }
        }
        for (defaultValue in props)
          props.hasOwnProperty(defaultValue) && (hasSrc = props[defaultValue], null != hasSrc && setProp(domElement, tag, defaultValue, hasSrc, props, null));
      }
      function updateProperties(domElement, tag, lastProps, nextProps) {
        switch (tag) {
          case "div":
          case "span":
          case "svg":
          case "path":
          case "a":
          case "g":
          case "p":
          case "li":
            break;
          case "input":
            var name = null, type = null, value = null, defaultValue = null, lastDefaultValue = null, checked = null, defaultChecked = null;
            for (propKey in lastProps) {
              var lastProp = lastProps[propKey];
              if (lastProps.hasOwnProperty(propKey) && null != lastProp)
                switch (propKey) {
                  case "checked":
                    break;
                  case "value":
                    break;
                  case "defaultValue":
                    lastDefaultValue = lastProp;
                  default:
                    nextProps.hasOwnProperty(propKey) || setProp(domElement, tag, propKey, null, nextProps, lastProp);
                }
            }
            for (var propKey$201 in nextProps) {
              var propKey = nextProps[propKey$201];
              lastProp = lastProps[propKey$201];
              if (nextProps.hasOwnProperty(propKey$201) && (null != propKey || null != lastProp))
                switch (propKey$201) {
                  case "type":
                    type = propKey;
                    break;
                  case "name":
                    name = propKey;
                    break;
                  case "checked":
                    checked = propKey;
                    break;
                  case "defaultChecked":
                    defaultChecked = propKey;
                    break;
                  case "value":
                    value = propKey;
                    break;
                  case "defaultValue":
                    defaultValue = propKey;
                    break;
                  case "children":
                  case "dangerouslySetInnerHTML":
                    if (null != propKey)
                      throw Error(formatProdErrorMessage(137, tag));
                    break;
                  default:
                    propKey !== lastProp && setProp(
                      domElement,
                      tag,
                      propKey$201,
                      propKey,
                      nextProps,
                      lastProp
                    );
                }
            }
            updateInput(
              domElement,
              value,
              defaultValue,
              lastDefaultValue,
              checked,
              defaultChecked,
              type,
              name
            );
            return;
          case "select":
            propKey = value = defaultValue = propKey$201 = null;
            for (type in lastProps)
              if (lastDefaultValue = lastProps[type], lastProps.hasOwnProperty(type) && null != lastDefaultValue)
                switch (type) {
                  case "value":
                    break;
                  case "multiple":
                    propKey = lastDefaultValue;
                  default:
                    nextProps.hasOwnProperty(type) || setProp(
                      domElement,
                      tag,
                      type,
                      null,
                      nextProps,
                      lastDefaultValue
                    );
                }
            for (name in nextProps)
              if (type = nextProps[name], lastDefaultValue = lastProps[name], nextProps.hasOwnProperty(name) && (null != type || null != lastDefaultValue))
                switch (name) {
                  case "value":
                    propKey$201 = type;
                    break;
                  case "defaultValue":
                    defaultValue = type;
                    break;
                  case "multiple":
                    value = type;
                  default:
                    type !== lastDefaultValue && setProp(
                      domElement,
                      tag,
                      name,
                      type,
                      nextProps,
                      lastDefaultValue
                    );
                }
            tag = defaultValue;
            lastProps = value;
            nextProps = propKey;
            null != propKey$201 ? updateOptions(domElement, !!lastProps, propKey$201, false) : !!nextProps !== !!lastProps && (null != tag ? updateOptions(domElement, !!lastProps, tag, true) : updateOptions(domElement, !!lastProps, lastProps ? [] : "", false));
            return;
          case "textarea":
            propKey = propKey$201 = null;
            for (defaultValue in lastProps)
              if (name = lastProps[defaultValue], lastProps.hasOwnProperty(defaultValue) && null != name && !nextProps.hasOwnProperty(defaultValue))
                switch (defaultValue) {
                  case "value":
                    break;
                  case "children":
                    break;
                  default:
                    setProp(domElement, tag, defaultValue, null, nextProps, name);
                }
            for (value in nextProps)
              if (name = nextProps[value], type = lastProps[value], nextProps.hasOwnProperty(value) && (null != name || null != type))
                switch (value) {
                  case "value":
                    propKey$201 = name;
                    break;
                  case "defaultValue":
                    propKey = name;
                    break;
                  case "children":
                    break;
                  case "dangerouslySetInnerHTML":
                    if (null != name) throw Error(formatProdErrorMessage(91));
                    break;
                  default:
                    name !== type && setProp(domElement, tag, value, name, nextProps, type);
                }
            updateTextarea(domElement, propKey$201, propKey);
            return;
          case "option":
            for (var propKey$217 in lastProps)
              if (propKey$201 = lastProps[propKey$217], lastProps.hasOwnProperty(propKey$217) && null != propKey$201 && !nextProps.hasOwnProperty(propKey$217))
                switch (propKey$217) {
                  case "selected":
                    domElement.selected = false;
                    break;
                  default:
                    setProp(
                      domElement,
                      tag,
                      propKey$217,
                      null,
                      nextProps,
                      propKey$201
                    );
                }
            for (lastDefaultValue in nextProps)
              if (propKey$201 = nextProps[lastDefaultValue], propKey = lastProps[lastDefaultValue], nextProps.hasOwnProperty(lastDefaultValue) && propKey$201 !== propKey && (null != propKey$201 || null != propKey))
                switch (lastDefaultValue) {
                  case "selected":
                    domElement.selected = propKey$201 && "function" !== typeof propKey$201 && "symbol" !== typeof propKey$201;
                    break;
                  default:
                    setProp(
                      domElement,
                      tag,
                      lastDefaultValue,
                      propKey$201,
                      nextProps,
                      propKey
                    );
                }
            return;
          case "img":
          case "link":
          case "area":
          case "base":
          case "br":
          case "col":
          case "embed":
          case "hr":
          case "keygen":
          case "meta":
          case "param":
          case "source":
          case "track":
          case "wbr":
          case "menuitem":
            for (var propKey$222 in lastProps)
              propKey$201 = lastProps[propKey$222], lastProps.hasOwnProperty(propKey$222) && null != propKey$201 && !nextProps.hasOwnProperty(propKey$222) && setProp(domElement, tag, propKey$222, null, nextProps, propKey$201);
            for (checked in nextProps)
              if (propKey$201 = nextProps[checked], propKey = lastProps[checked], nextProps.hasOwnProperty(checked) && propKey$201 !== propKey && (null != propKey$201 || null != propKey))
                switch (checked) {
                  case "children":
                  case "dangerouslySetInnerHTML":
                    if (null != propKey$201)
                      throw Error(formatProdErrorMessage(137, tag));
                    break;
                  default:
                    setProp(
                      domElement,
                      tag,
                      checked,
                      propKey$201,
                      nextProps,
                      propKey
                    );
                }
            return;
          default:
            if (isCustomElement(tag)) {
              for (var propKey$227 in lastProps)
                propKey$201 = lastProps[propKey$227], lastProps.hasOwnProperty(propKey$227) && void 0 !== propKey$201 && !nextProps.hasOwnProperty(propKey$227) && setPropOnCustomElement(
                  domElement,
                  tag,
                  propKey$227,
                  void 0,
                  nextProps,
                  propKey$201
                );
              for (defaultChecked in nextProps)
                propKey$201 = nextProps[defaultChecked], propKey = lastProps[defaultChecked], !nextProps.hasOwnProperty(defaultChecked) || propKey$201 === propKey || void 0 === propKey$201 && void 0 === propKey || setPropOnCustomElement(
                  domElement,
                  tag,
                  defaultChecked,
                  propKey$201,
                  nextProps,
                  propKey
                );
              return;
            }
        }
        for (var propKey$232 in lastProps)
          propKey$201 = lastProps[propKey$232], lastProps.hasOwnProperty(propKey$232) && null != propKey$201 && !nextProps.hasOwnProperty(propKey$232) && setProp(domElement, tag, propKey$232, null, nextProps, propKey$201);
        for (lastProp in nextProps)
          propKey$201 = nextProps[lastProp], propKey = lastProps[lastProp], !nextProps.hasOwnProperty(lastProp) || propKey$201 === propKey || null == propKey$201 && null == propKey || setProp(domElement, tag, lastProp, propKey$201, nextProps, propKey);
      }
      function isLikelyStaticResource(initiatorType) {
        switch (initiatorType) {
          case "css":
          case "script":
          case "font":
          case "img":
          case "image":
          case "input":
          case "link":
            return true;
          default:
            return false;
        }
      }
      function estimateBandwidth() {
        if ("function" === typeof performance.getEntriesByType) {
          for (var count3 = 0, bits = 0, resourceEntries = performance.getEntriesByType("resource"), i = 0; i < resourceEntries.length; i++) {
            var entry = resourceEntries[i], transferSize = entry.transferSize, initiatorType = entry.initiatorType, duration = entry.duration;
            if (transferSize && duration && isLikelyStaticResource(initiatorType)) {
              initiatorType = 0;
              duration = entry.responseEnd;
              for (i += 1; i < resourceEntries.length; i++) {
                var overlapEntry = resourceEntries[i], overlapStartTime = overlapEntry.startTime;
                if (overlapStartTime > duration) break;
                var overlapTransferSize = overlapEntry.transferSize, overlapInitiatorType = overlapEntry.initiatorType;
                overlapTransferSize && isLikelyStaticResource(overlapInitiatorType) && (overlapEntry = overlapEntry.responseEnd, initiatorType += overlapTransferSize * (overlapEntry < duration ? 1 : (duration - overlapStartTime) / (overlapEntry - overlapStartTime)));
              }
              --i;
              bits += 8 * (transferSize + initiatorType) / (entry.duration / 1e3);
              count3++;
              if (10 < count3) break;
            }
          }
          if (0 < count3) return bits / count3 / 1e6;
        }
        return navigator.connection && (count3 = navigator.connection.downlink, "number" === typeof count3) ? count3 : 5;
      }
      var eventsEnabled = null;
      var selectionInformation = null;
      function getOwnerDocumentFromRootContainer(rootContainerElement) {
        return 9 === rootContainerElement.nodeType ? rootContainerElement : rootContainerElement.ownerDocument;
      }
      function getOwnHostContext(namespaceURI) {
        switch (namespaceURI) {
          case "http://www.w3.org/2000/svg":
            return 1;
          case "http://www.w3.org/1998/Math/MathML":
            return 2;
          default:
            return 0;
        }
      }
      function getChildHostContextProd(parentNamespace, type) {
        if (0 === parentNamespace)
          switch (type) {
            case "svg":
              return 1;
            case "math":
              return 2;
            default:
              return 0;
          }
        return 1 === parentNamespace && "foreignObject" === type ? 0 : parentNamespace;
      }
      function shouldSetTextContent(type, props) {
        return "textarea" === type || "noscript" === type || "string" === typeof props.children || "number" === typeof props.children || "bigint" === typeof props.children || "object" === typeof props.dangerouslySetInnerHTML && null !== props.dangerouslySetInnerHTML && null != props.dangerouslySetInnerHTML.__html;
      }
      var currentPopstateTransitionEvent = null;
      function shouldAttemptEagerTransition() {
        var event = window.event;
        if (event && "popstate" === event.type) {
          if (event === currentPopstateTransitionEvent) return false;
          currentPopstateTransitionEvent = event;
          return true;
        }
        currentPopstateTransitionEvent = null;
        return false;
      }
      var scheduleTimeout = "function" === typeof setTimeout ? setTimeout : void 0;
      var cancelTimeout = "function" === typeof clearTimeout ? clearTimeout : void 0;
      var localPromise = "function" === typeof Promise ? Promise : void 0;
      var scheduleMicrotask = "function" === typeof queueMicrotask ? queueMicrotask : "undefined" !== typeof localPromise ? function(callback) {
        return localPromise.resolve(null).then(callback).catch(handleErrorInNextTick);
      } : scheduleTimeout;
      function handleErrorInNextTick(error) {
        setTimeout(function() {
          throw error;
        });
      }
      function isSingletonScope(type) {
        return "head" === type;
      }
      function clearHydrationBoundary(parentInstance, hydrationInstance) {
        var node = hydrationInstance, depth = 0;
        do {
          var nextNode = node.nextSibling;
          parentInstance.removeChild(node);
          if (nextNode && 8 === nextNode.nodeType)
            if (node = nextNode.data, "/$" === node || "/&" === node) {
              if (0 === depth) {
                parentInstance.removeChild(nextNode);
                retryIfBlockedOn(hydrationInstance);
                return;
              }
              depth--;
            } else if ("$" === node || "$?" === node || "$~" === node || "$!" === node || "&" === node)
              depth++;
            else if ("html" === node)
              releaseSingletonInstance(parentInstance.ownerDocument.documentElement);
            else if ("head" === node) {
              node = parentInstance.ownerDocument.head;
              releaseSingletonInstance(node);
              for (var node$jscomp$0 = node.firstChild; node$jscomp$0; ) {
                var nextNode$jscomp$0 = node$jscomp$0.nextSibling, nodeName = node$jscomp$0.nodeName;
                node$jscomp$0[internalHoistableMarker] || "SCRIPT" === nodeName || "STYLE" === nodeName || "LINK" === nodeName && "stylesheet" === node$jscomp$0.rel.toLowerCase() || node.removeChild(node$jscomp$0);
                node$jscomp$0 = nextNode$jscomp$0;
              }
            } else
              "body" === node && releaseSingletonInstance(parentInstance.ownerDocument.body);
          node = nextNode;
        } while (node);
        retryIfBlockedOn(hydrationInstance);
      }
      function hideOrUnhideDehydratedBoundary(suspenseInstance, isHidden2) {
        var node = suspenseInstance;
        suspenseInstance = 0;
        do {
          var nextNode = node.nextSibling;
          1 === node.nodeType ? isHidden2 ? (node._stashedDisplay = node.style.display, node.style.display = "none") : (node.style.display = node._stashedDisplay || "", "" === node.getAttribute("style") && node.removeAttribute("style")) : 3 === node.nodeType && (isHidden2 ? (node._stashedText = node.nodeValue, node.nodeValue = "") : node.nodeValue = node._stashedText || "");
          if (nextNode && 8 === nextNode.nodeType)
            if (node = nextNode.data, "/$" === node)
              if (0 === suspenseInstance) break;
              else suspenseInstance--;
            else
              "$" !== node && "$?" !== node && "$~" !== node && "$!" !== node || suspenseInstance++;
          node = nextNode;
        } while (node);
      }
      function clearContainerSparingly(container) {
        var nextNode = container.firstChild;
        nextNode && 10 === nextNode.nodeType && (nextNode = nextNode.nextSibling);
        for (; nextNode; ) {
          var node = nextNode;
          nextNode = nextNode.nextSibling;
          switch (node.nodeName) {
            case "HTML":
            case "HEAD":
            case "BODY":
              clearContainerSparingly(node);
              detachDeletedInstance(node);
              continue;
            case "SCRIPT":
            case "STYLE":
              continue;
            case "LINK":
              if ("stylesheet" === node.rel.toLowerCase()) continue;
          }
          container.removeChild(node);
        }
      }
      function canHydrateInstance(instance, type, props, inRootOrSingleton) {
        for (; 1 === instance.nodeType; ) {
          var anyProps = props;
          if (instance.nodeName.toLowerCase() !== type.toLowerCase()) {
            if (!inRootOrSingleton && ("INPUT" !== instance.nodeName || "hidden" !== instance.type))
              break;
          } else if (!inRootOrSingleton)
            if ("input" === type && "hidden" === instance.type) {
              var name = null == anyProps.name ? null : "" + anyProps.name;
              if ("hidden" === anyProps.type && instance.getAttribute("name") === name)
                return instance;
            } else return instance;
          else if (!instance[internalHoistableMarker])
            switch (type) {
              case "meta":
                if (!instance.hasAttribute("itemprop")) break;
                return instance;
              case "link":
                name = instance.getAttribute("rel");
                if ("stylesheet" === name && instance.hasAttribute("data-precedence"))
                  break;
                else if (name !== anyProps.rel || instance.getAttribute("href") !== (null == anyProps.href || "" === anyProps.href ? null : anyProps.href) || instance.getAttribute("crossorigin") !== (null == anyProps.crossOrigin ? null : anyProps.crossOrigin) || instance.getAttribute("title") !== (null == anyProps.title ? null : anyProps.title))
                  break;
                return instance;
              case "style":
                if (instance.hasAttribute("data-precedence")) break;
                return instance;
              case "script":
                name = instance.getAttribute("src");
                if ((name !== (null == anyProps.src ? null : anyProps.src) || instance.getAttribute("type") !== (null == anyProps.type ? null : anyProps.type) || instance.getAttribute("crossorigin") !== (null == anyProps.crossOrigin ? null : anyProps.crossOrigin)) && name && instance.hasAttribute("async") && !instance.hasAttribute("itemprop"))
                  break;
                return instance;
              default:
                return instance;
            }
          instance = getNextHydratable(instance.nextSibling);
          if (null === instance) break;
        }
        return null;
      }
      function canHydrateTextInstance(instance, text5, inRootOrSingleton) {
        if ("" === text5) return null;
        for (; 3 !== instance.nodeType; ) {
          if ((1 !== instance.nodeType || "INPUT" !== instance.nodeName || "hidden" !== instance.type) && !inRootOrSingleton)
            return null;
          instance = getNextHydratable(instance.nextSibling);
          if (null === instance) return null;
        }
        return instance;
      }
      function canHydrateHydrationBoundary(instance, inRootOrSingleton) {
        for (; 8 !== instance.nodeType; ) {
          if ((1 !== instance.nodeType || "INPUT" !== instance.nodeName || "hidden" !== instance.type) && !inRootOrSingleton)
            return null;
          instance = getNextHydratable(instance.nextSibling);
          if (null === instance) return null;
        }
        return instance;
      }
      function isSuspenseInstancePending(instance) {
        return "$?" === instance.data || "$~" === instance.data;
      }
      function isSuspenseInstanceFallback(instance) {
        return "$!" === instance.data || "$?" === instance.data && "loading" !== instance.ownerDocument.readyState;
      }
      function registerSuspenseInstanceRetry(instance, callback) {
        var ownerDocument = instance.ownerDocument;
        if ("$~" === instance.data) instance._reactRetry = callback;
        else if ("$?" !== instance.data || "loading" !== ownerDocument.readyState)
          callback();
        else {
          var listener = function() {
            callback();
            ownerDocument.removeEventListener("DOMContentLoaded", listener);
          };
          ownerDocument.addEventListener("DOMContentLoaded", listener);
          instance._reactRetry = listener;
        }
      }
      function getNextHydratable(node) {
        for (; null != node; node = node.nextSibling) {
          var nodeType = node.nodeType;
          if (1 === nodeType || 3 === nodeType) break;
          if (8 === nodeType) {
            nodeType = node.data;
            if ("$" === nodeType || "$!" === nodeType || "$?" === nodeType || "$~" === nodeType || "&" === nodeType || "F!" === nodeType || "F" === nodeType)
              break;
            if ("/$" === nodeType || "/&" === nodeType) return null;
          }
        }
        return node;
      }
      var previousHydratableOnEnteringScopedSingleton = null;
      function getNextHydratableInstanceAfterHydrationBoundary(hydrationInstance) {
        hydrationInstance = hydrationInstance.nextSibling;
        for (var depth = 0; hydrationInstance; ) {
          if (8 === hydrationInstance.nodeType) {
            var data = hydrationInstance.data;
            if ("/$" === data || "/&" === data) {
              if (0 === depth)
                return getNextHydratable(hydrationInstance.nextSibling);
              depth--;
            } else
              "$" !== data && "$!" !== data && "$?" !== data && "$~" !== data && "&" !== data || depth++;
          }
          hydrationInstance = hydrationInstance.nextSibling;
        }
        return null;
      }
      function getParentHydrationBoundary(targetInstance) {
        targetInstance = targetInstance.previousSibling;
        for (var depth = 0; targetInstance; ) {
          if (8 === targetInstance.nodeType) {
            var data = targetInstance.data;
            if ("$" === data || "$!" === data || "$?" === data || "$~" === data || "&" === data) {
              if (0 === depth) return targetInstance;
              depth--;
            } else "/$" !== data && "/&" !== data || depth++;
          }
          targetInstance = targetInstance.previousSibling;
        }
        return null;
      }
      function resolveSingletonInstance(type, props, rootContainerInstance) {
        props = getOwnerDocumentFromRootContainer(rootContainerInstance);
        switch (type) {
          case "html":
            type = props.documentElement;
            if (!type) throw Error(formatProdErrorMessage(452));
            return type;
          case "head":
            type = props.head;
            if (!type) throw Error(formatProdErrorMessage(453));
            return type;
          case "body":
            type = props.body;
            if (!type) throw Error(formatProdErrorMessage(454));
            return type;
          default:
            throw Error(formatProdErrorMessage(451));
        }
      }
      function releaseSingletonInstance(instance) {
        for (var attributes = instance.attributes; attributes.length; )
          instance.removeAttributeNode(attributes[0]);
        detachDeletedInstance(instance);
      }
      var preloadPropsMap = /* @__PURE__ */ new Map();
      var preconnectsSet = /* @__PURE__ */ new Set();
      function getHoistableRoot(container) {
        return "function" === typeof container.getRootNode ? container.getRootNode() : 9 === container.nodeType ? container : container.ownerDocument;
      }
      var previousDispatcher = ReactDOMSharedInternals.d;
      ReactDOMSharedInternals.d = {
        f: flushSyncWork,
        r: requestFormReset,
        D: prefetchDNS,
        C: preconnect,
        L: preload,
        m: preloadModule,
        X: preinitScript,
        S: preinitStyle,
        M: preinitModuleScript
      };
      function flushSyncWork() {
        var previousWasRendering = previousDispatcher.f(), wasRendering = flushSyncWork$1();
        return previousWasRendering || wasRendering;
      }
      function requestFormReset(form) {
        var formInst = getInstanceFromNode(form);
        null !== formInst && 5 === formInst.tag && "form" === formInst.type ? requestFormReset$1(formInst) : previousDispatcher.r(form);
      }
      var globalDocument = "undefined" === typeof document ? null : document;
      function preconnectAs(rel, href, crossOrigin) {
        var ownerDocument = globalDocument;
        if (ownerDocument && "string" === typeof href && href) {
          var limitedEscapedHref = escapeSelectorAttributeValueInsideDoubleQuotes(href);
          limitedEscapedHref = 'link[rel="' + rel + '"][href="' + limitedEscapedHref + '"]';
          "string" === typeof crossOrigin && (limitedEscapedHref += '[crossorigin="' + crossOrigin + '"]');
          preconnectsSet.has(limitedEscapedHref) || (preconnectsSet.add(limitedEscapedHref), rel = { rel, crossOrigin, href }, null === ownerDocument.querySelector(limitedEscapedHref) && (href = ownerDocument.createElement("link"), setInitialProperties(href, "link", rel), markNodeAsHoistable(href), ownerDocument.head.appendChild(href)));
        }
      }
      function prefetchDNS(href) {
        previousDispatcher.D(href);
        preconnectAs("dns-prefetch", href, null);
      }
      function preconnect(href, crossOrigin) {
        previousDispatcher.C(href, crossOrigin);
        preconnectAs("preconnect", href, crossOrigin);
      }
      function preload(href, as, options2) {
        previousDispatcher.L(href, as, options2);
        var ownerDocument = globalDocument;
        if (ownerDocument && href && as) {
          var preloadSelector = 'link[rel="preload"][as="' + escapeSelectorAttributeValueInsideDoubleQuotes(as) + '"]';
          "image" === as ? options2 && options2.imageSrcSet ? (preloadSelector += '[imagesrcset="' + escapeSelectorAttributeValueInsideDoubleQuotes(
            options2.imageSrcSet
          ) + '"]', "string" === typeof options2.imageSizes && (preloadSelector += '[imagesizes="' + escapeSelectorAttributeValueInsideDoubleQuotes(
            options2.imageSizes
          ) + '"]')) : preloadSelector += '[href="' + escapeSelectorAttributeValueInsideDoubleQuotes(href) + '"]' : preloadSelector += '[href="' + escapeSelectorAttributeValueInsideDoubleQuotes(href) + '"]';
          var key = preloadSelector;
          switch (as) {
            case "style":
              key = getStyleKey(href);
              break;
            case "script":
              key = getScriptKey(href);
          }
          preloadPropsMap.has(key) || (href = assign(
            {
              rel: "preload",
              href: "image" === as && options2 && options2.imageSrcSet ? void 0 : href,
              as
            },
            options2
          ), preloadPropsMap.set(key, href), null !== ownerDocument.querySelector(preloadSelector) || "style" === as && ownerDocument.querySelector(getStylesheetSelectorFromKey(key)) || "script" === as && ownerDocument.querySelector(getScriptSelectorFromKey(key)) || (as = ownerDocument.createElement("link"), setInitialProperties(as, "link", href), markNodeAsHoistable(as), ownerDocument.head.appendChild(as)));
        }
      }
      function preloadModule(href, options2) {
        previousDispatcher.m(href, options2);
        var ownerDocument = globalDocument;
        if (ownerDocument && href) {
          var as = options2 && "string" === typeof options2.as ? options2.as : "script", preloadSelector = 'link[rel="modulepreload"][as="' + escapeSelectorAttributeValueInsideDoubleQuotes(as) + '"][href="' + escapeSelectorAttributeValueInsideDoubleQuotes(href) + '"]', key = preloadSelector;
          switch (as) {
            case "audioworklet":
            case "paintworklet":
            case "serviceworker":
            case "sharedworker":
            case "worker":
            case "script":
              key = getScriptKey(href);
          }
          if (!preloadPropsMap.has(key) && (href = assign({ rel: "modulepreload", href }, options2), preloadPropsMap.set(key, href), null === ownerDocument.querySelector(preloadSelector))) {
            switch (as) {
              case "audioworklet":
              case "paintworklet":
              case "serviceworker":
              case "sharedworker":
              case "worker":
              case "script":
                if (ownerDocument.querySelector(getScriptSelectorFromKey(key)))
                  return;
            }
            as = ownerDocument.createElement("link");
            setInitialProperties(as, "link", href);
            markNodeAsHoistable(as);
            ownerDocument.head.appendChild(as);
          }
        }
      }
      function preinitStyle(href, precedence, options2) {
        previousDispatcher.S(href, precedence, options2);
        var ownerDocument = globalDocument;
        if (ownerDocument && href) {
          var styles = getResourcesFromRoot(ownerDocument).hoistableStyles, key = getStyleKey(href);
          precedence = precedence || "default";
          var resource = styles.get(key);
          if (!resource) {
            var state2 = { loading: 0, preload: null };
            if (resource = ownerDocument.querySelector(
              getStylesheetSelectorFromKey(key)
            ))
              state2.loading = 5;
            else {
              href = assign(
                { rel: "stylesheet", href, "data-precedence": precedence },
                options2
              );
              (options2 = preloadPropsMap.get(key)) && adoptPreloadPropsForStylesheet(href, options2);
              var link = resource = ownerDocument.createElement("link");
              markNodeAsHoistable(link);
              setInitialProperties(link, "link", href);
              link._p = new Promise(function(resolve, reject) {
                link.onload = resolve;
                link.onerror = reject;
              });
              link.addEventListener("load", function() {
                state2.loading |= 1;
              });
              link.addEventListener("error", function() {
                state2.loading |= 2;
              });
              state2.loading |= 4;
              insertStylesheet(resource, precedence, ownerDocument);
            }
            resource = {
              type: "stylesheet",
              instance: resource,
              count: 1,
              state: state2
            };
            styles.set(key, resource);
          }
        }
      }
      function preinitScript(src, options2) {
        previousDispatcher.X(src, options2);
        var ownerDocument = globalDocument;
        if (ownerDocument && src) {
          var scripts = getResourcesFromRoot(ownerDocument).hoistableScripts, key = getScriptKey(src), resource = scripts.get(key);
          resource || (resource = ownerDocument.querySelector(getScriptSelectorFromKey(key)), resource || (src = assign({ src, async: true }, options2), (options2 = preloadPropsMap.get(key)) && adoptPreloadPropsForScript(src, options2), resource = ownerDocument.createElement("script"), markNodeAsHoistable(resource), setInitialProperties(resource, "link", src), ownerDocument.head.appendChild(resource)), resource = {
            type: "script",
            instance: resource,
            count: 1,
            state: null
          }, scripts.set(key, resource));
        }
      }
      function preinitModuleScript(src, options2) {
        previousDispatcher.M(src, options2);
        var ownerDocument = globalDocument;
        if (ownerDocument && src) {
          var scripts = getResourcesFromRoot(ownerDocument).hoistableScripts, key = getScriptKey(src), resource = scripts.get(key);
          resource || (resource = ownerDocument.querySelector(getScriptSelectorFromKey(key)), resource || (src = assign({ src, async: true, type: "module" }, options2), (options2 = preloadPropsMap.get(key)) && adoptPreloadPropsForScript(src, options2), resource = ownerDocument.createElement("script"), markNodeAsHoistable(resource), setInitialProperties(resource, "link", src), ownerDocument.head.appendChild(resource)), resource = {
            type: "script",
            instance: resource,
            count: 1,
            state: null
          }, scripts.set(key, resource));
        }
      }
      function getResource(type, currentProps, pendingProps, currentResource) {
        var JSCompiler_inline_result = (JSCompiler_inline_result = rootInstanceStackCursor.current) ? getHoistableRoot(JSCompiler_inline_result) : null;
        if (!JSCompiler_inline_result) throw Error(formatProdErrorMessage(446));
        switch (type) {
          case "meta":
          case "title":
            return null;
          case "style":
            return "string" === typeof pendingProps.precedence && "string" === typeof pendingProps.href ? (currentProps = getStyleKey(pendingProps.href), pendingProps = getResourcesFromRoot(
              JSCompiler_inline_result
            ).hoistableStyles, currentResource = pendingProps.get(currentProps), currentResource || (currentResource = {
              type: "style",
              instance: null,
              count: 0,
              state: null
            }, pendingProps.set(currentProps, currentResource)), currentResource) : { type: "void", instance: null, count: 0, state: null };
          case "link":
            if ("stylesheet" === pendingProps.rel && "string" === typeof pendingProps.href && "string" === typeof pendingProps.precedence) {
              type = getStyleKey(pendingProps.href);
              var styles$243 = getResourcesFromRoot(
                JSCompiler_inline_result
              ).hoistableStyles, resource$244 = styles$243.get(type);
              resource$244 || (JSCompiler_inline_result = JSCompiler_inline_result.ownerDocument || JSCompiler_inline_result, resource$244 = {
                type: "stylesheet",
                instance: null,
                count: 0,
                state: { loading: 0, preload: null }
              }, styles$243.set(type, resource$244), (styles$243 = JSCompiler_inline_result.querySelector(
                getStylesheetSelectorFromKey(type)
              )) && !styles$243._p && (resource$244.instance = styles$243, resource$244.state.loading = 5), preloadPropsMap.has(type) || (pendingProps = {
                rel: "preload",
                as: "style",
                href: pendingProps.href,
                crossOrigin: pendingProps.crossOrigin,
                integrity: pendingProps.integrity,
                media: pendingProps.media,
                hrefLang: pendingProps.hrefLang,
                referrerPolicy: pendingProps.referrerPolicy
              }, preloadPropsMap.set(type, pendingProps), styles$243 || preloadStylesheet(
                JSCompiler_inline_result,
                type,
                pendingProps,
                resource$244.state
              )));
              if (currentProps && null === currentResource)
                throw Error(formatProdErrorMessage(528, ""));
              return resource$244;
            }
            if (currentProps && null !== currentResource)
              throw Error(formatProdErrorMessage(529, ""));
            return null;
          case "script":
            return currentProps = pendingProps.async, pendingProps = pendingProps.src, "string" === typeof pendingProps && currentProps && "function" !== typeof currentProps && "symbol" !== typeof currentProps ? (currentProps = getScriptKey(pendingProps), pendingProps = getResourcesFromRoot(
              JSCompiler_inline_result
            ).hoistableScripts, currentResource = pendingProps.get(currentProps), currentResource || (currentResource = {
              type: "script",
              instance: null,
              count: 0,
              state: null
            }, pendingProps.set(currentProps, currentResource)), currentResource) : { type: "void", instance: null, count: 0, state: null };
          default:
            throw Error(formatProdErrorMessage(444, type));
        }
      }
      function getStyleKey(href) {
        return 'href="' + escapeSelectorAttributeValueInsideDoubleQuotes(href) + '"';
      }
      function getStylesheetSelectorFromKey(key) {
        return 'link[rel="stylesheet"][' + key + "]";
      }
      function stylesheetPropsFromRawProps(rawProps) {
        return assign({}, rawProps, {
          "data-precedence": rawProps.precedence,
          precedence: null
        });
      }
      function preloadStylesheet(ownerDocument, key, preloadProps, state2) {
        ownerDocument.querySelector('link[rel="preload"][as="style"][' + key + "]") ? state2.loading = 1 : (key = ownerDocument.createElement("link"), state2.preload = key, key.addEventListener("load", function() {
          return state2.loading |= 1;
        }), key.addEventListener("error", function() {
          return state2.loading |= 2;
        }), setInitialProperties(key, "link", preloadProps), markNodeAsHoistable(key), ownerDocument.head.appendChild(key));
      }
      function getScriptKey(src) {
        return '[src="' + escapeSelectorAttributeValueInsideDoubleQuotes(src) + '"]';
      }
      function getScriptSelectorFromKey(key) {
        return "script[async]" + key;
      }
      function acquireResource(hoistableRoot, resource, props) {
        resource.count++;
        if (null === resource.instance)
          switch (resource.type) {
            case "style":
              var instance = hoistableRoot.querySelector(
                'style[data-href~="' + escapeSelectorAttributeValueInsideDoubleQuotes(props.href) + '"]'
              );
              if (instance)
                return resource.instance = instance, markNodeAsHoistable(instance), instance;
              var styleProps = assign({}, props, {
                "data-href": props.href,
                "data-precedence": props.precedence,
                href: null,
                precedence: null
              });
              instance = (hoistableRoot.ownerDocument || hoistableRoot).createElement(
                "style"
              );
              markNodeAsHoistable(instance);
              setInitialProperties(instance, "style", styleProps);
              insertStylesheet(instance, props.precedence, hoistableRoot);
              return resource.instance = instance;
            case "stylesheet":
              styleProps = getStyleKey(props.href);
              var instance$249 = hoistableRoot.querySelector(
                getStylesheetSelectorFromKey(styleProps)
              );
              if (instance$249)
                return resource.state.loading |= 4, resource.instance = instance$249, markNodeAsHoistable(instance$249), instance$249;
              instance = stylesheetPropsFromRawProps(props);
              (styleProps = preloadPropsMap.get(styleProps)) && adoptPreloadPropsForStylesheet(instance, styleProps);
              instance$249 = (hoistableRoot.ownerDocument || hoistableRoot).createElement("link");
              markNodeAsHoistable(instance$249);
              var linkInstance = instance$249;
              linkInstance._p = new Promise(function(resolve, reject) {
                linkInstance.onload = resolve;
                linkInstance.onerror = reject;
              });
              setInitialProperties(instance$249, "link", instance);
              resource.state.loading |= 4;
              insertStylesheet(instance$249, props.precedence, hoistableRoot);
              return resource.instance = instance$249;
            case "script":
              instance$249 = getScriptKey(props.src);
              if (styleProps = hoistableRoot.querySelector(
                getScriptSelectorFromKey(instance$249)
              ))
                return resource.instance = styleProps, markNodeAsHoistable(styleProps), styleProps;
              instance = props;
              if (styleProps = preloadPropsMap.get(instance$249))
                instance = assign({}, props), adoptPreloadPropsForScript(instance, styleProps);
              hoistableRoot = hoistableRoot.ownerDocument || hoistableRoot;
              styleProps = hoistableRoot.createElement("script");
              markNodeAsHoistable(styleProps);
              setInitialProperties(styleProps, "link", instance);
              hoistableRoot.head.appendChild(styleProps);
              return resource.instance = styleProps;
            case "void":
              return null;
            default:
              throw Error(formatProdErrorMessage(443, resource.type));
          }
        else
          "stylesheet" === resource.type && 0 === (resource.state.loading & 4) && (instance = resource.instance, resource.state.loading |= 4, insertStylesheet(instance, props.precedence, hoistableRoot));
        return resource.instance;
      }
      function insertStylesheet(instance, precedence, root2) {
        for (var nodes = root2.querySelectorAll(
          'link[rel="stylesheet"][data-precedence],style[data-precedence]'
        ), last = nodes.length ? nodes[nodes.length - 1] : null, prior = last, i = 0; i < nodes.length; i++) {
          var node = nodes[i];
          if (node.dataset.precedence === precedence) prior = node;
          else if (prior !== last) break;
        }
        prior ? prior.parentNode.insertBefore(instance, prior.nextSibling) : (precedence = 9 === root2.nodeType ? root2.head : root2, precedence.insertBefore(instance, precedence.firstChild));
      }
      function adoptPreloadPropsForStylesheet(stylesheetProps, preloadProps) {
        null == stylesheetProps.crossOrigin && (stylesheetProps.crossOrigin = preloadProps.crossOrigin);
        null == stylesheetProps.referrerPolicy && (stylesheetProps.referrerPolicy = preloadProps.referrerPolicy);
        null == stylesheetProps.title && (stylesheetProps.title = preloadProps.title);
      }
      function adoptPreloadPropsForScript(scriptProps, preloadProps) {
        null == scriptProps.crossOrigin && (scriptProps.crossOrigin = preloadProps.crossOrigin);
        null == scriptProps.referrerPolicy && (scriptProps.referrerPolicy = preloadProps.referrerPolicy);
        null == scriptProps.integrity && (scriptProps.integrity = preloadProps.integrity);
      }
      var tagCaches = null;
      function getHydratableHoistableCache(type, keyAttribute, ownerDocument) {
        if (null === tagCaches) {
          var cache = /* @__PURE__ */ new Map();
          var caches2 = tagCaches = /* @__PURE__ */ new Map();
          caches2.set(ownerDocument, cache);
        } else
          caches2 = tagCaches, cache = caches2.get(ownerDocument), cache || (cache = /* @__PURE__ */ new Map(), caches2.set(ownerDocument, cache));
        if (cache.has(type)) return cache;
        cache.set(type, null);
        ownerDocument = ownerDocument.getElementsByTagName(type);
        for (caches2 = 0; caches2 < ownerDocument.length; caches2++) {
          var node = ownerDocument[caches2];
          if (!(node[internalHoistableMarker] || node[internalInstanceKey] || "link" === type && "stylesheet" === node.getAttribute("rel")) && "http://www.w3.org/2000/svg" !== node.namespaceURI) {
            var nodeKey = node.getAttribute(keyAttribute) || "";
            nodeKey = type + nodeKey;
            var existing = cache.get(nodeKey);
            existing ? existing.push(node) : cache.set(nodeKey, [node]);
          }
        }
        return cache;
      }
      function mountHoistable(hoistableRoot, type, instance) {
        hoistableRoot = hoistableRoot.ownerDocument || hoistableRoot;
        hoistableRoot.head.insertBefore(
          instance,
          "title" === type ? hoistableRoot.querySelector("head > title") : null
        );
      }
      function isHostHoistableType(type, props, hostContext) {
        if (1 === hostContext || null != props.itemProp) return false;
        switch (type) {
          case "meta":
          case "title":
            return true;
          case "style":
            if ("string" !== typeof props.precedence || "string" !== typeof props.href || "" === props.href)
              break;
            return true;
          case "link":
            if ("string" !== typeof props.rel || "string" !== typeof props.href || "" === props.href || props.onLoad || props.onError)
              break;
            switch (props.rel) {
              case "stylesheet":
                return type = props.disabled, "string" === typeof props.precedence && null == type;
              default:
                return true;
            }
          case "script":
            if (props.async && "function" !== typeof props.async && "symbol" !== typeof props.async && !props.onLoad && !props.onError && props.src && "string" === typeof props.src)
              return true;
        }
        return false;
      }
      function preloadResource(resource) {
        return "stylesheet" === resource.type && 0 === (resource.state.loading & 3) ? false : true;
      }
      function suspendResource(state2, hoistableRoot, resource, props) {
        if ("stylesheet" === resource.type && ("string" !== typeof props.media || false !== matchMedia(props.media).matches) && 0 === (resource.state.loading & 4)) {
          if (null === resource.instance) {
            var key = getStyleKey(props.href), instance = hoistableRoot.querySelector(
              getStylesheetSelectorFromKey(key)
            );
            if (instance) {
              hoistableRoot = instance._p;
              null !== hoistableRoot && "object" === typeof hoistableRoot && "function" === typeof hoistableRoot.then && (state2.count++, state2 = onUnsuspend.bind(state2), hoistableRoot.then(state2, state2));
              resource.state.loading |= 4;
              resource.instance = instance;
              markNodeAsHoistable(instance);
              return;
            }
            instance = hoistableRoot.ownerDocument || hoistableRoot;
            props = stylesheetPropsFromRawProps(props);
            (key = preloadPropsMap.get(key)) && adoptPreloadPropsForStylesheet(props, key);
            instance = instance.createElement("link");
            markNodeAsHoistable(instance);
            var linkInstance = instance;
            linkInstance._p = new Promise(function(resolve, reject) {
              linkInstance.onload = resolve;
              linkInstance.onerror = reject;
            });
            setInitialProperties(instance, "link", props);
            resource.instance = instance;
          }
          null === state2.stylesheets && (state2.stylesheets = /* @__PURE__ */ new Map());
          state2.stylesheets.set(resource, hoistableRoot);
          (hoistableRoot = resource.state.preload) && 0 === (resource.state.loading & 3) && (state2.count++, resource = onUnsuspend.bind(state2), hoistableRoot.addEventListener("load", resource), hoistableRoot.addEventListener("error", resource));
        }
      }
      var estimatedBytesWithinLimit = 0;
      function waitForCommitToBeReady(state2, timeoutOffset) {
        state2.stylesheets && 0 === state2.count && insertSuspendedStylesheets(state2, state2.stylesheets);
        return 0 < state2.count || 0 < state2.imgCount ? function(commit) {
          var stylesheetTimer = setTimeout(function() {
            state2.stylesheets && insertSuspendedStylesheets(state2, state2.stylesheets);
            if (state2.unsuspend) {
              var unsuspend = state2.unsuspend;
              state2.unsuspend = null;
              unsuspend();
            }
          }, 6e4 + timeoutOffset);
          0 < state2.imgBytes && 0 === estimatedBytesWithinLimit && (estimatedBytesWithinLimit = 62500 * estimateBandwidth());
          var imgTimer = setTimeout(
            function() {
              state2.waitingForImages = false;
              if (0 === state2.count && (state2.stylesheets && insertSuspendedStylesheets(state2, state2.stylesheets), state2.unsuspend)) {
                var unsuspend = state2.unsuspend;
                state2.unsuspend = null;
                unsuspend();
              }
            },
            (state2.imgBytes > estimatedBytesWithinLimit ? 50 : 800) + timeoutOffset
          );
          state2.unsuspend = commit;
          return function() {
            state2.unsuspend = null;
            clearTimeout(stylesheetTimer);
            clearTimeout(imgTimer);
          };
        } : null;
      }
      function onUnsuspend() {
        this.count--;
        if (0 === this.count && (0 === this.imgCount || !this.waitingForImages)) {
          if (this.stylesheets) insertSuspendedStylesheets(this, this.stylesheets);
          else if (this.unsuspend) {
            var unsuspend = this.unsuspend;
            this.unsuspend = null;
            unsuspend();
          }
        }
      }
      var precedencesByRoot = null;
      function insertSuspendedStylesheets(state2, resources) {
        state2.stylesheets = null;
        null !== state2.unsuspend && (state2.count++, precedencesByRoot = /* @__PURE__ */ new Map(), resources.forEach(insertStylesheetIntoRoot, state2), precedencesByRoot = null, onUnsuspend.call(state2));
      }
      function insertStylesheetIntoRoot(root2, resource) {
        if (!(resource.state.loading & 4)) {
          var precedences = precedencesByRoot.get(root2);
          if (precedences) var last = precedences.get(null);
          else {
            precedences = /* @__PURE__ */ new Map();
            precedencesByRoot.set(root2, precedences);
            for (var nodes = root2.querySelectorAll(
              "link[data-precedence],style[data-precedence]"
            ), i = 0; i < nodes.length; i++) {
              var node = nodes[i];
              if ("LINK" === node.nodeName || "not all" !== node.getAttribute("media"))
                precedences.set(node.dataset.precedence, node), last = node;
            }
            last && precedences.set(null, last);
          }
          nodes = resource.instance;
          node = nodes.getAttribute("data-precedence");
          i = precedences.get(node) || last;
          i === last && precedences.set(null, nodes);
          precedences.set(node, nodes);
          this.count++;
          last = onUnsuspend.bind(this);
          nodes.addEventListener("load", last);
          nodes.addEventListener("error", last);
          i ? i.parentNode.insertBefore(nodes, i.nextSibling) : (root2 = 9 === root2.nodeType ? root2.head : root2, root2.insertBefore(nodes, root2.firstChild));
          resource.state.loading |= 4;
        }
      }
      var HostTransitionContext = {
        $$typeof: REACT_CONTEXT_TYPE,
        Provider: null,
        Consumer: null,
        _currentValue: sharedNotPendingObject,
        _currentValue2: sharedNotPendingObject,
        _threadCount: 0
      };
      function FiberRootNode(containerInfo, tag, hydrate, identifierPrefix, onUncaughtError, onCaughtError, onRecoverableError, onDefaultTransitionIndicator, formState) {
        this.tag = 1;
        this.containerInfo = containerInfo;
        this.pingCache = this.current = this.pendingChildren = null;
        this.timeoutHandle = -1;
        this.callbackNode = this.next = this.pendingContext = this.context = this.cancelPendingCommit = null;
        this.callbackPriority = 0;
        this.expirationTimes = createLaneMap(-1);
        this.entangledLanes = this.shellSuspendCounter = this.errorRecoveryDisabledLanes = this.expiredLanes = this.warmLanes = this.pingedLanes = this.suspendedLanes = this.pendingLanes = 0;
        this.entanglements = createLaneMap(0);
        this.hiddenUpdates = createLaneMap(null);
        this.identifierPrefix = identifierPrefix;
        this.onUncaughtError = onUncaughtError;
        this.onCaughtError = onCaughtError;
        this.onRecoverableError = onRecoverableError;
        this.pooledCache = null;
        this.pooledCacheLanes = 0;
        this.formState = formState;
        this.incompleteTransitions = /* @__PURE__ */ new Map();
      }
      function createFiberRoot(containerInfo, tag, hydrate, initialChildren, hydrationCallbacks, isStrictMode, identifierPrefix, formState, onUncaughtError, onCaughtError, onRecoverableError, onDefaultTransitionIndicator) {
        containerInfo = new FiberRootNode(
          containerInfo,
          tag,
          hydrate,
          identifierPrefix,
          onUncaughtError,
          onCaughtError,
          onRecoverableError,
          onDefaultTransitionIndicator,
          formState
        );
        tag = 1;
        true === isStrictMode && (tag |= 24);
        isStrictMode = createFiberImplClass(3, null, null, tag);
        containerInfo.current = isStrictMode;
        isStrictMode.stateNode = containerInfo;
        tag = createCache();
        tag.refCount++;
        containerInfo.pooledCache = tag;
        tag.refCount++;
        isStrictMode.memoizedState = {
          element: initialChildren,
          isDehydrated: hydrate,
          cache: tag
        };
        initializeUpdateQueue(isStrictMode);
        return containerInfo;
      }
      function getContextForSubtree(parentComponent) {
        if (!parentComponent) return emptyContextObject;
        parentComponent = emptyContextObject;
        return parentComponent;
      }
      function updateContainerImpl(rootFiber, lane, element, container, parentComponent, callback) {
        parentComponent = getContextForSubtree(parentComponent);
        null === container.context ? container.context = parentComponent : container.pendingContext = parentComponent;
        container = createUpdate(lane);
        container.payload = { element };
        callback = void 0 === callback ? null : callback;
        null !== callback && (container.callback = callback);
        element = enqueueUpdate(rootFiber, container, lane);
        null !== element && (scheduleUpdateOnFiber(element, rootFiber, lane), entangleTransitions(element, rootFiber, lane));
      }
      function markRetryLaneImpl(fiber, retryLane) {
        fiber = fiber.memoizedState;
        if (null !== fiber && null !== fiber.dehydrated) {
          var a = fiber.retryLane;
          fiber.retryLane = 0 !== a && a < retryLane ? a : retryLane;
        }
      }
      function markRetryLaneIfNotHydrated(fiber, retryLane) {
        markRetryLaneImpl(fiber, retryLane);
        (fiber = fiber.alternate) && markRetryLaneImpl(fiber, retryLane);
      }
      function attemptContinuousHydration(fiber) {
        if (13 === fiber.tag || 31 === fiber.tag) {
          var root2 = enqueueConcurrentRenderForLane(fiber, 67108864);
          null !== root2 && scheduleUpdateOnFiber(root2, fiber, 67108864);
          markRetryLaneIfNotHydrated(fiber, 67108864);
        }
      }
      function attemptHydrationAtCurrentPriority(fiber) {
        if (13 === fiber.tag || 31 === fiber.tag) {
          var lane = requestUpdateLane();
          lane = getBumpedLaneForHydrationByLane(lane);
          var root2 = enqueueConcurrentRenderForLane(fiber, lane);
          null !== root2 && scheduleUpdateOnFiber(root2, fiber, lane);
          markRetryLaneIfNotHydrated(fiber, lane);
        }
      }
      var _enabled = true;
      function dispatchDiscreteEvent(domEventName, eventSystemFlags, container, nativeEvent) {
        var prevTransition = ReactSharedInternals.T;
        ReactSharedInternals.T = null;
        var previousPriority = ReactDOMSharedInternals.p;
        try {
          ReactDOMSharedInternals.p = 2, dispatchEvent(domEventName, eventSystemFlags, container, nativeEvent);
        } finally {
          ReactDOMSharedInternals.p = previousPriority, ReactSharedInternals.T = prevTransition;
        }
      }
      function dispatchContinuousEvent(domEventName, eventSystemFlags, container, nativeEvent) {
        var prevTransition = ReactSharedInternals.T;
        ReactSharedInternals.T = null;
        var previousPriority = ReactDOMSharedInternals.p;
        try {
          ReactDOMSharedInternals.p = 8, dispatchEvent(domEventName, eventSystemFlags, container, nativeEvent);
        } finally {
          ReactDOMSharedInternals.p = previousPriority, ReactSharedInternals.T = prevTransition;
        }
      }
      function dispatchEvent(domEventName, eventSystemFlags, targetContainer, nativeEvent) {
        if (_enabled) {
          var blockedOn = findInstanceBlockingEvent(nativeEvent);
          if (null === blockedOn)
            dispatchEventForPluginEventSystem(
              domEventName,
              eventSystemFlags,
              nativeEvent,
              return_targetInst,
              targetContainer
            ), clearIfContinuousEvent(domEventName, nativeEvent);
          else if (queueIfContinuousEvent(
            blockedOn,
            domEventName,
            eventSystemFlags,
            targetContainer,
            nativeEvent
          ))
            nativeEvent.stopPropagation();
          else if (clearIfContinuousEvent(domEventName, nativeEvent), eventSystemFlags & 4 && -1 < discreteReplayableEvents.indexOf(domEventName)) {
            for (; null !== blockedOn; ) {
              var fiber = getInstanceFromNode(blockedOn);
              if (null !== fiber)
                switch (fiber.tag) {
                  case 3:
                    fiber = fiber.stateNode;
                    if (fiber.current.memoizedState.isDehydrated) {
                      var lanes = getHighestPriorityLanes(fiber.pendingLanes);
                      if (0 !== lanes) {
                        var root2 = fiber;
                        root2.pendingLanes |= 2;
                        for (root2.entangledLanes |= 2; lanes; ) {
                          var lane = 1 << 31 - clz32(lanes);
                          root2.entanglements[1] |= lane;
                          lanes &= ~lane;
                        }
                        ensureRootIsScheduled(fiber);
                        0 === (executionContext & 6) && (workInProgressRootRenderTargetTime = now() + 500, flushSyncWorkAcrossRoots_impl(0, false));
                      }
                    }
                    break;
                  case 31:
                  case 13:
                    root2 = enqueueConcurrentRenderForLane(fiber, 2), null !== root2 && scheduleUpdateOnFiber(root2, fiber, 2), flushSyncWork$1(), markRetryLaneIfNotHydrated(fiber, 2);
                }
              fiber = findInstanceBlockingEvent(nativeEvent);
              null === fiber && dispatchEventForPluginEventSystem(
                domEventName,
                eventSystemFlags,
                nativeEvent,
                return_targetInst,
                targetContainer
              );
              if (fiber === blockedOn) break;
              blockedOn = fiber;
            }
            null !== blockedOn && nativeEvent.stopPropagation();
          } else
            dispatchEventForPluginEventSystem(
              domEventName,
              eventSystemFlags,
              nativeEvent,
              null,
              targetContainer
            );
        }
      }
      function findInstanceBlockingEvent(nativeEvent) {
        nativeEvent = getEventTarget(nativeEvent);
        return findInstanceBlockingTarget(nativeEvent);
      }
      var return_targetInst = null;
      function findInstanceBlockingTarget(targetNode) {
        return_targetInst = null;
        targetNode = getClosestInstanceFromNode(targetNode);
        if (null !== targetNode) {
          var nearestMounted = getNearestMountedFiber(targetNode);
          if (null === nearestMounted) targetNode = null;
          else {
            var tag = nearestMounted.tag;
            if (13 === tag) {
              targetNode = getSuspenseInstanceFromFiber(nearestMounted);
              if (null !== targetNode) return targetNode;
              targetNode = null;
            } else if (31 === tag) {
              targetNode = getActivityInstanceFromFiber(nearestMounted);
              if (null !== targetNode) return targetNode;
              targetNode = null;
            } else if (3 === tag) {
              if (nearestMounted.stateNode.current.memoizedState.isDehydrated)
                return 3 === nearestMounted.tag ? nearestMounted.stateNode.containerInfo : null;
              targetNode = null;
            } else nearestMounted !== targetNode && (targetNode = null);
          }
        }
        return_targetInst = targetNode;
        return null;
      }
      function getEventPriority(domEventName) {
        switch (domEventName) {
          case "beforetoggle":
          case "cancel":
          case "click":
          case "close":
          case "contextmenu":
          case "copy":
          case "cut":
          case "auxclick":
          case "dblclick":
          case "dragend":
          case "dragstart":
          case "drop":
          case "focusin":
          case "focusout":
          case "input":
          case "invalid":
          case "keydown":
          case "keypress":
          case "keyup":
          case "mousedown":
          case "mouseup":
          case "paste":
          case "pause":
          case "play":
          case "pointercancel":
          case "pointerdown":
          case "pointerup":
          case "ratechange":
          case "reset":
          case "resize":
          case "seeked":
          case "submit":
          case "toggle":
          case "touchcancel":
          case "touchend":
          case "touchstart":
          case "volumechange":
          case "change":
          case "selectionchange":
          case "textInput":
          case "compositionstart":
          case "compositionend":
          case "compositionupdate":
          case "beforeblur":
          case "afterblur":
          case "beforeinput":
          case "blur":
          case "fullscreenchange":
          case "focus":
          case "hashchange":
          case "popstate":
          case "select":
          case "selectstart":
            return 2;
          case "drag":
          case "dragenter":
          case "dragexit":
          case "dragleave":
          case "dragover":
          case "mousemove":
          case "mouseout":
          case "mouseover":
          case "pointermove":
          case "pointerout":
          case "pointerover":
          case "scroll":
          case "touchmove":
          case "wheel":
          case "mouseenter":
          case "mouseleave":
          case "pointerenter":
          case "pointerleave":
            return 8;
          case "message":
            switch (getCurrentPriorityLevel()) {
              case ImmediatePriority:
                return 2;
              case UserBlockingPriority:
                return 8;
              case NormalPriority$1:
              case LowPriority:
                return 32;
              case IdlePriority:
                return 268435456;
              default:
                return 32;
            }
          default:
            return 32;
        }
      }
      var hasScheduledReplayAttempt = false;
      var queuedFocus = null;
      var queuedDrag = null;
      var queuedMouse = null;
      var queuedPointers = /* @__PURE__ */ new Map();
      var queuedPointerCaptures = /* @__PURE__ */ new Map();
      var queuedExplicitHydrationTargets = [];
      var discreteReplayableEvents = "mousedown mouseup touchcancel touchend touchstart auxclick dblclick pointercancel pointerdown pointerup dragend dragstart drop compositionend compositionstart keydown keypress keyup input textInput copy cut paste click change contextmenu reset".split(
        " "
      );
      function clearIfContinuousEvent(domEventName, nativeEvent) {
        switch (domEventName) {
          case "focusin":
          case "focusout":
            queuedFocus = null;
            break;
          case "dragenter":
          case "dragleave":
            queuedDrag = null;
            break;
          case "mouseover":
          case "mouseout":
            queuedMouse = null;
            break;
          case "pointerover":
          case "pointerout":
            queuedPointers.delete(nativeEvent.pointerId);
            break;
          case "gotpointercapture":
          case "lostpointercapture":
            queuedPointerCaptures.delete(nativeEvent.pointerId);
        }
      }
      function accumulateOrCreateContinuousQueuedReplayableEvent(existingQueuedEvent, blockedOn, domEventName, eventSystemFlags, targetContainer, nativeEvent) {
        if (null === existingQueuedEvent || existingQueuedEvent.nativeEvent !== nativeEvent)
          return existingQueuedEvent = {
            blockedOn,
            domEventName,
            eventSystemFlags,
            nativeEvent,
            targetContainers: [targetContainer]
          }, null !== blockedOn && (blockedOn = getInstanceFromNode(blockedOn), null !== blockedOn && attemptContinuousHydration(blockedOn)), existingQueuedEvent;
        existingQueuedEvent.eventSystemFlags |= eventSystemFlags;
        blockedOn = existingQueuedEvent.targetContainers;
        null !== targetContainer && -1 === blockedOn.indexOf(targetContainer) && blockedOn.push(targetContainer);
        return existingQueuedEvent;
      }
      function queueIfContinuousEvent(blockedOn, domEventName, eventSystemFlags, targetContainer, nativeEvent) {
        switch (domEventName) {
          case "focusin":
            return queuedFocus = accumulateOrCreateContinuousQueuedReplayableEvent(
              queuedFocus,
              blockedOn,
              domEventName,
              eventSystemFlags,
              targetContainer,
              nativeEvent
            ), true;
          case "dragenter":
            return queuedDrag = accumulateOrCreateContinuousQueuedReplayableEvent(
              queuedDrag,
              blockedOn,
              domEventName,
              eventSystemFlags,
              targetContainer,
              nativeEvent
            ), true;
          case "mouseover":
            return queuedMouse = accumulateOrCreateContinuousQueuedReplayableEvent(
              queuedMouse,
              blockedOn,
              domEventName,
              eventSystemFlags,
              targetContainer,
              nativeEvent
            ), true;
          case "pointerover":
            var pointerId = nativeEvent.pointerId;
            queuedPointers.set(
              pointerId,
              accumulateOrCreateContinuousQueuedReplayableEvent(
                queuedPointers.get(pointerId) || null,
                blockedOn,
                domEventName,
                eventSystemFlags,
                targetContainer,
                nativeEvent
              )
            );
            return true;
          case "gotpointercapture":
            return pointerId = nativeEvent.pointerId, queuedPointerCaptures.set(
              pointerId,
              accumulateOrCreateContinuousQueuedReplayableEvent(
                queuedPointerCaptures.get(pointerId) || null,
                blockedOn,
                domEventName,
                eventSystemFlags,
                targetContainer,
                nativeEvent
              )
            ), true;
        }
        return false;
      }
      function attemptExplicitHydrationTarget(queuedTarget) {
        var targetInst = getClosestInstanceFromNode(queuedTarget.target);
        if (null !== targetInst) {
          var nearestMounted = getNearestMountedFiber(targetInst);
          if (null !== nearestMounted) {
            if (targetInst = nearestMounted.tag, 13 === targetInst) {
              if (targetInst = getSuspenseInstanceFromFiber(nearestMounted), null !== targetInst) {
                queuedTarget.blockedOn = targetInst;
                runWithPriority(queuedTarget.priority, function() {
                  attemptHydrationAtCurrentPriority(nearestMounted);
                });
                return;
              }
            } else if (31 === targetInst) {
              if (targetInst = getActivityInstanceFromFiber(nearestMounted), null !== targetInst) {
                queuedTarget.blockedOn = targetInst;
                runWithPriority(queuedTarget.priority, function() {
                  attemptHydrationAtCurrentPriority(nearestMounted);
                });
                return;
              }
            } else if (3 === targetInst && nearestMounted.stateNode.current.memoizedState.isDehydrated) {
              queuedTarget.blockedOn = 3 === nearestMounted.tag ? nearestMounted.stateNode.containerInfo : null;
              return;
            }
          }
        }
        queuedTarget.blockedOn = null;
      }
      function attemptReplayContinuousQueuedEvent(queuedEvent) {
        if (null !== queuedEvent.blockedOn) return false;
        for (var targetContainers = queuedEvent.targetContainers; 0 < targetContainers.length; ) {
          var nextBlockedOn = findInstanceBlockingEvent(queuedEvent.nativeEvent);
          if (null === nextBlockedOn) {
            nextBlockedOn = queuedEvent.nativeEvent;
            var nativeEventClone = new nextBlockedOn.constructor(
              nextBlockedOn.type,
              nextBlockedOn
            );
            currentReplayingEvent = nativeEventClone;
            nextBlockedOn.target.dispatchEvent(nativeEventClone);
            currentReplayingEvent = null;
          } else
            return targetContainers = getInstanceFromNode(nextBlockedOn), null !== targetContainers && attemptContinuousHydration(targetContainers), queuedEvent.blockedOn = nextBlockedOn, false;
          targetContainers.shift();
        }
        return true;
      }
      function attemptReplayContinuousQueuedEventInMap(queuedEvent, key, map) {
        attemptReplayContinuousQueuedEvent(queuedEvent) && map.delete(key);
      }
      function replayUnblockedEvents() {
        hasScheduledReplayAttempt = false;
        null !== queuedFocus && attemptReplayContinuousQueuedEvent(queuedFocus) && (queuedFocus = null);
        null !== queuedDrag && attemptReplayContinuousQueuedEvent(queuedDrag) && (queuedDrag = null);
        null !== queuedMouse && attemptReplayContinuousQueuedEvent(queuedMouse) && (queuedMouse = null);
        queuedPointers.forEach(attemptReplayContinuousQueuedEventInMap);
        queuedPointerCaptures.forEach(attemptReplayContinuousQueuedEventInMap);
      }
      function scheduleCallbackIfUnblocked(queuedEvent, unblocked) {
        queuedEvent.blockedOn === unblocked && (queuedEvent.blockedOn = null, hasScheduledReplayAttempt || (hasScheduledReplayAttempt = true, Scheduler.unstable_scheduleCallback(
          Scheduler.unstable_NormalPriority,
          replayUnblockedEvents
        )));
      }
      var lastScheduledReplayQueue = null;
      function scheduleReplayQueueIfNeeded(formReplayingQueue) {
        lastScheduledReplayQueue !== formReplayingQueue && (lastScheduledReplayQueue = formReplayingQueue, Scheduler.unstable_scheduleCallback(
          Scheduler.unstable_NormalPriority,
          function() {
            lastScheduledReplayQueue === formReplayingQueue && (lastScheduledReplayQueue = null);
            for (var i = 0; i < formReplayingQueue.length; i += 3) {
              var form = formReplayingQueue[i], submitterOrAction = formReplayingQueue[i + 1], formData = formReplayingQueue[i + 2];
              if ("function" !== typeof submitterOrAction)
                if (null === findInstanceBlockingTarget(submitterOrAction || form))
                  continue;
                else break;
              var formInst = getInstanceFromNode(form);
              null !== formInst && (formReplayingQueue.splice(i, 3), i -= 3, startHostTransition(
                formInst,
                {
                  pending: true,
                  data: formData,
                  method: form.method,
                  action: submitterOrAction
                },
                submitterOrAction,
                formData
              ));
            }
          }
        ));
      }
      function retryIfBlockedOn(unblocked) {
        function unblock(queuedEvent) {
          return scheduleCallbackIfUnblocked(queuedEvent, unblocked);
        }
        null !== queuedFocus && scheduleCallbackIfUnblocked(queuedFocus, unblocked);
        null !== queuedDrag && scheduleCallbackIfUnblocked(queuedDrag, unblocked);
        null !== queuedMouse && scheduleCallbackIfUnblocked(queuedMouse, unblocked);
        queuedPointers.forEach(unblock);
        queuedPointerCaptures.forEach(unblock);
        for (var i = 0; i < queuedExplicitHydrationTargets.length; i++) {
          var queuedTarget = queuedExplicitHydrationTargets[i];
          queuedTarget.blockedOn === unblocked && (queuedTarget.blockedOn = null);
        }
        for (; 0 < queuedExplicitHydrationTargets.length && (i = queuedExplicitHydrationTargets[0], null === i.blockedOn); )
          attemptExplicitHydrationTarget(i), null === i.blockedOn && queuedExplicitHydrationTargets.shift();
        i = (unblocked.ownerDocument || unblocked).$$reactFormReplay;
        if (null != i)
          for (queuedTarget = 0; queuedTarget < i.length; queuedTarget += 3) {
            var form = i[queuedTarget], submitterOrAction = i[queuedTarget + 1], formProps = form[internalPropsKey] || null;
            if ("function" === typeof submitterOrAction)
              formProps || scheduleReplayQueueIfNeeded(i);
            else if (formProps) {
              var action = null;
              if (submitterOrAction && submitterOrAction.hasAttribute("formAction"))
                if (form = submitterOrAction, formProps = submitterOrAction[internalPropsKey] || null)
                  action = formProps.formAction;
                else {
                  if (null !== findInstanceBlockingTarget(form)) continue;
                }
              else action = formProps.action;
              "function" === typeof action ? i[queuedTarget + 1] = action : (i.splice(queuedTarget, 3), queuedTarget -= 3);
              scheduleReplayQueueIfNeeded(i);
            }
          }
      }
      function defaultOnDefaultTransitionIndicator() {
        function handleNavigate(event) {
          event.canIntercept && "react-transition" === event.info && event.intercept({
            handler: function() {
              return new Promise(function(resolve) {
                return pendingResolve = resolve;
              });
            },
            focusReset: "manual",
            scroll: "manual"
          });
        }
        function handleNavigateComplete() {
          null !== pendingResolve && (pendingResolve(), pendingResolve = null);
          isCancelled || setTimeout(startFakeNavigation, 20);
        }
        function startFakeNavigation() {
          if (!isCancelled && !navigation.transition) {
            var currentEntry = navigation.currentEntry;
            currentEntry && null != currentEntry.url && navigation.navigate(currentEntry.url, {
              state: currentEntry.getState(),
              info: "react-transition",
              history: "replace"
            });
          }
        }
        if ("object" === typeof navigation) {
          var isCancelled = false, pendingResolve = null;
          navigation.addEventListener("navigate", handleNavigate);
          navigation.addEventListener("navigatesuccess", handleNavigateComplete);
          navigation.addEventListener("navigateerror", handleNavigateComplete);
          setTimeout(startFakeNavigation, 100);
          return function() {
            isCancelled = true;
            navigation.removeEventListener("navigate", handleNavigate);
            navigation.removeEventListener("navigatesuccess", handleNavigateComplete);
            navigation.removeEventListener("navigateerror", handleNavigateComplete);
            null !== pendingResolve && (pendingResolve(), pendingResolve = null);
          };
        }
      }
      function ReactDOMRoot(internalRoot) {
        this._internalRoot = internalRoot;
      }
      ReactDOMHydrationRoot.prototype.render = ReactDOMRoot.prototype.render = function(children) {
        var root2 = this._internalRoot;
        if (null === root2) throw Error(formatProdErrorMessage(409));
        var current = root2.current, lane = requestUpdateLane();
        updateContainerImpl(current, lane, children, root2, null, null);
      };
      ReactDOMHydrationRoot.prototype.unmount = ReactDOMRoot.prototype.unmount = function() {
        var root2 = this._internalRoot;
        if (null !== root2) {
          this._internalRoot = null;
          var container = root2.containerInfo;
          updateContainerImpl(root2.current, 2, null, root2, null, null);
          flushSyncWork$1();
          container[internalContainerInstanceKey] = null;
        }
      };
      function ReactDOMHydrationRoot(internalRoot) {
        this._internalRoot = internalRoot;
      }
      ReactDOMHydrationRoot.prototype.unstable_scheduleHydration = function(target) {
        if (target) {
          var updatePriority = resolveUpdatePriority();
          target = { blockedOn: null, target, priority: updatePriority };
          for (var i = 0; i < queuedExplicitHydrationTargets.length && 0 !== updatePriority && updatePriority < queuedExplicitHydrationTargets[i].priority; i++) ;
          queuedExplicitHydrationTargets.splice(i, 0, target);
          0 === i && attemptExplicitHydrationTarget(target);
        }
      };
      var isomorphicReactPackageVersion$jscomp$inline_1840 = React42.version;
      if ("19.2.7" !== isomorphicReactPackageVersion$jscomp$inline_1840)
        throw Error(
          formatProdErrorMessage(
            527,
            isomorphicReactPackageVersion$jscomp$inline_1840,
            "19.2.7"
          )
        );
      ReactDOMSharedInternals.findDOMNode = function(componentOrElement) {
        var fiber = componentOrElement._reactInternals;
        if (void 0 === fiber) {
          if ("function" === typeof componentOrElement.render)
            throw Error(formatProdErrorMessage(188));
          componentOrElement = Object.keys(componentOrElement).join(",");
          throw Error(formatProdErrorMessage(268, componentOrElement));
        }
        componentOrElement = findCurrentFiberUsingSlowPath(fiber);
        componentOrElement = null !== componentOrElement ? findCurrentHostFiberImpl(componentOrElement) : null;
        componentOrElement = null === componentOrElement ? null : componentOrElement.stateNode;
        return componentOrElement;
      };
      var internals$jscomp$inline_2347 = {
        bundleType: 0,
        version: "19.2.7",
        rendererPackageName: "react-dom",
        currentDispatcherRef: ReactSharedInternals,
        reconcilerVersion: "19.2.7"
      };
      if ("undefined" !== typeof __REACT_DEVTOOLS_GLOBAL_HOOK__) {
        hook$jscomp$inline_2348 = __REACT_DEVTOOLS_GLOBAL_HOOK__;
        if (!hook$jscomp$inline_2348.isDisabled && hook$jscomp$inline_2348.supportsFiber)
          try {
            rendererID = hook$jscomp$inline_2348.inject(
              internals$jscomp$inline_2347
            ), injectedHook = hook$jscomp$inline_2348;
          } catch (err) {
          }
      }
      var hook$jscomp$inline_2348;
      exports.createRoot = function(container, options2) {
        if (!isValidContainer(container)) throw Error(formatProdErrorMessage(299));
        var isStrictMode = false, identifierPrefix = "", onUncaughtError = defaultOnUncaughtError, onCaughtError = defaultOnCaughtError, onRecoverableError = defaultOnRecoverableError;
        null !== options2 && void 0 !== options2 && (true === options2.unstable_strictMode && (isStrictMode = true), void 0 !== options2.identifierPrefix && (identifierPrefix = options2.identifierPrefix), void 0 !== options2.onUncaughtError && (onUncaughtError = options2.onUncaughtError), void 0 !== options2.onCaughtError && (onCaughtError = options2.onCaughtError), void 0 !== options2.onRecoverableError && (onRecoverableError = options2.onRecoverableError));
        options2 = createFiberRoot(
          container,
          1,
          false,
          null,
          null,
          isStrictMode,
          identifierPrefix,
          null,
          onUncaughtError,
          onCaughtError,
          onRecoverableError,
          defaultOnDefaultTransitionIndicator
        );
        container[internalContainerInstanceKey] = options2.current;
        listenToAllSupportedEvents(container);
        return new ReactDOMRoot(options2);
      };
      exports.hydrateRoot = function(container, initialChildren, options2) {
        if (!isValidContainer(container)) throw Error(formatProdErrorMessage(299));
        var isStrictMode = false, identifierPrefix = "", onUncaughtError = defaultOnUncaughtError, onCaughtError = defaultOnCaughtError, onRecoverableError = defaultOnRecoverableError, formState = null;
        null !== options2 && void 0 !== options2 && (true === options2.unstable_strictMode && (isStrictMode = true), void 0 !== options2.identifierPrefix && (identifierPrefix = options2.identifierPrefix), void 0 !== options2.onUncaughtError && (onUncaughtError = options2.onUncaughtError), void 0 !== options2.onCaughtError && (onCaughtError = options2.onCaughtError), void 0 !== options2.onRecoverableError && (onRecoverableError = options2.onRecoverableError), void 0 !== options2.formState && (formState = options2.formState));
        initialChildren = createFiberRoot(
          container,
          1,
          true,
          initialChildren,
          null != options2 ? options2 : null,
          isStrictMode,
          identifierPrefix,
          formState,
          onUncaughtError,
          onCaughtError,
          onRecoverableError,
          defaultOnDefaultTransitionIndicator
        );
        initialChildren.context = getContextForSubtree(null);
        options2 = initialChildren.current;
        isStrictMode = requestUpdateLane();
        isStrictMode = getBumpedLaneForHydrationByLane(isStrictMode);
        identifierPrefix = createUpdate(isStrictMode);
        identifierPrefix.callback = null;
        enqueueUpdate(options2, identifierPrefix, isStrictMode);
        options2 = isStrictMode;
        initialChildren.current.lanes = options2;
        markRootUpdated$1(initialChildren, options2);
        ensureRootIsScheduled(initialChildren);
        container[internalContainerInstanceKey] = initialChildren.current;
        listenToAllSupportedEvents(container);
        return new ReactDOMHydrationRoot(initialChildren);
      };
      exports.version = "19.2.7";
    }
  });

  // node_modules/react-dom/client.js
  var require_client = __commonJS({
    "node_modules/react-dom/client.js"(exports, module) {
      "use strict";
      function checkDCE() {
        if (typeof __REACT_DEVTOOLS_GLOBAL_HOOK__ === "undefined" || typeof __REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE !== "function") {
          return;
        }
        if (false) {
          throw new Error("^_^");
        }
        try {
          __REACT_DEVTOOLS_GLOBAL_HOOK__.checkDCE(checkDCE);
        } catch (err) {
          console.error(err);
        }
      }
      if (true) {
        checkDCE();
        module.exports = require_react_dom_client_production();
      } else {
        module.exports = null;
      }
    }
  });

  // node_modules/react/cjs/react-jsx-runtime.production.js
  var require_react_jsx_runtime_production = __commonJS({
    "node_modules/react/cjs/react-jsx-runtime.production.js"(exports) {
      "use strict";
      var REACT_ELEMENT_TYPE = /* @__PURE__ */ Symbol.for("react.transitional.element");
      var REACT_FRAGMENT_TYPE = /* @__PURE__ */ Symbol.for("react.fragment");
      function jsxProd(type, config, maybeKey) {
        var key = null;
        void 0 !== maybeKey && (key = "" + maybeKey);
        void 0 !== config.key && (key = "" + config.key);
        if ("key" in config) {
          maybeKey = {};
          for (var propName in config)
            "key" !== propName && (maybeKey[propName] = config[propName]);
        } else maybeKey = config;
        config = maybeKey.ref;
        return {
          $$typeof: REACT_ELEMENT_TYPE,
          type,
          key,
          ref: void 0 !== config ? config : null,
          props: maybeKey
        };
      }
      exports.Fragment = REACT_FRAGMENT_TYPE;
      exports.jsx = jsxProd;
      exports.jsxs = jsxProd;
    }
  });

  // node_modules/react/jsx-runtime.js
  var require_jsx_runtime = __commonJS({
    "node_modules/react/jsx-runtime.js"(exports, module) {
      "use strict";
      if (true) {
        module.exports = require_react_jsx_runtime_production();
      } else {
        module.exports = null;
      }
    }
  });

  // src/web-ui/browser/legacy-pwa-cleanup.ts
  var LEGACY_PWA_CLEANUP_KEY = "wand-legacy-pwa-cleanup-v1";
  var LEGACY_CACHE_PREFIXES = ["wand-static-", "wand-runtime-"];
  function isLegacyWandServiceWorker(worker) {
    if (!worker) return false;
    try {
      const scriptUrl = new URL(worker.scriptURL, location.href);
      return scriptUrl.origin === location.origin && scriptUrl.pathname === "/sw.js";
    } catch {
      return false;
    }
  }
  function isLegacyWandRegistration(registration) {
    return isLegacyWandServiceWorker(registration.installing) || isLegacyWandServiceWorker(registration.waiting) || isLegacyWandServiceWorker(registration.active);
  }
  function hasCompletedCleanup() {
    try {
      return localStorage.getItem(LEGACY_PWA_CLEANUP_KEY) === "done";
    } catch {
      return false;
    }
  }
  function markCleanupCompleted() {
    try {
      localStorage.setItem(LEGACY_PWA_CLEANUP_KEY, "done");
    } catch {
    }
  }
  async function unregisterLegacyServiceWorkers() {
    if (!("serviceWorker" in navigator)) return true;
    try {
      const registrations = await navigator.serviceWorker.getRegistrations();
      const legacyRegistrations = registrations.filter(isLegacyWandRegistration);
      await Promise.all(legacyRegistrations.map((registration) => registration.unregister()));
      return true;
    } catch {
      return false;
    }
  }
  async function deleteLegacyCaches() {
    if (!("caches" in window)) return true;
    try {
      const cacheNames = await caches.keys();
      const legacyCacheNames = cacheNames.filter(
        (name) => LEGACY_CACHE_PREFIXES.some((prefix) => name.startsWith(prefix))
      );
      await Promise.all(legacyCacheNames.map((name) => caches.delete(name)));
      return true;
    } catch {
      return false;
    }
  }
  async function cleanupLegacyPwaState() {
    if (hasCompletedCleanup()) return;
    const controlledByLegacyWorker = "serviceWorker" in navigator && isLegacyWandServiceWorker(navigator.serviceWorker.controller);
    const [workersCleaned, cachesCleaned] = await Promise.all([
      unregisterLegacyServiceWorkers(),
      deleteLegacyCaches()
    ]);
    if (!workersCleaned || !cachesCleaned) return;
    markCleanupCompleted();
    if (controlledByLegacyWorker) location.reload();
  }
  void cleanupLegacyPwaState();

  // src/web-ui/browser/state.ts
  var configPath = "${escapeHtml(configPath)}";
  var CHAT_EXPAND_STATE_STORAGE_KEY = "wand-chat-expand-state-v1";
  var LS_MIGRATIONS = [
    // v1 保留为 no-op：曾经这里会删除 wand-sidebar-pinned，导致升级或刷新时
    // 覆盖用户明确选择的侧栏状态。迁移函数必须只修正格式，不能抹掉偏好。
    function migrateSidebarPinDefault() {
    }
  ];
  (function runLocalStorageMigrations() {
    try {
      var raw = localStorage.getItem("wand-ls-schema");
      var applied = raw == null ? 0 : parseInt(raw, 10);
      if (!(applied >= 0)) applied = 0;
      for (var i = applied; i < LS_MIGRATIONS.length; i++) {
        try {
          LS_MIGRATIONS[i]();
        } catch (e) {
        }
      }
      if (applied < LS_MIGRATIONS.length) {
        localStorage.setItem("wand-ls-schema", String(LS_MIGRATIONS.length));
      }
    } catch (e) {
    }
  })();
  function readStoredBoolean(key, defaultValue) {
    try {
      var value = localStorage.getItem(key);
      if (value === "true") return true;
      if (value === "false") return false;
      return defaultValue;
    } catch (e) {
      return defaultValue;
    }
  }
  function writeStoredBoolean(key, value) {
    try {
      localStorage.setItem(key, String(!!value));
    } catch (e) {
    }
  }
  var state = {
    selectedId: (function() {
      try {
        var url = new URL(window.location.href);
        var requested = url.searchParams.get("session");
        if (requested) {
          localStorage.setItem("wand-selected-session", requested);
          url.searchParams.delete("session");
          history.replaceState(history.state, "", url.toString());
          return requested;
        }
      } catch (e) {
      }
      try {
        return localStorage.getItem("wand-selected-session") || null;
      } catch (e) {
        return null;
      }
    })(),
    pollTimer: null,
    config: null,
    sessions: [],
    terminal: null,
    terminalFitInProgress: false,
    terminalSessionId: null,
    terminalOutput: "",
    // R8: /clear marker。Claude 的 /clear 不发任何 ANSI 清屏序列，它只
    // 就地把对话框重画成空、把旧对话推进 scrollback。但 wand 的
    // state.terminalOutput 是 append-only buffer，softResync 一触发就
    // 把 /clear 之前的历史全部重放回 wterm（用户看到"/clear 后短暂闪
    // 回旧内容"）。marker 表示 buffer 里"用户上次 /clear 时刻的位置"，
    // softResync 只重放 slice(marker)，从根上避免历史被重放。
    terminalOutputMarker: 0,
    terminalLiveStreamSessions: {},
    // CSI ?2026h..l 同步输出缓冲：begin 时拿到 "\x1b[?2026h" 后开始缓冲，
    // end 时拿到 "\x1b[?2026l" 一次性 flush 给 wterm。null 表示当前不在
    // sync 包帧内。@wterm/core 0.1.8 不实现 sync output，begin/end 之间
    // 每个 write 立即落到 grid + mark dirty —— 跨 server-debounce 窗口
    // 时浏览器看到中间帧 + 触发 softResync 时状态机被打断，正是
    // askuserquestion 菜单多份叠加的最强候选根因。
    syncOutputBuffer: null,
    syncOutputDeadline: 0,
    syncFramingResidue: false,
    lastChunkAt: 0,
    terminalHealthTimer: null,
    lastTerminalResyncAt: 0,
    terminalAutoFollow: true,
    // 程序触发的滚动（wand 主动 scrollTo / wterm 内部因 _shouldScrollToBottom=true
    // 拽 scrollTop=scrollHeight）落到 scroll handler 时会被误判为"用户滚回严格
    // 底部"，把 autoFollow 反转回 true，把用户刚 wheel 上滚的意图吞掉。
    // 存"窗口截止时间戳"而非"开始时间戳"：不同调用方按各自动画长度延长窗口
    // （瞬时 120ms 覆盖一次 rAF + 事件分发；smooth 500ms 覆盖 Chromium smooth
    // scroll 动画），多次调用用 Math.max 合并、不会被短窗口缩短。
    terminalProgrammaticScrollUntil: 0,
    terminalScrollIdleTimer: null,
    terminalScrollThreshold: 12,
    showTerminalJumpToBottom: false,
    terminalViewportEl: null,
    terminalViewportScrollHandler: null,
    terminalViewportTouchHandler: null,
    terminalViewportTouchStartHandler: null,
    terminalTouchStartY: 0,
    terminalComposing: false,
    resizeObserver: null,
    resizeHandler: null,
    resizeTimer: null,
    inputQueue: Promise.resolve(),
    pendingMessages: [],
    // WebSocket 断线期间的消息队列
    messageQueue: [],
    // 用户消息排队等待发送
    crossSessionQueue: (function() {
      try {
        var saved = localStorage.getItem("wand-cross-session-queue");
        var parsed = saved ? JSON.parse(saved) : [];
        return Array.isArray(parsed) ? parsed : [];
      } catch (e) {
        return [];
      }
    })(),
    // 跨会话排队消息 [{ id, text, cwd, mode, tool }]
    structuredInputQueue: [],
    // 结构化会话同会话排队消息
    // 排队条 UI 局部状态 ——
    //   queueBarExpanded: 整条气泡条是否处于展开态（true = 展开成垂直详情列表；
    //     false = 收起成水平小气泡胶囊）。点击胶囊空白 / 气泡本体 / +N 徽章切换。
    //     ESC / 清空 / 全部 promote 出去时也会被自动收回。
    //   queueBarDrag: 拖拽排序进行中时的临时状态（pointer 捕获、起始坐标、参考 rect）。
    //   收起态以前还有"hover 展开某一条"的旧实现，已在 iOS 26 玻璃条改造里一起下线；
    //   queueBarHoverIndex 不再被任何代码读写，保留 null 占位以免破坏其他模块的
    //   类型推断。
    queueBarExpanded: false,
    queueBarHoverIndex: null,
    queueBarDrag: null,
    queueBarPromoting: false,
    drafts: {},
    isSyncingInputBox: false,
    loginPending: false,
    loginChecked: false,
    bootstrapping: true,
    sessionsDrawerOpen: readStoredBoolean("wand-sidebar-open", false),
    // 新交互：桌面默认呼出即常驻；只有用户主动关闭过才记 "false"。
    sidebarPinned: readStoredBoolean("wand-sidebar-pinned", true),
    sidebarCollapsed: readStoredBoolean("wand-sidebar-collapsed", false),
    modeValue: "managed",
    chatMode: "managed",
    chatModels: (function() {
      try {
        var legacy = localStorage.getItem("wand-chat-model") || "";
        return {
          claude: localStorage.getItem("wand-chat-model-claude") || legacy,
          codex: localStorage.getItem("wand-chat-model-codex") || "",
          opencode: localStorage.getItem("wand-chat-model-opencode") || "",
          grok: localStorage.getItem("wand-chat-model-grok") || "",
          qoder: localStorage.getItem("wand-chat-model-qoder") || ""
        };
      } catch (e) {
        return { claude: "", codex: "", opencode: "", grok: "", qoder: "" };
      }
    })(),
    chatModel: (function() {
      try {
        return localStorage.getItem("wand-chat-model") || "";
      } catch (e) {
        return "";
      }
    })(),
    chatThinking: (function() {
      try {
        var v = localStorage.getItem("wand-thinking-effort") || "off";
        return v === "off" || v === "standard" || v === "deep" || v === "max" || /^codex:[a-z0-9][a-z0-9_-]{0,31}$/.test(v) ? v : "off";
      } catch (e) {
        return "off";
      }
    })(),
    availableModels: [],
    availableCodexModels: [],
    availableOpenCodeModels: [],
    availableGrokModels: [],
    availableQoderModels: [],
    modelsRefreshing: false,
    sessionTool: "claude",
    preferredCommand: "claude",
    structuredRunner: "claude-cli-print",
    lastResize: { cols: 0, rows: 0 },
    isOnline: navigator.onLine,
    ws: null,
    wsConnected: false,
    // 上一次从服务器收到任意 WS 消息（包括 ping）的时间戳。心跳 stale 检测
    // 用它来判断半开连接：长时间没消息 → forceReconnect。0 表示尚未连接过。
    lastWsMessageAt: 0,
    // 心跳检查 timer 句柄。每 10s 跑一次 evaluateWsHeartbeatStale()。
    wsHeartbeatCheckTimer: null,
    _updateBubbleShown: false,
    notificationHistory: {},
    delayedNotificationTimer: null,
    notifSound: (function() {
      try {
        var v = localStorage.getItem("wand-notif-sound");
        return v === null ? true : v === "true";
      } catch (e) {
        return true;
      }
    })(),
    notifVolume: (function() {
      try {
        var v = localStorage.getItem("wand-notif-volume");
        return v === null ? 80 : Math.max(0, Math.min(100, parseInt(v, 10) || 80));
      } catch (e) {
        return 80;
      }
    })(),
    notifBubble: (function() {
      try {
        var v = localStorage.getItem("wand-notif-bubble");
        return v === null ? true : v === "true";
      } catch (e) {
        return true;
      }
    })(),
    toolContentCache: {},
    // Per-session WS output sequence tracker. Reset on connect/reconnect.
    // Used to detect gaps caused by server-side backpressure drops and
    // request a fresh snapshot.
    lastSeqBySession: {},
    currentView: "terminal",
    terminalScale: (function() {
      try {
        var saved = localStorage.getItem("wand-terminal-scale");
        return saved ? parseFloat(saved) : 1;
      } catch (e) {
        return 1;
      }
    })(),
    terminalBaseFontSize: 13,
    keyboardPopupOpen: false,
    filePanelOpen: (function() {
      try {
        return localStorage.getItem("wand-file-panel-open") === "true";
      } catch (e) {
        return false;
      }
    })(),
    topbarMoreOpen: false,
    gitStatus: null,
    gitStatusSessionId: null,
    gitStatusLoading: false,
    gitStatusInflight: null,
    gitStatusLastFetchAt: 0,
    // Telegram 风格的"贴底"状态：true = 用户当前贴在底部，新消息会自然出现；
    // false = 用户向上滚了，未读会累积到气泡里，不会自动滚他们的视图。
    chatStickToBottom: true,
    // 旧版自动折叠横条已禁用：不再把最新一轮摘要固定到聊天顶部。
    chatAutoFoldEnabled: false,
    // 当前会话视图里"激活的折叠快照"，记录顶部预览对应的最新 user / assistant 索引。
    chatAutoFoldSnapshot: null,
    chatUnreadCount: 0,
    // state.currentMessages 中第一条未读消息的 index，-1 表示没有未读。
    chatUnreadStartIndex: -1,
    // 业界共识 150-180px：120px 在触控板/移动端惯性下边界来回弹。
    chatScrollThreshold: 160,
    chatIsProgrammaticScroll: false,
    // 程序触发滚动的"宽限期"时间戳：scrollTop 赋值后浏览器的 scroll 事件
    // 往往晚于单个 rAF 才派发，单靠 chatIsProgrammaticScroll 在 rAF 里复位会
    // 太早，导致 pin 自己的重定位被 scroll handler 误判成用户滚动而释放。
    // 在此时间戳之前到达的 scroll 事件一律当作程序滚动忽略。
    chatProgrammaticScrollUntil: 0,
    chatScrollElement: null,
    chatScrollHandler: null,
    chatScrollWheelHandler: null,
    chatScrollTouchStartHandler: null,
    chatScrollTouchMoveHandler: null,
    chatTouchStartY: 0,
    // 仅在"首次渲染当前会话视图"时才允许 fullRenderChat 强制贴底。
    // resetChatRenderCache 会把它设回 false；fullRenderChat 第一次跑完就置 true。
    // page-refresh / ws 重连不重置此标记，避免把用户拽到底部。
    chatInitialRenderDone: false,
    lastForegroundSyncAt: 0,
    foregroundSyncTimer: null,
    wsReconnectAttempts: 0,
    wsReconnectTimer: null,
    currentMessages: [],
    lastRenderedHash: 0,
    lastRenderedMsgCount: 0,
    lastRenderedEmpty: null,
    renderPending: false,
    chatPageSize: 20,
    chatRenderedCount: 20,
    currentTask: null,
    // Current task title from Claude
    terminalInteractive: false,
    miniKeyboardVisible: false,
    modifiers: { ctrl: false, alt: false, shift: false },
    // ── 终端悬浮摇杆遥控器（手机端 PTY 遥控）状态 ──
    // joystickPos 持久化球球位置 {right, bottom}（localStorage wand-ball-pos）
    joystickPos: (function() {
      try {
        var saved = localStorage.getItem("wand-ball-pos");
        if (!saved) return null;
        var parsed = JSON.parse(saved);
        return parsed && typeof parsed === "object" ? parsed : null;
      } catch (e) {
        return null;
      }
    })(),
    joystickPinnedOpen: false,
    // 钉住面板是否展开（不持久化，切会话复位）
    joystickRootEl: null,
    // 以下均为运行期句柄，teardown 复位
    joystickPanelEl: null,
    joystickBackdropEl: null,
    joystickBallEl: null,
    joystickPointerId: null,
    joystickGesture: null,
    // null|'pending'|'cancelled'|'move'
    joystickPressStart: null,
    // {x, y, t}
    joystickLongPressTimer: null,
    joystickMoveHandler: null,
    joystickUpHandler: null,
    joystickResizeHandler: null,
    fileSearchQuery: "",
    fileExplorerLoading: false,
    allFiles: [],
    fileExplorerCwd: "",
    fileExplorerTruncated: false,
    fileExplorerTotal: 0,
    claudeHistory: [],
    claudeHistoryLoaded: false,
    claudeHistoryExpanded: false,
    claudeHistoryExpandedDirs: {},
    archivedExpanded: false,
    sessionsManageMode: false,
    selectedSessionIds: {},
    selectedClaudeHistoryIds: {},
    codexHistory: [],
    codexHistoryLoaded: false,
    codexHistoryExpandedDirs: {},
    selectedCodexHistoryIds: {},
    askUserSelections: {},
    // { toolUseId: { 0: [optIdx...], submitted: false } }
    queueEpoch: 0,
    // Monotonic counter for queue state freshness
    pendingAttachments: [],
    // [{ file, previewUrl, name, size }]
    // Load last used working directory from localStorage
    workingDir: (function() {
      try {
        var saved = localStorage.getItem("wand-working-dir");
        return saved || "";
      } catch (e) {
        return "";
      }
    })()
  };

  // src/web-ui/browser/i18n.ts
  var I18N_DEFAULT_LANG = "\u4E2D\u6587";
  var I18N = {
    "\u4E2D\u6587": {
      "subagent.tag": "\u5B50\u4EE3\u7406",
      "subagent.handoff": "{parent} \u8BA9 {sub} \u5E2E\u5FD9",
      "subagent.handoff.with_desc": "{parent} \u8BA9 {sub} \u5E2E\u5FD9\uFF1A",
      "subagent.continued": "\u7EE7\u7EED\u8F93\u51FA",
      "subagent.task.done": "\u4EFB\u52A1\u5B8C\u6210",
      "subagent.task.failed": "\u4EFB\u52A1\u5931\u8D25",
      "subagent.running": "\u8FD0\u884C\u4E2D",
      "subagent.no_output": "\uFF08\u65E0\u8F93\u51FA\uFF09",
      "subagent.helper_fallback_prefix": "\u534F\u4F5C\u732B\xB7",
      "subagent.title_aria": "\u70B9\u51FB\u5C55\u5F00 / \u6536\u8D77\u5B50\u4EE3\u7406\u8F93\u51FA",
      "subagent.tag_title": "\u5B50\u4EE3\u7406 / subagent",
      "ui.expand": "\u5C55\u5F00",
      "ui.collapse": "\u6536\u8D77",
      "ui.expand_panel_aria": "\u5C55\u5F00\u5B50\u4EE3\u7406\u8F93\u51FA",
      "ui.collapse_panel_aria": "\u6536\u8D77\u5B50\u4EE3\u7406\u8F93\u51FA",
      "history.expand": "\u5C55\u5F00\u5386\u53F2\u5BF9\u8BDD",
      "history.collapse": "\u6536\u8D77\u5386\u53F2\u5BF9\u8BDD",
      "history.rounds": "{n} \u8F6E\u5BF9\u8BDD",
      "history.tools": "{n} \u6B21\u5DE5\u5177\u8C03\u7528",
      "history.agents": "{n} \u4E2A\u5B50\u4EE3\u7406",
      "history.errors": "{n} \u4E2A\u5931\u8D25",
      "stop.confirm.title": "\u505C\u6B62\u4EFB\u52A1",
      "stop.confirm.message": "\u786E\u5B9A\u8981\u505C\u6B62\u5F53\u524D\u6B63\u5728\u8FD0\u884C\u7684\u4EFB\u52A1\u5417\uFF1F",
      "stop.confirm.ok": "\u505C\u6B62",
      "stop.confirm.cancel": "\u53D6\u6D88"
    },
    "English": {
      "subagent.tag": "Subagent",
      "subagent.handoff": "{parent} asked {sub} for help",
      "subagent.handoff.with_desc": "{parent} asked {sub} for help with: ",
      "subagent.continued": "continued",
      "subagent.task.done": "Task complete",
      "subagent.task.failed": "Task failed",
      "subagent.running": "Running",
      "subagent.no_output": "(no output)",
      "subagent.helper_fallback_prefix": "Helper\xB7",
      "subagent.title_aria": "Click to expand / collapse subagent output",
      "subagent.tag_title": "Subagent",
      "ui.expand": "Expand",
      "ui.collapse": "Collapse",
      "ui.expand_panel_aria": "Expand subagent output",
      "ui.collapse_panel_aria": "Collapse subagent output",
      "history.expand": "Show earlier conversation",
      "history.collapse": "Hide earlier conversation",
      "history.rounds": "{n} rounds",
      "history.tools": "{n} tool calls",
      "history.agents": "{n} subagents",
      "history.errors": "{n} failed",
      "stop.confirm.title": "Stop task",
      "stop.confirm.message": "Stop the task that's currently running?",
      "stop.confirm.ok": "Stop",
      "stop.confirm.cancel": "Cancel"
    }
  };
  function getActiveLang() {
    var raw = state.config && typeof state.config.language === "string" ? state.config.language.trim() : "";
    if (!raw) return I18N_DEFAULT_LANG;
    if (I18N[raw]) return raw;
    var lower = raw.toLowerCase();
    if (lower === "english" || lower === "en" || lower.indexOf("english") === 0 || lower.indexOf("\u82F1") === 0) return "English";
    if (lower === "\u4E2D\u6587" || lower === "zh" || lower.indexOf("zh") === 0 || lower.indexOf("\u4E2D") === 0 || lower.indexOf("chinese") === 0) return "\u4E2D\u6587";
    return "English";
  }
  function t(key, params) {
    var lang = getActiveLang();
    var table = I18N[lang] || I18N[I18N_DEFAULT_LANG];
    var template = table && key in table ? table[key] : null;
    if (template == null) {
      var def = I18N[I18N_DEFAULT_LANG];
      template = def && key in def ? def[key] : key;
    }
    if (params && typeof template === "string") {
      for (var k in params) {
        if (!Object.prototype.hasOwnProperty.call(params, k)) continue;
        template = template.split("{" + k + "}").join(params[k]);
      }
    }
    return template;
  }
  var ICON_PATHS = {
    // shape sets — 24x24 viewbox, currentColor stroke
    shield: '<path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/>',
    shieldCheck: '<path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/><polyline points="9 12 11 14 15 10"/>',
    lock: '<rect x="5" y="10" width="14" height="11" rx="2"/><path d="M8 10V7a4 4 0 0 1 8 0v3"/><path d="M12 14v3"/>',
    certificate: '<path d="M6 3h12v14H6z"/><path d="M9 7h6"/><path d="M9 11h4"/><path d="m10 17-1 4 3-1 3 1-1-4"/>',
    cpu: '<rect x="7" y="7" width="10" height="10" rx="2"/><path d="M9 1v3"/><path d="M15 1v3"/><path d="M9 20v3"/><path d="M15 20v3"/><path d="M20 9h3"/><path d="M20 15h3"/><path d="M1 9h3"/><path d="M1 15h3"/><rect x="10" y="10" width="4" height="4" rx="1"/>',
    brain: '<path d="M9.5 4.5a3 3 0 0 0-4.7 3.1 3.3 3.3 0 0 0 .3 6.1A3 3 0 0 0 8 19h1.5V4.5z"/><path d="M14.5 4.5a3 3 0 0 1 4.7 3.1 3.3 3.3 0 0 1-.3 6.1A3 3 0 0 1 16 19h-1.5V4.5z"/><path d="M9.5 8H7.8"/><path d="M14.5 8h1.7"/><path d="M9.5 13H7.6"/><path d="M14.5 13h1.9"/>',
    keyboard: '<rect x="2" y="6" width="20" height="12" rx="2"/><line x1="6" y1="10" x2="6" y2="10"/><line x1="10" y1="10" x2="10" y2="10"/><line x1="14" y1="10" x2="14" y2="10"/><line x1="18" y1="10" x2="18" y2="10"/><line x1="6" y1="14" x2="6" y2="14"/><line x1="18" y1="14" x2="18" y2="14"/><line x1="9" y1="14" x2="15" y2="14"/>',
    terminal: '<polyline points="4 7 9 12 4 17"/><line x1="12" y1="17" x2="20" y2="17"/>',
    chat: '<path d="M21 12a8 8 0 0 1-12.9 6.3L3 20l1.7-5.1A8 8 0 1 1 21 12z"/>',
    folder: '<path d="M3 7a2 2 0 0 1 2-2h4l2 2h8a2 2 0 0 1 2 2v8a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/>',
    trash: '<polyline points="3 6 5 6 21 6"/><path d="M19 6l-1 13a2 2 0 0 1-2 2H8a2 2 0 0 1-2-2L5 6"/><path d="M10 11v6"/><path d="M14 11v6"/><path d="M9 6V4a1 1 0 0 1 1-1h4a1 1 0 0 1 1 1v2"/>',
    chevronDown: '<polyline points="6 9 12 15 18 9"/>',
    bell: '<path d="M18 16v-5a6 6 0 1 0-12 0v5l-2 2h16z"/><path d="M10 21a2 2 0 0 0 4 0"/>',
    music: '<path d="M9 18V5l12-2v13"/><circle cx="6" cy="18" r="3"/><circle cx="18" cy="16" r="3"/>',
    vibrate: '<rect x="9" y="4" width="6" height="16" rx="1"/><path d="M5 8v8"/><path d="M3 10v4"/><path d="M19 8v8"/><path d="M21 10v4"/>',
    globe: '<circle cx="12" cy="12" r="9"/><line x1="3" y1="12" x2="21" y2="12"/><path d="M12 3a14 14 0 0 1 0 18"/><path d="M12 3a14 14 0 0 0 0 18"/>',
    smartphone: '<rect x="6" y="2" width="12" height="20" rx="2.5"/><line x1="11" y1="18" x2="13" y2="18"/>',
    desktop: '<rect x="3" y="4" width="18" height="12" rx="2"/><line x1="8" y1="20" x2="16" y2="20"/><line x1="12" y1="16" x2="12" y2="20"/>',
    link: '<path d="M10 14a4.5 4.5 0 0 0 6.36 0l3-3a4.5 4.5 0 1 0-6.36-6.36l-1.42 1.41"/><path d="M14 10a4.5 4.5 0 0 0-6.36 0l-3 3a4.5 4.5 0 1 0 6.36 6.36l1.42-1.41"/>',
    palette: '<circle cx="13.5" cy="6.5" r="1"/><circle cx="17.5" cy="10.5" r="1"/><circle cx="8.5" cy="7.5" r="1"/><circle cx="6.5" cy="12.5" r="1"/><path d="M12 3a9 9 0 1 0 0 18 1.5 1.5 0 0 0 1.1-2.5 1.5 1.5 0 0 1 1.1-2.5h2.3A4.5 4.5 0 0 0 21 11.5C21 6.8 16.97 3 12 3z"/>',
    play: '<polygon points="6 4 20 12 6 20 6 4"/>',
    wrench: '<path d="M14.7 6.3a4 4 0 1 1 4 4l-9 9-3.5 1 1-3.5 7.5-7.5z"/>',
    paw: '<circle cx="7.5" cy="9" r="2" fill="currentColor" stroke="none"/><circle cx="12" cy="6.8" r="2" fill="currentColor" stroke="none"/><circle cx="16.5" cy="9" r="2" fill="currentColor" stroke="none"/><circle cx="18" cy="13.3" r="1.8" fill="currentColor" stroke="none"/><path d="M7.2 16.3c.5-2.9 2.3-4.8 4.8-4.8s4.3 1.9 4.8 4.8c.3 1.8-.9 3.2-2.6 2.6-.8-.3-1.4-.6-2.2-.6s-1.4.3-2.2.6c-1.7.6-2.9-.8-2.6-2.6z" fill="currentColor" stroke="none"/>',
    edit: '<path d="M12 20h9"/><path d="M16.5 3.5a2.12 2.12 0 0 1 3 3L7 19l-4 1 1-4z"/>',
    signal: '<path d="M2 12a15 15 0 0 1 20 0"/><path d="M5 16a10 10 0 0 1 14 0"/><path d="M9 20a4 4 0 0 1 6 0"/><circle cx="12" cy="20" r="0.5" fill="currentColor"/>',
    file: '<path d="M14 3H6a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h12a2 2 0 0 0 2-2V9z"/><polyline points="14 3 14 9 20 9"/>',
    sigma: '<polyline points="18 4 6 4 13 12 6 20 18 20"/>',
    x: '<path d="M18 6 6 18"/><path d="M6 6l12 12"/>',
    // 「+」：附件入口（替代旧曲别针图标），更直观、与微信/iMessage 习惯一致。
    plus: '<line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/>',
    // 麦克风：语音输入入口。stroke 线性风格与项目其他图标统一。
    mic: '<rect x="9" y="3" width="6" height="11" rx="3"/><path d="M5 11a7 7 0 0 0 14 0"/><line x1="12" y1="18" x2="12" y2="21"/><line x1="9" y1="21" x2="15" y2="21"/>',
    // 曲别针：加号 popover 内"上传附件"项的图标（+ 入口已被外层占用，这里就用回曲别针）。
    paperclip: '<path d="M21.44 11.05 12.25 20.24a6 6 0 0 1-8.49-8.49l8.84-8.84a4 4 0 1 1 5.66 5.66L9.41 17.41a2 2 0 1 1-2.83-2.83l8.84-8.83"/>'
  };
  function iconSvg(name, opts) {
    var path = ICON_PATHS[name];
    if (!path) return "";
    opts = opts || {};
    var size4 = opts.size || 14;
    var stroke = opts.strokeWidth || 1.8;
    var cls = opts.cls ? ' class="' + opts.cls + '"' : "";
    var fill = opts.fill || "none";
    return "<svg" + cls + ' width="' + size4 + '" height="' + size4 + '" viewBox="0 0 24 24" fill="' + fill + '" stroke="currentColor" stroke-width="' + stroke + '" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">' + path + "</svg>";
  }

  // src/web-ui/browser/message-reconciliation.ts
  function turnContentVolume(turn) {
    if (!turn || !Array.isArray(turn.content)) return 0;
    var total = 0;
    for (var i = 0; i < turn.content.length; i++) {
      var block = turn.content[i];
      if (!block) continue;
      if (typeof block.text === "string") total += block.text.length;
      if (typeof block.thinking === "string") total += block.thinking.length;
      if (typeof block.content === "string") total += block.content.length;
      else if (Array.isArray(block.content)) {
        for (var k = 0; k < block.content.length; k++) {
          var nestedBlock = block.content[k];
          if (nestedBlock && typeof nestedBlock.text === "string") total += nestedBlock.text.length;
        }
      }
      if (block.input) {
        try {
          total += JSON.stringify(block.input).length;
        } catch (_error) {
        }
      }
    }
    return total;
  }
  function mergeAssistantTurn(localTurn, incomingTurn) {
    if (!localTurn) return incomingTurn;
    if (!incomingTurn) return localTurn;
    if (turnContentVolume(incomingTurn) >= turnContentVolume(localTurn)) return incomingTurn;
    return Object.assign({}, localTurn, {
      usage: incomingTurn.usage || localTurn.usage
    });
  }
  function mergeOverlappingTurns(localTurn, incomingTurn) {
    if (localTurn?.role === "assistant" && incomingTurn?.role === "assistant") {
      return mergeAssistantTurn(localTurn, incomingTurn);
    }
    return incomingTurn || localTurn;
  }
  function mergeWindowedMessages(prev, incoming, offset4, total) {
    var snapOffset = offset4 || 0;
    var snapTotal = typeof total === "number" ? total : Math.max(snapOffset + incoming.length, incoming.length);
    var prevMsgs = prev && Array.isArray(prev.messages) ? prev.messages : [];
    var prevOffset = prev && typeof prev.messageOffset === "number" ? prev.messageOffset : 0;
    var prevTotal = prev && typeof prev.messageTotal === "number" ? prev.messageTotal : prevOffset + prevMsgs.length;
    if (incoming.length === 0 && prevMsgs.length > 0 && snapTotal === 0) {
      return { messages: prevMsgs, messageOffset: prevOffset, messageTotal: prevTotal };
    }
    if (prevMsgs.length > 0 && snapTotal < prevTotal) {
      return { messages: prevMsgs, messageOffset: prevOffset, messageTotal: prevTotal };
    }
    if (prevMsgs.length === 0) {
      return { messages: incoming, messageOffset: snapOffset, messageTotal: snapTotal };
    }
    var prevEnd = prevOffset + prevMsgs.length;
    var snapEnd = snapOffset + incoming.length;
    if (snapOffset > prevEnd || prevOffset > snapEnd) {
      return { messages: incoming, messageOffset: snapOffset, messageTotal: snapTotal };
    }
    var mergedOffset = Math.min(prevOffset, snapOffset);
    var mergedEnd = Math.max(prevEnd, snapEnd);
    var merged = [];
    for (var absoluteIndex = mergedOffset; absoluteIndex < mergedEnd; absoluteIndex++) {
      var localTurn = absoluteIndex >= prevOffset && absoluteIndex < prevEnd ? prevMsgs[absoluteIndex - prevOffset] : void 0;
      var incomingTurn = absoluteIndex >= snapOffset && absoluteIndex < snapEnd ? incoming[absoluteIndex - snapOffset] : void 0;
      merged.push(mergeOverlappingTurns(localTurn, incomingTurn));
    }
    return {
      messages: merged,
      messageOffset: mergedOffset,
      messageTotal: Math.max(prevTotal, snapTotal, mergedOffset + merged.length)
    };
  }

  // src/web-ui/browser/chat-scroll.ts
  function getChatScrollElement() {
    var chatOutput = document.getElementById("chat-output");
    if (!chatOutput) {
      state.chatScrollElement = null;
      return null;
    }
    var chatMessages = chatOutput.querySelector(".chat-messages");
    if (chatMessages) {
      state.chatScrollElement = chatMessages;
      return chatMessages;
    }
    state.chatScrollElement = null;
    return null;
  }
  function isChatNearBottom(chatMsgs) {
    var el = chatMsgs || getChatScrollElement();
    if (!el) return true;
    return Math.abs(el.scrollTop) < state.chatScrollThreshold;
  }
  function clearChatUnread(options) {
    options = options || {};
    var hadUnread = state.chatUnreadCount > 0 || state.chatUnreadStartIndex >= 0;
    state.chatUnreadCount = 0;
    state.chatUnreadStartIndex = -1;
    if (options.removeDivider !== false) {
      var chatMsgs = getChatScrollElement();
      if (chatMsgs) {
        var divider = chatMsgs.querySelector(".chat-unread-divider");
        if (divider && divider.parentNode) divider.parentNode.removeChild(divider);
      }
    }
    if (hadUnread) updateChatUnreadBubble();
  }
  function refreshChatUnreadDivider(chatMessages) {
    if (!chatMessages) chatMessages = getChatScrollElement();
    if (!chatMessages) return;
    var existing = chatMessages.querySelector(".chat-unread-divider");
    if (state.chatUnreadStartIndex < 0 || state.chatUnreadCount <= 0) {
      if (existing && existing.parentNode) existing.parentNode.removeChild(existing);
      return;
    }
    var startIdx = state.chatUnreadStartIndex;
    var nodes = chatMessages.querySelectorAll(".chat-message");
    var boundary = null;
    for (var i = 0; i < nodes.length; i++) {
      var idxAttr = nodes[i].getAttribute("data-msg-index");
      if (idxAttr === null) continue;
      var idx = parseInt(idxAttr, 10);
      if (!isNaN(idx) && idx < startIdx) {
        boundary = nodes[i];
        break;
      }
    }
    var label = state.chatUnreadCount + " \u6761\u65B0\u6D88\u606F";
    if (!existing) {
      existing = document.createElement("div");
      existing.className = "chat-unread-divider";
      existing.setAttribute("role", "separator");
      existing.innerHTML = '<span class="chat-unread-divider-line"></span><span class="chat-unread-divider-label"></span><span class="chat-unread-divider-line"></span>';
    }
    existing.querySelector(".chat-unread-divider-label").textContent = label;
    if (boundary) {
      if (existing.nextSibling !== boundary || existing.parentNode !== chatMessages) {
        chatMessages.insertBefore(existing, boundary);
      }
    } else {
      if (existing.parentNode !== chatMessages || existing.nextSibling !== null) {
        chatMessages.appendChild(existing);
      }
    }
  }
  function updateChatUnreadBubble() {
    var bubble = document.getElementById("chat-unread-bubble");
    if (!bubble) return;
    var selectedSession = state.sessions.find(function(s) {
      return s.id === state.selectedId;
    });
    var notAtBottom = !isChatNearBottom();
    var shouldShow = !!selectedSession && state.currentView === "chat" && notAtBottom;
    bubble.classList.toggle("visible", shouldShow);
    bubble.classList.toggle("has-unread", state.chatUnreadCount > 0);
    var countEl = bubble.querySelector(".chat-unread-bubble-count");
    if (countEl) {
      if (state.chatUnreadCount > 0) {
        countEl.textContent = state.chatUnreadCount > 99 ? "99+" : String(state.chatUnreadCount);
        countEl.classList.add("visible");
      } else {
        countEl.textContent = "";
        countEl.classList.remove("visible");
      }
    }
    var label = state.chatUnreadCount > 0 ? state.chatUnreadCount + " \u6761\u65B0\u6D88\u606F\uFF0C\u70B9\u51FB\u67E5\u770B" : "\u56DE\u5230\u6700\u65B0\u6D88\u606F";
    bubble.setAttribute("aria-label", label);
    bubble.setAttribute("title", label);
    var chatContainer = document.getElementById("chat-output");
    if (chatContainer) chatContainer.classList.toggle("has-jump-btn", shouldShow);
  }
  function releaseChatTurnPin() {
    removeChatPinSpacer();
  }
  function removeChatPinSpacer(chatMsgs) {
    var el = chatMsgs || getChatScrollElement();
    if (!el) return;
    var spacer = el.querySelector(".chat-pin-spacer");
    if (spacer && spacer.parentNode) spacer.parentNode.removeChild(spacer);
  }
  function scrollChatToBottom(smooth) {
    var chatMsgs = getChatScrollElement();
    if (!chatMsgs || !chatMsgs.isConnected) return;
    releaseChatTurnPin();
    removeChatPinSpacer(chatMsgs);
    state.chatIsProgrammaticScroll = true;
    var done = function() {
      state.chatIsProgrammaticScroll = false;
      state.chatStickToBottom = true;
      clearChatUnread({ removeDivider: true });
      updateChatUnreadBubble();
    };
    if (smooth && typeof chatMsgs.scrollTo === "function") {
      chatMsgs.scrollTo({ top: 0, behavior: "smooth" });
      setTimeout(done, 260);
      return;
    }
    chatMsgs.scrollTop = 0;
    requestAnimationFrame(done);
  }
  function prepareChatBottomFollow() {
    var chatMsgs = getChatScrollElement();
    releaseChatTurnPin();
    if (chatMsgs) removeChatPinSpacer(chatMsgs);
    state.chatStickToBottom = true;
    clearChatUnread({ removeDivider: true });
    if (chatMsgs && chatMsgs.isConnected) {
      state.chatIsProgrammaticScroll = true;
      state.chatProgrammaticScrollUntil = Date.now() + 180;
      chatMsgs.scrollTop = 0;
      requestAnimationFrame(function() {
        state.chatIsProgrammaticScroll = false;
      });
    }
    updateChatUnreadBubble();
  }
  function bindChatScrollListener() {
    var chatMsgs = getChatScrollElement();
    if (!chatMsgs || !chatMsgs.isConnected) return;
    if (state.chatScrollElement === chatMsgs && state.chatScrollHandler) {
      updateChatUnreadBubble();
      return;
    }
    if (state.chatScrollElement) {
      if (state.chatScrollHandler) {
        state.chatScrollElement.removeEventListener("scroll", state.chatScrollHandler);
      }
      if (state.chatScrollWheelHandler) {
        state.chatScrollElement.removeEventListener("wheel", state.chatScrollWheelHandler);
      }
      if (state.chatScrollTouchStartHandler) {
        state.chatScrollElement.removeEventListener("touchstart", state.chatScrollTouchStartHandler);
      }
      if (state.chatScrollTouchMoveHandler) {
        state.chatScrollElement.removeEventListener("touchmove", state.chatScrollTouchMoveHandler);
      }
    }
    state.chatScrollElement = chatMsgs;
    function handleManualHistoryScroll() {
      releaseChatTurnPin();
      state.chatStickToBottom = false;
      if (chatMsgs.querySelector(".chat-history-summary")) {
        renderChat(true);
      }
      updateChatUnreadBubble();
    }
    state.chatScrollHandler = function() {
      if (!chatMsgs.isConnected) return;
      if (state.chatIsProgrammaticScroll || Date.now() < state.chatProgrammaticScrollUntil) {
        updateChatUnreadBubble();
        return;
      }
      var atBottom = isChatNearBottom(chatMsgs);
      if (atBottom) {
        releaseChatTurnPin();
        state.chatStickToBottom = true;
        clearChatUnread({ removeDivider: true });
      } else {
        handleManualHistoryScroll();
        return;
      }
      updateChatUnreadBubble();
    };
    state.chatScrollWheelHandler = function(e) {
      if (state.chatIsProgrammaticScroll) return;
      if (e.deltaY < 0) {
        handleManualHistoryScroll();
      }
    };
    state.chatScrollTouchStartHandler = function(e) {
      if (!e.touches || e.touches.length === 0) return;
      state.chatTouchStartY = e.touches[0].clientY;
    };
    state.chatScrollTouchMoveHandler = function(e) {
      if (state.chatIsProgrammaticScroll) return;
      if (!e.touches || e.touches.length === 0) return;
      var deltaY = e.touches[0].clientY - state.chatTouchStartY;
      if (deltaY > 4) {
        handleManualHistoryScroll();
      }
    };
    chatMsgs.addEventListener("scroll", state.chatScrollHandler, { passive: true });
    chatMsgs.addEventListener("wheel", state.chatScrollWheelHandler, { passive: true });
    chatMsgs.addEventListener("touchstart", state.chatScrollTouchStartHandler, { passive: true });
    chatMsgs.addEventListener("touchmove", state.chatScrollTouchMoveHandler, { passive: true });
    updateChatUnreadBubble();
  }
  function loadMoreChatMessages() {
    if (state.chatRenderedCount < state.currentMessages.length) {
      state.chatRenderedCount += state.chatPageSize;
      renderChat(true);
      return;
    }
    var sess = state.sessions.find(function(s) {
      return s.id === state.selectedId;
    });
    if (sess && typeof sess.messageOffset === "number" && sess.messageOffset > 0) {
      fetchEarlierMessages();
    }
  }
  var _loadMoreObserver = null;
  function observeLoadMoreSentinel() {
    if (_loadMoreObserver) {
      _loadMoreObserver.disconnect();
      _loadMoreObserver = null;
    }
    var sentinel = document.getElementById("chat-load-more-sentinel");
    if (!sentinel) return;
    var btn = sentinel.querySelector(".chat-load-more-btn");
    if (btn) btn.onclick = function() {
      loadMoreChatMessages();
    };
    var coarsePointer = !!(window.matchMedia && window.matchMedia("(pointer: coarse)").matches);
    var mobileViewport = window.innerWidth <= 768;
    if (coarsePointer || mobileViewport || window.__wandImeNative || window.__wandIosNative) return;
    if (typeof IntersectionObserver === "undefined") return;
    _loadMoreObserver = new IntersectionObserver(function(entries) {
      for (var i = 0; i < entries.length; i++) {
        if (entries[i].isIntersecting) {
          loadMoreChatMessages();
          break;
        }
      }
    }, { root: getChatScrollElement(), rootMargin: "200px" });
    _loadMoreObserver.observe(sentinel);
  }
  function persistSelectedId() {
    try {
      if (state.selectedId) {
        localStorage.setItem("wand-selected-session", state.selectedId);
      } else {
        localStorage.removeItem("wand-selected-session");
      }
    } catch (e) {
    }
  }
  function getStructuredQueuedInputs(session) {
    if (session && Array.isArray(session.queuedMessages)) {
      return session.queuedMessages;
    }
    return state.structuredInputQueue;
  }
  function getSelectedStructuredQueuedInputs() {
    var session = state.sessions.find(function(s) {
      return s.id === state.selectedId;
    });
    return getStructuredQueuedInputs(session);
  }
  function syncStructuredQueueFromSession(session) {
    var queued = getStructuredQueuedInputs(session);
    state.structuredInputQueue = Array.isArray(queued) ? queued.slice() : [];
  }
  function hasRenderOnlyStructuredBlock(message, marker) {
    return !!(message && Array.isArray(message.content) && message.content.some(function(block) {
      return block && typeof block === "object" && block[marker];
    }));
  }
  function isQueuedStructuredMessage(message) {
    return !!(message && message.role === "user" && hasRenderOnlyStructuredBlock(message, "__queued"));
  }
  function isProcessingStructuredMessage(message) {
    return !!(message && message.role === "assistant" && hasRenderOnlyStructuredBlock(message, "__processing"));
  }
  function stripRenderOnlyStructuredMessages(messages) {
    if (!Array.isArray(messages)) return [];
    var removed = false;
    var filtered = [];
    for (var i = 0; i < messages.length; i++) {
      var message = messages[i];
      if (isQueuedStructuredMessage(message) || isProcessingStructuredMessage(message)) {
        removed = true;
        continue;
      }
      filtered.push(message);
    }
    return removed ? filtered : messages;
  }
  function normalizeStructuredSnapshot(snapshot7, existingSession) {
    if (!snapshot7 || !Array.isArray(snapshot7.messages)) {
      return snapshot7;
    }
    var sessionKind = snapshot7.sessionKind || existingSession && existingSession.sessionKind;
    if (sessionKind !== "structured") {
      return snapshot7;
    }
    var sanitizedMessages = stripRenderOnlyStructuredMessages(snapshot7.messages);
    if (sanitizedMessages === snapshot7.messages) {
      return snapshot7;
    }
    return Object.assign({}, snapshot7, { messages: sanitizedMessages });
  }
  function saveStructuredQueue() {
    try {
      var queued = getSelectedStructuredQueuedInputs();
      if (!state.selectedId || queued.length === 0) {
        return;
      }
      localStorage.setItem("wand-structured-queue", JSON.stringify({
        sessionId: state.selectedId,
        items: queued
      }));
    } catch (e) {
    }
  }
  function clearStructuredQueuePersistence(sessionId) {
    try {
      var saved = localStorage.getItem("wand-structured-queue");
      if (!saved) return;
      var parsed = JSON.parse(saved);
      if (!sessionId || !parsed || parsed.sessionId === sessionId) {
        localStorage.removeItem("wand-structured-queue");
      }
    } catch (e) {
      localStorage.removeItem("wand-structured-queue");
    }
  }
  function restoreStructuredQueue() {
    var selectedSession = state.sessions.find(function(s) {
      return s.id === state.selectedId;
    });
    if (selectedSession && Array.isArray(selectedSession.queuedMessages)) {
      syncStructuredQueueFromSession(selectedSession);
      saveStructuredQueue();
      return;
    }
    try {
      var saved = localStorage.getItem("wand-structured-queue");
      if (!saved) return;
      var parsed = JSON.parse(saved);
      if (!parsed || parsed.sessionId !== state.selectedId || !Array.isArray(parsed.items)) {
        return;
      }
      state.structuredInputQueue = parsed.items.slice(0, 10);
    } catch (e) {
      state.structuredInputQueue = [];
    }
  }
  function persistCrossSessionQueue() {
    try {
      if (state.crossSessionQueue.length === 0) {
        localStorage.removeItem("wand-cross-session-queue");
        return;
      }
      localStorage.setItem("wand-cross-session-queue", JSON.stringify(state.crossSessionQueue));
    } catch (e) {
    }
  }
  function getConfigCwd() {
    return state.config && state.config.defaultCwd || "/tmp";
  }
  function loadChatExpandStateMap() {
    try {
      var saved = localStorage.getItem(CHAT_EXPAND_STATE_STORAGE_KEY);
      if (!saved) return {};
      var parsed = JSON.parse(saved);
      return parsed && typeof parsed === "object" ? parsed : {};
    } catch (e) {
      return {};
    }
  }
  function saveChatExpandStateMap(map) {
    try {
      if (!map || Object.keys(map).length === 0) {
        localStorage.removeItem(CHAT_EXPAND_STATE_STORAGE_KEY);
        return;
      }
      localStorage.setItem(CHAT_EXPAND_STATE_STORAGE_KEY, JSON.stringify(map));
    } catch (e) {
    }
  }
  function getCurrentChatExpandState() {
    var sessionId = state.selectedId;
    if (!sessionId) return {};
    var map = loadChatExpandStateMap();
    var sessionState = map[sessionId];
    return sessionState && typeof sessionState === "object" ? sessionState : {};
  }
  function getPersistedExpandState(itemKey) {
    if (!itemKey || !state.selectedId) return null;
    var sessionState = getCurrentChatExpandState();
    return typeof sessionState[itemKey] === "boolean" ? sessionState[itemKey] : null;
  }
  function setPersistedExpandState(itemKey, expanded) {
    if (!itemKey || !state.selectedId) return;
    var map = loadChatExpandStateMap();
    var sessionId = state.selectedId;
    var sessionState = map[sessionId];
    if (!sessionState || typeof sessionState !== "object") {
      sessionState = {};
    }
    sessionState[itemKey] = !!expanded;
    map[sessionId] = sessionState;
    saveChatExpandStateMap(map);
  }
  function getMessageKey(msg, fallbackIndex) {
    if (!msg) {
      return "msg:unknown-" + (typeof fallbackIndex === "number" ? fallbackIndex : 0);
    }
    if (msg.uuid) return "msg:" + msg.uuid;
    if (msg.id) return "msg:" + msg.id;
    if (msg.messageId) return "msg:" + msg.messageId;
    if (msg.turnId) return "msg:" + msg.turnId;
    return "msg:" + (typeof fallbackIndex === "number" ? fallbackIndex : 0);
  }
  function buildExpandKey(kind, parts) {
    var filtered = [];
    for (var i = 0; i < parts.length; i++) {
      var part = parts[i];
      if (part === void 0 || part === null || part === "") continue;
      filtered.push(String(part));
    }
    return kind + ":" + filtered.join(":");
  }
  function getElementExpandKey(el) {
    if (!el || !el.dataset) return "";
    return el.dataset.expandKey || "";
  }
  function isElementExpanded(el, kind) {
    if (!el) return false;
    switch (kind) {
      case "tool-card":
      case "diff":
        return !el.classList.contains("collapsed");
      case "thinking":
        return el.classList.contains("expanded") && !el.classList.contains("collapsed");
      case "inline-tool":
        return el.classList.contains("inline-tool-open");
      case "terminal": {
        var body = el.querySelector(".term-body");
        if (body) return body.style.display !== "none";
        return el.dataset.expanded === "true";
      }
      case "tool-group":
        return el.getAttribute("data-expanded") === "true";
      case "subagent-reply":
        return el.getAttribute("data-expanded") === "true";
      case "subagent-panel":
        return el.getAttribute("data-expanded") === "true";
      default:
        return false;
    }
  }
  function applyExpandedState(el, kind, expanded) {
    if (!el) return;
    switch (kind) {
      case "tool-card":
      case "diff": {
        el.classList.toggle("collapsed", !expanded);
        break;
      }
      case "thinking": {
        el.classList.toggle("collapsed", !expanded);
        el.classList.toggle("expanded", !!expanded);
        var previewEl = el.querySelector(".thinking-inline-preview");
        if (previewEl) {
          var fullText = el.dataset.thinking || "";
          var preview = fullText.slice(0, 57) + (fullText.length > 60 ? "\u2026" : "");
          previewEl.textContent = expanded ? fullText : preview;
        }
        var actionEl = el.querySelector(".thinking-inline-action");
        if (actionEl) actionEl.textContent = expanded ? "\u6536\u8D77" : "\u5C55\u5F00";
        break;
      }
      case "inline-tool": {
        el.classList.toggle("inline-tool-open", !!expanded);
        var inlineBody = el.querySelector(".inline-tool-expanded");
        if (inlineBody) inlineBody.style.display = expanded ? "block" : "none";
        break;
      }
      case "terminal": {
        var body = el.querySelector(".term-body");
        if (body) body.style.display = expanded ? "block" : "none";
        el.dataset.expanded = expanded ? "true" : "false";
        var toggleIcon = el.querySelector(".term-toggle-icon");
        if (toggleIcon) toggleIcon.textContent = expanded ? "\u25BC" : "\u25B6";
        break;
      }
      case "tool-group": {
        el.setAttribute("data-expanded", expanded ? "true" : "false");
        var groupBody = el.querySelector(".tool-group-body");
        if (groupBody) groupBody.style.display = expanded ? "block" : "none";
        var chevron = el.querySelector(".tool-group-chevron");
        if (chevron) chevron.style.transform = expanded ? "rotate(180deg)" : "";
        break;
      }
      case "subagent-reply": {
        el.setAttribute("data-expanded", expanded ? "true" : "false");
        var subLabel = el.querySelector(".subagent-reply-toggle-label");
        if (subLabel) subLabel.textContent = expanded ? "\u6536\u8D77" : "\u5C55\u5F00";
        var subToggleBtn = el.querySelector(".subagent-reply-toggle");
        if (subToggleBtn) {
          subToggleBtn.setAttribute("aria-expanded", expanded ? "true" : "false");
          subToggleBtn.setAttribute("aria-label", expanded ? "\u6536\u8D77\u5B50\u4EE3\u7406\u56DE\u590D" : "\u5C55\u5F00\u5B50\u4EE3\u7406\u56DE\u590D\u5168\u6587");
        }
        break;
      }
      case "subagent-panel": {
        el.setAttribute("data-expanded", expanded ? "true" : "false");
        var panelBtns = el.querySelectorAll(".subagent-panel-toggle");
        for (var pbi = 0; pbi < panelBtns.length; pbi++) {
          var pb = panelBtns[pbi];
          pb.setAttribute("aria-expanded", expanded ? "true" : "false");
          pb.setAttribute("aria-label", expanded ? "\u6536\u8D77\u5B50\u4EE3\u7406\u8F93\u51FA" : "\u5C55\u5F00\u5B50\u4EE3\u7406\u8F93\u51FA");
          var pblbl = pb.querySelector(".subagent-panel-toggle-label");
          if (pblbl) pblbl.textContent = expanded ? "\u6536\u8D77" : "\u5C55\u5F00";
        }
        var pbody = el.querySelector(".subagent-panel-body");
        if (pbody) {
          if (expanded) {
            pbody.scrollTop = 0;
          } else {
            pbody.scrollTop = pbody.scrollHeight;
          }
        }
        break;
      }
    }
  }
  function persistElementExpandState(el, kind) {
    var itemKey = getElementExpandKey(el);
    if (!itemKey) return;
    setPersistedExpandState(itemKey, isElementExpanded(el, kind));
  }
  function applyPersistedExpandState(container) {
    if (!container || !state.selectedId) return;
    container.querySelectorAll("[data-expand-key]").forEach(function(el) {
      var itemKey = getElementExpandKey(el);
      var kind = el.dataset.expandKind || "";
      var persisted = getPersistedExpandState(itemKey);
      if (persisted === null || !kind) return;
      applyExpandedState(el, kind, persisted);
    });
  }

  // src/web-ui/browser/shell-runtime.ts
  var React41 = __toESM(require_react(), 1);
  var import_react_dom3 = __toESM(require_react_dom(), 1);
  var import_client2 = __toESM(require_client(), 1);

  // src/web-ui/react/index.tsx
  var import_client = __toESM(require_client(), 1);

  // src/web-ui/react/feature-flags.ts
  var REACT_UI_STORAGE_KEY = "wand.reactUi.enabled";
  var REACT_SHELL_STORAGE_KEY = "wand.reactShell.enabled";
  function readBoolean(value) {
    if (value == null || value === "") return null;
    const normalized = value.trim().toLowerCase();
    if (["1", "true", "on", "enabled"].includes(normalized)) return true;
    if (["0", "false", "off", "disabled"].includes(normalized)) return false;
    return null;
  }
  function isReactUiEnabled(target = window) {
    try {
      const queryValue = readBoolean(new URL(target.location.href).searchParams.get("reactUi"));
      if (queryValue != null) return queryValue;
    } catch {
    }
    const configured = target.__wandFeatureFlags?.reactUi;
    if (typeof configured === "boolean") return configured;
    try {
      const stored = readBoolean(target.localStorage.getItem(REACT_UI_STORAGE_KEY));
      if (stored != null) return stored;
    } catch {
    }
    return true;
  }
  function isReactShellEnabled(target = window) {
    if (!isReactUiEnabled(target)) return false;
    try {
      const queryValue = readBoolean(new URL(target.location.href).searchParams.get("reactShell"));
      if (queryValue != null) return queryValue;
    } catch {
    }
    const configured = target.__wandFeatureFlags?.reactShell;
    if (typeof configured === "boolean") return configured;
    try {
      const stored = readBoolean(target.localStorage.getItem(REACT_SHELL_STORAGE_KEY));
      if (stored != null) return stored;
    } catch {
    }
    return true;
  }

  // src/web-ui/react/overlay-host.tsx
  var import_react16 = __toESM(require_react(), 1);

  // src/web-ui/react/overlay-controller.tsx
  var nextId = 0;
  var dialogQueue = [];
  var toasts = [];
  var snapshot = { activeDialog: null, toasts: [] };
  var listeners = /* @__PURE__ */ new Set();
  function publish() {
    snapshot = {
      activeDialog: dialogQueue[0] ?? null,
      toasts: [...toasts]
    };
    for (const listener of listeners) listener();
  }
  function dismissToast(id) {
    const entry = toasts.find((toast) => toast.id === id);
    if (!entry || !entry.open) return;
    toasts = toasts.map((toast) => toast.id === id ? { ...toast, open: false } : toast);
    publish();
    window.setTimeout(() => {
      toasts = toasts.filter((toast) => toast.id !== id);
      publish();
    }, 180);
  }
  var wandOverlay = {
    dialog(options) {
      const id = ++nextId;
      return new Promise((resolve) => {
        dialogQueue = [
          ...dialogQueue,
          {
            id,
            options,
            resolve
          }
        ];
        publish();
      });
    },
    toast(message, options = {}) {
      const id = ++nextId;
      toasts = [...toasts, { id, message, options, open: true }];
      publish();
      return { id, dismiss: () => dismissToast(id) };
    },
    closeTopmost() {
      const entry = dialogQueue[0];
      if (!entry || entry.options.dismissable === false) return false;
      overlayStore.completeDialog(entry.id, { dismissed: true });
      return true;
    }
  };
  var overlayStore = {
    subscribe(listener) {
      listeners.add(listener);
      return () => listeners.delete(listener);
    },
    getSnapshot() {
      return snapshot;
    },
    completeDialog(id, result) {
      const entry = dialogQueue.find((dialog) => dialog.id === id);
      if (!entry) return;
      dialogQueue = dialogQueue.filter((dialog) => dialog.id !== id);
      publish();
      entry.resolve(result);
    },
    dismissToast
  };

  // src/web-ui/react/ui/button.tsx
  var import_react = __toESM(require_react(), 1);

  // src/web-ui/react/ui/class-names.ts
  function classNames(...values) {
    return values.filter(Boolean).join(" ");
  }

  // src/web-ui/react/ui/button.tsx
  var import_jsx_runtime = __toESM(require_jsx_runtime(), 1);
  var WandButton = (0, import_react.forwardRef)(function WandButton2({ className, kind = "secondary", size: size4 = "medium", type = "button", ...props }, ref) {
    return /* @__PURE__ */ (0, import_jsx_runtime.jsx)(
      "button",
      {
        ...props,
        ref,
        type,
        className: classNames(
          "wand-ui-button",
          `wand-ui-button-${kind}`,
          size4 !== "medium" && `wand-ui-button-${size4}`,
          className
        )
      }
    );
  });

  // node_modules/@radix-ui/react-dialog/dist/index.mjs
  var React21 = __toESM(require_react(), 1);

  // node_modules/@radix-ui/primitive/dist/index.mjs
  var canUseDOM = !!(typeof window !== "undefined" && window.document && window.document.createElement);
  function composeEventHandlers(originalEventHandler, ourEventHandler, { checkForDefaultPrevented = true } = {}) {
    return function handleEvent(event) {
      originalEventHandler?.(event);
      if (checkForDefaultPrevented === false || !event || !event.defaultPrevented) {
        return ourEventHandler?.(event);
      }
    };
  }

  // node_modules/@radix-ui/react-compose-refs/dist/index.mjs
  var React = __toESM(require_react(), 1);
  function setRef(ref, value) {
    if (typeof ref === "function") {
      return ref(value);
    } else if (ref !== null && ref !== void 0) {
      ref.current = value;
    }
  }
  function composeRefs(...refs) {
    return (node) => {
      let hasCleanup = false;
      const cleanups = refs.map((ref) => {
        const cleanup = setRef(ref, node);
        if (!hasCleanup && typeof cleanup == "function") {
          hasCleanup = true;
        }
        return cleanup;
      });
      if (hasCleanup) {
        return () => {
          for (let i = 0; i < cleanups.length; i++) {
            const cleanup = cleanups[i];
            if (typeof cleanup == "function") {
              cleanup();
            } else {
              setRef(refs[i], null);
            }
          }
        };
      }
    };
  }
  function useComposedRefs(...refs) {
    return React.useCallback(composeRefs(...refs), refs);
  }

  // node_modules/@radix-ui/react-context/dist/index.mjs
  var React2 = __toESM(require_react(), 1);
  var import_jsx_runtime2 = __toESM(require_jsx_runtime(), 1);
  function createContextScope(scopeName, createContextScopeDeps = []) {
    let defaultContexts = [];
    function createContext32(rootComponentName, defaultContext) {
      const BaseContext = React2.createContext(defaultContext);
      BaseContext.displayName = rootComponentName + "Context";
      const index2 = defaultContexts.length;
      defaultContexts = [...defaultContexts, defaultContext];
      const Provider2 = (props) => {
        const { scope, children, ...context } = props;
        const Context = scope?.[scopeName]?.[index2] || BaseContext;
        const value = React2.useMemo(() => context, Object.values(context));
        return /* @__PURE__ */ (0, import_jsx_runtime2.jsx)(Context.Provider, { value, children });
      };
      Provider2.displayName = rootComponentName + "Provider";
      function useContext22(consumerName, scope, options = {}) {
        const { optional = false } = options;
        const Context = scope?.[scopeName]?.[index2] || BaseContext;
        const context = React2.useContext(Context);
        if (context) return context;
        if (defaultContext !== void 0) return defaultContext;
        if (optional) return void 0;
        throw new Error(`\`${consumerName}\` must be used within \`${rootComponentName}\``);
      }
      return [Provider2, useContext22];
    }
    const createScope = () => {
      const scopeContexts = defaultContexts.map((defaultContext) => {
        return React2.createContext(defaultContext);
      });
      return function useScope(scope) {
        const contexts = scope?.[scopeName] || scopeContexts;
        return React2.useMemo(
          () => ({ [`__scope${scopeName}`]: { ...scope, [scopeName]: contexts } }),
          [scope, contexts]
        );
      };
    };
    createScope.scopeName = scopeName;
    return [createContext32, composeContextScopes(createScope, ...createContextScopeDeps)];
  }
  function composeContextScopes(...scopes) {
    const baseScope = scopes[0];
    if (scopes.length === 1) return baseScope;
    const createScope = () => {
      const scopeHooks = scopes.map((createScope2) => ({
        useScope: createScope2(),
        scopeName: createScope2.scopeName
      }));
      return function useComposedScopes(overrideScopes) {
        const nextScopes = scopeHooks.reduce((nextScopes2, { useScope, scopeName }) => {
          const scopeProps = useScope(overrideScopes);
          const currentScope = scopeProps[`__scope${scopeName}`];
          return { ...nextScopes2, ...currentScope };
        }, {});
        return React2.useMemo(() => ({ [`__scope${baseScope.scopeName}`]: nextScopes }), [nextScopes]);
      };
    };
    createScope.scopeName = baseScope.scopeName;
    return createScope;
  }

  // node_modules/@radix-ui/react-id/dist/index.mjs
  var React4 = __toESM(require_react(), 1);

  // node_modules/@radix-ui/react-use-layout-effect/dist/index.mjs
  var React3 = __toESM(require_react(), 1);
  var useLayoutEffect2 = globalThis?.document ? React3.useLayoutEffect : () => {
  };

  // node_modules/@radix-ui/react-id/dist/index.mjs
  var useReactId = React4[" useId ".trim().toString()] || (() => void 0);
  var count = 0;
  function useId(deterministicId) {
    const [id, setId] = React4.useState(useReactId());
    useLayoutEffect2(() => {
      if (!deterministicId) setId((reactId) => reactId ?? String(count++));
    }, [deterministicId]);
    return deterministicId || (id ? `radix-${id}` : "");
  }

  // node_modules/@radix-ui/react-use-controllable-state/dist/index.mjs
  var React5 = __toESM(require_react(), 1);
  var React22 = __toESM(require_react(), 1);
  var useInsertionEffect = React5[" useInsertionEffect ".trim().toString()] || useLayoutEffect2;
  function useControllableState({
    prop,
    defaultProp,
    onChange = () => {
    },
    caller
  }) {
    const [uncontrolledProp, setUncontrolledProp, onChangeRef] = useUncontrolledState({
      defaultProp,
      onChange
    });
    const isControlled = prop !== void 0;
    const value = isControlled ? prop : uncontrolledProp;
    if (true) {
      const isControlledRef = React5.useRef(prop !== void 0);
      React5.useEffect(() => {
        const wasControlled = isControlledRef.current;
        if (wasControlled !== isControlled) {
          const from = wasControlled ? "controlled" : "uncontrolled";
          const to = isControlled ? "controlled" : "uncontrolled";
          console.warn(
            `${caller} is changing from ${from} to ${to}. Components should not switch from controlled to uncontrolled (or vice versa). Decide between using a controlled or uncontrolled value for the lifetime of the component.`
          );
        }
        isControlledRef.current = isControlled;
      }, [isControlled, caller]);
    }
    const setValue = React5.useCallback(
      (nextValue) => {
        if (isControlled) {
          const value2 = isFunction(nextValue) ? nextValue(prop) : nextValue;
          if (value2 !== prop) {
            onChangeRef.current?.(value2);
          }
        } else {
          setUncontrolledProp(nextValue);
        }
      },
      [isControlled, prop, setUncontrolledProp, onChangeRef]
    );
    return [value, setValue];
  }
  function useUncontrolledState({
    defaultProp,
    onChange
  }) {
    const [value, setValue] = React5.useState(defaultProp);
    const prevValueRef = React5.useRef(value);
    const onChangeRef = React5.useRef(onChange);
    useInsertionEffect(() => {
      onChangeRef.current = onChange;
    }, [onChange]);
    React5.useEffect(() => {
      if (prevValueRef.current !== value) {
        onChangeRef.current?.(value);
        prevValueRef.current = value;
      }
    }, [value, prevValueRef]);
    return [value, setValue, onChangeRef];
  }
  function isFunction(value) {
    return typeof value === "function";
  }

  // node_modules/@radix-ui/react-dismissable-layer/dist/index.mjs
  var React9 = __toESM(require_react(), 1);

  // node_modules/@radix-ui/react-primitive/dist/index.mjs
  var React7 = __toESM(require_react(), 1);
  var ReactDOM = __toESM(require_react_dom(), 1);

  // node_modules/@radix-ui/react-slot/dist/index.mjs
  var React6 = __toESM(require_react(), 1);
  // @__NO_SIDE_EFFECTS__
  function createSlot(ownerName) {
    const Slot22 = React6.forwardRef((props, forwardedRef) => {
      let { children, ...slotProps } = props;
      let slottableElement = null;
      let hasSlottable = false;
      const newChildren = [];
      if (isLazyComponent(children) && typeof use === "function") {
        children = use(children._payload);
      }
      React6.Children.forEach(children, (maybeSlottable) => {
        if (isSlottable(maybeSlottable)) {
          hasSlottable = true;
          const slottable = maybeSlottable;
          let child = "child" in slottable.props ? slottable.props.child : slottable.props.children;
          if (isLazyComponent(child) && typeof use === "function") {
            child = use(child._payload);
          }
          slottableElement = getSlottableElementFromSlottable(slottable, child);
          newChildren.push(slottableElement?.props?.children);
        } else {
          newChildren.push(maybeSlottable);
        }
      });
      if (slottableElement) {
        slottableElement = React6.cloneElement(slottableElement, void 0, newChildren);
      } else if (
        // A `Slottable` was found but it didn't resolve to a single element (e.g.
        // it wrapped multiple elements, text, or a render-prop `child` that
        // wasn't an element). Don't fall back to treating the `Slottable` wrapper
        // itself as the slot target — throw a descriptive error below instead.
        !hasSlottable && React6.Children.count(children) === 1 && React6.isValidElement(children)
      ) {
        slottableElement = children;
      }
      const slottableElementRef = slottableElement ? getElementRef(slottableElement) : void 0;
      const composedRef = useComposedRefs(forwardedRef, slottableElementRef);
      if (!slottableElement) {
        if (children || children === 0) {
          throw new Error(
            hasSlottable ? createSlottableError(ownerName) : createSlotError(ownerName)
          );
        }
        return children;
      }
      const mergedProps = mergeProps(slotProps, slottableElement.props ?? {});
      if (slottableElement.type !== React6.Fragment) {
        mergedProps.ref = forwardedRef ? composedRef : slottableElementRef;
      }
      return React6.cloneElement(slottableElement, mergedProps);
    });
    Slot22.displayName = `${ownerName}.Slot`;
    return Slot22;
  }
  var SLOTTABLE_IDENTIFIER = /* @__PURE__ */ Symbol.for("radix.slottable");
  var getSlottableElementFromSlottable = (slottable, child) => {
    if ("child" in slottable.props) {
      const child2 = slottable.props.child;
      if (!React6.isValidElement(child2)) return null;
      return React6.cloneElement(child2, void 0, slottable.props.children(child2.props.children));
    }
    return React6.isValidElement(child) ? child : null;
  };
  function mergeProps(slotProps, childProps) {
    const overrideProps = { ...childProps };
    for (const propName in childProps) {
      const slotPropValue = slotProps[propName];
      const childPropValue = childProps[propName];
      const isHandler = /^on[A-Z]/.test(propName);
      if (isHandler) {
        if (slotPropValue && childPropValue) {
          overrideProps[propName] = (...args) => {
            const result = childPropValue(...args);
            slotPropValue(...args);
            return result;
          };
        } else if (slotPropValue) {
          overrideProps[propName] = slotPropValue;
        }
      } else if (propName === "style") {
        overrideProps[propName] = { ...slotPropValue, ...childPropValue };
      } else if (propName === "className") {
        overrideProps[propName] = [slotPropValue, childPropValue].filter(Boolean).join(" ");
      }
    }
    return { ...slotProps, ...overrideProps };
  }
  function getElementRef(element) {
    let getter = Object.getOwnPropertyDescriptor(element.props, "ref")?.get;
    let mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
    if (mayWarn) {
      return element.ref;
    }
    getter = Object.getOwnPropertyDescriptor(element, "ref")?.get;
    mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
    if (mayWarn) {
      return element.props.ref;
    }
    return element.props.ref || element.ref;
  }
  function isSlottable(child) {
    return React6.isValidElement(child) && typeof child.type === "function" && "__radixId" in child.type && child.type.__radixId === SLOTTABLE_IDENTIFIER;
  }
  var REACT_LAZY_TYPE = /* @__PURE__ */ Symbol.for("react.lazy");
  function isLazyComponent(element) {
    return element != null && typeof element === "object" && "$$typeof" in element && element.$$typeof === REACT_LAZY_TYPE && "_payload" in element && isPromiseLike(element._payload);
  }
  function isPromiseLike(value) {
    return typeof value === "object" && value !== null && "then" in value;
  }
  var createSlotError = (ownerName) => {
    return `${ownerName} failed to slot onto its children. Expected a single React element child or \`Slottable\`.`;
  };
  var createSlottableError = (ownerName) => {
    return `${ownerName} failed to slot onto its \`Slottable\`. Expected \`Slottable\` to receive a single React element child.`;
  };
  var use = React6[" use ".trim().toString()];

  // node_modules/@radix-ui/react-primitive/dist/index.mjs
  var import_jsx_runtime3 = __toESM(require_jsx_runtime(), 1);
  var NODES = [
    "a",
    "button",
    "div",
    "form",
    "h2",
    "h3",
    "img",
    "input",
    "label",
    "li",
    "nav",
    "ol",
    "p",
    "select",
    "span",
    "svg",
    "ul"
  ];
  var Primitive = NODES.reduce((primitive, node) => {
    const Slot4 = createSlot(`Primitive.${node}`);
    const Node2 = React7.forwardRef((props, forwardedRef) => {
      const { asChild, ...primitiveProps } = props;
      const Comp = asChild ? Slot4 : node;
      if (typeof window !== "undefined") {
        window[/* @__PURE__ */ Symbol.for("radix-ui")] = true;
      }
      return /* @__PURE__ */ (0, import_jsx_runtime3.jsx)(Comp, { ...primitiveProps, ref: forwardedRef });
    });
    Node2.displayName = `Primitive.${node}`;
    return { ...primitive, [node]: Node2 };
  }, {});
  function dispatchDiscreteCustomEvent(target, event) {
    if (target) ReactDOM.flushSync(() => target.dispatchEvent(event));
  }

  // node_modules/@radix-ui/react-use-callback-ref/dist/index.mjs
  var React8 = __toESM(require_react(), 1);
  function useCallbackRef(callback) {
    const callbackRef = React8.useRef(callback);
    React8.useEffect(() => {
      callbackRef.current = callback;
    });
    return React8.useMemo(() => ((...args) => callbackRef.current?.(...args)), []);
  }

  // node_modules/@radix-ui/react-dismissable-layer/dist/index.mjs
  var import_jsx_runtime4 = __toESM(require_jsx_runtime(), 1);
  var DISMISSABLE_LAYER_NAME = "DismissableLayer";
  var CONTEXT_UPDATE = "dismissableLayer.update";
  var POINTER_DOWN_OUTSIDE = "dismissableLayer.pointerDownOutside";
  var FOCUS_OUTSIDE = "dismissableLayer.focusOutside";
  var originalBodyPointerEvents;
  var DismissableLayerContext = React9.createContext({
    layers: /* @__PURE__ */ new Set(),
    layersWithOutsidePointerEventsDisabled: /* @__PURE__ */ new Set(),
    branches: /* @__PURE__ */ new Set(),
    // Outside elements that belong to a layer's own dismiss affordance (eg, a
    // dialog overlay). Pressing them should dismiss the layer regardless of
    // whether or not they stop propagation.
    //
    // See https://github.com/radix-ui/primitives/issues/3346
    dismissableSurfaces: /* @__PURE__ */ new Set()
  });
  var DismissableLayer = React9.forwardRef(
    (props, forwardedRef) => {
      const {
        disableOutsidePointerEvents = false,
        deferPointerDownOutside = false,
        onEscapeKeyDown,
        onPointerDownOutside,
        onFocusOutside,
        onInteractOutside,
        onDismiss,
        ...layerProps
      } = props;
      const context = React9.useContext(DismissableLayerContext);
      const [node, setNode] = React9.useState(null);
      const ownerDocument = node?.ownerDocument ?? globalThis?.document;
      const [, force] = React9.useState({});
      const composedRefs = useComposedRefs(forwardedRef, setNode);
      const layers = Array.from(context.layers);
      const [highestLayerWithOutsidePointerEventsDisabled] = [
        ...context.layersWithOutsidePointerEventsDisabled
      ].slice(-1);
      const highestLayerWithOutsidePointerEventsDisabledIndex = highestLayerWithOutsidePointerEventsDisabled ? layers.indexOf(highestLayerWithOutsidePointerEventsDisabled) : -1;
      const index2 = node ? layers.indexOf(node) : -1;
      const isBodyPointerEventsDisabled = context.layersWithOutsidePointerEventsDisabled.size > 0;
      const isPointerEventsEnabled = index2 >= highestLayerWithOutsidePointerEventsDisabledIndex;
      const isDeferredPointerDownOutsideRef = React9.useRef(false);
      const pointerDownOutside = usePointerDownOutside(
        (event) => {
          onPointerDownOutside?.(event);
          onInteractOutside?.(event);
          if (!event.defaultPrevented) onDismiss?.();
        },
        {
          ownerDocument,
          deferPointerDownOutside,
          isDeferredPointerDownOutsideRef,
          dismissableSurfaces: context.dismissableSurfaces,
          shouldHandlePointerDownOutside: React9.useCallback(
            (target) => {
              if (!(target instanceof Node)) {
                return false;
              }
              const isPointerDownOnBranch = [...context.branches].some(
                (branch) => branch.contains(target)
              );
              return isPointerEventsEnabled && !isPointerDownOnBranch;
            },
            [context.branches, isPointerEventsEnabled]
          )
        }
      );
      const focusOutside = useFocusOutside((event) => {
        if (deferPointerDownOutside && isDeferredPointerDownOutsideRef.current) {
          return;
        }
        const target = event.target;
        const isFocusInBranch = [...context.branches].some((branch) => branch.contains(target));
        if (isFocusInBranch) return;
        onFocusOutside?.(event);
        onInteractOutside?.(event);
        if (!event.defaultPrevented) onDismiss?.();
      }, ownerDocument);
      const isHighestLayer = node ? index2 === layers.length - 1 : false;
      const handleKeyDown2 = useCallbackRef((event) => {
        if (event.key !== "Escape") {
          return;
        }
        onEscapeKeyDown?.(event);
        if (!event.defaultPrevented && onDismiss) {
          event.preventDefault();
          onDismiss();
        }
      });
      React9.useEffect(() => {
        if (!isHighestLayer) {
          return;
        }
        ownerDocument.addEventListener("keydown", handleKeyDown2, { capture: true });
        return () => ownerDocument.removeEventListener("keydown", handleKeyDown2, { capture: true });
      }, [ownerDocument, isHighestLayer, handleKeyDown2]);
      React9.useEffect(() => {
        if (!node) return;
        if (disableOutsidePointerEvents) {
          if (context.layersWithOutsidePointerEventsDisabled.size === 0) {
            originalBodyPointerEvents = ownerDocument.body.style.pointerEvents;
            ownerDocument.body.style.pointerEvents = "none";
          }
          context.layersWithOutsidePointerEventsDisabled.add(node);
        }
        context.layers.add(node);
        dispatchUpdate();
        return () => {
          if (disableOutsidePointerEvents) {
            context.layersWithOutsidePointerEventsDisabled.delete(node);
            if (context.layersWithOutsidePointerEventsDisabled.size === 0) {
              ownerDocument.body.style.pointerEvents = originalBodyPointerEvents;
            }
          }
        };
      }, [node, ownerDocument, disableOutsidePointerEvents, context]);
      React9.useEffect(() => {
        return () => {
          if (!node) return;
          context.layers.delete(node);
          context.layersWithOutsidePointerEventsDisabled.delete(node);
          dispatchUpdate();
        };
      }, [node, context]);
      React9.useEffect(() => {
        const handleUpdate = () => force({});
        document.addEventListener(CONTEXT_UPDATE, handleUpdate);
        return () => document.removeEventListener(CONTEXT_UPDATE, handleUpdate);
      }, []);
      return /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(
        Primitive.div,
        {
          ...layerProps,
          ref: composedRefs,
          style: {
            pointerEvents: isBodyPointerEventsDisabled ? isPointerEventsEnabled ? "auto" : "none" : void 0,
            ...props.style
          },
          onFocusCapture: composeEventHandlers(props.onFocusCapture, focusOutside.onFocusCapture),
          onBlurCapture: composeEventHandlers(props.onBlurCapture, focusOutside.onBlurCapture),
          onPointerDownCapture: composeEventHandlers(
            props.onPointerDownCapture,
            pointerDownOutside.onPointerDownCapture
          )
        }
      );
    }
  );
  DismissableLayer.displayName = DISMISSABLE_LAYER_NAME;
  var BRANCH_NAME = "DismissableLayerBranch";
  var DismissableLayerBranch = React9.forwardRef((props, forwardedRef) => {
    const context = React9.useContext(DismissableLayerContext);
    const ref = React9.useRef(null);
    const composedRefs = useComposedRefs(forwardedRef, ref);
    React9.useEffect(() => {
      const node = ref.current;
      if (node) {
        context.branches.add(node);
        return () => {
          context.branches.delete(node);
        };
      }
    }, [context.branches]);
    return /* @__PURE__ */ (0, import_jsx_runtime4.jsx)(Primitive.div, { ...props, ref: composedRefs });
  });
  DismissableLayerBranch.displayName = BRANCH_NAME;
  function useDismissableLayerSurface() {
    const context = React9.useContext(DismissableLayerContext);
    const [node, setNode] = React9.useState(null);
    React9.useEffect(() => {
      if (!node) {
        return;
      }
      context.dismissableSurfaces.add(node);
      return () => {
        context.dismissableSurfaces.delete(node);
      };
    }, [node, context.dismissableSurfaces]);
    return setNode;
  }
  var IS_TRUE = () => true;
  function usePointerDownOutside(onPointerDownOutside, args) {
    const {
      ownerDocument = globalThis?.document,
      deferPointerDownOutside = false,
      isDeferredPointerDownOutsideRef,
      dismissableSurfaces,
      shouldHandlePointerDownOutside = IS_TRUE
    } = args;
    const handlePointerDownOutside = useCallbackRef(onPointerDownOutside);
    const isPointerInsideReactTreeRef = React9.useRef(false);
    const isPointerDownOutsideRef = React9.useRef(false);
    const interceptedOutsideInteractionEventsRef = React9.useRef(/* @__PURE__ */ new Map());
    const handleClickRef = React9.useRef(() => {
    });
    React9.useEffect(() => {
      function resetOutsideInteraction() {
        isPointerDownOutsideRef.current = false;
        isDeferredPointerDownOutsideRef.current = false;
        interceptedOutsideInteractionEventsRef.current.clear();
      }
      function isOutsideInteractionIntercepted() {
        return Array.from(interceptedOutsideInteractionEventsRef.current.values()).some(Boolean);
      }
      function handleInteractionCapture(event) {
        if (!isPointerDownOutsideRef.current) {
          return;
        }
        const target = event.target;
        const isDismissableSurface = target instanceof Node && [...dismissableSurfaces].some((surface) => surface.contains(target));
        if (!isDismissableSurface) {
          interceptedOutsideInteractionEventsRef.current.set(event.type, true);
        }
        if (event.type === "click") {
          window.setTimeout(() => {
            if (isPointerDownOutsideRef.current) {
              handleClickRef.current();
            }
          }, 0);
        }
      }
      function handleInteractionBubble(event) {
        if (isPointerDownOutsideRef.current) {
          interceptedOutsideInteractionEventsRef.current.set(event.type, false);
        }
      }
      const handlePointerDown = (event) => {
        if (event.target && !isPointerInsideReactTreeRef.current) {
          let handleAndDispatchPointerDownOutsideEvent2 = function() {
            ownerDocument.removeEventListener("click", handleClickRef.current);
            const wasOutsideInteractionIntercepted = isOutsideInteractionIntercepted();
            resetOutsideInteraction();
            if (!wasOutsideInteractionIntercepted) {
              handleAndDispatchCustomEvent(
                POINTER_DOWN_OUTSIDE,
                handlePointerDownOutside,
                eventDetail,
                { discrete: true }
              );
            }
          };
          var handleAndDispatchPointerDownOutsideEvent = handleAndDispatchPointerDownOutsideEvent2;
          if (!shouldHandlePointerDownOutside(event.target)) {
            ownerDocument.removeEventListener("click", handleClickRef.current);
            resetOutsideInteraction();
            isPointerInsideReactTreeRef.current = false;
            return;
          }
          const eventDetail = { originalEvent: event };
          isPointerDownOutsideRef.current = true;
          isDeferredPointerDownOutsideRef.current = deferPointerDownOutside && event.button === 0;
          interceptedOutsideInteractionEventsRef.current.clear();
          if (!deferPointerDownOutside || event.button !== 0) {
            handleAndDispatchPointerDownOutsideEvent2();
          } else {
            ownerDocument.removeEventListener("click", handleClickRef.current);
            handleClickRef.current = handleAndDispatchPointerDownOutsideEvent2;
            ownerDocument.addEventListener("click", handleClickRef.current, { once: true });
          }
        } else {
          ownerDocument.removeEventListener("click", handleClickRef.current);
          resetOutsideInteraction();
        }
        isPointerInsideReactTreeRef.current = false;
      };
      const outsideInteractionEvents = [
        "pointerup",
        "mousedown",
        "mouseup",
        "touchstart",
        "touchend",
        "click"
      ];
      for (const eventName of outsideInteractionEvents) {
        ownerDocument.addEventListener(eventName, handleInteractionCapture, true);
        ownerDocument.addEventListener(eventName, handleInteractionBubble);
      }
      const timerId = window.setTimeout(() => {
        ownerDocument.addEventListener("pointerdown", handlePointerDown);
      }, 0);
      return () => {
        window.clearTimeout(timerId);
        ownerDocument.removeEventListener("pointerdown", handlePointerDown);
        ownerDocument.removeEventListener("click", handleClickRef.current);
        for (const eventName of outsideInteractionEvents) {
          ownerDocument.removeEventListener(eventName, handleInteractionCapture, true);
          ownerDocument.removeEventListener(eventName, handleInteractionBubble);
        }
      };
    }, [
      ownerDocument,
      handlePointerDownOutside,
      deferPointerDownOutside,
      isDeferredPointerDownOutsideRef,
      dismissableSurfaces,
      shouldHandlePointerDownOutside
    ]);
    return {
      // ensures we check React component tree (not just DOM tree)
      onPointerDownCapture: () => isPointerInsideReactTreeRef.current = true
    };
  }
  function useFocusOutside(onFocusOutside, ownerDocument = globalThis?.document) {
    const handleFocusOutside = useCallbackRef(onFocusOutside);
    const isFocusInsideReactTreeRef = React9.useRef(false);
    React9.useEffect(() => {
      const handleFocus = (event) => {
        if (event.target && !isFocusInsideReactTreeRef.current) {
          const eventDetail = { originalEvent: event };
          handleAndDispatchCustomEvent(FOCUS_OUTSIDE, handleFocusOutside, eventDetail, {
            discrete: false
          });
        }
      };
      ownerDocument.addEventListener("focusin", handleFocus);
      return () => ownerDocument.removeEventListener("focusin", handleFocus);
    }, [ownerDocument, handleFocusOutside]);
    return {
      onFocusCapture: () => isFocusInsideReactTreeRef.current = true,
      onBlurCapture: () => isFocusInsideReactTreeRef.current = false
    };
  }
  function dispatchUpdate() {
    const event = new CustomEvent(CONTEXT_UPDATE);
    document.dispatchEvent(event);
  }
  function handleAndDispatchCustomEvent(name, handler, detail, { discrete }) {
    const target = detail.originalEvent.target;
    const event = new CustomEvent(name, { bubbles: false, cancelable: true, detail });
    if (handler) target.addEventListener(name, handler, { once: true });
    if (discrete) {
      dispatchDiscreteCustomEvent(target, event);
    } else {
      target.dispatchEvent(event);
    }
  }
  var Root = DismissableLayer;
  var Branch = DismissableLayerBranch;

  // node_modules/@radix-ui/react-focus-scope/dist/index.mjs
  var React10 = __toESM(require_react(), 1);
  var import_jsx_runtime5 = __toESM(require_jsx_runtime(), 1);
  var AUTOFOCUS_ON_MOUNT = "focusScope.autoFocusOnMount";
  var AUTOFOCUS_ON_UNMOUNT = "focusScope.autoFocusOnUnmount";
  var EVENT_OPTIONS = { bubbles: false, cancelable: true };
  var FOCUS_SCOPE_NAME = "FocusScope";
  var FocusScope = React10.forwardRef((props, forwardedRef) => {
    const {
      loop = false,
      trapped = false,
      onMountAutoFocus: onMountAutoFocusProp,
      onUnmountAutoFocus: onUnmountAutoFocusProp,
      ...scopeProps
    } = props;
    const [container, setContainer] = React10.useState(null);
    const onMountAutoFocus = useCallbackRef(onMountAutoFocusProp);
    const onUnmountAutoFocus = useCallbackRef(onUnmountAutoFocusProp);
    const lastFocusedElementRef = React10.useRef(null);
    const composedRefs = useComposedRefs(forwardedRef, setContainer);
    const focusScope = React10.useRef({
      paused: false,
      pause() {
        this.paused = true;
      },
      resume() {
        this.paused = false;
      }
    }).current;
    React10.useEffect(() => {
      if (trapped) {
        let handleFocusIn2 = function(event) {
          if (focusScope.paused || !container) return;
          const target = event.target;
          if (container.contains(target)) {
            lastFocusedElementRef.current = target;
          } else {
            focus(lastFocusedElementRef.current, { select: true });
          }
        }, handleFocusOut2 = function(event) {
          if (focusScope.paused || !container) return;
          const relatedTarget = event.relatedTarget;
          if (relatedTarget === null) return;
          if (!container.contains(relatedTarget)) {
            focus(lastFocusedElementRef.current, { select: true });
          }
        }, handleMutations2 = function(mutations) {
          const focusedElement = document.activeElement;
          if (focusedElement !== document.body) return;
          for (const mutation of mutations) {
            if (mutation.removedNodes.length > 0) focus(container);
          }
        };
        var handleFocusIn = handleFocusIn2, handleFocusOut = handleFocusOut2, handleMutations = handleMutations2;
        document.addEventListener("focusin", handleFocusIn2);
        document.addEventListener("focusout", handleFocusOut2);
        const mutationObserver = new MutationObserver(handleMutations2);
        if (container) mutationObserver.observe(container, { childList: true, subtree: true });
        return () => {
          document.removeEventListener("focusin", handleFocusIn2);
          document.removeEventListener("focusout", handleFocusOut2);
          mutationObserver.disconnect();
        };
      }
    }, [trapped, container, focusScope.paused]);
    React10.useEffect(() => {
      if (container) {
        focusScopesStack.add(focusScope);
        const previouslyFocusedElement = document.activeElement;
        const hasFocusedCandidate = container.contains(previouslyFocusedElement);
        if (!hasFocusedCandidate) {
          const mountEvent = new CustomEvent(AUTOFOCUS_ON_MOUNT, EVENT_OPTIONS);
          container.addEventListener(AUTOFOCUS_ON_MOUNT, onMountAutoFocus);
          container.dispatchEvent(mountEvent);
          if (!mountEvent.defaultPrevented) {
            focusFirst(removeLinks(getTabbableCandidates(container)), { select: true });
            if (document.activeElement === previouslyFocusedElement) {
              focus(container);
            }
          }
        }
        return () => {
          container.removeEventListener(AUTOFOCUS_ON_MOUNT, onMountAutoFocus);
          setTimeout(() => {
            const unmountEvent = new CustomEvent(AUTOFOCUS_ON_UNMOUNT, EVENT_OPTIONS);
            container.addEventListener(AUTOFOCUS_ON_UNMOUNT, onUnmountAutoFocus);
            container.dispatchEvent(unmountEvent);
            if (!unmountEvent.defaultPrevented) {
              focus(previouslyFocusedElement ?? document.body, { select: true });
            }
            container.removeEventListener(AUTOFOCUS_ON_UNMOUNT, onUnmountAutoFocus);
            focusScopesStack.remove(focusScope);
          }, 0);
        };
      }
    }, [container, onMountAutoFocus, onUnmountAutoFocus, focusScope]);
    const handleKeyDown2 = React10.useCallback(
      (event) => {
        if (!loop && !trapped) return;
        if (focusScope.paused) return;
        const isTabKey = event.key === "Tab" && !event.altKey && !event.ctrlKey && !event.metaKey;
        const focusedElement = document.activeElement;
        if (isTabKey && focusedElement) {
          const container2 = event.currentTarget;
          const [first, last] = getTabbableEdges(container2);
          const hasTabbableElementsInside = first && last;
          if (!hasTabbableElementsInside) {
            if (focusedElement === container2) event.preventDefault();
          } else {
            if (!event.shiftKey && focusedElement === last) {
              event.preventDefault();
              if (loop) focus(first, { select: true });
            } else if (event.shiftKey && focusedElement === first) {
              event.preventDefault();
              if (loop) focus(last, { select: true });
            }
          }
        }
      },
      [loop, trapped, focusScope.paused]
    );
    return /* @__PURE__ */ (0, import_jsx_runtime5.jsx)(Primitive.div, { tabIndex: -1, ...scopeProps, ref: composedRefs, onKeyDown: handleKeyDown2 });
  });
  FocusScope.displayName = FOCUS_SCOPE_NAME;
  function focusFirst(candidates, { select = false } = {}) {
    const previouslyFocusedElement = document.activeElement;
    for (const candidate of candidates) {
      focus(candidate, { select });
      if (document.activeElement !== previouslyFocusedElement) return;
    }
  }
  function getTabbableEdges(container) {
    const candidates = getTabbableCandidates(container);
    const first = findVisible(candidates, container);
    const last = findVisible(candidates.reverse(), container);
    return [first, last];
  }
  function getTabbableCandidates(container) {
    const nodes = [];
    const walker = document.createTreeWalker(container, NodeFilter.SHOW_ELEMENT, {
      acceptNode: (node) => {
        const isHiddenInput = node.tagName === "INPUT" && node.type === "hidden";
        if (node.disabled || node.hidden || isHiddenInput) return NodeFilter.FILTER_SKIP;
        return node.tabIndex >= 0 ? NodeFilter.FILTER_ACCEPT : NodeFilter.FILTER_SKIP;
      }
    });
    while (walker.nextNode()) nodes.push(walker.currentNode);
    return nodes;
  }
  function findVisible(elements, container) {
    const canUseCheckVisibility = typeof container.checkVisibility === "function" && container.checkVisibility({ checkVisibilityCSS: true });
    for (const element of elements) {
      const hidden = canUseCheckVisibility ? !element.checkVisibility({ checkVisibilityCSS: true }) : isHidden(element, { upTo: container });
      if (!hidden) {
        return element;
      }
    }
  }
  function isHidden(node, { upTo }) {
    if (getComputedStyle(node).visibility === "hidden") return true;
    while (node) {
      if (upTo !== void 0 && node === upTo) return false;
      if (getComputedStyle(node).display === "none") return true;
      node = node.parentElement;
    }
    return false;
  }
  function isSelectableInput(element) {
    return element instanceof HTMLInputElement && "select" in element;
  }
  function focus(element, { select = false } = {}) {
    if (element && element.focus) {
      const previouslyFocusedElement = document.activeElement;
      element.focus({ preventScroll: true });
      if (element !== previouslyFocusedElement && isSelectableInput(element) && select)
        element.select();
    }
  }
  var focusScopesStack = createFocusScopesStack();
  function createFocusScopesStack() {
    let stack = [];
    return {
      add(focusScope) {
        const activeFocusScope = stack[0];
        if (focusScope !== activeFocusScope) {
          activeFocusScope?.pause();
        }
        stack = arrayRemove(stack, focusScope);
        stack.unshift(focusScope);
      },
      remove(focusScope) {
        stack = arrayRemove(stack, focusScope);
        stack[0]?.resume();
      }
    };
  }
  function arrayRemove(array, item) {
    const updatedArray = [...array];
    const index2 = updatedArray.indexOf(item);
    if (index2 !== -1) {
      updatedArray.splice(index2, 1);
    }
    return updatedArray;
  }
  function removeLinks(items) {
    return items.filter((item) => item.tagName !== "A");
  }

  // node_modules/@radix-ui/react-portal/dist/index.mjs
  var React11 = __toESM(require_react(), 1);
  var ReactDOM2 = __toESM(require_react_dom(), 1);
  var import_jsx_runtime6 = __toESM(require_jsx_runtime(), 1);
  var PORTAL_NAME = "Portal";
  var Portal = React11.forwardRef((props, forwardedRef) => {
    const { container: containerProp, ...portalProps } = props;
    const [mounted, setMounted] = React11.useState(false);
    useLayoutEffect2(() => setMounted(true), []);
    const container = containerProp || mounted && globalThis?.document?.body;
    return container ? ReactDOM2.createPortal(/* @__PURE__ */ (0, import_jsx_runtime6.jsx)(Primitive.div, { ...portalProps, ref: forwardedRef }), container) : null;
  });
  Portal.displayName = PORTAL_NAME;

  // node_modules/@radix-ui/react-presence/dist/index.mjs
  var React23 = __toESM(require_react(), 1);
  var React12 = __toESM(require_react(), 1);
  function useStateMachine(initialState, machine) {
    return React12.useReducer((state2, event) => {
      const nextState = machine[state2][event];
      return nextState ?? state2;
    }, initialState);
  }
  var Presence = (props) => {
    const { present, children } = props;
    const presence = usePresence(present);
    const child = typeof children === "function" ? children({ present: presence.isPresent }) : React23.Children.only(children);
    const ref = useStableComposedRefs(presence.ref, getElementRef2(child));
    const forceMount = typeof children === "function";
    return forceMount || presence.isPresent ? React23.cloneElement(child, { ref }) : null;
  };
  Presence.displayName = "Presence";
  function usePresence(present) {
    const [node, setNode] = React23.useState();
    const stylesRef = React23.useRef(null);
    const prevPresentRef = React23.useRef(present);
    const prevAnimationNameRef = React23.useRef("none");
    const mountAnimationNameRef = React23.useRef(void 0);
    const initialState = present ? "mounted" : "unmounted";
    const [state2, send] = useStateMachine(initialState, {
      mounted: {
        UNMOUNT: "unmounted",
        ANIMATION_OUT: "unmountSuspended"
      },
      unmountSuspended: {
        MOUNT: "mounted",
        ANIMATION_END: "unmounted"
      },
      unmounted: {
        MOUNT: "mounted"
      }
    });
    React23.useEffect(() => {
      if (state2 === "mounted") {
        prevAnimationNameRef.current = mountAnimationNameRef.current ?? getAnimationName(stylesRef.current);
        mountAnimationNameRef.current = void 0;
      } else {
        prevAnimationNameRef.current = "none";
      }
    }, [state2]);
    useLayoutEffect2(() => {
      const styles = stylesRef.current;
      const wasPresent = prevPresentRef.current;
      const hasPresentChanged = wasPresent !== present;
      if (hasPresentChanged) {
        const prevAnimationName = prevAnimationNameRef.current;
        const currentAnimationName = getAnimationName(styles);
        if (present) {
          mountAnimationNameRef.current = currentAnimationName;
          send("MOUNT");
        } else if (currentAnimationName === "none" || styles?.display === "none") {
          send("UNMOUNT");
        } else {
          const isAnimating = prevAnimationName !== currentAnimationName;
          if (wasPresent && isAnimating) {
            send("ANIMATION_OUT");
          } else {
            send("UNMOUNT");
          }
        }
        prevPresentRef.current = present;
      }
    }, [present, send]);
    useLayoutEffect2(() => {
      if (node) {
        let timeoutId;
        const ownerWindow = node.ownerDocument.defaultView ?? window;
        const handleAnimationEnd = (event) => {
          const currentAnimationName = getAnimationName(stylesRef.current);
          const isCurrentAnimation = currentAnimationName.includes(CSS.escape(event.animationName));
          if (event.target === node && isCurrentAnimation) {
            send("ANIMATION_END");
            if (!prevPresentRef.current) {
              const currentFillMode = node.style.animationFillMode;
              node.style.animationFillMode = "forwards";
              timeoutId = ownerWindow.setTimeout(() => {
                if (node.style.animationFillMode === "forwards") {
                  node.style.animationFillMode = currentFillMode;
                }
              });
            }
          }
        };
        const handleAnimationStart = (event) => {
          if (event.target === node) {
            prevAnimationNameRef.current = getAnimationName(stylesRef.current);
          }
        };
        node.addEventListener("animationstart", handleAnimationStart);
        node.addEventListener("animationcancel", handleAnimationEnd);
        node.addEventListener("animationend", handleAnimationEnd);
        return () => {
          ownerWindow.clearTimeout(timeoutId);
          node.removeEventListener("animationstart", handleAnimationStart);
          node.removeEventListener("animationcancel", handleAnimationEnd);
          node.removeEventListener("animationend", handleAnimationEnd);
        };
      } else {
        send("ANIMATION_END");
      }
    }, [node, send]);
    return {
      isPresent: ["mounted", "unmountSuspended"].includes(state2),
      ref: React23.useCallback((node2) => {
        if (node2) {
          const styles = getComputedStyle(node2);
          stylesRef.current = styles;
          mountAnimationNameRef.current = getAnimationName(styles);
        } else {
          stylesRef.current = null;
        }
        setNode(node2);
      }, [])
    };
  }
  function setRef2(ref, value) {
    if (typeof ref === "function") {
      return ref(value);
    } else if (ref !== null && ref !== void 0) {
      ref.current = value;
    }
  }
  function useStableComposedRefs(...refs) {
    const refsRef = React23.useRef(refs);
    refsRef.current = refs;
    return React23.useCallback((node) => {
      const currentRefs = refsRef.current;
      let hasCleanup = false;
      const cleanups = currentRefs.map((ref) => {
        const cleanup = setRef2(ref, node);
        if (!hasCleanup && typeof cleanup === "function") {
          hasCleanup = true;
        }
        return cleanup;
      });
      if (hasCleanup) {
        return () => {
          for (let i = 0; i < cleanups.length; i++) {
            const cleanup = cleanups[i];
            if (typeof cleanup === "function") {
              cleanup();
            } else {
              setRef2(currentRefs[i], null);
            }
          }
        };
      }
    }, []);
  }
  function getAnimationName(styles) {
    return styles?.animationName || "none";
  }
  function getElementRef2(element) {
    let getter = Object.getOwnPropertyDescriptor(element.props, "ref")?.get;
    let mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
    if (mayWarn) {
      return element.ref;
    }
    getter = Object.getOwnPropertyDescriptor(element, "ref")?.get;
    mayWarn = getter && "isReactWarning" in getter && getter.isReactWarning;
    if (mayWarn) {
      return element.props.ref;
    }
    return element.props.ref || element.ref;
  }

  // node_modules/@radix-ui/react-focus-guards/dist/index.mjs
  var React13 = __toESM(require_react(), 1);
  var count2 = 0;
  var guards = null;
  function useFocusGuards() {
    React13.useEffect(() => {
      if (!guards) {
        guards = { start: createFocusGuard(), end: createFocusGuard() };
      }
      const { start, end } = guards;
      if (document.body.firstElementChild !== start) {
        document.body.insertAdjacentElement("afterbegin", start);
      }
      if (document.body.lastElementChild !== end) {
        document.body.insertAdjacentElement("beforeend", end);
      }
      count2++;
      return () => {
        if (count2 === 1) {
          guards?.start.remove();
          guards?.end.remove();
          guards = null;
        }
        count2 = Math.max(0, count2 - 1);
      };
    }, []);
  }
  function createFocusGuard() {
    const element = document.createElement("span");
    element.setAttribute("data-radix-focus-guard", "");
    element.tabIndex = 0;
    element.style.outline = "none";
    element.style.opacity = "0";
    element.style.position = "fixed";
    element.style.pointerEvents = "none";
    return element;
  }

  // node_modules/tslib/tslib.es6.mjs
  var __assign = function() {
    __assign = Object.assign || function __assign2(t6) {
      for (var s, i = 1, n = arguments.length; i < n; i++) {
        s = arguments[i];
        for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p)) t6[p] = s[p];
      }
      return t6;
    };
    return __assign.apply(this, arguments);
  };
  function __rest(s, e) {
    var t6 = {};
    for (var p in s) if (Object.prototype.hasOwnProperty.call(s, p) && e.indexOf(p) < 0)
      t6[p] = s[p];
    if (s != null && typeof Object.getOwnPropertySymbols === "function")
      for (var i = 0, p = Object.getOwnPropertySymbols(s); i < p.length; i++) {
        if (e.indexOf(p[i]) < 0 && Object.prototype.propertyIsEnumerable.call(s, p[i]))
          t6[p[i]] = s[p[i]];
      }
    return t6;
  }
  function __spreadArray(to, from, pack) {
    if (pack || arguments.length === 2) for (var i = 0, l = from.length, ar; i < l; i++) {
      if (ar || !(i in from)) {
        if (!ar) ar = Array.prototype.slice.call(from, 0, i);
        ar[i] = from[i];
      }
    }
    return to.concat(ar || Array.prototype.slice.call(from));
  }

  // node_modules/react-remove-scroll/dist/es2015/Combination.js
  var React20 = __toESM(require_react());

  // node_modules/react-remove-scroll/dist/es2015/UI.js
  var React16 = __toESM(require_react());

  // node_modules/react-remove-scroll-bar/dist/es2015/constants.js
  var zeroRightClassName = "right-scroll-bar-position";
  var fullWidthClassName = "width-before-scroll-bar";
  var noScrollbarsClassName = "with-scroll-bars-hidden";
  var removedBarSizeVariable = "--removed-body-scroll-bar-size";

  // node_modules/use-callback-ref/dist/es2015/assignRef.js
  function assignRef(ref, value) {
    if (typeof ref === "function") {
      ref(value);
    } else if (ref) {
      ref.current = value;
    }
    return ref;
  }

  // node_modules/use-callback-ref/dist/es2015/useRef.js
  var import_react2 = __toESM(require_react());
  function useCallbackRef2(initialValue, callback) {
    var ref = (0, import_react2.useState)(function() {
      return {
        // value
        value: initialValue,
        // last callback
        callback,
        // "memoized" public interface
        facade: {
          get current() {
            return ref.value;
          },
          set current(value) {
            var last = ref.value;
            if (last !== value) {
              ref.value = value;
              ref.callback(value, last);
            }
          }
        }
      };
    })[0];
    ref.callback = callback;
    return ref.facade;
  }

  // node_modules/use-callback-ref/dist/es2015/useMergeRef.js
  var React14 = __toESM(require_react());
  var useIsomorphicLayoutEffect = typeof window !== "undefined" ? React14.useLayoutEffect : React14.useEffect;
  var currentValues = /* @__PURE__ */ new WeakMap();
  function useMergeRefs(refs, defaultValue) {
    var callbackRef = useCallbackRef2(defaultValue || null, function(newValue) {
      return refs.forEach(function(ref) {
        return assignRef(ref, newValue);
      });
    });
    useIsomorphicLayoutEffect(function() {
      var oldValue = currentValues.get(callbackRef);
      if (oldValue) {
        var prevRefs_1 = new Set(oldValue);
        var nextRefs_1 = new Set(refs);
        var current_1 = callbackRef.current;
        prevRefs_1.forEach(function(ref) {
          if (!nextRefs_1.has(ref)) {
            assignRef(ref, null);
          }
        });
        nextRefs_1.forEach(function(ref) {
          if (!prevRefs_1.has(ref)) {
            assignRef(ref, current_1);
          }
        });
      }
      currentValues.set(callbackRef, refs);
    }, [refs]);
    return callbackRef;
  }

  // node_modules/use-sidecar/dist/es2015/medium.js
  function ItoI(a) {
    return a;
  }
  function innerCreateMedium(defaults, middleware) {
    if (middleware === void 0) {
      middleware = ItoI;
    }
    var buffer = [];
    var assigned = false;
    var medium = {
      read: function() {
        if (assigned) {
          throw new Error("Sidecar: could not `read` from an `assigned` medium. `read` could be used only with `useMedium`.");
        }
        if (buffer.length) {
          return buffer[buffer.length - 1];
        }
        return defaults;
      },
      useMedium: function(data) {
        var item = middleware(data, assigned);
        buffer.push(item);
        return function() {
          buffer = buffer.filter(function(x) {
            return x !== item;
          });
        };
      },
      assignSyncMedium: function(cb) {
        assigned = true;
        while (buffer.length) {
          var cbs = buffer;
          buffer = [];
          cbs.forEach(cb);
        }
        buffer = {
          push: function(x) {
            return cb(x);
          },
          filter: function() {
            return buffer;
          }
        };
      },
      assignMedium: function(cb) {
        assigned = true;
        var pendingQueue = [];
        if (buffer.length) {
          var cbs = buffer;
          buffer = [];
          cbs.forEach(cb);
          pendingQueue = buffer;
        }
        var executeQueue = function() {
          var cbs2 = pendingQueue;
          pendingQueue = [];
          cbs2.forEach(cb);
        };
        var cycle = function() {
          return Promise.resolve().then(executeQueue);
        };
        cycle();
        buffer = {
          push: function(x) {
            pendingQueue.push(x);
            cycle();
          },
          filter: function(filter) {
            pendingQueue = pendingQueue.filter(filter);
            return buffer;
          }
        };
      }
    };
    return medium;
  }
  function createSidecarMedium(options) {
    if (options === void 0) {
      options = {};
    }
    var medium = innerCreateMedium(null);
    medium.options = __assign({ async: true, ssr: false }, options);
    return medium;
  }

  // node_modules/use-sidecar/dist/es2015/exports.js
  var React15 = __toESM(require_react());
  var SideCar = function(_a) {
    var sideCar = _a.sideCar, rest = __rest(_a, ["sideCar"]);
    if (!sideCar) {
      throw new Error("Sidecar: please provide `sideCar` property to import the right car");
    }
    var Target = sideCar.read();
    if (!Target) {
      throw new Error("Sidecar medium not found");
    }
    return React15.createElement(Target, __assign({}, rest));
  };
  SideCar.isSideCarExport = true;
  function exportSidecar(medium, exported) {
    medium.useMedium(exported);
    return SideCar;
  }

  // node_modules/react-remove-scroll/dist/es2015/medium.js
  var effectCar = createSidecarMedium();

  // node_modules/react-remove-scroll/dist/es2015/UI.js
  var nothing = function() {
    return;
  };
  var RemoveScroll = React16.forwardRef(function(props, parentRef) {
    var ref = React16.useRef(null);
    var _a = React16.useState({
      onScrollCapture: nothing,
      onWheelCapture: nothing,
      onTouchMoveCapture: nothing
    }), callbacks = _a[0], setCallbacks = _a[1];
    var forwardProps = props.forwardProps, children = props.children, className = props.className, removeScrollBar = props.removeScrollBar, enabled = props.enabled, shards = props.shards, sideCar = props.sideCar, noRelative = props.noRelative, noIsolation = props.noIsolation, inert = props.inert, allowPinchZoom = props.allowPinchZoom, _b = props.as, Container = _b === void 0 ? "div" : _b, gapMode = props.gapMode, rest = __rest(props, ["forwardProps", "children", "className", "removeScrollBar", "enabled", "shards", "sideCar", "noRelative", "noIsolation", "inert", "allowPinchZoom", "as", "gapMode"]);
    var SideCar2 = sideCar;
    var containerRef = useMergeRefs([ref, parentRef]);
    var containerProps = __assign(__assign({}, rest), callbacks);
    return React16.createElement(
      React16.Fragment,
      null,
      enabled && React16.createElement(SideCar2, { sideCar: effectCar, removeScrollBar, shards, noRelative, noIsolation, inert, setCallbacks, allowPinchZoom: !!allowPinchZoom, lockRef: ref, gapMode }),
      forwardProps ? React16.cloneElement(React16.Children.only(children), __assign(__assign({}, containerProps), { ref: containerRef })) : React16.createElement(Container, __assign({}, containerProps, { className, ref: containerRef }), children)
    );
  });
  RemoveScroll.defaultProps = {
    enabled: true,
    removeScrollBar: true,
    inert: false
  };
  RemoveScroll.classNames = {
    fullWidth: fullWidthClassName,
    zeroRight: zeroRightClassName
  };

  // node_modules/react-remove-scroll/dist/es2015/SideEffect.js
  var React19 = __toESM(require_react());

  // node_modules/react-remove-scroll-bar/dist/es2015/component.js
  var React18 = __toESM(require_react());

  // node_modules/react-style-singleton/dist/es2015/hook.js
  var React17 = __toESM(require_react());

  // node_modules/get-nonce/dist/es2015/index.js
  var currentNonce;
  var getNonce = function() {
    if (currentNonce) {
      return currentNonce;
    }
    if (typeof __webpack_nonce__ !== "undefined") {
      return __webpack_nonce__;
    }
    return void 0;
  };

  // node_modules/react-style-singleton/dist/es2015/singleton.js
  function makeStyleTag() {
    if (!document)
      return null;
    var tag = document.createElement("style");
    tag.type = "text/css";
    var nonce = getNonce();
    if (nonce) {
      tag.setAttribute("nonce", nonce);
    }
    return tag;
  }
  function injectStyles(tag, css) {
    if (tag.styleSheet) {
      tag.styleSheet.cssText = css;
    } else {
      tag.appendChild(document.createTextNode(css));
    }
  }
  function insertStyleTag(tag) {
    var head = document.head || document.getElementsByTagName("head")[0];
    head.appendChild(tag);
  }
  var stylesheetSingleton = function() {
    var counter = 0;
    var stylesheet = null;
    return {
      add: function(style) {
        if (counter == 0) {
          if (stylesheet = makeStyleTag()) {
            injectStyles(stylesheet, style);
            insertStyleTag(stylesheet);
          }
        }
        counter++;
      },
      remove: function() {
        counter--;
        if (!counter && stylesheet) {
          stylesheet.parentNode && stylesheet.parentNode.removeChild(stylesheet);
          stylesheet = null;
        }
      }
    };
  };

  // node_modules/react-style-singleton/dist/es2015/hook.js
  var styleHookSingleton = function() {
    var sheet = stylesheetSingleton();
    return function(styles, isDynamic) {
      React17.useEffect(function() {
        sheet.add(styles);
        return function() {
          sheet.remove();
        };
      }, [styles && isDynamic]);
    };
  };

  // node_modules/react-style-singleton/dist/es2015/component.js
  var styleSingleton = function() {
    var useStyle = styleHookSingleton();
    var Sheet = function(_a) {
      var styles = _a.styles, dynamic = _a.dynamic;
      useStyle(styles, dynamic);
      return null;
    };
    return Sheet;
  };

  // node_modules/react-remove-scroll-bar/dist/es2015/utils.js
  var zeroGap = {
    left: 0,
    top: 0,
    right: 0,
    gap: 0
  };
  var parse = function(x) {
    return parseInt(x || "", 10) || 0;
  };
  var getOffset = function(gapMode) {
    var cs = window.getComputedStyle(document.body);
    var left = cs[gapMode === "padding" ? "paddingLeft" : "marginLeft"];
    var top = cs[gapMode === "padding" ? "paddingTop" : "marginTop"];
    var right = cs[gapMode === "padding" ? "paddingRight" : "marginRight"];
    return [parse(left), parse(top), parse(right)];
  };
  var getGapWidth = function(gapMode) {
    if (gapMode === void 0) {
      gapMode = "margin";
    }
    if (typeof window === "undefined") {
      return zeroGap;
    }
    var offsets = getOffset(gapMode);
    var documentWidth = document.documentElement.clientWidth;
    var windowWidth = window.innerWidth;
    return {
      left: offsets[0],
      top: offsets[1],
      right: offsets[2],
      gap: Math.max(0, windowWidth - documentWidth + offsets[2] - offsets[0])
    };
  };

  // node_modules/react-remove-scroll-bar/dist/es2015/component.js
  var Style = styleSingleton();
  var lockAttribute = "data-scroll-locked";
  var getStyles = function(_a, allowRelative, gapMode, important) {
    var left = _a.left, top = _a.top, right = _a.right, gap = _a.gap;
    if (gapMode === void 0) {
      gapMode = "margin";
    }
    return "\n  .".concat(noScrollbarsClassName, " {\n   overflow: hidden ").concat(important, ";\n   padding-right: ").concat(gap, "px ").concat(important, ";\n  }\n  body[").concat(lockAttribute, "] {\n    overflow: hidden ").concat(important, ";\n    overscroll-behavior: contain;\n    ").concat([
      allowRelative && "position: relative ".concat(important, ";"),
      gapMode === "margin" && "\n    padding-left: ".concat(left, "px;\n    padding-top: ").concat(top, "px;\n    padding-right: ").concat(right, "px;\n    margin-left:0;\n    margin-top:0;\n    margin-right: ").concat(gap, "px ").concat(important, ";\n    "),
      gapMode === "padding" && "padding-right: ".concat(gap, "px ").concat(important, ";")
    ].filter(Boolean).join(""), "\n  }\n  \n  .").concat(zeroRightClassName, " {\n    right: ").concat(gap, "px ").concat(important, ";\n  }\n  \n  .").concat(fullWidthClassName, " {\n    margin-right: ").concat(gap, "px ").concat(important, ";\n  }\n  \n  .").concat(zeroRightClassName, " .").concat(zeroRightClassName, " {\n    right: 0 ").concat(important, ";\n  }\n  \n  .").concat(fullWidthClassName, " .").concat(fullWidthClassName, " {\n    margin-right: 0 ").concat(important, ";\n  }\n  \n  body[").concat(lockAttribute, "] {\n    ").concat(removedBarSizeVariable, ": ").concat(gap, "px;\n  }\n");
  };
  var getCurrentUseCounter = function() {
    var counter = parseInt(document.body.getAttribute(lockAttribute) || "0", 10);
    return isFinite(counter) ? counter : 0;
  };
  var useLockAttribute = function() {
    React18.useEffect(function() {
      document.body.setAttribute(lockAttribute, (getCurrentUseCounter() + 1).toString());
      return function() {
        var newCounter = getCurrentUseCounter() - 1;
        if (newCounter <= 0) {
          document.body.removeAttribute(lockAttribute);
        } else {
          document.body.setAttribute(lockAttribute, newCounter.toString());
        }
      };
    }, []);
  };
  var RemoveScrollBar = function(_a) {
    var noRelative = _a.noRelative, noImportant = _a.noImportant, _b = _a.gapMode, gapMode = _b === void 0 ? "margin" : _b;
    useLockAttribute();
    var gap = React18.useMemo(function() {
      return getGapWidth(gapMode);
    }, [gapMode]);
    return React18.createElement(Style, { styles: getStyles(gap, !noRelative, gapMode, !noImportant ? "!important" : "") });
  };

  // node_modules/react-remove-scroll/dist/es2015/aggresiveCapture.js
  var passiveSupported = false;
  if (typeof window !== "undefined") {
    try {
      options = Object.defineProperty({}, "passive", {
        get: function() {
          passiveSupported = true;
          return true;
        }
      });
      window.addEventListener("test", options, options);
      window.removeEventListener("test", options, options);
    } catch (err) {
      passiveSupported = false;
    }
  }
  var options;
  var nonPassive = passiveSupported ? { passive: false } : false;

  // node_modules/react-remove-scroll/dist/es2015/handleScroll.js
  var alwaysContainsScroll = function(node) {
    return node.tagName === "TEXTAREA";
  };
  var elementCanBeScrolled = function(node, overflow) {
    if (!(node instanceof Element)) {
      return false;
    }
    var styles = window.getComputedStyle(node);
    return (
      // not-not-scrollable
      styles[overflow] !== "hidden" && // contains scroll inside self
      !(styles.overflowY === styles.overflowX && !alwaysContainsScroll(node) && styles[overflow] === "visible")
    );
  };
  var elementCouldBeVScrolled = function(node) {
    return elementCanBeScrolled(node, "overflowY");
  };
  var elementCouldBeHScrolled = function(node) {
    return elementCanBeScrolled(node, "overflowX");
  };
  var locationCouldBeScrolled = function(axis, node) {
    var ownerDocument = node.ownerDocument;
    var current = node;
    do {
      if (typeof ShadowRoot !== "undefined" && current instanceof ShadowRoot) {
        current = current.host;
      }
      var isScrollable = elementCouldBeScrolled(axis, current);
      if (isScrollable) {
        var _a = getScrollVariables(axis, current), scrollHeight = _a[1], clientHeight = _a[2];
        if (scrollHeight > clientHeight) {
          return true;
        }
      }
      current = current.parentNode;
    } while (current && current !== ownerDocument.body);
    return false;
  };
  var getVScrollVariables = function(_a) {
    var scrollTop = _a.scrollTop, scrollHeight = _a.scrollHeight, clientHeight = _a.clientHeight;
    return [
      scrollTop,
      scrollHeight,
      clientHeight
    ];
  };
  var getHScrollVariables = function(_a) {
    var scrollLeft = _a.scrollLeft, scrollWidth = _a.scrollWidth, clientWidth = _a.clientWidth;
    return [
      scrollLeft,
      scrollWidth,
      clientWidth
    ];
  };
  var elementCouldBeScrolled = function(axis, node) {
    return axis === "v" ? elementCouldBeVScrolled(node) : elementCouldBeHScrolled(node);
  };
  var getScrollVariables = function(axis, node) {
    return axis === "v" ? getVScrollVariables(node) : getHScrollVariables(node);
  };
  var getDirectionFactor = function(axis, direction) {
    return axis === "h" && direction === "rtl" ? -1 : 1;
  };
  var handleScroll = function(axis, endTarget, event, sourceDelta, noOverscroll) {
    var directionFactor = getDirectionFactor(axis, window.getComputedStyle(endTarget).direction);
    var delta = directionFactor * sourceDelta;
    var target = event.target;
    var targetInLock = endTarget.contains(target);
    var shouldCancelScroll = false;
    var isDeltaPositive = delta > 0;
    var availableScroll = 0;
    var availableScrollTop = 0;
    do {
      if (!target) {
        break;
      }
      var _a = getScrollVariables(axis, target), position = _a[0], scroll_1 = _a[1], capacity = _a[2];
      var elementScroll = scroll_1 - capacity - directionFactor * position;
      if (position || elementScroll) {
        if (elementCouldBeScrolled(axis, target)) {
          availableScroll += elementScroll;
          availableScrollTop += position;
        }
      }
      var parent_1 = target.parentNode;
      target = parent_1 && parent_1.nodeType === Node.DOCUMENT_FRAGMENT_NODE ? parent_1.host : parent_1;
    } while (
      // portaled content
      !targetInLock && target !== document.body || // self content
      targetInLock && (endTarget.contains(target) || endTarget === target)
    );
    if (isDeltaPositive && (noOverscroll && Math.abs(availableScroll) < 1 || !noOverscroll && delta > availableScroll)) {
      shouldCancelScroll = true;
    } else if (!isDeltaPositive && (noOverscroll && Math.abs(availableScrollTop) < 1 || !noOverscroll && -delta > availableScrollTop)) {
      shouldCancelScroll = true;
    }
    return shouldCancelScroll;
  };

  // node_modules/react-remove-scroll/dist/es2015/SideEffect.js
  var getTouchXY = function(event) {
    return "changedTouches" in event ? [event.changedTouches[0].clientX, event.changedTouches[0].clientY] : [0, 0];
  };
  var getDeltaXY = function(event) {
    return [event.deltaX, event.deltaY];
  };
  var extractRef = function(ref) {
    return ref && "current" in ref ? ref.current : ref;
  };
  var deltaCompare = function(x, y) {
    return x[0] === y[0] && x[1] === y[1];
  };
  var generateStyle = function(id) {
    return "\n  .block-interactivity-".concat(id, " {pointer-events: none;}\n  .allow-interactivity-").concat(id, " {pointer-events: all;}\n");
  };
  var idCounter = 0;
  var lockStack = [];
  function RemoveScrollSideCar(props) {
    var shouldPreventQueue = React19.useRef([]);
    var touchStartRef = React19.useRef([0, 0]);
    var activeAxis = React19.useRef();
    var id = React19.useState(idCounter++)[0];
    var Style2 = React19.useState(styleSingleton)[0];
    var lastProps = React19.useRef(props);
    React19.useEffect(function() {
      lastProps.current = props;
    }, [props]);
    React19.useEffect(function() {
      if (props.inert) {
        document.body.classList.add("block-interactivity-".concat(id));
        var allow_1 = __spreadArray([props.lockRef.current], (props.shards || []).map(extractRef), true).filter(Boolean);
        allow_1.forEach(function(el) {
          return el.classList.add("allow-interactivity-".concat(id));
        });
        return function() {
          document.body.classList.remove("block-interactivity-".concat(id));
          allow_1.forEach(function(el) {
            return el.classList.remove("allow-interactivity-".concat(id));
          });
        };
      }
      return;
    }, [props.inert, props.lockRef.current, props.shards]);
    var shouldCancelEvent = React19.useCallback(function(event, parent) {
      if ("touches" in event && event.touches.length === 2 || event.type === "wheel" && event.ctrlKey) {
        return !lastProps.current.allowPinchZoom;
      }
      var touch = getTouchXY(event);
      var touchStart = touchStartRef.current;
      var deltaX = "deltaX" in event ? event.deltaX : touchStart[0] - touch[0];
      var deltaY = "deltaY" in event ? event.deltaY : touchStart[1] - touch[1];
      var currentAxis;
      var target = event.target;
      var moveDirection = Math.abs(deltaX) > Math.abs(deltaY) ? "h" : "v";
      if ("touches" in event && moveDirection === "h" && target.type === "range") {
        return false;
      }
      var selection = window.getSelection();
      var anchorNode = selection && selection.anchorNode;
      var isTouchingSelection = anchorNode ? anchorNode === target || anchorNode.contains(target) : false;
      if (isTouchingSelection) {
        return false;
      }
      var canBeScrolledInMainDirection = locationCouldBeScrolled(moveDirection, target);
      if (!canBeScrolledInMainDirection) {
        return true;
      }
      if (canBeScrolledInMainDirection) {
        currentAxis = moveDirection;
      } else {
        currentAxis = moveDirection === "v" ? "h" : "v";
        canBeScrolledInMainDirection = locationCouldBeScrolled(moveDirection, target);
      }
      if (!canBeScrolledInMainDirection) {
        return false;
      }
      if (!activeAxis.current && "changedTouches" in event && (deltaX || deltaY)) {
        activeAxis.current = currentAxis;
      }
      if (!currentAxis) {
        return true;
      }
      var cancelingAxis = activeAxis.current || currentAxis;
      return handleScroll(cancelingAxis, parent, event, cancelingAxis === "h" ? deltaX : deltaY, true);
    }, []);
    var shouldPrevent = React19.useCallback(function(_event) {
      var event = _event;
      if (!lockStack.length || lockStack[lockStack.length - 1] !== Style2) {
        return;
      }
      var delta = "deltaY" in event ? getDeltaXY(event) : getTouchXY(event);
      var sourceEvent = shouldPreventQueue.current.filter(function(e) {
        return e.name === event.type && (e.target === event.target || event.target === e.shadowParent) && deltaCompare(e.delta, delta);
      })[0];
      if (sourceEvent && sourceEvent.should) {
        if (event.cancelable) {
          event.preventDefault();
        }
        return;
      }
      if (!sourceEvent) {
        var shardNodes = (lastProps.current.shards || []).map(extractRef).filter(Boolean).filter(function(node) {
          return node.contains(event.target);
        });
        var shouldStop = shardNodes.length > 0 ? shouldCancelEvent(event, shardNodes[0]) : !lastProps.current.noIsolation;
        if (shouldStop) {
          if (event.cancelable) {
            event.preventDefault();
          }
        }
      }
    }, []);
    var shouldCancel = React19.useCallback(function(name, delta, target, should) {
      var event = { name, delta, target, should, shadowParent: getOutermostShadowParent(target) };
      shouldPreventQueue.current.push(event);
      setTimeout(function() {
        shouldPreventQueue.current = shouldPreventQueue.current.filter(function(e) {
          return e !== event;
        });
      }, 1);
    }, []);
    var scrollTouchStart = React19.useCallback(function(event) {
      touchStartRef.current = getTouchXY(event);
      activeAxis.current = void 0;
    }, []);
    var scrollWheel = React19.useCallback(function(event) {
      shouldCancel(event.type, getDeltaXY(event), event.target, shouldCancelEvent(event, props.lockRef.current));
    }, []);
    var scrollTouchMove = React19.useCallback(function(event) {
      shouldCancel(event.type, getTouchXY(event), event.target, shouldCancelEvent(event, props.lockRef.current));
    }, []);
    React19.useEffect(function() {
      lockStack.push(Style2);
      props.setCallbacks({
        onScrollCapture: scrollWheel,
        onWheelCapture: scrollWheel,
        onTouchMoveCapture: scrollTouchMove
      });
      document.addEventListener("wheel", shouldPrevent, nonPassive);
      document.addEventListener("touchmove", shouldPrevent, nonPassive);
      document.addEventListener("touchstart", scrollTouchStart, nonPassive);
      return function() {
        lockStack = lockStack.filter(function(inst) {
          return inst !== Style2;
        });
        document.removeEventListener("wheel", shouldPrevent, nonPassive);
        document.removeEventListener("touchmove", shouldPrevent, nonPassive);
        document.removeEventListener("touchstart", scrollTouchStart, nonPassive);
      };
    }, []);
    var removeScrollBar = props.removeScrollBar, inert = props.inert;
    return React19.createElement(
      React19.Fragment,
      null,
      inert ? React19.createElement(Style2, { styles: generateStyle(id) }) : null,
      removeScrollBar ? React19.createElement(RemoveScrollBar, { noRelative: props.noRelative, gapMode: props.gapMode }) : null
    );
  }
  function getOutermostShadowParent(node) {
    var shadowParent = null;
    while (node !== null) {
      if (node instanceof ShadowRoot) {
        shadowParent = node.host;
        node = node.host;
      }
      node = node.parentNode;
    }
    return shadowParent;
  }

  // node_modules/react-remove-scroll/dist/es2015/sidecar.js
  var sidecar_default = exportSidecar(effectCar, RemoveScrollSideCar);

  // node_modules/react-remove-scroll/dist/es2015/Combination.js
  var ReactRemoveScroll = React20.forwardRef(function(props, ref) {
    return React20.createElement(RemoveScroll, __assign({}, props, { ref, sideCar: sidecar_default }));
  });
  ReactRemoveScroll.classNames = RemoveScroll.classNames;
  var Combination_default = ReactRemoveScroll;

  // node_modules/aria-hidden/dist/es2015/index.js
  var getDefaultParent = function(originalTarget) {
    if (typeof document === "undefined") {
      return null;
    }
    var sampleTarget = Array.isArray(originalTarget) ? originalTarget[0] : originalTarget;
    return sampleTarget.ownerDocument.body;
  };
  var counterMap = /* @__PURE__ */ new WeakMap();
  var uncontrolledNodes = /* @__PURE__ */ new WeakMap();
  var markerMap = {};
  var lockCount = 0;
  var unwrapHost = function(node) {
    return node && (node.host || unwrapHost(node.parentNode));
  };
  var correctTargets = function(parent, targets) {
    return targets.map(function(target) {
      if (parent.contains(target)) {
        return target;
      }
      var correctedTarget = unwrapHost(target);
      if (correctedTarget && parent.contains(correctedTarget)) {
        return correctedTarget;
      }
      console.error("aria-hidden", target, "in not contained inside", parent, ". Doing nothing");
      return null;
    }).filter(function(x) {
      return Boolean(x);
    });
  };
  var applyAttributeToOthers = function(originalTarget, parentNode, markerName, controlAttribute) {
    var targets = correctTargets(parentNode, Array.isArray(originalTarget) ? originalTarget : [originalTarget]);
    if (!markerMap[markerName]) {
      markerMap[markerName] = /* @__PURE__ */ new WeakMap();
    }
    var markerCounter = markerMap[markerName];
    var hiddenNodes = [];
    var elementsToKeep = /* @__PURE__ */ new Set();
    var elementsToStop = new Set(targets);
    var keep = function(el) {
      if (!el || elementsToKeep.has(el)) {
        return;
      }
      elementsToKeep.add(el);
      keep(el.parentNode);
    };
    targets.forEach(keep);
    var deep = function(parent) {
      if (!parent || elementsToStop.has(parent)) {
        return;
      }
      Array.prototype.forEach.call(parent.children, function(node) {
        if (elementsToKeep.has(node)) {
          deep(node);
        } else {
          try {
            var attr = node.getAttribute(controlAttribute);
            var alreadyHidden = attr !== null && attr !== "false";
            var counterValue = (counterMap.get(node) || 0) + 1;
            var markerValue = (markerCounter.get(node) || 0) + 1;
            counterMap.set(node, counterValue);
            markerCounter.set(node, markerValue);
            hiddenNodes.push(node);
            if (counterValue === 1 && alreadyHidden) {
              uncontrolledNodes.set(node, true);
            }
            if (markerValue === 1) {
              node.setAttribute(markerName, "true");
            }
            if (!alreadyHidden) {
              node.setAttribute(controlAttribute, "true");
            }
          } catch (e) {
            console.error("aria-hidden: cannot operate on ", node, e);
          }
        }
      });
    };
    deep(parentNode);
    elementsToKeep.clear();
    lockCount++;
    return function() {
      hiddenNodes.forEach(function(node) {
        var counterValue = counterMap.get(node) - 1;
        var markerValue = markerCounter.get(node) - 1;
        counterMap.set(node, counterValue);
        markerCounter.set(node, markerValue);
        if (!counterValue) {
          if (!uncontrolledNodes.has(node)) {
            node.removeAttribute(controlAttribute);
          }
          uncontrolledNodes.delete(node);
        }
        if (!markerValue) {
          node.removeAttribute(markerName);
        }
      });
      lockCount--;
      if (!lockCount) {
        counterMap = /* @__PURE__ */ new WeakMap();
        counterMap = /* @__PURE__ */ new WeakMap();
        uncontrolledNodes = /* @__PURE__ */ new WeakMap();
        markerMap = {};
      }
    };
  };
  var hideOthers = function(originalTarget, parentNode, markerName) {
    if (markerName === void 0) {
      markerName = "data-aria-hidden";
    }
    var targets = Array.from(Array.isArray(originalTarget) ? originalTarget : [originalTarget]);
    var activeParentNode = parentNode || getDefaultParent(originalTarget);
    if (!activeParentNode) {
      return function() {
        return null;
      };
    }
    targets.push.apply(targets, Array.from(activeParentNode.querySelectorAll("[aria-live], script")));
    return applyAttributeToOthers(targets, activeParentNode, markerName, "aria-hidden");
  };

  // node_modules/@radix-ui/react-dialog/dist/index.mjs
  var import_jsx_runtime7 = __toESM(require_jsx_runtime(), 1);
  var DIALOG_NAME = "Dialog";
  var [createDialogContext, createDialogScope] = createContextScope(DIALOG_NAME);
  var [DialogProvider, useDialogContext] = createDialogContext(DIALOG_NAME);
  var Dialog = (props) => {
    const {
      __scopeDialog,
      children,
      open: openProp,
      defaultOpen,
      onOpenChange,
      modal = true
    } = props;
    const triggerRef = React21.useRef(null);
    const contentRef = React21.useRef(null);
    const [open, setOpen] = useControllableState({
      prop: openProp,
      defaultProp: defaultOpen ?? false,
      onChange: onOpenChange,
      caller: DIALOG_NAME
    });
    return /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(
      DialogProvider,
      {
        scope: __scopeDialog,
        triggerRef,
        contentRef,
        contentId: useId(),
        titleId: useId(),
        descriptionId: useId(),
        open,
        onOpenChange: setOpen,
        onOpenToggle: React21.useCallback(() => setOpen((prevOpen) => !prevOpen), [setOpen]),
        modal,
        children
      }
    );
  };
  Dialog.displayName = DIALOG_NAME;
  var TRIGGER_NAME = "DialogTrigger";
  var DialogTrigger = React21.forwardRef(
    (props, forwardedRef) => {
      const { __scopeDialog, ...triggerProps } = props;
      const context = useDialogContext(TRIGGER_NAME, __scopeDialog);
      const composedTriggerRef = useComposedRefs(forwardedRef, context.triggerRef);
      return /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(
        Primitive.button,
        {
          type: "button",
          "aria-haspopup": "dialog",
          "aria-expanded": context.open,
          "aria-controls": context.open ? context.contentId : void 0,
          "data-state": getState(context.open),
          ...triggerProps,
          ref: composedTriggerRef,
          onClick: composeEventHandlers(props.onClick, context.onOpenToggle)
        }
      );
    }
  );
  DialogTrigger.displayName = TRIGGER_NAME;
  var PORTAL_NAME2 = "DialogPortal";
  var [PortalProvider, usePortalContext] = createDialogContext(PORTAL_NAME2, {
    forceMount: void 0
  });
  var DialogPortal = (props) => {
    const { __scopeDialog, forceMount, children, container } = props;
    const context = useDialogContext(PORTAL_NAME2, __scopeDialog);
    return /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(PortalProvider, { scope: __scopeDialog, forceMount, children: React21.Children.map(children, (child) => /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(Presence, { present: forceMount || context.open, children: /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(Portal, { asChild: true, container, children: child }) })) });
  };
  DialogPortal.displayName = PORTAL_NAME2;
  var OVERLAY_NAME = "DialogOverlay";
  var DialogOverlay = React21.forwardRef(
    (props, forwardedRef) => {
      const portalContext = usePortalContext(OVERLAY_NAME, props.__scopeDialog);
      const { forceMount = portalContext.forceMount, ...overlayProps } = props;
      const context = useDialogContext(OVERLAY_NAME, props.__scopeDialog);
      return context.modal ? /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(Presence, { present: forceMount || context.open, children: /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(DialogOverlayImpl, { ...overlayProps, ref: forwardedRef }) }) : null;
    }
  );
  DialogOverlay.displayName = OVERLAY_NAME;
  var Slot = createSlot("DialogOverlay.RemoveScroll");
  var DialogOverlayImpl = React21.forwardRef(
    (props, forwardedRef) => {
      const { __scopeDialog, ...overlayProps } = props;
      const context = useDialogContext(OVERLAY_NAME, __scopeDialog);
      const registerDismissableSurface = useDismissableLayerSurface();
      const composedRefs = useComposedRefs(forwardedRef, registerDismissableSurface);
      return (
        // Make sure `Content` is scrollable even when it doesn't live inside `RemoveScroll`
        // ie. when `Overlay` and `Content` are siblings
        /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(Combination_default, { as: Slot, allowPinchZoom: true, shards: [context.contentRef], children: /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(
          Primitive.div,
          {
            "data-state": getState(context.open),
            ...overlayProps,
            ref: composedRefs,
            style: { pointerEvents: "auto", ...overlayProps.style }
          }
        ) })
      );
    }
  );
  var CONTENT_NAME = "DialogContent";
  var DialogContent = React21.forwardRef(
    (props, forwardedRef) => {
      const portalContext = usePortalContext(CONTENT_NAME, props.__scopeDialog);
      const { forceMount = portalContext.forceMount, ...contentProps } = props;
      const context = useDialogContext(CONTENT_NAME, props.__scopeDialog);
      return /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(Presence, { present: forceMount || context.open, children: context.modal ? /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(DialogContentModal, { ...contentProps, ref: forwardedRef }) : /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(DialogContentNonModal, { ...contentProps, ref: forwardedRef }) });
    }
  );
  DialogContent.displayName = CONTENT_NAME;
  var DialogContentModal = React21.forwardRef(
    (props, forwardedRef) => {
      const context = useDialogContext(CONTENT_NAME, props.__scopeDialog);
      const contentRef = React21.useRef(null);
      const composedRefs = useComposedRefs(forwardedRef, context.contentRef, contentRef);
      React21.useEffect(() => {
        const content = contentRef.current;
        if (content) return hideOthers(content);
      }, []);
      return /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(
        DialogContentImpl,
        {
          ...props,
          ref: composedRefs,
          trapFocus: context.open,
          disableOutsidePointerEvents: context.open,
          onCloseAutoFocus: composeEventHandlers(props.onCloseAutoFocus, (event) => {
            event.preventDefault();
            context.triggerRef.current?.focus();
          }),
          onPointerDownOutside: composeEventHandlers(props.onPointerDownOutside, (event) => {
            const originalEvent = event.detail.originalEvent;
            const ctrlLeftClick = originalEvent.button === 0 && originalEvent.ctrlKey === true;
            const isRightClick = originalEvent.button === 2 || ctrlLeftClick;
            if (isRightClick) event.preventDefault();
          }),
          onFocusOutside: composeEventHandlers(
            props.onFocusOutside,
            (event) => event.preventDefault()
          )
        }
      );
    }
  );
  var DialogContentNonModal = React21.forwardRef(
    (props, forwardedRef) => {
      const context = useDialogContext(CONTENT_NAME, props.__scopeDialog);
      const hasInteractedOutsideRef = React21.useRef(false);
      const hasPointerDownOutsideRef = React21.useRef(false);
      return /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(
        DialogContentImpl,
        {
          ...props,
          ref: forwardedRef,
          trapFocus: false,
          disableOutsidePointerEvents: false,
          onCloseAutoFocus: (event) => {
            props.onCloseAutoFocus?.(event);
            if (!event.defaultPrevented) {
              if (!hasInteractedOutsideRef.current) context.triggerRef.current?.focus();
              event.preventDefault();
            }
            hasInteractedOutsideRef.current = false;
            hasPointerDownOutsideRef.current = false;
          },
          onInteractOutside: (event) => {
            props.onInteractOutside?.(event);
            if (!event.defaultPrevented) {
              hasInteractedOutsideRef.current = true;
              if (event.detail.originalEvent.type === "pointerdown") {
                hasPointerDownOutsideRef.current = true;
              }
            }
            const target = event.target;
            const targetIsTrigger = context.triggerRef.current?.contains(target);
            if (targetIsTrigger) event.preventDefault();
            if (event.detail.originalEvent.type === "focusin" && hasPointerDownOutsideRef.current) {
              event.preventDefault();
            }
          }
        }
      );
    }
  );
  var DialogContentImpl = React21.forwardRef(
    (props, forwardedRef) => {
      const { __scopeDialog, trapFocus, onOpenAutoFocus, onCloseAutoFocus, ...contentProps } = props;
      const context = useDialogContext(CONTENT_NAME, __scopeDialog);
      useFocusGuards();
      return /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(import_jsx_runtime7.Fragment, { children: /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(
        FocusScope,
        {
          asChild: true,
          loop: true,
          trapped: trapFocus,
          onMountAutoFocus: onOpenAutoFocus,
          onUnmountAutoFocus: onCloseAutoFocus,
          children: /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(
            DismissableLayer,
            {
              role: "dialog",
              id: context.contentId,
              "aria-describedby": context.descriptionId,
              "aria-labelledby": context.titleId,
              "data-state": getState(context.open),
              ...contentProps,
              ref: forwardedRef,
              deferPointerDownOutside: true,
              onDismiss: () => context.onOpenChange(false)
            }
          )
        }
      ) });
    }
  );
  var TITLE_NAME = "DialogTitle";
  var DialogTitle = React21.forwardRef(
    (props, forwardedRef) => {
      const { __scopeDialog, ...titleProps } = props;
      const context = useDialogContext(TITLE_NAME, __scopeDialog);
      return /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(Primitive.h2, { id: context.titleId, ...titleProps, ref: forwardedRef });
    }
  );
  DialogTitle.displayName = TITLE_NAME;
  var DESCRIPTION_NAME = "DialogDescription";
  var DialogDescription = React21.forwardRef(
    (props, forwardedRef) => {
      const { __scopeDialog, ...descriptionProps } = props;
      const context = useDialogContext(DESCRIPTION_NAME, __scopeDialog);
      return /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(Primitive.p, { id: context.descriptionId, ...descriptionProps, ref: forwardedRef });
    }
  );
  DialogDescription.displayName = DESCRIPTION_NAME;
  var CLOSE_NAME = "DialogClose";
  var DialogClose = React21.forwardRef(
    (props, forwardedRef) => {
      const { __scopeDialog, ...closeProps } = props;
      const context = useDialogContext(CLOSE_NAME, __scopeDialog);
      return /* @__PURE__ */ (0, import_jsx_runtime7.jsx)(
        Primitive.button,
        {
          type: "button",
          ...closeProps,
          ref: forwardedRef,
          onClick: composeEventHandlers(props.onClick, () => context.onOpenChange(false))
        }
      );
    }
  );
  DialogClose.displayName = CLOSE_NAME;
  function getState(open) {
    return open ? "open" : "closed";
  }

  // src/web-ui/react/ui/dialog.tsx
  var import_react4 = __toESM(require_react(), 1);

  // src/web-ui/react/ui/portal-context.tsx
  var import_react3 = __toESM(require_react(), 1);
  var import_jsx_runtime8 = __toESM(require_jsx_runtime(), 1);
  var PortalContainerContext = (0, import_react3.createContext)(null);
  function PortalContainerProvider({ container, children }) {
    return /* @__PURE__ */ (0, import_jsx_runtime8.jsx)(PortalContainerContext.Provider, { value: container, children });
  }
  function usePortalContainer() {
    return (0, import_react3.useContext)(PortalContainerContext) ?? void 0;
  }

  // src/web-ui/react/ui/dialog.tsx
  var import_jsx_runtime9 = __toESM(require_jsx_runtime(), 1);
  var defaultIcons = {
    info: "i",
    warning: "!",
    danger: "!",
    success: "\u2713",
    question: "?"
  };
  function WandDialogSurface({
    open,
    title,
    description,
    children,
    className = "wand-ui-dialog-content",
    overlayClassName = "wand-ui-dialog-overlay",
    titleClassName = "wand-ui-dialog-title",
    descriptionClassName = "wand-ui-dialog-description",
    headerClassName = "wand-ui-dialog-heading",
    closeLabel = "\u5173\u95ED",
    closeContent = "\xD7",
    testId,
    dismissable = true,
    onOpenChange
  }) {
    const portalContainer = usePortalContainer();
    const contentRef = (0, import_react4.useRef)(null);
    const previouslyFocusedRef = (0, import_react4.useRef)(null);
    return /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(
      Dialog,
      {
        open,
        onOpenChange: (nextOpen) => {
          if (nextOpen || dismissable) onOpenChange(nextOpen);
        },
        children: /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)(DialogPortal, { container: portalContainer, children: [
          /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(DialogOverlay, { className: overlayClassName }),
          /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)(
            DialogContent,
            {
              ref: contentRef,
              className,
              "data-testid": testId,
              ...description ? {} : { "aria-describedby": void 0 },
              onEscapeKeyDown: (event) => {
                if (!dismissable) event.preventDefault();
              },
              onInteractOutside: (event) => {
                if (!dismissable) event.preventDefault();
              },
              onOpenAutoFocus: (event) => {
                event.preventDefault();
                previouslyFocusedRef.current = document.activeElement instanceof HTMLElement ? document.activeElement : null;
                const target = contentRef.current?.querySelector("[data-wand-autofocus]") ?? contentRef.current?.querySelector("button, input, [tabindex='0']");
                target?.focus();
              },
              onCloseAutoFocus: (event) => {
                event.preventDefault();
                const previous = previouslyFocusedRef.current;
                previouslyFocusedRef.current = null;
                if (previous && document.contains(previous)) previous.focus();
              },
              children: [
                /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)("div", { className: headerClassName, children: [
                  /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)("div", { children: [
                    /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(DialogTitle, { className: titleClassName, children: title }),
                    description ? /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(DialogDescription, { className: descriptionClassName, children: description }) : null
                  ] }),
                  /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(DialogClose, { asChild: true, children: /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(WandButton, { kind: "ghost", "aria-label": closeLabel, disabled: !dismissable, children: closeContent }) })
                ] }),
                children
              ]
            }
          )
        ] })
      }
    );
  }
  function WandDialog({
    open,
    title,
    description,
    tone = "info",
    icon,
    actions,
    input,
    dismissable = true,
    onAction,
    onDismiss
  }) {
    const portalContainer = usePortalContainer();
    const inputRef = (0, import_react4.useRef)(null);
    const contentRef = (0, import_react4.useRef)(null);
    const previouslyFocusedRef = (0, import_react4.useRef)(
      document.activeElement instanceof HTMLElement ? document.activeElement : null
    );
    const [inputValue, setInputValue] = (0, import_react4.useState)(input?.value ?? "");
    const primaryAction = actions.find((action) => action.kind === "primary" || action.kind === "danger") ?? actions.at(-1);
    function submitPrimary(event) {
      if (event.key !== "Enter" || !primaryAction) return;
      event.preventDefault();
      onAction(primaryAction.value, inputValue);
    }
    return /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(
      Dialog,
      {
        open,
        onOpenChange: (nextOpen) => {
          if (!nextOpen && dismissable) onDismiss();
        },
        children: /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)(DialogPortal, { container: portalContainer, children: [
          /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(DialogOverlay, { className: "wand-ui-dialog-overlay" }),
          /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)(
            DialogContent,
            {
              ref: contentRef,
              className: "wand-ui-dialog-content",
              ...description ? {} : { "aria-describedby": void 0 },
              onEscapeKeyDown: (event) => {
                if (!dismissable) event.preventDefault();
              },
              onInteractOutside: (event) => {
                if (!dismissable) event.preventDefault();
              },
              onOpenAutoFocus: (event) => {
                event.preventDefault();
                if (inputRef.current) {
                  inputRef.current.focus();
                  inputRef.current.select();
                  return;
                }
                const target = contentRef.current?.querySelector("[data-wand-autofocus]") ?? contentRef.current?.querySelector("button");
                target?.focus();
              },
              onCloseAutoFocus: (event) => {
                event.preventDefault();
                const previous = previouslyFocusedRef.current;
                if (previous && document.contains(previous)) previous.focus();
              },
              children: [
                /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)("div", { className: "wand-ui-dialog-header", children: [
                  /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(
                    "div",
                    {
                      "aria-hidden": "true",
                      className: classNames("wand-ui-dialog-icon", `wand-ui-dialog-icon-${tone}`),
                      children: icon ?? defaultIcons[tone]
                    }
                  ),
                  /* @__PURE__ */ (0, import_jsx_runtime9.jsxs)("div", { className: "wand-ui-dialog-heading", children: [
                    /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(DialogTitle, { className: "wand-ui-dialog-title", children: title }),
                    description ? /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(DialogDescription, { className: "wand-ui-dialog-description", children: description }) : null
                  ] })
                ] }),
                input ? /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("div", { className: "wand-ui-dialog-body", children: /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(
                  "input",
                  {
                    ref: inputRef,
                    className: "wand-ui-dialog-input",
                    type: "text",
                    "aria-label": input.label ?? title,
                    autoComplete: "off",
                    spellCheck: false,
                    placeholder: input.placeholder,
                    value: inputValue,
                    onChange: (event) => setInputValue(event.currentTarget.value),
                    onKeyDown: submitPrimary
                  }
                ) }) : null,
                /* @__PURE__ */ (0, import_jsx_runtime9.jsx)("div", { className: "wand-ui-dialog-actions", children: actions.map((action, index2) => /* @__PURE__ */ (0, import_jsx_runtime9.jsx)(
                  WandButton,
                  {
                    kind: action.kind,
                    "data-wand-autofocus": action.autoFocus ? "true" : void 0,
                    onClick: () => onAction(action.value, input ? inputValue : void 0),
                    children: action.label
                  },
                  `${action.label}-${index2}`
                )) })
              ]
            }
          )
        ] })
      }
    );
  }

  // node_modules/@radix-ui/react-popover/dist/index.mjs
  var React28 = __toESM(require_react(), 1);

  // node_modules/@radix-ui/react-popper/dist/index.mjs
  var React27 = __toESM(require_react(), 1);

  // node_modules/@floating-ui/utils/dist/floating-ui.utils.mjs
  var sides = ["top", "right", "bottom", "left"];
  var min = Math.min;
  var max = Math.max;
  var round = Math.round;
  var floor = Math.floor;
  var createCoords = (v) => ({
    x: v,
    y: v
  });
  var oppositeSideMap = {
    left: "right",
    right: "left",
    bottom: "top",
    top: "bottom"
  };
  function clamp(start, value, end) {
    return max(start, min(value, end));
  }
  function evaluate(value, param) {
    return typeof value === "function" ? value(param) : value;
  }
  function getSide(placement) {
    return placement.split("-")[0];
  }
  function getAlignment(placement) {
    return placement.split("-")[1];
  }
  function getOppositeAxis(axis) {
    return axis === "x" ? "y" : "x";
  }
  function getAxisLength(axis) {
    return axis === "y" ? "height" : "width";
  }
  function getSideAxis(placement) {
    const firstChar = placement[0];
    return firstChar === "t" || firstChar === "b" ? "y" : "x";
  }
  function getAlignmentAxis(placement) {
    return getOppositeAxis(getSideAxis(placement));
  }
  function getAlignmentSides(placement, rects, rtl) {
    if (rtl === void 0) {
      rtl = false;
    }
    const alignment = getAlignment(placement);
    const alignmentAxis = getAlignmentAxis(placement);
    const length = getAxisLength(alignmentAxis);
    let mainAlignmentSide = alignmentAxis === "x" ? alignment === (rtl ? "end" : "start") ? "right" : "left" : alignment === "start" ? "bottom" : "top";
    if (rects.reference[length] > rects.floating[length]) {
      mainAlignmentSide = getOppositePlacement(mainAlignmentSide);
    }
    return [mainAlignmentSide, getOppositePlacement(mainAlignmentSide)];
  }
  function getExpandedPlacements(placement) {
    const oppositePlacement = getOppositePlacement(placement);
    return [getOppositeAlignmentPlacement(placement), oppositePlacement, getOppositeAlignmentPlacement(oppositePlacement)];
  }
  function getOppositeAlignmentPlacement(placement) {
    return placement.includes("start") ? placement.replace("start", "end") : placement.replace("end", "start");
  }
  var lrPlacement = ["left", "right"];
  var rlPlacement = ["right", "left"];
  var tbPlacement = ["top", "bottom"];
  var btPlacement = ["bottom", "top"];
  function getSideList(side, isStart, rtl) {
    switch (side) {
      case "top":
      case "bottom":
        if (rtl) return isStart ? rlPlacement : lrPlacement;
        return isStart ? lrPlacement : rlPlacement;
      case "left":
      case "right":
        return isStart ? tbPlacement : btPlacement;
      default:
        return [];
    }
  }
  function getOppositeAxisPlacements(placement, flipAlignment, direction, rtl) {
    const alignment = getAlignment(placement);
    let list = getSideList(getSide(placement), direction === "start", rtl);
    if (alignment) {
      list = list.map((side) => side + "-" + alignment);
      if (flipAlignment) {
        list = list.concat(list.map(getOppositeAlignmentPlacement));
      }
    }
    return list;
  }
  function getOppositePlacement(placement) {
    const side = getSide(placement);
    return oppositeSideMap[side] + placement.slice(side.length);
  }
  function expandPaddingObject(padding) {
    var _padding$top, _padding$right, _padding$bottom, _padding$left;
    return {
      top: (_padding$top = padding.top) != null ? _padding$top : 0,
      right: (_padding$right = padding.right) != null ? _padding$right : 0,
      bottom: (_padding$bottom = padding.bottom) != null ? _padding$bottom : 0,
      left: (_padding$left = padding.left) != null ? _padding$left : 0
    };
  }
  function getPaddingObject(padding) {
    return typeof padding !== "number" ? expandPaddingObject(padding) : {
      top: padding,
      right: padding,
      bottom: padding,
      left: padding
    };
  }
  function rectToClientRect(rect) {
    const {
      x,
      y,
      width,
      height
    } = rect;
    return {
      width,
      height,
      top: y,
      left: x,
      right: x + width,
      bottom: y + height,
      x,
      y
    };
  }

  // node_modules/@floating-ui/core/dist/floating-ui.core.mjs
  function computeCoordsFromPlacement(_ref, placement, rtl) {
    let {
      reference,
      floating
    } = _ref;
    const sideAxis = getSideAxis(placement);
    const alignmentAxis = getAlignmentAxis(placement);
    const alignLength = getAxisLength(alignmentAxis);
    const side = getSide(placement);
    const isVertical = sideAxis === "y";
    const commonX = reference.x + reference.width / 2 - floating.width / 2;
    const commonY = reference.y + reference.height / 2 - floating.height / 2;
    const commonAlign = reference[alignLength] / 2 - floating[alignLength] / 2;
    let coords;
    switch (side) {
      case "top":
        coords = {
          x: commonX,
          y: reference.y - floating.height
        };
        break;
      case "bottom":
        coords = {
          x: commonX,
          y: reference.y + reference.height
        };
        break;
      case "right":
        coords = {
          x: reference.x + reference.width,
          y: commonY
        };
        break;
      case "left":
        coords = {
          x: reference.x - floating.width,
          y: commonY
        };
        break;
      default:
        coords = {
          x: reference.x,
          y: reference.y
        };
    }
    const alignment = getAlignment(placement);
    if (alignment) {
      coords[alignmentAxis] += commonAlign * (alignment === "end" ? 1 : -1) * (rtl && isVertical ? -1 : 1);
    }
    return coords;
  }
  async function detectOverflow(state2, options) {
    var _await$platform$isEle;
    if (options === void 0) {
      options = {};
    }
    const {
      x,
      y,
      platform: platform2,
      rects,
      elements,
      strategy
    } = state2;
    const {
      boundary = "clippingAncestors",
      rootBoundary = "viewport",
      elementContext = "floating",
      altBoundary = false,
      padding = 0
    } = evaluate(options, state2);
    const paddingObject = getPaddingObject(padding);
    const altContext = elementContext === "floating" ? "reference" : "floating";
    const element = elements[altBoundary ? altContext : elementContext];
    const clippingClientRect = rectToClientRect(await platform2.getClippingRect({
      element: ((_await$platform$isEle = await (platform2.isElement == null ? void 0 : platform2.isElement(element))) != null ? _await$platform$isEle : true) ? element : element.contextElement || await (platform2.getDocumentElement == null ? void 0 : platform2.getDocumentElement(elements.floating)),
      boundary,
      rootBoundary,
      strategy
    }));
    const rect = elementContext === "floating" ? {
      x,
      y,
      width: rects.floating.width,
      height: rects.floating.height
    } : rects.reference;
    const offsetParent = await (platform2.getOffsetParent == null ? void 0 : platform2.getOffsetParent(elements.floating));
    const offsetScale = await (platform2.isElement == null ? void 0 : platform2.isElement(offsetParent)) && await (platform2.getScale == null ? void 0 : platform2.getScale(offsetParent)) || {
      x: 1,
      y: 1
    };
    const elementClientRect = rectToClientRect(platform2.convertOffsetParentRelativeRectToViewportRelativeRect ? await platform2.convertOffsetParentRelativeRectToViewportRelativeRect({
      elements,
      rect,
      offsetParent,
      strategy
    }) : rect);
    return {
      top: (clippingClientRect.top - elementClientRect.top + paddingObject.top) / offsetScale.y,
      bottom: (elementClientRect.bottom - clippingClientRect.bottom + paddingObject.bottom) / offsetScale.y,
      left: (clippingClientRect.left - elementClientRect.left + paddingObject.left) / offsetScale.x,
      right: (elementClientRect.right - clippingClientRect.right + paddingObject.right) / offsetScale.x
    };
  }
  var MAX_RESET_COUNT = 50;
  var computePosition = async (reference, floating, config) => {
    const {
      placement = "bottom",
      strategy = "absolute",
      middleware = [],
      platform: platform2
    } = config;
    const platformWithDetectOverflow = platform2.detectOverflow ? platform2 : {
      ...platform2,
      detectOverflow
    };
    const rtl = await (platform2.isRTL == null ? void 0 : platform2.isRTL(floating));
    let rects = await platform2.getElementRects({
      reference,
      floating,
      strategy
    });
    let {
      x,
      y
    } = computeCoordsFromPlacement(rects, placement, rtl);
    let statefulPlacement = placement;
    let resetCount = 0;
    const middlewareData = {};
    for (let i = 0; i < middleware.length; i++) {
      const currentMiddleware = middleware[i];
      if (!currentMiddleware) {
        continue;
      }
      const {
        name,
        fn
      } = currentMiddleware;
      const {
        x: nextX,
        y: nextY,
        data,
        reset
      } = await fn({
        x,
        y,
        initialPlacement: placement,
        placement: statefulPlacement,
        strategy,
        middlewareData,
        rects,
        platform: platformWithDetectOverflow,
        elements: {
          reference,
          floating
        }
      });
      x = nextX != null ? nextX : x;
      y = nextY != null ? nextY : y;
      middlewareData[name] = {
        ...middlewareData[name],
        ...data
      };
      if (reset && resetCount < MAX_RESET_COUNT) {
        resetCount++;
        if (typeof reset === "object") {
          if (reset.placement) {
            statefulPlacement = reset.placement;
          }
          if (reset.rects) {
            rects = reset.rects === true ? await platform2.getElementRects({
              reference,
              floating,
              strategy
            }) : reset.rects;
          }
          ({
            x,
            y
          } = computeCoordsFromPlacement(rects, statefulPlacement, rtl));
        }
        i = -1;
      }
    }
    return {
      x,
      y,
      placement: statefulPlacement,
      strategy,
      middlewareData
    };
  };
  var arrow = (options) => ({
    name: "arrow",
    options,
    async fn(state2) {
      const {
        x,
        y,
        placement,
        rects,
        platform: platform2,
        elements,
        middlewareData
      } = state2;
      const {
        element,
        padding = 0
      } = evaluate(options, state2) || {};
      if (element == null) {
        return {};
      }
      const paddingObject = getPaddingObject(padding);
      const coords = {
        x,
        y
      };
      const axis = getAlignmentAxis(placement);
      const length = getAxisLength(axis);
      const arrowDimensions = await platform2.getDimensions(element);
      const isYAxis = axis === "y";
      const minProp = isYAxis ? "top" : "left";
      const maxProp = isYAxis ? "bottom" : "right";
      const clientProp = isYAxis ? "clientHeight" : "clientWidth";
      const endDiff = rects.reference[length] + rects.reference[axis] - coords[axis] - rects.floating[length];
      const startDiff = coords[axis] - rects.reference[axis];
      const arrowOffsetParent = await (platform2.getOffsetParent == null ? void 0 : platform2.getOffsetParent(element));
      let clientSize = arrowOffsetParent ? arrowOffsetParent[clientProp] : 0;
      if (!clientSize || !await (platform2.isElement == null ? void 0 : platform2.isElement(arrowOffsetParent))) {
        clientSize = elements.floating[clientProp] || rects.floating[length];
      }
      const centerToReference = endDiff / 2 - startDiff / 2;
      const largestPossiblePadding = clientSize / 2 - arrowDimensions[length] / 2 - 1;
      const minPadding = min(paddingObject[minProp], largestPossiblePadding);
      const maxPadding = min(paddingObject[maxProp], largestPossiblePadding);
      const max2 = clientSize - arrowDimensions[length] - maxPadding;
      const center = clientSize / 2 - arrowDimensions[length] / 2 + centerToReference;
      const offset4 = clamp(minPadding, center, max2);
      const shouldAddOffset = !middlewareData.arrow && getAlignment(placement) != null && center !== offset4 && rects.reference[length] / 2 - (center < minPadding ? minPadding : maxPadding) - arrowDimensions[length] / 2 < 0;
      const alignmentOffset = shouldAddOffset ? center < minPadding ? center - minPadding : center - max2 : 0;
      return {
        [axis]: coords[axis] + alignmentOffset,
        data: {
          [axis]: offset4,
          centerOffset: center - offset4 - alignmentOffset,
          ...shouldAddOffset && {
            alignmentOffset
          }
        },
        reset: shouldAddOffset
      };
    }
  });
  var flip = function(options) {
    if (options === void 0) {
      options = {};
    }
    return {
      name: "flip",
      options,
      async fn(state2) {
        var _middlewareData$arrow, _middlewareData$flip;
        const {
          placement,
          middlewareData,
          rects,
          initialPlacement,
          platform: platform2,
          elements
        } = state2;
        const {
          mainAxis: checkMainAxis = true,
          crossAxis: checkCrossAxis = true,
          fallbackPlacements: specifiedFallbackPlacements,
          fallbackStrategy = "bestFit",
          fallbackAxisSideDirection = "none",
          flipAlignment = true,
          ...detectOverflowOptions
        } = evaluate(options, state2);
        if ((_middlewareData$arrow = middlewareData.arrow) != null && _middlewareData$arrow.alignmentOffset) {
          return {};
        }
        const side = getSide(placement);
        const initialSideAxis = getSideAxis(initialPlacement);
        const isBasePlacement = getSide(initialPlacement) === initialPlacement;
        const rtl = await (platform2.isRTL == null ? void 0 : platform2.isRTL(elements.floating));
        const fallbackPlacements = specifiedFallbackPlacements || (isBasePlacement || !flipAlignment ? [getOppositePlacement(initialPlacement)] : getExpandedPlacements(initialPlacement));
        const hasFallbackAxisSideDirection = fallbackAxisSideDirection !== "none";
        if (!specifiedFallbackPlacements && hasFallbackAxisSideDirection) {
          fallbackPlacements.push(...getOppositeAxisPlacements(initialPlacement, flipAlignment, fallbackAxisSideDirection, rtl));
        }
        const placements2 = [initialPlacement, ...fallbackPlacements];
        const overflow = await platform2.detectOverflow(state2, detectOverflowOptions);
        const overflows = [];
        let overflowsData = ((_middlewareData$flip = middlewareData.flip) == null ? void 0 : _middlewareData$flip.overflows) || [];
        if (checkMainAxis) {
          overflows.push(overflow[side]);
        }
        if (checkCrossAxis) {
          const sides2 = getAlignmentSides(placement, rects, rtl);
          overflows.push(overflow[sides2[0]], overflow[sides2[1]]);
        }
        overflowsData = [...overflowsData, {
          placement,
          overflows
        }];
        if (!overflows.every((side2) => side2 <= 0)) {
          var _middlewareData$flip2, _overflowsData$filter;
          const nextIndex = (((_middlewareData$flip2 = middlewareData.flip) == null ? void 0 : _middlewareData$flip2.index) || 0) + 1;
          const nextPlacement = placements2[nextIndex];
          if (nextPlacement) {
            const ignoreCrossAxisOverflow = checkCrossAxis === "alignment" ? initialSideAxis !== getSideAxis(nextPlacement) : false;
            if (!ignoreCrossAxisOverflow || // We leave the current main axis only if every placement on that axis
            // overflows the main axis.
            overflowsData.every((d) => getSideAxis(d.placement) === initialSideAxis ? d.overflows[0] > 0 : true)) {
              return {
                data: {
                  index: nextIndex,
                  overflows: overflowsData
                },
                reset: {
                  placement: nextPlacement
                }
              };
            }
          }
          let resetPlacement = (_overflowsData$filter = overflowsData.filter((d) => d.overflows[0] <= 0).sort((a, b) => a.overflows[1] - b.overflows[1])[0]) == null ? void 0 : _overflowsData$filter.placement;
          if (!resetPlacement) {
            switch (fallbackStrategy) {
              case "bestFit": {
                var _overflowsData$filter2;
                const placement2 = (_overflowsData$filter2 = overflowsData.filter((d) => {
                  if (hasFallbackAxisSideDirection) {
                    const currentSideAxis = getSideAxis(d.placement);
                    return currentSideAxis === initialSideAxis || // Create a bias to the `y` side axis due to horizontal
                    // reading directions favoring greater width.
                    currentSideAxis === "y";
                  }
                  return true;
                }).map((d) => [d.placement, d.overflows.filter((overflow2) => overflow2 > 0).reduce((acc, overflow2) => acc + overflow2, 0)]).sort((a, b) => a[1] - b[1])[0]) == null ? void 0 : _overflowsData$filter2[0];
                if (placement2) {
                  resetPlacement = placement2;
                }
                break;
              }
              case "initialPlacement":
                resetPlacement = initialPlacement;
                break;
            }
          }
          if (placement !== resetPlacement) {
            return {
              reset: {
                placement: resetPlacement
              }
            };
          }
        }
        return {};
      }
    };
  };
  function getSideOffsets(overflow, rect) {
    return {
      top: overflow.top - rect.height,
      right: overflow.right - rect.width,
      bottom: overflow.bottom - rect.height,
      left: overflow.left - rect.width
    };
  }
  function isAnySideFullyClipped(overflow) {
    return sides.some((side) => overflow[side] >= 0);
  }
  var hide = function(options) {
    if (options === void 0) {
      options = {};
    }
    return {
      name: "hide",
      options,
      async fn(state2) {
        const {
          rects,
          platform: platform2
        } = state2;
        const {
          strategy = "referenceHidden",
          ...detectOverflowOptions
        } = evaluate(options, state2);
        switch (strategy) {
          case "referenceHidden": {
            const overflow = await platform2.detectOverflow(state2, {
              ...detectOverflowOptions,
              elementContext: "reference"
            });
            const offsets = getSideOffsets(overflow, rects.reference);
            return {
              data: {
                referenceHiddenOffsets: offsets,
                referenceHidden: isAnySideFullyClipped(offsets)
              }
            };
          }
          case "escaped": {
            const overflow = await platform2.detectOverflow(state2, {
              ...detectOverflowOptions,
              altBoundary: true
            });
            const offsets = getSideOffsets(overflow, rects.floating);
            return {
              data: {
                escapedOffsets: offsets,
                escaped: isAnySideFullyClipped(offsets)
              }
            };
          }
          default: {
            return {};
          }
        }
      }
    };
  };
  var originSides = /* @__PURE__ */ new Set(["left", "top"]);
  async function convertValueToCoords(state2, options) {
    const {
      placement,
      platform: platform2,
      elements
    } = state2;
    const rtl = await (platform2.isRTL == null ? void 0 : platform2.isRTL(elements.floating));
    const side = getSide(placement);
    const alignment = getAlignment(placement);
    const isVertical = getSideAxis(placement) === "y";
    const mainAxisMulti = originSides.has(side) ? -1 : 1;
    const crossAxisMulti = rtl && isVertical ? -1 : 1;
    const rawValue = evaluate(options, state2);
    let {
      mainAxis,
      crossAxis,
      alignmentAxis
    } = typeof rawValue === "number" ? {
      mainAxis: rawValue,
      crossAxis: 0,
      alignmentAxis: null
    } : {
      mainAxis: rawValue.mainAxis || 0,
      crossAxis: rawValue.crossAxis || 0,
      alignmentAxis: rawValue.alignmentAxis
    };
    if (alignment && typeof alignmentAxis === "number") {
      crossAxis = alignment === "end" ? alignmentAxis * -1 : alignmentAxis;
    }
    return isVertical ? {
      x: crossAxis * crossAxisMulti,
      y: mainAxis * mainAxisMulti
    } : {
      x: mainAxis * mainAxisMulti,
      y: crossAxis * crossAxisMulti
    };
  }
  var offset = function(options) {
    if (options === void 0) {
      options = 0;
    }
    return {
      name: "offset",
      options,
      async fn(state2) {
        var _middlewareData$offse, _middlewareData$arrow;
        const {
          x,
          y,
          placement,
          middlewareData
        } = state2;
        const diffCoords = await convertValueToCoords(state2, options);
        if (placement === ((_middlewareData$offse = middlewareData.offset) == null ? void 0 : _middlewareData$offse.placement) && (_middlewareData$arrow = middlewareData.arrow) != null && _middlewareData$arrow.alignmentOffset) {
          return {};
        }
        return {
          x: x + diffCoords.x,
          y: y + diffCoords.y,
          data: {
            ...diffCoords,
            placement
          }
        };
      }
    };
  };
  var shift = function(options) {
    if (options === void 0) {
      options = {};
    }
    return {
      name: "shift",
      options,
      async fn(state2) {
        const {
          x,
          y,
          placement,
          platform: platform2
        } = state2;
        const {
          mainAxis: checkMainAxis = true,
          crossAxis: checkCrossAxis = false,
          limiter = {
            fn: (_ref) => {
              let {
                x: x2,
                y: y2
              } = _ref;
              return {
                x: x2,
                y: y2
              };
            }
          },
          ...detectOverflowOptions
        } = evaluate(options, state2);
        const coords = {
          x,
          y
        };
        const overflow = await platform2.detectOverflow(state2, detectOverflowOptions);
        const crossAxis = getSideAxis(placement);
        const mainAxis = getOppositeAxis(crossAxis);
        let mainAxisCoord = coords[mainAxis];
        let crossAxisCoord = coords[crossAxis];
        const clampCoord = (axis, coord) => clamp(coord + overflow[axis === "y" ? "top" : "left"], coord, coord - overflow[axis === "y" ? "bottom" : "right"]);
        if (checkMainAxis) {
          mainAxisCoord = clampCoord(mainAxis, mainAxisCoord);
        }
        if (checkCrossAxis) {
          crossAxisCoord = clampCoord(crossAxis, crossAxisCoord);
        }
        const limitedCoords = limiter.fn({
          ...state2,
          [mainAxis]: mainAxisCoord,
          [crossAxis]: crossAxisCoord
        });
        return {
          ...limitedCoords,
          data: {
            x: limitedCoords.x - x,
            y: limitedCoords.y - y,
            enabled: {
              [mainAxis]: checkMainAxis,
              [crossAxis]: checkCrossAxis
            }
          }
        };
      }
    };
  };
  var limitShift = function(options) {
    if (options === void 0) {
      options = {};
    }
    return {
      options,
      fn(state2) {
        var _rawOffset$mainAxis, _rawOffset$crossAxis;
        const {
          x,
          y,
          placement,
          rects,
          middlewareData
        } = state2;
        const {
          offset: offset4 = 0,
          mainAxis: checkMainAxis = true,
          crossAxis: checkCrossAxis = true
        } = evaluate(options, state2);
        const coords = {
          x,
          y
        };
        const crossAxis = getSideAxis(placement);
        const mainAxis = getOppositeAxis(crossAxis);
        let mainAxisCoord = coords[mainAxis];
        let crossAxisCoord = coords[crossAxis];
        const rawOffset = evaluate(offset4, state2);
        const computedOffset = typeof rawOffset === "number" ? {
          mainAxis: rawOffset,
          crossAxis: 0
        } : {
          mainAxis: (_rawOffset$mainAxis = rawOffset.mainAxis) != null ? _rawOffset$mainAxis : 0,
          crossAxis: (_rawOffset$crossAxis = rawOffset.crossAxis) != null ? _rawOffset$crossAxis : 0
        };
        if (checkMainAxis) {
          const len = mainAxis === "y" ? "height" : "width";
          const limitMin = rects.reference[mainAxis] - rects.floating[len] + computedOffset.mainAxis;
          const limitMax = rects.reference[mainAxis] + rects.reference[len] - computedOffset.mainAxis;
          if (mainAxisCoord < limitMin) {
            mainAxisCoord = limitMin;
          } else if (mainAxisCoord > limitMax) {
            mainAxisCoord = limitMax;
          }
        }
        if (checkCrossAxis) {
          var _middlewareData$offse, _middlewareData$offse2;
          const len = mainAxis === "y" ? "width" : "height";
          const isOriginSide = originSides.has(getSide(placement));
          const limitMin = rects.reference[crossAxis] - rects.floating[len] + (isOriginSide ? ((_middlewareData$offse = middlewareData.offset) == null ? void 0 : _middlewareData$offse[crossAxis]) || 0 : 0) + (isOriginSide ? 0 : computedOffset.crossAxis);
          const limitMax = rects.reference[crossAxis] + rects.reference[len] + (isOriginSide ? 0 : ((_middlewareData$offse2 = middlewareData.offset) == null ? void 0 : _middlewareData$offse2[crossAxis]) || 0) - (isOriginSide ? computedOffset.crossAxis : 0);
          if (crossAxisCoord < limitMin) {
            crossAxisCoord = limitMin;
          } else if (crossAxisCoord > limitMax) {
            crossAxisCoord = limitMax;
          }
        }
        return {
          [mainAxis]: mainAxisCoord,
          [crossAxis]: crossAxisCoord
        };
      }
    };
  };
  var size = function(options) {
    if (options === void 0) {
      options = {};
    }
    return {
      name: "size",
      options,
      async fn(state2) {
        const {
          placement,
          rects,
          platform: platform2,
          elements
        } = state2;
        const {
          apply = () => {
          },
          ...detectOverflowOptions
        } = evaluate(options, state2);
        const overflow = await platform2.detectOverflow(state2, detectOverflowOptions);
        const side = getSide(placement);
        const alignment = getAlignment(placement);
        const isYAxis = getSideAxis(placement) === "y";
        const {
          width,
          height
        } = rects.floating;
        let heightSide;
        let widthSide;
        if (side === "top" || side === "bottom") {
          heightSide = side;
          widthSide = alignment === (await (platform2.isRTL == null ? void 0 : platform2.isRTL(elements.floating)) ? "start" : "end") ? "left" : "right";
        } else {
          widthSide = side;
          heightSide = alignment === "end" ? "top" : "bottom";
        }
        const maximumClippingHeight = height - overflow.top - overflow.bottom;
        const maximumClippingWidth = width - overflow.left - overflow.right;
        const overflowAvailableHeight = min(height - overflow[heightSide], maximumClippingHeight);
        const overflowAvailableWidth = min(width - overflow[widthSide], maximumClippingWidth);
        const shiftData = state2.middlewareData.shift;
        const noShift = !shiftData;
        let availableHeight = overflowAvailableHeight;
        let availableWidth = overflowAvailableWidth;
        if (shiftData != null && shiftData.enabled.x) {
          availableWidth = maximumClippingWidth;
        }
        if (shiftData != null && shiftData.enabled.y) {
          availableHeight = maximumClippingHeight;
        }
        if (noShift && !alignment) {
          if (isYAxis) {
            availableWidth = width - 2 * max(overflow.left, overflow.right);
          } else {
            availableHeight = height - 2 * max(overflow.top, overflow.bottom);
          }
        }
        await apply({
          ...state2,
          availableWidth,
          availableHeight
        });
        const nextDimensions = await platform2.getDimensions(elements.floating);
        if (width !== nextDimensions.width || height !== nextDimensions.height) {
          return {
            reset: {
              rects: true
            }
          };
        }
        return {};
      }
    };
  };

  // node_modules/@floating-ui/utils/dist/floating-ui.utils.dom.mjs
  function hasWindow() {
    return typeof window !== "undefined";
  }
  function getNodeName(node) {
    if (isNode(node)) {
      return (node.nodeName || "").toLowerCase();
    }
    return "#document";
  }
  function getWindow(node) {
    var _node$ownerDocument;
    return (node == null || (_node$ownerDocument = node.ownerDocument) == null ? void 0 : _node$ownerDocument.defaultView) || window;
  }
  function getDocumentElement(node) {
    var _ref;
    return (_ref = (isNode(node) ? node.ownerDocument : node.document) || window.document) == null ? void 0 : _ref.documentElement;
  }
  function isNode(value) {
    if (!hasWindow()) {
      return false;
    }
    return value instanceof Node || value instanceof getWindow(value).Node;
  }
  function isElement(value) {
    if (!hasWindow()) {
      return false;
    }
    return value instanceof Element || value instanceof getWindow(value).Element;
  }
  function isHTMLElement(value) {
    if (!hasWindow()) {
      return false;
    }
    return value instanceof HTMLElement || value instanceof getWindow(value).HTMLElement;
  }
  function isShadowRoot(value) {
    if (!hasWindow() || typeof ShadowRoot === "undefined") {
      return false;
    }
    return value instanceof ShadowRoot || value instanceof getWindow(value).ShadowRoot;
  }
  function isOverflowElement(element) {
    const {
      overflow,
      overflowX,
      overflowY,
      display
    } = getComputedStyle2(element);
    return /auto|scroll|overlay|hidden|clip/.test(overflow + overflowY + overflowX) && display !== "inline" && display !== "contents";
  }
  function isTableElement(element) {
    return /^(table|td|th)$/.test(getNodeName(element));
  }
  function isTopLayer(element) {
    try {
      if (element.matches(":popover-open")) {
        return true;
      }
    } catch (_e) {
    }
    try {
      return element.matches(":modal");
    } catch (_e) {
      return false;
    }
  }
  var willChangeRe = /transform|translate|scale|rotate|perspective|filter/;
  var containRe = /paint|layout|strict|content/;
  var isNotNone = (value) => !!value && value !== "none";
  var isWebKitValue;
  function isContainingBlock(elementOrCss) {
    const css = isElement(elementOrCss) ? getComputedStyle2(elementOrCss) : elementOrCss;
    return isNotNone(css.transform) || isNotNone(css.translate) || isNotNone(css.scale) || isNotNone(css.rotate) || isNotNone(css.perspective) || !isWebKit() && (isNotNone(css.backdropFilter) || isNotNone(css.filter)) || willChangeRe.test(css.willChange || "") || containRe.test(css.contain || "");
  }
  function getContainingBlock(element) {
    let currentNode = getParentNode(element);
    while (isHTMLElement(currentNode) && !isLastTraversableNode(currentNode)) {
      if (isContainingBlock(currentNode)) {
        return currentNode;
      } else if (isTopLayer(currentNode)) {
        return null;
      }
      currentNode = getParentNode(currentNode);
    }
    return null;
  }
  function isWebKit() {
    if (isWebKitValue == null) {
      isWebKitValue = typeof CSS !== "undefined" && CSS.supports && CSS.supports("-webkit-backdrop-filter", "none");
    }
    return isWebKitValue;
  }
  function isLastTraversableNode(node) {
    return /^(html|body|#document)$/.test(getNodeName(node));
  }
  function getComputedStyle2(element) {
    return getWindow(element).getComputedStyle(element);
  }
  function getNodeScroll(element) {
    if (isElement(element)) {
      return {
        scrollLeft: element.scrollLeft,
        scrollTop: element.scrollTop
      };
    }
    return {
      scrollLeft: element.scrollX,
      scrollTop: element.scrollY
    };
  }
  function getParentNode(node) {
    if (getNodeName(node) === "html") {
      return node;
    }
    const result = (
      // Step into the shadow DOM of the parent of a slotted node.
      node.assignedSlot || // DOM Element detected.
      node.parentNode || // ShadowRoot detected.
      isShadowRoot(node) && node.host || // Fallback.
      getDocumentElement(node)
    );
    return isShadowRoot(result) ? result.host : result;
  }
  function getNearestOverflowAncestor(node) {
    const parentNode = getParentNode(node);
    if (isLastTraversableNode(parentNode)) {
      return (node.ownerDocument || node).body;
    }
    if (isHTMLElement(parentNode) && isOverflowElement(parentNode)) {
      return parentNode;
    }
    return getNearestOverflowAncestor(parentNode);
  }
  function getOverflowAncestors(node, list, traverseIframes) {
    var _node$ownerDocument2;
    if (list === void 0) {
      list = [];
    }
    if (traverseIframes === void 0) {
      traverseIframes = true;
    }
    const scrollableAncestor = getNearestOverflowAncestor(node);
    const isBody = scrollableAncestor === ((_node$ownerDocument2 = node.ownerDocument) == null ? void 0 : _node$ownerDocument2.body);
    const win = getWindow(scrollableAncestor);
    if (isBody) {
      const frameElement = getFrameElement(win);
      return list.concat(win, win.visualViewport || [], isOverflowElement(scrollableAncestor) ? scrollableAncestor : [], frameElement && traverseIframes ? getOverflowAncestors(frameElement) : []);
    } else {
      return list.concat(scrollableAncestor, getOverflowAncestors(scrollableAncestor, [], traverseIframes));
    }
  }
  function getFrameElement(win) {
    return win.parent && Object.getPrototypeOf(win.parent) ? win.frameElement : null;
  }

  // node_modules/@floating-ui/dom/dist/floating-ui.dom.mjs
  function getCssDimensions(element) {
    const css = getComputedStyle2(element);
    let width = parseFloat(css.width) || 0;
    let height = parseFloat(css.height) || 0;
    const hasOffset = isHTMLElement(element);
    const offsetWidth = hasOffset ? element.offsetWidth : width;
    const offsetHeight = hasOffset ? element.offsetHeight : height;
    const shouldFallback = round(width) !== offsetWidth || round(height) !== offsetHeight;
    if (shouldFallback) {
      width = offsetWidth;
      height = offsetHeight;
    }
    return {
      width,
      height,
      $: shouldFallback
    };
  }
  function unwrapElement(element) {
    return !isElement(element) ? element.contextElement : element;
  }
  function getScale(element) {
    const domElement = unwrapElement(element);
    if (!isHTMLElement(domElement)) {
      return createCoords(1);
    }
    const rect = domElement.getBoundingClientRect();
    const {
      width,
      height,
      $
    } = getCssDimensions(domElement);
    let x = ($ ? round(rect.width) : rect.width) / width;
    let y = ($ ? round(rect.height) : rect.height) / height;
    if (!x || !Number.isFinite(x)) {
      x = 1;
    }
    if (!y || !Number.isFinite(y)) {
      y = 1;
    }
    return {
      x,
      y
    };
  }
  var noOffsets = /* @__PURE__ */ createCoords(0);
  function getVisualOffsets(element) {
    const win = getWindow(element);
    if (!isWebKit() || !win.visualViewport) {
      return noOffsets;
    }
    return {
      x: win.visualViewport.offsetLeft,
      y: win.visualViewport.offsetTop
    };
  }
  function shouldAddVisualOffsets(element, isFixed, floatingOffsetParent) {
    if (isFixed === void 0) {
      isFixed = false;
    }
    return !!floatingOffsetParent && isFixed && floatingOffsetParent === getWindow(element);
  }
  function getBoundingClientRect(element, includeScale, isFixedStrategy, offsetParent) {
    if (includeScale === void 0) {
      includeScale = false;
    }
    if (isFixedStrategy === void 0) {
      isFixedStrategy = false;
    }
    const clientRect = element.getBoundingClientRect();
    const domElement = unwrapElement(element);
    let scale = createCoords(1);
    if (includeScale) {
      if (offsetParent) {
        if (isElement(offsetParent)) {
          scale = getScale(offsetParent);
        }
      } else {
        scale = getScale(element);
      }
    }
    const visualOffsets = shouldAddVisualOffsets(domElement, isFixedStrategy, offsetParent) ? getVisualOffsets(domElement) : createCoords(0);
    let x = (clientRect.left + visualOffsets.x) / scale.x;
    let y = (clientRect.top + visualOffsets.y) / scale.y;
    let width = clientRect.width / scale.x;
    let height = clientRect.height / scale.y;
    if (domElement && offsetParent) {
      const win = getWindow(domElement);
      const offsetWin = isElement(offsetParent) ? getWindow(offsetParent) : offsetParent;
      let currentWin = win;
      let currentIFrame = getFrameElement(currentWin);
      while (currentIFrame && offsetWin !== currentWin) {
        const iframeScale = getScale(currentIFrame);
        const iframeRect = currentIFrame.getBoundingClientRect();
        const css = getComputedStyle2(currentIFrame);
        const left = iframeRect.left + (currentIFrame.clientLeft + parseFloat(css.paddingLeft)) * iframeScale.x;
        const top = iframeRect.top + (currentIFrame.clientTop + parseFloat(css.paddingTop)) * iframeScale.y;
        x *= iframeScale.x;
        y *= iframeScale.y;
        width *= iframeScale.x;
        height *= iframeScale.y;
        x += left;
        y += top;
        currentWin = getWindow(currentIFrame);
        currentIFrame = getFrameElement(currentWin);
      }
    }
    return rectToClientRect({
      width,
      height,
      x,
      y
    });
  }
  function getWindowScrollBarX(element, rect) {
    const leftScroll = getNodeScroll(element).scrollLeft;
    if (!rect) {
      return getBoundingClientRect(getDocumentElement(element)).left + leftScroll;
    }
    return rect.left + leftScroll;
  }
  function getHTMLOffset(documentElement, scroll) {
    const htmlRect = documentElement.getBoundingClientRect();
    const x = htmlRect.left + scroll.scrollLeft - getWindowScrollBarX(documentElement, htmlRect);
    const y = htmlRect.top + scroll.scrollTop;
    return {
      x,
      y
    };
  }
  function convertOffsetParentRelativeRectToViewportRelativeRect(_ref) {
    let {
      elements,
      rect,
      offsetParent,
      strategy
    } = _ref;
    const isFixed = strategy === "fixed";
    const documentElement = getDocumentElement(offsetParent);
    const topLayer = elements ? isTopLayer(elements.floating) : false;
    if (offsetParent === documentElement || topLayer && isFixed) {
      return rect;
    }
    let scroll = {
      scrollLeft: 0,
      scrollTop: 0
    };
    let scale = createCoords(1);
    const offsets = createCoords(0);
    const isOffsetParentAnElement = isHTMLElement(offsetParent);
    if (isOffsetParentAnElement || !isFixed) {
      if (getNodeName(offsetParent) !== "body" || isOverflowElement(documentElement)) {
        scroll = getNodeScroll(offsetParent);
      }
      if (isOffsetParentAnElement) {
        const offsetRect = getBoundingClientRect(offsetParent);
        scale = getScale(offsetParent);
        offsets.x = offsetRect.x + offsetParent.clientLeft;
        offsets.y = offsetRect.y + offsetParent.clientTop;
      }
    }
    const htmlOffset = documentElement && !isOffsetParentAnElement && !isFixed ? getHTMLOffset(documentElement, scroll) : createCoords(0);
    return {
      width: rect.width * scale.x,
      height: rect.height * scale.y,
      x: rect.x * scale.x - scroll.scrollLeft * scale.x + offsets.x + htmlOffset.x,
      y: rect.y * scale.y - scroll.scrollTop * scale.y + offsets.y + htmlOffset.y
    };
  }
  function getClientRects(element) {
    return element.getClientRects ? Array.from(element.getClientRects()) : [];
  }
  function getDocumentRect(html) {
    const scroll = getNodeScroll(html);
    const body = html.ownerDocument.body;
    const width = max(html.scrollWidth, html.clientWidth, body.scrollWidth, body.clientWidth);
    const height = max(html.scrollHeight, html.clientHeight, body.scrollHeight, body.clientHeight);
    let x = -scroll.scrollLeft + getWindowScrollBarX(html);
    const y = -scroll.scrollTop;
    if (getComputedStyle2(body).direction === "rtl") {
      x += max(html.clientWidth, body.clientWidth) - width;
    }
    return {
      width,
      height,
      x,
      y
    };
  }
  var SCROLLBAR_MAX = 25;
  function getViewportRect(element, strategy, rootBoundary) {
    if (rootBoundary === void 0) {
      rootBoundary = "viewport";
    }
    const isLayoutViewport = rootBoundary === "layoutViewport";
    const win = getWindow(element);
    const html = getDocumentElement(element);
    const visualViewport = win.visualViewport;
    let width = html.clientWidth;
    let height = html.clientHeight;
    let x = 0;
    let y = 0;
    if (visualViewport) {
      const layoutRelativeClientCoords = !isWebKit() || strategy === "fixed";
      if (isLayoutViewport) {
        if (!layoutRelativeClientCoords) {
          x = -visualViewport.offsetLeft;
          y = -visualViewport.offsetTop;
        }
      } else {
        width = visualViewport.width;
        height = visualViewport.height;
        if (layoutRelativeClientCoords) {
          x = visualViewport.offsetLeft;
          y = visualViewport.offsetTop;
        }
      }
    }
    const windowScrollbarX = getWindowScrollBarX(html);
    if (windowScrollbarX <= 0) {
      const doc = html.ownerDocument;
      const body = doc.body;
      const bodyStyles = getComputedStyle(body);
      const bodyMarginInline = doc.compatMode === "CSS1Compat" ? parseFloat(bodyStyles.marginLeft) + parseFloat(bodyStyles.marginRight) || 0 : 0;
      const reservedWidth = Math.abs(html.clientWidth - body.clientWidth - bodyMarginInline);
      const gutter = getComputedStyle(html).scrollbarGutter === "stable both-edges" ? reservedWidth / 2 : reservedWidth;
      if (gutter <= SCROLLBAR_MAX) {
        width -= gutter;
      }
    }
    return {
      width,
      height,
      x,
      y
    };
  }
  function getInnerBoundingClientRect(element, strategy) {
    const clientRect = getBoundingClientRect(element, true, strategy === "fixed");
    const top = clientRect.top + element.clientTop;
    const left = clientRect.left + element.clientLeft;
    const scale = getScale(element);
    const width = element.clientWidth * scale.x;
    const height = element.clientHeight * scale.y;
    const x = left * scale.x;
    const y = top * scale.y;
    return {
      width,
      height,
      x,
      y
    };
  }
  function getClientRectFromClippingAncestor(element, clippingAncestor, strategy) {
    let rect;
    if (clippingAncestor === "viewport" || clippingAncestor === "layoutViewport") {
      rect = getViewportRect(element, strategy, clippingAncestor);
    } else if (clippingAncestor === "document") {
      rect = getDocumentRect(getDocumentElement(element));
    } else if (isElement(clippingAncestor)) {
      rect = getInnerBoundingClientRect(clippingAncestor, strategy);
    } else {
      const visualOffsets = getVisualOffsets(element);
      rect = {
        x: clippingAncestor.x - visualOffsets.x,
        y: clippingAncestor.y - visualOffsets.y,
        width: clippingAncestor.width,
        height: clippingAncestor.height
      };
    }
    return rectToClientRect(rect);
  }
  function getClippingElementAncestors(element, cache) {
    const cachedResult = cache.get(element);
    if (cachedResult) {
      return cachedResult;
    }
    let result = getOverflowAncestors(element, [], false).filter((el) => isElement(el) && getNodeName(el) !== "body");
    let lastKeptComputedStyle = null;
    const elementIsFixed = getComputedStyle2(element).position === "fixed";
    let currentNode = elementIsFixed ? getParentNode(element) : element;
    while (isElement(currentNode) && !isLastTraversableNode(currentNode)) {
      const computedStyle = getComputedStyle2(currentNode);
      const currentNodeIsContaining = isContainingBlock(currentNode);
      const lastPosition = lastKeptComputedStyle ? lastKeptComputedStyle.position : elementIsFixed ? "fixed" : "";
      const shouldDropCurrentNode = !currentNodeIsContaining && (lastPosition === "fixed" || lastPosition === "absolute" && computedStyle.position === "static");
      if (shouldDropCurrentNode) {
        result = result.filter((ancestor) => ancestor !== currentNode);
      } else {
        lastKeptComputedStyle = computedStyle;
      }
      currentNode = getParentNode(currentNode);
    }
    cache.set(element, result);
    return result;
  }
  function getClippingRect(_ref) {
    let {
      element,
      boundary,
      rootBoundary,
      strategy
    } = _ref;
    const elementClippingAncestors = boundary === "clippingAncestors" ? isTopLayer(element) ? [] : getClippingElementAncestors(element, this._c) : [].concat(boundary);
    const clippingAncestors = [...elementClippingAncestors, rootBoundary];
    const firstRect = getClientRectFromClippingAncestor(element, clippingAncestors[0], strategy);
    let top = firstRect.top;
    let right = firstRect.right;
    let bottom = firstRect.bottom;
    let left = firstRect.left;
    for (let i = 1; i < clippingAncestors.length; i++) {
      const rect = getClientRectFromClippingAncestor(element, clippingAncestors[i], strategy);
      top = max(rect.top, top);
      right = min(rect.right, right);
      bottom = min(rect.bottom, bottom);
      left = max(rect.left, left);
    }
    return {
      width: right - left,
      height: bottom - top,
      x: left,
      y: top
    };
  }
  function getDimensions(element) {
    const {
      width,
      height
    } = getCssDimensions(element);
    return {
      width,
      height
    };
  }
  function getRectRelativeToOffsetParent(element, offsetParent, strategy) {
    const isOffsetParentAnElement = isHTMLElement(offsetParent);
    const documentElement = getDocumentElement(offsetParent);
    const isFixed = strategy === "fixed";
    const rect = getBoundingClientRect(element, true, isFixed, offsetParent);
    let scroll = {
      scrollLeft: 0,
      scrollTop: 0
    };
    const offsets = createCoords(0);
    if (isOffsetParentAnElement || !isFixed) {
      if (getNodeName(offsetParent) !== "body" || isOverflowElement(documentElement)) {
        scroll = getNodeScroll(offsetParent);
      }
      if (isOffsetParentAnElement) {
        const offsetRect = getBoundingClientRect(offsetParent, true, isFixed, offsetParent);
        offsets.x = offsetRect.x + offsetParent.clientLeft;
        offsets.y = offsetRect.y + offsetParent.clientTop;
      }
    }
    if (!isOffsetParentAnElement && documentElement) {
      offsets.x = getWindowScrollBarX(documentElement);
    }
    const htmlOffset = documentElement && !isOffsetParentAnElement && !isFixed ? getHTMLOffset(documentElement, scroll) : createCoords(0);
    const x = rect.left + scroll.scrollLeft - offsets.x - htmlOffset.x;
    const y = rect.top + scroll.scrollTop - offsets.y - htmlOffset.y;
    return {
      x,
      y,
      width: rect.width,
      height: rect.height
    };
  }
  function isStaticPositioned(element) {
    return getComputedStyle2(element).position === "static";
  }
  function getTrueOffsetParent(element, polyfill) {
    if (!isHTMLElement(element) || getComputedStyle2(element).position === "fixed") {
      return null;
    }
    if (polyfill) {
      return polyfill(element);
    }
    let rawOffsetParent = element.offsetParent;
    if (getDocumentElement(element) === rawOffsetParent) {
      rawOffsetParent = rawOffsetParent.ownerDocument.body;
    }
    return rawOffsetParent;
  }
  function getOffsetParent(element, polyfill) {
    const win = getWindow(element);
    if (isTopLayer(element)) {
      return win;
    }
    if (!isHTMLElement(element)) {
      let svgOffsetParent = getParentNode(element);
      while (svgOffsetParent && !isLastTraversableNode(svgOffsetParent)) {
        if (isElement(svgOffsetParent) && !isStaticPositioned(svgOffsetParent)) {
          return svgOffsetParent;
        }
        svgOffsetParent = getParentNode(svgOffsetParent);
      }
      return win;
    }
    let offsetParent = getTrueOffsetParent(element, polyfill);
    while (offsetParent && isTableElement(offsetParent) && isStaticPositioned(offsetParent)) {
      offsetParent = getTrueOffsetParent(offsetParent, polyfill);
    }
    if (offsetParent && isLastTraversableNode(offsetParent) && isStaticPositioned(offsetParent) && !isContainingBlock(offsetParent)) {
      return win;
    }
    return offsetParent || getContainingBlock(element) || win;
  }
  var getElementRects = async function(data) {
    const getOffsetParentFn = this.getOffsetParent || getOffsetParent;
    const getDimensionsFn = this.getDimensions;
    const floatingDimensions = await getDimensionsFn(data.floating);
    return {
      reference: getRectRelativeToOffsetParent(data.reference, await getOffsetParentFn(data.floating), data.strategy),
      floating: {
        x: 0,
        y: 0,
        width: floatingDimensions.width,
        height: floatingDimensions.height
      }
    };
  };
  function isRTL(element) {
    return getComputedStyle2(element).direction === "rtl";
  }
  var platform = {
    convertOffsetParentRelativeRectToViewportRelativeRect,
    getDocumentElement,
    getClippingRect,
    getOffsetParent,
    getElementRects,
    getClientRects,
    getDimensions,
    getScale,
    isElement,
    isRTL
  };
  function rectsAreEqual(a, b) {
    return a.x === b.x && a.y === b.y && a.width === b.width && a.height === b.height;
  }
  function observeMove(element, onMove, ancestorResize) {
    let io = null;
    let timeoutId;
    const root = getDocumentElement(element);
    function cleanup() {
      var _io;
      clearTimeout(timeoutId);
      (_io = io) == null || _io.disconnect();
      io = null;
    }
    function refresh(skip, threshold) {
      if (skip === void 0) {
        skip = false;
      }
      if (threshold === void 0) {
        threshold = 1;
      }
      cleanup();
      const elementRectForRootMargin = element.getBoundingClientRect();
      const {
        left,
        top,
        width,
        height
      } = elementRectForRootMargin;
      if (!skip) {
        onMove();
      }
      if (!width || !height) {
        return;
      }
      const insetTop = floor(top);
      const insetRight = floor(root.clientWidth - (left + width));
      const insetBottom = floor(root.clientHeight - (top + height));
      const insetLeft = floor(left);
      const rootMargin = -insetTop + "px " + -insetRight + "px " + -insetBottom + "px " + -insetLeft + "px";
      const options = {
        rootMargin,
        threshold: max(0, min(1, threshold)) || 1
      };
      let isFirstUpdate = true;
      function handleObserve(entries) {
        const ratio = entries[0].intersectionRatio;
        if (!rectsAreEqual(elementRectForRootMargin, element.getBoundingClientRect())) {
          return refresh();
        }
        if (ratio !== threshold) {
          if (!isFirstUpdate) {
            return refresh();
          }
          if (!ratio) {
            timeoutId = setTimeout(() => {
              refresh(false, 1e-7);
            }, 1e3);
          } else {
            refresh(false, ratio);
          }
        }
        isFirstUpdate = false;
      }
      try {
        io = new IntersectionObserver(handleObserve, {
          ...options,
          // Handle <iframe>s
          root: root.ownerDocument
        });
      } catch (_e) {
        io = new IntersectionObserver(handleObserve, options);
      }
      io.observe(element);
    }
    const win = getWindow(element);
    const handleResize = () => refresh(ancestorResize);
    win.addEventListener("resize", handleResize);
    refresh(true);
    return () => {
      win.removeEventListener("resize", handleResize);
      cleanup();
    };
  }
  function autoUpdate(reference, floating, update, options) {
    if (options === void 0) {
      options = {};
    }
    const {
      ancestorScroll = true,
      ancestorResize = true,
      elementResize = typeof ResizeObserver === "function",
      layoutShift = typeof IntersectionObserver === "function",
      animationFrame = false
    } = options;
    const referenceEl = unwrapElement(reference);
    const ancestors = ancestorScroll || ancestorResize ? [...referenceEl ? getOverflowAncestors(referenceEl) : [], ...floating ? getOverflowAncestors(floating) : []] : [];
    ancestors.forEach((ancestor) => {
      ancestorScroll && ancestor.addEventListener("scroll", update);
      ancestorResize && ancestor.addEventListener("resize", update);
    });
    const cleanupIo = referenceEl && layoutShift ? observeMove(referenceEl, update, ancestorResize) : null;
    let reobserveFrame = -1;
    let resizeObserver = null;
    if (elementResize) {
      resizeObserver = new ResizeObserver((_ref) => {
        let [firstEntry] = _ref;
        if (firstEntry && firstEntry.target === referenceEl && resizeObserver && floating) {
          resizeObserver.unobserve(floating);
          cancelAnimationFrame(reobserveFrame);
          reobserveFrame = requestAnimationFrame(() => {
            var _resizeObserver;
            (_resizeObserver = resizeObserver) == null || _resizeObserver.observe(floating);
          });
        }
        update();
      });
      if (referenceEl && !animationFrame) {
        resizeObserver.observe(referenceEl);
      }
      if (floating) {
        resizeObserver.observe(floating);
      }
    }
    let frameId;
    let prevRefRect = animationFrame ? getBoundingClientRect(reference) : null;
    if (animationFrame) {
      frameLoop();
    }
    function frameLoop() {
      const nextRefRect = getBoundingClientRect(reference);
      if (prevRefRect && !rectsAreEqual(prevRefRect, nextRefRect)) {
        update();
      }
      prevRefRect = nextRefRect;
      frameId = requestAnimationFrame(frameLoop);
    }
    update();
    return () => {
      var _resizeObserver2;
      ancestors.forEach((ancestor) => {
        ancestorScroll && ancestor.removeEventListener("scroll", update);
        ancestorResize && ancestor.removeEventListener("resize", update);
      });
      cleanupIo == null || cleanupIo();
      (_resizeObserver2 = resizeObserver) == null || _resizeObserver2.disconnect();
      resizeObserver = null;
      if (animationFrame) {
        cancelAnimationFrame(frameId);
      }
    };
  }
  var offset2 = offset;
  var shift2 = shift;
  var flip2 = flip;
  var size2 = size;
  var hide2 = hide;
  var arrow2 = arrow;
  var limitShift2 = limitShift;
  var computePosition2 = (reference, floating, options) => {
    const cache = /* @__PURE__ */ new Map();
    const mergedOptions = options != null ? options : {};
    const platformWithCache = {
      ...platform,
      ...mergedOptions.platform,
      _c: cache
    };
    return computePosition(reference, floating, {
      ...mergedOptions,
      platform: platformWithCache
    });
  };

  // node_modules/@floating-ui/react-dom/dist/floating-ui.react-dom.mjs
  var React24 = __toESM(require_react(), 1);
  var import_react5 = __toESM(require_react(), 1);
  var ReactDOM3 = __toESM(require_react_dom(), 1);
  var isClient = typeof document !== "undefined";
  var noop = function noop2() {
  };
  var index = isClient ? import_react5.useLayoutEffect : noop;
  function deepEqual(a, b) {
    if (a === b) {
      return true;
    }
    if (typeof a !== typeof b) {
      return false;
    }
    if (typeof a === "function" && a.toString() === b.toString()) {
      return true;
    }
    let length;
    let i;
    let keys;
    if (a && b && typeof a === "object") {
      if (Array.isArray(a)) {
        length = a.length;
        if (length !== b.length) return false;
        for (i = length; i-- !== 0; ) {
          if (!deepEqual(a[i], b[i])) {
            return false;
          }
        }
        return true;
      }
      keys = Object.keys(a);
      length = keys.length;
      if (length !== Object.keys(b).length) {
        return false;
      }
      for (i = length; i-- !== 0; ) {
        if (!{}.hasOwnProperty.call(b, keys[i])) {
          return false;
        }
      }
      for (i = length; i-- !== 0; ) {
        const key = keys[i];
        if (key === "_owner" && a.$$typeof) {
          continue;
        }
        if (!deepEqual(a[key], b[key])) {
          return false;
        }
      }
      return true;
    }
    return a !== a && b !== b;
  }
  function getDPR(element) {
    if (typeof window === "undefined") {
      return 1;
    }
    const win = element.ownerDocument.defaultView || window;
    return win.devicePixelRatio || 1;
  }
  function roundByDPR(element, value) {
    const dpr = getDPR(element);
    return Math.round(value * dpr) / dpr;
  }
  function useLatestRef(value) {
    const ref = React24.useRef(value);
    index(() => {
      ref.current = value;
    });
    return ref;
  }
  function useFloating(options) {
    if (options === void 0) {
      options = {};
    }
    const {
      placement = "bottom",
      strategy = "absolute",
      middleware = [],
      platform: platform2,
      elements: {
        reference: externalReference,
        floating: externalFloating
      } = {},
      transform = true,
      whileElementsMounted,
      open
    } = options;
    const [data, setData] = React24.useState({
      x: 0,
      y: 0,
      strategy,
      placement,
      middlewareData: {},
      isPositioned: false
    });
    const [latestMiddleware, setLatestMiddleware] = React24.useState(middleware);
    if (!deepEqual(latestMiddleware, middleware)) {
      setLatestMiddleware(middleware);
    }
    const [_reference, _setReference] = React24.useState(null);
    const [_floating, _setFloating] = React24.useState(null);
    const setReference = React24.useCallback((node) => {
      if (node !== referenceRef.current) {
        referenceRef.current = node;
        _setReference(node);
      }
    }, []);
    const setFloating = React24.useCallback((node) => {
      if (node !== floatingRef.current) {
        floatingRef.current = node;
        _setFloating(node);
      }
    }, []);
    const referenceEl = externalReference || _reference;
    const floatingEl = externalFloating || _floating;
    const referenceRef = React24.useRef(null);
    const floatingRef = React24.useRef(null);
    const dataRef = React24.useRef(data);
    const hasWhileElementsMounted = whileElementsMounted != null;
    const whileElementsMountedRef = useLatestRef(whileElementsMounted);
    const platformRef = useLatestRef(platform2);
    const openRef = useLatestRef(open);
    const update = React24.useCallback(() => {
      if (!referenceRef.current || !floatingRef.current) {
        return;
      }
      const config = {
        placement,
        strategy,
        middleware: latestMiddleware
      };
      if (platformRef.current) {
        config.platform = platformRef.current;
      }
      computePosition2(referenceRef.current, floatingRef.current, config).then((data2) => {
        const fullData = {
          ...data2,
          // The floating element's position may be recomputed while it's closed
          // but still mounted (such as when transitioning out). To ensure
          // `isPositioned` will be `false` initially on the next open, avoid
          // setting it to `true` when `open === false` (must be specified).
          isPositioned: openRef.current !== false
        };
        if (isMountedRef.current && !deepEqual(dataRef.current, fullData)) {
          dataRef.current = fullData;
          ReactDOM3.flushSync(() => {
            setData(fullData);
          });
        }
      });
    }, [latestMiddleware, placement, strategy, platformRef, openRef]);
    index(() => {
      if (open === false && dataRef.current.isPositioned) {
        dataRef.current.isPositioned = false;
        setData((data2) => ({
          ...data2,
          isPositioned: false
        }));
      }
    }, [open]);
    const isMountedRef = React24.useRef(false);
    index(() => {
      isMountedRef.current = true;
      return () => {
        isMountedRef.current = false;
      };
    }, []);
    index(() => {
      if (referenceEl) referenceRef.current = referenceEl;
      if (floatingEl) floatingRef.current = floatingEl;
      if (referenceEl && floatingEl) {
        if (whileElementsMountedRef.current) {
          return whileElementsMountedRef.current(referenceEl, floatingEl, update);
        }
        update();
      }
    }, [referenceEl, floatingEl, update, whileElementsMountedRef, hasWhileElementsMounted]);
    const refs = React24.useMemo(() => ({
      reference: referenceRef,
      floating: floatingRef,
      setReference,
      setFloating
    }), [setReference, setFloating]);
    const elements = React24.useMemo(() => ({
      reference: referenceEl,
      floating: floatingEl
    }), [referenceEl, floatingEl]);
    const floatingStyles = React24.useMemo(() => {
      const initialStyles = {
        position: strategy,
        left: 0,
        top: 0
      };
      if (!elements.floating) {
        return initialStyles;
      }
      const x = roundByDPR(elements.floating, data.x);
      const y = roundByDPR(elements.floating, data.y);
      if (transform) {
        return {
          ...initialStyles,
          transform: "translate(" + x + "px, " + y + "px)",
          ...getDPR(elements.floating) >= 1.5 && {
            willChange: "transform"
          }
        };
      }
      return {
        position: strategy,
        left: x,
        top: y
      };
    }, [strategy, transform, elements.floating, data.x, data.y]);
    return React24.useMemo(() => ({
      ...data,
      update,
      refs,
      elements,
      floatingStyles
    }), [data, update, refs, elements, floatingStyles]);
  }
  var arrow$1 = (options) => {
    function isRef(value) {
      return {}.hasOwnProperty.call(value, "current");
    }
    return {
      name: "arrow",
      options,
      fn(state2) {
        const {
          element,
          padding
        } = typeof options === "function" ? options(state2) : options;
        if (element && isRef(element)) {
          if (element.current != null) {
            return arrow2({
              element: element.current,
              padding
            }).fn(state2);
          }
          return {};
        }
        if (element) {
          return arrow2({
            element,
            padding
          }).fn(state2);
        }
        return {};
      }
    };
  };
  var offset3 = (options, deps) => {
    const result = offset2(options);
    return {
      name: result.name,
      fn: result.fn,
      options: [options, deps]
    };
  };
  var shift3 = (options, deps) => {
    const result = shift2(options);
    return {
      name: result.name,
      fn: result.fn,
      options: [options, deps]
    };
  };
  var limitShift3 = (options, deps) => {
    const result = limitShift2(options);
    return {
      fn: result.fn,
      options: [options, deps]
    };
  };
  var flip3 = (options, deps) => {
    const result = flip2(options);
    return {
      name: result.name,
      fn: result.fn,
      options: [options, deps]
    };
  };
  var size3 = (options, deps) => {
    const result = size2(options);
    return {
      name: result.name,
      fn: result.fn,
      options: [options, deps]
    };
  };
  var hide3 = (options, deps) => {
    const result = hide2(options);
    return {
      name: result.name,
      fn: result.fn,
      options: [options, deps]
    };
  };
  var arrow3 = (options, deps) => {
    const result = arrow$1(options);
    return {
      name: result.name,
      fn: result.fn,
      options: [options, deps]
    };
  };

  // node_modules/@radix-ui/react-arrow/dist/index.mjs
  var React25 = __toESM(require_react(), 1);
  var import_jsx_runtime10 = __toESM(require_jsx_runtime(), 1);
  var NAME = "Arrow";
  var Arrow = React25.forwardRef((props, forwardedRef) => {
    const { children, width = 10, height = 5, ...arrowProps } = props;
    return /* @__PURE__ */ (0, import_jsx_runtime10.jsx)(
      Primitive.svg,
      {
        ...arrowProps,
        ref: forwardedRef,
        width,
        height,
        viewBox: "0 0 30 10",
        preserveAspectRatio: "none",
        children: props.asChild ? children : /* @__PURE__ */ (0, import_jsx_runtime10.jsx)("polygon", { points: "0,0 30,0 15,10" })
      }
    );
  });
  Arrow.displayName = NAME;
  var Root2 = Arrow;

  // node_modules/@radix-ui/react-use-size/dist/index.mjs
  var React26 = __toESM(require_react(), 1);
  function useSize(element) {
    const [size4, setSize] = React26.useState(void 0);
    useLayoutEffect2(() => {
      if (element) {
        setSize({ width: element.offsetWidth, height: element.offsetHeight });
        const resizeObserver = new ResizeObserver((entries) => {
          if (!Array.isArray(entries)) {
            return;
          }
          if (!entries.length) {
            return;
          }
          const entry = entries[0];
          let width;
          let height;
          if ("borderBoxSize" in entry) {
            const borderSizeEntry = entry["borderBoxSize"];
            const borderSize = Array.isArray(borderSizeEntry) ? borderSizeEntry[0] : borderSizeEntry;
            width = borderSize["inlineSize"];
            height = borderSize["blockSize"];
          } else {
            width = element.offsetWidth;
            height = element.offsetHeight;
          }
          setSize({ width, height });
        });
        resizeObserver.observe(element, { box: "border-box" });
        return () => resizeObserver.unobserve(element);
      } else {
        setSize(void 0);
      }
    }, [element]);
    return size4;
  }

  // node_modules/@radix-ui/react-popper/dist/index.mjs
  var import_jsx_runtime11 = __toESM(require_jsx_runtime(), 1);
  var POPPER_NAME = "Popper";
  var [createPopperContext, createPopperScope] = createContextScope(POPPER_NAME);
  var [PopperProvider, usePopperContext] = createPopperContext(POPPER_NAME);
  var Popper = (props) => {
    const { __scopePopper, children } = props;
    const [anchor, setAnchor] = React27.useState(null);
    const [placementState, setPlacementState] = React27.useState(void 0);
    return /* @__PURE__ */ (0, import_jsx_runtime11.jsx)(
      PopperProvider,
      {
        scope: __scopePopper,
        anchor,
        onAnchorChange: setAnchor,
        placementState,
        setPlacementState,
        children
      }
    );
  };
  Popper.displayName = POPPER_NAME;
  var ANCHOR_NAME = "PopperAnchor";
  var PopperAnchor = React27.forwardRef(
    (props, forwardedRef) => {
      const { __scopePopper, virtualRef, ...anchorProps } = props;
      const context = usePopperContext(ANCHOR_NAME, __scopePopper);
      const ref = React27.useRef(null);
      const onAnchorChange = context.onAnchorChange;
      const callbackRef = React27.useCallback(
        (node) => {
          ref.current = node;
          if (node) {
            onAnchorChange(node);
          }
        },
        [onAnchorChange]
      );
      const composedRefs = useComposedRefs(forwardedRef, callbackRef);
      const anchorRef = React27.useRef(null);
      React27.useEffect(() => {
        if (!virtualRef) {
          return;
        }
        const previousAnchor = anchorRef.current;
        anchorRef.current = virtualRef.current;
        if (previousAnchor !== anchorRef.current) {
          onAnchorChange(anchorRef.current);
        }
      });
      const sideAndAlign = context.placementState && getSideAndAlignFromPlacement(context.placementState);
      const placedSide = sideAndAlign?.[0];
      const placedAlign = sideAndAlign?.[1];
      return virtualRef ? null : /* @__PURE__ */ (0, import_jsx_runtime11.jsx)(
        Primitive.div,
        {
          "data-radix-popper-side": placedSide,
          "data-radix-popper-align": placedAlign,
          ...anchorProps,
          ref: composedRefs
        }
      );
    }
  );
  PopperAnchor.displayName = ANCHOR_NAME;
  var CONTENT_NAME2 = "PopperContent";
  var [PopperContentProvider, useContentContext] = createPopperContext(CONTENT_NAME2);
  var PopperContent = React27.forwardRef(
    (props, forwardedRef) => {
      const {
        __scopePopper,
        side = "bottom",
        sideOffset = 0,
        align = "center",
        alignOffset = 0,
        arrowPadding = 0,
        avoidCollisions = true,
        collisionBoundary = [],
        collisionPadding: collisionPaddingProp = 0,
        sticky = "partial",
        hideWhenDetached = false,
        updatePositionStrategy = "optimized",
        onPlaced,
        ...contentProps
      } = props;
      const context = usePopperContext(CONTENT_NAME2, __scopePopper);
      const [content, setContent] = React27.useState(null);
      const composedRefs = useComposedRefs(forwardedRef, setContent);
      const [arrow4, setArrow] = React27.useState(null);
      const arrowSize = useSize(arrow4);
      const arrowWidth = arrowSize?.width ?? 0;
      const arrowHeight = arrowSize?.height ?? 0;
      const desiredPlacement = side + (align !== "center" ? "-" + align : "");
      const collisionPadding = typeof collisionPaddingProp === "number" ? collisionPaddingProp : { top: 0, right: 0, bottom: 0, left: 0, ...collisionPaddingProp };
      const boundary = Array.isArray(collisionBoundary) ? collisionBoundary : [collisionBoundary];
      const hasExplicitBoundaries = boundary.length > 0;
      const detectOverflowOptions = {
        padding: collisionPadding,
        boundary: boundary.filter(isNotNull),
        // with `strategy: 'fixed'`, this is the only way to get it to respect boundaries
        altBoundary: hasExplicitBoundaries
      };
      const { refs, floatingStyles, placement, isPositioned, middlewareData } = useFloating({
        // default to `fixed` strategy so users don't have to pick and we also avoid focus scroll issues
        strategy: "fixed",
        placement: desiredPlacement,
        whileElementsMounted: (...args) => {
          const cleanup = autoUpdate(...args, {
            animationFrame: updatePositionStrategy === "always"
          });
          return cleanup;
        },
        elements: {
          reference: context.anchor
        },
        middleware: [
          offset3({ mainAxis: sideOffset + arrowHeight, alignmentAxis: alignOffset }),
          avoidCollisions && shift3({
            mainAxis: true,
            crossAxis: false,
            limiter: sticky === "partial" ? limitShift3() : void 0,
            ...detectOverflowOptions
          }),
          avoidCollisions && flip3({ ...detectOverflowOptions }),
          size3({
            ...detectOverflowOptions,
            apply: ({ elements, rects, availableWidth, availableHeight }) => {
              const { width: anchorWidth, height: anchorHeight } = rects.reference;
              const contentStyle = elements.floating.style;
              contentStyle.setProperty("--radix-popper-available-width", `${availableWidth}px`);
              contentStyle.setProperty("--radix-popper-available-height", `${availableHeight}px`);
              contentStyle.setProperty("--radix-popper-anchor-width", `${anchorWidth}px`);
              contentStyle.setProperty("--radix-popper-anchor-height", `${anchorHeight}px`);
            }
          }),
          arrow4 && arrow3({ element: arrow4, padding: arrowPadding }),
          transformOrigin({ arrowWidth, arrowHeight }),
          hideWhenDetached && hide3({
            strategy: "referenceHidden",
            ...detectOverflowOptions,
            // `hide` detects whether the anchor (reference) is clipped, so when
            // no explicit `collisionBoundary` is set we fall back to Floating
            // UI's default clipping ancestors (e.g. a scrollable menu). This
            // lets an occluded submenu hide once its anchor scrolls out of view
            // (#3237). The collision/size middlewares deliberately keep the
            // viewport-based default to avoid clamping content rendered inside
            // transformed or overflow-clipping portal containers.
            boundary: hasExplicitBoundaries ? detectOverflowOptions.boundary : void 0
          })
        ]
      });
      const setPlacementState = context.setPlacementState;
      useLayoutEffect2(() => {
        setPlacementState(placement);
        return () => {
          setPlacementState(void 0);
        };
      }, [placement, setPlacementState]);
      const [placedSide, placedAlign] = getSideAndAlignFromPlacement(placement);
      const handlePlaced = useCallbackRef(onPlaced);
      useLayoutEffect2(() => {
        if (isPositioned) {
          handlePlaced?.();
        }
      }, [isPositioned, handlePlaced]);
      const arrowX = middlewareData.arrow?.x;
      const arrowY = middlewareData.arrow?.y;
      const cannotCenterArrow = middlewareData.arrow?.centerOffset !== 0;
      const [contentZIndex, setContentZIndex] = React27.useState();
      useLayoutEffect2(() => {
        if (content) setContentZIndex(window.getComputedStyle(content).zIndex);
      }, [content]);
      return /* @__PURE__ */ (0, import_jsx_runtime11.jsx)(
        "div",
        {
          ref: refs.setFloating,
          "data-radix-popper-content-wrapper": "",
          style: {
            ...floatingStyles,
            transform: isPositioned ? floatingStyles.transform : "translate(0, -200%)",
            // keep off the page when measuring
            minWidth: "max-content",
            zIndex: contentZIndex,
            "--radix-popper-transform-origin": [
              middlewareData.transformOrigin?.x,
              middlewareData.transformOrigin?.y
            ].join(" "),
            // hide the content if using the hide middleware and should be hidden
            // set visibility to hidden and disable pointer events so the UI behaves
            // as if the PopperContent isn't there at all
            ...middlewareData.hide?.referenceHidden && {
              visibility: "hidden",
              pointerEvents: "none"
            }
          },
          dir: props.dir,
          children: /* @__PURE__ */ (0, import_jsx_runtime11.jsx)(
            PopperContentProvider,
            {
              scope: __scopePopper,
              placedSide,
              placedAlign,
              onArrowChange: setArrow,
              arrowX,
              arrowY,
              shouldHideArrow: cannotCenterArrow,
              children: /* @__PURE__ */ (0, import_jsx_runtime11.jsx)(
                Primitive.div,
                {
                  "data-side": placedSide,
                  "data-align": placedAlign,
                  ...contentProps,
                  ref: composedRefs,
                  style: {
                    ...contentProps.style,
                    // if the PopperContent hasn't been placed yet (not all measurements done)
                    // we prevent animations so that users's animation don't kick in too early referring wrong sides
                    animation: !isPositioned ? "none" : void 0
                  }
                }
              )
            }
          )
        }
      );
    }
  );
  PopperContent.displayName = CONTENT_NAME2;
  var ARROW_NAME = "PopperArrow";
  var OPPOSITE_SIDE = {
    top: "bottom",
    right: "left",
    bottom: "top",
    left: "right"
  };
  var PopperArrow = React27.forwardRef(function PopperArrow2(props, forwardedRef) {
    const { __scopePopper, ...arrowProps } = props;
    const contentContext = useContentContext(ARROW_NAME, __scopePopper);
    const baseSide = OPPOSITE_SIDE[contentContext.placedSide];
    return (
      // we have to use an extra wrapper because `ResizeObserver` (used by `useSize`)
      // doesn't report size as we'd expect on SVG elements.
      // it reports their bounding box which is effectively the largest path inside the SVG.
      /* @__PURE__ */ (0, import_jsx_runtime11.jsx)(
        "span",
        {
          ref: contentContext.onArrowChange,
          style: {
            position: "absolute",
            left: contentContext.arrowX,
            top: contentContext.arrowY,
            [baseSide]: 0,
            transformOrigin: {
              top: "",
              right: "0 0",
              bottom: "center 0",
              left: "100% 0"
            }[contentContext.placedSide],
            transform: {
              top: "translateY(100%)",
              right: "translateY(50%) rotate(90deg) translateX(-50%)",
              bottom: `rotate(180deg)`,
              left: "translateY(50%) rotate(-90deg) translateX(50%)"
            }[contentContext.placedSide],
            visibility: contentContext.shouldHideArrow ? "hidden" : void 0
          },
          children: /* @__PURE__ */ (0, import_jsx_runtime11.jsx)(
            Root2,
            {
              ...arrowProps,
              ref: forwardedRef,
              style: {
                ...arrowProps.style,
                // ensures the element can be measured correctly (mostly for if SVG)
                display: "block"
              }
            }
          )
        }
      )
    );
  });
  PopperArrow.displayName = ARROW_NAME;
  function isNotNull(value) {
    return value !== null;
  }
  var transformOrigin = (options) => ({
    name: "transformOrigin",
    options,
    fn(data) {
      const { placement, rects, middlewareData } = data;
      const cannotCenterArrow = middlewareData.arrow?.centerOffset !== 0;
      const isArrowHidden = cannotCenterArrow;
      const arrowWidth = isArrowHidden ? 0 : options.arrowWidth;
      const arrowHeight = isArrowHidden ? 0 : options.arrowHeight;
      const [placedSide, placedAlign] = getSideAndAlignFromPlacement(placement);
      const noArrowAlign = { start: "0%", center: "50%", end: "100%" }[placedAlign];
      const arrowXCenter = (middlewareData.arrow?.x ?? 0) + arrowWidth / 2;
      const arrowYCenter = (middlewareData.arrow?.y ?? 0) + arrowHeight / 2;
      let x = "";
      let y = "";
      if (placedSide === "bottom") {
        x = isArrowHidden ? noArrowAlign : `${arrowXCenter}px`;
        y = `${-arrowHeight}px`;
      } else if (placedSide === "top") {
        x = isArrowHidden ? noArrowAlign : `${arrowXCenter}px`;
        y = `${rects.floating.height + arrowHeight}px`;
      } else if (placedSide === "right") {
        x = `${-arrowHeight}px`;
        y = isArrowHidden ? noArrowAlign : `${arrowYCenter}px`;
      } else if (placedSide === "left") {
        x = `${rects.floating.width + arrowHeight}px`;
        y = isArrowHidden ? noArrowAlign : `${arrowYCenter}px`;
      }
      return { data: { x, y } };
    }
  });
  function getSideAndAlignFromPlacement(placement) {
    const [side, align = "center"] = placement.split("-");
    return [side, align];
  }
  var Root22 = Popper;
  var Anchor = PopperAnchor;
  var Content = PopperContent;
  var Arrow2 = PopperArrow;

  // node_modules/@radix-ui/react-popover/dist/index.mjs
  var import_jsx_runtime12 = __toESM(require_jsx_runtime(), 1);
  var POPOVER_NAME = "Popover";
  var [createPopoverContext, createPopoverScope] = createContextScope(POPOVER_NAME, [
    createPopperScope
  ]);
  var usePopperScope = createPopperScope();
  var [PopoverProvider, usePopoverContext] = createPopoverContext(POPOVER_NAME);
  var Popover = (props) => {
    const {
      __scopePopover,
      children,
      open: openProp,
      defaultOpen,
      onOpenChange,
      modal = false
    } = props;
    const popperScope = usePopperScope(__scopePopover);
    const triggerRef = React28.useRef(null);
    const [hasCustomAnchor, setHasCustomAnchor] = React28.useState(false);
    const [open, setOpen] = useControllableState({
      prop: openProp,
      defaultProp: defaultOpen ?? false,
      onChange: onOpenChange,
      caller: POPOVER_NAME
    });
    return /* @__PURE__ */ (0, import_jsx_runtime12.jsx)(Root22, { ...popperScope, children: /* @__PURE__ */ (0, import_jsx_runtime12.jsx)(
      PopoverProvider,
      {
        scope: __scopePopover,
        contentId: useId(),
        triggerRef,
        open,
        onOpenChange: setOpen,
        onOpenToggle: React28.useCallback(() => setOpen((prevOpen) => !prevOpen), [setOpen]),
        hasCustomAnchor,
        onCustomAnchorAdd: React28.useCallback(() => setHasCustomAnchor(true), []),
        onCustomAnchorRemove: React28.useCallback(() => setHasCustomAnchor(false), []),
        modal,
        children
      }
    ) });
  };
  Popover.displayName = POPOVER_NAME;
  var ANCHOR_NAME2 = "PopoverAnchor";
  var PopoverAnchor = React28.forwardRef(
    (props, forwardedRef) => {
      const { __scopePopover, ...anchorProps } = props;
      const context = usePopoverContext(ANCHOR_NAME2, __scopePopover);
      const popperScope = usePopperScope(__scopePopover);
      const { onCustomAnchorAdd, onCustomAnchorRemove } = context;
      React28.useEffect(() => {
        onCustomAnchorAdd();
        return () => onCustomAnchorRemove();
      }, [onCustomAnchorAdd, onCustomAnchorRemove]);
      return /* @__PURE__ */ (0, import_jsx_runtime12.jsx)(Anchor, { ...popperScope, ...anchorProps, ref: forwardedRef });
    }
  );
  PopoverAnchor.displayName = ANCHOR_NAME2;
  var TRIGGER_NAME2 = "PopoverTrigger";
  var PopoverTrigger = React28.forwardRef(
    (props, forwardedRef) => {
      const { __scopePopover, ...triggerProps } = props;
      const context = usePopoverContext(TRIGGER_NAME2, __scopePopover);
      const popperScope = usePopperScope(__scopePopover);
      const composedTriggerRef = useComposedRefs(forwardedRef, context.triggerRef);
      const trigger = /* @__PURE__ */ (0, import_jsx_runtime12.jsx)(
        Primitive.button,
        {
          type: "button",
          "aria-haspopup": "dialog",
          "aria-expanded": context.open,
          "aria-controls": context.open ? context.contentId : void 0,
          "data-state": getState2(context.open),
          ...triggerProps,
          ref: composedTriggerRef,
          onClick: composeEventHandlers(props.onClick, context.onOpenToggle)
        }
      );
      return context.hasCustomAnchor ? trigger : /* @__PURE__ */ (0, import_jsx_runtime12.jsx)(Anchor, { asChild: true, ...popperScope, children: trigger });
    }
  );
  PopoverTrigger.displayName = TRIGGER_NAME2;
  var PORTAL_NAME3 = "PopoverPortal";
  var [PortalProvider2, usePortalContext2] = createPopoverContext(PORTAL_NAME3, {
    forceMount: void 0
  });
  var PopoverPortal = (props) => {
    const { __scopePopover, forceMount, children, container } = props;
    const context = usePopoverContext(PORTAL_NAME3, __scopePopover);
    return /* @__PURE__ */ (0, import_jsx_runtime12.jsx)(PortalProvider2, { scope: __scopePopover, forceMount, children: /* @__PURE__ */ (0, import_jsx_runtime12.jsx)(Presence, { present: forceMount || context.open, children: /* @__PURE__ */ (0, import_jsx_runtime12.jsx)(Portal, { asChild: true, container, children }) }) });
  };
  PopoverPortal.displayName = PORTAL_NAME3;
  var CONTENT_NAME3 = "PopoverContent";
  var PopoverContent = React28.forwardRef(
    (props, forwardedRef) => {
      const portalContext = usePortalContext2(CONTENT_NAME3, props.__scopePopover);
      const { forceMount = portalContext.forceMount, ...contentProps } = props;
      const context = usePopoverContext(CONTENT_NAME3, props.__scopePopover);
      return /* @__PURE__ */ (0, import_jsx_runtime12.jsx)(Presence, { present: forceMount || context.open, children: context.modal ? /* @__PURE__ */ (0, import_jsx_runtime12.jsx)(PopoverContentModal, { ...contentProps, ref: forwardedRef }) : /* @__PURE__ */ (0, import_jsx_runtime12.jsx)(PopoverContentNonModal, { ...contentProps, ref: forwardedRef }) });
    }
  );
  PopoverContent.displayName = CONTENT_NAME3;
  var Slot2 = createSlot("PopoverContent.RemoveScroll");
  var PopoverContentModal = React28.forwardRef(
    (props, forwardedRef) => {
      const context = usePopoverContext(CONTENT_NAME3, props.__scopePopover);
      const contentRef = React28.useRef(null);
      const composedRefs = useComposedRefs(forwardedRef, contentRef);
      const isRightClickOutsideRef = React28.useRef(false);
      React28.useEffect(() => {
        const content = contentRef.current;
        if (content) return hideOthers(content);
      }, []);
      return /* @__PURE__ */ (0, import_jsx_runtime12.jsx)(Combination_default, { as: Slot2, allowPinchZoom: true, children: /* @__PURE__ */ (0, import_jsx_runtime12.jsx)(
        PopoverContentImpl,
        {
          ...props,
          ref: composedRefs,
          trapFocus: context.open,
          disableOutsidePointerEvents: true,
          onCloseAutoFocus: composeEventHandlers(props.onCloseAutoFocus, (event) => {
            event.preventDefault();
            if (!isRightClickOutsideRef.current) context.triggerRef.current?.focus();
          }),
          onPointerDownOutside: composeEventHandlers(
            props.onPointerDownOutside,
            (event) => {
              const originalEvent = event.detail.originalEvent;
              const ctrlLeftClick = originalEvent.button === 0 && originalEvent.ctrlKey === true;
              const isRightClick = originalEvent.button === 2 || ctrlLeftClick;
              isRightClickOutsideRef.current = isRightClick;
            },
            { checkForDefaultPrevented: false }
          ),
          onFocusOutside: composeEventHandlers(
            props.onFocusOutside,
            (event) => event.preventDefault(),
            { checkForDefaultPrevented: false }
          )
        }
      ) });
    }
  );
  var PopoverContentNonModal = React28.forwardRef(
    (props, forwardedRef) => {
      const context = usePopoverContext(CONTENT_NAME3, props.__scopePopover);
      const hasInteractedOutsideRef = React28.useRef(false);
      const hasPointerDownOutsideRef = React28.useRef(false);
      return /* @__PURE__ */ (0, import_jsx_runtime12.jsx)(
        PopoverContentImpl,
        {
          ...props,
          ref: forwardedRef,
          trapFocus: false,
          disableOutsidePointerEvents: false,
          onCloseAutoFocus: (event) => {
            props.onCloseAutoFocus?.(event);
            if (!event.defaultPrevented) {
              if (!hasInteractedOutsideRef.current) context.triggerRef.current?.focus();
              event.preventDefault();
            }
            hasInteractedOutsideRef.current = false;
            hasPointerDownOutsideRef.current = false;
          },
          onInteractOutside: (event) => {
            props.onInteractOutside?.(event);
            if (!event.defaultPrevented) {
              hasInteractedOutsideRef.current = true;
              if (event.detail.originalEvent.type === "pointerdown") {
                hasPointerDownOutsideRef.current = true;
              }
            }
            const target = event.target;
            const targetIsTrigger = context.triggerRef.current?.contains(target);
            if (targetIsTrigger) event.preventDefault();
            if (event.detail.originalEvent.type === "focusin" && hasPointerDownOutsideRef.current) {
              event.preventDefault();
            }
          }
        }
      );
    }
  );
  var PopoverContentImpl = React28.forwardRef(
    (props, forwardedRef) => {
      const {
        __scopePopover,
        trapFocus,
        onOpenAutoFocus,
        onCloseAutoFocus,
        disableOutsidePointerEvents,
        onEscapeKeyDown,
        onPointerDownOutside,
        onFocusOutside,
        onInteractOutside,
        ...contentProps
      } = props;
      const context = usePopoverContext(CONTENT_NAME3, __scopePopover);
      const popperScope = usePopperScope(__scopePopover);
      useFocusGuards();
      return /* @__PURE__ */ (0, import_jsx_runtime12.jsx)(
        FocusScope,
        {
          asChild: true,
          loop: true,
          trapped: trapFocus,
          onMountAutoFocus: onOpenAutoFocus,
          onUnmountAutoFocus: onCloseAutoFocus,
          children: /* @__PURE__ */ (0, import_jsx_runtime12.jsx)(
            DismissableLayer,
            {
              asChild: true,
              disableOutsidePointerEvents,
              onInteractOutside,
              onEscapeKeyDown,
              onPointerDownOutside,
              onFocusOutside,
              onDismiss: () => context.onOpenChange(false),
              deferPointerDownOutside: true,
              children: /* @__PURE__ */ (0, import_jsx_runtime12.jsx)(
                Content,
                {
                  "data-state": getState2(context.open),
                  role: "dialog",
                  id: context.contentId,
                  ...popperScope,
                  ...contentProps,
                  ref: forwardedRef,
                  style: {
                    ...contentProps.style,
                    // re-namespace exposed content custom properties
                    ...{
                      "--radix-popover-content-transform-origin": "var(--radix-popper-transform-origin)",
                      "--radix-popover-content-available-width": "var(--radix-popper-available-width)",
                      "--radix-popover-content-available-height": "var(--radix-popper-available-height)",
                      "--radix-popover-trigger-width": "var(--radix-popper-anchor-width)",
                      "--radix-popover-trigger-height": "var(--radix-popper-anchor-height)"
                    }
                  }
                }
              )
            }
          )
        }
      );
    }
  );
  var CLOSE_NAME2 = "PopoverClose";
  var PopoverClose = React28.forwardRef(
    (props, forwardedRef) => {
      const { __scopePopover, ...closeProps } = props;
      const context = usePopoverContext(CLOSE_NAME2, __scopePopover);
      return /* @__PURE__ */ (0, import_jsx_runtime12.jsx)(
        Primitive.button,
        {
          type: "button",
          ...closeProps,
          ref: forwardedRef,
          onClick: composeEventHandlers(props.onClick, () => context.onOpenChange(false))
        }
      );
    }
  );
  PopoverClose.displayName = CLOSE_NAME2;
  var ARROW_NAME2 = "PopoverArrow";
  var PopoverArrow = React28.forwardRef(
    (props, forwardedRef) => {
      const { __scopePopover, ...arrowProps } = props;
      const popperScope = usePopperScope(__scopePopover);
      return /* @__PURE__ */ (0, import_jsx_runtime12.jsx)(Arrow2, { ...popperScope, ...arrowProps, ref: forwardedRef });
    }
  );
  PopoverArrow.displayName = ARROW_NAME2;
  function getState2(open) {
    return open ? "open" : "closed";
  }
  var Root23 = Popover;
  var Trigger = PopoverTrigger;
  var Portal2 = PopoverPortal;
  var Content2 = PopoverContent;
  var Arrow22 = PopoverArrow;

  // src/web-ui/react/ui/popover.tsx
  var import_jsx_runtime13 = __toESM(require_jsx_runtime(), 1);
  function WandPopover({
    trigger,
    children,
    ariaLabel,
    align = "center",
    side = "bottom",
    sideOffset = 8,
    className,
    open,
    contentId,
    contentRole,
    forceMount,
    portalled = true,
    showArrow = true,
    onOpenChange
  }) {
    const portalContainer = usePortalContainer();
    const content = /* @__PURE__ */ (0, import_jsx_runtime13.jsxs)(
      Content2,
      {
        id: contentId,
        role: contentRole,
        forceMount,
        className: classNames("wand-ui-popover-content", className),
        "aria-label": ariaLabel,
        align,
        side,
        sideOffset,
        collisionPadding: 12,
        children: [
          children,
          showArrow ? /* @__PURE__ */ (0, import_jsx_runtime13.jsx)(Arrow22, { className: "wand-ui-popover-arrow" }) : null
        ]
      }
    );
    return /* @__PURE__ */ (0, import_jsx_runtime13.jsxs)(Root23, { open, onOpenChange, children: [
      /* @__PURE__ */ (0, import_jsx_runtime13.jsx)(Trigger, { asChild: true, children: trigger }),
      portalled ? /* @__PURE__ */ (0, import_jsx_runtime13.jsx)(Portal2, { container: portalContainer, children: content }) : content
    ] });
  }

  // node_modules/@radix-ui/react-select/dist/index.mjs
  var React33 = __toESM(require_react(), 1);
  var ReactDOM4 = __toESM(require_react_dom(), 1);

  // node_modules/@radix-ui/number/dist/index.mjs
  function clamp2(value, [min2, max2]) {
    return Math.min(max2, Math.max(min2, value));
  }

  // node_modules/@radix-ui/react-collection/dist/index.mjs
  var React29 = __toESM(require_react(), 1);
  var import_jsx_runtime14 = __toESM(require_jsx_runtime(), 1);
  var React210 = __toESM(require_react(), 1);
  var import_jsx_runtime15 = __toESM(require_jsx_runtime(), 1);
  function createCollection(name) {
    const PROVIDER_NAME3 = name + "CollectionProvider";
    const [createCollectionContext, createCollectionScope4] = createContextScope(PROVIDER_NAME3);
    const [CollectionProviderImpl, useCollectionContext] = createCollectionContext(
      PROVIDER_NAME3,
      { collectionRef: { current: null }, itemMap: /* @__PURE__ */ new Map() }
    );
    const CollectionProvider = (props) => {
      const { scope, children } = props;
      const ref = React29.useRef(null);
      const itemMap = React29.useRef(/* @__PURE__ */ new Map()).current;
      return /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(CollectionProviderImpl, { scope, itemMap, collectionRef: ref, children });
    };
    CollectionProvider.displayName = PROVIDER_NAME3;
    const COLLECTION_SLOT_NAME = name + "CollectionSlot";
    const CollectionSlotImpl = createSlot(COLLECTION_SLOT_NAME);
    const CollectionSlot = React29.forwardRef(
      (props, forwardedRef) => {
        const { scope, children } = props;
        const context = useCollectionContext(COLLECTION_SLOT_NAME, scope);
        const composedRefs = useComposedRefs(forwardedRef, context.collectionRef);
        return /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(CollectionSlotImpl, { ref: composedRefs, children });
      }
    );
    CollectionSlot.displayName = COLLECTION_SLOT_NAME;
    const ITEM_SLOT_NAME = name + "CollectionItemSlot";
    const ITEM_DATA_ATTR = "data-radix-collection-item";
    const CollectionItemSlotImpl = createSlot(ITEM_SLOT_NAME);
    const CollectionItemSlot = React29.forwardRef(
      (props, forwardedRef) => {
        const { scope, children, ...itemData } = props;
        const ref = React29.useRef(null);
        const composedRefs = useComposedRefs(forwardedRef, ref);
        const context = useCollectionContext(ITEM_SLOT_NAME, scope);
        React29.useEffect(() => {
          context.itemMap.set(ref, { ref, ...itemData });
          return () => void context.itemMap.delete(ref);
        });
        return /* @__PURE__ */ (0, import_jsx_runtime14.jsx)(CollectionItemSlotImpl, { ...{ [ITEM_DATA_ATTR]: "" }, ref: composedRefs, children });
      }
    );
    CollectionItemSlot.displayName = ITEM_SLOT_NAME;
    function useCollection4(scope) {
      const context = useCollectionContext(name + "CollectionConsumer", scope);
      const getItems = React29.useCallback(() => {
        const collectionNode = context.collectionRef.current;
        if (!collectionNode) return [];
        const orderedNodes = Array.from(collectionNode.querySelectorAll(`[${ITEM_DATA_ATTR}]`));
        const items = Array.from(context.itemMap.values());
        const orderedItems = items.sort(
          (a, b) => orderedNodes.indexOf(a.ref.current) - orderedNodes.indexOf(b.ref.current)
        );
        return orderedItems;
      }, [context.collectionRef, context.itemMap]);
      return getItems;
    }
    return [
      { Provider: CollectionProvider, Slot: CollectionSlot, ItemSlot: CollectionItemSlot },
      useCollection4,
      createCollectionScope4
    ];
  }

  // node_modules/@radix-ui/react-direction/dist/index.mjs
  var React30 = __toESM(require_react(), 1);
  var import_jsx_runtime16 = __toESM(require_jsx_runtime(), 1);
  var DirectionContext = React30.createContext(void 0);
  function useDirection(localDir) {
    const globalDir = React30.useContext(DirectionContext);
    return localDir || globalDir || "ltr";
  }

  // node_modules/@radix-ui/react-use-previous/dist/index.mjs
  var React31 = __toESM(require_react(), 1);
  function usePrevious(value) {
    const ref = React31.useRef({ value, previous: value });
    return React31.useMemo(() => {
      if (ref.current.value !== value) {
        ref.current.previous = ref.current.value;
        ref.current.value = value;
      }
      return ref.current.previous;
    }, [value]);
  }

  // node_modules/@radix-ui/react-visually-hidden/dist/index.mjs
  var React32 = __toESM(require_react(), 1);
  var import_jsx_runtime17 = __toESM(require_jsx_runtime(), 1);
  var VISUALLY_HIDDEN_STYLES = Object.freeze({
    // See: https://github.com/twbs/bootstrap/blob/main/scss/mixins/_visually-hidden.scss
    position: "absolute",
    border: 0,
    width: 1,
    height: 1,
    padding: 0,
    margin: -1,
    overflow: "hidden",
    clip: "rect(0, 0, 0, 0)",
    whiteSpace: "nowrap",
    wordWrap: "normal"
  });
  var NAME2 = "VisuallyHidden";
  var VisuallyHidden = React32.forwardRef(
    (props, forwardedRef) => {
      return /* @__PURE__ */ (0, import_jsx_runtime17.jsx)(
        Primitive.span,
        {
          ...props,
          ref: forwardedRef,
          style: { ...VISUALLY_HIDDEN_STYLES, ...props.style }
        }
      );
    }
  );
  VisuallyHidden.displayName = NAME2;

  // node_modules/@radix-ui/react-select/dist/index.mjs
  var import_jsx_runtime18 = __toESM(require_jsx_runtime(), 1);
  var OPEN_KEYS = [" ", "Enter", "ArrowUp", "ArrowDown"];
  var SELECTION_KEYS = [" ", "Enter"];
  var SELECT_NAME = "Select";
  var [Collection, useCollection, createCollectionScope] = createCollection(SELECT_NAME);
  var [createSelectContext, createSelectScope] = createContextScope(SELECT_NAME, [
    createCollectionScope,
    createPopperScope
  ]);
  var usePopperScope2 = createPopperScope();
  var [SelectProviderImpl, useSelectContext] = createSelectContext(SELECT_NAME);
  var [SelectNativeOptionsProvider, useSelectNativeOptionsContext] = createSelectContext(SELECT_NAME);
  var PROVIDER_NAME = "SelectProvider";
  function SelectProvider(props) {
    const {
      __scopeSelect,
      children,
      open: openProp,
      defaultOpen,
      onOpenChange,
      value: valueProp,
      defaultValue,
      onValueChange,
      dir,
      name,
      autoComplete,
      disabled,
      required,
      form,
      // @ts-expect-error internal render prop used by `Select` to compose its default parts
      internal_do_not_use_render
    } = props;
    const popperScope = usePopperScope2(__scopeSelect);
    const [trigger, setTrigger] = React33.useState(null);
    const [valueNode, setValueNode] = React33.useState(null);
    const [valueNodeHasChildren, setValueNodeHasChildren] = React33.useState(false);
    const direction = useDirection(dir);
    const [open, setOpen] = useControllableState({
      prop: openProp,
      defaultProp: defaultOpen ?? false,
      onChange: onOpenChange,
      caller: SELECT_NAME
    });
    const [value, setValue] = useControllableState({
      prop: valueProp,
      defaultProp: defaultValue,
      onChange: onValueChange,
      caller: SELECT_NAME
    });
    const triggerPointerDownPosRef = React33.useRef(null);
    const initialValueRef = React33.useRef(value);
    React33.useEffect(() => {
      const associatedForm = form ? trigger?.ownerDocument.getElementById(form) : trigger?.form;
      if (associatedForm instanceof HTMLFormElement) {
        const reset = () => setValue(initialValueRef.current);
        associatedForm.addEventListener("reset", reset);
        return () => associatedForm.removeEventListener("reset", reset);
      }
    }, [form, trigger, setValue]);
    const isFormControl = trigger ? !!form || !!trigger.closest("form") : true;
    const [nativeOptionsSet, setNativeOptionsSet] = React33.useState(/* @__PURE__ */ new Set());
    const contentId = useId();
    const nativeSelectKey = Array.from(nativeOptionsSet).map((option) => option.props.value).join(";");
    const handleNativeOptionAdd = React33.useCallback((option) => {
      setNativeOptionsSet((prev) => new Set(prev).add(option));
    }, []);
    const handleNativeOptionRemove = React33.useCallback((option) => {
      setNativeOptionsSet((prev) => {
        const optionsSet = new Set(prev);
        optionsSet.delete(option);
        return optionsSet;
      });
    }, []);
    const context = {
      required,
      trigger,
      onTriggerChange: setTrigger,
      valueNode,
      onValueNodeChange: setValueNode,
      valueNodeHasChildren,
      onValueNodeHasChildrenChange: setValueNodeHasChildren,
      contentId,
      value,
      onValueChange: setValue,
      open,
      onOpenChange: setOpen,
      dir: direction,
      triggerPointerDownPosRef,
      disabled,
      name,
      autoComplete,
      form,
      nativeOptions: nativeOptionsSet,
      nativeSelectKey,
      isFormControl
    };
    return /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(Root22, { ...popperScope, children: /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(SelectProviderImpl, { scope: __scopeSelect, ...context, children: /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(Collection.Provider, { scope: __scopeSelect, children: /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(
      SelectNativeOptionsProvider,
      {
        scope: __scopeSelect,
        onNativeOptionAdd: handleNativeOptionAdd,
        onNativeOptionRemove: handleNativeOptionRemove,
        children: isFunction2(internal_do_not_use_render) ? internal_do_not_use_render(context) : children
      }
    ) }) }) });
  }
  SelectProvider.displayName = PROVIDER_NAME;
  var Select = (props) => {
    const { __scopeSelect, children, ...providerProps } = props;
    return /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(
      SelectProvider,
      {
        __scopeSelect,
        ...providerProps,
        internal_do_not_use_render: ({ isFormControl }) => /* @__PURE__ */ (0, import_jsx_runtime18.jsxs)(import_jsx_runtime18.Fragment, { children: [
          children,
          isFormControl ? /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(
            SelectBubbleInput,
            {
              __scopeSelect
            }
          ) : null
        ] })
      }
    );
  };
  Select.displayName = SELECT_NAME;
  var TRIGGER_NAME3 = "SelectTrigger";
  var SelectTrigger = React33.forwardRef(
    (props, forwardedRef) => {
      const { __scopeSelect, disabled = false, ...triggerProps } = props;
      const popperScope = usePopperScope2(__scopeSelect);
      const context = useSelectContext(TRIGGER_NAME3, __scopeSelect);
      const isDisabled = context.disabled || disabled;
      const composedRefs = useComposedRefs(forwardedRef, context.onTriggerChange);
      const getItems = useCollection(__scopeSelect);
      const pointerTypeRef = React33.useRef("touch");
      const [searchRef, handleTypeaheadSearch, resetTypeahead] = useTypeaheadSearch((search) => {
        const enabledItems = getItems().filter((item) => !item.disabled);
        const currentItem = enabledItems.find((item) => item.value === context.value);
        const nextItem = findNextItem(enabledItems, search, currentItem);
        if (nextItem !== void 0) {
          context.onValueChange(nextItem.value);
        }
      });
      const handleOpen = (pointerEvent) => {
        if (!isDisabled) {
          context.onOpenChange(true);
          resetTypeahead();
        }
        if (pointerEvent) {
          context.triggerPointerDownPosRef.current = {
            x: Math.round(pointerEvent.pageX),
            y: Math.round(pointerEvent.pageY)
          };
        }
      };
      return /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(Anchor, { asChild: true, ...popperScope, children: /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(
        Primitive.button,
        {
          type: "button",
          role: "combobox",
          "aria-controls": context.open ? context.contentId : void 0,
          "aria-expanded": context.open,
          "aria-required": context.required,
          "aria-autocomplete": "none",
          dir: context.dir,
          "data-state": context.open ? "open" : "closed",
          disabled: isDisabled,
          "data-disabled": isDisabled ? "" : void 0,
          "data-placeholder": shouldShowPlaceholder(context.value) ? "" : void 0,
          ...triggerProps,
          ref: composedRefs,
          onClick: composeEventHandlers(triggerProps.onClick, (event) => {
            event.currentTarget.focus();
            if (pointerTypeRef.current !== "mouse") {
              handleOpen(event);
            }
          }),
          onPointerDown: composeEventHandlers(triggerProps.onPointerDown, (event) => {
            pointerTypeRef.current = event.pointerType;
            const target = event.target;
            if (target.hasPointerCapture(event.pointerId)) {
              target.releasePointerCapture(event.pointerId);
            }
            if (event.button === 0 && event.ctrlKey === false && event.pointerType === "mouse") {
              handleOpen(event);
              event.preventDefault();
            }
          }),
          onKeyDown: composeEventHandlers(triggerProps.onKeyDown, (event) => {
            const isTypingAhead = searchRef.current !== "";
            const isModifierKey2 = event.ctrlKey || event.altKey || event.metaKey;
            if (!isModifierKey2 && event.key.length === 1) handleTypeaheadSearch(event.key);
            if (isTypingAhead && event.key === " ") return;
            if (OPEN_KEYS.includes(event.key)) {
              handleOpen();
              event.preventDefault();
            }
          })
        }
      ) });
    }
  );
  SelectTrigger.displayName = TRIGGER_NAME3;
  var VALUE_NAME = "SelectValue";
  var SelectValue = React33.forwardRef(
    (props, forwardedRef) => {
      const { __scopeSelect, className, style, children, placeholder = "", ...valueProps } = props;
      const context = useSelectContext(VALUE_NAME, __scopeSelect);
      const { onValueNodeHasChildrenChange } = context;
      const hasChildren = children !== void 0;
      const composedRefs = useComposedRefs(forwardedRef, context.onValueNodeChange);
      useLayoutEffect2(() => {
        onValueNodeHasChildrenChange(hasChildren);
      }, [onValueNodeHasChildrenChange, hasChildren]);
      const showPlaceholder = shouldShowPlaceholder(context.value);
      return /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(
        Primitive.span,
        {
          ...valueProps,
          asChild: showPlaceholder ? false : valueProps.asChild,
          ref: composedRefs,
          style: { pointerEvents: "none" },
          children: /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(React33.Fragment, { children: showPlaceholder ? placeholder : children }, showPlaceholder ? "placeholder" : "value")
        }
      );
    }
  );
  SelectValue.displayName = VALUE_NAME;
  var ICON_NAME = "SelectIcon";
  var SelectIcon = React33.forwardRef(
    (props, forwardedRef) => {
      const { __scopeSelect, children, ...iconProps } = props;
      return /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(Primitive.span, { "aria-hidden": true, ...iconProps, ref: forwardedRef, children: children || "\u25BC" });
    }
  );
  SelectIcon.displayName = ICON_NAME;
  var PORTAL_NAME4 = "SelectPortal";
  var [PortalProvider3, usePortalContext3] = createSelectContext(PORTAL_NAME4, {
    forceMount: void 0
  });
  var SelectPortal = (props) => {
    const { __scopeSelect, forceMount, ...portalProps } = props;
    return /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(PortalProvider3, { scope: props.__scopeSelect, forceMount, children: /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(Portal, { asChild: true, ...portalProps }) });
  };
  SelectPortal.displayName = PORTAL_NAME4;
  var CONTENT_NAME4 = "SelectContent";
  var SelectContent = React33.forwardRef(
    (props, forwardedRef) => {
      const portalContext = usePortalContext3(CONTENT_NAME4, props.__scopeSelect);
      const { forceMount = portalContext.forceMount, ...contentProps } = props;
      const context = useSelectContext(CONTENT_NAME4, props.__scopeSelect);
      const [fragment, setFragment] = React33.useState();
      useLayoutEffect2(() => {
        setFragment(new DocumentFragment());
      }, []);
      return /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(Presence, { present: forceMount || context.open, children: ({ present }) => present ? /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(SelectContentImpl, { ...contentProps, ref: forwardedRef }) : /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(SelectContentFragment, { ...contentProps, fragment }) });
    }
  );
  SelectContent.displayName = CONTENT_NAME4;
  var SelectContentFragment = React33.forwardRef((props, forwardedRef) => {
    const { __scopeSelect, children, fragment } = props;
    if (!fragment) return null;
    return ReactDOM4.createPortal(
      /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(SelectContentProvider, { scope: __scopeSelect, children: /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(Collection.Slot, { scope: __scopeSelect, children: /* @__PURE__ */ (0, import_jsx_runtime18.jsx)("div", { ref: forwardedRef, children }) }) }),
      fragment
    );
  });
  SelectContentFragment.displayName = "SelectContentFragment";
  var CONTENT_MARGIN = 10;
  var [SelectContentProvider, useSelectContentContext] = createSelectContext(CONTENT_NAME4);
  var CONTENT_IMPL_NAME = "SelectContentImpl";
  var Slot3 = createSlot("SelectContent.RemoveScroll");
  var SelectContentImpl = React33.forwardRef(
    (props, forwardedRef) => {
      const { __scopeSelect } = props;
      const {
        position = "item-aligned",
        onCloseAutoFocus,
        onEscapeKeyDown,
        onPointerDownOutside,
        //
        // PopperContent props
        side,
        sideOffset,
        align,
        alignOffset,
        arrowPadding,
        collisionBoundary,
        collisionPadding,
        sticky,
        hideWhenDetached,
        avoidCollisions,
        //
        ...contentProps
      } = props;
      const context = useSelectContext(CONTENT_NAME4, __scopeSelect);
      const [content, setContent] = React33.useState(null);
      const [viewport, setViewport] = React33.useState(null);
      const composedRefs = useComposedRefs(forwardedRef, setContent);
      const [selectedItem, setSelectedItem] = React33.useState(null);
      const [selectedItemText, setSelectedItemText] = React33.useState(
        null
      );
      const getItems = useCollection(__scopeSelect);
      const [isPositioned, setIsPositioned] = React33.useState(false);
      const firstValidItemFoundRef = React33.useRef(false);
      React33.useEffect(() => {
        if (content) return hideOthers(content);
      }, [content]);
      useFocusGuards();
      const focusFirst4 = React33.useCallback(
        (candidates) => {
          const [firstItem, ...restItems] = getItems().map((item) => item.ref.current);
          const [lastItem] = restItems.slice(-1);
          const PREVIOUSLY_FOCUSED_ELEMENT = document.activeElement;
          for (const candidate of candidates) {
            if (candidate === PREVIOUSLY_FOCUSED_ELEMENT) return;
            candidate?.scrollIntoView({ block: "nearest" });
            if (candidate === firstItem && viewport) viewport.scrollTop = 0;
            if (candidate === lastItem && viewport) viewport.scrollTop = viewport.scrollHeight;
            candidate?.focus();
            if (document.activeElement !== PREVIOUSLY_FOCUSED_ELEMENT) return;
          }
        },
        [getItems, viewport]
      );
      const focusSelectedItem = React33.useCallback(
        () => focusFirst4([selectedItem, content]),
        [focusFirst4, selectedItem, content]
      );
      React33.useEffect(() => {
        if (isPositioned) {
          focusSelectedItem();
        }
      }, [isPositioned, focusSelectedItem]);
      const { onOpenChange, triggerPointerDownPosRef } = context;
      React33.useEffect(() => {
        if (content) {
          let pointerMoveDelta = { x: 0, y: 0 };
          const handlePointerMove = (event) => {
            pointerMoveDelta = {
              x: Math.abs(Math.round(event.pageX) - (triggerPointerDownPosRef.current?.x ?? 0)),
              y: Math.abs(Math.round(event.pageY) - (triggerPointerDownPosRef.current?.y ?? 0))
            };
          };
          const handlePointerUp = (event) => {
            if (pointerMoveDelta.x <= 10 && pointerMoveDelta.y <= 10) {
              event.preventDefault();
            } else {
              if (!event.composedPath().includes(content)) {
                onOpenChange(false);
              }
            }
            document.removeEventListener("pointermove", handlePointerMove);
            triggerPointerDownPosRef.current = null;
          };
          if (triggerPointerDownPosRef.current !== null) {
            document.addEventListener("pointermove", handlePointerMove);
            document.addEventListener("pointerup", handlePointerUp, { capture: true, once: true });
          }
          return () => {
            document.removeEventListener("pointermove", handlePointerMove);
            document.removeEventListener("pointerup", handlePointerUp, { capture: true });
          };
        }
      }, [content, onOpenChange, triggerPointerDownPosRef]);
      React33.useEffect(() => {
        const close = () => onOpenChange(false);
        window.addEventListener("blur", close);
        window.addEventListener("resize", close);
        return () => {
          window.removeEventListener("blur", close);
          window.removeEventListener("resize", close);
        };
      }, [onOpenChange]);
      const [searchRef, handleTypeaheadSearch] = useTypeaheadSearch((search) => {
        const enabledItems = getItems().filter((item) => !item.disabled);
        const currentItem = enabledItems.find((item) => item.ref.current === document.activeElement);
        const nextItem = findNextItem(enabledItems, search, currentItem);
        if (nextItem) {
          setTimeout(() => nextItem.ref.current?.focus());
        }
      });
      const itemRefCallback = React33.useCallback(
        (node, value, disabled) => {
          const isFirstValidItem = !firstValidItemFoundRef.current && !disabled;
          const isSelectedItem = context.value !== void 0 && context.value === value;
          if (isSelectedItem || isFirstValidItem) {
            setSelectedItem(node);
            if (isFirstValidItem) firstValidItemFoundRef.current = true;
          }
        },
        [context.value]
      );
      const handleItemLeave = React33.useCallback(() => content?.focus(), [content]);
      const itemTextRefCallback = React33.useCallback(
        (node, value, disabled) => {
          const isFirstValidItem = !firstValidItemFoundRef.current && !disabled;
          const isSelectedItem = context.value !== void 0 && context.value === value;
          if (isSelectedItem || isFirstValidItem) {
            setSelectedItemText(node);
          }
        },
        [context.value]
      );
      const SelectPosition = position === "popper" ? SelectPopperPosition : SelectItemAlignedPosition;
      const popperContentProps = SelectPosition === SelectPopperPosition ? {
        side,
        sideOffset,
        align,
        alignOffset,
        arrowPadding,
        collisionBoundary,
        collisionPadding,
        sticky,
        hideWhenDetached,
        avoidCollisions
      } : {};
      return /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(
        SelectContentProvider,
        {
          scope: __scopeSelect,
          content,
          viewport,
          onViewportChange: setViewport,
          itemRefCallback,
          selectedItem,
          onItemLeave: handleItemLeave,
          itemTextRefCallback,
          focusSelectedItem,
          selectedItemText,
          position,
          isPositioned,
          searchRef,
          children: /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(Combination_default, { as: Slot3, allowPinchZoom: true, children: /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(
            FocusScope,
            {
              asChild: true,
              trapped: context.open,
              onMountAutoFocus: (event) => {
                event.preventDefault();
              },
              onUnmountAutoFocus: composeEventHandlers(onCloseAutoFocus, (event) => {
                context.trigger?.focus({ preventScroll: true });
                event.preventDefault();
              }),
              children: /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(
                DismissableLayer,
                {
                  asChild: true,
                  disableOutsidePointerEvents: true,
                  onEscapeKeyDown,
                  onPointerDownOutside,
                  onFocusOutside: (event) => event.preventDefault(),
                  onDismiss: () => context.onOpenChange(false),
                  children: /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(
                    SelectPosition,
                    {
                      role: "listbox",
                      id: context.contentId,
                      "data-state": context.open ? "open" : "closed",
                      dir: context.dir,
                      onContextMenu: (event) => event.preventDefault(),
                      ...contentProps,
                      ...popperContentProps,
                      onPlaced: () => setIsPositioned(true),
                      ref: composedRefs,
                      style: {
                        // flex layout so we can place the scroll buttons properly
                        display: "flex",
                        flexDirection: "column",
                        // reset the outline by default as the content MAY get focused
                        outline: "none",
                        ...contentProps.style
                      },
                      onKeyDown: composeEventHandlers(contentProps.onKeyDown, (event) => {
                        const isModifierKey2 = event.ctrlKey || event.altKey || event.metaKey;
                        if (event.key === "Tab") event.preventDefault();
                        if (!isModifierKey2 && event.key.length === 1) handleTypeaheadSearch(event.key);
                        if (["ArrowUp", "ArrowDown", "Home", "End"].includes(event.key)) {
                          const items = getItems().filter((item) => !item.disabled);
                          let candidateNodes = items.map((item) => item.ref.current);
                          if (["ArrowUp", "End"].includes(event.key)) {
                            candidateNodes = candidateNodes.slice().reverse();
                          }
                          if (["ArrowUp", "ArrowDown"].includes(event.key)) {
                            const currentElement = event.target;
                            const currentIndex = candidateNodes.indexOf(currentElement);
                            candidateNodes = candidateNodes.slice(currentIndex + 1);
                          }
                          setTimeout(() => focusFirst4(candidateNodes));
                          event.preventDefault();
                        }
                      })
                    }
                  )
                }
              )
            }
          ) })
        }
      );
    }
  );
  SelectContentImpl.displayName = CONTENT_IMPL_NAME;
  var ITEM_ALIGNED_POSITION_NAME = "SelectItemAlignedPosition";
  var SelectItemAlignedPosition = React33.forwardRef((props, forwardedRef) => {
    const { __scopeSelect, onPlaced, ...popperProps } = props;
    const context = useSelectContext(CONTENT_NAME4, __scopeSelect);
    const contentContext = useSelectContentContext(CONTENT_NAME4, __scopeSelect);
    const [contentWrapper, setContentWrapper] = React33.useState(null);
    const [content, setContent] = React33.useState(null);
    const composedRefs = useComposedRefs(forwardedRef, setContent);
    const getItems = useCollection(__scopeSelect);
    const shouldExpandOnScrollRef = React33.useRef(false);
    const shouldRepositionRef = React33.useRef(true);
    const { viewport, selectedItem, selectedItemText, focusSelectedItem } = contentContext;
    const position = React33.useCallback(() => {
      if (context.trigger && context.valueNode && contentWrapper && content && viewport && selectedItem && selectedItemText) {
        const triggerRect = context.trigger.getBoundingClientRect();
        const contentRect = content.getBoundingClientRect();
        const valueNodeRect = context.valueNode.getBoundingClientRect();
        const itemTextRect = selectedItemText.getBoundingClientRect();
        if (context.dir !== "rtl") {
          const itemTextOffset = itemTextRect.left - contentRect.left;
          const left = valueNodeRect.left - itemTextOffset;
          const leftDelta = triggerRect.left - left;
          const minContentWidth = triggerRect.width + leftDelta;
          const contentWidth = Math.max(minContentWidth, contentRect.width);
          const rightEdge = window.innerWidth - CONTENT_MARGIN;
          const clampedLeft = clamp2(left, [
            CONTENT_MARGIN,
            // Prevents the content from going off the starting edge of the
            // viewport. It may still go off the ending edge, but this can be
            // controlled by the user since they may want to manage overflow in a
            // specific way.
            // https://github.com/radix-ui/primitives/issues/2049
            Math.max(CONTENT_MARGIN, rightEdge - contentWidth)
          ]);
          contentWrapper.style.minWidth = minContentWidth + "px";
          contentWrapper.style.left = clampedLeft + "px";
        } else {
          const itemTextOffset = contentRect.right - itemTextRect.right;
          const right = window.innerWidth - valueNodeRect.right - itemTextOffset;
          const rightDelta = window.innerWidth - triggerRect.right - right;
          const minContentWidth = triggerRect.width + rightDelta;
          const contentWidth = Math.max(minContentWidth, contentRect.width);
          const leftEdge = window.innerWidth - CONTENT_MARGIN;
          const clampedRight = clamp2(right, [
            CONTENT_MARGIN,
            Math.max(CONTENT_MARGIN, leftEdge - contentWidth)
          ]);
          contentWrapper.style.minWidth = minContentWidth + "px";
          contentWrapper.style.right = clampedRight + "px";
        }
        const items = getItems();
        const availableHeight = window.innerHeight - CONTENT_MARGIN * 2;
        const itemsHeight = viewport.scrollHeight;
        const contentStyles = window.getComputedStyle(content);
        const contentBorderTopWidth = parseInt(contentStyles.borderTopWidth, 10);
        const contentPaddingTop = parseInt(contentStyles.paddingTop, 10);
        const contentBorderBottomWidth = parseInt(contentStyles.borderBottomWidth, 10);
        const contentPaddingBottom = parseInt(contentStyles.paddingBottom, 10);
        const fullContentHeight = contentBorderTopWidth + contentPaddingTop + itemsHeight + contentPaddingBottom + contentBorderBottomWidth;
        const minContentHeight = Math.min(selectedItem.offsetHeight * 5, fullContentHeight);
        const viewportStyles = window.getComputedStyle(viewport);
        const viewportPaddingTop = parseInt(viewportStyles.paddingTop, 10);
        const viewportPaddingBottom = parseInt(viewportStyles.paddingBottom, 10);
        const topEdgeToTriggerMiddle = triggerRect.top + triggerRect.height / 2 - CONTENT_MARGIN;
        const triggerMiddleToBottomEdge = availableHeight - topEdgeToTriggerMiddle;
        const selectedItemHalfHeight = selectedItem.offsetHeight / 2;
        const itemOffsetMiddle = selectedItem.offsetTop + selectedItemHalfHeight;
        const contentTopToItemMiddle = contentBorderTopWidth + contentPaddingTop + itemOffsetMiddle;
        const itemMiddleToContentBottom = fullContentHeight - contentTopToItemMiddle;
        const willAlignWithoutTopOverflow = contentTopToItemMiddle <= topEdgeToTriggerMiddle;
        if (willAlignWithoutTopOverflow) {
          const isLastItem = items.length > 0 && selectedItem === items[items.length - 1].ref.current;
          contentWrapper.style.bottom = "0px";
          const viewportOffsetBottom = content.clientHeight - viewport.offsetTop - viewport.offsetHeight;
          const clampedTriggerMiddleToBottomEdge = Math.max(
            triggerMiddleToBottomEdge,
            selectedItemHalfHeight + // viewport might have padding bottom, include it to avoid a scrollable viewport
            (isLastItem ? viewportPaddingBottom : 0) + viewportOffsetBottom + contentBorderBottomWidth
          );
          const height = contentTopToItemMiddle + clampedTriggerMiddleToBottomEdge;
          contentWrapper.style.height = height + "px";
        } else {
          const isFirstItem = items.length > 0 && selectedItem === items[0].ref.current;
          contentWrapper.style.top = "0px";
          const clampedTopEdgeToTriggerMiddle = Math.max(
            topEdgeToTriggerMiddle,
            contentBorderTopWidth + viewport.offsetTop + // viewport might have padding top, include it to avoid a scrollable viewport
            (isFirstItem ? viewportPaddingTop : 0) + selectedItemHalfHeight
          );
          const height = clampedTopEdgeToTriggerMiddle + itemMiddleToContentBottom;
          contentWrapper.style.height = height + "px";
          viewport.scrollTop = contentTopToItemMiddle - topEdgeToTriggerMiddle + viewport.offsetTop;
        }
        contentWrapper.style.margin = `${CONTENT_MARGIN}px 0`;
        contentWrapper.style.minHeight = minContentHeight + "px";
        contentWrapper.style.maxHeight = availableHeight + "px";
        onPlaced?.();
        requestAnimationFrame(() => shouldExpandOnScrollRef.current = true);
      }
    }, [
      getItems,
      context.trigger,
      context.valueNode,
      contentWrapper,
      content,
      viewport,
      selectedItem,
      selectedItemText,
      context.dir,
      onPlaced
    ]);
    useLayoutEffect2(() => position(), [position]);
    const [contentZIndex, setContentZIndex] = React33.useState();
    useLayoutEffect2(() => {
      if (content) setContentZIndex(window.getComputedStyle(content).zIndex);
    }, [content]);
    const handleScrollButtonChange = React33.useCallback(
      (node) => {
        if (node && shouldRepositionRef.current === true) {
          position();
          focusSelectedItem?.();
          shouldRepositionRef.current = false;
        }
      },
      [position, focusSelectedItem]
    );
    return /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(
      SelectViewportProvider,
      {
        scope: __scopeSelect,
        contentWrapper,
        shouldExpandOnScrollRef,
        onScrollButtonChange: handleScrollButtonChange,
        children: /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(
          "div",
          {
            ref: setContentWrapper,
            style: {
              display: "flex",
              flexDirection: "column",
              position: "fixed",
              zIndex: contentZIndex
            },
            children: /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(
              Primitive.div,
              {
                ...popperProps,
                ref: composedRefs,
                style: {
                  // When we get the height of the content, it includes borders. If we were to set
                  // the height without having `boxSizing: 'border-box'` it would be too big.
                  boxSizing: "border-box",
                  // We need to ensure the content doesn't get taller than the wrapper
                  maxHeight: "100%",
                  ...popperProps.style
                }
              }
            )
          }
        )
      }
    );
  });
  SelectItemAlignedPosition.displayName = ITEM_ALIGNED_POSITION_NAME;
  var POPPER_POSITION_NAME = "SelectPopperPosition";
  var SelectPopperPosition = React33.forwardRef((props, forwardedRef) => {
    const {
      __scopeSelect,
      align = "start",
      collisionPadding = CONTENT_MARGIN,
      ...popperProps
    } = props;
    const popperScope = usePopperScope2(__scopeSelect);
    return /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(
      Content,
      {
        ...popperScope,
        ...popperProps,
        ref: forwardedRef,
        align,
        collisionPadding,
        style: {
          // Ensure border-box for floating-ui calculations
          boxSizing: "border-box",
          ...popperProps.style,
          // re-namespace exposed content custom properties
          ...{
            "--radix-select-content-transform-origin": "var(--radix-popper-transform-origin)",
            "--radix-select-content-available-width": "var(--radix-popper-available-width)",
            "--radix-select-content-available-height": "var(--radix-popper-available-height)",
            "--radix-select-trigger-width": "var(--radix-popper-anchor-width)",
            "--radix-select-trigger-height": "var(--radix-popper-anchor-height)"
          }
        }
      }
    );
  });
  SelectPopperPosition.displayName = POPPER_POSITION_NAME;
  var [SelectViewportProvider, useSelectViewportContext] = createSelectContext(CONTENT_NAME4, {});
  var VIEWPORT_NAME = "SelectViewport";
  var SelectViewport = React33.forwardRef(
    (props, forwardedRef) => {
      const { __scopeSelect, nonce, ...viewportProps } = props;
      const contentContext = useSelectContentContext(VIEWPORT_NAME, __scopeSelect);
      const viewportContext = useSelectViewportContext(VIEWPORT_NAME, __scopeSelect);
      const composedRefs = useComposedRefs(forwardedRef, contentContext.onViewportChange);
      const prevScrollTopRef = React33.useRef(0);
      return /* @__PURE__ */ (0, import_jsx_runtime18.jsxs)(import_jsx_runtime18.Fragment, { children: [
        /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(
          "style",
          {
            dangerouslySetInnerHTML: {
              __html: `[data-radix-select-viewport]{scrollbar-width:none;-ms-overflow-style:none;-webkit-overflow-scrolling:touch;}[data-radix-select-viewport]::-webkit-scrollbar{display:none}`
            },
            nonce
          }
        ),
        /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(Collection.Slot, { scope: __scopeSelect, children: /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(
          Primitive.div,
          {
            "data-radix-select-viewport": "",
            role: "presentation",
            ...viewportProps,
            ref: composedRefs,
            style: {
              // we use position: 'relative' here on the `viewport` so that when we call
              // `selectedItem.offsetTop` in calculations, the offset is relative to the viewport
              // (independent of the scrollUpButton).
              position: "relative",
              flex: 1,
              // Viewport should only be scrollable in the vertical direction.
              // This won't work in vertical writing modes, so we'll need to
              // revisit this if/when that is supported
              // https://developer.chrome.com/blog/vertical-form-controls
              overflow: "hidden auto",
              ...viewportProps.style
            },
            onScroll: composeEventHandlers(viewportProps.onScroll, (event) => {
              const viewport = event.currentTarget;
              const { contentWrapper, shouldExpandOnScrollRef } = viewportContext;
              if (shouldExpandOnScrollRef?.current && contentWrapper) {
                const scrolledBy = Math.abs(prevScrollTopRef.current - viewport.scrollTop);
                if (scrolledBy > 0) {
                  const availableHeight = window.innerHeight - CONTENT_MARGIN * 2;
                  const cssMinHeight = parseFloat(contentWrapper.style.minHeight);
                  const cssHeight = parseFloat(contentWrapper.style.height);
                  const prevHeight = Math.max(cssMinHeight, cssHeight);
                  if (prevHeight < availableHeight) {
                    const nextHeight = prevHeight + scrolledBy;
                    const clampedNextHeight = Math.min(availableHeight, nextHeight);
                    const heightDiff = nextHeight - clampedNextHeight;
                    contentWrapper.style.height = clampedNextHeight + "px";
                    if (contentWrapper.style.bottom === "0px") {
                      viewport.scrollTop = heightDiff > 0 ? heightDiff : 0;
                      contentWrapper.style.justifyContent = "flex-end";
                    }
                  }
                }
              }
              prevScrollTopRef.current = viewport.scrollTop;
            })
          }
        ) })
      ] });
    }
  );
  SelectViewport.displayName = VIEWPORT_NAME;
  var GROUP_NAME = "SelectGroup";
  var [SelectGroupContextProvider, useSelectGroupContext] = createSelectContext(GROUP_NAME);
  var SelectGroup = React33.forwardRef(
    (props, forwardedRef) => {
      const { __scopeSelect, ...groupProps } = props;
      const groupId = useId();
      return /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(SelectGroupContextProvider, { scope: __scopeSelect, id: groupId, children: /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(Primitive.div, { role: "group", "aria-labelledby": groupId, ...groupProps, ref: forwardedRef }) });
    }
  );
  SelectGroup.displayName = GROUP_NAME;
  var LABEL_NAME = "SelectLabel";
  var SelectLabel = React33.forwardRef(
    (props, forwardedRef) => {
      const { __scopeSelect, ...labelProps } = props;
      const groupContext = useSelectGroupContext(LABEL_NAME, __scopeSelect);
      return /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(Primitive.div, { id: groupContext.id, ...labelProps, ref: forwardedRef });
    }
  );
  SelectLabel.displayName = LABEL_NAME;
  var ITEM_NAME = "SelectItem";
  var [SelectItemContextProvider, useSelectItemContext] = createSelectContext(ITEM_NAME);
  var SelectItem = React33.forwardRef(
    (props, forwardedRef) => {
      const {
        __scopeSelect,
        value,
        disabled = false,
        textValue: textValueProp,
        ...itemProps
      } = props;
      const context = useSelectContext(ITEM_NAME, __scopeSelect);
      const contentContext = useSelectContentContext(ITEM_NAME, __scopeSelect);
      const isSelected = context.value === value;
      const [textValue, setTextValue] = React33.useState(textValueProp ?? "");
      const [isFocused, setIsFocused] = React33.useState(false);
      const handleItemRefCallback = useCallbackRef(
        (node) => contentContext.itemRefCallback?.(node, value, disabled)
      );
      const composedRefs = useComposedRefs(forwardedRef, handleItemRefCallback);
      const textId = useId();
      const pointerTypeRef = React33.useRef("touch");
      const handleSelect = () => {
        if (!disabled) {
          context.onValueChange(value);
          context.onOpenChange(false);
        }
      };
      return /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(
        SelectItemContextProvider,
        {
          scope: __scopeSelect,
          value,
          disabled,
          textId,
          isSelected,
          onItemTextChange: React33.useCallback((node) => {
            setTextValue((prevTextValue) => prevTextValue || (node?.textContent ?? "").trim());
          }, []),
          children: /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(
            Collection.ItemSlot,
            {
              scope: __scopeSelect,
              value,
              disabled,
              textValue,
              children: /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(
                Primitive.div,
                {
                  role: "option",
                  "aria-labelledby": textId,
                  "data-highlighted": isFocused ? "" : void 0,
                  "aria-selected": isSelected && isFocused,
                  "data-state": isSelected ? "checked" : "unchecked",
                  "aria-disabled": disabled || void 0,
                  "data-disabled": disabled ? "" : void 0,
                  tabIndex: disabled ? void 0 : -1,
                  ...itemProps,
                  ref: composedRefs,
                  onFocus: composeEventHandlers(itemProps.onFocus, () => setIsFocused(true)),
                  onBlur: composeEventHandlers(itemProps.onBlur, () => setIsFocused(false)),
                  onClick: composeEventHandlers(itemProps.onClick, () => {
                    if (pointerTypeRef.current !== "mouse") handleSelect();
                  }),
                  onPointerUp: composeEventHandlers(itemProps.onPointerUp, () => {
                    if (pointerTypeRef.current === "mouse") handleSelect();
                  }),
                  onPointerDown: composeEventHandlers(itemProps.onPointerDown, (event) => {
                    pointerTypeRef.current = event.pointerType;
                  }),
                  onPointerMove: composeEventHandlers(itemProps.onPointerMove, (event) => {
                    pointerTypeRef.current = event.pointerType;
                    if (disabled) {
                      contentContext.onItemLeave?.();
                    } else if (pointerTypeRef.current === "mouse") {
                      event.currentTarget.focus({ preventScroll: true });
                    }
                  }),
                  onPointerLeave: composeEventHandlers(itemProps.onPointerLeave, (event) => {
                    if (event.currentTarget === document.activeElement) {
                      contentContext.onItemLeave?.();
                    }
                  }),
                  onKeyDown: composeEventHandlers(itemProps.onKeyDown, (event) => {
                    if (disabled || event.target !== event.currentTarget) {
                      return;
                    }
                    const isTypingAhead = contentContext.searchRef?.current !== "";
                    if (isTypingAhead && event.key === " ") {
                      return;
                    }
                    if (SELECTION_KEYS.includes(event.key)) {
                      handleSelect();
                    }
                    if (event.key === " ") {
                      event.preventDefault();
                    }
                  })
                }
              )
            }
          )
        }
      );
    }
  );
  SelectItem.displayName = ITEM_NAME;
  var ITEM_TEXT_NAME = "SelectItemText";
  var SelectItemText = React33.forwardRef(
    (props, forwardedRef) => {
      const { __scopeSelect, className, style, ...itemTextProps } = props;
      const context = useSelectContext(ITEM_TEXT_NAME, __scopeSelect);
      const contentContext = useSelectContentContext(ITEM_TEXT_NAME, __scopeSelect);
      const itemContext = useSelectItemContext(ITEM_TEXT_NAME, __scopeSelect);
      const nativeOptionsContext = useSelectNativeOptionsContext(ITEM_TEXT_NAME, __scopeSelect);
      const [itemTextNode, setItemTextNode] = React33.useState(null);
      const handleItemTextRefCallback = useCallbackRef(
        (node) => contentContext.itemTextRefCallback?.(node, itemContext.value, itemContext.disabled)
      );
      const composedRefs = useComposedRefs(
        forwardedRef,
        setItemTextNode,
        itemContext.onItemTextChange,
        handleItemTextRefCallback
      );
      const textContent = itemTextNode?.textContent;
      const nativeOption = React33.useMemo(
        () => /* @__PURE__ */ (0, import_jsx_runtime18.jsx)("option", { value: itemContext.value, disabled: itemContext.disabled, children: textContent }, itemContext.value),
        [itemContext.disabled, itemContext.value, textContent]
      );
      const { onNativeOptionAdd, onNativeOptionRemove } = nativeOptionsContext;
      useLayoutEffect2(() => {
        onNativeOptionAdd(nativeOption);
        return () => onNativeOptionRemove(nativeOption);
      }, [onNativeOptionAdd, onNativeOptionRemove, nativeOption]);
      return /* @__PURE__ */ (0, import_jsx_runtime18.jsxs)(import_jsx_runtime18.Fragment, { children: [
        /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(Primitive.span, { id: itemContext.textId, ...itemTextProps, ref: composedRefs }),
        itemContext.isSelected && context.valueNode && !context.valueNodeHasChildren && !shouldShowPlaceholder(context.value) ? ReactDOM4.createPortal(itemTextProps.children, context.valueNode) : null
      ] });
    }
  );
  SelectItemText.displayName = ITEM_TEXT_NAME;
  var ITEM_INDICATOR_NAME = "SelectItemIndicator";
  var SelectItemIndicator = React33.forwardRef(
    (props, forwardedRef) => {
      const { __scopeSelect, ...itemIndicatorProps } = props;
      const itemContext = useSelectItemContext(ITEM_INDICATOR_NAME, __scopeSelect);
      return itemContext.isSelected ? /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(Primitive.span, { "aria-hidden": true, ...itemIndicatorProps, ref: forwardedRef }) : null;
    }
  );
  SelectItemIndicator.displayName = ITEM_INDICATOR_NAME;
  var SCROLL_UP_BUTTON_NAME = "SelectScrollUpButton";
  var SelectScrollUpButton = React33.forwardRef((props, forwardedRef) => {
    const contentContext = useSelectContentContext(SCROLL_UP_BUTTON_NAME, props.__scopeSelect);
    const viewportContext = useSelectViewportContext(SCROLL_UP_BUTTON_NAME, props.__scopeSelect);
    const [canScrollUp, setCanScrollUp] = React33.useState(false);
    const composedRefs = useComposedRefs(forwardedRef, viewportContext.onScrollButtonChange);
    useLayoutEffect2(() => {
      if (contentContext.viewport && contentContext.isPositioned) {
        let handleScroll22 = function() {
          const canScrollUp2 = viewport.scrollTop > 0;
          setCanScrollUp(canScrollUp2);
        };
        var handleScroll2 = handleScroll22;
        const viewport = contentContext.viewport;
        handleScroll22();
        viewport.addEventListener("scroll", handleScroll22);
        return () => viewport.removeEventListener("scroll", handleScroll22);
      }
    }, [contentContext.viewport, contentContext.isPositioned]);
    return canScrollUp ? /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(
      SelectScrollButtonImpl,
      {
        ...props,
        ref: composedRefs,
        onAutoScroll: () => {
          const { viewport, selectedItem } = contentContext;
          if (viewport && selectedItem) {
            viewport.scrollTop = viewport.scrollTop - selectedItem.offsetHeight;
          }
        }
      }
    ) : null;
  });
  SelectScrollUpButton.displayName = SCROLL_UP_BUTTON_NAME;
  var SCROLL_DOWN_BUTTON_NAME = "SelectScrollDownButton";
  var SelectScrollDownButton = React33.forwardRef((props, forwardedRef) => {
    const contentContext = useSelectContentContext(SCROLL_DOWN_BUTTON_NAME, props.__scopeSelect);
    const viewportContext = useSelectViewportContext(SCROLL_DOWN_BUTTON_NAME, props.__scopeSelect);
    const [canScrollDown, setCanScrollDown] = React33.useState(false);
    const composedRefs = useComposedRefs(forwardedRef, viewportContext.onScrollButtonChange);
    useLayoutEffect2(() => {
      if (contentContext.viewport && contentContext.isPositioned) {
        let handleScroll22 = function() {
          const maxScroll = viewport.scrollHeight - viewport.clientHeight;
          const canScrollDown2 = Math.ceil(viewport.scrollTop) < maxScroll;
          setCanScrollDown(canScrollDown2);
        };
        var handleScroll2 = handleScroll22;
        const viewport = contentContext.viewport;
        handleScroll22();
        viewport.addEventListener("scroll", handleScroll22);
        return () => viewport.removeEventListener("scroll", handleScroll22);
      }
    }, [contentContext.viewport, contentContext.isPositioned]);
    return canScrollDown ? /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(
      SelectScrollButtonImpl,
      {
        ...props,
        ref: composedRefs,
        onAutoScroll: () => {
          const { viewport, selectedItem } = contentContext;
          if (viewport && selectedItem) {
            viewport.scrollTop = viewport.scrollTop + selectedItem.offsetHeight;
          }
        }
      }
    ) : null;
  });
  SelectScrollDownButton.displayName = SCROLL_DOWN_BUTTON_NAME;
  var SelectScrollButtonImpl = React33.forwardRef((props, forwardedRef) => {
    const { __scopeSelect, onAutoScroll, ...scrollIndicatorProps } = props;
    const contentContext = useSelectContentContext("SelectScrollButton", __scopeSelect);
    const autoScrollTimerRef = React33.useRef(null);
    const getItems = useCollection(__scopeSelect);
    const clearAutoScrollTimer = React33.useCallback(() => {
      if (autoScrollTimerRef.current !== null) {
        window.clearInterval(autoScrollTimerRef.current);
        autoScrollTimerRef.current = null;
      }
    }, []);
    React33.useEffect(() => {
      return () => clearAutoScrollTimer();
    }, [clearAutoScrollTimer]);
    useLayoutEffect2(() => {
      const activeItem = getItems().find((item) => item.ref.current === document.activeElement);
      activeItem?.ref.current?.scrollIntoView({ block: "nearest" });
    }, [getItems]);
    return /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(
      Primitive.div,
      {
        "aria-hidden": true,
        ...scrollIndicatorProps,
        ref: forwardedRef,
        style: { flexShrink: 0, ...scrollIndicatorProps.style },
        onPointerDown: composeEventHandlers(scrollIndicatorProps.onPointerDown, () => {
          if (autoScrollTimerRef.current === null) {
            autoScrollTimerRef.current = window.setInterval(onAutoScroll, 50);
          }
        }),
        onPointerMove: composeEventHandlers(scrollIndicatorProps.onPointerMove, () => {
          contentContext.onItemLeave?.();
          if (autoScrollTimerRef.current === null) {
            autoScrollTimerRef.current = window.setInterval(onAutoScroll, 50);
          }
        }),
        onPointerLeave: composeEventHandlers(scrollIndicatorProps.onPointerLeave, () => {
          clearAutoScrollTimer();
        })
      }
    );
  });
  var SEPARATOR_NAME = "SelectSeparator";
  var SelectSeparator = React33.forwardRef(
    (props, forwardedRef) => {
      const { __scopeSelect, ...separatorProps } = props;
      return /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(Primitive.div, { "aria-hidden": true, ...separatorProps, ref: forwardedRef });
    }
  );
  SelectSeparator.displayName = SEPARATOR_NAME;
  var ARROW_NAME3 = "SelectArrow";
  var SelectArrow = React33.forwardRef(
    (props, forwardedRef) => {
      const { __scopeSelect, ...arrowProps } = props;
      const popperScope = usePopperScope2(__scopeSelect);
      const contentContext = useSelectContentContext(ARROW_NAME3, __scopeSelect);
      return contentContext.position === "popper" ? /* @__PURE__ */ (0, import_jsx_runtime18.jsx)(Arrow2, { ...popperScope, ...arrowProps, ref: forwardedRef }) : null;
    }
  );
  SelectArrow.displayName = ARROW_NAME3;
  var BUBBLE_INPUT_NAME = "SelectBubbleInput";
  var SelectBubbleInput = React33.forwardRef(
    ({ __scopeSelect, ...props }, forwardedRef) => {
      const context = useSelectContext(BUBBLE_INPUT_NAME, __scopeSelect);
      const { value, onValueChange, required, disabled, name, autoComplete, form } = context;
      const { nativeOptions, nativeSelectKey } = context;
      const ref = React33.useRef(null);
      const composedRefs = useComposedRefs(forwardedRef, ref);
      const selectValue = value ?? "";
      const prevValue = usePrevious(selectValue);
      const hasEmptyValueOption = Array.from(nativeOptions).some(
        (option) => (option.props.value ?? "") === ""
      );
      React33.useEffect(() => {
        const select = ref.current;
        if (!select) return;
        const selectProto = window.HTMLSelectElement.prototype;
        const descriptor = Object.getOwnPropertyDescriptor(
          selectProto,
          "value"
        );
        const setValue = descriptor.set;
        if (prevValue !== selectValue && setValue) {
          const event = new Event("change", { bubbles: true });
          setValue.call(select, selectValue);
          select.dispatchEvent(event);
        }
      }, [prevValue, selectValue]);
      return /* @__PURE__ */ (0, import_jsx_runtime18.jsxs)(
        Primitive.select,
        {
          "aria-hidden": true,
          required,
          tabIndex: -1,
          name,
          autoComplete,
          disabled,
          form,
          onChange: (event) => onValueChange(event.target.value),
          ...props,
          style: { ...VISUALLY_HIDDEN_STYLES, ...props.style },
          ref: composedRefs,
          defaultValue: selectValue,
          children: [
            shouldShowPlaceholder(value) && !hasEmptyValueOption ? /* @__PURE__ */ (0, import_jsx_runtime18.jsx)("option", { value: "" }) : null,
            Array.from(nativeOptions)
          ]
        },
        nativeSelectKey
      );
    }
  );
  SelectBubbleInput.displayName = BUBBLE_INPUT_NAME;
  function isFunction2(value) {
    return typeof value === "function";
  }
  function shouldShowPlaceholder(value) {
    return value === "" || value === void 0;
  }
  function useTypeaheadSearch(onSearchChange) {
    const handleSearchChange = useCallbackRef(onSearchChange);
    const searchRef = React33.useRef("");
    const timerRef = React33.useRef(0);
    const handleTypeaheadSearch = React33.useCallback(
      (key) => {
        const search = searchRef.current + key;
        handleSearchChange(search);
        (function updateSearch(value) {
          searchRef.current = value;
          window.clearTimeout(timerRef.current);
          if (value !== "") timerRef.current = window.setTimeout(() => updateSearch(""), 1e3);
        })(search);
      },
      [handleSearchChange]
    );
    const resetTypeahead = React33.useCallback(() => {
      searchRef.current = "";
      window.clearTimeout(timerRef.current);
    }, []);
    React33.useEffect(() => {
      return () => window.clearTimeout(timerRef.current);
    }, []);
    return [searchRef, handleTypeaheadSearch, resetTypeahead];
  }
  function findNextItem(items, search, currentItem) {
    const isRepeated = search.length > 1 && Array.from(search).every((char) => char === search[0]);
    const normalizedSearch = isRepeated ? search[0] : search;
    const currentItemIndex = currentItem ? items.indexOf(currentItem) : -1;
    let wrappedItems = wrapArray(items, Math.max(currentItemIndex, 0));
    const excludeCurrentItem = normalizedSearch.length === 1;
    if (excludeCurrentItem) wrappedItems = wrappedItems.filter((v) => v !== currentItem);
    const nextItem = wrappedItems.find(
      (item) => item.textValue.toLowerCase().startsWith(normalizedSearch.toLowerCase())
    );
    return nextItem !== currentItem ? nextItem : void 0;
  }
  function wrapArray(array, startIndex) {
    return array.map((_, index2) => array[(startIndex + index2) % array.length]);
  }

  // src/web-ui/react/ui/select.tsx
  var import_jsx_runtime19 = __toESM(require_jsx_runtime(), 1);
  function WandSelect({
    value,
    defaultValue,
    options,
    placeholder = "\u8BF7\u9009\u62E9",
    ariaLabel,
    disabled,
    className,
    onValueChange
  }) {
    const portalContainer = usePortalContainer();
    return /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)(
      Select,
      {
        value,
        defaultValue,
        disabled,
        onValueChange,
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)(
            SelectTrigger,
            {
              className: classNames("wand-ui-select-trigger", className),
              "aria-label": ariaLabel,
              children: [
                /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(SelectValue, { placeholder }),
                /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(SelectIcon, { "aria-hidden": "true", children: "\u2304" })
              ]
            }
          ),
          /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(SelectPortal, { container: portalContainer, children: /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)(
            SelectContent,
            {
              className: "wand-ui-select-content",
              position: "popper",
              sideOffset: 6,
              collisionPadding: 12,
              children: [
                /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(SelectScrollUpButton, { className: "wand-ui-select-scroll-button", children: "\u2303" }),
                /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(SelectViewport, { className: "wand-ui-select-viewport", children: options.map((option) => /* @__PURE__ */ (0, import_jsx_runtime19.jsxs)(
                  SelectItem,
                  {
                    className: "wand-ui-select-item",
                    value: option.value,
                    disabled: option.disabled,
                    children: [
                      /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(SelectItemText, { children: option.label }),
                      /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(SelectItemIndicator, { className: "wand-ui-select-indicator", children: "\u2713" })
                    ]
                  },
                  option.value
                )) }),
                /* @__PURE__ */ (0, import_jsx_runtime19.jsx)(SelectScrollDownButton, { className: "wand-ui-select-scroll-button", children: "\u2304" })
              ]
            }
          ) })
        ]
      }
    );
  }

  // node_modules/@radix-ui/react-switch/dist/index.mjs
  var React34 = __toESM(require_react(), 1);
  var import_jsx_runtime20 = __toESM(require_jsx_runtime(), 1);
  var SWITCH_NAME = "Switch";
  var [createSwitchContext, createSwitchScope] = createContextScope(SWITCH_NAME);
  var [SwitchProviderImpl, useSwitchContext] = createSwitchContext(SWITCH_NAME);
  function SwitchProvider(props) {
    const {
      __scopeSwitch,
      checked: checkedProp,
      children,
      defaultChecked,
      disabled,
      form,
      name,
      onCheckedChange,
      required,
      value = "on",
      // @ts-expect-error
      internal_do_not_use_render
    } = props;
    const [checked, setChecked] = useControllableState({
      prop: checkedProp,
      defaultProp: defaultChecked ?? false,
      onChange: onCheckedChange,
      caller: SWITCH_NAME
    });
    const [control, setControl] = React34.useState(null);
    const [bubbleInput, setBubbleInput] = React34.useState(null);
    const hasConsumerStoppedPropagationRef = React34.useRef(false);
    const isFormControl = control ? !!form || !!control.closest("form") : (
      // We set this to true by default so that events bubble to forms without JS (SSR)
      true
    );
    const context = {
      checked,
      setChecked,
      disabled,
      control,
      setControl,
      name,
      form,
      value,
      hasConsumerStoppedPropagationRef,
      required,
      defaultChecked,
      isFormControl,
      bubbleInput,
      setBubbleInput
    };
    return /* @__PURE__ */ (0, import_jsx_runtime20.jsx)(SwitchProviderImpl, { scope: __scopeSwitch, ...context, children: isFunction3(internal_do_not_use_render) ? internal_do_not_use_render(context) : children });
  }
  var TRIGGER_NAME4 = "SwitchTrigger";
  var SwitchTrigger = React34.forwardRef(
    ({ __scopeSwitch, onClick, ...switchProps }, forwardedRef) => {
      const {
        control,
        form,
        value,
        disabled,
        checked,
        required,
        setControl,
        setChecked,
        hasConsumerStoppedPropagationRef,
        isFormControl,
        bubbleInput
      } = useSwitchContext(TRIGGER_NAME4, __scopeSwitch);
      const composedRefs = useComposedRefs(forwardedRef, setControl);
      const initialCheckedStateRef = React34.useRef(checked);
      React34.useEffect(() => {
        const associatedForm = form ? control?.ownerDocument.getElementById(form) : control?.form;
        if (associatedForm instanceof HTMLFormElement) {
          const reset = () => setChecked(initialCheckedStateRef.current);
          associatedForm.addEventListener("reset", reset);
          return () => associatedForm.removeEventListener("reset", reset);
        }
      }, [control, form, setChecked]);
      return /* @__PURE__ */ (0, import_jsx_runtime20.jsx)(
        Primitive.button,
        {
          type: "button",
          role: "switch",
          "aria-checked": checked,
          "aria-required": required,
          "data-state": getState3(checked),
          "data-disabled": disabled ? "" : void 0,
          disabled,
          value,
          ...switchProps,
          ref: composedRefs,
          onClick: composeEventHandlers(onClick, (event) => {
            setChecked((prevChecked) => !prevChecked);
            if (bubbleInput && isFormControl) {
              hasConsumerStoppedPropagationRef.current = event.isPropagationStopped();
              if (!hasConsumerStoppedPropagationRef.current) event.stopPropagation();
            }
          })
        }
      );
    }
  );
  SwitchTrigger.displayName = TRIGGER_NAME4;
  var Switch = React34.forwardRef(
    (props, forwardedRef) => {
      const {
        __scopeSwitch,
        name,
        checked,
        defaultChecked,
        required,
        disabled,
        value,
        onCheckedChange,
        form,
        ...switchProps
      } = props;
      return /* @__PURE__ */ (0, import_jsx_runtime20.jsx)(
        SwitchProvider,
        {
          __scopeSwitch,
          checked,
          defaultChecked,
          disabled,
          required,
          onCheckedChange,
          name,
          form,
          value,
          internal_do_not_use_render: ({ isFormControl }) => /* @__PURE__ */ (0, import_jsx_runtime20.jsxs)(import_jsx_runtime20.Fragment, { children: [
            /* @__PURE__ */ (0, import_jsx_runtime20.jsx)(
              SwitchTrigger,
              {
                ...switchProps,
                ref: forwardedRef,
                __scopeSwitch
              }
            ),
            isFormControl && /* @__PURE__ */ (0, import_jsx_runtime20.jsx)(
              SwitchBubbleInput,
              {
                __scopeSwitch
              }
            )
          ] })
        }
      );
    }
  );
  Switch.displayName = SWITCH_NAME;
  var THUMB_NAME = "SwitchThumb";
  var SwitchThumb = React34.forwardRef(
    (props, forwardedRef) => {
      const { __scopeSwitch, ...thumbProps } = props;
      const context = useSwitchContext(THUMB_NAME, __scopeSwitch);
      return /* @__PURE__ */ (0, import_jsx_runtime20.jsx)(
        Primitive.span,
        {
          "data-state": getState3(context.checked),
          "data-disabled": context.disabled ? "" : void 0,
          ...thumbProps,
          ref: forwardedRef
        }
      );
    }
  );
  SwitchThumb.displayName = THUMB_NAME;
  var BUBBLE_INPUT_NAME2 = "SwitchBubbleInput";
  var SwitchBubbleInput = React34.forwardRef(
    ({ __scopeSwitch, ...props }, forwardedRef) => {
      const {
        control,
        hasConsumerStoppedPropagationRef,
        checked,
        defaultChecked,
        required,
        disabled,
        name,
        value,
        form,
        bubbleInput,
        setBubbleInput
      } = useSwitchContext(BUBBLE_INPUT_NAME2, __scopeSwitch);
      const composedRefs = useComposedRefs(forwardedRef, setBubbleInput);
      const prevChecked = usePrevious(checked);
      const controlSize = useSize(control);
      React34.useEffect(() => {
        const input = bubbleInput;
        if (!input) return;
        const inputProto = window.HTMLInputElement.prototype;
        const descriptor = Object.getOwnPropertyDescriptor(
          inputProto,
          "checked"
        );
        const setChecked = descriptor.set;
        const bubbles = !hasConsumerStoppedPropagationRef.current;
        if (prevChecked !== checked && setChecked) {
          const event = new Event("click", { bubbles });
          setChecked.call(input, checked);
          input.dispatchEvent(event);
        }
      }, [bubbleInput, prevChecked, checked, hasConsumerStoppedPropagationRef]);
      const defaultCheckedRef = React34.useRef(checked);
      return /* @__PURE__ */ (0, import_jsx_runtime20.jsx)(
        Primitive.input,
        {
          type: "checkbox",
          "aria-hidden": true,
          defaultChecked: defaultChecked ?? defaultCheckedRef.current,
          required,
          disabled,
          name,
          value,
          form,
          ...props,
          tabIndex: -1,
          ref: composedRefs,
          style: {
            ...props.style,
            ...controlSize,
            position: "absolute",
            pointerEvents: "none",
            opacity: 0,
            margin: 0,
            // We transform because the input is absolutely positioned but we have
            // rendered it **after** the button. This pulls it back to sit on top
            // of the button.
            transform: "translateX(-100%)"
          }
        }
      );
    }
  );
  SwitchBubbleInput.displayName = BUBBLE_INPUT_NAME2;
  function isFunction3(value) {
    return typeof value === "function";
  }
  function getState3(checked) {
    return checked ? "checked" : "unchecked";
  }

  // src/web-ui/react/ui/switch.tsx
  var import_react6 = __toESM(require_react(), 1);
  var import_jsx_runtime21 = __toESM(require_jsx_runtime(), 1);
  function WandSwitch({
    checked,
    onCheckedChange,
    ariaLabel,
    label,
    disabled,
    className,
    id
  }) {
    const generatedId = (0, import_react6.useId)();
    const switchId = id ?? generatedId;
    return /* @__PURE__ */ (0, import_jsx_runtime21.jsxs)("div", { className: classNames("wand-ui-switch-row", className), children: [
      /* @__PURE__ */ (0, import_jsx_runtime21.jsx)(
        Switch,
        {
          id: switchId,
          className: "wand-ui-switch",
          checked,
          disabled,
          "aria-label": ariaLabel,
          onCheckedChange,
          children: /* @__PURE__ */ (0, import_jsx_runtime21.jsx)(SwitchThumb, { className: "wand-ui-switch-thumb" })
        }
      ),
      label ? /* @__PURE__ */ (0, import_jsx_runtime21.jsx)("label", { htmlFor: switchId, children: label }) : null
    ] });
  }

  // node_modules/@radix-ui/react-tabs/dist/index.mjs
  var React37 = __toESM(require_react(), 1);

  // node_modules/@radix-ui/react-roving-focus/dist/index.mjs
  var React36 = __toESM(require_react(), 1);

  // node_modules/@radix-ui/react-use-is-hydrated/dist/index.mjs
  var React211 = __toESM(require_react(), 1);
  var React35 = __toESM(require_react(), 1);
  var _isHydrated = false;
  function useIsHydrated() {
    const [isHydrated, setIsHydrated] = React35.useState(_isHydrated);
    React35.useEffect(() => {
      if (!_isHydrated) {
        _isHydrated = true;
        setIsHydrated(true);
      }
    }, []);
    return isHydrated;
  }
  var useReactSyncExternalStore = React211[" useSyncExternalStore ".trim().toString()];
  function subscribe() {
    return () => {
    };
  }
  function useIsHydratedModern() {
    return useReactSyncExternalStore(
      subscribe,
      () => true,
      () => false
    );
  }
  var useIsHydrated2 = typeof useReactSyncExternalStore === "function" ? useIsHydratedModern : useIsHydrated;

  // node_modules/@radix-ui/react-roving-focus/dist/index.mjs
  var import_jsx_runtime22 = __toESM(require_jsx_runtime(), 1);
  var ENTRY_FOCUS = "rovingFocusGroup.onEntryFocus";
  var EVENT_OPTIONS2 = { bubbles: false, cancelable: true };
  var GROUP_NAME2 = "RovingFocusGroup";
  var [Collection2, useCollection2, createCollectionScope2] = createCollection(GROUP_NAME2);
  var [createRovingFocusGroupContext, createRovingFocusGroupScope] = createContextScope(
    GROUP_NAME2,
    [createCollectionScope2]
  );
  var [RovingFocusProvider, useRovingFocusContext] = createRovingFocusGroupContext(GROUP_NAME2);
  var RovingFocusGroup = React36.forwardRef(
    (props, forwardedRef) => {
      return /* @__PURE__ */ (0, import_jsx_runtime22.jsx)(Collection2.Provider, { scope: props.__scopeRovingFocusGroup, children: /* @__PURE__ */ (0, import_jsx_runtime22.jsx)(Collection2.Slot, { scope: props.__scopeRovingFocusGroup, children: /* @__PURE__ */ (0, import_jsx_runtime22.jsx)(RovingFocusGroupImpl, { ...props, ref: forwardedRef }) }) });
    }
  );
  RovingFocusGroup.displayName = GROUP_NAME2;
  var RovingFocusGroupImpl = React36.forwardRef((props, forwardedRef) => {
    const {
      __scopeRovingFocusGroup,
      orientation,
      loop = false,
      dir,
      currentTabStopId: currentTabStopIdProp,
      defaultCurrentTabStopId,
      onCurrentTabStopIdChange,
      onEntryFocus,
      preventScrollOnEntryFocus = false,
      ...groupProps
    } = props;
    const ref = React36.useRef(null);
    const composedRefs = useComposedRefs(forwardedRef, ref);
    const direction = useDirection(dir);
    const [currentTabStopId, setCurrentTabStopId] = useControllableState({
      prop: currentTabStopIdProp,
      defaultProp: defaultCurrentTabStopId ?? null,
      onChange: onCurrentTabStopIdChange,
      caller: GROUP_NAME2
    });
    const [isTabbingBackOut, setIsTabbingBackOut] = React36.useState(false);
    const handleEntryFocus = useCallbackRef(onEntryFocus);
    const getItems = useCollection2(__scopeRovingFocusGroup);
    const isClickFocusRef = React36.useRef(false);
    const [focusableItemsCount, setFocusableItemsCount] = React36.useState(0);
    React36.useEffect(() => {
      const node = ref.current;
      if (node) {
        node.addEventListener(ENTRY_FOCUS, handleEntryFocus);
        return () => node.removeEventListener(ENTRY_FOCUS, handleEntryFocus);
      }
    }, [handleEntryFocus]);
    return /* @__PURE__ */ (0, import_jsx_runtime22.jsx)(
      RovingFocusProvider,
      {
        scope: __scopeRovingFocusGroup,
        orientation,
        dir: direction,
        loop,
        currentTabStopId,
        onItemFocus: React36.useCallback(
          (tabStopId) => setCurrentTabStopId(tabStopId),
          [setCurrentTabStopId]
        ),
        onItemShiftTab: React36.useCallback(() => setIsTabbingBackOut(true), []),
        onFocusableItemAdd: React36.useCallback(
          () => setFocusableItemsCount((prevCount) => prevCount + 1),
          []
        ),
        onFocusableItemRemove: React36.useCallback(
          () => setFocusableItemsCount((prevCount) => prevCount - 1),
          []
        ),
        children: /* @__PURE__ */ (0, import_jsx_runtime22.jsx)(
          Primitive.div,
          {
            tabIndex: isTabbingBackOut || focusableItemsCount === 0 ? -1 : 0,
            "data-orientation": orientation,
            ...groupProps,
            ref: composedRefs,
            style: { outline: "none", ...props.style },
            onMouseDown: composeEventHandlers(props.onMouseDown, () => {
              isClickFocusRef.current = true;
            }),
            onFocus: composeEventHandlers(props.onFocus, (event) => {
              const isKeyboardFocus = !isClickFocusRef.current;
              if (event.target === event.currentTarget && isKeyboardFocus && !isTabbingBackOut) {
                const entryFocusEvent = new CustomEvent(ENTRY_FOCUS, EVENT_OPTIONS2);
                event.currentTarget.dispatchEvent(entryFocusEvent);
                if (!entryFocusEvent.defaultPrevented) {
                  const items = getItems().filter((item) => item.focusable);
                  const activeItem = items.find((item) => item.active);
                  const currentItem = items.find((item) => item.id === currentTabStopId);
                  const candidateItems = [activeItem, currentItem, ...items].filter(
                    Boolean
                  );
                  const candidateNodes = candidateItems.map((item) => item.ref.current);
                  focusFirst2(candidateNodes, preventScrollOnEntryFocus);
                }
              }
              isClickFocusRef.current = false;
            }),
            onBlur: composeEventHandlers(props.onBlur, () => setIsTabbingBackOut(false))
          }
        )
      }
    );
  });
  var ITEM_NAME2 = "RovingFocusGroupItem";
  var RovingFocusGroupItem = React36.forwardRef(
    (props, forwardedRef) => {
      const {
        __scopeRovingFocusGroup,
        focusable = true,
        active = false,
        tabStopId,
        children,
        ...itemProps
      } = props;
      const autoId = useId();
      const id = tabStopId || autoId;
      const context = useRovingFocusContext(ITEM_NAME2, __scopeRovingFocusGroup);
      const isCurrentTabStop = context.currentTabStopId === id;
      const getItems = useCollection2(__scopeRovingFocusGroup);
      const { onFocusableItemAdd, onFocusableItemRemove, currentTabStopId } = context;
      const isHydrated = useIsHydrated2();
      useLayoutEffect2(() => {
        if (!isHydrated || !focusable) {
          return;
        }
        onFocusableItemAdd();
        return () => onFocusableItemRemove();
      }, [isHydrated, focusable, onFocusableItemAdd, onFocusableItemRemove]);
      React36.useEffect(() => {
        if (isHydrated || !focusable) {
          return;
        }
        onFocusableItemAdd();
        return () => onFocusableItemRemove();
      }, [isHydrated, focusable, onFocusableItemAdd, onFocusableItemRemove]);
      return /* @__PURE__ */ (0, import_jsx_runtime22.jsx)(
        Collection2.ItemSlot,
        {
          scope: __scopeRovingFocusGroup,
          id,
          focusable,
          active,
          children: /* @__PURE__ */ (0, import_jsx_runtime22.jsx)(
            Primitive.span,
            {
              tabIndex: isCurrentTabStop ? 0 : -1,
              "data-orientation": context.orientation,
              ...itemProps,
              ref: forwardedRef,
              onMouseDown: composeEventHandlers(props.onMouseDown, (event) => {
                if (!focusable) event.preventDefault();
                else context.onItemFocus(id);
              }),
              onFocus: composeEventHandlers(props.onFocus, () => context.onItemFocus(id)),
              onKeyDown: composeEventHandlers(props.onKeyDown, (event) => {
                if (event.key === "Tab" && event.shiftKey) {
                  context.onItemShiftTab();
                  return;
                }
                if (event.target !== event.currentTarget) return;
                const focusIntent = getFocusIntent(event, context.orientation, context.dir);
                if (focusIntent !== void 0) {
                  if (event.metaKey || event.ctrlKey || event.altKey || event.shiftKey) return;
                  event.preventDefault();
                  const items = getItems().filter((item) => item.focusable);
                  let candidateNodes = items.map((item) => item.ref.current);
                  if (focusIntent === "last") candidateNodes.reverse();
                  else if (focusIntent === "prev" || focusIntent === "next") {
                    if (focusIntent === "prev") candidateNodes.reverse();
                    const currentIndex = candidateNodes.indexOf(event.currentTarget);
                    candidateNodes = context.loop ? wrapArray2(candidateNodes, currentIndex + 1) : candidateNodes.slice(currentIndex + 1);
                  }
                  setTimeout(() => focusFirst2(candidateNodes));
                }
              }),
              children: typeof children === "function" ? children({ isCurrentTabStop, hasTabStop: currentTabStopId != null }) : children
            }
          )
        }
      );
    }
  );
  RovingFocusGroupItem.displayName = ITEM_NAME2;
  var MAP_KEY_TO_FOCUS_INTENT = {
    ArrowLeft: "prev",
    ArrowUp: "prev",
    ArrowRight: "next",
    ArrowDown: "next",
    PageUp: "first",
    Home: "first",
    PageDown: "last",
    End: "last"
  };
  function getDirectionAwareKey(key, dir) {
    if (dir !== "rtl") return key;
    return key === "ArrowLeft" ? "ArrowRight" : key === "ArrowRight" ? "ArrowLeft" : key;
  }
  function getFocusIntent(event, orientation, dir) {
    const key = getDirectionAwareKey(event.key, dir);
    if (orientation === "vertical" && ["ArrowLeft", "ArrowRight"].includes(key)) return void 0;
    if (orientation === "horizontal" && ["ArrowUp", "ArrowDown"].includes(key)) return void 0;
    return MAP_KEY_TO_FOCUS_INTENT[key];
  }
  function focusFirst2(candidates, preventScroll = false) {
    const PREVIOUSLY_FOCUSED_ELEMENT = document.activeElement;
    for (const candidate of candidates) {
      if (candidate === PREVIOUSLY_FOCUSED_ELEMENT) return;
      candidate.focus({ preventScroll });
      if (document.activeElement !== PREVIOUSLY_FOCUSED_ELEMENT) return;
    }
  }
  function wrapArray2(array, startIndex) {
    return array.map((_, index2) => array[(startIndex + index2) % array.length]);
  }
  var Root3 = RovingFocusGroup;
  var Item = RovingFocusGroupItem;

  // node_modules/@radix-ui/react-tabs/dist/index.mjs
  var import_jsx_runtime23 = __toESM(require_jsx_runtime(), 1);
  var TABS_NAME = "Tabs";
  var [createTabsContext, createTabsScope] = createContextScope(TABS_NAME, [
    createRovingFocusGroupScope
  ]);
  var useRovingFocusGroupScope = createRovingFocusGroupScope();
  var [TabsProvider, useTabsContext] = createTabsContext(TABS_NAME);
  var Tabs = React37.forwardRef(
    (props, forwardedRef) => {
      const {
        __scopeTabs,
        value: valueProp,
        onValueChange,
        defaultValue,
        orientation = "horizontal",
        dir,
        activationMode = "automatic",
        ...tabsProps
      } = props;
      const direction = useDirection(dir);
      const [value, setValue] = useControllableState({
        prop: valueProp,
        onChange: onValueChange,
        defaultProp: defaultValue ?? "",
        caller: TABS_NAME
      });
      return /* @__PURE__ */ (0, import_jsx_runtime23.jsx)(
        TabsProvider,
        {
          scope: __scopeTabs,
          baseId: useId(),
          value,
          onValueChange: setValue,
          orientation,
          dir: direction,
          activationMode,
          children: /* @__PURE__ */ (0, import_jsx_runtime23.jsx)(
            Primitive.div,
            {
              dir: direction,
              "data-orientation": orientation,
              ...tabsProps,
              ref: forwardedRef
            }
          )
        }
      );
    }
  );
  Tabs.displayName = TABS_NAME;
  var TAB_LIST_NAME = "TabsList";
  var TabsList = React37.forwardRef(
    (props, forwardedRef) => {
      const { __scopeTabs, loop = true, ...listProps } = props;
      const context = useTabsContext(TAB_LIST_NAME, __scopeTabs);
      const rovingFocusGroupScope = useRovingFocusGroupScope(__scopeTabs);
      return /* @__PURE__ */ (0, import_jsx_runtime23.jsx)(
        Root3,
        {
          asChild: true,
          ...rovingFocusGroupScope,
          orientation: context.orientation,
          dir: context.dir,
          loop,
          children: /* @__PURE__ */ (0, import_jsx_runtime23.jsx)(
            Primitive.div,
            {
              role: "tablist",
              "aria-orientation": context.orientation,
              ...listProps,
              ref: forwardedRef
            }
          )
        }
      );
    }
  );
  TabsList.displayName = TAB_LIST_NAME;
  var TRIGGER_NAME5 = "TabsTrigger";
  var TabsTrigger = React37.forwardRef(
    (props, forwardedRef) => {
      const { __scopeTabs, value, disabled = false, ...triggerProps } = props;
      const context = useTabsContext(TRIGGER_NAME5, __scopeTabs);
      const rovingFocusGroupScope = useRovingFocusGroupScope(__scopeTabs);
      const triggerId = makeTriggerId(context.baseId, value);
      const contentId = makeContentId(context.baseId, value);
      const isSelected = value === context.value;
      return /* @__PURE__ */ (0, import_jsx_runtime23.jsx)(
        Item,
        {
          asChild: true,
          ...rovingFocusGroupScope,
          focusable: !disabled,
          active: isSelected,
          children: /* @__PURE__ */ (0, import_jsx_runtime23.jsx)(
            Primitive.button,
            {
              type: "button",
              role: "tab",
              "aria-selected": isSelected,
              "aria-controls": contentId,
              "data-state": isSelected ? "active" : "inactive",
              "data-disabled": disabled ? "" : void 0,
              disabled,
              id: triggerId,
              ...triggerProps,
              ref: forwardedRef,
              onMouseDown: composeEventHandlers(props.onMouseDown, (event) => {
                if (!disabled && event.button === 0 && event.ctrlKey === false) {
                  context.onValueChange(value);
                } else {
                  event.preventDefault();
                }
              }),
              onKeyDown: composeEventHandlers(props.onKeyDown, (event) => {
                if (disabled || event.target !== event.currentTarget) {
                  return;
                }
                if ([" ", "Enter"].includes(event.key)) {
                  context.onValueChange(value);
                }
              }),
              onFocus: composeEventHandlers(props.onFocus, () => {
                const isAutomaticActivation = context.activationMode !== "manual";
                if (!isSelected && !disabled && isAutomaticActivation) {
                  context.onValueChange(value);
                }
              })
            }
          )
        }
      );
    }
  );
  TabsTrigger.displayName = TRIGGER_NAME5;
  var CONTENT_NAME5 = "TabsContent";
  var TabsContent = React37.forwardRef(
    (props, forwardedRef) => {
      const { __scopeTabs, value, forceMount, children, ...contentProps } = props;
      const context = useTabsContext(CONTENT_NAME5, __scopeTabs);
      const triggerId = makeTriggerId(context.baseId, value);
      const contentId = makeContentId(context.baseId, value);
      const isSelected = value === context.value;
      const isMountAnimationPreventedRef = React37.useRef(isSelected);
      React37.useEffect(() => {
        const rAF = requestAnimationFrame(() => isMountAnimationPreventedRef.current = false);
        return () => cancelAnimationFrame(rAF);
      }, []);
      return /* @__PURE__ */ (0, import_jsx_runtime23.jsx)(Presence, { present: forceMount || isSelected, children: ({ present }) => /* @__PURE__ */ (0, import_jsx_runtime23.jsx)(
        Primitive.div,
        {
          "data-state": isSelected ? "active" : "inactive",
          "data-orientation": context.orientation,
          role: "tabpanel",
          "aria-labelledby": triggerId,
          hidden: !present,
          id: contentId,
          tabIndex: 0,
          ...contentProps,
          ref: forwardedRef,
          style: {
            ...props.style,
            animationDuration: isMountAnimationPreventedRef.current ? "0s" : void 0
          },
          children: present && children
        }
      ) });
    }
  );
  TabsContent.displayName = CONTENT_NAME5;
  function makeTriggerId(baseId, value) {
    return `${baseId}-trigger-${value}`;
  }
  function makeContentId(baseId, value) {
    return `${baseId}-content-${value}`;
  }
  var Root24 = Tabs;
  var List = TabsList;
  var Trigger2 = TabsTrigger;
  var Content3 = TabsContent;

  // src/web-ui/react/ui/tabs.tsx
  var import_jsx_runtime24 = __toESM(require_jsx_runtime(), 1);
  function WandTabs({
    tabs,
    value,
    defaultValue,
    ariaLabel,
    className,
    onValueChange
  }) {
    const initialValue = defaultValue ?? tabs[0]?.value;
    return /* @__PURE__ */ (0, import_jsx_runtime24.jsxs)(
      Root24,
      {
        className,
        value,
        defaultValue: initialValue,
        onValueChange,
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime24.jsx)(List, { className: "wand-ui-tabs-list", "aria-label": ariaLabel, children: tabs.map((tab) => /* @__PURE__ */ (0, import_jsx_runtime24.jsx)(
            Trigger2,
            {
              className: "wand-ui-tabs-trigger",
              value: tab.value,
              disabled: tab.disabled,
              children: tab.label
            },
            tab.value
          )) }),
          tabs.map((tab) => /* @__PURE__ */ (0, import_jsx_runtime24.jsx)(
            Content3,
            {
              className: classNames("wand-ui-tabs-content"),
              value: tab.value,
              children: tab.content
            },
            tab.value
          ))
        ]
      }
    );
  }

  // node_modules/@radix-ui/react-toast/dist/index.mjs
  var React38 = __toESM(require_react(), 1);
  var ReactDOM5 = __toESM(require_react_dom(), 1);
  var import_jsx_runtime25 = __toESM(require_jsx_runtime(), 1);
  var PROVIDER_NAME2 = "ToastProvider";
  var [Collection3, useCollection3, createCollectionScope3] = createCollection("Toast");
  var [createToastContext, createToastScope] = createContextScope("Toast", [createCollectionScope3]);
  var [ToastProviderProvider, useToastProviderContext] = createToastContext(PROVIDER_NAME2);
  var ToastProvider = (props) => {
    const {
      __scopeToast,
      label = "Notification",
      duration = 5e3,
      swipeDirection = "right",
      swipeThreshold = 50,
      announcerContainer,
      children
    } = props;
    const [viewport, setViewport] = React38.useState(null);
    const [toastCount, setToastCount] = React38.useState(0);
    const isFocusedToastEscapeKeyDownRef = React38.useRef(false);
    const isClosePausedRef = React38.useRef(false);
    if (!label.trim()) {
      console.error(
        `Invalid prop \`label\` supplied to \`${PROVIDER_NAME2}\`. Expected non-empty \`string\`.`
      );
    }
    return /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(Collection3.Provider, { scope: __scopeToast, children: /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(
      ToastProviderProvider,
      {
        scope: __scopeToast,
        label,
        duration,
        swipeDirection,
        swipeThreshold,
        toastCount,
        viewport,
        onViewportChange: setViewport,
        onToastAdd: React38.useCallback(() => setToastCount((prevCount) => prevCount + 1), []),
        onToastRemove: React38.useCallback(() => setToastCount((prevCount) => prevCount - 1), []),
        isFocusedToastEscapeKeyDownRef,
        isClosePausedRef,
        announcerContainer,
        children
      }
    ) });
  };
  ToastProvider.displayName = PROVIDER_NAME2;
  var VIEWPORT_NAME2 = "ToastViewport";
  var VIEWPORT_DEFAULT_HOTKEY = ["F8"];
  var VIEWPORT_PAUSE = "toast.viewportPause";
  var VIEWPORT_RESUME = "toast.viewportResume";
  var ToastViewport = React38.forwardRef(
    (props, forwardedRef) => {
      const {
        __scopeToast,
        hotkey = VIEWPORT_DEFAULT_HOTKEY,
        label = "Notifications ({hotkey})",
        ...viewportProps
      } = props;
      const context = useToastProviderContext(VIEWPORT_NAME2, __scopeToast);
      const getItems = useCollection3(__scopeToast);
      const wrapperRef = React38.useRef(null);
      const headFocusProxyRef = React38.useRef(null);
      const tailFocusProxyRef = React38.useRef(null);
      const ref = React38.useRef(null);
      const composedRefs = useComposedRefs(forwardedRef, ref, context.onViewportChange);
      const hotkeyLabel = hotkey.join("+").replace(/Key/g, "").replace(/Digit/g, "");
      const hasToasts = context.toastCount > 0;
      React38.useEffect(() => {
        const handleKeyDown2 = (event) => {
          const isHotkeyPressed = hotkey.length !== 0 && hotkey.every((key) => event[key] || event.code === key);
          if (isHotkeyPressed) ref.current?.focus();
        };
        document.addEventListener("keydown", handleKeyDown2);
        return () => document.removeEventListener("keydown", handleKeyDown2);
      }, [hotkey]);
      React38.useEffect(() => {
        const wrapper = wrapperRef.current;
        const viewport = ref.current;
        if (hasToasts && wrapper && viewport) {
          const handlePause = () => {
            if (!context.isClosePausedRef.current) {
              const pauseEvent = new CustomEvent(VIEWPORT_PAUSE);
              viewport.dispatchEvent(pauseEvent);
              context.isClosePausedRef.current = true;
            }
          };
          const handleResume = () => {
            if (context.isClosePausedRef.current) {
              const resumeEvent = new CustomEvent(VIEWPORT_RESUME);
              viewport.dispatchEvent(resumeEvent);
              context.isClosePausedRef.current = false;
            }
          };
          const handleFocusOutResume = (event) => {
            const isFocusMovingOutside = !wrapper.contains(event.relatedTarget);
            if (isFocusMovingOutside) handleResume();
          };
          const handlePointerLeaveResume = () => {
            const isFocusInside = wrapper.contains(document.activeElement);
            if (!isFocusInside) handleResume();
          };
          wrapper.addEventListener("focusin", handlePause);
          wrapper.addEventListener("focusout", handleFocusOutResume);
          wrapper.addEventListener("pointermove", handlePause);
          wrapper.addEventListener("pointerleave", handlePointerLeaveResume);
          window.addEventListener("blur", handlePause);
          window.addEventListener("focus", handleResume);
          return () => {
            wrapper.removeEventListener("focusin", handlePause);
            wrapper.removeEventListener("focusout", handleFocusOutResume);
            wrapper.removeEventListener("pointermove", handlePause);
            wrapper.removeEventListener("pointerleave", handlePointerLeaveResume);
            window.removeEventListener("blur", handlePause);
            window.removeEventListener("focus", handleResume);
          };
        }
      }, [hasToasts, context.isClosePausedRef]);
      const getSortedTabbableCandidates = React38.useCallback(
        ({ tabbingDirection }) => {
          const toastItems = getItems();
          const tabbableCandidates = toastItems.map((toastItem) => {
            const toastNode = toastItem.ref.current;
            const toastTabbableCandidates = [toastNode, ...getTabbableCandidates2(toastNode)];
            return tabbingDirection === "forwards" ? toastTabbableCandidates : toastTabbableCandidates.reverse();
          });
          return (tabbingDirection === "forwards" ? tabbableCandidates.reverse() : tabbableCandidates).flat();
        },
        [getItems]
      );
      React38.useEffect(() => {
        const viewport = ref.current;
        if (viewport) {
          const handleKeyDown2 = (event) => {
            const isMetaKey = event.altKey || event.ctrlKey || event.metaKey;
            const isTabKey = event.key === "Tab" && !isMetaKey;
            if (isTabKey) {
              const focusedElement = document.activeElement;
              const isTabbingBackwards = event.shiftKey;
              const targetIsViewport = event.target === viewport;
              if (targetIsViewport && isTabbingBackwards) {
                headFocusProxyRef.current?.focus();
                return;
              }
              const tabbingDirection = isTabbingBackwards ? "backwards" : "forwards";
              const sortedCandidates = getSortedTabbableCandidates({ tabbingDirection });
              const index2 = sortedCandidates.findIndex((candidate) => candidate === focusedElement);
              if (focusFirst3(sortedCandidates.slice(index2 + 1))) {
                event.preventDefault();
              } else {
                isTabbingBackwards ? headFocusProxyRef.current?.focus() : tailFocusProxyRef.current?.focus();
              }
            }
          };
          viewport.addEventListener("keydown", handleKeyDown2);
          return () => viewport.removeEventListener("keydown", handleKeyDown2);
        }
      }, [getItems, getSortedTabbableCandidates]);
      return /* @__PURE__ */ (0, import_jsx_runtime25.jsxs)(
        Branch,
        {
          ref: wrapperRef,
          role: "region",
          "aria-label": label.replace("{hotkey}", hotkeyLabel),
          tabIndex: -1,
          style: { pointerEvents: hasToasts ? void 0 : "none" },
          children: [
            hasToasts && /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(
              FocusProxy,
              {
                ref: headFocusProxyRef,
                onFocusFromOutsideViewport: () => {
                  const tabbableCandidates = getSortedTabbableCandidates({
                    tabbingDirection: "forwards"
                  });
                  focusFirst3(tabbableCandidates);
                }
              }
            ),
            /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(Collection3.Slot, { scope: __scopeToast, children: /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(Primitive.ol, { tabIndex: -1, ...viewportProps, ref: composedRefs }) }),
            hasToasts && /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(
              FocusProxy,
              {
                ref: tailFocusProxyRef,
                onFocusFromOutsideViewport: () => {
                  const tabbableCandidates = getSortedTabbableCandidates({
                    tabbingDirection: "backwards"
                  });
                  focusFirst3(tabbableCandidates);
                }
              }
            )
          ]
        }
      );
    }
  );
  ToastViewport.displayName = VIEWPORT_NAME2;
  var FOCUS_PROXY_NAME = "ToastFocusProxy";
  var FocusProxy = React38.forwardRef(
    (props, forwardedRef) => {
      const { __scopeToast, onFocusFromOutsideViewport, ...proxyProps } = props;
      const context = useToastProviderContext(FOCUS_PROXY_NAME, __scopeToast);
      return /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(
        VisuallyHidden,
        {
          tabIndex: 0,
          ...proxyProps,
          ref: forwardedRef,
          style: { position: "fixed" },
          onFocus: (event) => {
            const prevFocusedElement = event.relatedTarget;
            const isFocusFromOutsideViewport = !context.viewport?.contains(prevFocusedElement);
            if (isFocusFromOutsideViewport) onFocusFromOutsideViewport();
          }
        }
      );
    }
  );
  FocusProxy.displayName = FOCUS_PROXY_NAME;
  var TOAST_NAME = "Toast";
  var TOAST_SWIPE_START = "toast.swipeStart";
  var TOAST_SWIPE_MOVE = "toast.swipeMove";
  var TOAST_SWIPE_CANCEL = "toast.swipeCancel";
  var TOAST_SWIPE_END = "toast.swipeEnd";
  var Toast = React38.forwardRef(
    (props, forwardedRef) => {
      const { forceMount, open: openProp, defaultOpen, onOpenChange, ...toastProps } = props;
      const [open, setOpen] = useControllableState({
        prop: openProp,
        defaultProp: defaultOpen ?? true,
        onChange: onOpenChange,
        caller: TOAST_NAME
      });
      return /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(Presence, { present: forceMount || open, children: /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(
        ToastImpl,
        {
          open,
          ...toastProps,
          ref: forwardedRef,
          onClose: () => setOpen(false),
          onPause: useCallbackRef(props.onPause),
          onResume: useCallbackRef(props.onResume),
          onSwipeStart: composeEventHandlers(props.onSwipeStart, (event) => {
            event.currentTarget.setAttribute("data-swipe", "start");
          }),
          onSwipeMove: composeEventHandlers(props.onSwipeMove, (event) => {
            const { x, y } = event.detail.delta;
            event.currentTarget.setAttribute("data-swipe", "move");
            event.currentTarget.style.setProperty("--radix-toast-swipe-move-x", `${x}px`);
            event.currentTarget.style.setProperty("--radix-toast-swipe-move-y", `${y}px`);
          }),
          onSwipeCancel: composeEventHandlers(props.onSwipeCancel, (event) => {
            event.currentTarget.setAttribute("data-swipe", "cancel");
            event.currentTarget.style.removeProperty("--radix-toast-swipe-move-x");
            event.currentTarget.style.removeProperty("--radix-toast-swipe-move-y");
            event.currentTarget.style.removeProperty("--radix-toast-swipe-end-x");
            event.currentTarget.style.removeProperty("--radix-toast-swipe-end-y");
          }),
          onSwipeEnd: composeEventHandlers(props.onSwipeEnd, (event) => {
            const { x, y } = event.detail.delta;
            event.currentTarget.setAttribute("data-swipe", "end");
            event.currentTarget.style.removeProperty("--radix-toast-swipe-move-x");
            event.currentTarget.style.removeProperty("--radix-toast-swipe-move-y");
            event.currentTarget.style.setProperty("--radix-toast-swipe-end-x", `${x}px`);
            event.currentTarget.style.setProperty("--radix-toast-swipe-end-y", `${y}px`);
            setOpen(false);
          })
        }
      ) });
    }
  );
  Toast.displayName = TOAST_NAME;
  var [ToastInteractiveProvider, useToastInteractiveContext] = createToastContext(TOAST_NAME, {
    onClose() {
    }
  });
  var ToastImpl = React38.forwardRef(
    (props, forwardedRef) => {
      const {
        __scopeToast,
        type = "foreground",
        duration: durationProp,
        open,
        onClose,
        onEscapeKeyDown,
        onPause,
        onResume,
        onSwipeStart,
        onSwipeMove,
        onSwipeCancel,
        onSwipeEnd,
        ...toastProps
      } = props;
      const context = useToastProviderContext(TOAST_NAME, __scopeToast);
      const [node, setNode] = React38.useState(null);
      const composedRefs = useComposedRefs(forwardedRef, setNode);
      const pointerStartRef = React38.useRef(null);
      const swipeDeltaRef = React38.useRef(null);
      const duration = durationProp || context.duration;
      const closeTimerStartTimeRef = React38.useRef(0);
      const closeTimerRemainingTimeRef = React38.useRef(duration);
      const closeTimerRef = React38.useRef(0);
      const { onToastAdd, onToastRemove } = context;
      const handleClose = useCallbackRef(() => {
        const isFocusInToast = node?.contains(document.activeElement);
        if (isFocusInToast) context.viewport?.focus();
        onClose();
      });
      const startTimer = React38.useCallback(
        (duration2) => {
          if (!duration2 || duration2 === Infinity) return;
          window.clearTimeout(closeTimerRef.current);
          closeTimerStartTimeRef.current = (/* @__PURE__ */ new Date()).getTime();
          closeTimerRef.current = window.setTimeout(handleClose, duration2);
        },
        [handleClose]
      );
      React38.useEffect(() => {
        const viewport = context.viewport;
        if (viewport) {
          const handleResume = () => {
            startTimer(closeTimerRemainingTimeRef.current);
            onResume?.();
          };
          const handlePause = () => {
            const elapsedTime2 = (/* @__PURE__ */ new Date()).getTime() - closeTimerStartTimeRef.current;
            closeTimerRemainingTimeRef.current = closeTimerRemainingTimeRef.current - elapsedTime2;
            window.clearTimeout(closeTimerRef.current);
            onPause?.();
          };
          viewport.addEventListener(VIEWPORT_PAUSE, handlePause);
          viewport.addEventListener(VIEWPORT_RESUME, handleResume);
          return () => {
            viewport.removeEventListener(VIEWPORT_PAUSE, handlePause);
            viewport.removeEventListener(VIEWPORT_RESUME, handleResume);
          };
        }
      }, [context.viewport, duration, onPause, onResume, startTimer]);
      React38.useEffect(() => {
        if (open && !context.isClosePausedRef.current) startTimer(duration);
      }, [open, duration, context.isClosePausedRef, startTimer]);
      React38.useEffect(() => {
        return () => {
          window.clearTimeout(closeTimerRef.current);
        };
      }, []);
      React38.useEffect(() => {
        onToastAdd();
        return () => onToastRemove();
      }, [onToastAdd, onToastRemove]);
      const announceTextContent = React38.useMemo(() => {
        return node ? getAnnounceTextContent(node) : null;
      }, [node]);
      if (!context.viewport) return null;
      return /* @__PURE__ */ (0, import_jsx_runtime25.jsxs)(import_jsx_runtime25.Fragment, { children: [
        announceTextContent && /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(
          ToastAnnounce,
          {
            __scopeToast,
            role: "status",
            "aria-live": type === "foreground" ? "assertive" : "polite",
            children: announceTextContent
          }
        ),
        /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(ToastInteractiveProvider, { scope: __scopeToast, onClose: handleClose, children: ReactDOM5.createPortal(
          /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(Collection3.ItemSlot, { scope: __scopeToast, children: /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(
            Root,
            {
              asChild: true,
              onEscapeKeyDown: composeEventHandlers(onEscapeKeyDown, () => {
                if (!context.isFocusedToastEscapeKeyDownRef.current) handleClose();
                context.isFocusedToastEscapeKeyDownRef.current = false;
              }),
              children: /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(
                Primitive.li,
                {
                  tabIndex: 0,
                  "data-state": open ? "open" : "closed",
                  "data-swipe-direction": context.swipeDirection,
                  ...toastProps,
                  ref: composedRefs,
                  style: { userSelect: "none", touchAction: "none", ...props.style },
                  onKeyDown: composeEventHandlers(props.onKeyDown, (event) => {
                    if (event.key !== "Escape") return;
                    onEscapeKeyDown?.(event.nativeEvent);
                    if (!event.nativeEvent.defaultPrevented) {
                      context.isFocusedToastEscapeKeyDownRef.current = true;
                      handleClose();
                    }
                  }),
                  onPointerDown: composeEventHandlers(props.onPointerDown, (event) => {
                    if (event.button !== 0) return;
                    pointerStartRef.current = { x: event.clientX, y: event.clientY };
                  }),
                  onPointerMove: composeEventHandlers(props.onPointerMove, (event) => {
                    if (!pointerStartRef.current) return;
                    const x = event.clientX - pointerStartRef.current.x;
                    const y = event.clientY - pointerStartRef.current.y;
                    const hasSwipeMoveStarted = Boolean(swipeDeltaRef.current);
                    const isHorizontalSwipe = ["left", "right"].includes(context.swipeDirection);
                    const clamp3 = ["left", "up"].includes(context.swipeDirection) ? Math.min : Math.max;
                    const clampedX = isHorizontalSwipe ? clamp3(0, x) : 0;
                    const clampedY = !isHorizontalSwipe ? clamp3(0, y) : 0;
                    const moveStartBuffer = event.pointerType === "touch" ? 10 : 2;
                    const delta = { x: clampedX, y: clampedY };
                    const eventDetail = { originalEvent: event, delta };
                    if (hasSwipeMoveStarted) {
                      swipeDeltaRef.current = delta;
                      handleAndDispatchCustomEvent2(TOAST_SWIPE_MOVE, onSwipeMove, eventDetail, {
                        discrete: false
                      });
                    } else if (isDeltaInDirection(delta, context.swipeDirection, moveStartBuffer)) {
                      swipeDeltaRef.current = delta;
                      handleAndDispatchCustomEvent2(TOAST_SWIPE_START, onSwipeStart, eventDetail, {
                        discrete: false
                      });
                      event.target.setPointerCapture(event.pointerId);
                    } else if (Math.abs(x) > moveStartBuffer || Math.abs(y) > moveStartBuffer) {
                      pointerStartRef.current = null;
                    }
                  }),
                  onPointerUp: composeEventHandlers(props.onPointerUp, (event) => {
                    const delta = swipeDeltaRef.current;
                    const target = event.target;
                    if (target.hasPointerCapture(event.pointerId)) {
                      target.releasePointerCapture(event.pointerId);
                    }
                    swipeDeltaRef.current = null;
                    pointerStartRef.current = null;
                    if (delta) {
                      const toast = event.currentTarget;
                      const eventDetail = { originalEvent: event, delta };
                      if (isDeltaInDirection(delta, context.swipeDirection, context.swipeThreshold)) {
                        handleAndDispatchCustomEvent2(TOAST_SWIPE_END, onSwipeEnd, eventDetail, {
                          discrete: true
                        });
                      } else {
                        handleAndDispatchCustomEvent2(
                          TOAST_SWIPE_CANCEL,
                          onSwipeCancel,
                          eventDetail,
                          {
                            discrete: true
                          }
                        );
                      }
                      toast.addEventListener("click", (event2) => event2.preventDefault(), {
                        once: true
                      });
                    }
                  })
                }
              )
            }
          ) }),
          context.viewport
        ) })
      ] });
    }
  );
  var ToastAnnounce = (props) => {
    const { __scopeToast, children, ...announceProps } = props;
    const context = useToastProviderContext(TOAST_NAME, __scopeToast);
    const [renderAnnounceText, setRenderAnnounceText] = React38.useState(false);
    const [isAnnounced, setIsAnnounced] = React38.useState(false);
    useNextFrame(() => setRenderAnnounceText(true));
    React38.useEffect(() => {
      const timer = window.setTimeout(() => setIsAnnounced(true), 1e3);
      return () => window.clearTimeout(timer);
    }, []);
    return isAnnounced ? null : /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(Portal, { asChild: true, container: context.announcerContainer || void 0, children: /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(VisuallyHidden, { ...announceProps, children: renderAnnounceText && /* @__PURE__ */ (0, import_jsx_runtime25.jsxs)(import_jsx_runtime25.Fragment, { children: [
      context.label,
      " ",
      children
    ] }) }) });
  };
  var TITLE_NAME2 = "ToastTitle";
  var ToastTitle = React38.forwardRef(
    (props, forwardedRef) => {
      const { __scopeToast, ...titleProps } = props;
      return /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(Primitive.div, { ...titleProps, ref: forwardedRef });
    }
  );
  ToastTitle.displayName = TITLE_NAME2;
  var DESCRIPTION_NAME2 = "ToastDescription";
  var ToastDescription = React38.forwardRef(
    (props, forwardedRef) => {
      const { __scopeToast, ...descriptionProps } = props;
      return /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(Primitive.div, { ...descriptionProps, ref: forwardedRef });
    }
  );
  ToastDescription.displayName = DESCRIPTION_NAME2;
  var ACTION_NAME = "ToastAction";
  var ToastAction = React38.forwardRef(
    (props, forwardedRef) => {
      const { altText, ...actionProps } = props;
      if (!altText.trim()) {
        console.error(
          `Invalid prop \`altText\` supplied to \`${ACTION_NAME}\`. Expected non-empty \`string\`.`
        );
        return null;
      }
      return /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(ToastAnnounceExclude, { altText, asChild: true, children: /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(ToastClose, { ...actionProps, ref: forwardedRef }) });
    }
  );
  ToastAction.displayName = ACTION_NAME;
  var CLOSE_NAME3 = "ToastClose";
  var ToastClose = React38.forwardRef(
    (props, forwardedRef) => {
      const { __scopeToast, ...closeProps } = props;
      const interactiveContext = useToastInteractiveContext(CLOSE_NAME3, __scopeToast);
      return /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(ToastAnnounceExclude, { asChild: true, children: /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(
        Primitive.button,
        {
          type: "button",
          ...closeProps,
          ref: forwardedRef,
          onClick: composeEventHandlers(props.onClick, interactiveContext.onClose)
        }
      ) });
    }
  );
  ToastClose.displayName = CLOSE_NAME3;
  var ToastAnnounceExclude = React38.forwardRef((props, forwardedRef) => {
    const { __scopeToast, altText, ...announceExcludeProps } = props;
    return /* @__PURE__ */ (0, import_jsx_runtime25.jsx)(
      Primitive.div,
      {
        "data-radix-toast-announce-exclude": "",
        "data-radix-toast-announce-alt": altText || void 0,
        ...announceExcludeProps,
        ref: forwardedRef
      }
    );
  });
  function getAnnounceTextContent(container) {
    const textContent = [];
    const childNodes = Array.from(container.childNodes);
    childNodes.forEach((node) => {
      if (node.nodeType === node.TEXT_NODE && node.textContent) textContent.push(node.textContent);
      if (isHTMLElement2(node)) {
        const isHidden2 = node.ariaHidden || node.hidden || node.style.display === "none";
        const isExcluded = node.dataset.radixToastAnnounceExclude === "";
        if (!isHidden2) {
          if (isExcluded) {
            const altText = node.dataset.radixToastAnnounceAlt;
            if (altText) textContent.push(altText);
          } else {
            textContent.push(...getAnnounceTextContent(node));
          }
        }
      }
    });
    return textContent;
  }
  function handleAndDispatchCustomEvent2(name, handler, detail, { discrete }) {
    const currentTarget = detail.originalEvent.currentTarget;
    const event = new CustomEvent(name, { bubbles: true, cancelable: true, detail });
    if (handler) currentTarget.addEventListener(name, handler, { once: true });
    if (discrete) {
      dispatchDiscreteCustomEvent(currentTarget, event);
    } else {
      currentTarget.dispatchEvent(event);
    }
  }
  var isDeltaInDirection = (delta, direction, threshold = 0) => {
    const deltaX = Math.abs(delta.x);
    const deltaY = Math.abs(delta.y);
    const isDeltaX = deltaX > deltaY;
    if (direction === "left" || direction === "right") {
      return isDeltaX && deltaX > threshold;
    } else {
      return !isDeltaX && deltaY > threshold;
    }
  };
  function useNextFrame(callback = () => {
  }) {
    const fn = useCallbackRef(callback);
    useLayoutEffect2(() => {
      let raf1 = 0;
      let raf2 = 0;
      raf1 = window.requestAnimationFrame(() => raf2 = window.requestAnimationFrame(fn));
      return () => {
        window.cancelAnimationFrame(raf1);
        window.cancelAnimationFrame(raf2);
      };
    }, [fn]);
  }
  function isHTMLElement2(node) {
    return node.nodeType === node.ELEMENT_NODE;
  }
  function getTabbableCandidates2(container) {
    const nodes = [];
    const walker = document.createTreeWalker(container, NodeFilter.SHOW_ELEMENT, {
      acceptNode: (node) => {
        const isHiddenInput = node.tagName === "INPUT" && node.type === "hidden";
        if (node.disabled || node.hidden || isHiddenInput) return NodeFilter.FILTER_SKIP;
        return node.tabIndex >= 0 ? NodeFilter.FILTER_ACCEPT : NodeFilter.FILTER_SKIP;
      }
    });
    while (walker.nextNode()) nodes.push(walker.currentNode);
    return nodes;
  }
  function focusFirst3(candidates) {
    const previouslyFocusedElement = document.activeElement;
    return candidates.some((candidate) => {
      if (candidate === previouslyFocusedElement) return true;
      candidate.focus();
      return document.activeElement !== previouslyFocusedElement;
    });
  }
  var Provider = ToastProvider;
  var Viewport = ToastViewport;
  var Root25 = Toast;
  var Title = ToastTitle;
  var Description = ToastDescription;
  var Close = ToastClose;

  // src/web-ui/react/ui/toast.tsx
  var import_react_dom2 = __toESM(require_react_dom(), 1);
  var import_jsx_runtime26 = __toESM(require_jsx_runtime(), 1);
  function WandToastRegion({ children }) {
    const portalContainer = usePortalContainer();
    return /* @__PURE__ */ (0, import_jsx_runtime26.jsxs)(Provider, { swipeDirection: "right", label: "\u901A\u77E5", children: [
      children,
      portalContainer ? (0, import_react_dom2.createPortal)(
        /* @__PURE__ */ (0, import_jsx_runtime26.jsx)(Viewport, { className: "wand-ui-toast-viewport" }),
        portalContainer
      ) : null
    ] });
  }
  function WandToastItem({
    open,
    title,
    description,
    tone = "info",
    duration = 3200,
    onDismiss
  }) {
    return /* @__PURE__ */ (0, import_jsx_runtime26.jsxs)(
      Root25,
      {
        open,
        duration,
        className: classNames("wand-ui-toast", `wand-ui-toast-${tone}`),
        onOpenChange: (nextOpen) => {
          if (!nextOpen) onDismiss();
        },
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime26.jsx)(Title, { className: "wand-ui-toast-title", children: title }),
          description ? /* @__PURE__ */ (0, import_jsx_runtime26.jsx)(Description, { className: "wand-ui-toast-description", children: description }) : null,
          /* @__PURE__ */ (0, import_jsx_runtime26.jsx)(Close, { className: "wand-ui-toast-close", "aria-label": "\u5173\u95ED\u901A\u77E5", children: "\xD7" })
        ]
      }
    );
  }

  // src/web-ui/react/settings/host.tsx
  var import_react8 = __toESM(require_react(), 1);
  var import_react9 = __toESM(require_react(), 1);

  // src/web-ui/react/settings/controller.ts
  var snapshot2 = {
    open: false,
    tab: "about",
    nested: null,
    revision: 0
  };
  var listeners2 = /* @__PURE__ */ new Set();
  function publish2(next) {
    snapshot2 = { ...next, revision: snapshot2.revision + 1 };
    for (const listener of listeners2) listener();
  }
  var settingsController = {
    open(tab = "about") {
      publish2({ open: true, tab, nested: null });
    },
    close() {
      if (!snapshot2.open && snapshot2.nested === null) return;
      publish2({ open: false, tab: "about", nested: null });
    },
    closeTopmost() {
      if (snapshot2.nested !== null) {
        publish2({ ...snapshot2, nested: null });
        return true;
      }
      return this.closeIfOpen();
    },
    closeIfOpen() {
      if (!snapshot2.open) return false;
      this.close();
      return true;
    },
    isOpen() {
      return snapshot2.open;
    }
  };
  var settingsStore = {
    subscribe(listener) {
      listeners2.add(listener);
      return () => listeners2.delete(listener);
    },
    getSnapshot() {
      return snapshot2;
    },
    setTab(tab) {
      if (snapshot2.tab === tab) return;
      publish2({ ...snapshot2, tab });
    },
    setNested(nested) {
      if (snapshot2.nested === nested) return;
      publish2({ ...snapshot2, nested });
    }
  };

  // src/web-ui/react/settings/repository.ts
  var BrowserSettingsRuntimeAdapter = class {
    notificationPreferencesChanged(preferences) {
      window.dispatchEvent(new CustomEvent("wand-settings-notifications-changed", { detail: preferences }));
    }
    configSaved(config) {
      window.dispatchEvent(new CustomEvent("wand-settings-config-saved", { detail: config }));
    }
  };
  var EMPTY_DISTRIBUTION = {
    enabled: false,
    hasArtifact: false,
    fileName: null,
    version: null,
    size: null,
    updatedAt: null,
    downloadUrl: null,
    source: null,
    local: null,
    github: null
  };
  var EMPTY_SYSTEM_AI = {
    enabled: false,
    protocol: "openai",
    baseUrl: "",
    apiKey: "",
    hasApiKey: false,
    model: "",
    authHeader: "bearer",
    source: "custom"
  };
  function record(value) {
    return value && typeof value === "object" && !Array.isArray(value) ? value : {};
  }
  function stringValue(value, fallback = "") {
    return typeof value === "string" ? value : fallback;
  }
  function nullableString(value) {
    return typeof value === "string" && value ? value : null;
  }
  function booleanValue(value, fallback = false) {
    return typeof value === "boolean" ? value : fallback;
  }
  function numberValue(value, fallback) {
    return typeof value === "number" && Number.isFinite(value) ? value : fallback;
  }
  function normalizeAsset(value) {
    const input = record(value);
    const fileName = stringValue(input.fileName);
    const downloadUrl = stringValue(input.downloadUrl);
    if (!fileName || !downloadUrl) return null;
    return {
      fileName,
      downloadUrl,
      version: nullableString(input.version),
      size: numberValue(input.size, 0),
      updatedAt: nullableString(input.updatedAt),
      releaseNotes: stringValue(input.releaseNotes) || void 0
    };
  }
  function normalizeDistribution(value) {
    const input = record(value);
    const local = normalizeAsset(input.local);
    const github = normalizeAsset(input.github);
    return {
      ...EMPTY_DISTRIBUTION,
      enabled: input.enabled !== false,
      hasArtifact: booleanValue(input.hasArtifact, Boolean(local || github || input.fileName)),
      fileName: nullableString(input.fileName),
      version: nullableString(input.version),
      size: typeof input.size === "number" ? input.size : null,
      updatedAt: nullableString(input.updatedAt),
      downloadUrl: nullableString(input.downloadUrl),
      source: input.source === "github" || input.source === "local" ? input.source : null,
      local,
      github
    };
  }
  function normalizeSystemAi(value) {
    const input = record(value);
    const source = input.source === "claude" || input.source === "codex" || input.source === "opencode" ? input.source : "custom";
    return {
      ...EMPTY_SYSTEM_AI,
      enabled: input.enabled === true,
      protocol: input.protocol === "anthropic" ? "anthropic" : "openai",
      baseUrl: stringValue(input.baseUrl),
      apiKey: "",
      hasApiKey: input.hasApiKey === true,
      model: stringValue(input.model),
      authHeader: input.authHeader === "x-api-key" ? "x-api-key" : "bearer",
      source
    };
  }
  function normalizeConfig(value) {
    const input = record(value);
    const defaults = record(input.defaultModels);
    const cards = record(input.cardDefaults);
    const presets = Array.isArray(input.commandPresets) ? input.commandPresets : [];
    const defaultMode = [
      "default",
      "assist",
      "agent",
      "agent-max",
      "auto-edit",
      "full-access",
      "native",
      "managed"
    ].includes(stringValue(input.defaultMode)) ? stringValue(input.defaultMode) : "default";
    const commitCli = input.commitCli === "codex" || input.commitCli === "opencode" ? input.commitCli : "claude";
    const claude = stringValue(defaults.claude, stringValue(input.defaultModel));
    const codex = stringValue(defaults.codex, stringValue(input.defaultCodexModel));
    const opencode = stringValue(defaults.opencode, stringValue(input.defaultOpenCodeModel));
    const grok = stringValue(defaults.grok, stringValue(input.defaultGrokModel));
    const qoder = stringValue(defaults.qoder, stringValue(input.defaultQoderModel));
    return {
      host: stringValue(input.host, "127.0.0.1"),
      port: numberValue(input.port, 3e3),
      https: input.https === true,
      defaultMode,
      defaultCwd: stringValue(input.defaultCwd),
      shell: stringValue(input.shell, "/bin/bash"),
      language: stringValue(input.language),
      structuredRunner: input.structuredRunner === "sdk" ? "sdk" : "cli",
      inheritEnv: input.inheritEnv !== false,
      defaultModel: claude,
      defaultCodexModel: codex,
      defaultOpenCodeModel: opencode,
      defaultGrokModel: grok,
      defaultQoderModel: qoder,
      defaultModels: { claude, codex, opencode, grok, qoder },
      commitCli,
      commitModel: stringValue(input.commitModel),
      commitAiSource: input.commitAiSource === "api" ? "api" : "cli",
      systemAi: normalizeSystemAi(input.systemAi),
      commandPresets: presets.map((item) => {
        const preset = record(item);
        return {
          label: stringValue(preset.label),
          command: stringValue(preset.command),
          mode: stringValue(preset.mode) || void 0
        };
      }).filter((preset) => preset.label || preset.command),
      cardDefaults: {
        editCards: cards.editCards === true,
        inlineTools: cards.inlineTools === true,
        terminal: cards.terminal === true,
        thinking: cards.thinking === true,
        toolGroup: cards.toolGroup === true
      }
    };
  }
  function normalizeAbout(value) {
    const input = record(value);
    const build = record(input.build);
    return {
      packageName: stringValue(input.packageName, "wand-local"),
      version: stringValue(input.version, "-"),
      nodeVersion: stringValue(input.nodeVersion, "-"),
      repoUrl: stringValue(input.repoUrl),
      updateAvailable: input.updateAvailable === true,
      latestVersion: nullableString(input.latestVersion),
      updateChannel: input.updateChannel === "beta" ? "beta" : "stable",
      build: {
        commit: nullableString(build.commit),
        shortCommit: nullableString(build.shortCommit),
        builtAt: nullableString(build.builtAt),
        channel: nullableString(build.channel)
      },
      androidApk: normalizeDistribution(input.androidApk),
      macosDmg: normalizeDistribution(input.macosDmg)
    };
  }
  function normalizeModels(value) {
    const input = record(value);
    const models = (key) => Array.isArray(input[key]) ? input[key] : [];
    const defaults = record(input.defaultModels);
    return {
      models: models("models"),
      codexModels: models("codexModels"),
      opencodeModels: models("opencodeModels"),
      grokModels: models("grokModels"),
      qoderModels: models("qoderModels"),
      claudeVersion: nullableString(input.claudeVersion),
      opencodeVersion: nullableString(input.opencodeVersion),
      refreshedAt: nullableString(input.refreshedAt),
      defaultModel: stringValue(input.defaultModel),
      defaultCodexModel: stringValue(input.defaultCodexModel),
      defaultOpenCodeModel: stringValue(input.defaultOpenCodeModel),
      defaultGrokModel: stringValue(input.defaultGrokModel),
      defaultQoderModel: stringValue(input.defaultQoderModel),
      defaultModels: {
        claude: stringValue(defaults.claude, stringValue(input.defaultModel)),
        codex: stringValue(defaults.codex, stringValue(input.defaultCodexModel)),
        opencode: stringValue(defaults.opencode, stringValue(input.defaultOpenCodeModel)),
        grok: stringValue(defaults.grok, stringValue(input.defaultGrokModel)),
        qoder: stringValue(defaults.qoder, stringValue(input.defaultQoderModel))
      }
    };
  }
  function storedBoolean(key, fallback) {
    try {
      const value = window.localStorage.getItem(key);
      return value == null ? fallback : value === "true";
    } catch {
      return fallback;
    }
  }
  function storedNumber(key, fallback) {
    try {
      const parsed = Number(window.localStorage.getItem(key));
      return Number.isFinite(parsed) && parsed >= 0 ? parsed : fallback;
    } catch {
      return fallback;
    }
  }
  function nativeBridge() {
    try {
      return typeof WandNative !== "undefined" ? WandNative : null;
    } catch {
      return null;
    }
  }
  function notificationPermission(bridge) {
    if (bridge?.getPermission) {
      try {
        const permission = bridge.getPermission();
        if (permission === "granted" || permission === "denied" || permission === "default") return permission;
      } catch {
      }
    }
    if (typeof Notification === "undefined") return "unsupported";
    return Notification.permission;
  }
  function platformSnapshot() {
    const bridge = nativeBridge();
    const ua = navigator.userAgent || "";
    const kind = /WandPlatform\/Android/.test(ua) ? "android" : /WandPlatform\/iOS/.test(ua) ? "ios" : /WandPlatform\/macOS/.test(ua) || window.__wandMacNative === true ? "macos" : "browser";
    let appIcon = null;
    if (bridge?.getAppIcon) {
      try {
        appIcon = bridge.getAppIcon() || null;
      } catch {
      }
    }
    const appVersion = ua.match(/WandApp\/([^\s]+)/)?.[1] || null;
    return {
      kind,
      appVersion,
      appIcon,
      canSetAppIcon: typeof bridge?.setAppIcon === "function",
      canInstallDistribution: typeof bridge?.downloadUpdate === "function",
      hasNativeNotifications: typeof bridge?.sendNotification === "function"
    };
  }
  function notificationSnapshot() {
    const bridge = nativeBridge();
    let nativeSounds = [];
    let nativeSound = null;
    let hapticsEnabled = null;
    try {
      if (bridge?.getAvailableSounds) nativeSounds = JSON.parse(bridge.getAvailableSounds());
      if (bridge?.getNotificationSound) nativeSound = bridge.getNotificationSound();
      if (bridge?.isHapticEnabled) hapticsEnabled = bridge.isHapticEnabled();
    } catch {
    }
    const volume = bridge?.getNotificationVolume ? (() => {
      try {
        return bridge.getNotificationVolume();
      } catch {
        return storedNumber("wand-notif-volume", 80);
      }
    })() : storedNumber("wand-notif-volume", 80);
    return {
      sound: storedBoolean("wand-notif-sound", true),
      volume: Math.max(0, Math.min(100, volume)),
      bubble: storedBoolean("wand-notif-bubble", true),
      permission: notificationPermission(bridge),
      permissionSource: bridge ? "native" : typeof Notification === "undefined" ? "none" : "browser",
      nativeSounds,
      nativeSound,
      hapticsEnabled
    };
  }
  function capabilities(access, platform2) {
    const admin = access === "admin";
    return {
      manageSettings: admin,
      revealEnvironment: admin,
      manageSecurity: admin,
      manageUpdates: admin,
      manageConnectCode: admin,
      nativeSounds: platform2.hasNativeNotifications,
      haptics: notificationSnapshot().hapticsEnabled !== null,
      appIcon: platform2.canSetAppIcon,
      installDistribution: platform2.canInstallDistribution
    };
  }
  async function responseJson(response) {
    let body = {};
    try {
      body = record(await response.json());
    } catch {
    }
    if (!response.ok || typeof body.error === "string") {
      const error = new Error(stringValue(body.error, `\u8BF7\u6C42\u5931\u8D25\uFF08${response.status}\uFF09`));
      error.status = response.status;
      throw error;
    }
    return body;
  }
  async function request(path, init = {}) {
    return responseJson(await fetch(path, { credentials: "same-origin", ...init }));
  }
  async function post(path, body, signal) {
    return request(path, {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: body === void 0 ? void 0 : JSON.stringify(body),
      signal
    });
  }
  function aboutSnapshot(input, access) {
    const platform2 = platformSnapshot();
    const config = access === "admin" ? normalizeConfig(input.config) : null;
    return {
      access,
      capabilities: capabilities(access, platform2),
      about: normalizeAbout(input),
      config,
      desiredConfig: access === "admin" ? normalizeConfig(input.desiredConfig || input.config) : null,
      activeConfig: access === "admin" ? normalizeConfig(input.activeConfig || input.config) : null,
      restartRequired: input.restartRequired === true,
      hasCert: input.hasCert === true,
      autoUpdate: {
        web: record(input.autoUpdate).web === true,
        apk: record(input.autoUpdate).apk === true,
        dmg: record(input.autoUpdate).dmg === true,
        cli: record(input.autoUpdate).cli === true
      },
      models: null,
      providerCliUpdates: null,
      connectCode: null,
      notifications: notificationSnapshot(),
      platform: platform2
    };
  }
  function updateNotificationPreference(value) {
    const bridge = nativeBridge();
    try {
      if (typeof value.sound === "boolean") window.localStorage.setItem("wand-notif-sound", String(value.sound));
      if (typeof value.bubble === "boolean") window.localStorage.setItem("wand-notif-bubble", String(value.bubble));
      if (typeof value.volume === "number") window.localStorage.setItem("wand-notif-volume", String(value.volume));
    } catch {
    }
    if (typeof value.volume === "number" && bridge?.setNotificationVolume) {
      try {
        bridge.setNotificationVolume(value.volume);
      } catch {
      }
    }
    return notificationSnapshot();
  }
  function playPreviewSound() {
    const bridge = nativeBridge();
    if (bridge?.previewSound && bridge.getNotificationSound) {
      try {
        bridge.previewSound(bridge.getNotificationSound());
        return true;
      } catch {
        return false;
      }
    }
    try {
      const AudioContextCtor = window.AudioContext || window.webkitAudioContext;
      if (!AudioContextCtor) return false;
      const context = new AudioContextCtor();
      const oscillator = context.createOscillator();
      const gain = context.createGain();
      gain.gain.value = notificationSnapshot().volume / 100 * 0.08;
      oscillator.frequency.value = 660;
      oscillator.connect(gain);
      gain.connect(context.destination);
      oscillator.start();
      oscillator.stop(context.currentTime + 0.12);
      return true;
    } catch {
      return false;
    }
  }
  async function requestPermission() {
    const bridge = nativeBridge();
    if (bridge?.requestPermission) {
      return new Promise((resolve) => {
        let settled = false;
        const finish = (permission) => {
          if (settled) return;
          settled = true;
          window.clearTimeout(timeout);
          delete window._onNativePermissionResult;
          resolve(permission);
        };
        const timeout = window.setTimeout(() => finish(notificationPermission(bridge)), 3e3);
        window._onNativePermissionResult = (result) => {
          finish(result === "granted" || result === "denied" ? result : "default");
        };
        try {
          bridge.requestPermission();
        } catch {
          finish(notificationPermission(bridge));
        }
      });
    }
    if (typeof Notification === "undefined") return "unsupported";
    return Notification.requestPermission();
  }
  function systemNotification() {
    const bridge = nativeBridge();
    const permission = notificationPermission(bridge);
    if (permission === "denied" || permission === "default") return "denied";
    if (permission === "unsupported") return "unsupported";
    try {
      if (bridge?.sendNotification) {
        bridge.sendNotification("Wand \u6D4B\u8BD5\u901A\u77E5", "\u7CFB\u7EDF\u901A\u77E5\u5DF2\u6B63\u5E38\u5DE5\u4F5C\u3002", "wand-test");
      } else {
        new Notification("Wand \u6D4B\u8BD5\u901A\u77E5", { body: "\u7CFB\u7EDF\u901A\u77E5\u5DF2\u6B63\u5E38\u5DE5\u4F5C\u3002", tag: "wand-test" });
      }
      return "passed";
    } catch {
      return "failed";
    }
  }
  var HttpSettingsRepository = class {
    constructor(runtime6 = new BrowserSettingsRuntimeAdapter()) {
      __publicField(this, "runtime", runtime6);
    }
    async load(options = {}) {
      if (options.canManageSettings === false) {
        return aboutSnapshot(await request("/api/settings/about", { signal: options.signal }), "read-only");
      }
      let admin;
      try {
        admin = await request("/api/settings", { signal: options.signal });
      } catch (error) {
        if (error.status === 403) {
          return aboutSnapshot(await request("/api/settings/about", { signal: options.signal }), "read-only");
        }
        throw error;
      }
      const snapshot7 = aboutSnapshot(admin, "admin");
      const origin = window.location?.origin ? `?origin=${encodeURIComponent(window.location.origin)}` : "";
      const [models, cliUpdates, connectCode] = await Promise.all([
        request("/api/models", { signal: options.signal }).catch(() => null),
        request("/api/provider-cli-updates", { signal: options.signal }).catch(() => null),
        request(`/api/app-connect-code${origin}`, { signal: options.signal }).catch(() => null)
      ]);
      snapshot7.models = models ? normalizeModels(models) : null;
      snapshot7.providerCliUpdates = cliUpdates;
      snapshot7.connectCode = connectCode ? {
        code: stringValue(connectCode.code),
        url: stringValue(connectCode.url)
      } : null;
      return snapshot7;
    }
    async execute(command, options = {}) {
      let result;
      switch (command.type) {
        case "general.save":
          result = await post("/api/settings/config", command.value, options.signal);
          this.runtime.configSaved(normalizeConfig(record(result).config));
          break;
        case "ai.save":
          result = await post("/api/settings/config", {
            ...command.value,
            defaultModels: {
              claude: command.value.defaultModel,
              codex: command.value.defaultCodexModel,
              opencode: command.value.defaultOpenCodeModel,
              grok: command.value.defaultGrokModel,
              qoder: command.value.defaultQoderModel
            }
          }, options.signal);
          this.runtime.configSaved(normalizeConfig(record(result).config));
          break;
        case "display.save":
          result = await post("/api/settings/config", { cardDefaults: command.value }, options.signal);
          this.runtime.configSaved(normalizeConfig(record(result).config));
          break;
        case "password.change":
          result = await post("/api/set-password", { password: command.password }, options.signal);
          break;
        case "certificate.upload": {
          const response = await post("/api/settings/upload-cert", { key: command.key, cert: command.cert }, options.signal);
          result = { ...response, hasCert: true };
          break;
        }
        case "environment.load":
          result = await request(`/api/settings/env-preview${command.reveal ? "?reveal=1" : ""}`, { signal: options.signal });
          break;
        case "models.refresh":
          result = normalizeModels(await post("/api/models/refresh", void 0, options.signal));
          break;
        case "systemAi.import":
          result = await post("/api/settings/system-ai/import", { source: command.source }, options.signal);
          break;
        case "webUpdate.check":
          result = await request("/api/check-update", { signal: options.signal });
          break;
        case "webUpdate.install":
          result = await post("/api/update", void 0, options.signal);
          break;
        case "server.restart":
          result = await post("/api/restart", void 0, options.signal);
          break;
        case "cliUpdates.load":
          result = await request(`/api/provider-cli-updates${command.force ? "?refresh=1" : ""}`, { signal: options.signal });
          break;
        case "cliUpdates.install":
          result = await post("/api/provider-cli-updates", { ids: command.ids || [] }, options.signal);
          break;
        case "autoUpdate.set": {
          const body = { [command.target]: command.enabled };
          result = await post("/api/auto-update", body, options.signal);
          break;
        }
        case "updateChannel.set": {
          const channel = await post("/api/update-channel", { channel: command.channel }, options.signal);
          const update = await request("/api/check-update", { signal: options.signal });
          result = { channel: channel.channel, update };
          break;
        }
        case "connectCode.load": {
          const origin = window.location?.origin ? `?origin=${encodeURIComponent(window.location.origin)}` : "";
          result = await request(`/api/app-connect-code${origin}`, { signal: options.signal });
          break;
        }
        case "distribution.download": {
          const bridge = nativeBridge();
          if (bridge?.downloadUpdate) {
            bridge.downloadUpdate(command.url, command.fileName, command.source);
            result = { started: true, native: true };
          } else if (command.source === "local") {
            const probe = await fetch(command.url, { method: "HEAD", credentials: "same-origin", signal: options.signal });
            if (!probe.ok) throw new Error(`\u5B89\u88C5\u5305\u4E0D\u53EF\u7528\uFF08${probe.status}\uFF09\u3002`);
            const anchor = document.createElement("a");
            anchor.href = command.url;
            anchor.download = command.fileName;
            anchor.rel = "noopener";
            document.body.appendChild(anchor);
            anchor.click();
            anchor.remove();
            result = { started: true, native: false };
          } else {
            window.open(command.url, "_blank", "noopener");
            result = { started: true, native: false };
          }
          break;
        }
        case "clipboard.copy": {
          const bridge = nativeBridge();
          if (bridge?.copyToClipboard) bridge.copyToClipboard(command.text);
          else await navigator.clipboard.writeText(command.text);
          result = { copied: true };
          break;
        }
        case "notification.preferences.set":
          result = updateNotificationPreference(command.value);
          this.runtime.notificationPreferencesChanged(result);
          break;
        case "notification.sound.preview":
          result = { played: playPreviewSound() };
          break;
        case "notification.permission.request":
          result = { permission: await requestPermission() };
          break;
        case "notification.test": {
          if (command.delayMs) await new Promise((resolve) => window.setTimeout(resolve, command.delayMs));
          const prefs = notificationSnapshot();
          const played = playPreviewSound();
          if (prefs.bubble) wandOverlay.toast("\u6D4B\u8BD5\u901A\u77E5", { description: "\u8FD9\u662F\u4E00\u6761\u6D4B\u8BD5\u901A\u77E5\u3002" });
          result = {
            sound: played ? "passed" : "failed",
            bubble: prefs.bubble ? "passed" : "disabled",
            system: systemNotification()
          };
          break;
        }
        case "notification.nativeSound.set": {
          nativeBridge()?.setNotificationSound?.(command.sound);
          result = { sound: command.sound };
          break;
        }
        case "notification.nativeSound.preview": {
          let played = false;
          try {
            nativeBridge()?.previewSound?.(command.sound);
            played = true;
          } catch {
          }
          result = { played };
          break;
        }
        case "notification.haptics.set": {
          nativeBridge()?.setHapticEnabled?.(command.enabled);
          if (command.enabled) nativeBridge()?.vibrate?.("medium");
          result = { enabled: command.enabled };
          break;
        }
        case "appIcon.set":
          nativeBridge()?.setAppIcon?.(command.icon);
          result = { icon: command.icon };
          break;
      }
      return result;
    }
  };
  var httpSettingsRepository = new HttpSettingsRepository();

  // src/web-ui/react/settings/tabs.tsx
  var import_react7 = __toESM(require_react(), 1);

  // src/web-ui/react/settings/fields.tsx
  var import_jsx_runtime27 = __toESM(require_jsx_runtime(), 1);
  function SettingsSection({
    title,
    description,
    action,
    children
  }) {
    return /* @__PURE__ */ (0, import_jsx_runtime27.jsxs)("section", { className: "wand-settings-section", children: [
      /* @__PURE__ */ (0, import_jsx_runtime27.jsxs)("div", { className: "wand-settings-section-heading", children: [
        /* @__PURE__ */ (0, import_jsx_runtime27.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime27.jsx)("h3", { children: title }),
          description ? /* @__PURE__ */ (0, import_jsx_runtime27.jsx)("p", { children: description }) : null
        ] }),
        action ? /* @__PURE__ */ (0, import_jsx_runtime27.jsx)("div", { className: "wand-settings-section-action", children: action }) : null
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime27.jsx)("div", { className: "wand-settings-section-body", children })
    ] });
  }
  function SettingsGrid({ children }) {
    return /* @__PURE__ */ (0, import_jsx_runtime27.jsx)("div", { className: "wand-settings-grid", children });
  }
  function SettingsField({
    label,
    hint,
    error,
    htmlFor,
    children
  }) {
    return /* @__PURE__ */ (0, import_jsx_runtime27.jsxs)("div", { className: "wand-settings-field", children: [
      /* @__PURE__ */ (0, import_jsx_runtime27.jsx)("label", { htmlFor, children: label }),
      children,
      error ? /* @__PURE__ */ (0, import_jsx_runtime27.jsx)("span", { className: "wand-settings-field-error", children: error }) : null,
      !error && hint ? /* @__PURE__ */ (0, import_jsx_runtime27.jsx)("span", { className: "wand-settings-field-hint", children: hint }) : null
    ] });
  }
  function SettingsTextInput({
    id,
    value,
    onChange,
    type = "text",
    placeholder,
    disabled,
    invalid,
    autoComplete,
    min: min2,
    max: max2,
    list
  }) {
    return /* @__PURE__ */ (0, import_jsx_runtime27.jsx)(
      "input",
      {
        id,
        className: "wand-settings-input",
        type,
        value,
        placeholder,
        disabled,
        "aria-invalid": invalid || void 0,
        autoComplete,
        min: min2,
        max: max2,
        list,
        spellCheck: false,
        onChange: (event) => onChange(event.currentTarget.value)
      }
    );
  }
  function SettingsSelect({
    id,
    ariaLabel,
    value,
    options,
    onChange,
    disabled
  }) {
    return /* @__PURE__ */ (0, import_jsx_runtime27.jsx)("div", { id, className: "wand-settings-select", children: /* @__PURE__ */ (0, import_jsx_runtime27.jsx)(
      WandSelect,
      {
        ariaLabel,
        value,
        options,
        disabled,
        onValueChange: onChange
      }
    ) });
  }
  function SettingsToggle({
    label,
    description,
    checked,
    onCheckedChange,
    disabled
  }) {
    return /* @__PURE__ */ (0, import_jsx_runtime27.jsxs)("div", { className: "wand-settings-toggle-row", children: [
      /* @__PURE__ */ (0, import_jsx_runtime27.jsxs)("div", { children: [
        /* @__PURE__ */ (0, import_jsx_runtime27.jsx)("strong", { children: label }),
        description ? /* @__PURE__ */ (0, import_jsx_runtime27.jsx)("span", { children: description }) : null
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime27.jsx)(
        WandSwitch,
        {
          ariaLabel: label,
          checked,
          disabled,
          onCheckedChange
        }
      )
    ] });
  }
  function SettingsStatus({
    children,
    tone = "info"
  }) {
    if (!children) return null;
    return /* @__PURE__ */ (0, import_jsx_runtime27.jsx)("div", { className: `wand-settings-status wand-settings-status-${tone}`, role: "status", "aria-live": "polite", children });
  }
  function SettingsSaveBar({
    label,
    pending,
    disabled,
    onSave,
    status,
    tone
  }) {
    return /* @__PURE__ */ (0, import_jsx_runtime27.jsxs)("div", { className: "wand-settings-save-bar", children: [
      /* @__PURE__ */ (0, import_jsx_runtime27.jsx)(SettingsStatus, { tone, children: status }),
      /* @__PURE__ */ (0, import_jsx_runtime27.jsx)(WandButton, { kind: "primary", disabled: disabled || pending, onClick: onSave, children: pending ? "\u4FDD\u5B58\u4E2D\u2026" : label })
    ] });
  }
  function SettingsActionButton({
    children,
    pending,
    ...props
  }) {
    return /* @__PURE__ */ (0, import_jsx_runtime27.jsx)(WandButton, { ...props, disabled: props.disabled || pending, children: pending ? "\u5904\u7406\u4E2D\u2026" : children });
  }

  // src/web-ui/react/settings/tabs.tsx
  var import_jsx_runtime28 = __toESM(require_jsx_runtime(), 1);
  function messageOf(error, fallback) {
    return error instanceof Error && error.message ? error.message : fallback;
  }
  function formatBytes(size4) {
    if (size4 == null || !Number.isFinite(size4)) return "-";
    if (size4 < 1024) return `${size4} B`;
    const units = ["KB", "MB", "GB"];
    let value = size4 / 1024;
    let unit = 0;
    while (value >= 1024 && unit < units.length - 1) {
      value /= 1024;
      unit += 1;
    }
    return `${value >= 10 ? value.toFixed(0) : value.toFixed(1)} ${units[unit]}`;
  }
  function versionParts(value) {
    const match = value?.match(/\d+(?:\.\d+){1,3}/)?.[0];
    return match ? match.split(".").map(Number) : [];
  }
  function isNewerVersion(candidate, current) {
    const left = versionParts(candidate);
    const right = versionParts(current);
    if (!left.length || !right.length) return true;
    for (let index2 = 0; index2 < Math.max(left.length, right.length); index2 += 1) {
      if ((left[index2] || 0) !== (right[index2] || 0)) return (left[index2] || 0) > (right[index2] || 0);
    }
    return false;
  }
  function ConnectCodeDialog({ code }) {
    const controller = (0, import_react7.useSyncExternalStore)(settingsStore.subscribe, settingsStore.getSnapshot, settingsStore.getSnapshot);
    const canvasRef = (0, import_react7.useRef)(null);
    const [qrError, setQrError] = (0, import_react7.useState)("");
    (0, import_react7.useEffect)(() => {
      if (controller.nested !== "qr" || !canvasRef.current) return;
      const library = window.QRCodeLib;
      if (!library || typeof library.toCanvas !== "function") {
        setQrError("\u4E8C\u7EF4\u7801\u5E93\u672A\u52A0\u8F7D\uFF0C\u53EF\u590D\u5236\u8FDE\u63A5\u7801\u3002 ");
        return;
      }
      setQrError("");
      try {
        library.toCanvas(canvasRef.current, code, {
          width: Math.max(240, Math.min(420, window.innerWidth * 0.72)),
          margin: 2,
          errorCorrectionLevel: "M",
          color: { dark: "#1f1b17", light: "#ffffff" }
        }, (error) => {
          if (error) setQrError("\u4E8C\u7EF4\u7801\u751F\u6210\u5931\u8D25\uFF0C\u53EF\u590D\u5236\u8FDE\u63A5\u7801\u3002");
        });
      } catch {
        setQrError("\u4E8C\u7EF4\u7801\u751F\u6210\u5931\u8D25\uFF0C\u53EF\u590D\u5236\u8FDE\u63A5\u7801\u3002");
      }
    }, [code, controller.nested]);
    return /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)(
      WandDialogSurface,
      {
        open: controller.nested === "qr",
        onOpenChange: (open) => {
          if (!open) settingsStore.setNested(null);
        },
        title: "App \u8FDE\u63A5\u4E8C\u7EF4\u7801",
        description: "\u7528 Wand App \u626B\u4E00\u626B\uFF0C\u8FDE\u63A5\u5F53\u524D\u670D\u52A1\u5668\u3002",
        className: "wand-settings-nested-dialog wand-settings-qr-dialog",
        overlayClassName: "wand-settings-nested-overlay",
        headerClassName: "wand-settings-header",
        titleClassName: "wand-settings-title",
        descriptionClassName: "wand-settings-description",
        closeLabel: "\u5173\u95ED\u8FDE\u63A5\u4E8C\u7EF4\u7801",
        testId: "settings-connect-qr-dialog",
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("canvas", { ref: canvasRef, "aria-label": "App \u8FDE\u63A5\u4E8C\u7EF4\u7801" }),
          qrError ? /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsStatus, { tone: "warning", children: qrError }) : null,
          /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("code", { className: "wand-settings-connect-code", children: code })
        ]
      }
    );
  }
  function DistributionSection({
    kind,
    title,
    distribution,
    currentVersion,
    repository,
    toast
  }) {
    const assets = ["github", "local"].map((source) => ({ source, asset: distribution[source] })).filter((entry) => entry.asset !== null);
    if (!distribution.enabled && assets.length === 0 && !currentVersion) return null;
    return /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsSection, { title, description: currentVersion ? `\u5F53\u524D App \u7248\u672C\uFF1A${currentVersion}` : "\u5BA2\u6237\u7AEF\u4E0B\u8F7D\u4E0E\u7248\u672C\u4FE1\u606F", children: assets.length ? assets.map(({ source, asset }) => {
      const installable = !currentVersion || isNewerVersion(asset.version, currentVersion);
      return /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("div", { className: "wand-settings-download-row", children: [
        /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("strong", { children: source === "github" ? "\u7EBF\u4E0A\u7248\u672C" : "\u672C\u5730\u7248\u672C" }),
          /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("span", { children: [
            asset.version ? `v${asset.version}` : asset.fileName,
            " \xB7 ",
            formatBytes(asset.size)
          ] })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(
          WandButton,
          {
            kind: "secondary",
            disabled: !installable,
            "aria-label": `${installable ? "\u4E0B\u8F7D" : "\u5DF2\u5B89\u88C5"}${title}${source === "github" ? "\u7EBF\u4E0A\u7248\u672C" : "\u672C\u5730\u7248\u672C"}`,
            onClick: async () => {
              await repository.execute({
                type: "distribution.download",
                kind,
                source,
                url: asset.downloadUrl,
                fileName: asset.fileName
              });
              toast("\u5DF2\u5F00\u59CB\u4E0B\u8F7D", "info");
            },
            children: installable ? currentVersion ? "\u4E0B\u8F7D\u5E76\u5B89\u88C5" : "\u4E0B\u8F7D" : "\u5DF2\u5B89\u88C5"
          }
        )
      ] }, source);
    }) : /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("div", { className: "wand-settings-empty", children: "\u6682\u65E0\u53EF\u7528\u5B89\u88C5\u5305" }) });
  }
  function AboutSettingsTab({ snapshot: snapshot7, repository, refresh, toast, showRestart: showRestart2 }) {
    const [pending, setPending] = (0, import_react7.useState)("");
    const [status, setStatus] = (0, import_react7.useState)("");
    const [tone, setTone] = (0, import_react7.useState)("info");
    const [update, setUpdate] = (0, import_react7.useState)(null);
    const about = snapshot7.about;
    async function action(name, task, success) {
      setPending(name);
      setStatus("");
      try {
        await task();
        if (success) {
          setStatus(success);
          setTone("success");
        }
      } catch (cause) {
        setStatus(messageOf(cause, "\u64CD\u4F5C\u5931\u8D25\u3002"));
        setTone("error");
      } finally {
        setPending("");
      }
    }
    const cliItems = snapshot7.providerCliUpdates?.items || [];
    const cliUpdates = cliItems.filter((item) => item.updateAvailable && item.updateSupported);
    return /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("section", { className: "wand-settings-panel", "aria-label": "\u5173\u4E8E Wand", children: [
      /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("header", { className: "wand-settings-panel-heading", children: [
        /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("h2", { children: "\u5173\u4E8E Wand" }),
        /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("p", { children: "\u67E5\u770B\u7248\u672C\u4FE1\u606F\u3001\u66F4\u65B0\u72B6\u6001\u548C\u5BA2\u6237\u7AEF\u8FDE\u63A5\u65B9\u5F0F\u3002" })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsSection, { title: "\u7248\u672C\u4FE1\u606F", children: /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("dl", { className: "wand-settings-about-list", children: [
        /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("dt", { children: "\u5305\u540D" }),
          /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("dd", { children: about.packageName })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("dt", { children: "\u5F53\u524D\u7248\u672C" }),
          /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("dd", { children: about.version })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("dt", { children: "Node.js \u8981\u6C42" }),
          /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("dd", { children: about.nodeVersion })
        ] }),
        about.build.shortCommit ? /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("dt", { children: "\u6784\u5EFA" }),
          /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("dd", { children: [
            about.build.shortCommit,
            about.build.channel ? ` \xB7 ${about.build.channel}` : ""
          ] })
        ] }) : null,
        about.repoUrl ? /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("div", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("dt", { children: "\u4ED3\u5E93\u5730\u5740" }),
          /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("dd", { children: /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("a", { href: about.repoUrl, target: "_blank", rel: "noopener noreferrer", children: about.repoUrl }) })
        ] }) : null
      ] }) }),
      snapshot7.access === "admin" ? /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)(import_jsx_runtime28.Fragment, { children: [
        /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)(SettingsSection, { title: "Web \u7AEF\u66F4\u65B0", description: `\u66F4\u65B0\u901A\u9053\uFF1A${about.updateChannel === "beta" ? "Beta" : "\u7A33\u5B9A\u7248"}`, children: [
          /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("div", { className: "wand-settings-about-list", children: /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("div", { children: [
            /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("span", { children: "\u6700\u65B0\u7248\u672C" }),
            /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("strong", { children: update?.latest || about.latestVersion || "\u5C1A\u672A\u68C0\u67E5" })
          ] }) }),
          /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(
            SettingsToggle,
            {
              label: "Beta \u901A\u9053",
              description: "\u63A5\u6536\u6D4B\u8BD5\u7248\u672C\uFF0C\u53EF\u80FD\u5305\u542B\u5C1A\u672A\u7A33\u5B9A\u7684\u529F\u80FD\u3002",
              checked: about.updateChannel === "beta",
              disabled: !!pending,
              onCheckedChange: (checked) => void action("channel", async () => {
                const result = await repository.execute({ type: "updateChannel.set", channel: checked ? "beta" : "stable" });
                setUpdate(result.update);
                await refresh();
              }, "\u66F4\u65B0\u901A\u9053\u5DF2\u5207\u6362\u3002")
            }
          ),
          /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(
            SettingsToggle,
            {
              label: "\u81EA\u52A8\u66F4\u65B0 Web \u670D\u52A1",
              description: "\u68C0\u6D4B\u5230\u65B0\u7248\u672C\u540E\u81EA\u52A8\u4E0B\u8F7D\u5B89\u88C5\u5E76\u91CD\u542F\u670D\u52A1\u3002",
              checked: snapshot7.autoUpdate.web,
              disabled: !!pending,
              onCheckedChange: (enabled) => void action("auto-web", async () => {
                await repository.execute({ type: "autoUpdate.set", target: "web", enabled });
                await refresh();
              }, "\u81EA\u52A8\u66F4\u65B0\u504F\u597D\u5DF2\u4FDD\u5B58\u3002")
            }
          ),
          /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("div", { className: "wand-settings-button-row", children: [
            /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsActionButton, { pending: pending === "check", kind: "secondary", onClick: () => void action("check", async () => setUpdate(await repository.execute({ type: "webUpdate.check" })), "\u7248\u672C\u68C0\u67E5\u5B8C\u6210\u3002"), children: "\u68C0\u67E5\u66F4\u65B0" }),
            /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsActionButton, { pending: pending === "install", kind: "primary", onClick: () => void action("install", async () => {
              const result = await repository.execute({ type: "webUpdate.install" });
              setStatus(result.message);
            }, void 0), children: "\u66F4\u65B0\u6216\u91CD\u65B0\u5B89\u88C5" }),
            snapshot7.restartRequired ? /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsActionButton, { pending: pending === "restart", kind: "secondary", onClick: () => void action("restart", async () => {
              try {
                await repository.execute({ type: "server.restart" });
              } finally {
                showRestart2();
              }
            }, "\u670D\u52A1\u6B63\u5728\u91CD\u542F\u2026"), children: "\u91CD\u542F\u670D\u52A1" }) : null
          ] })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)(SettingsSection, { title: "\u5F00\u53D1 CLI", description: "Claude Code\u3001Codex\u3001OpenCode \u4E0E Qoder CLI \u7684\u670D\u52A1\u7AEF\u7248\u672C\u3002", children: [
          /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("div", { className: "wand-settings-cli-list", children: [
            cliItems.map((item) => /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("div", { children: [
              /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("strong", { children: item.label }),
              /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("span", { children: [
                item.installed ? item.currentVersion || "\u672A\u77E5\u7248\u672C" : "\u672A\u5B89\u88C5",
                item.updateAvailable ? ` \u2192 ${item.latestVersion || "\u6700\u65B0\u7248"}` : ""
              ] })
            ] }, item.id)),
            !cliItems.length ? /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("div", { className: "wand-settings-empty", children: "\u5C1A\u672A\u68C0\u67E5 CLI \u7248\u672C" }) : null
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(
            SettingsToggle,
            {
              label: "\u81EA\u52A8\u66F4\u65B0\u5F00\u53D1 CLI",
              description: "\u670D\u52A1\u7AEF\u5B9A\u671F\u68C0\u67E5\u5E76\u8C03\u7528\u5404 CLI \u7684\u5B98\u65B9\u66F4\u65B0\u5668\u3002",
              checked: snapshot7.autoUpdate.cli,
              disabled: !!pending,
              onCheckedChange: (enabled) => void action("auto-cli", async () => {
                await repository.execute({ type: "autoUpdate.set", target: "cli", enabled });
                await refresh();
              }, "CLI \u81EA\u52A8\u66F4\u65B0\u504F\u597D\u5DF2\u4FDD\u5B58\u3002")
            }
          ),
          /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("div", { className: "wand-settings-button-row", children: [
            /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsActionButton, { pending: pending === "cli-check", kind: "secondary", onClick: () => void action("cli-check", async () => {
              await repository.execute({ type: "cliUpdates.load", force: true });
              await refresh();
            }, "CLI \u7248\u672C\u68C0\u67E5\u5B8C\u6210\u3002"), children: "\u68C0\u67E5 CLI \u66F4\u65B0" }),
            cliUpdates.length ? /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)(SettingsActionButton, { pending: pending === "cli-install", kind: "primary", onClick: () => void action("cli-install", async () => {
              await repository.execute({ type: "cliUpdates.install", ids: cliUpdates.map((item) => item.id) });
              await refresh();
            }, "CLI \u66F4\u65B0\u5B8C\u6210\u3002"), children: [
              "\u5FEB\u901F\u66F4\u65B0 (",
              cliUpdates.length,
              ")"
            ] }) : null
          ] })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(DistributionSection, { kind: "apk", title: "Android App", distribution: about.androidApk, currentVersion: snapshot7.platform.kind === "android" ? snapshot7.platform.appVersion : null, repository, toast }),
        /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(DistributionSection, { kind: "dmg", title: "macOS App", distribution: about.macosDmg, currentVersion: snapshot7.platform.kind === "macos" ? snapshot7.platform.appVersion : null, repository, toast }),
        /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)(SettingsSection, { title: "App \u8FDE\u63A5\u7801", description: "\u7C98\u8D34\u6216\u626B\u7801\u540E\u53EF\u8FDE\u63A5\u5F53\u524D\u670D\u52A1\uFF1B\u4FEE\u6539\u5BC6\u7801\u540E\u4F1A\u5931\u6548\u3002", children: [
          /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("code", { className: "wand-settings-connect-code", "aria-label": "App \u8FDE\u63A5\u7801", children: snapshot7.connectCode?.code || "\u6682\u4E0D\u53EF\u7528" }),
          /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("div", { className: "wand-settings-button-row", children: [
            /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(WandButton, { disabled: !snapshot7.connectCode?.code, kind: "secondary", onClick: async () => {
              await repository.execute({ type: "clipboard.copy", text: snapshot7.connectCode.code });
              toast("\u8FDE\u63A5\u7801\u5DF2\u590D\u5236", "success");
            }, children: "\u590D\u5236\u8FDE\u63A5\u7801" }),
            /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(WandButton, { disabled: !snapshot7.connectCode?.code, kind: "secondary", onClick: () => settingsStore.setNested("qr"), children: "\u653E\u5927\u8FDE\u63A5\u4E8C\u7EF4\u7801" })
          ] })
        ] })
      ] }) : null,
      status ? /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsStatus, { tone, children: status }) : null,
      /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(ConnectCodeDialog, { code: snapshot7.connectCode?.code || "" })
    ] });
  }
  var MODE_OPTIONS = [
    { value: "default", label: "\u9ED8\u8BA4" },
    { value: "assist", label: "\u8F85\u52A9\u6A21\u5F0F" },
    { value: "agent", label: "Agent" },
    { value: "agent-max", label: "Agent Max" },
    { value: "auto-edit", label: "\u81EA\u52A8\u7F16\u8F91" },
    { value: "full-access", label: "\u5B8C\u5168\u8BBF\u95EE" },
    { value: "native", label: "\u539F\u751F\u6A21\u5F0F" },
    { value: "managed", label: "\u6258\u7BA1\u6A21\u5F0F" }
  ];
  function generalFromSnapshot(snapshot7) {
    const config = snapshot7.config;
    return {
      host: config.host,
      port: config.port,
      https: config.https,
      defaultMode: config.defaultMode,
      defaultCwd: config.defaultCwd,
      shell: config.shell,
      language: config.language,
      structuredRunner: config.structuredRunner,
      inheritEnv: config.inheritEnv
    };
  }
  function EnvironmentDialog({ repository }) {
    const controller = (0, import_react7.useSyncExternalStore)(settingsStore.subscribe, settingsStore.getSnapshot, settingsStore.getSnapshot);
    const [preview, setPreview] = (0, import_react7.useState)(null);
    const [error, setError] = (0, import_react7.useState)("");
    const [search, setSearch] = (0, import_react7.useState)("");
    const [loading, setLoading] = (0, import_react7.useState)(false);
    async function load(reveal = false) {
      setLoading(true);
      setError("");
      try {
        setPreview(await repository.execute({ type: "environment.load", reveal }));
      } catch (cause) {
        setError(messageOf(cause, "\u73AF\u5883\u53D8\u91CF\u52A0\u8F7D\u5931\u8D25\u3002"));
      } finally {
        setLoading(false);
      }
    }
    (0, import_react7.useEffect)(() => {
      if (controller.nested === "environment") void load(false);
    }, [controller.nested]);
    const entries = (0, import_react7.useMemo)(() => {
      const needle = search.trim().toLowerCase();
      return (preview?.entries || []).filter((entry) => !needle || entry.name.toLowerCase().includes(needle));
    }, [preview, search]);
    return /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)(
      WandDialogSurface,
      {
        open: controller.nested === "environment",
        onOpenChange: (open) => {
          if (!open) settingsStore.setNested(null);
        },
        title: "\u5C06\u6CE8\u5165\u5B50\u8FDB\u7A0B\u7684\u73AF\u5883\u53D8\u91CF",
        description: "\u8FD9\u4E9B\u53D8\u91CF\u4F1A\u4F20\u7ED9 Claude\u3001Codex \u4E0E OpenCode \u7684\u5B50\u8FDB\u7A0B\u3002",
        className: "wand-settings-nested-dialog",
        overlayClassName: "wand-settings-nested-overlay",
        headerClassName: "wand-settings-header",
        titleClassName: "wand-settings-title",
        descriptionClassName: "wand-settings-description",
        closeLabel: "\u5173\u95ED\u73AF\u5883\u53D8\u91CF\u9884\u89C8",
        testId: "settings-environment-dialog",
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("div", { className: "wand-settings-env-toolbar", children: [
            /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(
              SettingsTextInput,
              {
                id: "settings-environment-search",
                value: search,
                type: "search",
                placeholder: "\u641C\u7D22\u53D8\u91CF\u540D",
                onChange: setSearch
              }
            ),
            /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(
              SettingsToggle,
              {
                label: "\u663E\u793A\u654F\u611F\u503C",
                description: "\u4E34\u65F6\u8BF7\u6C42\u670D\u52A1\u7AEF\u8FD4\u56DE\u672A\u63A9\u7801\u503C",
                checked: preview?.reveal === true,
                disabled: loading,
                onCheckedChange: (checked) => void load(checked)
              }
            )
          ] }),
          error ? /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsStatus, { tone: "error", children: error }) : null,
          /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("div", { className: "wand-settings-env-list", role: "table", "aria-label": "\u5B50\u8FDB\u7A0B\u73AF\u5883\u53D8\u91CF", children: [
            loading ? /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("div", { role: "status", children: "\u52A0\u8F7D\u4E2D\u2026" }) : entries.map((entry) => /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("div", { className: "wand-settings-env-row", role: "row", children: [
              /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("code", { role: "cell", children: entry.name }),
              /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("span", { role: "cell", title: entry.value, children: entry.value })
            ] }, entry.name)),
            !loading && entries.length === 0 ? /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("div", { className: "wand-settings-empty", children: "\u6CA1\u6709\u5339\u914D\u7684\u53D8\u91CF" }) : null
          ] })
        ]
      }
    );
  }
  function GeneralSettingsTab({ snapshot: snapshot7, repository, refresh, toast }) {
    const [form, setForm] = (0, import_react7.useState)(() => generalFromSnapshot(snapshot7));
    const [pending, setPending] = (0, import_react7.useState)(false);
    const [status, setStatus] = (0, import_react7.useState)("");
    const [tone, setTone] = (0, import_react7.useState)("info");
    const [errors, setErrors] = (0, import_react7.useState)({});
    (0, import_react7.useEffect)(() => setForm(generalFromSnapshot(snapshot7)), [snapshot7]);
    function update(key, value) {
      setForm((current) => ({ ...current, [key]: value }));
      setErrors((current) => ({ ...current, [key]: "" }));
    }
    async function save() {
      const nextErrors = {};
      if (!form.host.trim()) nextErrors.host = "Host \u4E0D\u80FD\u4E3A\u7A7A\u3002";
      if (!Number.isInteger(form.port) || form.port < 1 || form.port > 65535) nextErrors.port = "\u7AEF\u53E3\u5FC5\u987B\u662F 1\u201365535 \u7684\u6574\u6570\u3002";
      if (!form.shell.trim()) nextErrors.shell = "Shell \u4E0D\u80FD\u4E3A\u7A7A\u3002";
      setErrors(nextErrors);
      if (Object.keys(nextErrors).length) {
        setStatus("\u8BF7\u4FEE\u6B63\u6807\u8BB0\u7684\u914D\u7F6E\u9879\u3002");
        setTone("error");
        return;
      }
      setPending(true);
      setStatus("");
      try {
        const result = await repository.execute({ type: "general.save", value: form });
        setStatus(result.restartRequired ? "\u914D\u7F6E\u5DF2\u4FDD\u5B58\uFF1BHost\u3001\u7AEF\u53E3\u3001HTTPS \u6216 Shell \u7684\u53D8\u5316\u9700\u8981\u91CD\u542F\u670D\u52A1\u540E\u751F\u6548\u3002" : "\u57FA\u672C\u914D\u7F6E\u5DF2\u4FDD\u5B58\u3002");
        setTone(result.restartRequired ? "warning" : "success");
        await refresh();
        toast("\u57FA\u672C\u914D\u7F6E\u5DF2\u4FDD\u5B58", "success");
      } catch (cause) {
        setStatus(messageOf(cause, "\u4FDD\u5B58\u57FA\u672C\u914D\u7F6E\u5931\u8D25\u3002"));
        setTone("error");
      } finally {
        setPending(false);
      }
    }
    return /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("section", { className: "wand-settings-panel", "aria-label": "\u57FA\u672C\u914D\u7F6E", children: [
      /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("header", { className: "wand-settings-panel-heading", children: [
        /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("h2", { children: "\u57FA\u672C\u914D\u7F6E" }),
        /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("p", { children: "\u914D\u7F6E\u670D\u52A1\u8FDE\u63A5\u3001\u6267\u884C\u65B9\u5F0F\u548C\u5DE5\u4F5C\u76EE\u5F55\u3002" })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)(SettingsSection, { title: "\u670D\u52A1\u8FDE\u63A5", description: "\u90E8\u7F72\u5B57\u6BB5\u4FDD\u5B58\u540E\u53EF\u80FD\u9700\u8981\u91CD\u542F\u670D\u52A1\u3002", children: [
        /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)(SettingsGrid, { children: [
          /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsField, { label: "Host", htmlFor: "settings-host", error: errors.host, children: /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsTextInput, { id: "settings-host", value: form.host, invalid: !!errors.host, onChange: (value) => update("host", value) }) }),
          /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsField, { label: "\u7AEF\u53E3", htmlFor: "settings-port", error: errors.port, children: /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsTextInput, { id: "settings-port", type: "number", min: 1, max: 65535, value: form.port, invalid: !!errors.port, onChange: (value) => update("port", Number(value)) }) })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(
          SettingsToggle,
          {
            label: "\u542F\u7528 HTTPS",
            description: "\u4F7F\u7528\u670D\u52A1\u7AEF\u8BC1\u4E66\u52A0\u5BC6\u6D4F\u89C8\u5668\u4E0E\u670D\u52A1\u4E4B\u95F4\u7684\u8FDE\u63A5\u3002",
            checked: form.https,
            onCheckedChange: (checked) => update("https", checked)
          }
        )
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)(SettingsSection, { title: "\u6267\u884C\u504F\u597D", description: "\u5E94\u7528\u4E8E\u4E4B\u540E\u521B\u5EFA\u7684\u65B0\u4F1A\u8BDD\u3002", children: [
        /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)(SettingsGrid, { children: [
          /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsField, { label: "\u9ED8\u8BA4\u6A21\u5F0F", children: /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsSelect, { id: "settings-default-mode", ariaLabel: "\u9ED8\u8BA4\u6267\u884C\u6A21\u5F0F", value: form.defaultMode, options: MODE_OPTIONS, onChange: (value) => update("defaultMode", value) }) }),
          /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsField, { label: "\u7ED3\u6784\u5316\u8FD0\u884C\u5668", children: /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(
            SettingsSelect,
            {
              id: "settings-structured-runner",
              ariaLabel: "\u7ED3\u6784\u5316\u8FD0\u884C\u5668",
              value: form.structuredRunner,
              options: [{ value: "cli", label: "CLI" }, { value: "sdk", label: "SDK" }],
              onChange: (value) => update("structuredRunner", value)
            }
          ) }),
          /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsField, { label: "\u754C\u9762\u8BED\u8A00", children: /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(
            SettingsSelect,
            {
              id: "settings-language",
              ariaLabel: "\u754C\u9762\u8BED\u8A00",
              value: form.language || "auto",
              options: [{ value: "auto", label: "\u81EA\u52A8" }, { value: "zh-CN", label: "\u7B80\u4F53\u4E2D\u6587" }, { value: "en", label: "English" }],
              onChange: (value) => update("language", value === "auto" ? "" : value)
            }
          ) })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(
          SettingsToggle,
          {
            label: "\u7EE7\u627F\u73AF\u5883\u53D8\u91CF",
            description: "\u628A\u5F53\u524D\u670D\u52A1\u8FDB\u7A0B\u7684\u73AF\u5883\u53D8\u91CF\u4F20\u7ED9 PTY \u4E0E\u7ED3\u6784\u5316\u5B50\u8FDB\u7A0B\u3002",
            checked: form.inheritEnv,
            onCheckedChange: (checked) => update("inheritEnv", checked)
          }
        ),
        /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(WandButton, { kind: "secondary", onClick: () => settingsStore.setNested("environment"), children: "\u67E5\u770B\u5C06\u6CE8\u5165\u7684\u73AF\u5883\u53D8\u91CF" })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsSection, { title: "\u5DE5\u4F5C\u73AF\u5883", children: /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)(SettingsGrid, { children: [
        /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsField, { label: "\u9ED8\u8BA4\u5DE5\u4F5C\u76EE\u5F55", htmlFor: "settings-default-cwd", children: /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsTextInput, { id: "settings-default-cwd", value: form.defaultCwd, placeholder: "/home/user", onChange: (value) => update("defaultCwd", value) }) }),
        /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsField, { label: "Shell", htmlFor: "settings-shell", error: errors.shell, children: /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsTextInput, { id: "settings-shell", value: form.shell, invalid: !!errors.shell, placeholder: "/bin/bash", onChange: (value) => update("shell", value) }) })
      ] }) }),
      snapshot7.platform.canSetAppIcon ? /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsSection, { title: "\u5E94\u7528\u56FE\u6807", description: "\u8FD4\u56DE\u7CFB\u7EDF\u684C\u9762\u540E\u751F\u6548\u3002", children: /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("div", { className: "wand-settings-button-row", role: "group", "aria-label": "\u5E94\u7528\u56FE\u6807", children: ["shorthair", "garfield"].map((icon) => /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(
        WandButton,
        {
          kind: snapshot7.platform.appIcon === icon ? "primary" : "secondary",
          "aria-pressed": snapshot7.platform.appIcon === icon,
          onClick: async () => {
            await repository.execute({ type: "appIcon.set", icon });
            toast("\u56FE\u6807\u5DF2\u5207\u6362\uFF0C\u8FD4\u56DE\u684C\u9762\u540E\u751F\u6548", "success");
          },
          children: icon === "shorthair" ? "\u8D5B\u535A\u864E\u599E" : "\u52E4\u52B3\u521D\u4E8C"
        },
        icon
      )) }) }) : null,
      /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsSaveBar, { label: "\u4FDD\u5B58\u57FA\u672C\u914D\u7F6E", pending, onSave: () => void save(), status, tone }),
      /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(EnvironmentDialog, { repository })
    ] });
  }
  function aiFromSnapshot(snapshot7) {
    const config = snapshot7.config;
    return {
      defaultModel: config.defaultModel,
      defaultCodexModel: config.defaultCodexModel,
      defaultOpenCodeModel: config.defaultOpenCodeModel,
      defaultGrokModel: config.defaultGrokModel,
      defaultQoderModel: config.defaultQoderModel,
      commitCli: config.commitCli,
      commitModel: config.commitModel,
      commitAiSource: config.commitAiSource,
      systemAi: { ...config.systemAi, apiKey: "" }
    };
  }
  function ModelSuggestions({ id, models }) {
    return /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("datalist", { id, children: models.map((model) => /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("option", { value: model.id, children: model.label || model.id }, model.id)) });
  }
  function AiSettingsTab({ snapshot: snapshot7, repository, refresh, setSnapshot, toast }) {
    const [form, setForm] = (0, import_react7.useState)(() => aiFromSnapshot(snapshot7));
    const [pending, setPending] = (0, import_react7.useState)("");
    const [status, setStatus] = (0, import_react7.useState)("");
    const [tone, setTone] = (0, import_react7.useState)("info");
    const [errors, setErrors] = (0, import_react7.useState)({});
    (0, import_react7.useEffect)(() => setForm(aiFromSnapshot(snapshot7)), [snapshot7]);
    function update(key, value) {
      setForm((current) => ({ ...current, [key]: value }));
    }
    function updateSystem(key, value) {
      setForm((current) => ({ ...current, systemAi: { ...current.systemAi, [key]: value, ...key === "apiKey" ? { source: "custom" } : {} } }));
      setErrors((current) => ({ ...current, [key]: "" }));
    }
    async function refreshModels() {
      setPending("models");
      setStatus("");
      try {
        const models2 = await repository.execute({ type: "models.refresh" });
        setSnapshot((current) => current ? { ...current, models: models2 } : current);
        setStatus("\u6A21\u578B\u5217\u8868\u5DF2\u5237\u65B0\u3002" + (models2.claudeVersion ? ` Claude ${models2.claudeVersion}` : ""));
        setTone("success");
      } catch (cause) {
        setStatus(messageOf(cause, "\u5237\u65B0\u6A21\u578B\u5217\u8868\u5931\u8D25\u3002"));
        setTone("error");
      } finally {
        setPending("");
      }
    }
    async function importSystemAi() {
      setPending("import");
      setStatus("");
      try {
        const result = await repository.execute({ type: "systemAi.import", source: form.commitCli });
        setForm((current) => ({ ...current, systemAi: { ...result.systemAi, apiKey: "" } }));
        setStatus(`\u5DF2\u4ECE ${form.commitCli} CLI \u5BFC\u5165\u5E76\u4FDD\u5B58 API \u914D\u7F6E\u3002`);
        setTone("success");
        await refresh();
      } catch (cause) {
        setStatus(messageOf(cause, "\u6CA1\u6709\u627E\u5230\u53EF\u5BFC\u5165\u7684 CLI API \u914D\u7F6E\u3002"));
        setTone("error");
      } finally {
        setPending("");
      }
    }
    async function save() {
      const systemRequired = form.systemAi.enabled || form.commitAiSource === "api";
      const nextErrors = {};
      if (systemRequired) {
        if (!form.systemAi.baseUrl.trim()) nextErrors.baseUrl = "\u8BF7\u8F93\u5165 API \u5730\u5740\u3002";
        else {
          try {
            const url = new URL(form.systemAi.baseUrl);
            if (url.protocol !== "http:" && url.protocol !== "https:") nextErrors.baseUrl = "API \u5730\u5740\u5FC5\u987B\u4F7F\u7528 http(s)\u3002";
          } catch {
            nextErrors.baseUrl = "\u8BF7\u8F93\u5165\u6709\u6548\u7684 API \u5730\u5740\u3002";
          }
        }
        if (!form.systemAi.model.trim()) nextErrors.model = "\u8BF7\u8F93\u5165\u7CFB\u7EDF AI \u6A21\u578B\u3002";
        if (!form.systemAi.apiKey.trim() && !form.systemAi.hasApiKey) nextErrors.apiKey = "\u8BF7\u8F93\u5165 API Key\u3002";
      }
      setErrors(nextErrors);
      if (Object.keys(nextErrors).length) {
        setStatus("\u76F4\u8FDE API \u914D\u7F6E\u5C1A\u672A\u5B8C\u6574\u3002");
        setTone("error");
        return;
      }
      setPending("save");
      setStatus("");
      try {
        const result = await repository.execute({ type: "ai.save", value: form });
        setStatus(result.restartRequired ? "AI \u914D\u7F6E\u5DF2\u4FDD\u5B58\uFF1B\u90E8\u5206\u90E8\u7F72\u53D8\u5316\u7B49\u5F85\u91CD\u542F\u3002" : "AI \u4E0E\u6A21\u578B\u914D\u7F6E\u5DF2\u4FDD\u5B58\u3002");
        setTone(result.restartRequired ? "warning" : "success");
        await refresh();
        toast("AI \u4E0E\u6A21\u578B\u914D\u7F6E\u5DF2\u4FDD\u5B58", "success");
      } catch (cause) {
        setStatus(messageOf(cause, "\u4FDD\u5B58 AI \u914D\u7F6E\u5931\u8D25\u3002"));
        setTone("error");
      } finally {
        setPending("");
      }
    }
    const models = snapshot7.models;
    const commitModels = form.commitCli === "codex" ? models?.codexModels || [] : form.commitCli === "opencode" ? models?.opencodeModels || [] : models?.models || [];
    return /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("section", { className: "wand-settings-panel", "aria-label": "AI \u4E0E\u6A21\u578B", children: [
      /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("header", { className: "wand-settings-panel-heading", children: [
        /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("h2", { children: "AI \u4E0E\u6A21\u578B" }),
        /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("p", { children: "\u96C6\u4E2D\u7BA1\u7406\u4F1A\u8BDD\u9ED8\u8BA4\u6A21\u578B\u3001\u7CFB\u7EDF API \u548C\u5FEB\u6377\u63D0\u4EA4\u4F7F\u7528\u7684\u6A21\u578B\u3002" })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(
        SettingsSection,
        {
          title: "\u65B0\u4F1A\u8BDD\u9ED8\u8BA4\u6A21\u578B",
          description: "\u7559\u7A7A\u8868\u793A\u8DDF\u968F\u5BF9\u5E94 CLI \u9ED8\u8BA4\u503C\uFF1B\u4E5F\u53EF\u4EE5\u8F93\u5165\u5217\u8868\u5916\u7684\u81EA\u5B9A\u4E49\u6A21\u578B\u3002",
          action: /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsActionButton, { pending: pending === "models", kind: "secondary", onClick: () => void refreshModels(), children: "\u5237\u65B0\u6A21\u578B\u5217\u8868" }),
          children: /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)(SettingsGrid, { children: [
            /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)(SettingsField, { label: "Claude \u9ED8\u8BA4\u6A21\u578B", htmlFor: "settings-model-claude", hint: "\u4F1A\u539F\u6837\u4F20\u7ED9 --model", children: [
              /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsTextInput, { id: "settings-model-claude", list: "settings-models-claude", value: form.defaultModel, placeholder: "\u8DDF\u968F Claude \u9ED8\u8BA4", onChange: (value) => update("defaultModel", value) }),
              /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(ModelSuggestions, { id: "settings-models-claude", models: models?.models || [] })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)(SettingsField, { label: "Codex \u9ED8\u8BA4\u6A21\u578B", htmlFor: "settings-model-codex", hint: "\u7559\u7A7A\u5219\u4E0D\u4F20\u6A21\u578B\u53C2\u6570", children: [
              /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsTextInput, { id: "settings-model-codex", list: "settings-models-codex", value: form.defaultCodexModel, placeholder: "\u8DDF\u968F Codex \u9ED8\u8BA4", onChange: (value) => update("defaultCodexModel", value) }),
              /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(ModelSuggestions, { id: "settings-models-codex", models: models?.codexModels || [] })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)(SettingsField, { label: "OpenCode \u9ED8\u8BA4\u6A21\u578B", htmlFor: "settings-model-opencode", hint: "\u901A\u5E38\u4E3A provider/model", children: [
              /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsTextInput, { id: "settings-model-opencode", list: "settings-models-opencode", value: form.defaultOpenCodeModel, placeholder: "\u8DDF\u968F OpenCode \u9ED8\u8BA4", onChange: (value) => update("defaultOpenCodeModel", value) }),
              /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(ModelSuggestions, { id: "settings-models-opencode", models: models?.opencodeModels || [] })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)(SettingsField, { label: "Grok \u9ED8\u8BA4\u6A21\u578B", htmlFor: "settings-model-grok", hint: "\u7559\u7A7A\u5219\u4E0D\u4F20 --model", children: [
              /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsTextInput, { id: "settings-model-grok", list: "settings-models-grok", value: form.defaultGrokModel, placeholder: "\u8DDF\u968F Grok \u9ED8\u8BA4", onChange: (value) => update("defaultGrokModel", value) }),
              /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(ModelSuggestions, { id: "settings-models-grok", models: models?.grokModels || [] })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)(SettingsField, { label: "Qoder \u9ED8\u8BA4\u6A21\u578B", htmlFor: "settings-model-qoder", hint: "\u53EF\u9009 lite / efficient / auto / performance / ultimate", children: [
              /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsTextInput, { id: "settings-model-qoder", list: "settings-models-qoder", value: form.defaultQoderModel, placeholder: "\u8DDF\u968F Qoder \u9ED8\u8BA4", onChange: (value) => update("defaultQoderModel", value) }),
              /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(ModelSuggestions, { id: "settings-models-qoder", models: models?.qoderModels || [] })
            ] })
          ] })
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)(
        SettingsSection,
        {
          title: "\u7CFB\u7EDF AI API",
          description: "\u7528\u4E8E\u63D0\u793A\u8BCD\u4F18\u5316\u3001\u4F1A\u8BDD\u6807\u9898\uFF0C\u4EE5\u53CA\u53EF\u9009\u7684\u5FEB\u6377\u63D0\u4EA4\u3002API Key \u4E0D\u4F1A\u4ECE\u670D\u52A1\u7AEF\u56DE\u4F20\u3002",
          action: /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsActionButton, { pending: pending === "import", kind: "secondary", onClick: () => void importSystemAi(), children: "\u4ECE CLI \u5BFC\u5165" }),
          children: [
            /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(
              SettingsToggle,
              {
                label: "\u7528\u4E8E\u7CFB\u7EDF AI \u529F\u80FD",
                description: "\u542F\u7528\u63D0\u793A\u8BCD\u4F18\u5316\u548C\u4F1A\u8BDD\u6807\u9898\u751F\u6210\u3002",
                checked: form.systemAi.enabled,
                onCheckedChange: (checked) => updateSystem("enabled", checked)
              }
            ),
            /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)(SettingsGrid, { children: [
              /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsField, { label: "\u63A5\u53E3\u683C\u5F0F", children: /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(
                SettingsSelect,
                {
                  id: "settings-system-ai-protocol",
                  ariaLabel: "\u7CFB\u7EDF AI \u63A5\u53E3\u683C\u5F0F",
                  value: form.systemAi.protocol,
                  options: [{ value: "openai", label: "OpenAI-compatible" }, { value: "anthropic", label: "Anthropic-compatible" }],
                  onChange: (value) => updateSystem("protocol", value)
                }
              ) }),
              /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsField, { label: "\u8BA4\u8BC1\u65B9\u5F0F", children: /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(
                SettingsSelect,
                {
                  id: "settings-system-ai-auth",
                  ariaLabel: "\u7CFB\u7EDF AI \u8BA4\u8BC1\u65B9\u5F0F",
                  value: form.systemAi.authHeader,
                  options: [{ value: "bearer", label: "Bearer Token" }, { value: "x-api-key", label: "x-api-key" }],
                  onChange: (value) => updateSystem("authHeader", value)
                }
              ) }),
              /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsField, { label: "API \u5730\u5740", htmlFor: "settings-system-ai-url", error: errors.baseUrl, children: /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsTextInput, { id: "settings-system-ai-url", type: "url", value: form.systemAi.baseUrl, invalid: !!errors.baseUrl, placeholder: "https://api.example.com", onChange: (value) => updateSystem("baseUrl", value) }) }),
              /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsField, { label: "\u6A21\u578B", htmlFor: "settings-system-ai-model", error: errors.model, children: /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsTextInput, { id: "settings-system-ai-model", value: form.systemAi.model, invalid: !!errors.model, placeholder: "\u4F8B\u5982 gpt-5.5", onChange: (value) => updateSystem("model", value) }) }),
              /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsField, { label: "API Key", htmlFor: "settings-system-ai-key", error: errors.apiKey, hint: form.systemAi.hasApiKey ? "\u5DF2\u4FDD\u5B58\uFF1B\u7559\u7A7A\u4F1A\u4FDD\u7559\u73B0\u6709\u5BC6\u94A5\u3002" : "\u4EC5\u4FDD\u5B58\u5728\u670D\u52A1\u7AEF\u3002", children: /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsTextInput, { id: "settings-system-ai-key", type: "password", autoComplete: "new-password", value: form.systemAi.apiKey, invalid: !!errors.apiKey, placeholder: form.systemAi.hasApiKey ? "\u5DF2\u4FDD\u5B58\uFF1B\u7559\u7A7A\u4FDD\u6301\u4E0D\u53D8" : "\u8F93\u5165 API Key", onChange: (value) => updateSystem("apiKey", value) }) })
            ] })
          ]
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)(SettingsSection, { title: "Commit \u751F\u6210", description: "\u9009\u62E9\u5FEB\u6377\u63D0\u4EA4\u751F\u6210 message \u4E0E tag \u65F6\u4F7F\u7528\u7684 AI \u6765\u6E90\u3002", children: [
        /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("fieldset", { className: "wand-settings-radio-group", children: [
          /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("legend", { children: "\u751F\u6210\u65B9\u5F0F" }),
          /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("label", { children: [
            /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("input", { type: "radio", name: "settings-commit-source", value: "cli", checked: form.commitAiSource === "cli", onChange: () => update("commitAiSource", "cli") }),
            "CLI"
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("label", { children: [
            /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("input", { type: "radio", name: "settings-commit-source", value: "api", checked: form.commitAiSource === "api", onChange: () => update("commitAiSource", "api") }),
            "\u76F4\u8FDE API"
          ] })
        ] }),
        form.commitAiSource === "api" ? /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsStatus, { tone: form.systemAi.baseUrl && form.systemAi.model && (form.systemAi.apiKey || form.systemAi.hasApiKey) ? "success" : "warning", children: form.systemAi.baseUrl && form.systemAi.model && (form.systemAi.apiKey || form.systemAi.hasApiKey) ? "\u76F4\u8FDE API \u5DF2\u5C31\u7EEA\u3002" : "\u8BF7\u5148\u8865\u5168\u4E0A\u65B9\u76F4\u8FDE API \u914D\u7F6E\u3002" }) : /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)(SettingsGrid, { children: [
          /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsField, { label: "Commit CLI", children: /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(
            SettingsSelect,
            {
              id: "settings-commit-cli",
              ariaLabel: "Commit \u751F\u6210 CLI",
              value: form.commitCli,
              options: [{ value: "claude", label: "Claude" }, { value: "codex", label: "Codex" }, { value: "opencode", label: "OpenCode" }],
              onChange: (value) => {
                update("commitCli", value);
                update("commitModel", "");
              }
            }
          ) }),
          /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)(SettingsField, { label: "Commit \u6A21\u578B", htmlFor: "settings-commit-model", hint: "\u7559\u7A7A\u8DDF\u968F\u6240\u9009 CLI \u9ED8\u8BA4\u503C", children: [
            /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsTextInput, { id: "settings-commit-model", list: "settings-commit-models", value: form.commitModel, placeholder: "\u8DDF\u968F CLI \u9ED8\u8BA4", onChange: (value) => update("commitModel", value) }),
            /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(ModelSuggestions, { id: "settings-commit-models", models: commitModels })
          ] })
        ] })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsSaveBar, { label: "\u4FDD\u5B58 AI \u4E0E\u6A21\u578B\u914D\u7F6E", pending: pending === "save", disabled: !!pending && pending !== "save", onSave: () => void save(), status, tone })
    ] });
  }
  function NotificationSettingsTab(_props) {
    const { snapshot: snapshot7, repository, setSnapshot, toast } = _props;
    const [preferences, setPreferences] = (0, import_react7.useState)(() => ({ ...snapshot7.notifications }));
    const [pending, setPending] = (0, import_react7.useState)("");
    const [status, setStatus] = (0, import_react7.useState)("");
    const [tone, setTone] = (0, import_react7.useState)("info");
    (0, import_react7.useEffect)(() => setPreferences({ ...snapshot7.notifications }), [snapshot7.notifications]);
    async function savePreference(value, preview = false) {
      setPending("preference");
      setStatus("");
      try {
        const next = await repository.execute({ type: "notification.preferences.set", value });
        setPreferences(next);
        setSnapshot((current) => current ? { ...current, notifications: next } : current);
        if (preview) await repository.execute({ type: "notification.sound.preview" });
      } catch (cause) {
        setStatus(messageOf(cause, "\u4FDD\u5B58\u901A\u77E5\u504F\u597D\u5931\u8D25\u3002"));
        setTone("error");
      } finally {
        setPending("");
      }
    }
    async function run2(name, task) {
      setPending(name);
      setStatus("");
      try {
        setStatus(await task());
        setTone("success");
      } catch (cause) {
        setStatus(messageOf(cause, "\u901A\u77E5\u64CD\u4F5C\u5931\u8D25\u3002"));
        setTone("error");
      } finally {
        setPending("");
      }
    }
    const permissionLabel = preferences.permission === "granted" ? "\u5DF2\u6388\u6743" : preferences.permission === "denied" ? "\u5DF2\u62D2\u7EDD" : preferences.permission === "unsupported" ? "\u5F53\u524D\u73AF\u5883\u4E0D\u652F\u6301" : "\u5C1A\u672A\u6388\u6743";
    return /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("section", { className: "wand-settings-panel", "aria-label": "\u901A\u77E5", children: [
      /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("header", { className: "wand-settings-panel-heading", children: [
        /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("h2", { children: "\u901A\u77E5" }),
        /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("p", { children: "\u8BBE\u7F6E\u63D0\u793A\u97F3\u3001\u5E94\u7528\u5185\u6C14\u6CE1\u548C\u7CFB\u7EDF\u901A\u77E5\u7684\u884C\u4E3A\u3002" })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)(SettingsSection, { title: "\u901A\u77E5\u504F\u597D", description: "\u8FD9\u4E9B\u504F\u597D\u4F1A\u7ACB\u5373\u5E94\u7528\uFF0C\u65E0\u9700\u70B9\u51FB\u4FDD\u5B58\u3002", children: [
        /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(
          SettingsToggle,
          {
            label: "\u64AD\u653E\u63D0\u793A\u97F3",
            description: "\u91CD\u8981\u901A\u77E5\u5230\u8FBE\u65F6\u64AD\u653E\u67D4\u548C\u63D0\u793A\u97F3\u3002",
            checked: preferences.sound,
            disabled: pending === "preference",
            onCheckedChange: (sound) => void savePreference({ sound }, sound)
          }
        ),
        preferences.sound ? /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsField, { label: `\u63D0\u793A\u97F3\u97F3\u91CF\uFF08${preferences.volume}%\uFF09`, htmlFor: "settings-notification-volume", children: /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(
          "input",
          {
            id: "settings-notification-volume",
            className: "wand-settings-range",
            type: "range",
            min: "0",
            max: "100",
            step: "5",
            value: preferences.volume,
            "aria-label": "\u63D0\u793A\u97F3\u97F3\u91CF",
            onChange: (event) => {
              const volume = Number(event.currentTarget.value);
              setPreferences((current) => ({ ...current, volume }));
            },
            onPointerUp: (event) => void savePreference({ volume: Number(event.currentTarget.value) }, true),
            onKeyUp: (event) => {
              if (["ArrowLeft", "ArrowRight", "Home", "End"].includes(event.key)) void savePreference({ volume: Number(event.currentTarget.value) }, true);
            }
          }
        ) }) : null,
        /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(
          SettingsToggle,
          {
            label: "\u5E94\u7528\u5185\u901A\u77E5\u6C14\u6CE1",
            description: "\u5728\u9875\u9762\u9876\u90E8\u663E\u793A\u6D6E\u52A8\u901A\u77E5\u3002",
            checked: preferences.bubble,
            disabled: pending === "preference",
            onCheckedChange: (bubble) => void savePreference({ bubble })
          }
        )
      ] }),
      preferences.nativeSounds.length ? /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)(SettingsSection, { title: "\u7CFB\u7EDF\u901A\u77E5\u94C3\u58F0", description: "\u9009\u62E9\u539F\u751F\u5BA2\u6237\u7AEF\u53D1\u9001\u7CFB\u7EDF\u901A\u77E5\u65F6\u4F7F\u7528\u7684\u94C3\u58F0\u3002", children: [
        /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsField, { label: "\u901A\u77E5\u94C3\u58F0", children: /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(
          SettingsSelect,
          {
            id: "settings-native-sound",
            ariaLabel: "\u7CFB\u7EDF\u901A\u77E5\u94C3\u58F0",
            value: preferences.nativeSound || preferences.nativeSounds[0].id,
            options: preferences.nativeSounds.map((sound) => ({ value: sound.id, label: sound.name })),
            onChange: (sound) => void run2("native-sound", async () => {
              await repository.execute({ type: "notification.nativeSound.set", sound });
              setPreferences((current) => ({ ...current, nativeSound: sound }));
              return "\u901A\u77E5\u94C3\u58F0\u5DF2\u4FDD\u5B58\u3002";
            })
          }
        ) }),
        /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsActionButton, { pending: pending === "sound-preview", kind: "secondary", onClick: () => void run2("sound-preview", async () => {
          await repository.execute({ type: "notification.nativeSound.preview", sound: preferences.nativeSound || preferences.nativeSounds[0].id });
          return "\u5DF2\u64AD\u653E\u94C3\u58F0\u9884\u89C8\u3002";
        }), children: "\u8BD5\u542C\u94C3\u58F0" })
      ] }) : null,
      preferences.hapticsEnabled !== null ? /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsSection, { title: "\u89E6\u611F\u53CD\u9988", description: "\u6309\u94AE\u64CD\u4F5C\u548C\u4EFB\u52A1\u5B8C\u6210\u65F6\u63D0\u4F9B\u632F\u52A8\u53CD\u9988\u3002", children: /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(
        SettingsToggle,
        {
          label: "\u542F\u7528\u89E6\u611F\u53CD\u9988",
          checked: preferences.hapticsEnabled,
          disabled: pending === "haptics",
          onCheckedChange: (enabled) => void run2("haptics", async () => {
            await repository.execute({ type: "notification.haptics.set", enabled });
            setPreferences((current) => ({ ...current, hapticsEnabled: enabled }));
            return enabled ? "\u89E6\u611F\u53CD\u9988\u5DF2\u542F\u7528\u3002" : "\u89E6\u611F\u53CD\u9988\u5DF2\u5173\u95ED\u3002";
          })
        }
      ) }) : null,
      /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsSection, { title: "\u7CFB\u7EDF\u901A\u77E5", description: `\u6388\u6743\u72B6\u6001\uFF1A${permissionLabel}`, children: /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("div", { className: "wand-settings-button-row", children: [
        preferences.permission !== "granted" && preferences.permission !== "unsupported" ? /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsActionButton, { pending: pending === "permission", kind: "primary", onClick: () => void run2("permission", async () => {
          const result = await repository.execute({ type: "notification.permission.request" });
          setPreferences((current) => ({ ...current, permission: result.permission }));
          return result.permission === "granted" ? "\u7CFB\u7EDF\u901A\u77E5\u5DF2\u6388\u6743\u3002" : "\u7CFB\u7EDF\u901A\u77E5\u5C1A\u672A\u6388\u6743\u3002";
        }), children: "\u8BF7\u6C42\u901A\u77E5\u6743\u9650" }) : null,
        preferences.permission === "denied" ? /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(WandButton, { kind: "secondary", onClick: () => {
          setStatus("\u8BF7\u5728\u6D4F\u89C8\u5668\u5730\u5740\u680F\u5DE6\u4FA7\u7684\u7F51\u7AD9\u8BBE\u7F6E\u6216\u7CFB\u7EDF\u8BBE\u7F6E\u4E2D\uFF0C\u5C06\u901A\u77E5\u6539\u4E3A\u5141\u8BB8\u540E\u5237\u65B0\u9875\u9762\u3002");
          setTone("warning");
        }, children: "\u5982\u4F55\u91CD\u7F6E\u6743\u9650" }) : null,
        /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsActionButton, { pending: pending === "test", kind: "secondary", onClick: () => void run2("test", async () => {
          const result = await repository.execute({ type: "notification.test" });
          toast("\u6D4B\u8BD5\u901A\u77E5", "info");
          return `\u63D0\u793A\u97F3\uFF1A${result.sound === "passed" ? "\u901A\u8FC7" : "\u5931\u8D25"}\uFF1B\u6C14\u6CE1\uFF1A${result.bubble === "passed" ? "\u901A\u8FC7" : "\u5DF2\u5173\u95ED"}\uFF1B\u7CFB\u7EDF\uFF1A${result.system}`;
        }), children: "\u7ACB\u5373\u6D4B\u8BD5" }),
        /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsActionButton, { pending: pending === "delayed-test", kind: "secondary", onClick: () => void run2("delayed-test", async () => {
          await repository.execute({ type: "notification.test", delayMs: 1e4 });
          return "10 \u79D2\u5EF6\u8FDF\u901A\u77E5\u5DF2\u53D1\u9001\u3002";
        }), children: "10 \u79D2\u540E\u53D1\u9001" })
      ] }) }),
      status ? /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsStatus, { tone, children: status }) : null
    ] });
  }
  function SecuritySettingsTab(_props) {
    const { snapshot: snapshot7, repository, refresh, toast } = _props;
    const [password, setPassword] = (0, import_react7.useState)("");
    const [confirmation, setConfirmation] = (0, import_react7.useState)("");
    const [keyFile, setKeyFile] = (0, import_react7.useState)(null);
    const [certFile, setCertFile] = (0, import_react7.useState)(null);
    const [pending, setPending] = (0, import_react7.useState)("");
    const [status, setStatus] = (0, import_react7.useState)("");
    const [tone, setTone] = (0, import_react7.useState)("info");
    const [passwordError, setPasswordError] = (0, import_react7.useState)("");
    async function changePassword() {
      setPasswordError("");
      if (password.length < 6) {
        setPasswordError("\u5BC6\u7801\u957F\u5EA6\u81F3\u5C11\u4E3A 6 \u4E2A\u5B57\u7B26\u3002");
        return;
      }
      if (password !== confirmation) {
        setPasswordError("\u4E24\u6B21\u8F93\u5165\u7684\u5BC6\u7801\u4E0D\u4E00\u81F4\u3002");
        return;
      }
      setPending("password");
      setStatus("");
      try {
        const result = await repository.execute({ type: "password.change", password });
        setPassword("");
        setConfirmation("");
        setStatus("\u5BC6\u7801\u4FEE\u6539\u6210\u529F\uFF1B\u6240\u6709\u65E7\u767B\u5F55\u4F1A\u8BDD\u5DF2\u5931\u6548\uFF0C\u6B63\u5728\u8FD4\u56DE\u767B\u5F55\u9875\u2026");
        setTone("success");
        toast("\u5BC6\u7801\u5DF2\u4FEE\u6539\uFF0C\u8BF7\u91CD\u65B0\u767B\u5F55", "success");
        if (result.reauthenticationRequired) {
          window.setTimeout(() => {
            settingsStore.setNested(null);
            window.location.reload();
          }, 650);
        }
      } catch (cause) {
        setStatus(messageOf(cause, "\u4FEE\u6539\u5BC6\u7801\u5931\u8D25\u3002"));
        setTone("error");
      } finally {
        setPending("");
      }
    }
    async function uploadCertificate() {
      if (!keyFile || !certFile) {
        setStatus("\u8BF7\u9009\u62E9\u79C1\u94A5\u548C\u8BC1\u4E66\u6587\u4EF6\u3002");
        setTone("error");
        return;
      }
      setPending("certificate");
      setStatus("");
      try {
        const [key, cert] = await Promise.all([keyFile.text(), certFile.text()]);
        const result = await repository.execute({ type: "certificate.upload", key, cert });
        setStatus(result.restartRequired ? "\u8BC1\u4E66\u5DF2\u4E0A\u4F20\uFF0C\u91CD\u542F\u670D\u52A1\u540E\u751F\u6548\u3002" : "\u8BC1\u4E66\u5DF2\u4E0A\u4F20\u3002 ");
        setTone("success");
        await refresh();
        toast("SSL \u8BC1\u4E66\u5DF2\u4E0A\u4F20", "success");
      } catch (cause) {
        setStatus(messageOf(cause, "\u4E0A\u4F20\u8BC1\u4E66\u5931\u8D25\u3002"));
        setTone("error");
      } finally {
        setPending("");
      }
    }
    return /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("section", { className: "wand-settings-panel", "aria-label": "\u5B89\u5168", children: [
      /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("header", { className: "wand-settings-panel-heading", children: [
        /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("h2", { children: "\u5B89\u5168" }),
        /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("p", { children: "\u7BA1\u7406\u767B\u5F55\u5BC6\u7801\u4E0E SSL \u8BC1\u4E66\u3002\u654F\u611F\u53D8\u66F4\u4FDD\u5B58\u524D\u8BF7\u4ED4\u7EC6\u786E\u8BA4\u3002" })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsSection, { title: "\u4FEE\u6539\u5BC6\u7801", description: "\u81F3\u5C11 6 \u4E2A\u5B57\u7B26\uFF1B\u4FDD\u5B58\u540E\u4F1A\u64A4\u9500\u5305\u62EC\u5F53\u524D\u9875\u9762\u5728\u5185\u7684\u6240\u6709\u767B\u5F55\u4F1A\u8BDD\u3002", children: /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("form", { className: "wand-settings-security-form", onSubmit: (event) => {
        event.preventDefault();
        void changePassword();
      }, children: [
        /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("input", { type: "text", name: "username", autoComplete: "username", value: "wand", readOnly: true, hidden: true }),
        /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)(SettingsGrid, { children: [
          /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsField, { label: "\u65B0\u5BC6\u7801", htmlFor: "settings-new-password", error: passwordError, children: /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsTextInput, { id: "settings-new-password", type: "password", autoComplete: "new-password", value: password, invalid: !!passwordError, placeholder: "\u8F93\u5165\u65B0\u5BC6\u7801", onChange: (value) => {
            setPassword(value);
            setPasswordError("");
          } }) }),
          /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsField, { label: "\u786E\u8BA4\u5BC6\u7801", htmlFor: "settings-confirm-password", children: /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsTextInput, { id: "settings-confirm-password", type: "password", autoComplete: "new-password", value: confirmation, invalid: !!passwordError, placeholder: "\u518D\u6B21\u8F93\u5165\u65B0\u5BC6\u7801", onChange: (value) => {
            setConfirmation(value);
            setPasswordError("");
          } }) })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsActionButton, { type: "submit", pending: pending === "password", kind: "primary", children: "\u4FEE\u6539\u5BC6\u7801\u5E76\u91CD\u65B0\u767B\u5F55" })
      ] }) }),
      /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)(SettingsSection, { title: "SSL \u8BC1\u4E66", description: `\u5F53\u524D\u72B6\u6001\uFF1A${snapshot7.hasCert ? "\u5DF2\u5B89\u88C5\u8BC1\u4E66" : "\u672A\u5B89\u88C5\u8BC1\u4E66\uFF08\u4F7F\u7528\u81EA\u7B7E\u540D\u6216 HTTP\uFF09"}`, children: [
        /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("div", { className: "wand-settings-file-grid", children: [
          /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("label", { children: [
            /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("span", { children: "\u79C1\u94A5\u6587\u4EF6\uFF08server.key\uFF09" }),
            /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("input", { "aria-label": "SSL \u79C1\u94A5\u6587\u4EF6", type: "file", accept: ".key,.pem,text/plain", onChange: (event) => setKeyFile(event.currentTarget.files?.[0] || null) }),
            /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("small", { children: keyFile?.name || "\u672A\u9009\u62E9\u6587\u4EF6" })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("label", { children: [
            /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("span", { children: "\u8BC1\u4E66\u6587\u4EF6\uFF08server.crt\uFF09" }),
            /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("input", { "aria-label": "SSL \u8BC1\u4E66\u6587\u4EF6", type: "file", accept: ".crt,.pem,text/plain", onChange: (event) => setCertFile(event.currentTarget.files?.[0] || null) }),
            /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("small", { children: certFile?.name || "\u672A\u9009\u62E9\u6587\u4EF6" })
          ] })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsActionButton, { pending: pending === "certificate", kind: "primary", onClick: () => void uploadCertificate(), children: "\u4E0A\u4F20\u8BC1\u4E66" })
      ] }),
      status ? /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsStatus, { tone, children: status }) : null
    ] });
  }
  function PresetSettingsTab({ snapshot: snapshot7 }) {
    const presets = snapshot7.config?.commandPresets || [];
    return /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("section", { className: "wand-settings-panel", "aria-label": "\u547D\u4EE4\u9884\u8BBE", children: [
      /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("header", { className: "wand-settings-panel-heading", children: [
        /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("h2", { children: "\u547D\u4EE4\u9884\u8BBE" }),
        /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("p", { children: "\u9884\u8BBE\u7531\u670D\u52A1\u7AEF\u914D\u7F6E\u7BA1\u7406\uFF0C\u53EF\u5728\u521B\u5EFA\u4F1A\u8BDD\u65F6\u5FEB\u901F\u9009\u62E9\u3002" })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("div", { className: "wand-settings-preset-list", "aria-label": "\u5DF2\u6709\u547D\u4EE4\u9884\u8BBE", children: [
        presets.map((preset, index2) => /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("article", { className: "wand-settings-preset", children: [
          /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("strong", { children: preset.label || "\u672A\u547D\u540D\u9884\u8BBE" }),
          /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("code", { children: preset.command }),
          preset.mode ? /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("span", { children: [
            "\u6A21\u5F0F\uFF1A",
            preset.mode
          ] }) : null
        ] }, `${preset.label}-${index2}`)),
        presets.length === 0 ? /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("div", { className: "wand-settings-empty", children: "\u6CA1\u6709\u547D\u4EE4\u9884\u8BBE\uFF1B\u53EF\u5728 config.json \u7684 commandPresets \u4E2D\u914D\u7F6E\u3002" }) : null
      ] })
    ] });
  }
  var CARD_OPTIONS = [
    { key: "editCards", title: "\u6587\u4EF6\u7F16\u8F91", description: "Edit / Write \u5DE5\u5177\u7ED3\u679C" },
    { key: "inlineTools", title: "\u5185\u8054\u5DE5\u5177", description: "Read / Glob / Grep \u5DE5\u5177\u7ED3\u679C" },
    { key: "terminal", title: "\u7EC8\u7AEF\u8F93\u51FA", description: "Bash \u547D\u4EE4\u6267\u884C\u7ED3\u679C" },
    { key: "thinking", title: "\u601D\u8003\u8FC7\u7A0B", description: "\u6A21\u578B\u7684 Thinking \u5185\u5BB9" },
    { key: "toolGroup", title: "\u5DE5\u5177\u7EC4", description: "\u8FDE\u7EED\u540C\u7C7B\u5DE5\u5177\u8C03\u7528\u7684\u6298\u53E0\u7EC4" }
  ];
  function DisplaySettingsTab({ snapshot: snapshot7, repository, refresh, toast }) {
    const [value, setValue] = (0, import_react7.useState)(() => ({ ...snapshot7.config.cardDefaults }));
    const [pending, setPending] = (0, import_react7.useState)(false);
    const [status, setStatus] = (0, import_react7.useState)("");
    const [tone, setTone] = (0, import_react7.useState)("info");
    (0, import_react7.useEffect)(() => setValue({ ...snapshot7.config.cardDefaults }), [snapshot7]);
    async function save() {
      setPending(true);
      setStatus("");
      try {
        await repository.execute({ type: "display.save", value });
        setStatus("\u663E\u793A\u8BBE\u7F6E\u5DF2\u4FDD\u5B58\uFF0C\u5E76\u4F1A\u7ACB\u5373\u5E94\u7528\u4E8E\u4E4B\u540E\u6E32\u67D3\u7684\u5361\u7247\u3002");
        setTone("success");
        await refresh();
        toast("\u663E\u793A\u8BBE\u7F6E\u5DF2\u4FDD\u5B58", "success");
      } catch (cause) {
        setStatus(messageOf(cause, "\u4FDD\u5B58\u663E\u793A\u8BBE\u7F6E\u5931\u8D25\u3002"));
        setTone("error");
      } finally {
        setPending(false);
      }
    }
    return /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("section", { className: "wand-settings-panel", "aria-label": "\u663E\u793A", children: [
      /* @__PURE__ */ (0, import_jsx_runtime28.jsxs)("header", { className: "wand-settings-panel-heading", children: [
        /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("h2", { children: "\u663E\u793A" }),
        /* @__PURE__ */ (0, import_jsx_runtime28.jsx)("p", { children: "\u8BBE\u7F6E\u5404\u7C7B\u7ED3\u679C\u5361\u7247\u7684\u9ED8\u8BA4\u5C55\u5F00\u72B6\u6001\u3002" })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsSection, { title: "\u9ED8\u8BA4\u5C55\u5F00\u7684\u5361\u7247", description: "\u624B\u52A8\u5C55\u5F00\u6216\u6536\u8D77\u4ECD\u4F1A\u6309\u4F1A\u8BDD\u8BB0\u5F55\u3002", children: CARD_OPTIONS.map((option) => /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(
        SettingsToggle,
        {
          label: option.title,
          description: option.description,
          checked: value[option.key],
          onCheckedChange: (checked) => setValue((current) => ({ ...current, [option.key]: checked }))
        },
        option.key
      )) }),
      /* @__PURE__ */ (0, import_jsx_runtime28.jsx)(SettingsSaveBar, { label: "\u4FDD\u5B58\u663E\u793A\u8BBE\u7F6E", pending, onSave: () => void save(), status, tone })
    ] });
  }

  // src/web-ui/react/settings/host.tsx
  var import_jsx_runtime29 = __toESM(require_jsx_runtime(), 1);
  var TAB_LABELS = {
    about: { title: "\u5173\u4E8E", description: "\u7248\u672C\u3001\u66F4\u65B0\u4E0E\u8FDE\u63A5\u65B9\u5F0F" },
    general: { title: "\u57FA\u672C\u914D\u7F6E", description: "\u8FDE\u63A5\u3001\u6A21\u5F0F\u4E0E\u8FD0\u884C\u73AF\u5883" },
    ai: { title: "AI \u4E0E\u6A21\u578B", description: "\u9ED8\u8BA4\u6A21\u578B\u3001\u7CFB\u7EDF API \u4E0E Commit" },
    notifications: { title: "\u901A\u77E5", description: "\u63D0\u793A\u97F3\u4E0E\u7CFB\u7EDF\u901A\u77E5" },
    security: { title: "\u5B89\u5168", description: "\u5BC6\u7801\u4E0E\u8BC1\u4E66" },
    presets: { title: "\u547D\u4EE4\u9884\u8BBE", description: "\u67E5\u770B\u5DF2\u6709\u9884\u8BBE" },
    display: { title: "\u663E\u793A", description: "\u5361\u7247\u9ED8\u8BA4\u5C55\u5F00\u884C\u4E3A" }
  };
  function SettingsHost({
    repository = httpSettingsRepository,
    showRestart: showRestart2 = () => {
    }
  }) {
    const controller = (0, import_react9.useSyncExternalStore)(
      settingsStore.subscribe,
      settingsStore.getSnapshot,
      settingsStore.getSnapshot
    );
    const [snapshot7, setSnapshot] = (0, import_react8.useState)(null);
    const [loading, setLoading] = (0, import_react8.useState)(false);
    const [loadError, setLoadError] = (0, import_react8.useState)("");
    const load = (0, import_react8.useCallback)(async (signal, quiet = false) => {
      if (!quiet) setLoading(true);
      setLoadError("");
      try {
        const next = await repository.load({ signal });
        if (!signal?.aborted) setSnapshot(next);
      } catch (error) {
        if (!signal?.aborted) setLoadError(error instanceof Error ? error.message : "\u8BBE\u7F6E\u52A0\u8F7D\u5931\u8D25\u3002");
      } finally {
        if (!signal?.aborted && !quiet) setLoading(false);
      }
    }, [repository]);
    (0, import_react8.useEffect)(() => {
      if (!controller.open) return;
      const abort = new AbortController();
      void load(abort.signal);
      return () => abort.abort();
    }, [controller.open, load]);
    const refresh = (0, import_react8.useCallback)(async () => load(void 0, true), [load]);
    const toast = (0, import_react8.useCallback)((message, tone = "info") => {
      wandOverlay.toast(message, { tone });
    }, []);
    const tabs = (0, import_react8.useMemo)(() => {
      if (!snapshot7) return [];
      const props = { snapshot: snapshot7, repository, refresh, setSnapshot, toast, showRestart: showRestart2 };
      const items = [
        { value: "about", content: /* @__PURE__ */ (0, import_jsx_runtime29.jsx)(AboutSettingsTab, { ...props }) },
        { value: "general", content: /* @__PURE__ */ (0, import_jsx_runtime29.jsx)(GeneralSettingsTab, { ...props }) },
        { value: "ai", content: /* @__PURE__ */ (0, import_jsx_runtime29.jsx)(AiSettingsTab, { ...props }) },
        { value: "notifications", content: /* @__PURE__ */ (0, import_jsx_runtime29.jsx)(NotificationSettingsTab, { ...props }) },
        { value: "security", content: /* @__PURE__ */ (0, import_jsx_runtime29.jsx)(SecuritySettingsTab, { ...props }) },
        { value: "presets", content: /* @__PURE__ */ (0, import_jsx_runtime29.jsx)(PresetSettingsTab, { ...props }) },
        { value: "display", content: /* @__PURE__ */ (0, import_jsx_runtime29.jsx)(DisplaySettingsTab, { ...props }) }
      ];
      return items.filter((item) => snapshot7.access === "admin" || item.value === "about").map((item) => ({
        value: item.value,
        label: /* @__PURE__ */ (0, import_jsx_runtime29.jsxs)("span", { className: "wand-settings-tab-label", children: [
          /* @__PURE__ */ (0, import_jsx_runtime29.jsx)("strong", { children: TAB_LABELS[item.value].title }),
          /* @__PURE__ */ (0, import_jsx_runtime29.jsx)("span", { children: TAB_LABELS[item.value].description })
        ] }),
        content: item.content
      }));
    }, [refresh, repository, showRestart2, snapshot7, toast]);
    return /* @__PURE__ */ (0, import_jsx_runtime29.jsx)(
      WandDialogSurface,
      {
        open: controller.open,
        onOpenChange: (open) => {
          if (!open) settingsController.close();
        },
        title: "\u8BBE\u7F6E",
        description: "\u8C03\u6574\u5E94\u7528\u914D\u7F6E\u3001\u901A\u77E5\u3001\u5B89\u5168\u548C\u663E\u793A\u504F\u597D",
        className: "wand-settings-dialog",
        overlayClassName: "wand-settings-overlay",
        titleClassName: "wand-settings-title",
        descriptionClassName: "wand-settings-description",
        headerClassName: "wand-settings-header",
        closeLabel: "\u5173\u95ED\u8BBE\u7F6E",
        testId: "settings-dialog",
        children: loading ? /* @__PURE__ */ (0, import_jsx_runtime29.jsx)("div", { className: "wand-settings-loading", role: "status", "aria-label": "\u6B63\u5728\u52A0\u8F7D\u8BBE\u7F6E", children: "\u6B63\u5728\u52A0\u8F7D\u8BBE\u7F6E\u2026" }) : loadError ? /* @__PURE__ */ (0, import_jsx_runtime29.jsxs)("div", { className: "wand-settings-load-error", role: "alert", children: [
          /* @__PURE__ */ (0, import_jsx_runtime29.jsx)("p", { children: loadError }),
          /* @__PURE__ */ (0, import_jsx_runtime29.jsx)(WandButton, { kind: "primary", onClick: () => void load(), children: "\u91CD\u8BD5\u52A0\u8F7D\u8BBE\u7F6E" })
        ] }) : snapshot7 ? /* @__PURE__ */ (0, import_jsx_runtime29.jsxs)(import_jsx_runtime29.Fragment, { children: [
          snapshot7.access === "read-only" ? /* @__PURE__ */ (0, import_jsx_runtime29.jsx)("div", { className: "wand-settings-readonly", role: "note", children: "\u5F53\u524D\u662F App \u8FDE\u63A5\u4F1A\u8BDD\uFF0C\u4EC5\u5C55\u793A\u7248\u672C\u4E0E\u5BA2\u6237\u7AEF\u4E0B\u8F7D\u4FE1\u606F\u3002\u7BA1\u7406\u8BBE\u7F6E\u9700\u8981\u7BA1\u7406\u5458\u767B\u5F55\u3002" }) : null,
          /* @__PURE__ */ (0, import_jsx_runtime29.jsx)(
            WandTabs,
            {
              className: "wand-settings-tabs",
              ariaLabel: "\u8BBE\u7F6E\u5206\u7EC4",
              value: snapshot7.access === "read-only" ? "about" : controller.tab,
              tabs,
              onValueChange: (value) => settingsStore.setTab(value)
            }
          )
        ] }) : null
      }
    );
  }

  // src/web-ui/react/new-session/host.tsx
  var import_react10 = __toESM(require_react(), 1);

  // src/web-ui/react/new-session/controller.ts
  var runtime = null;
  var snapshot3 = { open: false, dismissable: true, revision: 0 };
  var listeners3 = /* @__PURE__ */ new Set();
  function publish3(open) {
    snapshot3 = { open, dismissable: true, revision: snapshot3.revision + 1 };
    for (const listener of listeners3) listener();
  }
  function publishDismissable(dismissable) {
    if (!snapshot3.open || snapshot3.dismissable === dismissable) return;
    snapshot3 = { ...snapshot3, dismissable };
    for (const listener of listeners3) listener();
  }
  var newSessionController = {
    open() {
      if (!runtime) return false;
      runtime.onOpen();
      publish3(true);
      return true;
    },
    close() {
      if (!snapshot3.open) return;
      publish3(false);
      runtime?.onClose();
    },
    closeIfOpen() {
      if (!snapshot3.open || !snapshot3.dismissable) return false;
      this.close();
      return true;
    },
    closeTopmost() {
      if (!snapshot3.open) return false;
      if (snapshot3.dismissable) this.close();
      return true;
    },
    isOpen() {
      return snapshot3.open;
    },
    setDismissable(dismissable) {
      publishDismissable(dismissable);
    }
  };
  var newSessionStore = {
    subscribe(listener) {
      listeners3.add(listener);
      return () => listeners3.delete(listener);
    },
    getSnapshot() {
      return snapshot3;
    },
    getRuntime() {
      return runtime;
    }
  };
  function configureNewSessionRuntime(adapter) {
    runtime = adapter;
    return () => {
      if (runtime !== adapter) return;
      if (snapshot3.open) newSessionController.close();
      runtime = null;
    };
  }

  // src/web-ui/react/new-session/repository.ts
  var PROVIDERS = ["claude", "codex", "opencode", "grok", "qoder"];
  var KINDS = ["structured", "pty"];
  var MODES = [
    "default",
    "full-access",
    "auto-edit",
    "native",
    "managed"
  ];
  function isRecord(value) {
    return !!value && typeof value === "object" && !Array.isArray(value);
  }
  function oneOf(value, choices, fallback) {
    return typeof value === "string" && choices.includes(value) ? value : fallback;
  }
  function text(value, fallback = "") {
    return typeof value === "string" ? value : fallback;
  }
  async function readJson(response) {
    let data;
    try {
      data = await response.json();
    } catch {
      throw new Error(`\u8BF7\u6C42\u5931\u8D25 (HTTP ${response.status})`);
    }
    const record2 = isRecord(data) ? data : {};
    if (!response.ok || typeof record2.error === "string") {
      const error = new Error(text(record2.error, `\u8BF7\u6C42\u5931\u8D25 (HTTP ${response.status})`));
      error.status = response.status;
      throw error;
    }
    return record2;
  }
  function normalizeConfig2(value) {
    return {
      defaultProvider: oneOf(value.defaultProvider, PROVIDERS, "claude"),
      defaultSessionKind: oneOf(value.defaultSessionKind, KINDS, "structured"),
      defaultMode: oneOf(value.defaultMode, MODES, "default"),
      defaultCwd: text(value.defaultCwd),
      structuredRunner: text(value.structuredRunner, "cli")
    };
  }
  function normalizePaths(value) {
    if (!Array.isArray(value)) return [];
    return value.flatMap((item) => {
      if (!isRecord(item) || typeof item.path !== "string" || !item.path.trim()) return [];
      return [{
        path: item.path,
        name: text(item.name, item.path.split("/").filter(Boolean).at(-1) ?? item.path)
      }];
    });
  }
  function supportedModes(provider) {
    if (provider === "codex") return ["full-access"];
    if (provider === "opencode" || provider === "grok") return ["default", "full-access", "managed"];
    if (provider === "qoder") return ["default", "full-access", "auto-edit", "managed"];
    return ["default", "full-access", "auto-edit", "native", "managed"];
  }
  function safeMode(provider, requested, fallback = "default") {
    const supported = supportedModes(provider);
    if (supported.includes(requested)) return requested;
    if (supported.includes(fallback)) return fallback;
    return supported[0];
  }
  function structuredRunner(provider, configured) {
    if (provider === "codex") return "codex-cli-exec";
    if (provider === "opencode") return "opencode-cli-run";
    if (provider === "grok") return "grok-cli-headless";
    if (provider === "qoder") return "qoder-cli-print";
    return configured === "sdk" || configured === "claude-sdk" ? "claude-sdk" : "claude-cli-print";
  }
  function buildCreateRequest(form, defaults, context, dimensions = {}) {
    const cwd = form.cwd.trim() || context.effectiveCwd.trim() || defaults.defaultCwd;
    const mode = safeMode(form.provider, form.mode, defaults.defaultMode);
    const base = {
      provider: form.provider,
      cwd,
      mode,
      worktreeEnabled: form.worktreeEnabled === true,
      sessionSource: "interactive"
    };
    if (form.kind === "pty") {
      const cols = Number.isFinite(dimensions.cols) && (dimensions.cols ?? 0) > 0 ? dimensions.cols : void 0;
      const rows = Number.isFinite(dimensions.rows) && (dimensions.rows ?? 0) > 0 ? dimensions.rows : void 0;
      return {
        ...base,
        kind: "pty",
        command: form.provider,
        cols,
        rows
      };
    }
    const model = context.selectedModels?.[form.provider]?.trim();
    const thinkingEffort = context.thinkingEffort?.trim();
    return {
      ...base,
      kind: "structured",
      runner: structuredRunner(form.provider, defaults.structuredRunner),
      model: model || void 0,
      thinkingEffort: thinkingEffort || void 0
    };
  }
  var HttpNewSessionRepository = class {
    constructor(fetchImpl = (input, init) => globalThis.fetch(input, init)) {
      __publicField(this, "fetchImpl", fetchImpl);
      __publicField(this, "preferenceWrite", Promise.resolve());
    }
    async load(options = {}) {
      await this.preferenceWrite.catch(() => void 0);
      const configResponse = await this.fetchImpl("/api/config", {
        credentials: "same-origin",
        signal: options.signal
      });
      const config = normalizeConfig2(await readJson(configResponse));
      const recentPaths = await this.fetchImpl("/api/recent-paths", {
        credentials: "same-origin",
        signal: options.signal
      }).then(async (response) => {
        if (!response.ok) return [];
        return normalizePaths(await response.json());
      }).catch((error) => {
        if (options.signal?.aborted) throw error;
        return [];
      });
      return { config, recentPaths };
    }
    savePreferences(patch) {
      if (Object.keys(patch).length === 0) return this.preferenceWrite;
      const write = this.preferenceWrite.catch(() => void 0).then(async () => {
        const response = await this.fetchImpl("/api/settings/config", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          credentials: "same-origin",
          body: JSON.stringify(patch)
        });
        await readJson(response);
      });
      this.preferenceWrite = write;
      return write;
    }
    async suggestPaths(query, options = {}) {
      const response = await this.fetchImpl(`/api/path-suggestions?q=${encodeURIComponent(query.trim())}`, {
        credentials: "same-origin",
        signal: options.signal
      });
      if (!response.ok) return [];
      return normalizePaths(await response.json());
    }
    async create(request2) {
      const structured = request2.kind === "structured";
      const endpoint = structured ? "/api/structured-sessions" : "/api/commands";
      const body = structured ? {
        cwd: request2.cwd,
        mode: request2.mode,
        provider: request2.provider,
        runner: request2.runner,
        worktreeEnabled: request2.worktreeEnabled,
        model: request2.model,
        thinkingEffort: request2.thinkingEffort,
        sessionSource: request2.sessionSource
      } : {
        command: request2.command,
        provider: request2.provider,
        cwd: request2.cwd,
        mode: request2.mode,
        worktreeEnabled: request2.worktreeEnabled,
        cols: request2.cols,
        rows: request2.rows,
        sessionSource: request2.sessionSource
      };
      const response = await this.fetchImpl(endpoint, {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        credentials: "same-origin",
        body: JSON.stringify(body)
      });
      const created = await readJson(response);
      if (typeof created.id !== "string" || !created.id) {
        throw new Error("\u670D\u52A1\u7AEF\u672A\u8FD4\u56DE\u65B0\u4F1A\u8BDD ID\u3002");
      }
      return created;
    }
  };
  var httpNewSessionRepository = new HttpNewSessionRepository();

  // src/web-ui/react/new-session/choice-navigation.ts
  function nextChoice(values, current, key) {
    if (values.length === 0) return current;
    if (key === "Home") return values[0];
    if (key === "End") return values[values.length - 1];
    const currentIndex = Math.max(0, values.indexOf(current));
    const forwards = key === "ArrowRight" || key === "ArrowDown";
    const offset4 = forwards ? 1 : -1;
    return values[(currentIndex + offset4 + values.length) % values.length];
  }

  // src/web-ui/react/new-session/host.tsx
  var import_jsx_runtime30 = __toESM(require_jsx_runtime(), 1);
  var PROVIDERS2 = [
    { value: "claude", label: "Claude", description: "\u5B8C\u6574 Claude \u4F1A\u8BDD\u80FD\u529B" },
    { value: "codex", label: "Codex", description: "\u7ED3\u6784\u5316 JSONL \u6216 PTY \u4F1A\u8BDD" },
    { value: "opencode", label: "OpenCode", description: "\u591A\u6A21\u578B\u7ED3\u6784\u5316\u6216 PTY \u4F1A\u8BDD" },
    { value: "grok", label: "Grok", description: "Grok Build \u7ED3\u6784\u5316\u6216 PTY \u4F1A\u8BDD" },
    { value: "qoder", label: "Qoder", description: "Qoder CLI \u7ED3\u6784\u5316\u6216 PTY \u4F1A\u8BDD" }
  ];
  var KINDS2 = [
    { value: "structured", label: "\u7ED3\u6784\u5316", description: "\u667A\u80FD\u5BF9\u8BDD\u6A21\u5F0F" },
    { value: "pty", label: "PTY", description: "\u4EA4\u4E92\u5F0F\u7EC8\u7AEF\u4F1A\u8BDD" }
  ];
  var MODES2 = [
    { value: "managed", label: "\u6258\u7BA1", description: "\u5168\u81EA\u52A8\u5B8C\u6210\u4EFB\u52A1" },
    { value: "full-access", label: "\u5168\u6743\u9650", description: "\u81EA\u52A8\u786E\u8BA4\u6743\u9650" },
    { value: "auto-edit", label: "\u81EA\u52A8\u7F16\u8F91", description: "\u81EA\u52A8\u786E\u8BA4\u4FEE\u6539" },
    { value: "default", label: "\u6807\u51C6", description: "\u9010\u6B65\u786E\u8BA4\u64CD\u4F5C" },
    { value: "native", label: "\u539F\u751F", description: "\u539F\u751F\u7ED3\u6784\u5316\u8F93\u51FA" }
  ];
  function GrokMark() {
    return /* @__PURE__ */ (0, import_jsx_runtime30.jsxs)("svg", { className: "wand-provider-grok-mark", viewBox: "0 0 34 33", "aria-hidden": "true", focusable: "false", children: [
      /* @__PURE__ */ (0, import_jsx_runtime30.jsx)("path", { d: "M13.2371 21.0407L24.3186 12.8506C24.8619 12.4491 25.6384 12.6057 25.8973 13.2294C27.2597 16.5185 26.651 20.4712 23.9403 23.1851C21.2297 25.8989 17.4581 26.4941 14.0108 25.1386L10.2449 26.8843C15.6463 30.5806 22.2053 29.6665 26.304 25.5601C29.5551 22.3051 30.562 17.8683 29.6205 13.8673L29.629 13.8758C28.2637 7.99809 29.9647 5.64871 33.449.844576C33.5314.730667 33.6139.616757 33.6964.5L29.1113 5.09055V5.07631L13.2343 21.0436Z" }),
      /* @__PURE__ */ (0, import_jsx_runtime30.jsx)("path", { d: "M10.9503 23.0313C7.07343 19.3235 7.74185 13.5853 11.0498 10.2763C13.4959 7.82722 17.5036 6.82767 21.0021 8.2971L24.7595 6.55998C24.0826 6.07017 23.215 5.54334 22.2195 5.17313C17.7198 3.31926 12.3326 4.24192 8.67479 7.90126C5.15635 11.4239 4.0499 16.8403 5.94992 21.4622C7.36924 24.9165 5.04257 27.3598 2.69884 29.826C1.86829 30.7002 1.0349 31.5745.36364 32.5L10.9474 23.0341Z" })
    ] });
  }
  function kindHint(provider, kind) {
    if (kind === "structured") {
      if (provider === "codex") return "Codex JSONL \u7ED3\u6784\u5316\u804A\u5929\u754C\u9762\uFF0C\u652F\u6301\u591A\u8F6E\u5BF9\u8BDD\u548C\u5DE5\u5177\u8C03\u7528\u5C55\u793A\u3002";
      if (provider === "opencode") return "OpenCode JSON \u7ED3\u6784\u5316\u804A\u5929\u754C\u9762\uFF0C\u652F\u6301\u7EED\u804A\u3001\u601D\u8003\u8FC7\u7A0B\u548C\u5DE5\u5177\u8C03\u7528\u5C55\u793A\u3002";
      if (provider === "grok") return "Grok streaming-json \u7ED3\u6784\u5316\u804A\u5929\u754C\u9762\uFF0C\u652F\u6301\u591A\u8F6E\u7EED\u804A\u4E0E\u601D\u8003\u8FC7\u7A0B\u5C55\u793A\u3002";
      if (provider === "qoder") return "Qoder stream-json \u7ED3\u6784\u5316\u804A\u5929\u754C\u9762\uFF0C\u652F\u6301\u7EED\u804A\u3001\u601D\u8003\u8FC7\u7A0B\u548C\u5DE5\u5177\u8C03\u7528\u5C55\u793A\u3002";
      return "\u7ED3\u6784\u5316\u804A\u5929\u754C\u9762\uFF0C\u652F\u6301\u591A\u8F6E\u5BF9\u8BDD\u3001\u6D41\u5F0F\u8F93\u51FA\u548C\u5DE5\u5177\u8C03\u7528\u5C55\u793A\u3002";
    }
    if (provider === "codex") return "Codex PTY \u7EC8\u7AEF\u4F1A\u8BDD\uFF1Bterminal \u662F\u539F\u59CB\u8F93\u51FA\uFF0Cchat \u662F\u89E3\u6790\u540E\u7684\u9605\u8BFB\u89C6\u56FE\u3002";
    if (provider === "opencode") return "OpenCode TUI \u7684\u539F\u59CB PTY \u7EC8\u7AEF\u4F1A\u8BDD\u3002";
    if (provider === "grok") return "Grok Build TUI \u7684\u539F\u59CB PTY \u7EC8\u7AEF\u4F1A\u8BDD\u3002";
    if (provider === "qoder") return "Qoder CLI TUI \u7684\u539F\u59CB PTY \u7EC8\u7AEF\u4F1A\u8BDD\u3002";
    return "\u539F\u59CB PTY \u7EC8\u7AEF\u4F1A\u8BDD\uFF0C\u652F\u6301\u6301\u7EED\u4EA4\u4E92\u3001\u7EC8\u7AEF\u89C6\u56FE\u548C\u6743\u9650\u6D41\u3002";
  }
  function modeHint(provider, mode) {
    if (provider === "codex") {
      return "Codex \u652F\u6301 PTY \u7EC8\u7AEF\u4E0E\u7ED3\u6784\u5316\uFF08JSONL\uFF09\u4E24\u79CD\u4F1A\u8BDD\uFF0C\u7ED3\u6784\u5316\u6A21\u5F0F\u6309 full-access \u542F\u52A8\u3002";
    }
    if (provider === "opencode") {
      return mode === "full-access" || mode === "managed" || mode === "auto-edit" ? "OpenCode \u5C06\u81EA\u52A8\u6279\u51C6\u672A\u663E\u5F0F\u62D2\u7EDD\u7684\u6743\u9650\uFF1B\u652F\u6301 TUI \u4E0E JSON \u7ED3\u6784\u5316\u4F1A\u8BDD\u3002" : "OpenCode \u4F7F\u7528\u81EA\u8EAB\u6743\u9650\u914D\u7F6E\uFF1B\u7ED3\u6784\u5316\u6A21\u5F0F\u4F1A\u81EA\u52A8\u62D2\u7EDD\u672A\u6279\u51C6\u7684\u6743\u9650\u8BF7\u6C42\u3002";
    }
    if (provider === "grok") {
      return mode === "full-access" || mode === "managed" ? "Grok \u5C06\u4EE5 always-approve \u8FD0\u884C\uFF1B\u652F\u6301 TUI \u4E0E streaming-json \u7ED3\u6784\u5316\u4F1A\u8BDD\u3002" : "Grok \u4F7F\u7528\u81EA\u8EAB\u6743\u9650\u786E\u8BA4\uFF1B\u652F\u6301 TUI \u4E0E streaming-json \u7ED3\u6784\u5316\u4F1A\u8BDD\u3002";
    }
    if (provider === "qoder") {
      return mode === "full-access" || mode === "managed" ? "Qoder \u5C06\u4EE5 bypass_permissions \u8FD0\u884C\uFF1B\u652F\u6301 TUI \u4E0E stream-json \u7ED3\u6784\u5316\u4F1A\u8BDD\u3002" : mode === "auto-edit" ? "Qoder \u5C06\u81EA\u52A8\u6279\u51C6\u5DE5\u4F5C\u533A\u5185\u7684\u5B89\u5168\u7F16\u8F91\u3002" : "Qoder \u4F7F\u7528\u81EA\u8EAB\u6743\u9650\u786E\u8BA4\uFF1B\u7ED3\u6784\u5316\u6A21\u5F0F\u4E0B\u672A\u6279\u51C6\u7684\u64CD\u4F5C\u4F1A\u88AB\u62D2\u7EDD\u3002";
    }
    if (mode === "full-access") return "\u81EA\u52A8\u786E\u8BA4\u6743\u9650\u8BF7\u6C42\u4E0E\u9AD8\u6743\u9650\u64CD\u4F5C\uFF0C\u9002\u5408\u4F60\u786E\u8BA4\u73AF\u5883\u5B89\u5168\u540E\u7684\u8FDE\u7EED\u4FEE\u6539\u3002";
    if (mode === "auto-edit") return "\u4FDD\u7559\u4EA4\u4E92\u5F0F\u4F1A\u8BDD\uFF0C\u540C\u65F6\u66F4\u504F\u5411\u76F4\u63A5\u7F16\u8F91\u4EE3\u7801\u3002";
    if (mode === "native") return "\u8C03\u7528 Claude \u539F\u751F API \u8F93\u51FA\uFF0C\u9002\u5408\u5FEB\u901F\u95EE\u7B54\u6216\u4E00\u6B21\u6027\u751F\u6210\u3002";
    if (mode === "managed") return "AI \u81EA\u52A8\u5B8C\u6210\u6240\u6709\u5DE5\u4F5C\uFF0C\u65E0\u9700\u4E2D\u9014\u786E\u8BA4\uFF0C\u9002\u5408\u6709\u660E\u786E\u76EE\u6807\u7684\u4EFB\u52A1\u3002";
    return "\u4FDD\u7559\u6807\u51C6\u4EA4\u4E92\u6D41\u7A0B\uFF0C\u9002\u5408\u624B\u52A8\u786E\u8BA4\u6BCF\u4E00\u6B65\u3002";
  }
  function creationFallback(provider, kind) {
    if (kind === "structured") return "\u65E0\u6CD5\u542F\u52A8\u7ED3\u6784\u5316\u4F1A\u8BDD\uFF0C\u8BF7\u786E\u8BA4\u5BF9\u5E94 Provider \u5DF2\u6B63\u786E\u5B89\u88C5\u3002";
    if (provider === "codex") return "\u65E0\u6CD5\u542F\u52A8 Codex \u4F1A\u8BDD\uFF0C\u8BF7\u786E\u8BA4 codex \u5DF2\u6B63\u786E\u5B89\u88C5\u5E76\u53EF\u5728\u7EC8\u7AEF\u4E2D\u6267\u884C\u3002";
    if (provider === "opencode") return "\u65E0\u6CD5\u542F\u52A8 OpenCode \u4F1A\u8BDD\uFF0C\u8BF7\u786E\u8BA4 opencode-ai \u5DF2\u6B63\u786E\u5B89\u88C5\u3002";
    if (provider === "grok") return "\u65E0\u6CD5\u542F\u52A8 Grok \u4F1A\u8BDD\uFF0C\u8BF7\u786E\u8BA4 Grok Build CLI \u5DF2\u6B63\u786E\u5B89\u88C5\u3002";
    if (provider === "qoder") return "\u65E0\u6CD5\u542F\u52A8 Qoder \u4F1A\u8BDD\uFF0C\u8BF7\u786E\u8BA4 @qoder-ai/qodercli \u5DF2\u6B63\u786E\u5B89\u88C5\u3002";
    return "\u65E0\u6CD5\u542F\u52A8 Claude \u4F1A\u8BDD\uFF0C\u8BF7\u786E\u8BA4 Claude \u5DF2\u6B63\u786E\u5B89\u88C5\u3002";
  }
  function presentError(error, fallback) {
    if (!(error instanceof Error) || !error.message || error.message === "Failed to fetch") return fallback;
    return error.message;
  }
  var RADIO_NAVIGATION_KEYS = /* @__PURE__ */ new Set([
    "ArrowLeft",
    "ArrowRight",
    "ArrowUp",
    "ArrowDown",
    "Home",
    "End"
  ]);
  function NewSessionHost({ repository = httpNewSessionRepository }) {
    const controller = (0, import_react10.useSyncExternalStore)(
      newSessionStore.subscribe,
      newSessionStore.getSnapshot,
      newSessionStore.getSnapshot
    );
    const [defaults, setDefaults] = (0, import_react10.useState)(null);
    const [form, setForm] = (0, import_react10.useState)(null);
    const [loading, setLoading] = (0, import_react10.useState)(false);
    const [submitting, setSubmitting] = (0, import_react10.useState)(false);
    const [error, setError] = (0, import_react10.useState)("");
    const [suggestions, setSuggestions] = (0, import_react10.useState)([]);
    const [suggestionsActive, setSuggestionsActive] = (0, import_react10.useState)(false);
    const providerRefs = (0, import_react10.useRef)({});
    const kindRefs = (0, import_react10.useRef)({});
    const modeRefs = (0, import_react10.useRef)({});
    (0, import_react10.useEffect)(() => {
      if (!controller.open) return;
      const abort = new AbortController();
      const runtime6 = newSessionStore.getRuntime();
      setLoading(true);
      setSubmitting(false);
      setError("");
      setDefaults(null);
      setForm(null);
      setSuggestions([]);
      setSuggestionsActive(false);
      void repository.load({ signal: abort.signal }).then((loaded) => {
        if (abort.signal.aborted) return;
        const context = runtime6?.getContext();
        setDefaults(loaded);
        setForm({
          provider: loaded.config.defaultProvider,
          kind: loaded.config.defaultSessionKind,
          mode: safeMode(
            loaded.config.defaultProvider,
            loaded.config.defaultMode,
            loaded.config.defaultMode
          ),
          cwd: "",
          worktreeEnabled: false
        });
        if (!context) setError("\u65B0\u5EFA\u4F1A\u8BDD\u8FD0\u884C\u73AF\u5883\u5C1A\u672A\u5C31\u7EEA\uFF0C\u8BF7\u5237\u65B0\u9875\u9762\u540E\u91CD\u8BD5\u3002");
      }).catch((loadError) => {
        if (!abort.signal.aborted) setError(presentError(loadError, "\u65E0\u6CD5\u52A0\u8F7D\u65B0\u5EFA\u4F1A\u8BDD\u914D\u7F6E\u3002"));
      }).finally(() => {
        if (!abort.signal.aborted) setLoading(false);
      });
      return () => abort.abort();
    }, [controller.open, controller.revision, repository]);
    (0, import_react10.useEffect)(() => {
      if (!controller.open || !form || !suggestionsActive) return;
      const abort = new AbortController();
      const timer = window.setTimeout(() => {
        void repository.suggestPaths(form.cwd, { signal: abort.signal }).then((items) => {
          if (!abort.signal.aborted) setSuggestions(items);
        }).catch(() => {
          if (!abort.signal.aborted) setSuggestions([]);
        });
      }, 120);
      return () => {
        window.clearTimeout(timer);
        abort.abort();
      };
    }, [controller.open, form?.cwd, repository, suggestionsActive]);
    const selectProvider = (0, import_react10.useCallback)((provider) => {
      if (!defaults) return;
      setForm((current) => current ? {
        ...current,
        provider,
        mode: safeMode(provider, current.mode, defaults.config.defaultMode)
      } : current);
      const currentMode = form ? safeMode(provider, form.mode, defaults.config.defaultMode) : safeMode(provider, defaults.config.defaultMode, defaults.config.defaultMode);
      void repository.savePreferences({ defaultProvider: provider, defaultMode: currentMode }).catch((saveError) => console.warn("[wand] Failed to persist new-session defaults", saveError));
    }, [defaults, form, repository]);
    const selectKind = (0, import_react10.useCallback)((kind) => {
      setForm((current) => current ? { ...current, kind } : current);
      void repository.savePreferences({ defaultSessionKind: kind }).catch((saveError) => console.warn("[wand] Failed to persist new-session defaults", saveError));
    }, [repository]);
    const selectMode = (0, import_react10.useCallback)((mode) => {
      if (!form || !supportedModes(form.provider).includes(mode)) return;
      setForm((current) => current ? { ...current, mode } : current);
      void repository.savePreferences({ defaultMode: mode }).catch((saveError) => console.warn("[wand] Failed to persist new-session defaults", saveError));
    }, [form, repository]);
    const supported = (0, import_react10.useMemo)(
      () => new Set(form ? supportedModes(form.provider) : []),
      [form?.provider]
    );
    function navigateChoice(event, current, values, choose, refs) {
      if (!RADIO_NAVIGATION_KEYS.has(event.key)) return;
      event.preventDefault();
      const next = nextChoice(values, current, event.key);
      choose(next);
      window.requestAnimationFrame(() => refs.current[next]?.focus());
    }
    async function submit(event) {
      event.preventDefault();
      if (!form || !defaults || submitting) return;
      const runtime6 = newSessionStore.getRuntime();
      if (!runtime6) {
        setError("\u65B0\u5EFA\u4F1A\u8BDD\u8FD0\u884C\u73AF\u5883\u5C1A\u672A\u5C31\u7EEA\uFF0C\u8BF7\u5237\u65B0\u9875\u9762\u540E\u91CD\u8BD5\u3002");
        return;
      }
      newSessionController.setDismissable(false);
      setSubmitting(true);
      setError("");
      try {
        const dimensions = await runtime6.prepareCreate(form.kind);
        const request2 = buildCreateRequest(form, defaults.config, runtime6.getContext(), dimensions);
        void repository.savePreferences({
          defaultProvider: request2.provider,
          defaultSessionKind: request2.kind,
          defaultMode: request2.mode
        }).catch((saveError) => console.warn("[wand] Failed to persist new-session defaults", saveError));
        const created = await repository.create(request2);
        await runtime6.completeCreate(request2, created);
        newSessionController.close();
      } catch (createError) {
        setError(presentError(createError, creationFallback(form.provider, form.kind)));
      } finally {
        newSessionController.setDismissable(true);
        setSubmitting(false);
      }
    }
    return /* @__PURE__ */ (0, import_jsx_runtime30.jsx)(
      WandDialogSurface,
      {
        open: controller.open,
        onOpenChange: (open) => {
          if (!open) newSessionController.close();
        },
        title: "\u65B0\u5BF9\u8BDD",
        description: "\u542F\u52A8 Claude\u3001Codex\u3001OpenCode\u3001Grok \u6216 Qoder \u4F1A\u8BDD\uFF0C\u9009\u62E9 provider\u3001\u4F1A\u8BDD\u7C7B\u578B\u3001\u6A21\u5F0F\u548C\u5DE5\u4F5C\u76EE\u5F55\u3002",
        className: "wand-new-session-dialog",
        overlayClassName: "wand-new-session-overlay",
        titleClassName: "wand-new-session-title",
        descriptionClassName: "wand-new-session-description",
        headerClassName: "wand-new-session-header",
        closeLabel: "\u5173\u95ED\u65B0\u5EFA\u4F1A\u8BDD",
        testId: "new-session-dialog",
        dismissable: !submitting,
        children: loading ? /* @__PURE__ */ (0, import_jsx_runtime30.jsx)("div", { className: "wand-new-session-loading", role: "status", children: "\u6B63\u5728\u52A0\u8F7D\u65B0\u5EFA\u4F1A\u8BDD\u914D\u7F6E\u2026" }) : form && defaults ? /* @__PURE__ */ (0, import_jsx_runtime30.jsxs)("form", { className: "wand-new-session-form", "aria-busy": submitting, onSubmit: (event) => void submit(event), children: [
          /* @__PURE__ */ (0, import_jsx_runtime30.jsxs)("div", { className: "wand-new-session-body", children: [
            /* @__PURE__ */ (0, import_jsx_runtime30.jsxs)("fieldset", { className: "wand-new-session-field wand-new-session-fieldset", children: [
              /* @__PURE__ */ (0, import_jsx_runtime30.jsx)("legend", { className: "wand-new-session-field-label", children: "Provider" }),
              /* @__PURE__ */ (0, import_jsx_runtime30.jsx)("div", { className: "wand-new-session-choices", role: "radiogroup", "aria-label": "Provider", children: PROVIDERS2.map((provider) => /* @__PURE__ */ (0, import_jsx_runtime30.jsxs)(
                "button",
                {
                  type: "button",
                  role: "radio",
                  "aria-checked": form.provider === provider.value,
                  tabIndex: form.provider === provider.value ? 0 : -1,
                  ref: (element) => {
                    providerRefs.current[provider.value] = element;
                  },
                  className: `wand-new-session-choice wand-new-session-provider-choice${form.provider === provider.value ? " active" : ""}`,
                  autoFocus: form.provider === provider.value,
                  "data-wand-autofocus": form.provider === provider.value ? "" : void 0,
                  onClick: () => selectProvider(provider.value),
                  onKeyDown: (event) => navigateChoice(
                    event,
                    form.provider,
                    PROVIDERS2.map((item) => item.value),
                    selectProvider,
                    providerRefs
                  ),
                  children: [
                    provider.value === "grok" && /* @__PURE__ */ (0, import_jsx_runtime30.jsx)(GrokMark, {}),
                    /* @__PURE__ */ (0, import_jsx_runtime30.jsx)("span", { className: "wand-new-session-choice-label", children: provider.label }),
                    /* @__PURE__ */ (0, import_jsx_runtime30.jsx)("span", { className: "wand-new-session-choice-description", children: provider.description })
                  ]
                },
                provider.value
              )) })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime30.jsxs)("fieldset", { className: "wand-new-session-field wand-new-session-fieldset", children: [
              /* @__PURE__ */ (0, import_jsx_runtime30.jsx)("legend", { className: "wand-new-session-field-label", children: "\u4F1A\u8BDD\u7C7B\u578B" }),
              /* @__PURE__ */ (0, import_jsx_runtime30.jsx)("div", { className: "wand-new-session-choices", role: "radiogroup", "aria-label": "\u4F1A\u8BDD\u7C7B\u578B", children: KINDS2.map((kind) => /* @__PURE__ */ (0, import_jsx_runtime30.jsxs)(
                "button",
                {
                  type: "button",
                  role: "radio",
                  "aria-checked": form.kind === kind.value,
                  tabIndex: form.kind === kind.value ? 0 : -1,
                  ref: (element) => {
                    kindRefs.current[kind.value] = element;
                  },
                  className: `wand-new-session-choice wand-new-session-kind-choice${form.kind === kind.value ? " active" : ""}`,
                  onClick: () => selectKind(kind.value),
                  onKeyDown: (event) => navigateChoice(
                    event,
                    form.kind,
                    KINDS2.map((item) => item.value),
                    selectKind,
                    kindRefs
                  ),
                  children: [
                    /* @__PURE__ */ (0, import_jsx_runtime30.jsx)("span", { className: "wand-new-session-choice-label", children: kind.label }),
                    /* @__PURE__ */ (0, import_jsx_runtime30.jsx)("span", { className: "wand-new-session-choice-description", children: kind.description })
                  ]
                },
                kind.value
              )) }),
              /* @__PURE__ */ (0, import_jsx_runtime30.jsx)("p", { className: "wand-new-session-field-hint", children: kindHint(form.provider, form.kind) }),
              /* @__PURE__ */ (0, import_jsx_runtime30.jsxs)("div", { className: "wand-new-session-worktree", children: [
                /* @__PURE__ */ (0, import_jsx_runtime30.jsxs)("div", { children: [
                  /* @__PURE__ */ (0, import_jsx_runtime30.jsx)("strong", { children: "Worktree \u6A21\u5F0F" }),
                  /* @__PURE__ */ (0, import_jsx_runtime30.jsx)("span", { children: "\u4E3A\u672C\u6B21\u4F1A\u8BDD\u521B\u5EFA\u72EC\u7ACB\u7684 Git worktree \u4E0E\u5206\u652F\u3002" })
                ] }),
                /* @__PURE__ */ (0, import_jsx_runtime30.jsx)(
                  WandSwitch,
                  {
                    id: "wand-new-session-worktree",
                    checked: form.worktreeEnabled,
                    ariaLabel: "\u542F\u7528 Worktree \u6A21\u5F0F",
                    onCheckedChange: (worktreeEnabled) => setForm({ ...form, worktreeEnabled })
                  }
                )
              ] })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime30.jsxs)("div", { className: "wand-new-session-field", children: [
              /* @__PURE__ */ (0, import_jsx_runtime30.jsx)("label", { className: "wand-new-session-field-label", htmlFor: "wand-new-session-cwd", children: "\u5DE5\u4F5C\u76EE\u5F55" }),
              /* @__PURE__ */ (0, import_jsx_runtime30.jsxs)("div", { className: "wand-new-session-suggestions-wrap", children: [
                /* @__PURE__ */ (0, import_jsx_runtime30.jsx)(
                  "input",
                  {
                    id: "wand-new-session-cwd",
                    className: "wand-new-session-input",
                    type: "text",
                    value: form.cwd,
                    placeholder: newSessionStore.getRuntime()?.getContext().effectiveCwd || defaults.config.defaultCwd,
                    autoComplete: "off",
                    autoCorrect: "off",
                    autoCapitalize: "off",
                    spellCheck: false,
                    onFocus: () => setSuggestionsActive(true),
                    onChange: (event) => setForm({ ...form, cwd: event.currentTarget.value }),
                    onBlur: () => window.setTimeout(() => setSuggestionsActive(false), 120)
                  }
                ),
                suggestionsActive && suggestions.length > 0 ? /* @__PURE__ */ (0, import_jsx_runtime30.jsx)("div", { className: "wand-new-session-suggestions", role: "listbox", "aria-label": "\u5DE5\u4F5C\u76EE\u5F55\u5EFA\u8BAE", children: suggestions.map((item) => /* @__PURE__ */ (0, import_jsx_runtime30.jsxs)(
                  "button",
                  {
                    type: "button",
                    className: "wand-new-session-suggestion",
                    role: "option",
                    "aria-selected": form.cwd === item.path,
                    onMouseDown: (event) => event.preventDefault(),
                    onClick: () => {
                      setForm({ ...form, cwd: item.path });
                      setSuggestionsActive(false);
                    },
                    children: [
                      /* @__PURE__ */ (0, import_jsx_runtime30.jsx)("strong", { children: item.name }),
                      /* @__PURE__ */ (0, import_jsx_runtime30.jsx)("small", { className: "wand-new-session-suggestion-path", children: item.path })
                    ]
                  },
                  item.path
                )) }) : null
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime30.jsx)("p", { className: "wand-new-session-field-hint", children: "\u521B\u5EFA\u524D\u5148\u786E\u8BA4\u76EE\u5F55\uFF1B\u7559\u7A7A\u5219\u4F7F\u7528\u4E0A\u65B9\u76EE\u5F55\uFF0C\u652F\u6301\u8DEF\u5F84\u81EA\u52A8\u8865\u5168\u3002" }),
              defaults.recentPaths.length > 0 ? /* @__PURE__ */ (0, import_jsx_runtime30.jsx)("div", { className: "wand-new-session-recent-paths", "aria-label": "\u6700\u8FD1\u4F7F\u7528\u7684\u5DE5\u4F5C\u76EE\u5F55", children: defaults.recentPaths.map((item) => /* @__PURE__ */ (0, import_jsx_runtime30.jsx)(
                "button",
                {
                  type: "button",
                  className: "wand-new-session-recent-path",
                  title: item.path,
                  onClick: () => setForm({ ...form, cwd: item.path }),
                  children: /* @__PURE__ */ (0, import_jsx_runtime30.jsx)("span", { className: "wand-new-session-recent-path-value", children: item.path })
                },
                item.path
              )) }) : null
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime30.jsxs)("fieldset", { className: "wand-new-session-field wand-new-session-fieldset", children: [
              /* @__PURE__ */ (0, import_jsx_runtime30.jsx)("legend", { className: "wand-new-session-field-label", children: "\u6A21\u5F0F" }),
              /* @__PURE__ */ (0, import_jsx_runtime30.jsx)("div", { className: "wand-new-session-choices", role: "radiogroup", "aria-label": "\u6267\u884C\u6A21\u5F0F", children: MODES2.map((mode) => {
                const disabled = !supported.has(mode.value);
                return /* @__PURE__ */ (0, import_jsx_runtime30.jsxs)(
                  "button",
                  {
                    type: "button",
                    role: "radio",
                    "aria-checked": form.mode === mode.value,
                    "aria-disabled": disabled,
                    tabIndex: form.mode === mode.value ? 0 : -1,
                    ref: (element) => {
                      modeRefs.current[mode.value] = element;
                    },
                    disabled,
                    className: `wand-new-session-choice${form.mode === mode.value ? " active" : ""}${disabled ? " disabled" : ""}`,
                    onClick: () => selectMode(mode.value),
                    onKeyDown: (event) => navigateChoice(
                      event,
                      form.mode,
                      MODES2.filter((item) => supported.has(item.value)).map((item) => item.value),
                      selectMode,
                      modeRefs
                    ),
                    children: [
                      /* @__PURE__ */ (0, import_jsx_runtime30.jsx)("span", { className: "wand-new-session-choice-label", children: mode.label }),
                      /* @__PURE__ */ (0, import_jsx_runtime30.jsx)("span", { className: "wand-new-session-choice-description", children: mode.description })
                    ]
                  },
                  mode.value
                );
              }) }),
              /* @__PURE__ */ (0, import_jsx_runtime30.jsx)("p", { className: "wand-new-session-field-hint", children: modeHint(form.provider, form.mode) })
            ] })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime30.jsxs)("div", { className: "wand-new-session-footer", children: [
            /* @__PURE__ */ (0, import_jsx_runtime30.jsx)(
              WandButton,
              {
                kind: "primary",
                size: "large",
                type: "submit",
                className: "wand-new-session-submit",
                disabled: submitting,
                children: submitting ? "\u6B63\u5728\u542F\u52A8\u2026" : "\u542F\u52A8\u4F1A\u8BDD"
              }
            ),
            error ? /* @__PURE__ */ (0, import_jsx_runtime30.jsx)("p", { className: "wand-new-session-error", role: "alert", children: error }) : null
          ] })
        ] }) : error ? /* @__PURE__ */ (0, import_jsx_runtime30.jsxs)("div", { className: "wand-new-session-load-error", role: "alert", children: [
          /* @__PURE__ */ (0, import_jsx_runtime30.jsx)("p", { children: error }),
          /* @__PURE__ */ (0, import_jsx_runtime30.jsx)(WandButton, { kind: "primary", onClick: () => newSessionController.open(), children: "\u91CD\u8BD5" })
        ] }) : null
      }
    );
  }

  // src/web-ui/react/folder-picker/host.tsx
  var import_react11 = __toESM(require_react(), 1);

  // src/web-ui/react/folder-picker/controller.ts
  var runtime2 = null;
  var snapshot4 = {
    open: false,
    dismissable: true,
    initialPath: "",
    revision: 0
  };
  var listeners4 = /* @__PURE__ */ new Set();
  function publish4(open, initialPath = snapshot4.initialPath) {
    snapshot4 = { open, dismissable: true, initialPath, revision: snapshot4.revision + 1 };
    for (const listener of listeners4) listener();
  }
  function publishDismissable2(dismissable) {
    if (!snapshot4.open || snapshot4.dismissable === dismissable) return;
    snapshot4 = { ...snapshot4, dismissable };
    for (const listener of listeners4) listener();
  }
  var folderPickerController = {
    open(initialPath) {
      if (!runtime2) return false;
      const resolved = (initialPath ?? runtime2.getInitialPath()).trim();
      if (!resolved) return false;
      runtime2.onOpen?.();
      publish4(true, resolved);
      return true;
    },
    close() {
      if (!snapshot4.open) return;
      publish4(false);
      runtime2?.onClose?.();
    },
    closeIfOpen() {
      if (!snapshot4.open || !snapshot4.dismissable) return false;
      this.close();
      return true;
    },
    closeTopmost() {
      if (!snapshot4.open) return false;
      if (snapshot4.dismissable) this.close();
      return true;
    },
    isOpen() {
      return snapshot4.open;
    },
    setDismissable(dismissable) {
      publishDismissable2(dismissable);
    },
    async choose(path) {
      const selectedPath = path.trim();
      const activeRuntime = runtime2;
      if (!snapshot4.open || !activeRuntime || !selectedPath) return false;
      await activeRuntime.applySelection(selectedPath);
      this.close();
      return true;
    }
  };
  var folderPickerStore = {
    subscribe(listener) {
      listeners4.add(listener);
      return () => listeners4.delete(listener);
    },
    getSnapshot() {
      return snapshot4;
    }
  };
  function configureFolderPickerRuntime(adapter) {
    runtime2 = adapter;
    return () => {
      if (runtime2 !== adapter) return;
      if (snapshot4.open) folderPickerController.close();
      runtime2 = null;
    };
  }

  // src/web-ui/react/folder-picker/model.ts
  function nextFolderPickerIndex(current, itemCount, key) {
    if (itemCount <= 0) return -1;
    if (key === "Home") return 0;
    if (key === "End") return itemCount - 1;
    if (key === "ArrowDown") return current < 0 ? 0 : (current + 1) % itemCount;
    return current < 0 ? itemCount - 1 : (current - 1 + itemCount) % itemCount;
  }

  // src/web-ui/react/folder-picker/repository.ts
  function messageFromPayload(payload, fallback) {
    if (payload && typeof payload === "object" && "error" in payload && typeof payload.error === "string" && payload.error.trim()) {
      return payload.error;
    }
    return fallback;
  }
  function normalizeListing(payload, requestedPath) {
    if (!payload || typeof payload !== "object") {
      throw new Error("\u76EE\u5F55\u670D\u52A1\u8FD4\u56DE\u4E86\u65E0\u6548\u6570\u636E\u3002");
    }
    const record2 = payload;
    const currentPath = typeof record2.currentPath === "string" && record2.currentPath.trim() ? record2.currentPath : requestedPath;
    const rawItems = Array.isArray(record2.items) ? record2.items : [];
    const items = [];
    for (const rawItem of rawItems) {
      if (!rawItem || typeof rawItem !== "object") continue;
      const item = rawItem;
      if (typeof item.path !== "string" || !item.path.trim() || typeof item.name !== "string" || item.type !== "parent" && item.type !== "dir") continue;
      items.push({ path: item.path, name: item.name, type: item.type });
    }
    return { currentPath, items };
  }
  var HttpFolderPickerRepository = class {
    constructor(fetchImpl = (input, init) => globalThis.fetch(input, init)) {
      __publicField(this, "fetchImpl", fetchImpl);
    }
    async list(path, options = {}) {
      const requestedPath = path.trim();
      if (!requestedPath) throw new Error("\u8BF7\u8F93\u5165\u5DE5\u4F5C\u76EE\u5F55\u3002");
      const response = await this.fetchImpl(`/api/folders?q=${encodeURIComponent(requestedPath)}`, {
        credentials: "same-origin",
        signal: options.signal
      });
      const payload = await response.json().catch(() => null);
      if (!response.ok) {
        throw new Error(messageFromPayload(payload, "\u65E0\u6CD5\u8BFB\u53D6\u8BE5\u76EE\u5F55\u3002"));
      }
      return normalizeListing(payload, requestedPath);
    }
  };
  var httpFolderPickerRepository = new HttpFolderPickerRepository();

  // src/web-ui/react/folder-picker/host.tsx
  var import_jsx_runtime31 = __toESM(require_jsx_runtime(), 1);
  var NAVIGATION_KEYS = /* @__PURE__ */ new Set([
    "ArrowDown",
    "ArrowUp",
    "Home",
    "End"
  ]);
  function presentError2(error) {
    return error instanceof Error && error.message ? error.message : "\u65E0\u6CD5\u8BFB\u53D6\u8BE5\u76EE\u5F55\u3002";
  }
  function FolderPickerHost({ repository = httpFolderPickerRepository }) {
    const controller = (0, import_react11.useSyncExternalStore)(
      folderPickerStore.subscribe,
      folderPickerStore.getSnapshot,
      folderPickerStore.getSnapshot
    );
    const [path, setPath] = (0, import_react11.useState)("");
    const [listing, setListing] = (0, import_react11.useState)(null);
    const [loading, setLoading] = (0, import_react11.useState)(false);
    const [choosing, setChoosing] = (0, import_react11.useState)(false);
    const [error, setError] = (0, import_react11.useState)("");
    const [activeIndex, setActiveIndex] = (0, import_react11.useState)(-1);
    const itemRefs = (0, import_react11.useRef)([]);
    (0, import_react11.useEffect)(() => {
      if (!controller.open) return;
      setPath(controller.initialPath);
      setListing(null);
      setLoading(false);
      setChoosing(false);
      setError("");
      setActiveIndex(-1);
    }, [controller.open, controller.revision, controller.initialPath]);
    (0, import_react11.useEffect)(() => {
      if (!controller.open) return;
      const requestedPath = path.trim();
      if (!requestedPath) {
        setListing(null);
        setLoading(false);
        setError("\u8BF7\u8F93\u5165\u5DE5\u4F5C\u76EE\u5F55\u3002");
        setActiveIndex(-1);
        return;
      }
      const abort = new AbortController();
      setLoading(true);
      setError("");
      setActiveIndex(-1);
      const timer = window.setTimeout(() => {
        void repository.list(requestedPath, { signal: abort.signal }).then((result) => {
          if (abort.signal.aborted) return;
          setListing(result);
          setError("");
        }).catch((loadError) => {
          if (abort.signal.aborted) return;
          setListing(null);
          setError(presentError2(loadError));
        }).finally(() => {
          if (!abort.signal.aborted) setLoading(false);
        });
      }, 120);
      return () => {
        window.clearTimeout(timer);
        abort.abort();
      };
    }, [controller.open, path, repository]);
    (0, import_react11.useEffect)(() => {
      if (activeIndex < 0) return;
      itemRefs.current[activeIndex]?.scrollIntoView({ block: "nearest" });
    }, [activeIndex]);
    function navigate(pathToOpen) {
      setPath(pathToOpen);
      setListing(null);
      setError("");
      setActiveIndex(-1);
    }
    async function choose(pathToChoose) {
      if (choosing) return;
      folderPickerController.setDismissable(false);
      setChoosing(true);
      setError("");
      try {
        const applied = await folderPickerController.choose(pathToChoose);
        if (!applied) setError("\u65E0\u6CD5\u5E94\u7528\u5DE5\u4F5C\u76EE\u5F55\uFF0C\u8BF7\u5237\u65B0\u9875\u9762\u540E\u91CD\u8BD5\u3002");
      } catch (selectionError) {
        setError(presentError2(selectionError));
      } finally {
        folderPickerController.setDismissable(true);
        setChoosing(false);
      }
    }
    function activateItem(item) {
      if (item.type === "parent") {
        navigate(item.path);
        return;
      }
      void choose(item.path);
    }
    async function submit(event) {
      event.preventDefault();
      const requestedPath = path.trim();
      if (!requestedPath || choosing) {
        if (!requestedPath) setError("\u8BF7\u8F93\u5165\u5DE5\u4F5C\u76EE\u5F55\u3002");
        return;
      }
      folderPickerController.setDismissable(false);
      setChoosing(true);
      setError("");
      try {
        const validated = await repository.list(requestedPath);
        const applied = await folderPickerController.choose(validated.currentPath);
        if (!applied) setError("\u65E0\u6CD5\u5E94\u7528\u5DE5\u4F5C\u76EE\u5F55\uFF0C\u8BF7\u5237\u65B0\u9875\u9762\u540E\u91CD\u8BD5\u3002");
      } catch (selectionError) {
        setError(presentError2(selectionError));
      } finally {
        folderPickerController.setDismissable(true);
        setChoosing(false);
      }
    }
    function handleInputKeyDown(event) {
      if (NAVIGATION_KEYS.has(event.key)) {
        event.preventDefault();
        setActiveIndex((current) => nextFolderPickerIndex(
          current,
          listing?.items.length ?? 0,
          event.key
        ));
        return;
      }
      if (event.key !== "Enter" || activeIndex < 0) return;
      const activeItem = listing?.items[activeIndex];
      if (!activeItem) return;
      event.preventDefault();
      activateItem(activeItem);
    }
    const parent = listing?.items.find((item) => item.type === "parent") ?? null;
    return /* @__PURE__ */ (0, import_jsx_runtime31.jsx)(
      WandDialogSurface,
      {
        open: controller.open,
        onOpenChange: (open) => {
          if (!open) folderPickerController.close();
        },
        title: "\u9009\u62E9\u5DE5\u4F5C\u76EE\u5F55",
        description: "\u8F93\u5165\u8DEF\u5F84\u6216\u4ECE\u76EE\u5F55\u5EFA\u8BAE\u4E2D\u9009\u62E9\uFF0C\u540E\u7EED\u65B0\u4F1A\u8BDD\u4F1A\u4ECE\u8BE5\u76EE\u5F55\u542F\u52A8\u3002",
        className: "wand-folder-picker-dialog",
        overlayClassName: "wand-folder-picker-overlay",
        titleClassName: "wand-folder-picker-title",
        descriptionClassName: "wand-folder-picker-description",
        headerClassName: "wand-folder-picker-header",
        closeLabel: "\u5173\u95ED\u5DE5\u4F5C\u76EE\u5F55\u9009\u62E9\u5668",
        testId: "folder-picker-dialog",
        dismissable: !choosing,
        children: /* @__PURE__ */ (0, import_jsx_runtime31.jsxs)("form", { className: "wand-folder-picker-form", "aria-busy": loading || choosing, onSubmit: (event) => void submit(event), children: [
          /* @__PURE__ */ (0, import_jsx_runtime31.jsxs)("div", { className: "wand-folder-picker-quick", "aria-label": "\u5FEB\u6377\u76EE\u5F55", children: [
            /* @__PURE__ */ (0, import_jsx_runtime31.jsx)(WandButton, { size: "small", onClick: () => navigate("/tmp"), children: "\u4E34\u65F6\u76EE\u5F55 /tmp" }),
            /* @__PURE__ */ (0, import_jsx_runtime31.jsx)(WandButton, { size: "small", onClick: () => navigate("/"), children: "\u6839\u76EE\u5F55 /" }),
            /* @__PURE__ */ (0, import_jsx_runtime31.jsx)(
              WandButton,
              {
                size: "small",
                disabled: !parent || loading,
                onClick: () => {
                  if (parent) navigate(parent.path);
                },
                children: "\u8FD4\u56DE\u4E0A\u7EA7"
              }
            )
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime31.jsxs)("label", { className: "wand-folder-picker-field", htmlFor: "wand-folder-picker-input", children: [
            /* @__PURE__ */ (0, import_jsx_runtime31.jsx)("span", { children: "\u5DE5\u4F5C\u76EE\u5F55" }),
            /* @__PURE__ */ (0, import_jsx_runtime31.jsx)(
              "input",
              {
                id: "wand-folder-picker-input",
                className: "wand-folder-picker-input",
                "data-wand-autofocus": true,
                autoFocus: true,
                autoComplete: "off",
                autoCorrect: "off",
                autoCapitalize: "off",
                spellCheck: false,
                "aria-invalid": error ? "true" : "false",
                "aria-controls": "wand-folder-picker-options",
                "aria-activedescendant": activeIndex >= 0 ? `wand-folder-picker-option-${activeIndex}` : void 0,
                value: path,
                onChange: (event) => setPath(event.currentTarget.value),
                onKeyDown: handleInputKeyDown
              }
            )
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime31.jsx)(
            "div",
            {
              id: "wand-folder-picker-options",
              className: "wand-folder-picker-options",
              role: "listbox",
              "aria-label": "\u76EE\u5F55\u5EFA\u8BAE",
              children: loading ? /* @__PURE__ */ (0, import_jsx_runtime31.jsx)("div", { className: "wand-folder-picker-state", role: "status", children: "\u6B63\u5728\u52A0\u8F7D\u76EE\u5F55\u2026" }) : listing && listing.items.length > 0 ? listing.items.map((item, index2) => /* @__PURE__ */ (0, import_jsx_runtime31.jsxs)(
                "button",
                {
                  ref: (element) => {
                    itemRefs.current[index2] = element;
                  },
                  id: `wand-folder-picker-option-${index2}`,
                  type: "button",
                  role: "option",
                  "aria-selected": activeIndex === index2,
                  className: `wand-folder-picker-option${activeIndex === index2 ? " active" : ""}`,
                  onMouseEnter: () => setActiveIndex(index2),
                  onClick: () => activateItem(item),
                  children: [
                    /* @__PURE__ */ (0, import_jsx_runtime31.jsx)("span", { "aria-hidden": "true", children: item.type === "parent" ? "\u21A9" : "\u25B8" }),
                    /* @__PURE__ */ (0, import_jsx_runtime31.jsx)("span", { children: item.type === "parent" ? "..\uFF08\u8FD4\u56DE\u4E0A\u7EA7\u76EE\u5F55\uFF09" : item.name }),
                    /* @__PURE__ */ (0, import_jsx_runtime31.jsx)("code", { children: item.path })
                  ]
                },
                `${item.type}:${item.path}`
              )) : error ? null : /* @__PURE__ */ (0, import_jsx_runtime31.jsx)("div", { className: "wand-folder-picker-state", children: "\u5F53\u524D\u76EE\u5F55\u6CA1\u6709\u5B50\u76EE\u5F55\u3002" })
            }
          ),
          error ? /* @__PURE__ */ (0, import_jsx_runtime31.jsx)("p", { className: "wand-folder-picker-error", role: "alert", children: error }) : null,
          /* @__PURE__ */ (0, import_jsx_runtime31.jsxs)("div", { className: "wand-folder-picker-footer", children: [
            /* @__PURE__ */ (0, import_jsx_runtime31.jsx)("span", { children: listing?.currentPath ?? path }),
            /* @__PURE__ */ (0, import_jsx_runtime31.jsx)(WandButton, { kind: "primary", type: "submit", disabled: loading || choosing || !path.trim(), children: choosing ? "\u6B63\u5728\u5E94\u7528\u2026" : "\u4F7F\u7528\u6B64\u76EE\u5F55" })
          ] })
        ] })
      }
    );
  }

  // src/web-ui/react/quick-commit/host.tsx
  var import_react12 = __toESM(require_react(), 1);

  // src/web-ui/react/quick-commit/controller.ts
  var runtime3 = null;
  var snapshot5 = {
    open: false,
    dismissable: true,
    context: null,
    revision: 0
  };
  var listeners5 = /* @__PURE__ */ new Set();
  function publish5(open, context) {
    snapshot5 = { open, dismissable: true, context, revision: snapshot5.revision + 1 };
    for (const listener of listeners5) listener();
  }
  function publishDismissable3(dismissable) {
    if (!snapshot5.open || snapshot5.dismissable === dismissable) return;
    snapshot5 = { ...snapshot5, dismissable };
    for (const listener of listeners5) listener();
  }
  var quickCommitController = {
    open(context) {
      const sessionId = context.sessionId.trim();
      if (!runtime3 || !sessionId) return false;
      if (snapshot5.open && snapshot5.context?.sessionId === sessionId) return true;
      const nextContext = { sessionId };
      runtime3.onOpen(nextContext);
      publish5(true, nextContext);
      return true;
    },
    close() {
      if (!snapshot5.open || !snapshot5.context) return;
      const context = snapshot5.context;
      publish5(false, null);
      runtime3?.onClose(context);
    },
    closeIfOpen() {
      if (!snapshot5.open || !snapshot5.dismissable) return false;
      this.close();
      return true;
    },
    closeTopmost() {
      if (!snapshot5.open) return false;
      if (snapshot5.dismissable) this.close();
      return true;
    },
    isOpen() {
      return snapshot5.open;
    },
    setDismissable(dismissable) {
      publishDismissable3(dismissable);
    }
  };
  var quickCommitStore = {
    subscribe(listener) {
      listeners5.add(listener);
      return () => listeners5.delete(listener);
    },
    getSnapshot() {
      return snapshot5;
    },
    getRuntime() {
      return runtime3;
    }
  };
  function configureQuickCommitRuntime(adapter) {
    runtime3 = adapter;
    return () => {
      if (runtime3 !== adapter) return;
      if (snapshot5.open) quickCommitController.close();
      runtime3 = null;
    };
  }

  // src/web-ui/react/quick-commit/model.ts
  var ACTIONS = {
    commit: {
      action: "commit",
      label: "Commit",
      verb: "\u4EC5\u63D0\u4EA4",
      withTag: false,
      push: false
    },
    "commit-tag": {
      action: "commit-tag",
      label: "Commit + Tag",
      verb: "\u63D0\u4EA4\u5E76\u6253 Tag",
      withTag: true,
      push: false
    },
    "commit-push": {
      action: "commit-push",
      label: "Commit + Push",
      verb: "\u63D0\u4EA4\u5E76\u63A8\u9001",
      withTag: false,
      push: true
    },
    "commit-tag-push": {
      action: "commit-tag-push",
      label: "Commit + Tag + Push",
      verb: "\u63D0\u4EA4\u3001\u6253 Tag \u5E76\u63A8\u9001",
      withTag: true,
      push: true
    }
  };
  var QUICK_COMMIT_ACTIONS = Object.values(ACTIONS);
  function normalizeQuickCommitAction(value) {
    return typeof value === "string" && value in ACTIONS ? value : "commit";
  }
  function quickCommitActionMeta(value) {
    return ACTIONS[normalizeQuickCommitAction(value)];
  }
  function buildQuickCommitInput(form, action, includeSubmodule) {
    const meta = quickCommitActionMeta(action);
    const message = form.message.trim();
    const tag = meta.withTag ? form.tag.trim() : "";
    return {
      autoMessage: !message,
      customMessage: message,
      tag,
      autoTag: meta.withTag && !tag,
      push: meta.push,
      submodule: includeSubmodule
    };
  }
  function hasQuickCommitChanges(status) {
    return !!status && status.isGit && status.modifiedCount > 0;
  }
  function buildQuickCommitOutcome(action, includeSubmodule, form, before, response) {
    return {
      action,
      includeSubmodule,
      pushed: response.pushed,
      pushError: response.pushError,
      commitHash: response.commit?.hash.slice(0, 7) ?? "",
      commitMessage: response.commit?.message || form.message.trim(),
      tagName: response.tag?.name ?? "",
      oldTag: before.latestTag,
      oldCommitHash: before.lastCommit?.shortHash || before.head.slice(0, 7),
      oldCommitSubject: before.lastCommit?.subject ?? "",
      submoduleCount: response.submoduleCommits.length
    };
  }
  function quickCommitStatusBadge(status) {
    const trimmed = status.trim();
    if (trimmed === "??") return { letter: "U", tone: "untracked", label: "\u672A\u8DDF\u8E2A" };
    if (trimmed === "!!") return { letter: "I", tone: "ignored", label: "\u5DF2\u5FFD\u7565" };
    const letter = [...status].find((character) => character !== "." && character !== " ")?.toUpperCase() || trimmed[0]?.toUpperCase() || "?";
    const map = {
      A: { tone: "added", label: "\u65B0\u589E" },
      M: { tone: "modified", label: "\u4FEE\u6539" },
      D: { tone: "deleted", label: "\u5220\u9664" },
      R: { tone: "renamed", label: "\u91CD\u547D\u540D" },
      C: { tone: "renamed", label: "\u590D\u5236" },
      T: { tone: "modified", label: "\u7C7B\u578B\u53D8\u66F4" },
      U: { tone: "deleted", label: "\u51B2\u7A81" }
    };
    return { letter, ...map[letter] ?? { tone: "other", label: "\u5DF2\u66F4\u6539" } };
  }

  // src/web-ui/react/quick-commit/repository.ts
  function isRecord2(value) {
    return !!value && typeof value === "object" && !Array.isArray(value);
  }
  function text2(value, fallback = "") {
    return typeof value === "string" ? value : fallback;
  }
  function finiteNumber(value, fallback = 0) {
    return typeof value === "number" && Number.isFinite(value) ? value : fallback;
  }
  async function readRecord(response, fallback) {
    let value;
    try {
      value = await response.json();
    } catch {
      throw new Error(`${fallback} (HTTP ${response.status})`);
    }
    const record2 = isRecord2(value) ? value : {};
    if (!response.ok) throw new Error(text2(record2.error, `${fallback} (HTTP ${response.status})`));
    return record2;
  }
  function normalizeFiles(value) {
    if (!Array.isArray(value)) return [];
    return value.flatMap((item) => {
      if (!isRecord2(item) || typeof item.path !== "string") return [];
      const rawState = isRecord2(item.submoduleState) ? item.submoduleState : null;
      return [{
        path: item.path,
        status: text2(item.status),
        isSubmodule: item.isSubmodule === true,
        submoduleState: rawState ? {
          commitChanged: rawState.commitChanged === true,
          hasTrackedChanges: rawState.hasTrackedChanges === true,
          hasUntracked: rawState.hasUntracked === true
        } : void 0
      }];
    });
  }
  function normalizeQuickCommitStatus(value) {
    const record2 = isRecord2(value) ? value : {};
    const files = normalizeFiles(record2.files);
    const rawLastCommit = isRecord2(record2.lastCommit) ? record2.lastCommit : null;
    const lastCommit = rawLastCommit ? {
      hash: text2(rawLastCommit.hash),
      shortHash: text2(rawLastCommit.shortHash),
      subject: text2(rawLastCommit.subject)
    } : void 0;
    return {
      isGit: record2.isGit === true,
      branch: text2(record2.branch),
      modifiedCount: Math.max(0, finiteNumber(record2.modifiedCount, files.length)),
      files,
      head: text2(record2.head),
      ahead: Math.max(0, finiteNumber(record2.ahead)),
      behind: Math.max(0, finiteNumber(record2.behind)),
      lastCommit,
      latestTag: text2(record2.latestTag),
      hasSubmodule: record2.hasSubmodule === true || files.some((file) => file.isSubmodule),
      error: typeof record2.error === "string" ? record2.error : void 0
    };
  }
  var HttpQuickCommitRepository = class {
    constructor(fetchImpl = (input, init) => fetch(input, init)) {
      __publicField(this, "fetchImpl", fetchImpl);
    }
    async loadStatus(sessionId, options = {}) {
      const response = await this.fetchImpl(
        `/api/sessions/${encodeURIComponent(sessionId)}/git-status`,
        { credentials: "same-origin", signal: options.signal }
      );
      return normalizeQuickCommitStatus(await readRecord(response, "\u65E0\u6CD5\u52A0\u8F7D Git \u72B6\u6001\u3002"));
    }
    async generate(sessionId, options = {}) {
      const response = await this.fetchImpl(
        `/api/sessions/${encodeURIComponent(sessionId)}/generate-commit-message`,
        {
          method: "POST",
          credentials: "same-origin",
          headers: { "Content-Type": "application/json" },
          body: "{}",
          signal: options.signal
        }
      );
      const data = await readRecord(response, "AI \u751F\u6210\u5931\u8D25\u3002");
      return { message: text2(data.message), suggestedTag: text2(data.suggestedTag).trim() };
    }
    async commit(sessionId, input) {
      const response = await this.fetchImpl(
        `/api/sessions/${encodeURIComponent(sessionId)}/quick-commit`,
        {
          method: "POST",
          credentials: "same-origin",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify(input)
        }
      );
      const data = await readRecord(response, "\u5FEB\u6377\u63D0\u4EA4\u5931\u8D25\u3002");
      const commit = isRecord2(data.commit) && typeof data.commit.hash === "string" ? { hash: data.commit.hash, message: text2(data.commit.message) } : void 0;
      const tag = isRecord2(data.tag) && typeof data.tag.name === "string" ? { name: data.tag.name } : void 0;
      const submoduleCommits = Array.isArray(data.submoduleCommits) ? data.submoduleCommits.flatMap((item) => isRecord2(item) && typeof item.path === "string" && typeof item.hash === "string" ? [{ path: item.path, hash: item.hash }] : []) : [];
      return {
        ok: data.ok !== false,
        commit,
        tag,
        pushed: data.pushed === true,
        pushError: text2(data.pushError),
        submoduleCommits
      };
    }
    async push(sessionId, input) {
      const response = await this.fetchImpl(
        `/api/sessions/${encodeURIComponent(sessionId)}/git/push`,
        {
          method: "POST",
          credentials: "same-origin",
          headers: { "Content-Type": "application/json" },
          body: JSON.stringify(input)
        }
      );
      const data = await readRecord(response, "\u63A8\u9001\u5931\u8D25\u3002");
      return {
        ok: data.ok !== false,
        pushedCommits: data.pushedCommits === true,
        pushedTags: data.pushedTags === true,
        error: text2(data.error)
      };
    }
  };
  var httpQuickCommitRepository = new HttpQuickCommitRepository();

  // src/web-ui/react/quick-commit/host.tsx
  var import_jsx_runtime32 = __toESM(require_jsx_runtime(), 1);
  var EMPTY_FORM = { message: "", tag: "", tagEdited: false };
  function presentError3(error, fallback) {
    if (!(error instanceof Error) || !error.message || error.message === "Failed to fetch") return fallback;
    return error.message;
  }
  function statusDescription(status) {
    if (!status) return "\u52A0\u8F7D Git \u72B6\u6001\u5E76\u51C6\u5907\u63D0\u4EA4\u3002";
    const parts = [
      status.branch || "(no branch)",
      status.modifiedCount > 0 ? `${status.modifiedCount} \u4E2A\u6539\u52A8` : "\u5DE5\u4F5C\u533A\u5E72\u51C0"
    ];
    if (status.ahead > 0) parts.push(`\u2191${status.ahead}`);
    if (status.behind > 0) parts.push(`\u2193${status.behind}`);
    return parts.join(" \xB7 ");
  }
  function commitSummary(outcome) {
    const submodule = outcome.submoduleCount > 0 ? `\u5DF2\u5148\u63D0\u4EA4 ${outcome.submoduleCount} \u4E2A submodule\uFF0C` : "";
    const commit = outcome.commitHash ? ` ${outcome.commitHash}` : "";
    const tag = outcome.tagName ? `\uFF0C\u5DF2\u6253 Tag ${outcome.tagName}` : "";
    return `${submodule}\u5DF2\u63D0\u4EA4${commit}${tag}`;
  }
  function CommitValue({ hash, subject, empty }) {
    if (!hash) return /* @__PURE__ */ (0, import_jsx_runtime32.jsx)("span", { className: "wand-quick-muted", children: empty });
    return /* @__PURE__ */ (0, import_jsx_runtime32.jsxs)("span", { className: "wand-quick-value-stack", children: [
      /* @__PURE__ */ (0, import_jsx_runtime32.jsx)("code", { children: hash }),
      subject ? /* @__PURE__ */ (0, import_jsx_runtime32.jsx)("span", { children: subject }) : null
    ] });
  }
  function ResultPair({
    label,
    before,
    after
  }) {
    return /* @__PURE__ */ (0, import_jsx_runtime32.jsxs)("div", { className: "wand-quick-result-pair", children: [
      /* @__PURE__ */ (0, import_jsx_runtime32.jsx)("span", { className: "wand-quick-result-label", children: label }),
      /* @__PURE__ */ (0, import_jsx_runtime32.jsxs)("div", { className: "wand-quick-result-flow", children: [
        /* @__PURE__ */ (0, import_jsx_runtime32.jsx)("div", { children: before }),
        /* @__PURE__ */ (0, import_jsx_runtime32.jsx)("span", { "aria-hidden": "true", children: "\u2192" }),
        /* @__PURE__ */ (0, import_jsx_runtime32.jsx)("div", { children: after })
      ] })
    ] });
  }
  function ChangedFiles({ status }) {
    return /* @__PURE__ */ (0, import_jsx_runtime32.jsxs)("section", { className: "wand-quick-files", "aria-labelledby": "wand-quick-files-title", children: [
      /* @__PURE__ */ (0, import_jsx_runtime32.jsxs)("div", { className: "wand-quick-section-heading", children: [
        /* @__PURE__ */ (0, import_jsx_runtime32.jsx)("h3", { id: "wand-quick-files-title", children: "\u6539\u52A8\u6587\u4EF6" }),
        /* @__PURE__ */ (0, import_jsx_runtime32.jsx)("span", { children: status.modifiedCount })
      ] }),
      status.files.length > 0 ? /* @__PURE__ */ (0, import_jsx_runtime32.jsx)("ul", { className: "wand-quick-file-list", children: status.files.map((file, index2) => {
        const badge = quickCommitStatusBadge(file.status);
        const submoduleLabels = file.submoduleState ? [
          file.submoduleState.commitChanged ? "\u65B0\u6307\u9488" : "",
          file.submoduleState.hasTrackedChanges ? "dirty" : "",
          file.submoduleState.hasUntracked ? "\u672A\u8DDF\u8E2A" : ""
        ].filter(Boolean) : [];
        return /* @__PURE__ */ (0, import_jsx_runtime32.jsxs)("li", { title: file.path, children: [
          /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(
            "span",
            {
              className: `wand-quick-file-badge wand-quick-file-badge-${badge.tone}`,
              title: badge.label,
              "aria-label": badge.label,
              children: badge.letter
            }
          ),
          /* @__PURE__ */ (0, import_jsx_runtime32.jsx)("span", { className: "wand-quick-file-path", children: file.path }),
          file.isSubmodule ? /* @__PURE__ */ (0, import_jsx_runtime32.jsxs)("span", { className: "wand-quick-submodule-badge", children: [
            "submodule",
            submoduleLabels.length ? ` \xB7 ${submoduleLabels.join(" / ")}` : ""
          ] }) : null
        ] }, `${file.path}-${index2}`);
      }) }) : /* @__PURE__ */ (0, import_jsx_runtime32.jsx)("p", { className: "wand-quick-empty", children: "\u6CA1\u6709\u53EF\u63D0\u4EA4\u7684\u6539\u52A8\u3002" })
    ] });
  }
  function QuickCommitHost({ repository = httpQuickCommitRepository }) {
    const controller = (0, import_react12.useSyncExternalStore)(
      quickCommitStore.subscribe,
      quickCommitStore.getSnapshot,
      quickCommitStore.getSnapshot
    );
    const [status, setStatus] = (0, import_react12.useState)(null);
    const [form, setForm] = (0, import_react12.useState)(EMPTY_FORM);
    const [action, setAction] = (0, import_react12.useState)("commit");
    const [includeSubmodule, setIncludeSubmodule] = (0, import_react12.useState)(false);
    const [outcome, setOutcome] = (0, import_react12.useState)(null);
    const [loading, setLoading] = (0, import_react12.useState)(false);
    const [generating, setGenerating] = (0, import_react12.useState)(false);
    const [submitting, setSubmitting] = (0, import_react12.useState)(false);
    const [pushing, setPushing] = (0, import_react12.useState)(false);
    const [error, setError] = (0, import_react12.useState)("");
    const [pushError, setPushError] = (0, import_react12.useState)("");
    const generationAbort = (0, import_react12.useRef)(null);
    const messageInput = (0, import_react12.useRef)(null);
    const context = controller.context;
    (0, import_react12.useEffect)(() => {
      if (!controller.open || !context) return;
      const abort = new AbortController();
      generationAbort.current?.abort();
      generationAbort.current = null;
      setStatus(null);
      setForm(EMPTY_FORM);
      setAction("commit");
      setIncludeSubmodule(false);
      setOutcome(null);
      setLoading(true);
      setGenerating(false);
      setSubmitting(false);
      setPushing(false);
      setError("");
      setPushError("");
      void repository.loadStatus(context.sessionId, { signal: abort.signal }).then((loaded) => {
        if (abort.signal.aborted) return;
        setStatus(loaded);
        if (!loaded.isGit) setError(loaded.error || "\u5F53\u524D\u76EE\u5F55\u4E0D\u662F Git \u4ED3\u5E93\u3002");
      }).catch((loadError) => {
        if (!abort.signal.aborted) setError(presentError3(loadError, "\u65E0\u6CD5\u52A0\u8F7D Git \u72B6\u6001\u3002"));
      }).finally(() => {
        if (!abort.signal.aborted) setLoading(false);
      });
      return () => {
        abort.abort();
        generationAbort.current?.abort();
        generationAbort.current = null;
      };
    }, [controller.open, controller.revision, context?.sessionId, repository]);
    (0, import_react12.useEffect)(() => {
      if (!controller.open || loading || !status || outcome) return;
      const frame = window.requestAnimationFrame(() => messageInput.current?.focus());
      return () => window.cancelAnimationFrame(frame);
    }, [controller.open, loading, outcome, status]);
    const selectedMeta = (0, import_react12.useMemo)(() => quickCommitActionMeta(action), [action]);
    const busy = submitting || pushing;
    const canCommit = hasQuickCommitChanges(status) && !busy && !loading;
    async function reloadStatus(sessionId) {
      quickCommitStore.getRuntime()?.onRepositoryChanged(sessionId);
      try {
        const loaded = await repository.loadStatus(sessionId);
        if (quickCommitStore.getSnapshot().context?.sessionId === sessionId) setStatus(loaded);
      } catch {
      }
    }
    async function generateSuggestion() {
      if (!context || generating || submitting) return;
      generationAbort.current?.abort();
      const abort = new AbortController();
      generationAbort.current = abort;
      setGenerating(true);
      setError("");
      try {
        const suggestion = await repository.generate(context.sessionId, { signal: abort.signal });
        if (abort.signal.aborted) return;
        setForm((current) => ({
          message: current.message.trim() ? current.message : suggestion.message,
          tag: current.tagEdited ? current.tag : suggestion.suggestedTag || current.tag,
          tagEdited: current.tagEdited
        }));
        if (suggestion.suggestedTag) setAction("commit-tag");
      } catch (generateError) {
        if (!abort.signal.aborted) setError(presentError3(generateError, "AI \u751F\u6210\u5931\u8D25\u3002"));
      } finally {
        if (!abort.signal.aborted) setGenerating(false);
        if (generationAbort.current === abort) generationAbort.current = null;
      }
    }
    async function submit(event) {
      event?.preventDefault();
      if (!context || !status || !canCommit) return;
      quickCommitController.setDismissable(false);
      setSubmitting(true);
      setError("");
      setPushError("");
      try {
        const response = await repository.commit(
          context.sessionId,
          buildQuickCommitInput(form, action, includeSubmodule)
        );
        if (!response.ok) throw new Error("\u5FEB\u6377\u63D0\u4EA4\u5931\u8D25\u3002");
        const nextOutcome = buildQuickCommitOutcome(
          action,
          includeSubmodule,
          form,
          status,
          response
        );
        const summary = commitSummary(nextOutcome);
        if (selectedMeta.push && !response.pushError) {
          quickCommitStore.getRuntime()?.toast(`${summary}\uFF0C\u5DF2\u63A8\u9001\u3002`, "success");
          void reloadStatus(context.sessionId);
          quickCommitController.close();
          return;
        }
        setOutcome(nextOutcome);
        if (response.pushError) {
          setPushError(response.pushError);
          quickCommitStore.getRuntime()?.toast(`${summary}\uFF1Bpush \u5931\u8D25\uFF1A${response.pushError}`, "error");
        } else {
          quickCommitStore.getRuntime()?.toast(`${summary}\u3002`, "success");
        }
        await reloadStatus(context.sessionId);
      } catch (commitError) {
        setError(presentError3(commitError, "\u5FEB\u6377\u63D0\u4EA4\u5931\u8D25\u3002"));
      } finally {
        quickCommitController.setDismissable(true);
        setSubmitting(false);
      }
    }
    async function pushAndClose() {
      if (!context || !outcome || pushing) return;
      quickCommitController.setDismissable(false);
      setPushing(true);
      setPushError("");
      try {
        const response = await repository.push(context.sessionId, {
          pushCommits: true,
          pushTags: !!outcome.tagName,
          submodule: outcome.includeSubmodule,
          tag: outcome.tagName
        });
        if (!response.ok || response.error) {
          const message = response.error || "\u63A8\u9001\u5931\u8D25\u3002";
          setPushError(message);
          quickCommitStore.getRuntime()?.toast(`\u63A8\u9001\u5931\u8D25\uFF1A${message}`, "error");
          return;
        }
        const pushed = [response.pushedCommits ? "commits" : "", response.pushedTags ? "tags" : ""].filter(Boolean).join(" \u548C ") || "\uFF08\u65E0\u5185\u5BB9\uFF09";
        quickCommitStore.getRuntime()?.toast(`\u5DF2\u63A8\u9001 ${pushed}`, "success");
        void reloadStatus(context.sessionId);
        quickCommitController.close();
      } catch (pushFailure) {
        const message = presentError3(pushFailure, "\u63A8\u9001\u5931\u8D25\u3002");
        setPushError(message);
        quickCommitStore.getRuntime()?.toast(message, "error");
      } finally {
        quickCommitController.setDismissable(true);
        setPushing(false);
      }
    }
    function submitShortcut(event) {
      if (event.key !== "Enter" || !event.metaKey && !event.ctrlKey) return;
      event.preventDefault();
      void submit();
    }
    return /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(
      WandDialogSurface,
      {
        open: controller.open,
        onOpenChange: (open) => {
          if (!open) quickCommitController.close();
        },
        title: "\u5FEB\u6377\u63D0\u4EA4",
        description: statusDescription(status),
        className: "wand-quick-dialog",
        overlayClassName: "wand-quick-overlay",
        titleClassName: "wand-quick-title",
        descriptionClassName: "wand-quick-description",
        headerClassName: "wand-quick-header",
        closeLabel: "\u5173\u95ED\u5FEB\u6377\u63D0\u4EA4",
        testId: "quick-commit-dialog",
        dismissable: !busy,
        children: loading ? /* @__PURE__ */ (0, import_jsx_runtime32.jsx)("div", { className: "wand-quick-loading", role: "status", children: "\u6B63\u5728\u52A0\u8F7D Git \u72B6\u6001\u2026" }) : outcome ? /* @__PURE__ */ (0, import_jsx_runtime32.jsxs)("section", { className: "wand-quick-result", "aria-label": "\u63D0\u4EA4\u7ED3\u679C", children: [
          /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(
            ResultPair,
            {
              label: "Commit",
              before: /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(CommitValue, { hash: outcome.oldCommitHash, subject: outcome.oldCommitSubject, empty: "\u65E0" }),
              after: /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(CommitValue, { hash: outcome.commitHash, subject: outcome.commitMessage, empty: "\u65E0" })
            }
          ),
          /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(
            ResultPair,
            {
              label: "Tag",
              before: outcome.oldTag ? /* @__PURE__ */ (0, import_jsx_runtime32.jsx)("code", { children: outcome.oldTag }) : /* @__PURE__ */ (0, import_jsx_runtime32.jsx)("span", { className: "wand-quick-muted", children: "\u65E0 tag" }),
              after: outcome.tagName ? /* @__PURE__ */ (0, import_jsx_runtime32.jsx)("code", { children: outcome.tagName }) : /* @__PURE__ */ (0, import_jsx_runtime32.jsx)("span", { className: "wand-quick-muted", children: "\u672A\u6253 tag" })
            }
          ),
          outcome.submoduleCount > 0 ? /* @__PURE__ */ (0, import_jsx_runtime32.jsxs)("p", { className: "wand-quick-result-note", children: [
            "\u5DF2\u63D0\u4EA4 ",
            outcome.submoduleCount,
            " \u4E2A submodule\u3002"
          ] }) : null,
          outcome.pushError || pushError ? /* @__PURE__ */ (0, import_jsx_runtime32.jsx)("p", { className: "wand-quick-error", role: "alert", children: pushError || outcome.pushError }) : null,
          /* @__PURE__ */ (0, import_jsx_runtime32.jsxs)("div", { className: "wand-quick-result-actions", children: [
            /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(WandButton, { kind: "ghost", disabled: pushing, onClick: () => quickCommitController.close(), children: "\u5173\u95ED" }),
            outcome.pushed ? /* @__PURE__ */ (0, import_jsx_runtime32.jsx)("span", { className: "wand-quick-pushed", children: "\u5DF2\u63A8\u9001" }) : /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(WandButton, { kind: "primary", disabled: pushing, onClick: () => void pushAndClose(), children: pushing ? "\u63A8\u9001\u4E2D\u2026" : "Push & Close" })
          ] })
        ] }) : status ? /* @__PURE__ */ (0, import_jsx_runtime32.jsxs)("form", { className: "wand-quick-form", "aria-busy": busy, onSubmit: (event) => void submit(event), children: [
          /* @__PURE__ */ (0, import_jsx_runtime32.jsxs)("div", { className: "wand-quick-body", children: [
            /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(ChangedFiles, { status }),
            /* @__PURE__ */ (0, import_jsx_runtime32.jsxs)("section", { className: "wand-quick-editor", "aria-labelledby": "wand-quick-editor-title", children: [
              /* @__PURE__ */ (0, import_jsx_runtime32.jsxs)("div", { className: "wand-quick-section-heading", children: [
                /* @__PURE__ */ (0, import_jsx_runtime32.jsx)("h3", { id: "wand-quick-editor-title", children: "New" }),
                /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(
                  WandButton,
                  {
                    kind: "ghost",
                    size: "small",
                    disabled: generating || submitting || !hasQuickCommitChanges(status),
                    title: "AI \u751F\u6210 commit message \u4E0E tag",
                    onClick: () => void generateSuggestion(),
                    children: generating ? "\u751F\u6210\u4E2D\u2026" : "\u2726 AI"
                  }
                )
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime32.jsxs)("label", { className: "wand-quick-field", htmlFor: "wand-quick-message", children: [
                /* @__PURE__ */ (0, import_jsx_runtime32.jsx)("span", { children: "Commit message" }),
                /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(
                  "textarea",
                  {
                    id: "wand-quick-message",
                    ref: messageInput,
                    "data-wand-autofocus": "",
                    rows: 3,
                    value: form.message,
                    disabled: submitting,
                    placeholder: "\u7559\u7A7A\u5219\u81EA\u52A8\u751F\u6210",
                    onChange: (event) => setForm({ ...form, message: event.currentTarget.value }),
                    onKeyDown: submitShortcut
                  }
                )
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime32.jsxs)("label", { className: "wand-quick-field", htmlFor: "wand-quick-tag", children: [
                /* @__PURE__ */ (0, import_jsx_runtime32.jsx)("span", { children: "\u7248\u672C Tag" }),
                /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(
                  "input",
                  {
                    id: "wand-quick-tag",
                    type: "text",
                    value: form.tag,
                    disabled: submitting,
                    placeholder: "\u9009\u62E9 Tag \u52A8\u4F5C\u65F6\uFF0C\u7559\u7A7A\u5219\u81EA\u52A8\u751F\u6210",
                    autoComplete: "off",
                    spellCheck: false,
                    onChange: (event) => setForm({
                      ...form,
                      tag: event.currentTarget.value,
                      tagEdited: true
                    })
                  }
                )
              ] })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime32.jsxs)("fieldset", { className: "wand-quick-actions", children: [
              /* @__PURE__ */ (0, import_jsx_runtime32.jsx)("legend", { children: "\u6267\u884C\u52A8\u4F5C" }),
              /* @__PURE__ */ (0, import_jsx_runtime32.jsx)("div", { className: "wand-quick-action-grid", children: QUICK_COMMIT_ACTIONS.map((item) => /* @__PURE__ */ (0, import_jsx_runtime32.jsxs)(
                "label",
                {
                  className: action === item.action ? "is-selected" : void 0,
                  children: [
                    /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(
                      "input",
                      {
                        type: "radio",
                        name: "wand-quick-action",
                        value: item.action,
                        checked: action === item.action,
                        disabled: !hasQuickCommitChanges(status) || busy,
                        onChange: () => setAction(item.action)
                      }
                    ),
                    /* @__PURE__ */ (0, import_jsx_runtime32.jsx)("strong", { children: item.label }),
                    /* @__PURE__ */ (0, import_jsx_runtime32.jsx)("span", { children: item.verb })
                  ]
                },
                item.action
              )) }),
              status.hasSubmodule ? /* @__PURE__ */ (0, import_jsx_runtime32.jsxs)("div", { className: "wand-quick-submodule-toggle", children: [
                /* @__PURE__ */ (0, import_jsx_runtime32.jsxs)("div", { children: [
                  /* @__PURE__ */ (0, import_jsx_runtime32.jsx)("strong", { children: "\u5305\u542B Submodule" }),
                  /* @__PURE__ */ (0, import_jsx_runtime32.jsx)("span", { children: "\u9012\u5F52\u6267\u884C commit\u3001tag \u548C push\u3002" })
                ] }),
                /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(
                  WandSwitch,
                  {
                    id: "wand-quick-submodule",
                    checked: includeSubmodule,
                    disabled: busy,
                    ariaLabel: "\u5305\u542B Submodule",
                    onCheckedChange: setIncludeSubmodule
                  }
                )
              ] }) : null
            ] }),
            error ? /* @__PURE__ */ (0, import_jsx_runtime32.jsx)("p", { className: "wand-quick-error", role: "alert", children: error }) : null
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime32.jsxs)("footer", { className: "wand-quick-footer", children: [
            /* @__PURE__ */ (0, import_jsx_runtime32.jsx)("span", { children: hasQuickCommitChanges(status) ? "\u2318/Ctrl + Enter \u5FEB\u901F\u6267\u884C" : "\u5DE5\u4F5C\u533A\u5E72\u51C0\uFF0C\u65E0\u53EF\u63D0\u4EA4\u6539\u52A8" }),
            /* @__PURE__ */ (0, import_jsx_runtime32.jsxs)("div", { children: [
              /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(WandButton, { kind: "ghost", disabled: busy, onClick: () => quickCommitController.close(), children: "\u53D6\u6D88" }),
              /* @__PURE__ */ (0, import_jsx_runtime32.jsx)(WandButton, { kind: "primary", type: "submit", disabled: !canCommit, children: submitting ? form.message.trim() ? "\u6267\u884C\u4E2D\u2026" : "AI \u751F\u6210 + \u63D0\u4EA4\u4E2D\u2026" : selectedMeta.verb })
            ] })
          ] })
        ] }) : /* @__PURE__ */ (0, import_jsx_runtime32.jsx)("div", { className: "wand-quick-loading", children: error ? /* @__PURE__ */ (0, import_jsx_runtime32.jsx)("p", { className: "wand-quick-error", role: "alert", children: error }) : "\u6CA1\u6709\u53EF\u7528\u7684 Git \u72B6\u6001\u3002" })
      }
    );
  }

  // src/web-ui/react/worktree-merge/host.tsx
  var import_react13 = __toESM(require_react(), 1);

  // src/web-ui/react/worktree-merge/controller.ts
  var runtime4 = null;
  var snapshot6 = {
    open: false,
    dismissable: true,
    context: null,
    revision: 0
  };
  var listeners6 = /* @__PURE__ */ new Set();
  function publish6(open, context) {
    snapshot6 = { open, dismissable: true, context, revision: snapshot6.revision + 1 };
    for (const listener of listeners6) listener();
  }
  function publishDismissable4(dismissable) {
    if (!snapshot6.open || snapshot6.dismissable === dismissable) return;
    snapshot6 = { ...snapshot6, dismissable };
    for (const listener of listeners6) listener();
  }
  var worktreeMergeController = {
    open(context) {
      const sessionId = context.sessionId.trim();
      if (!sessionId) return false;
      const nextContext = { ...context, sessionId };
      if (snapshot6.open && snapshot6.context?.sessionId === sessionId && snapshot6.context.intent === nextContext.intent) return true;
      runtime4?.onOpen(nextContext);
      publish6(true, nextContext);
      return true;
    },
    close() {
      if (!snapshot6.open || !snapshot6.context) return;
      const context = snapshot6.context;
      publish6(false, null);
      runtime4?.onClose(context);
    },
    closeIfOpen() {
      if (!snapshot6.open || !snapshot6.dismissable) return false;
      this.close();
      return true;
    },
    closeTopmost() {
      if (!snapshot6.open) return false;
      if (snapshot6.dismissable) this.close();
      return true;
    },
    isOpen() {
      return snapshot6.open;
    },
    setDismissable(dismissable) {
      publishDismissable4(dismissable);
    }
  };
  var worktreeMergeStore = {
    subscribe(listener) {
      listeners6.add(listener);
      return () => listeners6.delete(listener);
    },
    getSnapshot() {
      return snapshot6;
    },
    getRuntime() {
      return runtime4;
    }
  };
  function configureWorktreeMergeRuntime(adapter) {
    runtime4 = adapter;
    return () => {
      if (runtime4 !== adapter) return;
      if (snapshot6.open) worktreeMergeController.close();
      runtime4 = null;
    };
  }

  // src/web-ui/react/worktree-merge/model.ts
  function worktreeMergeAvailability(context) {
    if (!context.sessionId.trim()) {
      return { allowed: false, reason: "\u672A\u627E\u5230\u5BF9\u5E94\u4F1A\u8BDD\u3002" };
    }
    if (context.sessionStatus === "running") {
      return { allowed: false, reason: "\u4F1A\u8BDD\u4ECD\u5728\u8FD0\u884C\uFF0C\u8BF7\u7ED3\u675F\u540E\u518D\u5408\u5E76\u6216\u6E05\u7406\u3002" };
    }
    if (context.mergeStatus === "merging") {
      return { allowed: false, reason: "Worktree \u6B63\u5728\u5408\u5E76\uFF0C\u8BF7\u7B49\u5F85\u5F53\u524D\u64CD\u4F5C\u5B8C\u6210\u3002" };
    }
    if (context.intent === "cleanup") {
      return context.cleanupPending === true ? { allowed: true, reason: "" } : { allowed: false, reason: "\u5F53\u524D worktree \u65E0\u9700\u8865\u507F\u6E05\u7406\u3002" };
    }
    if (!context.sourceBranch?.trim() || !context.worktreePath?.trim()) {
      return { allowed: false, reason: "\u8BE5\u4F1A\u8BDD\u6CA1\u6709\u53EF\u5408\u5E76\u7684 worktree \u4FE1\u606F\u3002" };
    }
    if (context.mergeStatus === "merged") {
      return context.cleanupPending ? { allowed: false, reason: "\u5206\u652F\u5DF2\u7ECF\u5408\u5E76\uFF0C\u8BF7\u6539\u4E3A\u91CD\u8BD5 worktree \u6E05\u7406\u3002" } : { allowed: false, reason: "\u8BE5 worktree \u5DF2\u5B8C\u6210\u5408\u5E76\u548C\u6E05\u7406\u3002" };
    }
    return { allowed: true, reason: "" };
  }
  function canConfirmWorktreeMerge(inspection) {
    return !!inspection && inspection.ok && inspection.recommendedAction === "merge" && inspection.aheadCount > 0 && !inspection.hasUncommittedChanges && !inspection.hasConflicts;
  }
  function worktreeMergeResultMessage(result) {
    const target = result.targetBranch || "\u4E3B\u5206\u652F";
    return result.cleanupDone ? `\u5DF2\u5408\u5E76\u5230 ${target} \u5E76\u5B8C\u6210 worktree \u6E05\u7406\u3002` : `\u5DF2\u5408\u5E76\u5230 ${target}\uFF0C\u4F46\u5DE5\u4F5C\u6811\u4ECD\u5F85\u6E05\u7406\u3002`;
  }
  function inspectionStatusMessage(inspection) {
    if (inspection.ok) return `\u68C0\u67E5\u901A\u8FC7\uFF0C\u53EF\u5C06 ${inspection.aheadCount} \u4E2A\u63D0\u4EA4\u5408\u5E76\u5230 ${inspection.targetBranch || "\u76EE\u6807\u5206\u652F"}\u3002`;
    if (inspection.reason) return inspection.reason;
    if (inspection.hasUncommittedChanges) return "Worktree \u4E2D\u4ECD\u6709\u672A\u63D0\u4EA4\u6539\u52A8\uFF0C\u8BF7\u5148\u63D0\u4EA4\u540E\u518D\u5408\u5E76\u3002";
    if (inspection.hasConflicts) return "\u68C0\u6D4B\u5230\u51B2\u7A81\u98CE\u9669\uFF0C\u8BF7\u5148\u624B\u52A8\u5904\u7406\u3002";
    if (inspection.aheadCount <= 0) return "\u5F53\u524D worktree \u6CA1\u6709\u53EF\u5408\u5E76\u7684\u65B0\u63D0\u4EA4\u3002";
    return "\u5F53\u524D worktree \u6682\u4E0D\u53EF\u5408\u5E76\u3002";
  }

  // src/web-ui/react/worktree-merge/repository.ts
  function isRecord3(value) {
    return !!value && typeof value === "object" && !Array.isArray(value);
  }
  function text3(value, fallback = "") {
    return typeof value === "string" ? value : fallback;
  }
  function finiteNumber2(value, fallback = 0) {
    return typeof value === "number" && Number.isFinite(value) ? value : fallback;
  }
  function recommendedAction(value) {
    return value === "noop" || value === "resolve-conflict" ? value : "merge";
  }
  function normalizeCommits(value) {
    if (!Array.isArray(value)) return [];
    return value.flatMap((item) => {
      if (!isRecord3(item) || typeof item.hash !== "string" || !item.hash) return [];
      return [{
        hash: item.hash,
        shortHash: text3(item.shortHash, item.hash.slice(0, 7)),
        subject: text3(item.subject, text3(item.message))
      }];
    });
  }
  function normalizeWorktreeMergeInspection(value) {
    const result = isRecord3(value) ? value : {};
    return {
      ok: result.ok === true,
      sourceBranch: text3(result.sourceBranch),
      targetBranch: text3(result.targetBranch),
      worktreePath: text3(result.worktreePath),
      repoRoot: text3(result.repoRoot),
      hasUncommittedChanges: result.hasUncommittedChanges === true,
      aheadCount: Math.max(0, finiteNumber2(result.aheadCount)),
      hasConflicts: result.hasConflicts === true,
      recommendedAction: recommendedAction(result.recommendedAction),
      reason: text3(result.reason),
      commits: normalizeCommits(result.commits)
    };
  }
  function normalizeWorktreeMergeResult(value) {
    const result = isRecord3(value) ? value : {};
    return {
      ok: result.ok === true,
      sourceBranch: text3(result.sourceBranch),
      targetBranch: text3(result.targetBranch),
      repoRoot: text3(result.repoRoot),
      mergeCommit: text3(result.mergeCommit),
      mergedAt: text3(result.mergedAt),
      cleanupDone: result.cleanupDone === true,
      conflict: result.conflict === true,
      errorCode: text3(result.errorCode),
      reason: text3(result.reason)
    };
  }
  var WorktreeMergeRepositoryError = class extends Error {
    constructor(message, code = "", result = null, status = 0) {
      super(message);
      __publicField(this, "code", code);
      __publicField(this, "result", result);
      __publicField(this, "status", status);
      this.name = "WorktreeMergeRepositoryError";
    }
  };
  async function readRecord2(response, fallback) {
    let value;
    try {
      value = await response.json();
    } catch {
      throw new WorktreeMergeRepositoryError(`${fallback} (HTTP ${response.status})`, "", null, response.status);
    }
    const record2 = isRecord3(value) ? value : {};
    if (!response.ok || typeof record2.error === "string") {
      throw new WorktreeMergeRepositoryError(
        text3(record2.error, `${fallback} (HTTP ${response.status})`),
        text3(record2.errorCode),
        isRecord3(record2.result) ? normalizeWorktreeMergeResult(record2.result) : null,
        response.status
      );
    }
    return record2;
  }
  var HttpWorktreeMergeRepository = class {
    constructor(fetchImpl = (input, init) => globalThis.fetch(input, init)) {
      __publicField(this, "fetchImpl", fetchImpl);
    }
    async inspect(sessionId, options = {}) {
      const response = await this.fetchImpl(
        `/api/sessions/${encodeURIComponent(sessionId)}/worktree/merge/check`,
        { method: "POST", credentials: "same-origin", signal: options.signal }
      );
      const data = await readRecord2(response, "\u65E0\u6CD5\u68C0\u67E5 worktree \u5408\u5E76\u72B6\u6001\u3002");
      if (!isRecord3(data.result)) throw new WorktreeMergeRepositoryError("\u670D\u52A1\u7AEF\u672A\u8FD4\u56DE\u5408\u5E76\u68C0\u67E5\u7ED3\u679C\u3002");
      return normalizeWorktreeMergeInspection(data.result);
    }
    async merge(sessionId) {
      const response = await this.fetchImpl(
        `/api/sessions/${encodeURIComponent(sessionId)}/worktree/merge`,
        {
          method: "POST",
          credentials: "same-origin",
          headers: { "Content-Type": "application/json" },
          body: "{}"
        }
      );
      const data = await readRecord2(response, "\u65E0\u6CD5\u5408\u5E76 worktree\u3002");
      if (!isRecord3(data.result)) throw new WorktreeMergeRepositoryError("\u670D\u52A1\u7AEF\u672A\u8FD4\u56DE worktree \u5408\u5E76\u7ED3\u679C\u3002");
      return normalizeWorktreeMergeResult(data.result);
    }
    async cleanup(sessionId) {
      const response = await this.fetchImpl(
        `/api/sessions/${encodeURIComponent(sessionId)}/worktree/cleanup`,
        { method: "POST", credentials: "same-origin" }
      );
      const data = await readRecord2(response, "\u65E0\u6CD5\u6E05\u7406 worktree\u3002");
      return { ok: data.ok === true };
    }
  };
  var httpWorktreeMergeRepository = new HttpWorktreeMergeRepository();

  // src/web-ui/react/worktree-merge/host.tsx
  var import_jsx_runtime33 = __toESM(require_jsx_runtime(), 1);
  function presentError4(error, fallback) {
    if (!(error instanceof Error) || !error.message || error.message === "Failed to fetch") return fallback;
    return error.message;
  }
  function MergeDetail({
    label,
    value,
    tone
  }) {
    const valueClassName = tone ? `wand-worktree-value wand-worktree-value-${tone}` : "wand-worktree-value";
    return /* @__PURE__ */ (0, import_jsx_runtime33.jsxs)("div", { className: "wand-worktree-row", children: [
      /* @__PURE__ */ (0, import_jsx_runtime33.jsx)("span", { className: "wand-worktree-label", children: label }),
      /* @__PURE__ */ (0, import_jsx_runtime33.jsx)("strong", { className: valueClassName, children: value || "-" })
    ] });
  }
  function InspectionDetails({ inspection }) {
    return /* @__PURE__ */ (0, import_jsx_runtime33.jsxs)(import_jsx_runtime33.Fragment, { children: [
      /* @__PURE__ */ (0, import_jsx_runtime33.jsx)(MergeDetail, { label: "\u76EE\u6807\u5206\u652F", value: inspection.targetBranch }),
      /* @__PURE__ */ (0, import_jsx_runtime33.jsx)(MergeDetail, { label: "\u5F85\u5408\u5E76\u63D0\u4EA4", value: String(inspection.aheadCount) }),
      /* @__PURE__ */ (0, import_jsx_runtime33.jsx)(
        MergeDetail,
        {
          label: "\u672A\u63D0\u4EA4\u6539\u52A8",
          value: inspection.hasUncommittedChanges ? "\u6709" : "\u65E0",
          tone: inspection.hasUncommittedChanges ? "warning" : "success"
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime33.jsx)(
        MergeDetail,
        {
          label: "\u51B2\u7A81\u98CE\u9669",
          value: inspection.hasConflicts ? "\u6709" : "\u65E0",
          tone: inspection.hasConflicts ? "error" : "success"
        }
      ),
      inspection.commits.length > 0 ? /* @__PURE__ */ (0, import_jsx_runtime33.jsxs)("section", { className: "wand-worktree-commits", "aria-labelledby": "worktree-merge-commits-title", children: [
        /* @__PURE__ */ (0, import_jsx_runtime33.jsx)("p", { className: "wand-worktree-commits-title", id: "worktree-merge-commits-title", children: /* @__PURE__ */ (0, import_jsx_runtime33.jsxs)("strong", { children: [
          "\u5F85\u5408\u5E76\u63D0\u4EA4\u5217\u8868\uFF08",
          inspection.commits.length,
          "\uFF09"
        ] }) }),
        /* @__PURE__ */ (0, import_jsx_runtime33.jsx)("div", { className: "wand-worktree-commit-list", role: "list", "aria-label": "\u5F85\u5408\u5E76\u63D0\u4EA4", children: inspection.commits.map((commit) => /* @__PURE__ */ (0, import_jsx_runtime33.jsxs)("div", { className: "wand-worktree-row", role: "listitem", children: [
          /* @__PURE__ */ (0, import_jsx_runtime33.jsx)("span", { children: /* @__PURE__ */ (0, import_jsx_runtime33.jsx)("code", { children: commit.shortHash }) }),
          /* @__PURE__ */ (0, import_jsx_runtime33.jsx)("strong", { className: "wand-worktree-value", children: commit.subject || commit.hash })
        ] }, commit.hash)) })
      ] }) : null,
      /* @__PURE__ */ (0, import_jsx_runtime33.jsx)("p", { className: `wand-worktree-status wand-worktree-status-${inspection.ok ? "success" : inspection.hasConflicts ? "error" : "warning"}`, children: inspectionStatusMessage(inspection) })
    ] });
  }
  function WorktreeMergeHost({
    repository = httpWorktreeMergeRepository
  }) {
    const controller = (0, import_react13.useSyncExternalStore)(
      worktreeMergeStore.subscribe,
      worktreeMergeStore.getSnapshot,
      worktreeMergeStore.getSnapshot
    );
    const context = controller.context;
    const availability = (0, import_react13.useMemo)(
      () => context ? worktreeMergeAvailability(context) : { allowed: false, reason: "" },
      [context]
    );
    const [inspection, setInspection] = (0, import_react13.useState)(null);
    const [mergeResult, setMergeResult] = (0, import_react13.useState)(null);
    const [loading, setLoading] = (0, import_react13.useState)(false);
    const [submitting, setSubmitting] = (0, import_react13.useState)(false);
    const [error, setError] = (0, import_react13.useState)("");
    const cancelButton = (0, import_react13.useRef)(null);
    (0, import_react13.useEffect)(() => {
      if (!controller.open || !context) return;
      const abort = new AbortController();
      setInspection(null);
      setMergeResult(null);
      setSubmitting(false);
      setError("");
      if (!availability.allowed || context.intent === "cleanup") {
        setLoading(false);
        return () => abort.abort();
      }
      setLoading(true);
      void repository.inspect(context.sessionId, { signal: abort.signal }).then((result) => {
        if (!abort.signal.aborted) {
          setInspection(result);
          worktreeMergeStore.getRuntime()?.onRepositoryChanged(context.sessionId);
        }
      }).catch((inspectError) => {
        if (!abort.signal.aborted) {
          setError(presentError4(inspectError, "\u65E0\u6CD5\u68C0\u67E5 worktree \u5408\u5E76\u72B6\u6001\u3002"));
        }
      }).finally(() => {
        if (!abort.signal.aborted) setLoading(false);
      });
      return () => abort.abort();
    }, [availability.allowed, context?.intent, context?.sessionId, controller.open, controller.revision, repository]);
    (0, import_react13.useEffect)(() => {
      if (!controller.open || loading) return;
      const frame = window.requestAnimationFrame(() => cancelButton.current?.focus());
      return () => window.cancelAnimationFrame(frame);
    }, [controller.open, loading, inspection, mergeResult]);
    function notifyChanged(sessionId) {
      worktreeMergeStore.getRuntime()?.onRepositoryChanged(sessionId);
    }
    async function confirmMerge() {
      if (!context || submitting || !canConfirmWorktreeMerge(inspection)) return;
      worktreeMergeController.setDismissable(false);
      setSubmitting(true);
      setError("");
      try {
        const result = await repository.merge(context.sessionId);
        setMergeResult(result);
        notifyChanged(context.sessionId);
        const message = worktreeMergeResultMessage(result);
        worktreeMergeStore.getRuntime()?.toast(message, result.cleanupDone ? "success" : "info");
        if (result.cleanupDone) worktreeMergeController.close();
      } catch (mergeError) {
        if (mergeError instanceof WorktreeMergeRepositoryError && mergeError.result) {
          setMergeResult(mergeError.result);
          notifyChanged(context.sessionId);
        }
        setError(presentError4(mergeError, "\u65E0\u6CD5\u5408\u5E76 worktree\u3002"));
      } finally {
        worktreeMergeController.setDismissable(true);
        setSubmitting(false);
      }
    }
    async function confirmCleanup() {
      if (!context || submitting) return;
      worktreeMergeController.setDismissable(false);
      setSubmitting(true);
      setError("");
      try {
        const result = await repository.cleanup(context.sessionId);
        if (!result.ok) throw new Error("\u65E0\u6CD5\u6E05\u7406 worktree\u3002");
        notifyChanged(context.sessionId);
        worktreeMergeStore.getRuntime()?.toast("\u5DF2\u5B8C\u6210 worktree \u6E05\u7406\u3002", "success");
        worktreeMergeController.close();
      } catch (cleanupError) {
        setError(presentError4(cleanupError, "\u65E0\u6CD5\u6E05\u7406 worktree\u3002"));
      } finally {
        worktreeMergeController.setDismissable(true);
        setSubmitting(false);
      }
    }
    const cleanupRequired = context?.intent === "cleanup" || mergeResult != null && mergeResult.cleanupDone === false && !!mergeResult.mergeCommit;
    const mergeAllowed = availability.allowed && canConfirmWorktreeMerge(inspection);
    const title = cleanupRequired ? "\u6E05\u7406 Worktree" : "\u5408\u5E76 Worktree";
    const description = cleanupRequired ? "\u5B8C\u6210\u5DF2\u5408\u5E76\u5206\u652F\u9057\u7559\u7684 worktree \u6E05\u7406\u3002" : "\u68C0\u67E5\u5F53\u524D\u4EFB\u52A1\u5206\u652F\u5E76\u5B89\u5168\u5408\u5E76\u5230\u76EE\u6807\u5206\u652F\u3002";
    return /* @__PURE__ */ (0, import_jsx_runtime33.jsx)(
      WandDialogSurface,
      {
        open: controller.open,
        onOpenChange: (open) => {
          if (!open) worktreeMergeController.close();
        },
        title,
        description,
        className: "wand-ui-dialog-content wand-worktree-dialog",
        headerClassName: "wand-worktree-header",
        titleClassName: "wand-worktree-title",
        descriptionClassName: "wand-worktree-description",
        closeLabel: "\u5173\u95ED Worktree \u5408\u5E76",
        testId: "worktree-merge-dialog",
        dismissable: !submitting,
        children: /* @__PURE__ */ (0, import_jsx_runtime33.jsxs)("div", { className: "wand-worktree-body", children: [
          /* @__PURE__ */ (0, import_jsx_runtime33.jsxs)("div", { className: "wand-worktree-content", "aria-busy": loading || submitting, children: [
            context ? /* @__PURE__ */ (0, import_jsx_runtime33.jsxs)(import_jsx_runtime33.Fragment, { children: [
              /* @__PURE__ */ (0, import_jsx_runtime33.jsx)(
                MergeDetail,
                {
                  label: "\u6765\u6E90\u5206\u652F",
                  value: inspection?.sourceBranch || context.sourceBranch || "-"
                }
              ),
              /* @__PURE__ */ (0, import_jsx_runtime33.jsx)(
                MergeDetail,
                {
                  label: "\u5DE5\u4F5C\u76EE\u5F55",
                  value: inspection?.worktreePath || context.worktreePath || "-"
                }
              )
            ] }) : null,
            !availability.allowed ? /* @__PURE__ */ (0, import_jsx_runtime33.jsx)("p", { className: "wand-worktree-status wand-worktree-status-warning", role: "status", children: availability.reason }) : loading ? /* @__PURE__ */ (0, import_jsx_runtime33.jsx)("p", { className: "wand-worktree-status", role: "status", children: "\u6B63\u5728\u68C0\u67E5 worktree \u5408\u5E76\u72B6\u6001\u2026" }) : inspection && !cleanupRequired ? /* @__PURE__ */ (0, import_jsx_runtime33.jsx)(InspectionDetails, { inspection }) : cleanupRequired ? /* @__PURE__ */ (0, import_jsx_runtime33.jsxs)(import_jsx_runtime33.Fragment, { children: [
              /* @__PURE__ */ (0, import_jsx_runtime33.jsx)(
                MergeDetail,
                {
                  label: "\u76EE\u6807\u5206\u652F",
                  value: mergeResult?.targetBranch || context?.targetBranch || "\u4E3B\u5206\u652F"
                }
              ),
              mergeResult?.mergeCommit ? /* @__PURE__ */ (0, import_jsx_runtime33.jsx)(MergeDetail, { label: "\u5408\u5E76\u63D0\u4EA4", value: mergeResult.mergeCommit.slice(0, 12) }) : null,
              /* @__PURE__ */ (0, import_jsx_runtime33.jsx)("p", { className: "wand-worktree-status wand-worktree-status-warning", children: "\u5408\u5E76\u7ED3\u679C\u5DF2\u7ECF\u4FDD\u7559\uFF1B\u6B64\u64CD\u4F5C\u53EA\u91CD\u8BD5\u5220\u9664\u9057\u7559 worktree \u548C\u4EFB\u52A1\u5206\u652F\u3002" })
            ] }) : null,
            error ? /* @__PURE__ */ (0, import_jsx_runtime33.jsx)("p", { className: "wand-worktree-error", role: "alert", children: error }) : null
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime33.jsxs)("div", { className: "wand-worktree-actions", children: [
            /* @__PURE__ */ (0, import_jsx_runtime33.jsx)(
              WandButton,
              {
                ref: cancelButton,
                kind: "secondary",
                disabled: submitting,
                onClick: () => worktreeMergeController.close(),
                children: availability.allowed ? "\u53D6\u6D88" : "\u5173\u95ED"
              }
            ),
            cleanupRequired && availability.allowed ? /* @__PURE__ */ (0, import_jsx_runtime33.jsx)(WandButton, { kind: "primary", disabled: submitting, onClick: () => void confirmCleanup(), children: submitting ? "\u6E05\u7406\u4E2D\u2026" : "\u91CD\u8BD5\u6E05\u7406" }) : context?.intent === "merge" && availability.allowed ? /* @__PURE__ */ (0, import_jsx_runtime33.jsx)(
              WandButton,
              {
                kind: "primary",
                disabled: !mergeAllowed || submitting || loading,
                onClick: () => void confirmMerge(),
                children: submitting ? "\u5408\u5E76\u4E2D\u2026" : "\u786E\u8BA4\u5408\u5E76\u5E76\u6E05\u7406"
              }
            ) : null
          ] })
        ] })
      }
    );
  }

  // src/web-ui/react/restart-overlay/host.tsx
  var import_react14 = __toESM(require_react(), 1);

  // src/web-ui/react/restart-overlay/model.ts
  function normalizeRestartVersion(value) {
    return typeof value === "string" ? value.trim().replace(/^v/, "").split("+")[0] : "";
  }
  function evaluateRestartReadiness(target, config) {
    const previousInstanceId = target.previousInstanceId.trim();
    const expectedVersion = normalizeRestartVersion(target.expectedVersion);
    const currentInstanceId = config.serverInstanceId.trim();
    const currentVersion = normalizeRestartVersion(config.packageVersion || config.currentVersion);
    const instanceReady = !previousInstanceId || currentInstanceId.length > 0 && currentInstanceId !== previousInstanceId;
    const versionReady = !expectedVersion || currentVersion === expectedVersion;
    return {
      ready: instanceReady && versionReady,
      instanceReady,
      versionReady,
      currentInstanceId,
      currentVersion
    };
  }
  function waitingStatus(snapshot7) {
    if (snapshot7.lastError) {
      return `\u670D\u52A1\u6682\u4E0D\u53EF\u7528\uFF0C\u6B63\u5728\u7EE7\u7EED\u7B49\u5F85\uFF08${snapshot7.attempts}/${snapshot7.maxAttempts}\uFF09\u2026`;
    }
    if (snapshot7.attempts === 0) {
      return snapshot7.mode === "auto-update" ? "\u6B63\u5728\u7B49\u5F85\u66F4\u65B0\u5B8C\u6210\u5E76\u91CD\u542F\u670D\u52A1\u2026" : "\u6B63\u5728\u7B49\u5F85\u670D\u52A1\u91CD\u65B0\u542F\u52A8\u2026";
    }
    if (!snapshot7.readiness.instanceReady && !snapshot7.readiness.versionReady) {
      return `\u6B63\u5728\u7B49\u5F85\u65B0\u670D\u52A1\u5B9E\u4F8B\u548C\u76EE\u6807\u7248\u672C\uFF08${snapshot7.attempts}/${snapshot7.maxAttempts}\uFF09\u2026`;
    }
    if (!snapshot7.readiness.instanceReady) {
      return `\u76EE\u6807\u7248\u672C\u5DF2\u5C31\u7EEA\uFF0C\u6B63\u5728\u7B49\u5F85\u65B0\u670D\u52A1\u5B9E\u4F8B\uFF08${snapshot7.attempts}/${snapshot7.maxAttempts}\uFF09\u2026`;
    }
    if (!snapshot7.readiness.versionReady) {
      const expected = snapshot7.target.expectedVersion || snapshot7.latestVersion || "\u76EE\u6807\u7248\u672C";
      return `\u65B0\u670D\u52A1\u5DF2\u542F\u52A8\uFF0C\u6B63\u5728\u7B49\u5F85\u7248\u672C ${expected}\uFF08${snapshot7.attempts}/${snapshot7.maxAttempts}\uFF09\u2026`;
    }
    return `\u6B63\u5728\u7B49\u5F85\u670D\u52A1\u5C31\u7EEA\uFF08${snapshot7.attempts}/${snapshot7.maxAttempts}\uFF09\u2026`;
  }
  function restartOverlayPresentation(snapshot7) {
    const isAutoUpdate = snapshot7.mode === "auto-update";
    const title = isAutoUpdate ? "\u81EA\u52A8\u66F4\u65B0\u4E2D" : snapshot7.target.expectedVersion ? "\u6B63\u5728\u5B8C\u6210\u66F4\u65B0" : "\u670D\u52A1\u6B63\u5728\u91CD\u542F";
    const description = isAutoUpdate ? `${snapshot7.currentVersion || "-"} \u2192 ${snapshot7.latestVersion || "-"}
\u6B63\u5728\u4E0B\u8F7D\u5E76\u5B89\u88C5\u65B0\u7248\u672C\uFF0C\u5B8C\u6210\u540E\u5C06\u81EA\u52A8\u91CD\u542F\u3002` : snapshot7.target.expectedVersion ? "\u5B89\u88C5\u5B8C\u6210\u5E76\u542F\u52A8\u65B0\u7248\u672C\u540E\u5C06\u81EA\u52A8\u5237\u65B0\u9875\u9762\u3002" : "\u670D\u52A1\u6062\u590D\u540E\u5C06\u81EA\u52A8\u5237\u65B0\u9875\u9762\u3002";
    let liveStatus = "";
    if (snapshot7.phase === "checking") {
      liveStatus = `\u6B63\u5728\u68C0\u67E5\u670D\u52A1\u72B6\u6001\uFF08${snapshot7.attempts}/${snapshot7.maxAttempts}\uFF09\u2026`;
    } else if (snapshot7.phase === "ready") {
      liveStatus = "\u65B0\u670D\u52A1\u548C\u76EE\u6807\u7248\u672C\u5747\u5DF2\u5C31\u7EEA\uFF0C\u6B63\u5728\u5237\u65B0\u9875\u9762\u2026";
    } else if (snapshot7.phase === "timed-out") {
      liveStatus = "\u7B49\u5F85\u670D\u52A1\u91CD\u542F\u8D85\u65F6\uFF0C\u8BF7\u624B\u52A8\u5237\u65B0\u9875\u9762\u3002";
    } else if (snapshot7.phase === "waiting") {
      liveStatus = waitingStatus(snapshot7);
    }
    return { title, description, liveStatus };
  }

  // src/web-ui/react/restart-overlay/repository.ts
  function isRecord4(value) {
    return !!value && typeof value === "object" && !Array.isArray(value);
  }
  function text4(value) {
    return typeof value === "string" ? value : "";
  }
  function normalizeRestartOverlayConfig(value) {
    const config = isRecord4(value) ? value : {};
    return {
      serverInstanceId: text4(config.serverInstanceId),
      packageVersion: text4(config.packageVersion),
      currentVersion: text4(config.currentVersion)
    };
  }
  var RestartOverlayRepositoryError = class extends Error {
    constructor(message, status = 0) {
      super(message);
      __publicField(this, "status", status);
      this.name = "RestartOverlayRepositoryError";
    }
  };
  var HttpRestartOverlayRepository = class {
    constructor(fetchImpl = (input, init) => globalThis.fetch(input, init)) {
      __publicField(this, "fetchImpl", fetchImpl);
    }
    async loadConfig(options = {}) {
      const response = await this.fetchImpl("/api/config", {
        credentials: "same-origin",
        signal: options.signal
      });
      let value;
      try {
        value = await response.json();
      } catch {
        throw new RestartOverlayRepositoryError(
          `\u670D\u52A1\u72B6\u6001\u54CD\u5E94\u65E0\u6548\uFF08HTTP ${response.status}\uFF09\u3002`,
          response.status
        );
      }
      const record2 = isRecord4(value) ? value : {};
      if (!response.ok || typeof record2.error === "string") {
        throw new RestartOverlayRepositoryError(
          text4(record2.error) || `\u670D\u52A1\u5C1A\u672A\u5C31\u7EEA\uFF08HTTP ${response.status}\uFF09\u3002`,
          response.status
        );
      }
      return normalizeRestartOverlayConfig(record2);
    }
  };
  var httpRestartOverlayRepository = new HttpRestartOverlayRepository();

  // src/web-ui/react/restart-overlay/controller.ts
  var RESTART_POLL_INTERVAL_MS = 2e3;
  var RESTART_MAX_ATTEMPTS = 180;
  var RESTART_PROBE_TIMEOUT_MS = RESTART_POLL_INTERVAL_MS;
  var RESTART_DEADLINE_MS = RESTART_POLL_INTERVAL_MS * RESTART_MAX_ATTEMPTS;
  function blankSnapshot(revision = 0) {
    return {
      open: false,
      mode: "restart",
      phase: "idle",
      currentVersion: "",
      latestVersion: "",
      target: { previousInstanceId: "", expectedVersion: "" },
      attempts: 0,
      maxAttempts: RESTART_MAX_ATTEMPTS,
      readiness: {
        ready: false,
        instanceReady: false,
        versionReady: false,
        currentInstanceId: "",
        currentVersion: ""
      },
      lastError: "",
      revision
    };
  }
  function errorMessage(error) {
    return error instanceof Error && error.message ? error.message : "\u670D\u52A1\u5C1A\u672A\u5C31\u7EEA\u3002";
  }
  function normalizedText(value) {
    return typeof value === "string" ? value.trim() : "";
  }
  function createRestartOverlayController(dependencies) {
    let snapshot7 = blankSnapshot();
    let pollTimer = null;
    let deadlineTimer = null;
    let probeTimer = null;
    let request2 = null;
    let generation = 0;
    const listeners7 = /* @__PURE__ */ new Set();
    function publish7(patch) {
      snapshot7 = { ...snapshot7, ...patch, revision: snapshot7.revision + 1 };
      for (const listener of listeners7) listener();
    }
    function stopPollTimer() {
      if (pollTimer === null) return;
      dependencies.clock.clearInterval(pollTimer);
      pollTimer = null;
    }
    function stopDeadlineTimer() {
      if (deadlineTimer === null) return;
      dependencies.clock.clearTimeout(deadlineTimer);
      deadlineTimer = null;
    }
    function stopProbeTimer() {
      if (probeTimer === null) return;
      dependencies.clock.clearTimeout(probeTimer);
      probeTimer = null;
    }
    function abortProbe(reason = new DOMException("Restart probe cancelled.", "AbortError")) {
      stopProbeTimer();
      const activeRequest = request2;
      request2 = null;
      activeRequest?.abort(reason);
    }
    function stopSchedule() {
      stopPollTimer();
      stopDeadlineTimer();
    }
    function stopPolling2() {
      stopSchedule();
      abortProbe();
    }
    function timeOut(runGeneration) {
      if (runGeneration !== generation || !snapshot7.open || snapshot7.phase === "ready" || snapshot7.phase === "timed-out") return;
      stopSchedule();
      abortProbe();
      publish7({ phase: "timed-out" });
    }
    function failReload(error) {
      publish7({
        phase: "timed-out",
        lastError: errorMessage(error)
      });
    }
    function reloadPage() {
      try {
        dependencies.reloadPage();
      } catch (error) {
        failReload(error);
      }
    }
    async function poll(runGeneration) {
      if (runGeneration !== generation || !snapshot7.open || snapshot7.phase === "timed-out" || snapshot7.phase === "ready") return;
      if (request2 !== null) {
        abortProbe(new DOMException("Restart probe timed out.", "TimeoutError"));
      }
      const abort = new AbortController();
      request2 = abort;
      const attempt = snapshot7.attempts + 1;
      publish7({ phase: "checking", attempts: attempt, lastError: "" });
      let removeAbortListener = () => {
      };
      const aborted = new Promise((_resolve, reject) => {
        const handleAbort = () => {
          removeAbortListener();
          reject(abort.signal.reason instanceof Error ? abort.signal.reason : new DOMException("Restart probe cancelled.", "AbortError"));
        };
        removeAbortListener = () => abort.signal.removeEventListener("abort", handleAbort);
        abort.signal.addEventListener("abort", handleAbort, { once: true });
      });
      probeTimer = dependencies.clock.setTimeout(() => {
        if (runGeneration !== generation || request2 !== abort) return;
        abort.abort(new DOMException("Restart probe timed out.", "TimeoutError"));
      }, RESTART_PROBE_TIMEOUT_MS);
      let becameReady = false;
      try {
        const config = await Promise.race([
          dependencies.repository.loadConfig({ signal: abort.signal }),
          aborted
        ]);
        if (abort.signal.aborted || runGeneration !== generation || request2 !== abort || !snapshot7.open) return;
        const readiness = evaluateRestartReadiness(snapshot7.target, config);
        becameReady = readiness.ready;
        publish7({
          phase: readiness.ready ? "ready" : "waiting",
          readiness,
          lastError: ""
        });
        if (readiness.ready) {
          stopSchedule();
          reloadPage();
        }
      } catch (error) {
        if (runGeneration !== generation || request2 !== abort || !snapshot7.open) return;
        if (abort.signal.aborted && abort.signal.reason instanceof DOMException) {
          if (abort.signal.reason.name !== "TimeoutError") return;
          publish7({ phase: "waiting", lastError: "\u670D\u52A1\u72B6\u6001\u68C0\u67E5\u8D85\u65F6\uFF0C\u6B63\u5728\u91CD\u8BD5\u3002" });
        } else {
          publish7({ phase: "waiting", lastError: errorMessage(error) });
        }
      } finally {
        removeAbortListener();
        if (request2 === abort) {
          stopProbeTimer();
          request2 = null;
        }
      }
      if (!becameReady && runGeneration === generation && snapshot7.open && attempt >= RESTART_MAX_ATTEMPTS) {
        timeOut(runGeneration);
      }
    }
    function begin(mode, currentVersion, latestVersion, previousInstanceId, expectedVersion) {
      generation += 1;
      stopPolling2();
      const target = {
        previousInstanceId: normalizedText(previousInstanceId),
        expectedVersion: normalizeRestartVersion(expectedVersion)
      };
      snapshot7 = {
        open: true,
        mode,
        phase: "waiting",
        currentVersion: normalizedText(currentVersion),
        latestVersion: normalizedText(latestVersion),
        target,
        attempts: 0,
        maxAttempts: RESTART_MAX_ATTEMPTS,
        readiness: {
          ready: false,
          instanceReady: !target.previousInstanceId,
          versionReady: !target.expectedVersion,
          currentInstanceId: "",
          currentVersion: ""
        },
        lastError: "",
        revision: snapshot7.revision + 1
      };
      for (const listener of listeners7) listener();
      const runGeneration = generation;
      pollTimer = dependencies.clock.setInterval(
        () => {
          void poll(runGeneration);
        },
        RESTART_POLL_INTERVAL_MS
      );
      deadlineTimer = dependencies.clock.setTimeout(
        () => timeOut(runGeneration),
        RESTART_DEADLINE_MS
      );
    }
    return {
      showRestart(previousInstanceId, expectedVersion) {
        begin(
          "restart",
          "",
          normalizedText(expectedVersion),
          previousInstanceId,
          expectedVersion
        );
      },
      showAutoUpdate(currentVersion, latestVersion, previousInstanceId) {
        begin(
          "auto-update",
          currentVersion,
          latestVersion,
          previousInstanceId,
          latestVersion
        );
      },
      manualRefresh() {
        if (!snapshot7.open) return;
        reloadPage();
      },
      isOpen() {
        return snapshot7.open;
      },
      subscribe(listener) {
        listeners7.add(listener);
        return () => listeners7.delete(listener);
      },
      getSnapshot() {
        return snapshot7;
      },
      dispose() {
        generation += 1;
        stopPolling2();
        if (!snapshot7.open && snapshot7.phase === "idle") return;
        snapshot7 = blankSnapshot(snapshot7.revision + 1);
        for (const listener of listeners7) listener();
      }
    };
  }
  var browserClock = {
    setInterval(callback, delayMs) {
      return globalThis.setInterval(() => {
        void callback();
      }, delayMs);
    },
    clearInterval(handle) {
      globalThis.clearInterval(handle);
    },
    setTimeout(callback, delayMs) {
      return globalThis.setTimeout(() => {
        void callback();
      }, delayMs);
    },
    clearTimeout(handle) {
      globalThis.clearTimeout(handle);
    }
  };
  var restartOverlayController = createRestartOverlayController({
    repository: httpRestartOverlayRepository,
    clock: browserClock,
    reloadPage: () => globalThis.location.reload()
  });
  function showRestart(previousInstanceId, expectedVersion) {
    restartOverlayController.showRestart(previousInstanceId, expectedVersion);
  }
  function showAutoUpdate(currentVersion, latestVersion, previousInstanceId) {
    restartOverlayController.showAutoUpdate(currentVersion, latestVersion, previousInstanceId);
  }

  // src/web-ui/react/restart-overlay/styles.ts
  var restartOverlayStyles = String.raw`
.wand-restart-backdrop {
  position: fixed;
  inset: 0;
  z-index: 100;
  background: rgba(18, 13, 9, 0.78);
  backdrop-filter: blur(12px) saturate(120%);
  -webkit-backdrop-filter: blur(12px) saturate(120%);
  pointer-events: auto;
  animation: wand-restart-fade-in 180ms ease-out;
}

.wand-restart-surface {
  position: fixed;
  inset: 0;
  z-index: 101;
  box-sizing: border-box;
  display: flex;
  width: 100vw;
  height: 100dvh;
  max-height: none;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  overflow: auto;
  border: 0;
  border-radius: 0;
  padding:
    calc(24px + var(--wand-safe-top))
    calc(24px + var(--wand-safe-right))
    calc(24px + var(--wand-safe-bottom))
    calc(24px + var(--wand-safe-left));
  color: #fff;
  background: transparent;
  box-shadow: none;
  transform: none;
  pointer-events: auto;
  outline: none;
}

.wand-restart-surface[data-state="open"],
.wand-restart-surface[data-state="closed"] {
  animation: none;
}

.wand-restart-header {
  width: min(520px, 100%);
  text-align: center;
}

.wand-restart-header > div {
  min-width: 0;
}

.wand-restart-header > .wand-ui-button {
  display: none;
}

.wand-restart-title {
  margin: 0;
  color: #fff;
  font-size: clamp(1.35rem, 3.4vw, 1.8rem);
  font-weight: var(--font-weight-semibold);
  line-height: var(--line-height-tight);
}

.wand-restart-description {
  margin: 10px 0 0;
  color: rgba(255, 255, 255, 0.78);
  font-size: var(--font-size-sm);
  line-height: 1.65;
  white-space: pre-line;
}

.wand-restart-body {
  display: grid;
  width: min(520px, 100%);
  place-items: center;
  gap: 16px;
  margin-top: 22px;
  text-align: center;
}

.wand-restart-spinner {
  box-sizing: border-box;
  width: 42px;
  height: 42px;
  border: 3px solid rgba(255, 255, 255, 0.24);
  border-top-color: #fff;
  border-radius: 50%;
  animation: wand-restart-spin 800ms linear infinite;
}

.wand-restart-live {
  min-height: 1.6em;
  margin: 0;
  color: rgba(255, 255, 255, 0.88);
  font-size: var(--font-size-sm);
  line-height: 1.6;
  outline: none;
}

.wand-restart-progress {
  width: min(300px, 76vw);
  height: 4px;
  overflow: hidden;
  border: 0;
  border-radius: var(--radius-full);
  color: var(--accent);
  background: rgba(255, 255, 255, 0.16);
}

.wand-restart-progress::-webkit-progress-bar {
  border-radius: var(--radius-full);
  background: rgba(255, 255, 255, 0.16);
}

.wand-restart-progress::-webkit-progress-value {
  border-radius: var(--radius-full);
  background: var(--accent);
}

.wand-restart-progress::-moz-progress-bar {
  border-radius: var(--radius-full);
  background: var(--accent);
}

.wand-restart-attempts {
  color: rgba(255, 255, 255, 0.56);
  font-size: var(--font-size-xs);
}

.wand-restart-manual {
  min-width: 136px;
  margin-top: 2px;
}

@keyframes wand-restart-spin {
  to { transform: rotate(360deg); }
}

@keyframes wand-restart-fade-in {
  from { opacity: 0; }
  to { opacity: 1; }
}

@media (max-width: 600px) {
  .wand-restart-surface {
    padding:
      calc(18px + var(--wand-safe-top))
      calc(18px + var(--wand-safe-right))
      calc(18px + var(--wand-safe-bottom))
      calc(18px + var(--wand-safe-left));
  }

  .wand-restart-description { font-size: var(--font-size-xs); }
  .wand-restart-body { gap: 14px; margin-top: 18px; }
  .wand-restart-spinner { width: 38px; height: 38px; }
  .wand-restart-manual { width: min(280px, 100%); }
}

@media (prefers-reduced-motion: reduce) {
  .wand-restart-backdrop { animation: none; }
  .wand-restart-spinner { animation-duration: 1.8s; }
}
`;

  // src/web-ui/react/restart-overlay/host.tsx
  var import_jsx_runtime34 = __toESM(require_jsx_runtime(), 1);
  function RestartOverlayHost({
    controller = restartOverlayController
  }) {
    const snapshot7 = (0, import_react14.useSyncExternalStore)(
      controller.subscribe,
      controller.getSnapshot,
      controller.getSnapshot
    );
    const presentation = restartOverlayPresentation(snapshot7);
    const manualRefreshButton = (0, import_react14.useRef)(null);
    (0, import_react14.useEffect)(() => {
      if (snapshot7.phase !== "timed-out") return;
      manualRefreshButton.current?.focus();
    }, [snapshot7.phase]);
    const busy = snapshot7.phase === "waiting" || snapshot7.phase === "checking";
    const progressValue = Math.min(snapshot7.attempts, snapshot7.maxAttempts);
    return /* @__PURE__ */ (0, import_jsx_runtime34.jsxs)(import_jsx_runtime34.Fragment, { children: [
      /* @__PURE__ */ (0, import_jsx_runtime34.jsx)("style", { "data-wand-restart-overlay-styles": true, children: restartOverlayStyles }),
      /* @__PURE__ */ (0, import_jsx_runtime34.jsx)(
        WandDialogSurface,
        {
          open: snapshot7.open,
          onOpenChange: () => {
          },
          title: presentation.title,
          description: presentation.description,
          className: "wand-restart-surface",
          overlayClassName: "wand-restart-backdrop",
          headerClassName: "wand-restart-header",
          titleClassName: "wand-restart-title",
          descriptionClassName: "wand-restart-description",
          closeLabel: "\u670D\u52A1\u91CD\u542F\u671F\u95F4\u65E0\u6CD5\u5173\u95ED",
          testId: "restart-overlay",
          dismissable: false,
          children: /* @__PURE__ */ (0, import_jsx_runtime34.jsxs)("div", { className: "wand-restart-body", "aria-busy": busy, children: [
            snapshot7.phase !== "timed-out" ? /* @__PURE__ */ (0, import_jsx_runtime34.jsx)("div", { className: "wand-restart-spinner", "aria-hidden": "true" }) : null,
            /* @__PURE__ */ (0, import_jsx_runtime34.jsx)(
              "p",
              {
                className: "wand-restart-live",
                role: snapshot7.phase === "timed-out" ? "alert" : "status",
                "aria-live": snapshot7.phase === "timed-out" ? "assertive" : "polite",
                "data-wand-autofocus": "true",
                tabIndex: -1,
                children: presentation.liveStatus
              }
            ),
            snapshot7.phase !== "timed-out" ? /* @__PURE__ */ (0, import_jsx_runtime34.jsxs)(import_jsx_runtime34.Fragment, { children: [
              /* @__PURE__ */ (0, import_jsx_runtime34.jsx)(
                "progress",
                {
                  className: "wand-restart-progress",
                  max: snapshot7.maxAttempts,
                  value: progressValue,
                  "aria-label": "\u7B49\u5F85\u670D\u52A1\u91CD\u542F\u8FDB\u5EA6"
                }
              ),
              /* @__PURE__ */ (0, import_jsx_runtime34.jsxs)("span", { className: "wand-restart-attempts", children: [
                "\u5DF2\u68C0\u67E5 ",
                snapshot7.attempts,
                " / ",
                snapshot7.maxAttempts,
                " \u6B21"
              ] })
            ] }) : /* @__PURE__ */ (0, import_jsx_runtime34.jsx)(
              WandButton,
              {
                ref: manualRefreshButton,
                className: "wand-restart-manual",
                kind: "primary",
                onClick: () => controller.manualRefresh(),
                children: "\u624B\u52A8\u5237\u65B0"
              }
            )
          ] })
        }
      )
    ] });
  }

  // src/web-ui/react/file-preview/host.tsx
  var import_react15 = __toESM(require_react(), 1);

  // src/web-ui/react/file-preview/model.ts
  var DEFAULT_FONT_SIZE = 13;
  var MIN_FONT_SIZE = 10;
  var MAX_FONT_SIZE = 22;
  function fileNameFromPath(path) {
    const normalized = path.trim().replace(/[\\/]+$/, "");
    return normalized.split(/[\\/]/).pop() || normalized || "file";
  }
  function fileExtension(name) {
    const index2 = name.lastIndexOf(".");
    return index2 > 0 ? name.slice(index2).toLowerCase() : "";
  }
  function normalizeFilePreviewRequest(input) {
    const request2 = typeof input === "string" ? { path: input } : input;
    const path = request2.path.trim();
    if (!path) return null;
    const seen = /* @__PURE__ */ new Set();
    const siblings = [];
    for (const item of request2.siblings ?? []) {
      const siblingPath = item.path.trim();
      if (!siblingPath || item.type === "dir" || seen.has(siblingPath)) continue;
      seen.add(siblingPath);
      siblings.push({
        path: siblingPath,
        name: item.name?.trim() || fileNameFromPath(siblingPath),
        type: "file"
      });
    }
    return { path, siblings };
  }
  function nextFilePreviewSibling(request2, direction) {
    const siblings = request2?.siblings ?? [];
    if (siblings.length < 2 || !request2) return null;
    const currentIndex = siblings.findIndex((item) => item.path === request2.path);
    if (currentIndex < 0) return null;
    const nextIndex = (currentIndex + direction + siblings.length) % siblings.length;
    const next = siblings[nextIndex];
    return next && next.path !== request2.path ? next : null;
  }
  function clampFilePreviewFontSize(value) {
    if (!Number.isFinite(value)) return DEFAULT_FONT_SIZE;
    return Math.max(MIN_FONT_SIZE, Math.min(MAX_FONT_SIZE, Math.round(value)));
  }
  function defaultFilePreviewFontSize() {
    return DEFAULT_FONT_SIZE;
  }
  function formatFilePreviewSize(value) {
    const size4 = typeof value === "number" && Number.isFinite(value) ? Math.max(0, value) : 0;
    if (size4 < 1024) return `${Math.round(size4)} B`;
    const units = ["KB", "MB", "GB", "TB"];
    let amount = size4 / 1024;
    let unit = 0;
    while (amount >= 1024 && unit < units.length - 1) {
      amount /= 1024;
      unit += 1;
    }
    return `${amount >= 10 ? amount.toFixed(0) : amount.toFixed(1)} ${units[unit]}`;
  }
  function filePreviewKindLabel(file) {
    if (file.kind === "text") return file.lang || file.ext.replace(/^\./, "") || "text";
    const labels = {
      image: "\u56FE\u7247",
      pdf: "PDF",
      video: "\u89C6\u9891",
      audio: "\u97F3\u9891",
      binary: "\u4E8C\u8FDB\u5236"
    };
    return labels[file.kind] || file.ext.replace(/^\./, "") || file.kind;
  }
  function filePreviewIcon(kind) {
    switch (kind) {
      case "image":
        return "\u25A7";
      case "pdf":
        return "PDF";
      case "video":
        return "\u25B6";
      case "audio":
        return "\u266B";
      case "binary":
        return "\u25C7";
      default:
        return "\u2261";
    }
  }
  function isMarkdownPreview(file) {
    return file.lang === "markdown" || /\.(md|markdown|mdx)$/i.test(file.name);
  }
  function shellQuoteFilePath(path) {
    return `'${path.replace(/'/g, `'\\''`)}'`;
  }
  var KEYWORDS = /* @__PURE__ */ new Set([
    "abstract",
    "as",
    "async",
    "await",
    "break",
    "case",
    "catch",
    "class",
    "const",
    "continue",
    "default",
    "def",
    "delete",
    "do",
    "else",
    "enum",
    "export",
    "extends",
    "false",
    "finally",
    "fn",
    "for",
    "from",
    "func",
    "function",
    "go",
    "if",
    "impl",
    "import",
    "in",
    "instanceof",
    "interface",
    "let",
    "match",
    "mod",
    "new",
    "nil",
    "null",
    "of",
    "package",
    "pass",
    "private",
    "protected",
    "pub",
    "public",
    "raise",
    "readonly",
    "return",
    "self",
    "static",
    "struct",
    "super",
    "switch",
    "throw",
    "trait",
    "true",
    "try",
    "type",
    "typeof",
    "undefined",
    "unsafe",
    "use",
    "var",
    "void",
    "while",
    "with",
    "yield"
  ]);
  var CODE_TOKEN = /\/\/[^\n]*|#[^\n]*|"(?:[^"\\]|\\.)*"|'(?:[^'\\]|\\.)*'|`(?:[^`\\]|\\.)*`|\b(?:0x[\da-fA-F]+|0b[01]+|\d+(?:\.\d+)?)\b|\b[A-Za-z_$][\w$]*\b|[+\-*/%=<>!&|^~?:]+/g;
  function tokenizeFilePreviewCode(source) {
    const tokens = [];
    let offset4 = 0;
    for (const match of source.matchAll(CODE_TOKEN)) {
      const index2 = match.index ?? 0;
      if (index2 > offset4) tokens.push({ value: source.slice(offset4, index2) });
      const token = match[0];
      let kind;
      if (token.startsWith("//") || token.startsWith("#")) kind = "comment";
      else if (/^["'`]/.test(token)) kind = "string";
      else if (/^(?:0x|0b|\d)/.test(token)) kind = "number";
      else if (KEYWORDS.has(token)) kind = "keyword";
      else if (/^[+\-*/%=<>!&|^~?:]+$/.test(token)) kind = "operator";
      tokens.push({ value: token, kind });
      offset4 = index2 + token.length;
    }
    if (offset4 < source.length) tokens.push({ value: source.slice(offset4) });
    return tokens;
  }
  function safeMarkdownUrl(value, image = false) {
    const trimmed = value.trim();
    if (/^(?:https?:|mailto:|#|\/)/i.test(trimmed)) return trimmed;
    if (image && /^data:image\/(?:png|gif|jpe?g|webp);base64,/i.test(trimmed)) return trimmed;
    return null;
  }
  function splitMarkdownRow(line) {
    let value = line.trim();
    if (value.startsWith("|")) value = value.slice(1);
    if (value.endsWith("|")) value = value.slice(0, -1);
    return value.split("|");
  }
  var INLINE_MARKDOWN = /(!\[[^\]]*\]\([^)]+\)|\[[^\]]+\]\([^)]+\)|`[^`]+`|\*\*[^*]+\*\*|~~[^~]+~~|\*[^*]+\*)/g;
  function tokenizeFilePreviewMarkdownInline(source) {
    const tokens = [];
    let offset4 = 0;
    for (const match of source.matchAll(INLINE_MARKDOWN)) {
      const index2 = match.index ?? 0;
      if (index2 > offset4) tokens.push({ type: "text", value: source.slice(offset4, index2) });
      const value = match[0];
      const image = /^!\[([^\]]*)\]\(([^)]+)\)$/.exec(value);
      const link = /^\[([^\]]+)\]\(([^)]+)\)$/.exec(value);
      if (image) {
        const url = safeMarkdownUrl(image[2], true);
        tokens.push(url ? { type: "image", value: image[1], url } : { type: "text", value: image[1] });
      } else if (link) {
        const url = safeMarkdownUrl(link[2]);
        tokens.push(url ? { type: "link", value: link[1], url } : { type: "text", value: link[1] });
      } else if (value.startsWith("`")) {
        tokens.push({ type: "code", value: value.slice(1, -1) });
      } else if (value.startsWith("**")) {
        tokens.push({ type: "strong", value: value.slice(2, -2) });
      } else if (value.startsWith("~~")) {
        tokens.push({ type: "delete", value: value.slice(2, -2) });
      } else {
        tokens.push({ type: "emphasis", value: value.slice(1, -1) });
      }
      offset4 = index2 + value.length;
    }
    if (offset4 < source.length) tokens.push({ type: "text", value: source.slice(offset4) });
    return tokens;
  }
  function startsMarkdownBlock(lines, index2) {
    const line = lines[index2] ?? "";
    const next = lines[index2 + 1]?.trim() ?? "";
    return /^```/.test(line) || /^#{1,6}\s+/.test(line) || /^>\s?/.test(line) || /^[-*]\s+/.test(line) || /^\d+\.\s+/.test(line) || /^(?:---+|\*\*\*+)$/.test(line.trim()) || line.includes("|") && /^\|?\s*:?-+:?(\s*\|\s*:?-+:?)+\s*\|?$/.test(next);
  }
  function parseFilePreviewMarkdown(source) {
    const lines = source.split("\n");
    const blocks = [];
    let index2 = 0;
    while (index2 < lines.length) {
      const line = lines[index2];
      if (!line.trim()) {
        index2 += 1;
        continue;
      }
      const fence = /^```([\w+-]*)\s*$/.exec(line);
      if (fence) {
        const content = [];
        index2 += 1;
        while (index2 < lines.length && !/^```\s*$/.test(lines[index2])) {
          content.push(lines[index2]);
          index2 += 1;
        }
        if (index2 < lines.length) index2 += 1;
        blocks.push({ type: "code", lang: fence[1], value: content.join("\n") });
        continue;
      }
      const heading = /^(#{1,6})\s+(.*)$/.exec(line);
      if (heading) {
        blocks.push({
          type: "heading",
          level: heading[1].length,
          content: tokenizeFilePreviewMarkdownInline(heading[2])
        });
        index2 += 1;
        continue;
      }
      if (/^(?:---+|\*\*\*+)$/.test(line.trim())) {
        blocks.push({ type: "rule" });
        index2 += 1;
        continue;
      }
      const separator = lines[index2 + 1]?.trim() ?? "";
      if (line.includes("|") && /^\|?\s*:?-+:?(\s*\|\s*:?-+:?)+\s*\|?$/.test(separator)) {
        const headers = splitMarkdownRow(line).map((cell) => tokenizeFilePreviewMarkdownInline(cell.trim()));
        const aligns = splitMarkdownRow(separator).map((cell) => {
          const value = cell.trim();
          if (value.startsWith(":") && value.endsWith(":")) return "center";
          if (value.endsWith(":")) return "right";
          if (value.startsWith(":")) return "left";
          return void 0;
        });
        const rows = [];
        index2 += 2;
        while (index2 < lines.length && lines[index2].trim() && lines[index2].includes("|")) {
          rows.push(splitMarkdownRow(lines[index2]).map((cell) => tokenizeFilePreviewMarkdownInline(cell.trim())));
          index2 += 1;
        }
        blocks.push({ type: "table", headers, aligns, rows });
        continue;
      }
      if (/^>\s?/.test(line)) {
        const quote = [];
        while (index2 < lines.length && /^>\s?/.test(lines[index2])) {
          quote.push(lines[index2].replace(/^>\s?/, ""));
          index2 += 1;
        }
        blocks.push({ type: "blockquote", content: tokenizeFilePreviewMarkdownInline(quote.join("\n")) });
        continue;
      }
      const unordered = /^[-*]\s+/.test(line);
      const ordered = /^\d+\.\s+/.test(line);
      if (unordered || ordered) {
        const items = [];
        const itemPattern = ordered ? /^\d+\.\s+(.*)$/ : /^[-*]\s+(.*)$/;
        while (index2 < lines.length) {
          const item = itemPattern.exec(lines[index2]);
          if (!item) break;
          items.push(tokenizeFilePreviewMarkdownInline(item[1]));
          index2 += 1;
        }
        blocks.push({ type: "list", ordered, items });
        continue;
      }
      const paragraph = [line];
      index2 += 1;
      while (index2 < lines.length && lines[index2].trim() && !startsMarkdownBlock(lines, index2)) {
        paragraph.push(lines[index2]);
        index2 += 1;
      }
      blocks.push({ type: "paragraph", content: tokenizeFilePreviewMarkdownInline(paragraph.join("\n")) });
    }
    return blocks;
  }

  // src/web-ui/react/file-preview/repository.ts
  function isRecord5(value) {
    return !!value && typeof value === "object" && !Array.isArray(value);
  }
  function stringValue2(value, fallback = "") {
    return typeof value === "string" ? value : fallback;
  }
  function finiteNumber3(value, fallback = 0) {
    return typeof value === "number" && Number.isFinite(value) ? value : fallback;
  }
  function previewKind(value) {
    switch (value) {
      case "image":
      case "pdf":
      case "video":
      case "audio":
      case "binary":
        return value;
      default:
        return "text";
    }
  }
  function rawUrl(path, download = false) {
    return `/api/file-raw?${download ? "download=1&" : ""}path=${encodeURIComponent(path)}`;
  }
  async function readJson2(response) {
    try {
      const value = await response.json();
      return isRecord5(value) ? value : {};
    } catch {
      return {};
    }
  }
  function failureFromResponse(response, value, path, fallback) {
    const name = fileNameFromPath(path);
    const size4 = finiteNumber3(value.size, -1);
    const failure = {
      message: stringValue2(value.error, `${fallback} (HTTP ${response.status})`),
      status: response.status,
      size: size4 >= 0 ? size4 : void 0,
      maxSize: finiteNumber3(value.maxSize, -1) >= 0 ? finiteNumber3(value.maxSize) : void 0
    };
    if (response.status === 413) {
      failure.download = { path, name, url: rawUrl(path, true), size: failure.size };
    }
    return failure;
  }
  function normalizeFilePreview(value, requestedPath) {
    const record2 = isRecord5(value) ? value : {};
    const path = stringValue2(record2.path, requestedPath).trim() || requestedPath;
    const name = stringValue2(record2.name, fileNameFromPath(path));
    const kind = previewKind(record2.kind);
    const content = kind === "text" ? stringValue2(record2.content) : void 0;
    return {
      kind,
      path,
      name,
      ext: stringValue2(record2.ext, fileExtension(name)),
      size: Math.max(0, finiteNumber3(record2.size)),
      mime: stringValue2(record2.mime) || void 0,
      lang: stringValue2(record2.lang) || void 0,
      content,
      rawUrl: rawUrl(path),
      url: rawUrl(path, true)
    };
  }
  var HttpFilePreviewRepository = class {
    constructor(fetchImpl = (input, init) => globalThis.fetch(input, init)) {
      __publicField(this, "fetchImpl", fetchImpl);
    }
    async load(path, options = {}) {
      const response = await this.fetchImpl(
        `/api/file-preview?path=${encodeURIComponent(path)}`,
        { credentials: "same-origin", signal: options.signal }
      );
      const value = await readJson2(response);
      if (!response.ok || typeof value.error === "string") {
        return { ok: false, failure: failureFromResponse(response, value, path, "\u52A0\u8F7D\u9884\u89C8\u5931\u8D25") };
      }
      return { ok: true, file: normalizeFilePreview(value, path) };
    }
    async save(path, content) {
      const response = await this.fetchImpl("/api/file-write", {
        method: "POST",
        credentials: "same-origin",
        headers: { "Content-Type": "application/json" },
        body: JSON.stringify({ path, content })
      });
      const value = await readJson2(response);
      if (!response.ok || typeof value.error === "string") {
        return { ok: false, failure: failureFromResponse(response, value, path, "\u4FDD\u5B58\u6587\u4EF6\u5931\u8D25") };
      }
      return {
        ok: true,
        result: {
          path: stringValue2(value.path, path),
          size: Math.max(0, finiteNumber3(value.size, new TextEncoder().encode(content).byteLength)),
          mtime: stringValue2(value.mtime) || void 0
        }
      };
    }
  };
  var httpFilePreviewRepository = new HttpFilePreviewRepository();

  // src/web-ui/react/file-preview/controller.ts
  function initialSnapshot(revision = 0) {
    return {
      open: false,
      revision,
      request: null,
      status: "idle",
      file: null,
      failure: null,
      editing: false,
      draft: "",
      baseline: "",
      dirty: false,
      saving: false,
      wrap: false,
      fontSize: defaultFilePreviewFontSize(),
      imageZoomed: false
    };
  }
  function unknownFailure(error, fallback) {
    return {
      message: error instanceof Error && error.message ? error.message : fallback
    };
  }
  function isAbort(error) {
    return error instanceof DOMException && error.name === "AbortError";
  }
  function defaultDiscardCopy(reason) {
    if (reason === "exit-edit") {
      return {
        title: "\u653E\u5F03\u672A\u4FDD\u5B58\u7684\u4FEE\u6539\uFF1F",
        message: "\u5F53\u524D\u6587\u4EF6\u6709\u672A\u4FDD\u5B58\u7684\u6539\u52A8\uFF0C\u9000\u51FA\u7F16\u8F91\u540E\u4F1A\u4E22\u5931\u3002"
      };
    }
    if (reason === "replace") {
      return {
        title: "\u5207\u6362\u9884\u89C8\u6587\u4EF6\uFF1F",
        message: "\u5F53\u524D\u6587\u4EF6\u6709\u672A\u4FDD\u5B58\u7684\u6539\u52A8\uFF0C\u5207\u6362\u540E\u4F1A\u4E22\u5931\u3002"
      };
    }
    return {
      title: "\u653E\u5F03\u672A\u4FDD\u5B58\u7684\u4FEE\u6539\uFF1F",
      message: "\u5F53\u524D\u6587\u4EF6\u6709\u672A\u4FDD\u5B58\u7684\u6539\u52A8\uFF0C\u5173\u95ED\u540E\u4F1A\u4E22\u5931\u3002"
    };
  }
  var defaultRuntime = {
    async confirmDiscard(reason) {
      const copy = defaultDiscardCopy(reason);
      const result = await wandOverlay.dialog({
        title: copy.title,
        description: copy.message,
        tone: "warning",
        actions: [
          { label: "\u7EE7\u7EED\u7F16\u8F91", value: false, kind: "secondary", autoFocus: true },
          { label: "\u653E\u5F03\u4FEE\u6539", value: true, kind: "danger" }
        ],
        dismissable: true
      });
      return result.dismissed === false && result.action === true;
    },
    async copyText(text5) {
      if (!globalThis.navigator?.clipboard?.writeText) throw new Error("\u5F53\u524D\u73AF\u5883\u65E0\u6CD5\u8BBF\u95EE\u526A\u8D34\u677F\u3002");
      await globalThis.navigator.clipboard.writeText(text5);
    },
    appendToComposer() {
      return false;
    },
    notify(message, tone) {
      wandOverlay.toast(message, { tone });
    },
    onSaved() {
    }
  };
  function createFilePreviewModule(options) {
    const listeners7 = /* @__PURE__ */ new Set();
    let snapshot7 = initialSnapshot();
    let runtime6 = options.runtime ?? defaultRuntime;
    let loadSequence = 0;
    let activeAbort = null;
    function publish7(patch) {
      snapshot7 = { ...snapshot7, ...patch, revision: snapshot7.revision + 1 };
      for (const listener of listeners7) listener();
    }
    function closeNow() {
      activeAbort?.abort();
      activeAbort = null;
      loadSequence += 1;
      snapshot7 = initialSnapshot(snapshot7.revision + 1);
      for (const listener of listeners7) listener();
    }
    async function confirmDiscard(reason) {
      if (!snapshot7.dirty || !snapshot7.request) return true;
      try {
        return await runtime6.confirmDiscard(reason, snapshot7.request.path);
      } catch (error) {
        runtime6.notify(unknownFailure(error, "\u65E0\u6CD5\u786E\u8BA4\u662F\u5426\u653E\u5F03\u4FEE\u6539\u3002").message, "error");
        return false;
      }
    }
    async function load(request2) {
      activeAbort?.abort();
      const abort = new AbortController();
      activeAbort = abort;
      const sequence = ++loadSequence;
      publish7({
        open: true,
        request: request2,
        status: "loading",
        file: null,
        failure: null,
        editing: false,
        draft: "",
        baseline: "",
        dirty: false,
        saving: false,
        wrap: false,
        fontSize: defaultFilePreviewFontSize(),
        imageZoomed: false
      });
      try {
        const outcome = await options.repository.load(request2.path, { signal: abort.signal });
        if (sequence !== loadSequence || abort.signal.aborted || !snapshot7.open) return false;
        activeAbort = null;
        if (outcome.ok === false) {
          publish7({ status: "error", failure: outcome.failure });
          return false;
        }
        const baseline = outcome.file.kind === "text" ? outcome.file.content ?? "" : "";
        publish7({
          status: "ready",
          file: outcome.file,
          failure: null,
          draft: baseline,
          baseline
        });
        return true;
      } catch (error) {
        if (sequence !== loadSequence || abort.signal.aborted || isAbort(error) || !snapshot7.open) return false;
        activeAbort = null;
        publish7({ status: "error", failure: unknownFailure(error, "\u52A0\u8F7D\u9884\u89C8\u5931\u8D25") });
        return false;
      }
    }
    async function copy(text5, successMessage) {
      try {
        await runtime6.copyText(text5);
        runtime6.notify(successMessage, "success");
        return true;
      } catch (error) {
        runtime6.notify(unknownFailure(error, "\u590D\u5236\u5931\u8D25\u3002").message, "error");
        return false;
      }
    }
    async function execute(command) {
      if (!snapshot7.open) return false;
      switch (command.type) {
        case "close": {
          if (snapshot7.saving) return false;
          if (!snapshot7.dirty) {
            closeNow();
            return true;
          }
          if (!await confirmDiscard("close")) return false;
          closeNow();
          return true;
        }
        case "navigate": {
          if (snapshot7.editing || snapshot7.saving) return false;
          const sibling = nextFilePreviewSibling(snapshot7.request, command.direction);
          if (!sibling || !snapshot7.request) return false;
          return load({ ...snapshot7.request, path: sibling.path });
        }
        case "edit.enter": {
          if (snapshot7.status !== "ready" || snapshot7.file?.kind !== "text" || snapshot7.saving) return false;
          const baseline = snapshot7.file.content ?? "";
          publish7({ editing: true, draft: baseline, baseline, dirty: false, failure: null });
          return true;
        }
        case "edit.change": {
          if (!snapshot7.editing || snapshot7.saving) return false;
          publish7({ draft: command.value, dirty: command.value !== snapshot7.baseline, failure: null });
          return true;
        }
        case "edit.revert": {
          if (!snapshot7.editing || snapshot7.saving) return false;
          publish7({ draft: snapshot7.baseline, dirty: false, failure: null });
          return true;
        }
        case "edit.exit": {
          if (!snapshot7.editing || snapshot7.saving) return false;
          if (!await confirmDiscard("exit-edit")) return false;
          publish7({ editing: false, draft: snapshot7.baseline, dirty: false, failure: null });
          return true;
        }
        case "edit.save": {
          if (!snapshot7.editing || snapshot7.saving || snapshot7.file?.kind !== "text" || !snapshot7.request) return false;
          if (snapshot7.draft === snapshot7.baseline) {
            runtime6.notify("\u6CA1\u6709\u6539\u52A8", "info");
            return true;
          }
          const path = snapshot7.request.path;
          const draft = snapshot7.draft;
          publish7({ saving: true, failure: null });
          try {
            const outcome = await options.repository.save(path, draft);
            if (!snapshot7.open || snapshot7.request?.path !== path) return false;
            if (outcome.ok === false) {
              publish7({ saving: false, failure: outcome.failure });
              runtime6.notify(outcome.failure.message, "error");
              return false;
            }
            const currentFile = snapshot7.file;
            publish7({
              saving: false,
              file: currentFile ? { ...currentFile, content: draft, size: outcome.result.size } : currentFile,
              baseline: draft,
              draft,
              dirty: false,
              failure: null
            });
            runtime6.notify("\u5DF2\u4FDD\u5B58", "success");
            try {
              await runtime6.onSaved(path);
            } catch {
            }
            return true;
          } catch (error) {
            if (!snapshot7.open || snapshot7.request?.path !== path) return false;
            const failure = unknownFailure(error, "\u4FDD\u5B58\u5931\u8D25\uFF1A\u7F51\u7EDC\u9519\u8BEF");
            publish7({ saving: false, failure });
            runtime6.notify(failure.message, "error");
            return false;
          }
        }
        case "copy.path":
          return snapshot7.file ? copy(snapshot7.file.path, "\u5DF2\u590D\u5236\u8DEF\u5F84") : false;
        case "copy.content":
          return snapshot7.file?.kind === "text" ? copy(snapshot7.file.content ?? "", "\u5DF2\u590D\u5236\u5185\u5BB9") : false;
        case "composer.path": {
          if (!snapshot7.file) return false;
          const appended = runtime6.appendToComposer(snapshot7.file.path);
          runtime6.notify(appended ? "\u5DF2\u7C98\u8D34\u5230\u8F93\u5165\u6846" : "\u65E0\u6CD5\u7C98\u8D34\u5230\u8F93\u5165\u6846", appended ? "success" : "error");
          return appended;
        }
        case "composer.cat": {
          if (!snapshot7.file) return false;
          const appended = runtime6.appendToComposer(`cat -- ${shellQuoteFilePath(snapshot7.file.path)}`);
          runtime6.notify(appended ? "\u547D\u4EE4\u5DF2\u7C98\u8D34\u5230\u8F93\u5165\u6846" : "\u65E0\u6CD5\u7C98\u8D34\u5230\u8F93\u5165\u6846", appended ? "success" : "error");
          return appended;
        }
        case "view.wrap.toggle":
          if (snapshot7.file?.kind !== "text" || snapshot7.editing) return false;
          publish7({ wrap: !snapshot7.wrap });
          return true;
        case "view.font.adjust":
          if (snapshot7.file?.kind !== "text" || snapshot7.editing) return false;
          publish7({ fontSize: clampFilePreviewFontSize(snapshot7.fontSize + command.delta) });
          return true;
        case "view.image.zoom.toggle":
          if (snapshot7.file?.kind !== "image" || snapshot7.editing) return false;
          publish7({ imageZoomed: !snapshot7.imageZoomed });
          return true;
      }
    }
    const controller = {
      async open(input) {
        const request2 = normalizeFilePreviewRequest(input);
        if (!request2) return false;
        if (snapshot7.open && snapshot7.request?.path === request2.path && !snapshot7.dirty) {
          publish7({ request: request2 });
          return true;
        }
        if (snapshot7.open && snapshot7.dirty && !await confirmDiscard("replace")) return false;
        return load(request2);
      },
      execute,
      closeIfOpen() {
        if (!snapshot7.open) return false;
        void execute({ type: "close" });
        return true;
      },
      closeTopmost() {
        return this.closeIfOpen();
      },
      isOpen() {
        return snapshot7.open;
      }
    };
    return {
      controller,
      store: {
        subscribe(listener) {
          listeners7.add(listener);
          return () => listeners7.delete(listener);
        },
        getSnapshot() {
          return snapshot7;
        }
      },
      configureRuntime(adapter) {
        const previous = runtime6;
        runtime6 = adapter;
        return () => {
          if (runtime6 === adapter) runtime6 = previous;
        };
      }
    };
  }
  var singleton = createFilePreviewModule({ repository: httpFilePreviewRepository });
  var filePreviewController = singleton.controller;
  var filePreviewStore = singleton.store;
  var configureFilePreviewRuntime = singleton.configureRuntime;

  // src/web-ui/react/file-preview/styles.ts
  var filePreviewStyles = String.raw`
.wand-file-preview-overlay { z-index: 54; }

/* Generic confirmation dialogs render after File Preview in the shared portal. */
.wand-file-preview-overlay ~ .wand-ui-dialog-overlay:not(.wand-file-preview-overlay) { z-index: 56; }
.wand-file-preview-overlay ~ .wand-ui-dialog-content:not(.wand-file-preview-dialog) { z-index: 57; }

.wand-file-preview-dialog {
  z-index: 55;
  width: min(1040px, calc(100vw - var(--wand-safe-left) - var(--wand-safe-right) - 40px));
  height: min(760px, calc(100dvh - var(--wand-safe-top) - var(--wand-safe-bottom) - 40px));
  max-width: none;
  max-height: none;
  padding: 0;
  overflow: hidden;
}

.wand-file-preview-header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 16px;
  flex: 0 0 auto;
  padding: 17px 20px 13px;
  border-bottom: 1px solid var(--border-subtle);
  background: var(--bg-primary);
}

.wand-file-preview-header > div:first-child { min-width: 0; }
.wand-file-preview-title { overflow: hidden; text-overflow: ellipsis; white-space: nowrap; }
.wand-file-preview-path {
  max-width: min(76vw, 820px);
  overflow: hidden;
  color: var(--text-muted);
  font-family: var(--font-mono);
  text-overflow: ellipsis;
  white-space: nowrap;
}

.wand-file-preview-shell {
  display: flex;
  flex: 1 1 auto;
  flex-direction: column;
  min-width: 0;
  min-height: 0;
  outline: none;
}

.wand-file-preview-title-meta {
  display: flex;
  align-items: center;
  gap: 8px;
  min-height: 32px;
  padding: 6px 16px;
  border-bottom: 1px solid var(--border-subtle);
  color: var(--text-secondary);
  background: var(--bg-secondary);
  font-size: var(--font-size-xs);
}

.wand-file-preview-kind-icon {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-width: 24px;
  height: 20px;
  color: var(--accent-active);
  font-family: var(--font-mono);
  font-weight: var(--font-weight-bold);
}

.wand-file-preview-kind {
  padding: 2px 7px;
  border: 1px solid var(--border-subtle);
  border-radius: var(--radius-full);
  background: var(--bg-primary);
  font-family: var(--font-mono);
}

.wand-file-preview-dirty { margin-left: auto; color: var(--warning); font-weight: var(--font-weight-semibold); }

.wand-file-preview-toolbar {
  display: flex;
  align-items: center;
  gap: 8px;
  flex: 0 0 auto;
  min-height: 48px;
  padding: 7px 12px;
  overflow-x: auto;
  border-bottom: 1px solid var(--border-subtle);
  background: var(--bg-primary);
  scrollbar-width: thin;
}

.wand-file-preview-toolbar.editing { background: var(--bg-secondary); }
.wand-file-preview-toolbar-group { display: inline-flex; align-items: center; gap: 5px; flex: 0 0 auto; }
.wand-file-preview-toolbar-group + .wand-file-preview-toolbar-group {
  padding-left: 8px;
  border-left: 1px solid var(--border-subtle);
}

.wand-file-preview-download {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  min-height: 30px;
  padding: 5px 10px;
  border: 1px solid var(--border-default);
  border-radius: var(--radius-sm);
  color: var(--text-primary);
  background: var(--bg-secondary);
  font-size: var(--font-size-xs);
  font-weight: var(--font-weight-semibold);
  line-height: 1.2;
  text-decoration: none;
}

.wand-file-preview-download:hover { border-color: var(--accent); color: var(--accent-active); background: var(--bg-elevated); }
.wand-file-preview-download:focus-visible { outline: none; box-shadow: 0 0 0 2px var(--bg-primary), 0 0 0 4px var(--accent); }
.wand-file-preview-font-size { min-width: 24px; color: var(--text-muted); font-family: var(--font-mono); text-align: center; }

.wand-file-preview-inline-error {
  flex: 0 0 auto;
  margin: 0;
  padding: 8px 14px;
  border-bottom: 1px solid var(--danger);
  color: var(--danger);
  background: var(--danger-muted);
  font-size: var(--font-size-sm);
}

.wand-file-preview-body {
  position: relative;
  display: flex;
  flex: 1 1 auto;
  min-width: 0;
  min-height: 0;
  overflow: auto;
  background: var(--bg-secondary);
  overscroll-behavior: contain;
}

.wand-file-preview-state {
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  gap: 10px;
  width: 100%;
  min-height: 220px;
  padding: 28px;
  color: var(--text-muted);
  text-align: center;
}

.wand-file-preview-error > span {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  width: 36px;
  height: 36px;
  border-radius: var(--radius-full);
  color: var(--danger);
  background: var(--danger-muted);
  font-weight: var(--font-weight-bold);
}
.wand-file-preview-error strong { color: var(--danger); }

.wand-file-preview-code {
  display: grid;
  grid-template-columns: auto minmax(max-content, 1fr);
  min-width: 100%;
  min-height: 100%;
  background: var(--bg-primary);
}

.wand-file-preview-lines,
.wand-file-preview-code-content {
  box-sizing: border-box;
  min-height: 100%;
  margin: 0;
  padding: 16px 14px;
  font-family: var(--font-mono);
  line-height: 1.62;
  tab-size: 2;
  white-space: pre;
}

.wand-file-preview-lines {
  position: sticky;
  left: 0;
  z-index: 1;
  min-width: 52px;
  border-right: 1px solid var(--border-subtle);
  color: var(--text-muted);
  background: var(--bg-secondary);
  text-align: right;
  user-select: none;
}

.wand-file-preview-code-content { color: var(--text-primary); }
.wand-file-preview-code.wrap { grid-template-columns: auto minmax(0, 1fr); width: 100%; }
.wand-file-preview-code.wrap .wand-file-preview-code-content,
.wand-file-preview-markdown.wrap { overflow-wrap: anywhere; white-space: pre-wrap; }

.wand-file-preview-syntax-comment { color: var(--text-muted); font-style: italic; }
.wand-file-preview-syntax-string { color: var(--success); }
.wand-file-preview-syntax-number { color: var(--warning); }
.wand-file-preview-syntax-keyword { color: var(--accent-active); font-weight: var(--font-weight-semibold); }
.wand-file-preview-syntax-operator { color: var(--danger); }

.wand-file-preview-markdown {
  box-sizing: border-box;
  width: min(100%, 920px);
  min-height: 100%;
  margin: 0 auto;
  padding: 26px 32px 48px;
  color: var(--text-primary);
  background: var(--bg-primary);
  line-height: 1.72;
}

.wand-file-preview-markdown p,
.wand-file-preview-markdown blockquote { white-space: pre-wrap; }
.wand-file-preview-markdown h1,
.wand-file-preview-markdown h2,
.wand-file-preview-markdown h3 { margin: 1.35em 0 0.55em; line-height: 1.25; }
.wand-file-preview-markdown h1:first-child,
.wand-file-preview-markdown h2:first-child,
.wand-file-preview-markdown h3:first-child { margin-top: 0; }
.wand-file-preview-markdown a { color: var(--accent-active); }
.wand-file-preview-markdown code {
  border-radius: var(--radius-xs);
  padding: 0.12em 0.34em;
  background: var(--bg-tertiary);
  font-family: var(--font-mono);
}
.wand-file-preview-markdown pre {
  overflow: auto;
  padding: 14px 16px;
  border: 1px solid var(--border-subtle);
  border-radius: var(--radius-md);
  background: var(--bg-secondary);
  white-space: pre;
}
.wand-file-preview-markdown pre code { padding: 0; background: transparent; }
.wand-file-preview-markdown blockquote { margin-left: 0; padding-left: 14px; border-left: 3px solid var(--accent); color: var(--text-secondary); }
.wand-file-preview-markdown img { max-width: 100%; height: auto; border-radius: var(--radius-md); }
.wand-file-preview-table-wrap { max-width: 100%; overflow-x: auto; }
.wand-file-preview-table-wrap table { width: 100%; border-collapse: collapse; }
.wand-file-preview-table-wrap th,
.wand-file-preview-table-wrap td { padding: 8px 10px; border: 1px solid var(--border-subtle); }
.wand-file-preview-table-wrap th { background: var(--bg-secondary); }

.wand-file-preview-editor { display: flex; flex: 1 1 auto; min-width: 0; min-height: 0; }
.wand-file-preview-editor textarea {
  box-sizing: border-box;
  flex: 1 1 auto;
  width: 100%;
  min-height: 100%;
  resize: none;
  border: 0;
  outline: none;
  padding: 18px 20px 44px;
  color: var(--text-primary);
  background: var(--bg-primary);
  font-family: var(--font-mono);
  font-size: 13px;
  line-height: 1.62;
  tab-size: 2;
  caret-color: var(--accent-active);
}
.wand-file-preview-editor textarea:focus { box-shadow: inset 0 0 0 2px var(--accent-muted); }

.wand-file-preview-image {
  display: flex;
  align-items: center;
  justify-content: center;
  width: 100%;
  min-height: 100%;
  padding: 24px;
  overflow: auto;
  border: 0;
  color: inherit;
  background-image: linear-gradient(45deg, var(--bg-tertiary) 25%, transparent 25%), linear-gradient(-45deg, var(--bg-tertiary) 25%, transparent 25%), linear-gradient(45deg, transparent 75%, var(--bg-tertiary) 75%), linear-gradient(-45deg, transparent 75%, var(--bg-tertiary) 75%);
  background-position: 0 0, 0 8px, 8px -8px, -8px 0;
  background-size: 16px 16px;
  cursor: zoom-in;
}
.wand-file-preview-image img { display: block; max-width: 100%; max-height: 100%; object-fit: contain; box-shadow: var(--shadow-md); }
.wand-file-preview-image.zoomed { align-items: flex-start; justify-content: flex-start; cursor: zoom-out; }
.wand-file-preview-image.zoomed img { max-width: none; max-height: none; }

.wand-file-preview-pdf { width: 100%; min-height: 100%; border: 0; background: var(--bg-primary); }
.wand-file-preview-media {
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  gap: 14px;
  width: 100%;
  min-height: 100%;
  padding: 26px;
  color: var(--text-secondary);
}
.wand-file-preview-media video { max-width: 100%; max-height: calc(100% - 40px); border-radius: var(--radius-md); background: #000; }
.wand-file-preview-audio audio { width: min(520px, 100%); }
.wand-file-preview-media-icon { font-size: 48px; color: var(--accent-active); }

.wand-file-preview-binary {
  display: flex;
  align-items: center;
  justify-content: center;
  flex-direction: column;
  gap: 12px;
  width: min(560px, calc(100% - 32px));
  margin: auto;
  padding: 30px;
  border: 1px solid var(--border-subtle);
  border-radius: var(--radius-lg);
  color: var(--text-primary);
  background: var(--bg-primary);
  box-shadow: var(--shadow-sm);
  text-align: center;
}
.wand-file-preview-binary-icon { font-size: 44px; color: var(--accent-active); }
.wand-file-preview-binary-meta { display: flex; gap: 7px; color: var(--text-muted); }
.wand-file-preview-binary > code { max-width: 100%; overflow-wrap: anywhere; color: var(--text-secondary); font-family: var(--font-mono); }
.wand-file-preview-binary-actions { display: flex; gap: 8px; margin-top: 6px; }

.wand-file-preview-metadata {
  display: flex;
  align-items: center;
  gap: 12px;
  flex: 0 0 auto;
  min-height: 32px;
  padding: 6px 14px calc(6px + var(--wand-safe-bottom));
  border-top: 1px solid var(--border-subtle);
  color: var(--text-muted);
  background: var(--bg-primary);
  font-size: var(--font-size-xs);
}
.wand-file-preview-metadata code { min-width: 0; margin-left: auto; overflow: hidden; font-family: var(--font-mono); text-overflow: ellipsis; white-space: nowrap; }

@media (max-width: 768px) {
  .wand-file-preview-dialog {
    width: calc(100vw - var(--wand-safe-left) - var(--wand-safe-right) - 12px);
    height: calc(100dvh - var(--wand-safe-top) - var(--wand-safe-bottom) - 12px);
    border-radius: var(--radius-md);
  }
  .wand-file-preview-header { padding: 13px 14px 10px; }
  .wand-file-preview-path { max-width: calc(100vw - 88px); }
  .wand-file-preview-title-meta { padding-inline: 10px; }
  .wand-file-preview-toolbar { padding: 6px 8px; }
  .wand-file-preview-markdown { padding: 20px 18px 36px; }
  .wand-file-preview-lines,
  .wand-file-preview-code-content { padding: 12px 10px; }
  .wand-file-preview-lines { min-width: 42px; }
  .wand-file-preview-metadata { gap: 8px; padding-inline: 10px; }
  .wand-file-preview-metadata code { display: none; }
  .wand-file-preview-binary { padding: 22px 16px; }
  .wand-file-preview-binary-actions { width: 100%; flex-direction: column; }
  .wand-file-preview-binary-actions > * { width: 100%; }
}

@media (max-width: 420px) {
  .wand-file-preview-dialog {
    width: calc(100vw - var(--wand-safe-left) - var(--wand-safe-right) - 8px);
    height: calc(100dvh - var(--wand-safe-top) - var(--wand-safe-bottom) - 8px);
  }
  .wand-file-preview-header { padding-inline: 12px; }
  .wand-file-preview-toolbar-group + .wand-file-preview-toolbar-group { padding-left: 6px; }
  .wand-file-preview-download { white-space: nowrap; }
  .wand-file-preview-image { padding: 10px; }
  .wand-file-preview-media { padding: 16px 10px; }
}
`;

  // src/web-ui/react/file-preview/host.tsx
  var import_jsx_runtime35 = __toESM(require_jsx_runtime(), 1);
  function run(command) {
    void filePreviewController.execute(command);
  }
  function DownloadLink({ file, label = "\u4E0B\u8F7D" }) {
    return /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("a", { className: "wand-file-preview-download", href: file.url, download: file.name, children: label });
  }
  function CodeTokens({ tokens }) {
    return /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(import_jsx_runtime35.Fragment, { children: tokens.map((token, index2) => token.kind ? /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("span", { className: `wand-file-preview-syntax-${token.kind}`, children: token.value }, index2) : /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(import_react15.Fragment, { children: token.value }, index2)) });
  }
  function MarkdownInline({ tokens }) {
    return /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(import_jsx_runtime35.Fragment, { children: tokens.map((token, index2) => {
      switch (token.type) {
        case "code":
          return /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("code", { children: token.value }, index2);
        case "strong":
          return /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("strong", { children: token.value }, index2);
        case "emphasis":
          return /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("em", { children: token.value }, index2);
        case "delete":
          return /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("del", { children: token.value }, index2);
        case "link":
          return /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("a", { href: token.url, target: "_blank", rel: "noopener noreferrer", children: token.value }, index2);
        case "image":
          return /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("img", { src: token.url, alt: token.value }, index2);
        default:
          return /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(import_react15.Fragment, { children: token.value }, index2);
      }
    }) });
  }
  function MarkdownHeading({ block }) {
    const content = /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(MarkdownInline, { tokens: block.content });
    switch (block.level) {
      case 1:
        return /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("h1", { children: content });
      case 2:
        return /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("h2", { children: content });
      case 3:
        return /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("h3", { children: content });
      case 4:
        return /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("h4", { children: content });
      case 5:
        return /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("h5", { children: content });
      default:
        return /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("h6", { children: content });
    }
  }
  function MarkdownBlock({ block }) {
    switch (block.type) {
      case "heading":
        return /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(MarkdownHeading, { block });
      case "paragraph":
        return /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("p", { children: /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(MarkdownInline, { tokens: block.content }) });
      case "blockquote":
        return /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("blockquote", { children: /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(MarkdownInline, { tokens: block.content }) });
      case "list": {
        const items = block.items.map((item, index2) => /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("li", { children: /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(MarkdownInline, { tokens: item }) }, index2));
        return block.ordered ? /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("ol", { children: items }) : /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("ul", { children: items });
      }
      case "code":
        return /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("pre", { "data-language": block.lang || void 0, children: /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("code", { children: /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(CodeTokens, { tokens: tokenizeFilePreviewCode(block.value) }) }) });
      case "table":
        return /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("div", { className: "wand-file-preview-table-wrap", children: /* @__PURE__ */ (0, import_jsx_runtime35.jsxs)("table", { children: [
          /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("thead", { children: /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("tr", { children: block.headers.map((cell, index2) => /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("th", { style: { textAlign: block.aligns[index2] }, children: /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(MarkdownInline, { tokens: cell }) }, index2)) }) }),
          /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("tbody", { children: block.rows.map((row, rowIndex) => /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("tr", { children: row.map((cell, cellIndex) => /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("td", { style: { textAlign: block.aligns[cellIndex] }, children: /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(MarkdownInline, { tokens: cell }) }, cellIndex)) }, rowIndex)) })
        ] }) });
      case "rule":
        return /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("hr", {});
    }
  }
  function TextPreview({ snapshot: snapshot7, file }) {
    const content = file.content ?? "";
    if (isMarkdownPreview(file)) {
      return /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(
        "div",
        {
          className: `wand-file-preview-markdown${snapshot7.wrap ? " wrap" : ""}`,
          style: { fontSize: `${snapshot7.fontSize}px` },
          children: parseFilePreviewMarkdown(content).map((block, index2) => /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(MarkdownBlock, { block }, index2))
        }
      );
    }
    const lineCount = Math.max(1, content.split("\n").length);
    return /* @__PURE__ */ (0, import_jsx_runtime35.jsxs)("div", { className: `wand-file-preview-code${snapshot7.wrap ? " wrap" : ""}`, children: [
      /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(
        "pre",
        {
          "aria-hidden": "true",
          className: "wand-file-preview-lines",
          style: { fontSize: `${snapshot7.fontSize}px` },
          children: Array.from({ length: lineCount }, (_value, index2) => index2 + 1).join("\n")
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(
        "pre",
        {
          className: "wand-file-preview-code-content",
          style: { fontSize: `${snapshot7.fontSize}px` },
          children: /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("code", { children: /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(CodeTokens, { tokens: tokenizeFilePreviewCode(content) }) })
        }
      )
    ] });
  }
  function BinaryPreview({ file }) {
    return /* @__PURE__ */ (0, import_jsx_runtime35.jsxs)("div", { className: "wand-file-preview-binary", children: [
      /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("span", { className: "wand-file-preview-binary-icon", "aria-hidden": "true", children: "\u25C7" }),
      /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("strong", { children: file.name }),
      /* @__PURE__ */ (0, import_jsx_runtime35.jsxs)("div", { className: "wand-file-preview-binary-meta", children: [
        /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("span", { children: file.ext.replace(/^\./, "") || "\u672A\u77E5\u683C\u5F0F" }),
        /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("span", { "aria-hidden": "true", children: "\xB7" }),
        /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("span", { children: formatFilePreviewSize(file.size) })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("code", { children: file.path }),
      /* @__PURE__ */ (0, import_jsx_runtime35.jsxs)("div", { className: "wand-file-preview-binary-actions", children: [
        /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(DownloadLink, { file, label: "\u4E0B\u8F7D\u6587\u4EF6" }),
        /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(WandButton, { size: "small", onClick: () => run({ type: "composer.cat" }), children: "\u5728\u7EC8\u7AEF\u4E2D\u67E5\u770B" })
      ] })
    ] });
  }
  function PreviewBody({ snapshot: snapshot7, editorRef }) {
    if (snapshot7.status === "loading") {
      return /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("div", { className: "wand-file-preview-state", role: "status", children: "\u6B63\u5728\u52A0\u8F7D\u9884\u89C8\u2026" });
    }
    if (snapshot7.status === "error") {
      return /* @__PURE__ */ (0, import_jsx_runtime35.jsxs)("div", { className: "wand-file-preview-state wand-file-preview-error", role: "alert", children: [
        /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("span", { "aria-hidden": "true", children: "!" }),
        /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("strong", { children: snapshot7.failure?.message || "\u52A0\u8F7D\u9884\u89C8\u5931\u8D25" }),
        snapshot7.failure?.size != null ? /* @__PURE__ */ (0, import_jsx_runtime35.jsxs)("small", { children: [
          "\u6587\u4EF6\u5927\u5C0F\uFF1A",
          formatFilePreviewSize(snapshot7.failure.size)
        ] }) : null,
        snapshot7.failure?.download ? /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(DownloadLink, { file: snapshot7.failure.download, label: "\u4ECD\u7136\u4E0B\u8F7D\u6587\u4EF6" }) : null
      ] });
    }
    const file = snapshot7.file;
    if (!file) return /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("div", { className: "wand-file-preview-state", children: "\u9009\u62E9\u6587\u4EF6\u540E\u5C06\u5728\u8FD9\u91CC\u663E\u793A\u9884\u89C8\u3002" });
    if (snapshot7.editing) {
      return /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("div", { className: "wand-file-preview-editor", children: /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(
        "textarea",
        {
          ref: editorRef,
          "aria-label": `\u7F16\u8F91 ${file.name}`,
          autoComplete: "off",
          autoCorrect: "off",
          autoCapitalize: "off",
          spellCheck: false,
          wrap: "off",
          value: snapshot7.draft,
          onChange: (event) => run({ type: "edit.change", value: event.currentTarget.value }),
          onKeyDown: (event) => {
            if (event.key !== "Tab") return;
            event.preventDefault();
            const input = event.currentTarget;
            const start = input.selectionStart;
            const end = input.selectionEnd;
            const value = `${snapshot7.draft.slice(0, start)}  ${snapshot7.draft.slice(end)}`;
            run({ type: "edit.change", value });
            requestAnimationFrame(() => {
              input.selectionStart = start + 2;
              input.selectionEnd = start + 2;
            });
          }
        }
      ) });
    }
    switch (file.kind) {
      case "image":
        return /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(
          "button",
          {
            type: "button",
            className: `wand-file-preview-image${snapshot7.imageZoomed ? " zoomed" : ""}`,
            "aria-label": snapshot7.imageZoomed ? "\u7F29\u5C0F\u56FE\u7247" : "\u653E\u5927\u56FE\u7247",
            onClick: () => run({ type: "view.image.zoom.toggle" }),
            children: /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("img", { src: file.rawUrl, alt: file.name })
          }
        );
      case "pdf":
        return /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("iframe", { className: "wand-file-preview-pdf", src: file.rawUrl, title: file.name });
      case "video":
        return /* @__PURE__ */ (0, import_jsx_runtime35.jsxs)("div", { className: "wand-file-preview-media", children: [
          /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("video", { controls: true, preload: "metadata", src: file.rawUrl, children: "\u60A8\u7684\u6D4F\u89C8\u5668\u4E0D\u652F\u6301\u89C6\u9891\u9884\u89C8\u3002" }),
          /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("span", { children: formatFilePreviewSize(file.size) })
        ] });
      case "audio":
        return /* @__PURE__ */ (0, import_jsx_runtime35.jsxs)("div", { className: "wand-file-preview-media wand-file-preview-audio", children: [
          /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("span", { className: "wand-file-preview-media-icon", "aria-hidden": "true", children: "\u266B" }),
          /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("strong", { children: file.name }),
          /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("audio", { controls: true, preload: "metadata", src: file.rawUrl, children: "\u60A8\u7684\u6D4F\u89C8\u5668\u4E0D\u652F\u6301\u97F3\u9891\u9884\u89C8\u3002" }),
          /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("span", { children: formatFilePreviewSize(file.size) })
        ] });
      case "binary":
        return /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(BinaryPreview, { file });
      default:
        return /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(TextPreview, { snapshot: snapshot7, file });
    }
  }
  function PreviewToolbar({ snapshot: snapshot7 }) {
    const previous = nextFilePreviewSibling(snapshot7.request, -1);
    const next = nextFilePreviewSibling(snapshot7.request, 1);
    const file = snapshot7.file;
    return /* @__PURE__ */ (0, import_jsx_runtime35.jsxs)("div", { className: `wand-file-preview-toolbar${snapshot7.editing ? " editing" : ""}`, "aria-label": "\u6587\u4EF6\u9884\u89C8\u5DE5\u5177\u680F", children: [
      /* @__PURE__ */ (0, import_jsx_runtime35.jsxs)("div", { className: "wand-file-preview-toolbar-group", children: [
        /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(
          WandButton,
          {
            size: "small",
            kind: "ghost",
            "aria-label": "\u4E0A\u4E00\u4E2A\u6587\u4EF6",
            title: previous ? `\u4E0A\u4E00\u4E2A\u6587\u4EF6\uFF1A${previous.name}` : "\u6CA1\u6709\u4E0A\u4E00\u4E2A\u6587\u4EF6",
            disabled: !previous || snapshot7.editing || snapshot7.saving,
            onClick: () => run({ type: "navigate", direction: -1 }),
            children: "\u2190"
          }
        ),
        /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(
          WandButton,
          {
            size: "small",
            kind: "ghost",
            "aria-label": "\u4E0B\u4E00\u4E2A\u6587\u4EF6",
            title: next ? `\u4E0B\u4E00\u4E2A\u6587\u4EF6\uFF1A${next.name}` : "\u6CA1\u6709\u4E0B\u4E00\u4E2A\u6587\u4EF6",
            disabled: !next || snapshot7.editing || snapshot7.saving,
            onClick: () => run({ type: "navigate", direction: 1 }),
            children: "\u2192"
          }
        )
      ] }),
      snapshot7.editing ? /* @__PURE__ */ (0, import_jsx_runtime35.jsxs)("div", { className: "wand-file-preview-toolbar-group wand-file-preview-edit-actions", children: [
        /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(WandButton, { kind: "primary", size: "small", disabled: snapshot7.saving, onClick: () => run({ type: "edit.save" }), children: snapshot7.saving ? "\u4FDD\u5B58\u4E2D\u2026" : "\u4FDD\u5B58" }),
        /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(WandButton, { size: "small", disabled: snapshot7.saving || !snapshot7.dirty, onClick: () => run({ type: "edit.revert" }), children: "\u64A4\u9500\u6539\u52A8" }),
        /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(WandButton, { size: "small", disabled: snapshot7.saving, onClick: () => run({ type: "edit.exit" }), children: "\u9000\u51FA\u7F16\u8F91" })
      ] }) : file ? /* @__PURE__ */ (0, import_jsx_runtime35.jsxs)(import_jsx_runtime35.Fragment, { children: [
        /* @__PURE__ */ (0, import_jsx_runtime35.jsxs)("div", { className: "wand-file-preview-toolbar-group", children: [
          file.kind === "text" ? /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(WandButton, { kind: "primary", size: "small", onClick: () => run({ type: "edit.enter" }), children: "\u7F16\u8F91" }) : null,
          /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(WandButton, { size: "small", onClick: () => run({ type: "copy.path" }), children: "\u590D\u5236\u8DEF\u5F84" }),
          /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(WandButton, { size: "small", onClick: () => run({ type: "composer.path" }), children: "\u7C98\u8D34\u5230\u8F93\u5165\u6846" }),
          /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(DownloadLink, { file })
        ] }),
        file.kind === "text" ? /* @__PURE__ */ (0, import_jsx_runtime35.jsxs)("div", { className: "wand-file-preview-toolbar-group", children: [
          /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(WandButton, { size: "small", onClick: () => run({ type: "copy.content" }), children: "\u590D\u5236\u5185\u5BB9" }),
          /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(
            WandButton,
            {
              size: "small",
              kind: snapshot7.wrap ? "primary" : "secondary",
              "aria-pressed": snapshot7.wrap,
              onClick: () => run({ type: "view.wrap.toggle" }),
              children: "\u81EA\u52A8\u6362\u884C"
            }
          ),
          /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(WandButton, { size: "small", "aria-label": "\u7F29\u5C0F\u5B57\u53F7", onClick: () => run({ type: "view.font.adjust", delta: -1 }), children: "A\u2212" }),
          /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("span", { className: "wand-file-preview-font-size", "aria-label": `\u5B57\u53F7 ${snapshot7.fontSize}`, children: snapshot7.fontSize }),
          /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(WandButton, { size: "small", "aria-label": "\u653E\u5927\u5B57\u53F7", onClick: () => run({ type: "view.font.adjust", delta: 1 }), children: "A+" })
        ] }) : null
      ] }) : snapshot7.failure?.download ? /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(DownloadLink, { file: snapshot7.failure.download }) : null
    ] });
  }
  function FilePreviewHost() {
    const snapshot7 = (0, import_react15.useSyncExternalStore)(
      filePreviewStore.subscribe,
      filePreviewStore.getSnapshot,
      filePreviewStore.getSnapshot
    );
    const editorRef = (0, import_react15.useRef)(null);
    (0, import_react15.useEffect)(() => {
      if (!snapshot7.editing) return;
      const editor = editorRef.current;
      if (!editor) return;
      editor.focus();
      editor.setSelectionRange(0, 0);
      editor.scrollTop = 0;
    }, [snapshot7.editing]);
    const path = snapshot7.request?.path ?? "";
    const title = snapshot7.file?.name || (path ? fileNameFromPath(path) : "\u6587\u4EF6\u9884\u89C8");
    const file = snapshot7.file;
    function handleKeyboard(event) {
      if ((event.ctrlKey || event.metaKey) && event.key.toLowerCase() === "s" && snapshot7.editing) {
        event.preventDefault();
        event.stopPropagation();
        run({ type: "edit.save" });
        return;
      }
      if (event.key === "Escape") {
        event.preventDefault();
        event.stopPropagation();
        run({ type: snapshot7.editing ? "edit.exit" : "close" });
        return;
      }
      const target = event.target;
      if (target instanceof HTMLElement && (target.matches("input, textarea") || target.isContentEditable)) return;
      if (snapshot7.editing) return;
      if (event.key === "ArrowLeft") {
        event.preventDefault();
        run({ type: "navigate", direction: -1 });
      } else if (event.key === "ArrowRight") {
        event.preventDefault();
        run({ type: "navigate", direction: 1 });
      } else if (event.key.toLowerCase() === "e" && file?.kind === "text") {
        event.preventDefault();
        run({ type: "edit.enter" });
      }
    }
    return /* @__PURE__ */ (0, import_jsx_runtime35.jsxs)(import_jsx_runtime35.Fragment, { children: [
      /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("style", { id: "wand-file-preview-styles", children: filePreviewStyles }),
      /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(
        WandDialogSurface,
        {
          open: snapshot7.open,
          onOpenChange: (open) => {
            if (!open) run({ type: "close" });
          },
          title,
          description: path || "\u67E5\u770B\u6587\u4EF6\u5185\u5BB9\u4E0E\u5143\u6570\u636E\u3002",
          className: "wand-ui-dialog-content wand-file-preview-dialog",
          overlayClassName: "wand-ui-dialog-overlay wand-file-preview-overlay",
          titleClassName: "wand-ui-dialog-title wand-file-preview-title",
          descriptionClassName: "wand-ui-dialog-description wand-file-preview-path",
          headerClassName: "wand-ui-dialog-heading wand-file-preview-header",
          closeLabel: "\u5173\u95ED\u6587\u4EF6\u9884\u89C8",
          testId: "file-preview-dialog",
          dismissable: !snapshot7.saving && !snapshot7.editing,
          children: /* @__PURE__ */ (0, import_jsx_runtime35.jsxs)(
            "div",
            {
              className: "wand-file-preview-shell",
              "data-wand-autofocus": true,
              tabIndex: -1,
              onKeyDownCapture: handleKeyboard,
              children: [
                /* @__PURE__ */ (0, import_jsx_runtime35.jsxs)("div", { className: "wand-file-preview-title-meta", "aria-live": "polite", children: [
                  /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("span", { className: "wand-file-preview-kind-icon", "aria-hidden": "true", children: file ? filePreviewIcon(file.kind) : "\u2026" }),
                  file ? /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("span", { className: "wand-file-preview-kind", children: filePreviewKindLabel(file) }) : null,
                  snapshot7.dirty ? /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("span", { className: "wand-file-preview-dirty", children: "\u25CF \u672A\u4FDD\u5B58" }) : null
                ] }),
                /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(PreviewToolbar, { snapshot: snapshot7 }),
                snapshot7.status === "ready" && snapshot7.failure ? /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("p", { className: "wand-file-preview-inline-error", role: "alert", children: snapshot7.failure.message }) : null,
                /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("div", { className: `wand-file-preview-body kind-${file?.kind ?? snapshot7.status}`, children: /* @__PURE__ */ (0, import_jsx_runtime35.jsx)(PreviewBody, { snapshot: snapshot7, editorRef }) }),
                file ? /* @__PURE__ */ (0, import_jsx_runtime35.jsxs)("footer", { className: "wand-file-preview-metadata", "aria-label": "\u6587\u4EF6\u5143\u6570\u636E", children: [
                  /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("span", { children: formatFilePreviewSize(file.size) }),
                  /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("span", { children: file.mime || file.ext.replace(/^\./, "") || file.kind }),
                  /* @__PURE__ */ (0, import_jsx_runtime35.jsx)("code", { title: file.path, children: file.path })
                ] }) : null
              ]
            }
          )
        }
      )
    ] });
  }

  // src/web-ui/react/overlay-host.tsx
  var import_jsx_runtime36 = __toESM(require_jsx_runtime(), 1);
  function OverlayHost({ portalContainer }) {
    const current = (0, import_react16.useSyncExternalStore)(
      overlayStore.subscribe,
      overlayStore.getSnapshot,
      overlayStore.getSnapshot
    );
    const dialog = current.activeDialog;
    return /* @__PURE__ */ (0, import_jsx_runtime36.jsxs)(PortalContainerProvider, { container: portalContainer, children: [
      /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(SettingsHost, { showRestart: () => restartOverlayController.showRestart() }),
      /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(NewSessionHost, {}),
      /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(FolderPickerHost, {}),
      /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(QuickCommitHost, {}),
      /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(WorktreeMergeHost, {}),
      /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(FilePreviewHost, {}),
      /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(RestartOverlayHost, {}),
      /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(WandToastRegion, { children: current.toasts.map((toast) => /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(
        WandToastItem,
        {
          open: toast.open,
          title: toast.message,
          description: toast.options.description,
          tone: toast.options.tone,
          duration: toast.options.duration,
          onDismiss: () => overlayStore.dismissToast(toast.id)
        },
        toast.id
      )) }),
      dialog ? /* @__PURE__ */ (0, import_jsx_runtime36.jsx)(
        WandDialog,
        {
          open: true,
          title: dialog.options.title,
          description: dialog.options.description,
          tone: dialog.options.tone,
          icon: dialog.options.icon,
          actions: dialog.options.actions,
          input: dialog.options.input,
          dismissable: dialog.options.dismissable,
          onAction: (action, inputValue) => {
            overlayStore.completeDialog(dialog.id, {
              dismissed: false,
              action,
              inputValue
            });
          },
          onDismiss: () => {
            overlayStore.completeDialog(dialog.id, { dismissed: true });
          }
        },
        dialog.id
      ) : null
    ] });
  }

  // src/web-ui/react/styles/base.ts
  var foundationStyles = String.raw`
#overlay-root {
  isolation: isolate;
  position: fixed;
  inset: 0;
  z-index: 20000;
  pointer-events: none;
}

.wand-ui-mount {
  display: contents;
}

.wand-ui-portals {
  position: fixed;
  inset: 0;
  color: var(--text-primary);
  font-family: var(--font-sans);
  font-size: var(--font-size-base);
  line-height: var(--line-height-base);
}

.wand-ui-button {
  appearance: none;
  border: 1px solid transparent;
  border-radius: var(--radius-sm);
  box-sizing: border-box;
  cursor: pointer;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 7px;
  min-height: 36px;
  padding: 7px 14px;
  color: var(--text-primary);
  background: var(--bg-tertiary);
  font: inherit;
  font-size: var(--font-size-sm);
  font-weight: var(--font-weight-semibold);
  line-height: 1.2;
  transition: background var(--transition-fast), border-color var(--transition-fast), color var(--transition-fast), box-shadow var(--transition-fast), transform var(--transition-fast);
}

.wand-ui-button:hover:not(:disabled) {
  border-color: var(--border-default);
  background: var(--bg-elevated);
}

.wand-ui-button:active:not(:disabled) {
  transform: translateY(1px);
}

.wand-ui-button:focus-visible,
.wand-ui-select-trigger:focus-visible,
.wand-ui-switch:focus-visible,
.wand-ui-tabs-trigger:focus-visible {
  outline: none;
  box-shadow: 0 0 0 2px var(--bg-primary), 0 0 0 4px var(--accent);
}

.wand-ui-button:disabled {
  cursor: not-allowed;
  opacity: 0.52;
}

.wand-ui-button-primary {
  border-color: var(--accent-active);
  color: var(--text-inverse);
  background: var(--accent-active);
}

.wand-ui-button-primary:hover:not(:disabled) {
  border-color: var(--accent-active);
  background: var(--accent-active);
  filter: brightness(0.9);
}

.wand-ui-button-danger {
  border-color: var(--danger);
  color: var(--text-inverse);
  background: var(--danger);
}

.wand-ui-button-danger:hover:not(:disabled) {
  border-color: var(--danger);
  background: var(--danger);
  filter: brightness(0.9);
}

.wand-ui-button-outline,
.wand-ui-button-secondary {
  border-color: var(--border-default);
  background: var(--bg-secondary);
}

.wand-ui-button-ghost {
  color: var(--text-secondary);
  background: transparent;
}

.wand-ui-button-small {
  min-height: 30px;
  padding: 5px 10px;
  font-size: var(--font-size-xs);
}

.wand-ui-button-large {
  min-height: 42px;
  padding: 9px 18px;
  font-size: var(--font-size-base);
}

.wand-ui-dialog-overlay {
  position: fixed;
  inset: 0;
  z-index: 0;
  background: var(--bg-overlay);
  backdrop-filter: blur(4px);
  pointer-events: auto;
}

.wand-ui-dialog-overlay[data-state="open"] {
  animation: wand-ui-fade-in var(--transition-normal);
}

.wand-ui-dialog-overlay[data-state="closed"] {
  animation: wand-ui-fade-out var(--transition-fast);
}

.wand-ui-dialog-content {
  position: fixed;
  z-index: 1;
  top: 50%;
  left: 50%;
  box-sizing: border-box;
  width: min(520px, calc(100vw - var(--wand-safe-left) - var(--wand-safe-right) - 32px));
  max-height: calc(100dvh - var(--wand-safe-top) - var(--wand-safe-bottom) - 32px);
  overflow: auto;
  border: 1px solid var(--border-default);
  border-radius: var(--radius-lg);
  padding: 20px;
  color: var(--text-primary);
  background: var(--bg-elevated);
  box-shadow: var(--shadow-xl);
  transform: translate(-50%, -50%);
  pointer-events: auto;
}

.wand-ui-dialog-content[data-state="open"] {
  animation: wand-ui-dialog-in var(--transition-normal);
}

.wand-ui-dialog-content[data-state="closed"] {
  animation: wand-ui-dialog-out var(--transition-fast);
}

.wand-ui-dialog-header {
  display: flex;
  align-items: flex-start;
  gap: 12px;
}

.wand-ui-dialog-icon {
  flex: 0 0 auto;
  display: grid;
  place-items: center;
  width: 34px;
  height: 34px;
  border-radius: var(--radius-full);
  color: var(--info);
  background: var(--info-muted);
  font-weight: var(--font-weight-bold);
}

.wand-ui-dialog-icon-warning {
  color: var(--warning);
  background: var(--warning-muted);
}

.wand-ui-dialog-icon-danger {
  color: var(--danger);
  background: var(--danger-muted);
}

.wand-ui-dialog-icon-success {
  color: var(--success);
  background: var(--success-muted);
}

.wand-ui-dialog-icon-question {
  color: var(--accent);
  background: var(--accent-muted);
}

.wand-ui-dialog-heading {
  min-width: 0;
  flex: 1;
}

.wand-ui-dialog-title {
  margin: 0;
  color: var(--text-primary);
  font-size: var(--font-size-lg);
  font-weight: var(--font-weight-semibold);
  line-height: var(--line-height-tight);
}

.wand-ui-dialog-description {
  margin: 6px 0 0;
  color: var(--text-secondary);
  font-size: var(--font-size-sm);
  white-space: pre-wrap;
}

.wand-ui-dialog-body {
  margin-top: 16px;
}

.wand-ui-dialog-input {
  box-sizing: border-box;
  width: 100%;
  min-height: 40px;
  border: 1px solid var(--border-default);
  border-radius: var(--radius-sm);
  padding: 8px 11px;
  color: var(--text-primary);
  background: var(--bg-primary);
  font: inherit;
}

.wand-ui-dialog-input:focus {
  border-color: var(--accent);
  outline: none;
  box-shadow: 0 0 0 3px var(--accent-muted);
}

.wand-ui-dialog-actions {
  display: flex;
  flex-wrap: wrap;
  justify-content: flex-end;
  gap: 8px;
  margin-top: 20px;
}

.wand-ui-toast-viewport {
  position: fixed;
  z-index: 20;
  top: calc(16px + var(--wand-safe-top));
  right: calc(16px + var(--wand-safe-right));
  display: flex;
  flex-direction: column;
  gap: 8px;
  box-sizing: border-box;
  width: min(390px, calc(100vw - var(--wand-safe-left) - var(--wand-safe-right) - 32px));
  margin: 0;
  padding: 0;
  list-style: none;
  pointer-events: none;
}

.wand-ui-toast {
  display: grid;
  grid-template-columns: 1fr auto;
  gap: 8px 12px;
  box-sizing: border-box;
  border: 1px solid var(--border-default);
  border-left: 3px solid var(--info);
  border-radius: var(--radius-md);
  padding: 12px 13px;
  color: var(--text-primary);
  background: var(--bg-elevated);
  box-shadow: var(--shadow-lg);
  pointer-events: auto;
}

.wand-ui-toast[data-state="open"] {
  animation: wand-ui-toast-in var(--transition-spring);
}

.wand-ui-toast[data-state="closed"] {
  animation: wand-ui-toast-out var(--transition-fast);
}

.wand-ui-toast[data-swipe="move"] {
  transform: translateX(var(--radix-toast-swipe-move-x));
}

.wand-ui-toast[data-swipe="cancel"] {
  transform: translateX(0);
  transition: transform var(--transition-fast);
}

.wand-ui-toast[data-swipe="end"] {
  animation: wand-ui-toast-swipe-out var(--transition-fast);
}

.wand-ui-toast-success { border-left-color: var(--success); }
.wand-ui-toast-warning { border-left-color: var(--warning); }
.wand-ui-toast-error { border-left-color: var(--danger); }

.wand-ui-toast-title {
  color: var(--text-primary);
  font-size: var(--font-size-sm);
  font-weight: var(--font-weight-semibold);
}

.wand-ui-toast-description {
  grid-column: 1;
  color: var(--text-secondary);
  font-size: var(--font-size-xs);
  white-space: pre-wrap;
}

.wand-ui-toast-close {
  grid-column: 2;
  grid-row: 1 / span 2;
  align-self: start;
  border: 0;
  border-radius: var(--radius-xs);
  padding: 2px 6px;
  color: var(--text-muted);
  background: transparent;
  cursor: pointer;
  font: inherit;
  font-size: var(--font-size-lg);
}

.wand-ui-toast-close:hover {
  color: var(--text-primary);
  background: var(--bg-tertiary);
}

.wand-ui-popover-content,
.wand-ui-select-content {
  z-index: 10;
  box-sizing: border-box;
  border: 1px solid var(--border-default);
  border-radius: var(--radius-md);
  padding: 8px;
  color: var(--text-primary);
  background: var(--bg-elevated);
  box-shadow: var(--shadow-lg);
}

.wand-ui-popover-content {
  width: min(320px, calc(100vw - var(--wand-safe-left) - var(--wand-safe-right) - 24px));
}

.wand-ui-popover-content[data-state="open"],
.wand-ui-select-content[data-state="open"] {
  animation: wand-ui-scale-in var(--transition-fast);
}

.wand-ui-popover-arrow {
  fill: var(--bg-elevated);
}

.wand-ui-select-trigger {
  display: inline-flex;
  align-items: center;
  justify-content: space-between;
  gap: 10px;
  box-sizing: border-box;
  min-width: 160px;
  min-height: 38px;
  border: 1px solid var(--border-default);
  border-radius: var(--radius-sm);
  padding: 7px 10px;
  color: var(--text-primary);
  background: var(--bg-secondary);
  cursor: pointer;
  font: inherit;
  font-size: var(--font-size-sm);
}

.wand-ui-select-trigger[data-placeholder] {
  color: var(--text-muted);
}

.wand-ui-select-trigger:disabled {
  cursor: not-allowed;
  opacity: 0.52;
}

.wand-ui-select-content {
  max-height: min(360px, var(--radix-select-content-available-height));
  min-width: var(--radix-select-trigger-width);
  overflow: hidden;
}

.wand-ui-select-viewport {
  padding: 2px;
}

.wand-ui-select-item {
  position: relative;
  display: flex;
  align-items: center;
  min-height: 34px;
  border-radius: var(--radius-xs);
  padding: 5px 30px 5px 9px;
  color: var(--text-primary);
  cursor: default;
  font-size: var(--font-size-sm);
  user-select: none;
}

.wand-ui-select-item[data-highlighted] {
  outline: none;
  color: var(--accent);
  background: var(--accent-muted);
}

.wand-ui-select-item[data-disabled] {
  opacity: 0.45;
}

.wand-ui-select-indicator {
  position: absolute;
  right: 9px;
  color: var(--accent);
}

.wand-ui-select-scroll-button {
  display: grid;
  place-items: center;
  height: 24px;
  color: var(--text-muted);
}

.wand-ui-tabs-list {
  display: inline-flex;
  gap: 3px;
  border-radius: var(--radius-sm);
  padding: 3px;
  background: var(--bg-tertiary);
}

.wand-ui-tabs-trigger {
  border: 0;
  border-radius: var(--radius-xs);
  padding: 7px 11px;
  color: var(--text-secondary);
  background: transparent;
  cursor: pointer;
  font: inherit;
  font-size: var(--font-size-sm);
}

.wand-ui-tabs-trigger[data-state="active"] {
  color: var(--text-primary);
  background: var(--bg-elevated);
  box-shadow: var(--shadow-xs);
}

.wand-ui-tabs-trigger:disabled {
  cursor: not-allowed;
  opacity: 0.45;
}

.wand-ui-tabs-content {
  margin-top: 12px;
  outline: none;
}

.wand-ui-switch-row {
  display: inline-flex;
  align-items: center;
  gap: 9px;
  color: var(--text-primary);
  font-size: var(--font-size-sm);
}

.wand-ui-switch {
  position: relative;
  flex: 0 0 auto;
  width: 42px;
  height: 24px;
  border: 1px solid var(--border-strong);
  border-radius: var(--radius-full);
  padding: 0;
  background: var(--bg-tertiary);
  cursor: pointer;
  transition: background var(--transition-fast), border-color var(--transition-fast);
}

.wand-ui-switch[data-state="checked"] {
  border-color: var(--accent);
  background: var(--accent);
}

.wand-ui-switch:disabled {
  cursor: not-allowed;
  opacity: 0.52;
}

.wand-ui-switch-thumb {
  display: block;
  width: 18px;
  height: 18px;
  border-radius: var(--radius-full);
  background: var(--bg-elevated);
  box-shadow: var(--shadow-sm);
  transform: translateX(2px);
  transition: transform var(--transition-fast);
}

.wand-ui-switch-thumb[data-state="checked"] {
  transform: translateX(20px);
}


`;
  var sharedMotionStyles = String.raw`
@keyframes wand-ui-fade-in {
  from { opacity: 0; }
  to { opacity: 1; }
}

@keyframes wand-ui-fade-out {
  from { opacity: 1; }
  to { opacity: 0; }
}

@keyframes wand-ui-dialog-in {
  from { opacity: 0; transform: translate(-50%, calc(-50% + 10px)) scale(0.98); }
  to { opacity: 1; transform: translate(-50%, -50%) scale(1); }
}

@keyframes wand-ui-dialog-out {
  from { opacity: 1; transform: translate(-50%, -50%) scale(1); }
  to { opacity: 0; transform: translate(-50%, calc(-50% + 6px)) scale(0.99); }
}

@keyframes wand-ui-toast-in {
  from { opacity: 0; transform: translateX(18px) scale(0.98); }
  to { opacity: 1; transform: translateX(0) scale(1); }
}

@keyframes wand-ui-toast-out {
  from { opacity: 1; transform: translateX(0); }
  to { opacity: 0; transform: translateX(12px); }
}

@keyframes wand-ui-toast-swipe-out {
  from { transform: translateX(var(--radix-toast-swipe-end-x)); }
  to { transform: translateX(calc(100% + 16px)); }
}

@keyframes wand-ui-scale-in {
  from { opacity: 0; transform: scale(0.98); }
  to { opacity: 1; transform: scale(1); }
}

@media (max-width: 520px) {
  .wand-ui-dialog-content {
    width: calc(100vw - var(--wand-safe-left) - var(--wand-safe-right) - 20px);
    max-height: calc(100dvh - var(--wand-safe-top) - var(--wand-safe-bottom) - 20px);
    border-radius: var(--radius-md);
    padding: 16px;
  }

  .wand-ui-dialog-actions {
    flex-direction: column-reverse;
  }

  .wand-ui-dialog-actions .wand-ui-button {
    width: 100%;
  }
}


`;
  var reducedMotionStyles = String.raw`
@media (prefers-reduced-motion: reduce) {
  #overlay-root *,
  #overlay-root *::before,
  #overlay-root *::after {
    scroll-behavior: auto !important;
    animation-duration: 0.01ms !important;
    animation-iteration-count: 1 !important;
    transition-duration: 0.01ms !important;
  }
}
`;

  // src/web-ui/react/styles/features.ts
  var settingsAndQuickCommitStyles = String.raw`
.wand-settings-overlay,
.wand-settings-nested-overlay {
  position: fixed;
  inset: 0;
  background: var(--bg-overlay);
  backdrop-filter: blur(5px);
  pointer-events: auto;
}

.wand-settings-overlay { z-index: 30; }
.wand-settings-nested-overlay { z-index: 40; }

.wand-settings-dialog,
.wand-settings-nested-dialog {
  position: fixed;
  top: 50%;
  left: 50%;
  z-index: 31;
  box-sizing: border-box;
  color: var(--text-primary);
  background: var(--bg-elevated);
  border: 1px solid var(--border-default);
  box-shadow: var(--shadow-xl);
  transform: translate(-50%, -50%);
  pointer-events: auto;
}

.wand-settings-dialog {
  display: flex;
  flex-direction: column;
  width: min(1120px, calc(100vw - var(--wand-safe-left) - var(--wand-safe-right) - 40px));
  height: min(820px, calc(100dvh - var(--wand-safe-top) - var(--wand-safe-bottom) - 40px));
  min-height: 520px;
  overflow: hidden;
  border-radius: var(--radius-lg);
}

.wand-settings-nested-dialog {
  z-index: 41;
  display: flex;
  flex-direction: column;
  width: min(760px, calc(100vw - var(--wand-safe-left) - var(--wand-safe-right) - 32px));
  max-height: calc(100dvh - var(--wand-safe-top) - var(--wand-safe-bottom) - 32px);
  overflow: auto;
  border-radius: var(--radius-lg);
  padding-bottom: 18px;
}

.wand-settings-dialog[data-state="open"],
.wand-settings-nested-dialog[data-state="open"] {
  animation: wand-ui-dialog-in var(--transition-normal);
}

.wand-settings-header {
  display: flex;
  flex: 0 0 auto;
  align-items: flex-start;
  justify-content: space-between;
  gap: 20px;
  border-bottom: 1px solid var(--border-subtle);
  padding: 18px 20px;
}

.wand-settings-title {
  margin: 0;
  font-size: var(--font-size-xl);
  line-height: var(--line-height-tight);
}

.wand-settings-description,
.wand-settings-panel-heading p,
.wand-settings-section-heading p {
  margin: 5px 0 0;
  color: var(--text-secondary);
  font-size: var(--font-size-sm);
}

.wand-settings-loading,
.wand-settings-load-error {
  display: grid;
  flex: 1;
  place-content: center;
  justify-items: center;
  gap: 14px;
  padding: 28px;
  color: var(--text-secondary);
}

.wand-settings-readonly {
  flex: 0 0 auto;
  margin: 12px 18px 0;
  border: 1px solid var(--warning);
  border-radius: var(--radius-sm);
  padding: 9px 12px;
  color: var(--text-secondary);
  background: var(--warning-muted);
  font-size: var(--font-size-sm);
}

.wand-settings-tabs {
  display: grid;
  grid-template-columns: 238px minmax(0, 1fr);
  flex: 1;
  min-height: 0;
  overflow: hidden;
}

.wand-settings-tabs > .wand-ui-tabs-list {
  display: flex;
  flex-direction: column;
  gap: 4px;
  box-sizing: border-box;
  overflow: auto;
  border-right: 1px solid var(--border-subtle);
  padding: 14px 12px;
  background: var(--bg-secondary);
}

.wand-settings-tabs > .wand-ui-tabs-list .wand-ui-tabs-trigger {
  min-height: 54px;
  padding: 9px 11px;
  text-align: left;
}

.wand-settings-tab-label {
  display: flex;
  flex-direction: column;
  gap: 2px;
  min-width: 0;
}

.wand-settings-tab-label strong {
  color: inherit;
  font-size: var(--font-size-sm);
}

.wand-settings-tab-label span {
  overflow: hidden;
  color: var(--text-secondary);
  font-size: var(--font-size-xs);
  text-overflow: ellipsis;
  white-space: nowrap;
}

.wand-settings-tabs > .wand-ui-tabs-content {
  grid-column: 2;
  min-width: 0;
  min-height: 0;
  margin: 0;
  overflow: auto;
  overscroll-behavior: contain;
}

.wand-ui-popover-content.wand-shell-menu-popover {
  width: auto;
  padding: 4px;
}

.wand-settings-panel {
  box-sizing: border-box;
  width: 100%;
  max-width: 860px;
  margin: 0 auto;
  padding: 22px 24px 34px;
}

.wand-settings-panel-heading {
  margin-bottom: 20px;
}

.wand-settings-panel-heading h2,
.wand-settings-section-heading h3 {
  margin: 0;
  color: var(--text-primary);
}

.wand-settings-panel-heading h2 { font-size: var(--font-size-lg); }
.wand-settings-section-heading h3 { font-size: var(--font-size-base); }

.wand-settings-section {
  margin-bottom: 16px;
  border: 1px solid var(--border-default);
  border-radius: var(--radius-md);
  background: var(--bg-secondary);
  box-shadow: var(--shadow-xs);
}

.wand-settings-section-heading {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 16px;
  border-bottom: 1px solid var(--border-subtle);
  padding: 14px 16px;
}

.wand-settings-section-action { flex: 0 0 auto; }

.wand-settings-section-body {
  display: flex;
  flex-direction: column;
  gap: 14px;
  padding: 16px;
}

.wand-settings-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 14px;
}

.wand-settings-field {
  display: flex;
  flex-direction: column;
  gap: 6px;
  min-width: 0;
}

.wand-settings-field > label,
.wand-settings-file-grid label > span {
  color: var(--text-primary);
  font-size: var(--font-size-sm);
  font-weight: var(--font-weight-semibold);
}

.wand-settings-input {
  box-sizing: border-box;
  width: 100%;
  min-height: 40px;
  border: 1px solid var(--border-default);
  border-radius: var(--radius-sm);
  padding: 8px 10px;
  color: var(--text-primary);
  background: var(--bg-primary);
  font: inherit;
  font-size: var(--font-size-sm);
}

.wand-settings-input:focus,
.wand-settings-range:focus-visible,
.wand-settings-file-grid input:focus-visible {
  border-color: var(--accent);
  outline: none;
  box-shadow: 0 0 0 3px var(--accent-muted);
}

.wand-settings-input[aria-invalid="true"] { border-color: var(--danger); }

.wand-settings-select .wand-ui-select-trigger { width: 100%; }

.wand-settings-field-hint,
.wand-settings-field-error,
.wand-settings-file-grid small {
  color: var(--text-muted);
  font-size: var(--font-size-xs);
}

.wand-settings-field-error { color: var(--danger); }

.wand-settings-toggle-row,
.wand-settings-download-row,
.wand-settings-cli-list > div,
.wand-settings-about-list > div {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 18px;
}

.wand-settings-toggle-row > div,
.wand-settings-download-row > div {
  display: flex;
  flex-direction: column;
  gap: 3px;
  min-width: 0;
}

.wand-settings-toggle-row strong,
.wand-settings-download-row strong { font-size: var(--font-size-sm); }
.wand-settings-toggle-row span,
.wand-settings-download-row span,
.wand-settings-cli-list span { color: var(--text-secondary); font-size: var(--font-size-xs); }

.wand-settings-status {
  border: 1px solid var(--border-default);
  border-radius: var(--radius-sm);
  padding: 9px 11px;
  color: var(--text-secondary);
  background: var(--bg-tertiary);
  font-size: var(--font-size-sm);
  white-space: pre-wrap;
}

.wand-settings-status-success { border-color: var(--success); background: var(--success-muted); }
.wand-settings-status-warning { border-color: var(--warning); background: var(--warning-muted); }
.wand-settings-status-error { border-color: var(--danger); background: var(--danger-muted); }

.wand-settings-save-bar {
  position: sticky;
  bottom: -34px;
  z-index: 2;
  display: flex;
  align-items: center;
  justify-content: flex-end;
  gap: 12px;
  margin: 18px -24px -34px;
  border-top: 1px solid var(--border-default);
  padding: 13px 24px calc(13px + var(--wand-safe-bottom));
  background: color-mix(in srgb, var(--bg-elevated) 94%, transparent);
  backdrop-filter: blur(12px);
}

.wand-settings-save-bar .wand-settings-status { flex: 1; }

.wand-settings-button-row {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
}

.wand-settings-about-list,
.wand-settings-cli-list {
  display: flex;
  flex-direction: column;
  gap: 10px;
  margin: 0;
}

.wand-settings-about-list > div,
.wand-settings-cli-list > div {
  border-bottom: 1px solid var(--border-subtle);
  padding-bottom: 9px;
}

.wand-settings-about-list > div:last-child,
.wand-settings-cli-list > div:last-child { border-bottom: 0; padding-bottom: 0; }
.wand-settings-about-list dt { color: var(--text-secondary); }
.wand-settings-about-list dd { margin: 0; max-width: 70%; overflow-wrap: anywhere; text-align: right; }

.wand-settings-connect-code {
  display: block;
  overflow: auto;
  border: 1px solid var(--border-default);
  border-radius: var(--radius-sm);
  padding: 10px;
  color: var(--text-primary);
  background: var(--bg-primary);
  font-size: var(--font-size-xs);
  overflow-wrap: anywhere;
  white-space: pre-wrap;
}

.wand-settings-qr-dialog { align-items: stretch; }
.wand-settings-qr-dialog canvas { align-self: center; max-width: calc(100% - 32px); height: auto; margin: 18px; border-radius: var(--radius-sm); }
.wand-settings-qr-dialog .wand-settings-connect-code { margin: 0 18px; }

.wand-settings-env-toolbar {
  display: grid;
  grid-template-columns: minmax(180px, 1fr) minmax(240px, 1fr);
  gap: 14px;
  padding: 16px 18px;
}

.wand-settings-env-list {
  min-height: 180px;
  max-height: 55vh;
  margin: 0 18px;
  overflow: auto;
  border: 1px solid var(--border-default);
  border-radius: var(--radius-sm);
  background: var(--bg-primary);
}

.wand-settings-env-row {
  display: grid;
  grid-template-columns: minmax(140px, .45fr) minmax(0, 1fr);
  gap: 12px;
  border-bottom: 1px solid var(--border-subtle);
  padding: 8px 10px;
  font-size: var(--font-size-xs);
}

.wand-settings-env-row span { overflow: hidden; color: var(--text-secondary); text-overflow: ellipsis; white-space: nowrap; }

.wand-settings-range { width: 100%; accent-color: var(--accent); }

.wand-settings-radio-group {
  display: flex;
  flex-wrap: wrap;
  gap: 8px 18px;
  margin: 0;
  border: 0;
  padding: 0;
}
.wand-settings-radio-group legend { width: 100%; margin-bottom: 4px; font-size: var(--font-size-sm); font-weight: var(--font-weight-semibold); }
.wand-settings-radio-group label { display: inline-flex; align-items: center; gap: 6px; cursor: pointer; }
.wand-settings-radio-group input { accent-color: var(--accent); }

.wand-settings-preset-list { display: grid; gap: 10px; }
.wand-settings-preset { display: flex; flex-direction: column; gap: 5px; border: 1px solid var(--border-default); border-radius: var(--radius-sm); padding: 12px 14px; background: var(--bg-secondary); }
.wand-settings-preset code { color: var(--text-secondary); overflow-wrap: anywhere; }
.wand-settings-preset span { color: var(--text-muted); font-size: var(--font-size-xs); }

.wand-settings-empty { padding: 20px; color: var(--text-muted); text-align: center; font-size: var(--font-size-sm); }

.wand-settings-security-form { display: flex; flex-direction: column; gap: 14px; }
.wand-settings-file-grid { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 12px; }
.wand-settings-file-grid label { display: flex; flex-direction: column; gap: 7px; border: 1px dashed var(--border-default); border-radius: var(--radius-sm); padding: 12px; }
.wand-settings-file-grid input { max-width: 100%; color: var(--text-secondary); font-size: var(--font-size-xs); }

.wand-quick-overlay {
  position: fixed;
  inset: 0;
  z-index: 0;
  background: rgba(20, 14, 8, 0.34);
  backdrop-filter: blur(18px) saturate(140%);
  -webkit-backdrop-filter: blur(18px) saturate(140%);
  pointer-events: auto;
}

.wand-quick-dialog {
  position: fixed;
  z-index: 1;
  top: 50%;
  left: 50%;
  box-sizing: border-box;
  display: flex;
  width: min(720px, calc(100vw - var(--wand-safe-left) - var(--wand-safe-right) - 32px));
  max-height: calc(100dvh - var(--wand-safe-top) - var(--wand-safe-bottom) - 32px);
  flex-direction: column;
  overflow: hidden;
  border: 1px solid rgba(255, 255, 255, 0.58);
  border-radius: 24px;
  color: var(--text-primary);
  background: rgba(255, 252, 247, 0.94);
  box-shadow: var(--shadow-xl);
  transform: translate(-50%, -50%);
  pointer-events: auto;
  backdrop-filter: blur(36px) saturate(165%);
  -webkit-backdrop-filter: blur(36px) saturate(165%);
}

.wand-quick-dialog[data-state="open"] { animation: wand-ui-dialog-in var(--transition-normal); }
.wand-quick-dialog[data-state="closed"] { animation: wand-ui-dialog-out var(--transition-fast); }

.wand-quick-header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 18px;
  padding: 21px 22px 17px;
  border-bottom: 1px solid var(--border-subtle);
}

.wand-quick-title {
  margin: 0;
  color: var(--text-primary);
  font-size: var(--font-size-xl);
  line-height: var(--line-height-tight);
}

.wand-quick-description {
  margin: 5px 0 0;
  color: var(--text-muted);
  font-size: var(--font-size-sm);
}

.wand-quick-header > .wand-ui-button {
  flex: 0 0 auto;
  width: 32px;
  min-width: 32px;
  min-height: 32px;
  padding: 0;
  border-radius: var(--radius-full);
}

.wand-quick-loading {
  padding: 42px 24px;
  color: var(--text-secondary);
  text-align: center;
}

.wand-quick-form {
  display: flex;
  min-height: 0;
  flex: 1 1 auto;
  flex-direction: column;
}

.wand-quick-body {
  display: grid;
  min-height: 0;
  gap: 17px;
  overflow-y: auto;
  padding: 18px 22px 20px;
}

.wand-quick-files,
.wand-quick-editor,
.wand-quick-actions {
  min-width: 0;
  margin: 0;
  border: 1px solid var(--border-subtle);
  border-radius: var(--radius-md);
  background: rgba(255, 255, 255, 0.48);
}

.wand-quick-actions { padding: 14px; }

.wand-quick-section-heading {
  display: flex;
  min-height: 35px;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
  padding: 8px 11px;
  border-bottom: 1px solid var(--border-subtle);
}

.wand-quick-section-heading h3,
.wand-quick-actions legend {
  margin: 0;
  color: var(--text-secondary);
  font-size: var(--font-size-xs);
  font-weight: var(--font-weight-semibold);
  letter-spacing: 0.04em;
  text-transform: uppercase;
}

.wand-quick-section-heading > span {
  min-width: 22px;
  border-radius: var(--radius-full);
  padding: 2px 7px;
  color: var(--text-muted);
  background: var(--bg-tertiary);
  font-size: var(--font-size-xs);
  text-align: center;
}

.wand-quick-file-list {
  display: grid;
  max-height: 148px;
  margin: 0;
  overflow-y: auto;
  padding: 5px 0;
  list-style: none;
}

.wand-quick-file-list li {
  display: flex;
  min-width: 0;
  align-items: center;
  gap: 9px;
  padding: 5px 11px;
  color: var(--text-secondary);
  font-size: var(--font-size-sm);
}

.wand-quick-file-list li:hover { background: rgba(125, 91, 57, 0.05); }

.wand-quick-file-badge {
  display: inline-flex;
  width: 20px;
  height: 20px;
  flex: 0 0 20px;
  align-items: center;
  justify-content: center;
  border-radius: 6px;
  color: var(--text-secondary);
  background: var(--bg-tertiary);
  font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
  font-size: 11px;
  font-weight: var(--font-weight-bold);
}

.wand-quick-file-badge-added,
.wand-quick-file-badge-untracked { color: var(--success); background: var(--success-muted); }
.wand-quick-file-badge-modified { color: var(--warning); background: var(--warning-muted); }
.wand-quick-file-badge-deleted { color: var(--danger); background: var(--danger-muted); }
.wand-quick-file-badge-renamed { color: var(--info); background: var(--info-muted); }
.wand-quick-file-badge-ignored { color: var(--text-muted); }

.wand-quick-file-path {
  min-width: 0;
  flex: 1 1 auto;
  overflow: hidden;
  font-family: ui-monospace, SFMono-Regular, Menlo, monospace;
  text-overflow: ellipsis;
  white-space: nowrap;
}

.wand-quick-submodule-badge {
  flex: 0 0 auto;
  border-radius: var(--radius-full);
  padding: 2px 7px;
  color: var(--info);
  background: var(--info-muted);
  font-size: 10px;
}

.wand-quick-empty {
  margin: 0;
  padding: 17px 12px;
  color: var(--text-muted);
  font-size: var(--font-size-sm);
  text-align: center;
}

.wand-quick-editor { padding-bottom: 12px; }
.wand-quick-editor .wand-quick-section-heading { margin-bottom: 12px; }

.wand-quick-field {
  display: grid;
  grid-template-columns: 120px minmax(0, 1fr);
  align-items: start;
  gap: 12px;
  padding: 5px 12px;
}

.wand-quick-field > span {
  padding-top: 9px;
  color: var(--text-muted);
  font-size: var(--font-size-xs);
  font-weight: var(--font-weight-semibold);
}

.wand-quick-field input,
.wand-quick-field textarea {
  box-sizing: border-box;
  width: 100%;
  border: 1px solid var(--border-default);
  border-radius: var(--radius-sm);
  padding: 9px 11px;
  color: var(--text-primary);
  background: var(--bg-primary);
  font: inherit;
  font-size: var(--font-size-sm);
  line-height: var(--line-height-base);
  resize: vertical;
}

.wand-quick-field input:focus,
.wand-quick-field textarea:focus {
  outline: none;
  border-color: var(--accent);
  box-shadow: 0 0 0 3px var(--accent-muted);
}

.wand-quick-actions legend { padding: 0 5px; }

.wand-quick-action-grid {
  display: grid;
  grid-template-columns: repeat(2, minmax(0, 1fr));
  gap: 8px;
}

.wand-quick-action-grid label {
  position: relative;
  display: grid;
  gap: 3px;
  min-width: 0;
  cursor: pointer;
  border: 1px solid var(--border-default);
  border-radius: var(--radius-sm);
  padding: 10px 12px 10px 36px;
  background: var(--bg-primary);
  transition: border-color var(--transition-fast), background var(--transition-fast), box-shadow var(--transition-fast);
}

.wand-quick-action-grid label:hover { border-color: var(--border-strong); }
.wand-quick-action-grid label.is-selected { border-color: var(--accent); background: var(--accent-muted); }
.wand-quick-action-grid label:focus-within { box-shadow: 0 0 0 2px var(--bg-primary), 0 0 0 4px var(--accent); }

.wand-quick-action-grid input {
  position: absolute;
  top: 13px;
  left: 13px;
  width: 15px;
  height: 15px;
  margin: 0;
  accent-color: var(--accent-active);
}

.wand-quick-action-grid strong { font-size: var(--font-size-sm); }
.wand-quick-action-grid span { color: var(--text-muted); font-size: var(--font-size-xs); }

.wand-quick-submodule-toggle {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 14px;
  margin-top: 11px;
  padding: 10px 11px;
  border-radius: var(--radius-sm);
  background: var(--bg-secondary);
}

.wand-quick-submodule-toggle > div:first-child { display: grid; gap: 2px; }
.wand-quick-submodule-toggle strong { font-size: var(--font-size-sm); }
.wand-quick-submodule-toggle span { color: var(--text-muted); font-size: var(--font-size-xs); }

.wand-quick-error {
  margin: 0;
  border-radius: var(--radius-sm);
  padding: 9px 11px;
  color: var(--danger);
  background: var(--danger-muted);
  font-size: var(--font-size-sm);
}

.wand-quick-footer {
  display: flex;
  flex: 0 0 auto;
  align-items: center;
  justify-content: space-between;
  gap: 16px;
  padding: 13px 22px calc(13px + var(--wand-safe-bottom));
  border-top: 1px solid var(--border-subtle);
  background: rgba(255, 252, 247, 0.9);
}

.wand-quick-footer > span { color: var(--text-muted); font-size: var(--font-size-xs); }
.wand-quick-footer > div { display: flex; gap: 8px; }

.wand-quick-result {
  display: grid;
  gap: 13px;
  overflow-y: auto;
  padding: 22px;
}

.wand-quick-result-pair {
  display: grid;
  grid-template-columns: 78px minmax(0, 1fr);
  align-items: center;
  gap: 12px;
}

.wand-quick-result-label { color: var(--text-muted); font-size: var(--font-size-xs); font-weight: var(--font-weight-semibold); }

.wand-quick-result-flow {
  display: grid;
  grid-template-columns: minmax(0, 1fr) auto minmax(0, 1fr);
  align-items: center;
  gap: 11px;
}

.wand-quick-result-flow > div {
  min-width: 0;
  border: 1px solid var(--border-subtle);
  border-radius: var(--radius-sm);
  padding: 10px;
  background: var(--bg-secondary);
}

.wand-quick-result-flow > span { color: var(--text-muted); }
.wand-quick-value-stack { display: grid; min-width: 0; gap: 4px; }
.wand-quick-value-stack > span { overflow: hidden; color: var(--text-secondary); font-size: var(--font-size-xs); text-overflow: ellipsis; white-space: nowrap; }
.wand-quick-result code { color: var(--accent-active); font-family: ui-monospace, SFMono-Regular, Menlo, monospace; font-size: var(--font-size-sm); }
.wand-quick-muted { color: var(--text-muted); font-size: var(--font-size-sm); }
.wand-quick-result-note { margin: 0; color: var(--text-secondary); font-size: var(--font-size-sm); }

.wand-quick-result-actions {
  display: flex;
  align-items: center;
  justify-content: flex-end;
  gap: 8px;
  padding-top: 4px;
}

.wand-quick-pushed { color: var(--success); font-size: var(--font-size-sm); font-weight: var(--font-weight-semibold); }


`;
  var sessionPickerAndWorktreeStyles = String.raw`
.wand-new-session-overlay {
  position: fixed;
  inset: 0;
  z-index: 0;
  background: rgba(20, 14, 8, 0.34);
  backdrop-filter: blur(18px) saturate(140%);
  -webkit-backdrop-filter: blur(18px) saturate(140%);
  pointer-events: auto;
}

.wand-new-session-dialog {
  position: fixed;
  z-index: 1;
  top: 50%;
  left: 50%;
  box-sizing: border-box;
  display: flex;
  flex-direction: column;
  width: min(560px, calc(100vw - var(--wand-safe-left) - var(--wand-safe-right) - 32px));
  max-height: calc(100dvh - var(--wand-safe-top) - var(--wand-safe-bottom) - 32px);
  overflow: hidden;
  border: 0.5px solid rgba(255, 255, 255, 0.6);
  border-radius: 26px;
  color: var(--text-primary);
  background: rgba(255, 252, 247, 0.88);
  box-shadow: var(--shadow-xl);
  transform: translate(-50%, -50%);
  pointer-events: auto;
  backdrop-filter: blur(40px) saturate(180%);
  -webkit-backdrop-filter: blur(40px) saturate(180%);
}

.wand-new-session-dialog[data-state="open"] {
  animation: wand-ui-dialog-in var(--transition-normal);
}

.wand-new-session-dialog[data-state="closed"] {
  animation: wand-ui-dialog-out var(--transition-fast);
}

.wand-new-session-header {
  display: flex;
  flex: 0 0 auto;
  align-items: center;
  justify-content: space-between;
  min-height: 56px;
  padding: 18px 22px 16px;
  border-bottom: 1px solid var(--border-subtle);
}

.wand-new-session-header > div {
  min-width: 0;
}

.wand-new-session-header > .wand-ui-button {
  flex: 0 0 auto;
  width: 32px;
  min-width: 32px;
  min-height: 32px;
  padding: 0;
  border-radius: var(--radius-full);
}

.wand-new-session-title {
  margin: 0;
  color: var(--text-primary);
  font-size: 1.3125rem;
  font-weight: 700;
  letter-spacing: -0.022em;
  line-height: 1.2;
}

.wand-new-session-description {
  max-width: 44ch;
  margin: 5px 0 0;
  color: var(--text-muted);
  font-size: 0.8125rem;
  font-weight: 400;
  line-height: 1.5;
}

.wand-new-session-form {
  display: flex;
  min-height: 0;
  flex: 1 1 auto;
  flex-direction: column;
}

.wand-new-session-body {
  flex: 1 1 auto;
  min-height: 0;
  overflow-y: auto;
  padding: 20px 28px;
}

.wand-new-session-field {
  margin-bottom: 14px;
}

.wand-new-session-field:last-of-type {
  margin-bottom: 16px;
}

.wand-new-session-fieldset {
  min-width: 0;
  padding: 0;
  border: 0;
}

.wand-new-session-field-label {
  display: block;
  margin-bottom: 8px;
  padding: 0;
  color: var(--text-secondary);
  font-size: 0.8125rem;
  font-weight: 600;
}

.wand-new-session-field-hint {
  margin: 6px 0 0;
  color: var(--text-muted);
  font-size: 0.75rem;
  line-height: 1.5;
}

.wand-new-session-choices {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
}

.wand-new-session-choice {
  display: flex;
  flex: 1 1 0;
  min-width: 0;
  flex-direction: column;
  align-items: center;
  gap: 3px;
  border: 1px solid rgba(125, 91, 57, 0.1);
  border-radius: 12px;
  padding: 12px 10px;
  color: inherit;
  background: rgba(255, 255, 255, 0.6);
  box-shadow: 0 1px 2px rgba(125, 91, 57, 0.03);
  cursor: pointer;
  outline: none;
  text-align: center;
  transition: background 0.16s ease, border-color 0.16s ease, box-shadow 0.16s ease, transform 0.16s ease;
}

.wand-new-session-choice:hover {
  border-color: rgba(125, 91, 57, 0.2);
  background: rgba(255, 255, 255, 0.92);
  box-shadow: 0 4px 12px rgba(125, 91, 57, 0.08);
  transform: translateY(-1px);
}

.wand-new-session-choice.active {
  border: 0.5px solid rgba(255, 255, 255, 0.8);
  background: linear-gradient(180deg, rgba(255, 252, 247, 0.95) 0%, rgba(255, 240, 224, 0.82) 100%);
  box-shadow:
    0 0 0 1px rgba(197, 101, 61, 0.32),
    0 0 0 4px rgba(197, 101, 61, 0.14),
    0 8px 22px -4px rgba(197, 101, 61, 0.3),
    inset 0 1px 0 rgba(255, 255, 255, 0.95),
    inset 0 -1px 0 rgba(197, 101, 61, 0.08);
  transform: none;
  backdrop-filter: blur(16px) saturate(180%);
  -webkit-backdrop-filter: blur(16px) saturate(180%);
}

.wand-new-session-choice:focus-visible:not(.active) {
  border-color: var(--accent);
  box-shadow: 0 0 0 1.5px var(--accent-muted);
}

.wand-new-session-choice-label {
  color: var(--text-primary);
  font-size: 0.8rem;
  font-weight: 600;
  line-height: 1.3;
}

.wand-provider-grok-mark {
  width: 20px;
  height: 20px;
  fill: currentColor;
}

.wand-new-session-choice-description {
  color: var(--text-muted);
  font-size: 0.68rem;
  line-height: 1.3;
}

.wand-new-session-choice.active .wand-new-session-choice-label {
  color: var(--accent);
}

.wand-new-session-choice.disabled {
  cursor: not-allowed;
  opacity: 0.45;
  pointer-events: none;
}

.wand-new-session-choice.disabled:hover {
  border-color: rgba(125, 91, 57, 0.2);
  background: rgba(255, 255, 255, 0.6);
}

.wand-new-session-choice.disabled .wand-new-session-choice-label,
.wand-new-session-choice.disabled .wand-new-session-choice-description {
  color: var(--text-muted);
}

.wand-new-session-choice.active.disabled {
  box-shadow: none;
}

.wand-new-session-suggestions-wrap {
  position: relative;
}

.wand-new-session-input {
  box-sizing: border-box;
  width: 100%;
  min-height: 44px;
  border: 1px solid rgba(125, 91, 57, 0.14);
  border-radius: 12px;
  padding: 11px 14px;
  color: var(--text-primary);
  background: rgba(255, 255, 255, 0.78);
  box-shadow: inset 0 1px 1.5px rgba(125, 91, 57, 0.04);
  font-family: var(--font-mono);
  font-size: 0.875rem;
  outline: none;
  transition: border-color 0.15s ease, box-shadow 0.15s ease, background 0.15s ease;
}

.wand-new-session-input:hover {
  border-color: rgba(125, 91, 57, 0.24);
  background: rgba(255, 255, 255, 0.92);
}

.wand-new-session-input:focus {
  border-color: var(--accent);
  background: #ffffff;
  box-shadow: inset 0 1px 1.5px rgba(125, 91, 57, 0.04), 0 0 0 3px var(--accent-muted);
}

.wand-new-session-input::placeholder {
  color: var(--text-muted);
}

.wand-new-session-suggestions {
  position: absolute;
  z-index: 50;
  top: calc(100% + 4px);
  right: 0;
  left: 0;
  max-height: 200px;
  overflow: hidden;
  border: 1px solid rgba(125, 91, 57, 0.14);
  border-radius: 12px;
  background: rgba(255, 252, 247, 0.96);
  box-shadow: 0 0 0 0.5px rgba(125, 91, 57, 0.05), 0 12px 28px -8px rgba(20, 14, 8, 0.18);
  backdrop-filter: blur(20px) saturate(180%);
  -webkit-backdrop-filter: blur(20px) saturate(180%);
}

.wand-new-session-suggestion {
  width: 100%;
  border: 0;
  padding: 10px 12px;
  color: var(--text-primary);
  background: transparent;
  cursor: pointer;
  font-family: var(--font-mono);
  font-size: 0.8125rem;
  text-align: left;
  transition: background var(--transition-fast);
}

.wand-new-session-suggestion:hover {
  background: rgba(232, 197, 174, 0.32);
}

.wand-new-session-suggestion small {
  display: block;
  margin-top: 2px;
  color: var(--text-muted);
  font-size: 0.6875rem;
}

.wand-new-session-suggestion-path {
  display: block;
  font-family: var(--font-mono);
}

.wand-new-session-recent-paths {
  display: flex;
  flex-wrap: wrap;
  gap: 5px;
  margin-top: 6px;
}

.wand-new-session-recent-path {
  display: inline-flex;
  align-items: center;
  max-width: 180px;
  overflow: hidden;
  border: 1px solid rgba(125, 91, 57, 0.12);
  border-radius: 999px;
  padding: 2px 10px;
  color: var(--text-secondary);
  background: rgba(255, 255, 255, 0.55);
  box-shadow: 0 1px 2px rgba(125, 91, 57, 0.03);
  cursor: pointer;
  font-family: var(--font-mono);
  font-size: 0.6875rem;
  white-space: nowrap;
  transition: background 0.16s ease, border-color 0.16s ease, color 0.16s ease, transform 0.16s ease;
}

.wand-new-session-recent-path:hover {
  border-color: var(--accent);
  color: var(--accent);
  background: #fff7ef;
  transform: translateY(-1px);
}

.wand-new-session-recent-path-value {
  min-width: 0;
  max-width: 100%;
}

.wand-new-session-worktree {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 16px;
  margin-top: 12px;
  padding: 10px 12px;
  border: 1px solid var(--border-subtle);
  border-radius: var(--radius-md);
  background: rgba(125, 91, 57, 0.04);
}

.wand-new-session-worktree > div:first-child {
  display: grid;
  min-width: 0;
  gap: 3px;
}

.wand-new-session-worktree strong {
  color: var(--text-primary);
  font-size: var(--font-size-sm);
}

.wand-new-session-worktree span {
  color: var(--text-muted);
  font-size: var(--font-size-xs);
  line-height: var(--line-height-base);
}

.wand-new-session-worktree .wand-ui-switch-row {
  flex: 0 0 auto;
}

.wand-new-session-footer {
  display: block;
  flex: 0 0 auto;
  padding: 16px 28px 22px;
  border-top: 1px solid rgba(125, 91, 57, 0.08);
}

.wand-new-session-submit {
  width: 100%;
}

.wand-new-session-error {
  margin: 8px 0 0;
  border: 1px solid rgba(178, 79, 69, 0.2);
  border-radius: 10px;
  padding: 10px 12px;
  color: var(--danger);
  background: rgba(178, 79, 69, 0.1);
  font-size: 0.75rem;
  animation: wand-new-session-shake 0.3s ease;
}

@keyframes wand-new-session-shake {
  0%, 100% { transform: translateX(0); }
  25% { transform: translateX(-4px); }
  75% { transform: translateX(4px); }
}

.wand-new-session-loading,
.wand-new-session-load-error {
  padding: 28px;
  color: var(--text-secondary);
  text-align: center;
}

.wand-new-session-load-error p {
  margin: 0 0 14px;
  color: var(--danger);
}

.wand-folder-picker-overlay {
  position: fixed;
  inset: 0;
  z-index: 0;
  background: rgba(20, 14, 8, 0.34);
  backdrop-filter: blur(18px) saturate(140%);
  -webkit-backdrop-filter: blur(18px) saturate(140%);
  pointer-events: auto;
}

.wand-folder-picker-dialog {
  position: fixed;
  z-index: 1;
  top: 50%;
  left: 50%;
  box-sizing: border-box;
  display: flex;
  width: min(620px, calc(100vw - var(--wand-safe-left) - var(--wand-safe-right) - 32px));
  max-height: calc(100dvh - var(--wand-safe-top) - var(--wand-safe-bottom) - 32px);
  flex-direction: column;
  overflow: hidden;
  border: 1px solid rgba(255, 255, 255, 0.6);
  border-radius: 24px;
  color: var(--text-primary);
  background: rgba(255, 252, 247, 0.94);
  box-shadow: var(--shadow-xl);
  transform: translate(-50%, -50%);
  pointer-events: auto;
  backdrop-filter: blur(34px) saturate(165%);
  -webkit-backdrop-filter: blur(34px) saturate(165%);
}

.wand-folder-picker-dialog[data-state="open"] { animation: wand-ui-dialog-in var(--transition-normal); }
.wand-folder-picker-dialog[data-state="closed"] { animation: wand-ui-dialog-out var(--transition-fast); }

.wand-folder-picker-header {
  display: flex;
  align-items: flex-start;
  justify-content: space-between;
  gap: 16px;
  border-bottom: 1px solid var(--border-subtle);
  padding: 20px 22px 16px;
}

.wand-folder-picker-header > .wand-ui-button {
  flex: 0 0 auto;
  width: 32px;
  min-width: 32px;
  min-height: 32px;
  padding: 0;
  border-radius: var(--radius-full);
}

.wand-folder-picker-title {
  margin: 0;
  font-size: var(--font-size-xl);
  line-height: var(--line-height-tight);
}

.wand-folder-picker-description {
  max-width: 480px;
  margin: 5px 0 0;
  color: var(--text-secondary);
  font-size: var(--font-size-sm);
}

.wand-folder-picker-form {
  display: flex;
  min-height: 0;
  flex: 1 1 auto;
  flex-direction: column;
  gap: 12px;
  padding: 16px 20px 18px;
}

.wand-folder-picker-quick {
  display: flex;
  flex-wrap: wrap;
  gap: 8px;
}

.wand-folder-picker-field {
  display: grid;
  gap: 6px;
  color: var(--text-primary);
  font-size: var(--font-size-sm);
  font-weight: var(--font-weight-semibold);
}

.wand-folder-picker-input {
  box-sizing: border-box;
  width: 100%;
  min-height: 42px;
  border: 1px solid var(--border-default);
  border-radius: var(--radius-sm);
  padding: 9px 11px;
  color: var(--text-primary);
  background: var(--bg-primary);
  font-family: var(--font-mono);
  font-size: var(--font-size-sm);
}

.wand-folder-picker-input:focus {
  border-color: var(--accent);
  outline: none;
  box-shadow: 0 0 0 3px var(--accent-muted);
}

.wand-folder-picker-input[aria-invalid="true"] { border-color: var(--danger); }

.wand-folder-picker-options {
  min-height: 112px;
  max-height: min(310px, 42dvh);
  overflow: auto;
  overscroll-behavior: contain;
  border: 1px solid var(--border-default);
  border-radius: var(--radius-md);
  background: var(--bg-primary);
}

.wand-folder-picker-option {
  appearance: none;
  display: grid;
  grid-template-columns: 20px minmax(0, 1fr);
  gap: 2px 8px;
  width: 100%;
  min-height: 48px;
  border: 0;
  border-bottom: 1px solid var(--border-subtle);
  padding: 8px 11px;
  color: var(--text-primary);
  background: transparent;
  font: inherit;
  text-align: left;
  cursor: pointer;
}

.wand-folder-picker-option:last-child { border-bottom: 0; }
.wand-folder-picker-option:hover,
.wand-folder-picker-option.active,
.wand-folder-picker-option[aria-selected="true"] { background: var(--accent-muted); }
.wand-folder-picker-option:focus-visible { outline: 2px solid var(--accent); outline-offset: -2px; }
.wand-folder-picker-option > span:first-child { grid-row: 1 / span 2; color: var(--text-muted); }
.wand-folder-picker-option > span:nth-child(2) { overflow: hidden; font-weight: var(--font-weight-semibold); text-overflow: ellipsis; white-space: nowrap; }
.wand-folder-picker-option code { grid-column: 2; overflow: hidden; color: var(--text-muted); font-size: var(--font-size-xs); text-overflow: ellipsis; white-space: nowrap; }

.wand-folder-picker-state {
  display: grid;
  min-height: 110px;
  place-items: center;
  padding: 16px;
  color: var(--text-muted);
  font-size: var(--font-size-sm);
  text-align: center;
}

.wand-folder-picker-error {
  margin: 0;
  border: 1px solid color-mix(in srgb, var(--danger) 32%, transparent);
  border-radius: var(--radius-sm);
  padding: 8px 10px;
  color: var(--danger);
  background: var(--danger-muted);
  font-size: var(--font-size-sm);
}

.wand-folder-picker-footer {
  display: flex;
  align-items: center;
  justify-content: space-between;
  gap: 12px;
}

.wand-folder-picker-footer > span {
  min-width: 0;
  overflow: hidden;
  color: var(--text-muted);
  font-family: var(--font-mono);
  font-size: var(--font-size-xs);
  text-overflow: ellipsis;
  white-space: nowrap;
}

.wand-folder-picker-footer > .wand-ui-button { flex: 0 0 auto; }

.wand-worktree-dialog {
  display: flex;
  width: min(620px, calc(100vw - var(--wand-safe-left) - var(--wand-safe-right) - 32px));
  max-height: calc(100dvh - var(--wand-safe-top) - var(--wand-safe-bottom) - 32px);
  flex-direction: column;
  overflow: hidden;
  padding: 0;
}

.wand-worktree-header {
  display: flex;
  flex: 0 0 auto;
  align-items: flex-start;
  justify-content: space-between;
  gap: 18px;
  padding: 20px 22px 16px;
  border-bottom: 1px solid var(--border-subtle);
}

.wand-worktree-header > div {
  min-width: 0;
}

.wand-worktree-header > .wand-ui-button {
  width: 32px;
  min-width: 32px;
  min-height: 32px;
  flex: 0 0 auto;
  border-radius: var(--radius-full);
  padding: 0;
}

.wand-worktree-title {
  margin: 0;
  color: var(--text-primary);
  font-size: var(--font-size-xl);
  line-height: var(--line-height-tight);
}

.wand-worktree-description {
  max-width: 450px;
  margin: 5px 0 0;
  color: var(--text-secondary);
  font-size: var(--font-size-sm);
}

.wand-worktree-body {
  display: flex;
  min-height: 0;
  flex: 1 1 auto;
  flex-direction: column;
  overflow-y: auto;
  padding: 18px 22px 20px;
}

.wand-worktree-content {
  display: flex;
  min-height: 168px;
  flex-direction: column;
  gap: 10px;
}

.wand-worktree-row {
  display: flex;
  align-items: baseline;
  justify-content: space-between;
  gap: 16px;
  border: 1px solid var(--border-subtle);
  border-radius: var(--radius-sm);
  padding: 10px 12px;
  background: var(--bg-secondary);
  font-size: var(--font-size-sm);
}

.wand-worktree-label,
.wand-worktree-row > span {
  min-width: 0;
  color: var(--text-secondary);
}

.wand-worktree-value {
  min-width: 0;
  color: var(--text-primary);
  overflow-wrap: anywhere;
  text-align: right;
}

.wand-worktree-value-warning { color: var(--warning); }
.wand-worktree-value-success { color: var(--success); }
.wand-worktree-value-error { color: var(--danger); }

.wand-worktree-commits {
  display: grid;
  gap: 7px;
}

.wand-worktree-commits-title {
  margin: 2px 0 0;
  color: var(--text-secondary);
  font-size: var(--font-size-xs);
}

.wand-worktree-commit-list {
  display: grid;
  gap: 7px;
}

.wand-worktree-row code {
  color: var(--accent-active);
  background: transparent;
  font-family: var(--font-mono);
  font-size: var(--font-size-xs);
}

.wand-worktree-status,
.wand-worktree-error {
  margin: 0;
  border: 1px solid var(--border-subtle);
  border-radius: var(--radius-sm);
  padding: 9px 11px;
  color: var(--text-secondary);
  background: var(--bg-secondary);
  font-size: var(--font-size-sm);
  line-height: var(--line-height-base);
}

.wand-worktree-status-success {
  border-color: color-mix(in srgb, var(--success) 28%, transparent);
  color: var(--success);
  background: var(--success-muted);
}

.wand-worktree-status-warning {
  border-color: color-mix(in srgb, var(--warning) 30%, transparent);
  color: var(--warning);
  background: var(--warning-muted);
}

.wand-worktree-status-error,
.wand-worktree-error {
  border-color: color-mix(in srgb, var(--danger) 30%, transparent);
  color: var(--danger);
  background: var(--danger-muted);
}

.wand-worktree-actions {
  display: flex;
  flex: 0 0 auto;
  justify-content: flex-end;
  gap: 8px;
  margin-top: 18px;
  padding-top: 14px;
  border-top: 1px solid var(--border-subtle);
}

.wand-worktree-actions > .wand-ui-button {
  min-width: 120px;
}

@media (max-width: 760px) {
  .wand-quick-dialog {
    width: calc(100vw - var(--wand-safe-left) - var(--wand-safe-right) - 14px);
    max-height: calc(100dvh - var(--wand-safe-top) - var(--wand-safe-bottom) - 14px);
    border-radius: var(--radius-md);
  }

  .wand-quick-header { padding: 16px 15px 13px; }
  .wand-quick-body { gap: 13px; padding: 13px 12px 16px; }
  .wand-quick-action-grid { grid-template-columns: minmax(0, 1fr); }
  .wand-quick-field { grid-template-columns: minmax(0, 1fr); gap: 5px; }
  .wand-quick-field > span { padding-top: 0; }
  .wand-quick-footer { align-items: stretch; flex-direction: column; padding: 11px 12px calc(11px + var(--wand-safe-bottom)); }
  .wand-quick-footer > div { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); }
  .wand-quick-result { padding: 16px 12px; }
  .wand-quick-result-pair { grid-template-columns: minmax(0, 1fr); gap: 5px; }
  .wand-quick-result-flow { grid-template-columns: minmax(0, 1fr); }
  .wand-quick-result-flow > span { transform: rotate(90deg); text-align: center; }

  .wand-new-session-dialog {
    width: calc(100vw - var(--wand-safe-left) - var(--wand-safe-right) - 20px);
    max-height: calc(100dvh - var(--wand-safe-top) - var(--wand-safe-bottom) - 20px);
    border-radius: var(--radius-md);
  }

  .wand-folder-picker-dialog {
    width: calc(100vw - var(--wand-safe-left) - var(--wand-safe-right) - 20px);
    max-height: calc(100dvh - var(--wand-safe-top) - var(--wand-safe-bottom) - 20px);
    border-radius: var(--radius-md);
  }

  .wand-folder-picker-header { padding: 16px 15px 13px; }
  .wand-folder-picker-description { display: none; }
  .wand-folder-picker-form { gap: 10px; padding: 13px 12px calc(13px + var(--wand-safe-bottom)); }
  .wand-folder-picker-options { max-height: min(330px, 45dvh); }
  .wand-folder-picker-footer { align-items: stretch; flex-direction: column; }
  .wand-folder-picker-footer > .wand-ui-button { width: 100%; }

  .wand-worktree-dialog {
    width: calc(100vw - var(--wand-safe-left) - var(--wand-safe-right) - 20px);
    max-height: calc(100dvh - var(--wand-safe-top) - var(--wand-safe-bottom) - 20px);
    border-radius: var(--radius-md);
  }

  .wand-worktree-header { padding: 16px 15px 13px; }
  .wand-worktree-description { display: none; }
  .wand-worktree-body { padding: 13px 12px calc(13px + var(--wand-safe-bottom)); }
  .wand-worktree-content { min-height: 0; }
  .wand-worktree-row { align-items: flex-start; flex-direction: column; gap: 4px; }
  .wand-worktree-value { text-align: left; }
  .wand-worktree-actions { align-items: stretch; flex-direction: column-reverse; }
  .wand-worktree-actions > .wand-ui-button { width: 100%; }

  .wand-settings-dialog {
    width: calc(100vw - var(--wand-safe-left) - var(--wand-safe-right) - 12px);
    height: calc(100dvh - var(--wand-safe-top) - var(--wand-safe-bottom) - 12px);
    min-height: 0;
    border-radius: var(--radius-md);
  }

  .wand-settings-header { padding: 14px 15px; }
  .wand-settings-description { display: none; }

  .wand-settings-tabs {
    display: grid;
    grid-template-columns: minmax(0, 1fr);
    grid-template-rows: auto minmax(0, 1fr);
  }

  .wand-settings-tabs > .wand-ui-tabs-list {
    grid-row: 1;
    flex-direction: row;
    gap: 5px;
    overflow-x: auto;
    border-right: 0;
    border-bottom: 1px solid var(--border-subtle);
    padding: 8px;
    scrollbar-width: none;
  }

  .wand-settings-tabs > .wand-ui-tabs-list .wand-ui-tabs-trigger {
    flex: 0 0 auto;
    min-height: 36px;
    padding: 7px 11px;
  }

  .wand-settings-tab-label span { display: none; }
  .wand-settings-tabs > .wand-ui-tabs-content { grid-column: 1; grid-row: 2; }
  .wand-settings-panel { padding: 18px 14px 28px; }
  .wand-settings-grid, .wand-settings-file-grid, .wand-settings-env-toolbar { grid-template-columns: minmax(0, 1fr); }
  .wand-settings-section-heading { flex-direction: column; }
  .wand-settings-section-action { width: 100%; }
  .wand-settings-section-action .wand-ui-button { width: 100%; }
  .wand-settings-save-bar { bottom: -28px; margin: 16px -14px -28px; padding: 10px 14px calc(10px + var(--wand-safe-bottom)); }
  .wand-settings-save-bar { flex-direction: column; align-items: stretch; }
  .wand-settings-toggle-row, .wand-settings-download-row { align-items: flex-start; }
  .wand-settings-about-list dd { max-width: 62%; }
  .wand-settings-nested-dialog { width: calc(100vw - var(--wand-safe-left) - var(--wand-safe-right) - 16px); max-height: calc(100dvh - var(--wand-safe-top) - var(--wand-safe-bottom) - 16px); }
}

@media (max-width: 640px) {
  .wand-new-session-header { padding: 16px 18px 14px; }
  .wand-new-session-title { font-size: 1.125rem; }
  .wand-new-session-body { padding: 14px 18px; }
  .wand-new-session-footer { padding: 10px 12px 14px; }
  .wand-new-session-input { font-size: 16px; }
}

@media (max-width: 390px) {
  .wand-new-session-footer { padding: 8px 10px 12px; }
}


`;

  // src/web-ui/react/styles.ts
  var REACT_UI_STYLE_ID = "wand-react-ui-styles";
  var reactUiStyles = [
    foundationStyles,
    settingsAndQuickCommitStyles,
    sharedMotionStyles,
    sessionPickerAndWorktreeStyles,
    reducedMotionStyles
  ].join("");
  function installReactUiStyles(target = document) {
    if (target.getElementById(REACT_UI_STYLE_ID)) return;
    const style = target.createElement("style");
    style.id = REACT_UI_STYLE_ID;
    style.textContent = reactUiStyles;
    target.head.appendChild(style);
  }

  // src/web-ui/react/shell/legacy-snapshot.ts
  var STATUS_LABELS = {
    idle: "\u7A7A\u95F2",
    stopped: "\u5DF2\u505C\u6B62",
    running: "\u8FD0\u884C\u4E2D",
    exited: "\u5DF2\u9000\u51FA",
    failed: "\u5DF2\u5931\u8D25"
  };
  function asString(value, fallback = "") {
    return typeof value === "string" ? value : fallback;
  }
  function timestamp(value, fallback = 0) {
    if (!value) return fallback;
    const parsed = new Date(value).getTime();
    return Number.isFinite(parsed) ? parsed : fallback;
  }
  function countSelected(map) {
    if (!map) return 0;
    return Object.values(map).filter(Boolean).length;
  }
  function authPhase(state2) {
    if (state2.config) return "authenticated";
    return state2.loginChecked ? "anonymous" : "booting";
  }
  function isAutomation(session) {
    const source = asString(session.sessionSource).toLowerCase();
    return source === "automation" || source === "startup";
  }
  function sessionStatusLabel(session) {
    if (session.permissionBlocked) return "\u7B49\u5F85\u6388\u6743";
    if (session.sessionKind === "structured" && session.structuredState?.inFlight) return "\u601D\u8003\u4E2D";
    const status = asString(session.status, "idle");
    return STATUS_LABELS[status] ?? status;
  }
  function sessionStatusTone(session) {
    if (session.permissionBlocked) return "permission-blocked";
    if (session.sessionKind === "structured" && session.structuredState?.inFlight) return "running";
    return asString(session.status);
  }
  function defaultCwd(state2) {
    return asString(state2.workingDir) || asString(state2.config?.defaultCwd) || asString(state2.config?.cwd) || "/tmp";
  }
  function sessionTitle(session) {
    return asString(session.title) || asString(session.description) || asString(session.summary) || asString(session.command) || "Wand \u4F1A\u8BDD";
  }
  function sessionToVm(session, state2, manageSelection) {
    const id = asString(session.id);
    const provider = asString(session.provider, asString(session.command, "claude"));
    const status = asString(session.status, "idle");
    const kind = session.sessionKind === "structured" ? "structured" : "pty";
    const worktreeEnabled = Boolean(session.worktree?.enabled ?? session.worktreeEnabled);
    const source = isAutomation(session) ? "automation" : "wand";
    return {
      id,
      source,
      provider,
      kind,
      title: sessionTitle(session),
      description: asString(session.description),
      cwd: asString(session.cwd, defaultCwd(state2)),
      status,
      statusLabel: sessionStatusLabel(session),
      active: id !== "" && id === state2.selectedId,
      selected: Boolean(manageSelection[id]),
      resumable: kind !== "structured" && status !== "running" && (provider === "claude" || provider === "codex") && Boolean(session.claudeSessionId),
      permissionBlocked: Boolean(session.permissionBlocked),
      inFlight: Boolean(session.structuredState?.inFlight),
      ...session.startedAt ? { startedAt: session.startedAt } : {},
      ...session.endedAt ? { endedAt: session.endedAt } : {},
      ...session.claudeSessionId ? { claudeSessionId: session.claudeSessionId } : {},
      ...worktreeEnabled ? {
        worktree: {
          enabled: true,
          branch: asString(session.worktree?.branch ?? session.worktreeBranch) || void 0,
          path: asString(session.worktree?.path ?? session.worktreePath) || void 0,
          mergeStatus: asString(session.worktree?.mergeStatus ?? session.worktreeMergeStatus) || void 0
        }
      } : {}
    };
  }
  function historyToVm(history2, provider, state2, selected) {
    const id = asString(history2.claudeSessionId);
    const historyStartedAt = history2.timestamp || (Number.isFinite(history2.mtimeMs) && Number(history2.mtimeMs) > 0 ? new Date(Number(history2.mtimeMs)).toISOString() : void 0);
    return {
      id,
      source: provider === "codex" ? "codex-history" : "claude-history",
      provider,
      kind: "pty",
      title: asString(history2.firstUserMessage) || asString(history2.title) || asString(history2.summary) || "\uFF08\u7A7A\u4F1A\u8BDD\uFF09",
      description: "",
      cwd: asString(history2.cwd, defaultCwd(state2)),
      status: "stopped",
      statusLabel: "\u5386\u53F2",
      active: false,
      selected: Boolean(selected[id]),
      resumable: Boolean(id),
      permissionBlocked: false,
      inFlight: false,
      ...historyStartedAt ? { startedAt: historyStartedAt } : {},
      ...id ? { claudeSessionId: id } : {}
    };
  }
  function visibleHistory(histories, managedIds) {
    return (histories ?? []).filter((history2) => {
      const id = asString(history2.claudeSessionId);
      return Boolean(id) && Boolean(history2.hasConversation) && !history2.managedByWand && !managedIds.has(id);
    });
  }
  function sortSessionVms(entries) {
    return entries.sort((left, right) => timestamp(right.startedAt) - timestamp(left.startedAt));
  }
  function deriveLegacyUiSnapshot(state2, environment) {
    const sessions = state2.sessions ?? [];
    const sessionSelection = state2.selectedSessionIds ?? {};
    const claudeSelection = state2.selectedClaudeHistoryIds ?? {};
    const codexSelection = state2.selectedCodexHistoryIds ?? {};
    const sessionVms = sessions.map((session) => sessionToVm(session, state2, sessionSelection));
    const selected = sessionVms.find((session) => session.id === state2.selectedId) ?? null;
    const selectedLegacy = sessions.find((session) => session.id === state2.selectedId) ?? null;
    const managedProviderIds = new Set(
      sessions.map((session) => asString(session.claudeSessionId)).filter(Boolean)
    );
    const wand = sortSessionVms(sessionVms.filter((session) => session.source === "wand"));
    const automation = sortSessionVms(sessionVms.filter((session) => session.source === "automation"));
    const histories = [];
    if (state2.claudeHistoryLoaded) {
      histories.push(...visibleHistory(state2.claudeHistory, managedProviderIds).map((history2) => historyToVm(history2, "claude", state2, claudeSelection)));
    }
    if (state2.codexHistoryLoaded) {
      histories.push(...visibleHistory(state2.codexHistory, managedProviderIds).map((history2) => historyToVm(history2, "codex", state2, codexSelection)));
    }
    histories.sort((left, right) => timestamp(right.startedAt) - timestamp(left.startedAt));
    const mobile = environment.width <= 768;
    const drawerOpen = Boolean(state2.sessionsDrawerOpen);
    const sidebarPinned = Boolean(state2.sidebarPinned);
    const sidebarCollapsed = Boolean(state2.sidebarCollapsed);
    const filePanelOpen = Boolean(state2.filePanelOpen);
    const structuredSelected = selected?.kind === "structured";
    const currentView = structuredSelected || state2.currentView === "chat" ? "chat" : "terminal";
    const manageMode = Boolean(state2.sessionsManageMode);
    const selectedCount = countSelected(sessionSelection) + countSelected(claudeSelection) + countSelected(codexSelection);
    const effectiveCwd = selected?.cwd ?? defaultCwd(state2);
    const gitStatus = selected && state2.gitStatusSessionId === selected.id && state2.gitStatus?.isGit ? {
      branch: asString(state2.gitStatus.branch, "?"),
      modifiedCount: Number(state2.gitStatus.modifiedCount) || 0,
      clean: (Number(state2.gitStatus.modifiedCount) || 0) === 0
    } : null;
    return {
      auth: { phase: authPhase(state2) },
      viewport: {
        mobile,
        online: typeof state2.isOnline === "boolean" ? state2.isOnline : environment.online,
        embedTerminal: environment.embedTerminal,
        nativeInput: environment.nativeInput
      },
      capabilities: {
        backToNative: environment.backToNative,
        switchServer: environment.switchServer
      },
      layout: {
        sessionsDrawerOpen: drawerOpen,
        sidebarPinned,
        sidebarCollapsed,
        sidebarAnchored: sidebarCollapsed || !mobile && (sidebarPinned || drawerOpen),
        sessionsBackdropVisible: drawerOpen && (mobile || !sidebarPinned),
        filePanelOpen,
        filePanelBackdropVisible: filePanelOpen && mobile,
        topbarMoreOpen: Boolean(state2.topbarMoreOpen),
        currentView
      },
      selected,
      sidebar: {
        interactiveCount: sessions.filter((session) => !isAutomation(session)).length,
        totalCount: wand.length + automation.length + histories.length,
        manageMode,
        selectedCount,
        groups: [
          { kind: "wand", label: "Wand \u4F1A\u8BDD", expanded: true, entries: wand },
          {
            kind: "automation",
            label: "\u81EA\u52A8\u5316",
            expanded: manageMode || Boolean(environment.automationExpanded),
            entries: automation
          },
          {
            kind: "history",
            label: "\u975E Wand \u4F1A\u8BDD",
            expanded: manageMode || Boolean(environment.historyExpanded),
            entries: histories
          }
        ]
      },
      topbar: {
        title: selected?.title ?? "Wand \u63A7\u5236\u53F0",
        description: selected?.description ?? "",
        statusLabel: selected?.statusLabel ?? "",
        statusTone: selectedLegacy ? sessionStatusTone(selectedLegacy) : "",
        cwd: effectiveCwd,
        currentTask: asString(state2.currentTask?.title) || asString(selectedLegacy?.currentTaskTitle),
        git: gitStatus
      },
      legacyVisibility: {
        terminal: Boolean(selected) && currentView === "terminal",
        chat: Boolean(selected) && currentView === "chat",
        blank: !selected,
        composer: Boolean(selected)
      }
    };
  }

  // src/web-ui/react/shell/legacy-hosts.ts
  var LegacyHost = class {
    constructor(name, port) {
      __publicField(this, "name");
      __publicField(this, "port");
      __publicField(this, "rememberedSlot", null);
      __publicField(this, "mounted", false);
      __publicField(this, "disposed", false);
      __publicField(this, "generation", 0);
      this.name = name;
      this.port = port;
    }
    mount(slot) {
      if (this.disposed) throw new Error(`${this.name} has been disposed`);
      if (this.rememberedSlot && this.rememberedSlot !== slot) {
        throw new Error(`${this.name} cannot switch to a different slot`);
      }
      if (this.mounted) return this.currentLease();
      if (!this.rememberedSlot && slot.childNodes.length !== 0) {
        throw new Error(`${this.name} requires an empty React slot`);
      }
      this.port.mount(slot);
      this.rememberedSlot = slot;
      this.mounted = true;
      this.generation += 1;
      return this.currentLease();
    }
    unmount(slot) {
      if (slot && this.rememberedSlot && slot !== this.rememberedSlot) {
        throw new Error(`${this.name} cannot unmount a different slot`);
      }
      if (!this.mounted || !this.rememberedSlot) return;
      const activeSlot = this.rememberedSlot;
      this.mounted = false;
      this.generation += 1;
      this.port.unmount?.(activeSlot);
    }
    currentLease() {
      if (!this.mounted || !this.rememberedSlot || this.disposed) {
        throw new Error(`${this.name} is not mounted`);
      }
      return Object.freeze({ slot: this.rememberedSlot, generation: this.generation });
    }
    isCurrent(lease) {
      return !this.disposed && this.mounted && this.rememberedSlot === lease.slot && this.generation === lease.generation;
    }
    isMounted() {
      return !this.disposed && this.mounted;
    }
    dispose() {
      if (this.disposed) return;
      try {
        this.unmount();
      } finally {
        this.disposed = true;
      }
    }
  };

  // src/web-ui/react/shell/legacy-ui-actions.ts
  function applyLegacyUiAction(commands2, action) {
    switch (action.type) {
      case "nav.home":
        return commands2.goHome();
      case "nav.refresh":
        return commands2.refreshPage();
      case "session.new":
        return commands2.openNewSession();
      case "session.quickStart.claude":
        return commands2.quickStartClaudeTerminal();
      case "session.quickStart.codex":
        return commands2.quickStartCodexTerminal();
      case "session.quickStart.opencode":
        return commands2.quickStartOpenCodeTerminal();
      case "session.quickStart.structured":
        return commands2.quickStartStructuredSession();
      case "session.select":
        return commands2.selectSession(action.id);
      case "session.resume":
        return commands2.resumeSession(action.id);
      case "session.resumeHistory":
        return commands2.resumeHistory(action.provider, action.id, action.cwd);
      case "session.delete":
        return commands2.deleteItem(action.target, action.id);
      case "session.merge":
        return commands2.mergeSession(action.id);
      case "session.cleanup":
        return commands2.cleanupSession(action.id);
      case "session.manage.toggle":
        return commands2.toggleManageMode();
      case "session.manage.select":
        return commands2.toggleManagedSelection(action.target, action.id);
      case "session.manage.selectAll":
        return commands2.selectAllManaged();
      case "session.manage.clear":
        return commands2.clearManagedSelection();
      case "session.manage.deleteSelected":
        return commands2.deleteManagedSelection();
      case "layout.drawer.toggle":
        return commands2.toggleSessionsDrawer();
      case "layout.drawer.close":
        return commands2.closeSessionsDrawer();
      case "layout.drawer.pin":
        return commands2.toggleSidebarPin();
      case "layout.drawer.collapse":
        return commands2.toggleSidebarCollapsed();
      case "layout.drawer.expandGroup":
        return commands2.expandSidebarGroup(action.group);
      case "layout.drawer.group.set":
        return commands2.setSidebarGroupExpanded(action.group, action.expanded);
      case "layout.files.toggle":
        return commands2.toggleFilePanel();
      case "layout.files.close":
        return commands2.closeFilePanel();
      case "layout.files.refresh":
        return commands2.refreshFiles();
      case "layout.files.navigate":
        return commands2.navigateFiles(action.cwd);
      case "layout.files.up":
        return commands2.navigateFilesUp();
      case "layout.files.search":
        return commands2.searchFiles(action.query);
      case "layout.files.search.clear":
        return commands2.clearFileSearch();
      case "folderPicker.open":
        return commands2.openFolderPicker();
      case "topbar.menu.toggle":
        return commands2.toggleTopbarMenu();
      case "topbar.copy":
        return commands2.copyTopbarField(action.field);
      case "topbar.gitCommit":
        return commands2.openGitCommit();
      case "settings.open":
        return commands2.openSettings();
      case "native.back":
        return commands2.backToNative();
      case "native.switchServer":
        return commands2.switchServer();
      case "auth.logout":
        return commands2.logout();
      default:
        return assertNever(action);
    }
  }
  function assertNever(action) {
    throw new Error(`Unsupported UI action: ${JSON.stringify(action)}`);
  }

  // src/web-ui/react/shell/shell-file-panel.tsx
  var React39 = __toESM(require_react(), 1);

  // src/web-ui/react/shell/ui-store-react.tsx
  var import_react17 = __toESM(require_react(), 1);
  var UiStoreContext = (0, import_react17.createContext)(null);
  function createUiStoreExternalSource(store) {
    return {
      subscribe: (listener) => store.subscribe(listener),
      getSnapshot: () => store.getSnapshot()
    };
  }
  function UiStoreProvider({ store, children }) {
    return (0, import_react17.createElement)(UiStoreContext.Provider, { value: store }, children);
  }
  function useUiStore() {
    const store = (0, import_react17.useContext)(UiStoreContext);
    if (!store) {
      throw new Error("useUiStore must be used inside UiStoreProvider");
    }
    return store;
  }
  function useUiStoreSnapshot() {
    const store = useUiStore();
    const source = (0, import_react17.useMemo)(() => createUiStoreExternalSource(store), [store]);
    return (0, import_react17.useSyncExternalStore)(source.subscribe, source.getSnapshot, source.getSnapshot);
  }
  function useUiDispatch() {
    const store = useUiStore();
    return (0, import_react17.useCallback)((action) => store.dispatch(action), [store]);
  }

  // src/web-ui/react/shell/shell-file-panel.tsx
  var import_jsx_runtime37 = __toESM(require_jsx_runtime(), 1);
  function classNames2(...values) {
    return values.filter(Boolean).join(" ");
  }
  function normalizeFilePanelCwd(raw) {
    let cwd = raw.trim();
    if (!cwd) return "";
    cwd = cwd.replace(/\/{2,}/g, "/");
    if (cwd.length > 1) cwd = cwd.replace(/\/+$/, "");
    return cwd;
  }
  function getParentFilePanelCwd(raw) {
    const cwd = normalizeFilePanelCwd(raw);
    if (!cwd || cwd === "/") return cwd || "/";
    const parent = cwd.replace(/\/[^/]+$/, "");
    return parent || "/";
  }
  function FileIcon({ name, size: size4 = 16 }) {
    const common = {
      className: `wand-icon wand-icon-${name}`,
      width: size4,
      height: size4,
      viewBox: "0 0 24 24",
      fill: "none",
      stroke: "currentColor",
      strokeWidth: 2,
      strokeLinecap: "round",
      strokeLinejoin: "round",
      "aria-hidden": true
    };
    switch (name) {
      case "close":
        return /* @__PURE__ */ (0, import_jsx_runtime37.jsx)("svg", { ...common, children: /* @__PURE__ */ (0, import_jsx_runtime37.jsx)("path", { d: "M18 6L6 18M6 6l12 12" }) });
      case "folder":
        return /* @__PURE__ */ (0, import_jsx_runtime37.jsx)("svg", { ...common, className: "wand-icon wand-icon-folder-open", children: /* @__PURE__ */ (0, import_jsx_runtime37.jsx)("path", { d: "M6 14l1.5-3A2 2 0 019.2 10H20a2 2 0 011.9 2.5l-1.5 6A2 2 0 0118.4 20H4a2 2 0 01-2-2V5a2 2 0 012-2h4l3 3h7a2 2 0 012 2v2" }) });
      case "refresh":
        return /* @__PURE__ */ (0, import_jsx_runtime37.jsx)("svg", { ...common, children: /* @__PURE__ */ (0, import_jsx_runtime37.jsx)("path", { d: "M21 12a9 9 0 11-3-6.7M21 4v5h-5" }) });
      case "search":
        return /* @__PURE__ */ (0, import_jsx_runtime37.jsxs)("svg", { ...common, children: [
          /* @__PURE__ */ (0, import_jsx_runtime37.jsx)("circle", { cx: "11", cy: "11", r: "7" }),
          /* @__PURE__ */ (0, import_jsx_runtime37.jsx)("path", { d: "M21 21l-4.3-4.3" })
        ] });
      case "up":
        return /* @__PURE__ */ (0, import_jsx_runtime37.jsx)("svg", { ...common, children: /* @__PURE__ */ (0, import_jsx_runtime37.jsx)("path", { d: "M12 19V5M5 12l7-7 7 7" }) });
    }
  }
  function ShellFilePanel({ explorerRef } = {}) {
    const snapshot7 = useUiStoreSnapshot();
    const dispatch = useUiDispatch();
    const snapshotCwd = normalizeFilePanelCwd(snapshot7.topbar.cwd) || "/";
    const [cwd, setCwd] = React39.useState(snapshotCwd);
    const [search, setSearch] = React39.useState("");
    const committedCwd = React39.useRef(snapshotCwd);
    const editingCwd = React39.useRef(false);
    React39.useEffect(() => {
      if (editingCwd.current) return;
      committedCwd.current = snapshotCwd;
      setCwd(snapshotCwd);
    }, [snapshot7.selected?.id, snapshotCwd]);
    const commitCwd = React39.useCallback(() => {
      const normalized = normalizeFilePanelCwd(cwd);
      if (!normalized) {
        setCwd(committedCwd.current);
        return;
      }
      setCwd(normalized);
      if (normalized === committedCwd.current) return;
      committedCwd.current = normalized;
      void dispatch({ type: "layout.files.navigate", cwd: normalized });
    }, [cwd, dispatch]);
    const parentCwd = getParentFilePanelCwd(committedCwd.current);
    return /* @__PURE__ */ (0, import_jsx_runtime37.jsxs)(import_jsx_runtime37.Fragment, { children: [
      /* @__PURE__ */ (0, import_jsx_runtime37.jsx)(
        "div",
        {
          id: "file-panel-backdrop",
          className: classNames2("file-panel-backdrop", snapshot7.layout.filePanelBackdropVisible && "open"),
          "aria-hidden": "true",
          onClick: () => void dispatch({ type: "layout.files.close" })
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime37.jsxs)(
        "div",
        {
          id: "file-side-panel",
          className: classNames2("file-side-panel", snapshot7.layout.filePanelOpen && "open"),
          "aria-hidden": !snapshot7.layout.filePanelOpen,
          children: [
            /* @__PURE__ */ (0, import_jsx_runtime37.jsxs)("div", { className: "file-side-panel-header", children: [
              /* @__PURE__ */ (0, import_jsx_runtime37.jsxs)("div", { className: "file-side-panel-title-group", children: [
                /* @__PURE__ */ (0, import_jsx_runtime37.jsx)("span", { className: "file-side-panel-icon", children: /* @__PURE__ */ (0, import_jsx_runtime37.jsx)(FileIcon, { name: "folder" }) }),
                /* @__PURE__ */ (0, import_jsx_runtime37.jsx)("span", { className: "file-side-panel-title", children: "\u6587\u4EF6" })
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime37.jsxs)("div", { className: "file-side-panel-header-actions", children: [
                /* @__PURE__ */ (0, import_jsx_runtime37.jsx)(
                  "button",
                  {
                    className: "file-side-panel-iconbtn",
                    id: "file-explorer-refresh",
                    type: "button",
                    title: "\u5237\u65B0",
                    "aria-label": "\u5237\u65B0\u6587\u4EF6\u5217\u8868",
                    onClick: () => void dispatch({ type: "layout.files.refresh" }),
                    children: /* @__PURE__ */ (0, import_jsx_runtime37.jsx)(FileIcon, { name: "refresh", size: 15 })
                  }
                ),
                /* @__PURE__ */ (0, import_jsx_runtime37.jsx)(
                  "button",
                  {
                    id: "file-side-panel-close",
                    className: "file-side-panel-iconbtn close",
                    type: "button",
                    "aria-label": "\u5173\u95ED\u6587\u4EF6\u9762\u677F",
                    title: "\u5173\u95ED",
                    onClick: () => void dispatch({ type: "layout.files.close" }),
                    children: /* @__PURE__ */ (0, import_jsx_runtime37.jsx)(FileIcon, { name: "close" })
                  }
                )
              ] })
            ] }),
            /* @__PURE__ */ (0, import_jsx_runtime37.jsxs)("div", { className: "file-side-panel-body", children: [
              /* @__PURE__ */ (0, import_jsx_runtime37.jsxs)("div", { className: "file-explorer-header", children: [
                /* @__PURE__ */ (0, import_jsx_runtime37.jsx)(
                  "button",
                  {
                    className: "file-explorer-up",
                    id: "file-explorer-up",
                    type: "button",
                    title: "\u8FD4\u56DE\u4E0A\u7EA7\u76EE\u5F55",
                    "aria-label": "\u8FD4\u56DE\u4E0A\u7EA7\u76EE\u5F55",
                    disabled: committedCwd.current === "/",
                    onClick: () => {
                      if (parentCwd === committedCwd.current) return;
                      committedCwd.current = parentCwd;
                      setCwd(parentCwd);
                      void dispatch({ type: "layout.files.up" });
                    },
                    children: /* @__PURE__ */ (0, import_jsx_runtime37.jsx)(FileIcon, { name: "up", size: 15 })
                  }
                ),
                /* @__PURE__ */ (0, import_jsx_runtime37.jsx)(
                  "input",
                  {
                    type: "text",
                    className: "file-explorer-path",
                    id: "file-explorer-cwd",
                    value: cwd,
                    title: cwd,
                    placeholder: "\u8F93\u5165\u8DEF\u5F84\u5E76\u56DE\u8F66...",
                    spellCheck: false,
                    autoComplete: "off",
                    autoCapitalize: "off",
                    autoCorrect: "off",
                    "aria-label": "\u5F53\u524D\u8DEF\u5F84\uFF0C\u53EF\u76F4\u63A5\u4FEE\u6539\u540E\u56DE\u8F66",
                    onFocus: (event) => {
                      editingCwd.current = true;
                      event.currentTarget.select();
                    },
                    onChange: (event) => setCwd(event.currentTarget.value),
                    onBlur: () => {
                      editingCwd.current = false;
                      commitCwd();
                    },
                    onKeyDown: (event) => {
                      if (event.key === "Enter") {
                        event.preventDefault();
                        commitCwd();
                        event.currentTarget.blur();
                      } else if (event.key === "Escape") {
                        event.preventDefault();
                        editingCwd.current = false;
                        setCwd(committedCwd.current);
                        event.currentTarget.blur();
                      }
                    }
                  }
                )
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime37.jsxs)("div", { className: "file-search-box", children: [
                /* @__PURE__ */ (0, import_jsx_runtime37.jsx)("span", { className: "file-search-icon", children: /* @__PURE__ */ (0, import_jsx_runtime37.jsx)(FileIcon, { name: "search", size: 14 }) }),
                /* @__PURE__ */ (0, import_jsx_runtime37.jsx)(
                  "input",
                  {
                    type: "text",
                    id: "file-search-input",
                    className: "file-search-input",
                    value: search,
                    placeholder: "\u641C\u7D22\u5F53\u524D\u76EE\u5F55\u2026",
                    autoComplete: "off",
                    autoCorrect: "off",
                    autoCapitalize: "off",
                    spellCheck: false,
                    "aria-label": "\u641C\u7D22\u5F53\u524D\u76EE\u5F55",
                    onChange: (event) => {
                      const next = event.currentTarget.value;
                      setSearch(next);
                      void dispatch({ type: "layout.files.search", query: next.trim() });
                    }
                  }
                ),
                /* @__PURE__ */ (0, import_jsx_runtime37.jsx)(
                  "button",
                  {
                    className: classNames2("file-search-clear", search.trim() && "visible"),
                    id: "file-search-clear",
                    type: "button",
                    "aria-label": "\u6E05\u9664\u641C\u7D22",
                    title: "\u6E05\u9664",
                    disabled: !search,
                    onClick: () => {
                      setSearch("");
                      void dispatch({ type: "layout.files.search.clear" });
                    },
                    children: /* @__PURE__ */ (0, import_jsx_runtime37.jsx)(FileIcon, { name: "close", size: 13 })
                  }
                )
              ] }),
              /* @__PURE__ */ (0, import_jsx_runtime37.jsx)("div", { className: "file-explorer", id: "file-explorer", ref: explorerRef })
            ] })
          ]
        }
      )
    ] });
  }

  // src/web-ui/react/shell/shell-sidebar.tsx
  var React40 = __toESM(require_react(), 1);
  var import_jsx_runtime38 = __toESM(require_jsx_runtime(), 1);
  function classNames3(...values) {
    return values.filter(Boolean).join(" ");
  }
  function getSidebarEntryTarget(entry) {
    if (entry.source === "claude-history") return "claude-history";
    if (entry.source === "codex-history") return "codex-history";
    return "session";
  }
  function getShellSidebarEntryActions(entry, manageMode) {
    const target = getSidebarEntryTarget(entry);
    if (manageMode) {
      return {
        primary: { type: "session.manage.select", target, id: entry.id },
        resume: null,
        delete: null,
        merge: null,
        cleanup: null
      };
    }
    const historyProvider = entry.source === "codex-history" ? "codex" : entry.source === "claude-history" ? "claude" : null;
    const historyResume = historyProvider ? { type: "session.resumeHistory", provider: historyProvider, id: entry.id, cwd: entry.cwd } : null;
    const cleanup = entry.worktree?.enabled && entry.worktree.mergeStatus === "merged" ? { type: "session.cleanup", id: entry.id } : null;
    const merge = entry.worktree?.enabled && entry.worktree.branch && entry.worktree.path && entry.worktree.mergeStatus !== "merged" ? { type: "session.merge", id: entry.id } : null;
    return {
      primary: historyResume ?? { type: "session.select", id: entry.id },
      resume: historyResume ?? (entry.resumable ? { type: "session.resume", id: entry.id } : null),
      delete: { type: "session.delete", target, id: entry.id },
      merge,
      cleanup
    };
  }
  function Icon({ name, size: size4 = 14, className }) {
    const common = {
      width: size4,
      height: size4,
      viewBox: "0 0 24 24",
      fill: "none",
      stroke: "currentColor",
      strokeWidth: 2,
      strokeLinecap: "round",
      strokeLinejoin: "round",
      className,
      "aria-hidden": true
    };
    switch (name) {
      case "back":
        return /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)("svg", { ...common, children: [
          /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("rect", { x: "10", y: "3", width: "11", height: "18", rx: "2" }),
          /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("path", { d: "M7 8l-4 4 4 4M3 12h11" })
        ] });
      case "chevron":
        return /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("svg", { ...common, children: /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("path", { d: "M6 9l6 6 6-6" }) });
      case "cleanup":
      case "trash":
        return /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("svg", { ...common, children: /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("path", { d: "M3 6h18M8 6V4h8v2M19 6l-1 14H6L5 6" }) });
      case "close":
        return /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("svg", { ...common, children: /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("path", { d: "M6 6l12 12M18 6L6 18" }) });
      case "file":
        return /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("svg", { ...common, children: /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("path", { d: "M22 19a2 2 0 01-2 2H4a2 2 0 01-2-2V5a2 2 0 012-2h5l2 3h9a2 2 0 012 2z" }) });
      case "gear":
        return /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)("svg", { ...common, children: [
          /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("circle", { cx: "12", cy: "12", r: "3" }),
          /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("path", { d: "M12 2v3M12 19v3M4.9 4.9L7 7M17 17l2.1 2.1M2 12h3M19 12h3M4.9 19.1L7 17M17 7l2.1-2.1" })
        ] });
      case "history":
        return /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("svg", { ...common, children: /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("path", { d: "M3 12a9 9 0 109-9 9.7 9.7 0 00-6.7 2.7L3 8M3 3v5h5M12 7v5l3 2" }) });
      case "logout":
        return /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("svg", { ...common, children: /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("path", { d: "M9 21H5a2 2 0 01-2-2V5a2 2 0 012-2h4M16 17l5-5-5-5M21 12H9" }) });
      case "merge":
        return /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("svg", { ...common, children: /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("path", { d: "M7 7h10M7 12h10M7 17h10M5 7L3 9l2 2M19 15l2 2-2 2" }) });
      case "more":
        return /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)("svg", { ...common, children: [
          /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("circle", { cx: "12", cy: "5", r: "1" }),
          /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("circle", { cx: "12", cy: "12", r: "1" }),
          /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("circle", { cx: "12", cy: "19", r: "1" })
        ] });
      case "pin":
        return /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("svg", { ...common, children: /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("path", { d: "M12 17v5M5 17h14M9 12V6H8a2 2 0 010-4h8a2 2 0 010 4h-1v6" }) });
      case "resume":
        return /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("svg", { ...common, children: /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("path", { d: "M1 4v6h6M3.5 15A9 9 0 109 3.6L3 10" }) });
      case "server":
        return /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)("svg", { ...common, children: [
          /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("rect", { x: "2", y: "3", width: "20", height: "8", rx: "2" }),
          /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("rect", { x: "2", y: "13", width: "20", height: "8", rx: "2" }),
          /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("path", { d: "M6 7h.01M6 17h.01" })
        ] });
      case "spark":
        return /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)("svg", { ...common, children: [
          /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("path", { d: "M12 2v4M12 18v4M4.9 4.9l2.8 2.8M16.3 16.3l2.8 2.8M2 12h4M18 12h4M4.9 19.1l2.8-2.8M16.3 7.7l2.8-2.8" }),
          /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("circle", { cx: "12", cy: "12", r: "3" })
        ] });
    }
  }
  function ActionButton({
    action,
    dispatch,
    actionName,
    label,
    icon,
    className,
    disabled,
    data
  }) {
    return /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(
      "button",
      {
        type: "button",
        className: classNames3("session-action-btn", className),
        "data-action": actionName,
        "data-session-id": data?.sessionId,
        "data-claude-session-id": data?.historyId,
        "data-cwd": data?.cwd,
        "aria-label": label,
        title: label,
        disabled,
        onClick: (event) => {
          event.stopPropagation();
          void dispatch(action);
        },
        children: /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(Icon, { name: icon })
      }
    );
  }
  function ManageCheckbox({
    entry,
    dispatch
  }) {
    const target = getSidebarEntryTarget(entry);
    const legacyKind = target === "session" ? "sessions" : target === "codex-history" ? "codex" : "history";
    return /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)("label", { className: "session-manage-check", onClick: (event) => event.stopPropagation(), children: [
      /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(
        "input",
        {
          type: "checkbox",
          "data-action": "toggle-selection",
          "data-kind": legacyKind,
          "data-id": entry.id,
          checked: entry.selected,
          "aria-label": `\u9009\u62E9\u4F1A\u8BDD ${entry.title}`,
          onChange: () => void dispatch({ type: "session.manage.select", target, id: entry.id })
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("span", {})
    ] });
  }
  function WorktreeBadges({ entry }) {
    if (!entry.worktree?.enabled) return null;
    const labels = {
      ready: "\u53EF\u5408\u5E76",
      checking: "\u68C0\u67E5\u4E2D",
      merging: "\u5408\u5E76\u4E2D",
      merged: "\u5DF2\u5408\u5E76",
      failed: "\u5408\u5E76\u5931\u8D25"
    };
    const title = [
      entry.worktree.branch && `Worktree: ${entry.worktree.branch}`,
      entry.worktree.path && `Path: ${entry.worktree.path}`
    ].filter(Boolean).join("\n");
    return /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)(import_jsx_runtime38.Fragment, { children: [
      /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("span", { className: "session-kind-badge worktree", title: title || void 0, children: "Worktree" }),
      entry.worktree.mergeStatus && /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("span", { className: classNames3("session-kind-badge worktree-merge", entry.worktree.mergeStatus), children: labels[entry.worktree.mergeStatus] ?? entry.worktree.mergeStatus })
    ] });
  }
  function formatEntryTime(entry) {
    const value = entry.endedAt ?? entry.startedAt;
    if (!value) return "";
    const parsed = new Date(value);
    if (!Number.isFinite(parsed.getTime())) return "";
    return parsed.toLocaleDateString("zh-CN", { month: "numeric", day: "numeric" });
  }
  function SessionEntry({
    entry,
    manageMode,
    dispatch
  }) {
    const actions = getShellSidebarEntryActions(entry, manageMode);
    const isHistory = entry.source === "claude-history" || entry.source === "codex-history";
    const provider = entry.source === "codex-history" ? "codex" : "claude";
    const data = isHistory ? { historyId: entry.id, cwd: entry.cwd } : { sessionId: entry.id };
    const activate = () => void dispatch(actions.primary);
    const time = formatEntryTime(entry);
    return /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)(
      "div",
      {
        className: classNames3(
          "session-item",
          isHistory && "non-wand-session",
          entry.active && "active",
          manageMode && entry.selected && "selected"
        ),
        "data-session-id": isHistory ? void 0 : entry.id,
        "data-claude-history-id": isHistory ? entry.id : void 0,
        "data-provider": isHistory ? provider : void 0,
        "data-cwd": isHistory ? entry.cwd : void 0,
        role: "button",
        tabIndex: 0,
        "aria-current": entry.active ? "page" : void 0,
        onClick: activate,
        onKeyDown: (event) => {
          if (event.target !== event.currentTarget) return;
          if (event.key !== "Enter" && event.key !== " ") return;
          event.preventDefault();
          activate();
        },
        children: [
          !manageMode && !isHistory && actions.delete && /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("div", { className: "session-swipe-bg", "aria-hidden": "true", children: /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)(
            "button",
            {
              className: "session-swipe-delete",
              "data-action": "swipe-delete-session",
              "data-session-id": entry.id,
              type: "button",
              tabIndex: -1,
              "aria-label": "\u5220\u9664\u4F1A\u8BDD",
              onClick: (event) => {
                event.stopPropagation();
                void dispatch(actions.delete);
              },
              children: [
                /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(Icon, { name: "trash", size: 18 }),
                /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("span", { children: "\u5220\u9664" })
              ]
            }
          ) }),
          /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("div", { className: "session-item-content", children: /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)("div", { className: "session-item-row", children: [
            manageMode && /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(ManageCheckbox, { entry, dispatch }),
            /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)("div", { className: "session-main", children: [
              /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)("div", { className: "session-title-row", children: [
                /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("div", { className: isHistory ? "session-command claude-history-preview" : "session-title", children: entry.title }),
                time && /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("span", { className: "session-time", children: time })
              ] }),
              entry.description && /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("div", { className: "session-description", children: entry.description }),
              entry.inFlight && /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("div", { className: "session-activity", children: "\u601D\u8003\u4E2D" }),
              /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("div", { className: "session-meta", children: isHistory ? /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)(import_jsx_runtime38.Fragment, { children: [
                /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("span", { className: "session-id", title: entry.claudeSessionId ?? entry.id, children: entry.id.slice(0, 8) }),
                /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("span", { children: time })
              ] }) : /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)(import_jsx_runtime38.Fragment, { children: [
                /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("span", { className: classNames3("session-status", entry.permissionBlocked ? "permission-blocked" : entry.inFlight ? "running" : entry.status), children: entry.statusLabel }),
                /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(WorktreeBadges, { entry })
              ] }) })
            ] }),
            !manageMode && /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)("span", { className: "session-actions", children: [
              actions.resume && /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(
                ActionButton,
                {
                  action: actions.resume,
                  dispatch,
                  actionName: isHistory ? entry.source === "codex-history" ? "resume-codex-history" : "resume-history" : "resume",
                  label: isHistory ? `\u6062\u590D\u6B64 ${provider === "codex" ? "Codex" : "Claude"} \u4F1A\u8BDD` : "\u6062\u590D\u4F1A\u8BDD",
                  icon: "resume",
                  data
                }
              ),
              actions.merge && /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(
                ActionButton,
                {
                  action: actions.merge,
                  dispatch,
                  actionName: "worktree-merge",
                  label: "\u5408\u5E76\u5230\u4E3B\u5206\u652F",
                  icon: "merge",
                  className: "merge-btn",
                  disabled: entry.status === "running" || entry.worktree?.mergeStatus === "merging",
                  data
                }
              ),
              actions.cleanup && /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(
                ActionButton,
                {
                  action: actions.cleanup,
                  dispatch,
                  actionName: "worktree-cleanup",
                  label: "\u91CD\u8BD5\u6E05\u7406 worktree",
                  icon: "cleanup",
                  className: "merge-btn",
                  data
                }
              ),
              actions.delete && /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(
                ActionButton,
                {
                  action: actions.delete,
                  dispatch,
                  actionName: isHistory ? entry.source === "codex-history" ? "delete-codex-history" : "delete-history" : "delete-session",
                  label: "\u5220\u9664\u4F1A\u8BDD",
                  icon: "trash",
                  className: "delete-btn",
                  data
                }
              )
            ] })
          ] }) })
        ]
      }
    );
  }
  function SessionGroup({
    group,
    manageMode,
    dispatch
  }) {
    if (group.entries.length === 0) return null;
    const entries = /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("section", { className: classNames3(
      "session-group",
      group.kind === "automation" && "automation-session-list",
      group.kind === "history" && "non-wand-session-list"
    ), children: group.entries.map((entry) => /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(SessionEntry, { entry, manageMode, dispatch }, `${entry.source}:${entry.id}`)) });
    if (group.kind === "wand") return entries;
    const automation = group.kind === "automation";
    return /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)(
      "details",
      {
        className: classNames3(
          automation ? "automation-session-group" : "non-wand-session-group",
          manageMode && "manage-mode"
        ),
        open: manageMode || group.expanded,
        onToggle: (event) => {
          if (manageMode) return;
          void dispatch({
            type: "layout.drawer.group.set",
            group: automation ? "automation" : "history",
            expanded: event.currentTarget.open
          });
        },
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)(
            "summary",
            {
              className: automation ? "automation-session-summary" : "non-wand-session-summary",
              title: automation ? "\u7531\u81EA\u52A8\u5316\u6216\u542F\u52A8\u4EFB\u52A1\u521B\u5EFA\uFF0C\u4E0D\u53C2\u4E0E\u666E\u901A Wand \u4F1A\u8BDD\u6392\u5E8F" : "Claude \u4E0E Codex \u7684\u672C\u673A\u539F\u751F\u4F1A\u8BDD\uFF0C\u4E0D\u53C2\u4E0E Wand \u4F1A\u8BDD\u6392\u5E8F",
              onClick: manageMode ? (event) => event.preventDefault() : void 0,
              children: [
                /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("span", { className: automation ? "automation-session-icon" : "non-wand-session-icon", "aria-hidden": "true", children: /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(Icon, { name: automation ? "spark" : "history" }) }),
                /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("span", { className: automation ? "automation-session-title" : "non-wand-session-title", children: group.label }),
                /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(
                  "span",
                  {
                    className: automation ? "automation-session-count" : "non-wand-session-count",
                    "aria-label": `${group.entries.length} \u4E2A\u4F1A\u8BDD`,
                    children: group.entries.length
                  }
                ),
                /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(
                  Icon,
                  {
                    name: "chevron",
                    className: automation ? "automation-session-chevron" : "non-wand-session-chevron"
                  }
                )
              ]
            }
          ),
          entries
        ]
      }
    );
  }
  function ManageBar({
    manageMode,
    selectedCount,
    totalCount,
    dispatch
  }) {
    if (!manageMode) {
      return /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)("div", { className: "session-manage-bar", children: [
        /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("span", { className: "sidebar-intro", children: "Wand \u4F1A\u8BDD" }),
        /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(
          "button",
          {
            className: "btn btn-ghost btn-xs session-manage-toggle",
            "data-action": "toggle-manage-mode",
            type: "button",
            onClick: () => void dispatch({ type: "session.manage.toggle" }),
            children: /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("span", { children: "\u7BA1\u7406" })
          }
        )
      ] });
    }
    const allSelected = totalCount > 0 && selectedCount >= totalCount;
    return /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)("div", { className: "session-manage-bar active", children: [
      /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(
        "button",
        {
          className: "session-manage-exit",
          "data-action": "toggle-manage-mode",
          type: "button",
          "aria-label": "\u9000\u51FA\u7BA1\u7406\u6A21\u5F0F",
          title: "\u9000\u51FA\u7BA1\u7406\u6A21\u5F0F",
          onClick: () => void dispatch({ type: "session.manage.toggle" }),
          children: /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(Icon, { name: "close" })
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("div", { className: "session-manage-summary", children: selectedCount > 0 ? /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)(import_jsx_runtime38.Fragment, { children: [
        /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("span", { className: "session-manage-count", children: selectedCount }),
        /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("span", { className: "session-manage-summary-label", children: "\u5DF2\u9009\u62E9" })
      ] }) : /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("span", { className: "session-manage-summary-label muted", children: "\u9009\u62E9\u8981\u7BA1\u7406\u7684\u9879\u76EE" }) }),
      /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)("div", { className: "session-manage-actions", children: [
        /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(
          "button",
          {
            className: "btn btn-ghost btn-xs",
            "data-action": allSelected ? "clear-selection" : "select-all-visible",
            type: "button",
            disabled: totalCount === 0,
            onClick: () => void dispatch({
              type: allSelected ? "session.manage.clear" : "session.manage.selectAll"
            }),
            children: allSelected ? "\u53D6\u6D88\u5168\u9009" : "\u5168\u9009"
          }
        ),
        /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)(
          "button",
          {
            className: "btn btn-danger btn-xs",
            "data-action": "delete-selected",
            type: "button",
            disabled: selectedCount === 0,
            onClick: () => void dispatch({ type: "session.manage.deleteSelected" }),
            children: [
              "\u5220\u9664",
              selectedCount > 0 ? ` ${selectedCount}` : ""
            ]
          }
        )
      ] })
    ] });
  }
  function CollapsedSessions({
    groups,
    dispatch
  }) {
    const wand = groups.find((group) => group.kind === "wand")?.entries ?? [];
    const automation = groups.find((group) => group.kind === "automation")?.entries ?? [];
    const history2 = groups.find((group) => group.kind === "history")?.entries ?? [];
    return /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)("div", { className: "sidebar-collapsed-tiles", children: [
      wand.map((entry, index2) => /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(
        "button",
        {
          className: classNames3("sidebar-collapsed-tile", entry.active && "active"),
          type: "button",
          "data-collapsed-session-id": entry.id,
          title: entry.title,
          onClick: () => void dispatch({ type: "session.select", id: entry.id }),
          children: index2 + 1
        },
        entry.id
      )),
      automation.length > 0 && /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)(
        "button",
        {
          className: classNames3(
            "sidebar-collapsed-tile automation-count-tile",
            automation.some((entry) => entry.active) && "active-group"
          ),
          type: "button",
          "data-expand-session-group": "automation",
          title: `\u5C55\u5F00\u67E5\u770B ${automation.length} \u4E2A\u81EA\u52A8\u5316\u4F1A\u8BDD`,
          "aria-label": `\u5C55\u5F00\u67E5\u770B ${automation.length} \u4E2A\u81EA\u52A8\u5316\u4F1A\u8BDD`,
          onClick: () => void dispatch({ type: "layout.drawer.expandGroup", group: "automation" }),
          children: [
            /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(Icon, { name: "spark", size: 16 }),
            /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("span", { className: "non-wand-count-badge", children: automation.length > 99 ? "99+" : automation.length })
          ]
        }
      ),
      history2.length > 0 && /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)(
        "button",
        {
          className: "sidebar-collapsed-tile non-wand-count-tile",
          type: "button",
          "data-expand-session-group": "non-wand",
          title: `\u5C55\u5F00\u67E5\u770B ${history2.length} \u4E2A\u975E Wand \u4F1A\u8BDD`,
          "aria-label": `\u5C55\u5F00\u67E5\u770B ${history2.length} \u4E2A\u975E Wand \u4F1A\u8BDD`,
          onClick: () => void dispatch({ type: "layout.drawer.expandGroup", group: "history" }),
          children: [
            /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(Icon, { name: "history", size: 16 }),
            /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("span", { className: "non-wand-count-badge", children: history2.length > 99 ? "99+" : history2.length })
          ]
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(
        "button",
        {
          className: "sidebar-collapsed-tile add",
          type: "button",
          "data-collapsed-new-session": "1",
          title: "\u65B0\u5EFA\u4F1A\u8BDD",
          "aria-label": "\u65B0\u5EFA\u4F1A\u8BDD",
          onClick: () => void dispatch({ type: "session.new" }),
          children: /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("span", { "aria-hidden": "true", children: "\uFF0B" })
        }
      )
    ] });
  }
  function ShellSidebar() {
    const snapshot7 = useUiStoreSnapshot();
    const dispatch = useUiDispatch();
    const [moreOpen, setMoreOpen] = React40.useState(false);
    const narrow = snapshot7.layout.sidebarPinned && snapshot7.layout.sidebarCollapsed;
    const sidebarClass = classNames3(
      "sidebar",
      snapshot7.layout.sessionsDrawerOpen && "open",
      snapshot7.layout.sidebarAnchored && "pinned",
      narrow && "collapsed"
    );
    return /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)(import_jsx_runtime38.Fragment, { children: [
      /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(
        "div",
        {
          id: "sessions-drawer-backdrop",
          className: classNames3("drawer-backdrop", snapshot7.layout.sessionsBackdropVisible && "open"),
          "aria-hidden": "true",
          onClick: () => void dispatch({ type: "layout.drawer.close" })
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)("aside", { id: "sessions-drawer", className: sidebarClass, "aria-label": "\u4F1A\u8BDD\u4FA7\u680F", children: [
        /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)("div", { className: "sidebar-header", children: [
          /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)("div", { className: "sidebar-header-main", children: [
            /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("div", { className: "topbar-logo-icon", children: "W" }),
            /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("span", { className: "sidebar-title", children: "\u4F1A\u8BDD" }),
            /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("span", { className: "session-count", id: "session-count", children: snapshot7.sidebar.interactiveCount })
          ] }),
          /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)("div", { className: "sidebar-header-actions", children: [
            /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("div", { className: "sidebar-header-more", children: /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)(
              WandPopover,
              {
                open: moreOpen,
                onOpenChange: setMoreOpen,
                align: "end",
                sideOffset: 6,
                portalled: false,
                forceMount: true,
                showArrow: false,
                contentId: "sidebar-overflow-menu",
                contentRole: "menu",
                ariaLabel: "\u4FA7\u680F\u66F4\u591A\u64CD\u4F5C",
                className: classNames3("sidebar-header-overflow", "wand-shell-menu-popover", moreOpen && "open"),
                trigger: /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(
                  "button",
                  {
                    id: "sidebar-more-btn",
                    className: "btn btn-ghost btn-sm",
                    type: "button",
                    title: "\u66F4\u591A\u64CD\u4F5C",
                    "aria-haspopup": "menu",
                    "aria-expanded": moreOpen,
                    "aria-controls": "sidebar-overflow-menu",
                    children: /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(Icon, { name: "more" })
                  }
                ),
                children: [
                  /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(
                    "button",
                    {
                      className: "overflow-item",
                      id: "sidebar-home-btn",
                      type: "button",
                      role: "menuitem",
                      onClick: () => {
                        setMoreOpen(false);
                        void dispatch({ type: "nav.home" });
                      },
                      children: /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("span", { children: "\u56DE\u5230\u9996\u9875" })
                    }
                  ),
                  /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(
                    "button",
                    {
                      className: "overflow-item",
                      id: "sidebar-refresh-btn",
                      type: "button",
                      role: "menuitem",
                      onClick: () => {
                        setMoreOpen(false);
                        void dispatch({ type: "nav.refresh" });
                      },
                      children: /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("span", { children: "\u5237\u65B0\u9875\u9762" })
                    }
                  )
                ]
              }
            ) }),
            /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(
              "button",
              {
                id: "sidebar-pin-btn",
                className: classNames3("btn btn-ghost btn-sm sidebar-pin-toggle", snapshot7.layout.sidebarPinned && "pinned"),
                type: "button",
                title: snapshot7.layout.sidebarPinned ? "\u5DF2\u56FA\u5B9A\u5E38\u9A7B\uFF08\u70B9\u51FB\u89E3\u9664\u9501\u5B9A\uFF09" : "\u56FA\u5B9A\u4FA7\u680F\u5E38\u9A7B",
                "aria-label": snapshot7.layout.sidebarPinned ? "\u89E3\u9664\u56FA\u5B9A\u5E38\u9A7B" : "\u56FA\u5B9A\u4FA7\u680F\u5E38\u9A7B",
                "aria-pressed": snapshot7.layout.sidebarPinned,
                onClick: () => void dispatch({ type: "layout.drawer.pin" }),
                children: /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(Icon, { name: "pin" })
              }
            ),
            /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(
              "button",
              {
                id: "sidebar-collapse-btn",
                className: classNames3("btn btn-ghost btn-sm sidebar-collapse-toggle", narrow && "collapsed"),
                type: "button",
                title: narrow ? "\u5C55\u5F00\u4E3A\u5168\u5C3A\u5BF8" : "\u6536\u8D77\u4E3A\u7A84\u6761",
                "aria-label": narrow ? "\u5C55\u5F00\u4E3A\u5168\u5C3A\u5BF8" : "\u6536\u8D77\u4E3A\u7A84\u6761",
                onClick: () => void dispatch({ type: "layout.drawer.collapse" }),
                children: /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(Icon, { name: "chevron" })
              }
            ),
            /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(
              "button",
              {
                id: "close-drawer-button",
                className: "btn btn-ghost btn-icon sidebar-close drawer-close-btn",
                type: "button",
                "aria-label": "\u5173\u95ED\u83DC\u5355",
                onClick: () => void dispatch({ type: "layout.drawer.close" }),
                children: /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(Icon, { name: "close" })
              }
            )
          ] })
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("div", { className: "sidebar-body", children: /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("div", { id: "sessions-panel", children: /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("div", { className: "sessions-list", id: "sessions-list", children: narrow ? /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(CollapsedSessions, { groups: snapshot7.sidebar.groups, dispatch }) : /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)(import_jsx_runtime38.Fragment, { children: [
          /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(
            ManageBar,
            {
              manageMode: snapshot7.sidebar.manageMode,
              selectedCount: snapshot7.sidebar.selectedCount,
              totalCount: snapshot7.sidebar.totalCount,
              dispatch
            }
          ),
          snapshot7.sidebar.totalCount === 0 ? /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)("div", { className: "empty-state", children: [
            /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("strong", { children: "\u8FD8\u6CA1\u6709\u4F1A\u8BDD\u8BB0\u5F55" }),
            /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("br", {}),
            "\u70B9\u51FB\u4E0A\u65B9\u300C\u65B0\u5BF9\u8BDD\u300D\u5F00\u59CB\u4F60\u7684\u7B2C\u4E00\u6B21\u5BF9\u8BDD\u3002"
          ] }) : snapshot7.sidebar.groups.map((group) => /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(SessionGroup, { group, manageMode: snapshot7.sidebar.manageMode, dispatch }, group.kind))
        ] }) }) }) }),
        /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)("div", { className: "sidebar-footer", children: [
          /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)(
            "button",
            {
              id: "drawer-new-session-button",
              className: "btn btn-primary btn-block",
              type: "button",
              onClick: () => void dispatch({ type: "session.new" }),
              children: [
                /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("span", { children: "+" }),
                " \u65B0\u4F1A\u8BDD"
              ]
            }
          ),
          /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)("div", { className: "sidebar-footer-actions", children: [
            /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)(
              "button",
              {
                id: "file-panel-toggle-btn",
                className: classNames3("btn btn-ghost btn-sm", snapshot7.layout.filePanelOpen && "active"),
                type: "button",
                title: "\u67E5\u770B\u6587\u4EF6",
                onClick: () => void dispatch({ type: "layout.files.toggle" }),
                children: [
                  /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(Icon, { name: "file", size: 16 }),
                  /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("span", { children: "\u6587\u4EF6" })
                ]
              }
            ),
            /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)(
              "button",
              {
                id: "settings-button",
                className: "btn btn-ghost btn-sm",
                type: "button",
                title: "\u8BBE\u7F6E",
                onClick: () => void dispatch({ type: "settings.open" }),
                children: [
                  /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(Icon, { name: "gear", size: 16 }),
                  /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("span", { children: "\u8BBE\u7F6E" })
                ]
              }
            ),
            snapshot7.capabilities.backToNative && /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)(
              "button",
              {
                id: "back-to-native-button",
                className: "btn btn-ghost btn-sm sidebar-back-to-native",
                type: "button",
                title: "\u8FD4\u56DE App \u539F\u751F\u754C\u9762",
                onClick: () => void dispatch({ type: "native.back" }),
                children: [
                  /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(Icon, { name: "back", size: 16 }),
                  /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("span", { children: "\u8FD4\u56DEApp" })
                ]
              }
            ),
            snapshot7.capabilities.switchServer && /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)(
              "button",
              {
                id: "switch-server-button",
                className: "btn btn-ghost btn-sm sidebar-switch-server",
                type: "button",
                title: "\u5207\u6362\u670D\u52A1\u5668",
                onClick: () => void dispatch({ type: "native.switchServer" }),
                children: [
                  /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(Icon, { name: "server", size: 16 }),
                  /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("span", { children: "\u5207\u6362" })
                ]
              }
            ),
            /* @__PURE__ */ (0, import_jsx_runtime38.jsxs)(
              "button",
              {
                id: "logout-button",
                className: "btn btn-ghost btn-sm sidebar-logout",
                type: "button",
                title: "\u9000\u51FA\u767B\u5F55",
                onClick: () => void dispatch({ type: "auth.logout" }),
                children: [
                  /* @__PURE__ */ (0, import_jsx_runtime38.jsx)(Icon, { name: "logout", size: 16 }),
                  /* @__PURE__ */ (0, import_jsx_runtime38.jsx)("span", { children: "\u9000\u51FA" })
                ]
              }
            )
          ] })
        ] })
      ] })
    ] });
  }

  // src/web-ui/react/shell/shell-topbar.tsx
  var import_jsx_runtime39 = __toESM(require_jsx_runtime(), 1);
  function classNames4(...values) {
    return values.filter(Boolean).join(" ");
  }
  function TopbarIcon({ name, size: size4 = 16 }) {
    const common = {
      width: size4,
      height: size4,
      viewBox: "0 0 24 24",
      fill: "none",
      stroke: "currentColor",
      strokeWidth: 2,
      strokeLinecap: "round",
      strokeLinejoin: "round",
      "aria-hidden": true
    };
    switch (name) {
      case "copy":
        return /* @__PURE__ */ (0, import_jsx_runtime39.jsxs)("svg", { ...common, children: [
          /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("rect", { x: "9", y: "9", width: "13", height: "13", rx: "2" }),
          /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("path", { d: "M5 15H4a2 2 0 01-2-2V4a2 2 0 012-2h9a2 2 0 012 2v1" })
        ] });
      case "file":
        return /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("svg", { ...common, children: /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("path", { d: "M22 19a2 2 0 01-2 2H4a2 2 0 01-2-2V5a2 2 0 012-2h5l2 3h9a2 2 0 012 2z" }) });
      case "git":
        return /* @__PURE__ */ (0, import_jsx_runtime39.jsxs)("svg", { ...common, className: "topbar-git-icon", children: [
          /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("circle", { cx: "6", cy: "6", r: "2" }),
          /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("circle", { cx: "6", cy: "18", r: "2" }),
          /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("circle", { cx: "18", cy: "9", r: "2" }),
          /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("path", { d: "M6 8v8M18 11v1a3 3 0 01-3 3H9" })
        ] });
      case "hash":
        return /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("svg", { ...common, children: /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("path", { d: "M4 9h16M4 15h16M10 3L8 21M16 3l-2 18" }) });
      case "merge":
        return /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("svg", { ...common, children: /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("path", { d: "M7 7h10M7 12h10M7 17h10M5 7L3 9l2 2M19 15l2 2-2 2" }) });
      case "more":
        return /* @__PURE__ */ (0, import_jsx_runtime39.jsxs)("svg", { ...common, children: [
          /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("circle", { cx: "12", cy: "5", r: "1" }),
          /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("circle", { cx: "12", cy: "12", r: "1" }),
          /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("circle", { cx: "12", cy: "19", r: "1" })
        ] });
      case "trash":
        return /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("svg", { ...common, children: /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("path", { d: "M3 6h18M8 6V4h8v2M19 6l-1 14H6L5 6" }) });
    }
  }
  function MoreItem({
    action,
    actionName,
    label,
    icon,
    danger,
    disabled,
    onAction
  }) {
    return /* @__PURE__ */ (0, import_jsx_runtime39.jsxs)(
      "button",
      {
        className: classNames4("topbar-more-item", danger && "topbar-more-item-danger"),
        "data-action": actionName,
        type: "button",
        role: "menuitem",
        disabled,
        onClick: () => onAction(action),
        children: [
          /* @__PURE__ */ (0, import_jsx_runtime39.jsx)(TopbarIcon, { name: icon, size: 14 }),
          /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("span", { children: label })
        ]
      }
    );
  }
  function ShellTopbar() {
    const snapshot7 = useUiStoreSnapshot();
    const dispatch = useUiDispatch();
    const selected = snapshot7.selected;
    const moreOpen = snapshot7.layout.topbarMoreOpen;
    const selectedActions = selected ? getShellSidebarEntryActions(selected, false) : null;
    const openFiles = () => {
      if (!snapshot7.layout.filePanelOpen) void dispatch({ type: "layout.files.toggle" });
    };
    const runMoreAction = (action) => {
      if (moreOpen) void dispatch({ type: "topbar.menu.toggle" });
      void dispatch(action);
    };
    return /* @__PURE__ */ (0, import_jsx_runtime39.jsxs)("div", { className: "main-header-row", children: [
      /* @__PURE__ */ (0, import_jsx_runtime39.jsxs)("div", { className: "topbar-left", children: [
        /* @__PURE__ */ (0, import_jsx_runtime39.jsx)(
          "button",
          {
            id: "sessions-toggle-button",
            className: classNames4("floating-sidebar-toggle", snapshot7.layout.sessionsDrawerOpen && "active"),
            "aria-label": "\u5207\u6362\u4F1A\u8BDD\u4FA7\u680F",
            type: "button",
            onClick: () => void dispatch({ type: "layout.drawer.toggle" }),
            children: /* @__PURE__ */ (0, import_jsx_runtime39.jsxs)("span", { className: "hamburger-icon", children: [
              /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("span", {}),
              /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("span", {}),
              /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("span", {})
            ] })
          }
        ),
        /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("span", { className: "topbar-brand", "aria-hidden": "true", children: "W" })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("div", { className: "topbar-center", children: selected ? /* @__PURE__ */ (0, import_jsx_runtime39.jsxs)(import_jsx_runtime39.Fragment, { children: [
        /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("span", { className: "topbar-session-title", title: snapshot7.topbar.description || selected.title, children: snapshot7.topbar.title }),
        /* @__PURE__ */ (0, import_jsx_runtime39.jsxs)(
          "span",
          {
            className: classNames4("session-status-pill", snapshot7.topbar.statusTone),
            title: snapshot7.topbar.statusLabel,
            children: [
              /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("span", { className: "session-status-dot" }),
              /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("span", { className: "session-status-text", children: snapshot7.topbar.statusLabel })
            ]
          }
        ),
        /* @__PURE__ */ (0, import_jsx_runtime39.jsx)(
          "span",
          {
            className: classNames4("current-task", !snapshot7.topbar.currentTask && "hidden"),
            id: "current-task",
            title: snapshot7.topbar.currentTask || void 0,
            children: snapshot7.topbar.currentTask
          }
        ),
        snapshot7.topbar.cwd && /* @__PURE__ */ (0, import_jsx_runtime39.jsx)(
          "span",
          {
            className: "topbar-cwd tail-marquee-path",
            id: "topbar-cwd",
            role: "button",
            tabIndex: 0,
            title: snapshot7.topbar.cwd,
            onClick: openFiles,
            onKeyDown: (event) => {
              if (event.key !== "Enter" && event.key !== " ") return;
              event.preventDefault();
              openFiles();
            },
            children: /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("span", { className: "tail-marquee-path-inner", children: snapshot7.topbar.cwd })
          }
        )
      ] }) : /* @__PURE__ */ (0, import_jsx_runtime39.jsxs)(import_jsx_runtime39.Fragment, { children: [
        /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("span", { className: "topbar-tagline", children: snapshot7.topbar.title || "Wand \u63A7\u5236\u53F0" }),
        /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("span", { className: "current-task hidden", id: "current-task" })
      ] }) }),
      /* @__PURE__ */ (0, import_jsx_runtime39.jsxs)("div", { className: "topbar-right", children: [
        /* @__PURE__ */ (0, import_jsx_runtime39.jsx)(
          "button",
          {
            id: "topbar-file-button",
            className: classNames4("topbar-btn square", snapshot7.layout.filePanelOpen && "active"),
            type: "button",
            "aria-label": "\u6587\u4EF6",
            title: "\u67E5\u770B\u6587\u4EF6\uFF08\u53EF\u4FEE\u6539\u8DEF\u5F84\uFF09",
            onClick: () => void dispatch({ type: "layout.files.toggle" }),
            children: /* @__PURE__ */ (0, import_jsx_runtime39.jsx)(TopbarIcon, { name: "file" })
          }
        ),
        /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("span", { id: "topbar-git-slot", className: "topbar-git-slot", children: snapshot7.topbar.git && /* @__PURE__ */ (0, import_jsx_runtime39.jsxs)(
          "button",
          {
            id: "topbar-git-badge",
            className: "topbar-git-badge",
            type: "button",
            title: `${snapshot7.topbar.git.branch}  \xB7  ${snapshot7.topbar.git.clean ? "\u5DE5\u4F5C\u533A\u5E72\u51C0" : `${snapshot7.topbar.git.modifiedCount} \u4E2A\u6587\u4EF6\u5F85\u63D0\u4EA4`}`,
            "aria-label": "\u5FEB\u6377\u63D0\u4EA4",
            onClick: () => void dispatch({ type: "topbar.gitCommit" }),
            children: [
              /* @__PURE__ */ (0, import_jsx_runtime39.jsx)(TopbarIcon, { name: "git", size: 14 }),
              /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("span", { className: "topbar-git-branch", children: snapshot7.topbar.git.branch }),
              snapshot7.topbar.git.clean ? /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("span", { className: "topbar-git-clean", "aria-hidden": "true", children: "\u2713" }) : /* @__PURE__ */ (0, import_jsx_runtime39.jsxs)("span", { className: "topbar-git-count", children: [
                "\xB7",
                snapshot7.topbar.git.modifiedCount
              ] })
            ]
          }
        ) }),
        selected && /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("div", { className: "topbar-more-wrap", children: /* @__PURE__ */ (0, import_jsx_runtime39.jsxs)(
          WandPopover,
          {
            open: moreOpen,
            onOpenChange: (open) => {
              if (open !== moreOpen) void dispatch({ type: "topbar.menu.toggle" });
            },
            align: "end",
            sideOffset: 6,
            portalled: false,
            forceMount: true,
            showArrow: false,
            contentId: "topbar-more-menu",
            contentRole: "menu",
            ariaLabel: "\u5F53\u524D\u4F1A\u8BDD",
            className: classNames4("topbar-more-menu", "wand-shell-menu-popover", !moreOpen && "hidden"),
            trigger: /* @__PURE__ */ (0, import_jsx_runtime39.jsx)(
              "button",
              {
                id: "topbar-more-button",
                className: classNames4("topbar-btn square", moreOpen && "active"),
                type: "button",
                "aria-label": "\u5F53\u524D\u4F1A\u8BDD\u64CD\u4F5C",
                "aria-haspopup": "menu",
                "aria-expanded": moreOpen,
                "aria-controls": "topbar-more-menu",
                title: "\u5F53\u524D\u4F1A\u8BDD\u64CD\u4F5C",
                children: /* @__PURE__ */ (0, import_jsx_runtime39.jsx)(TopbarIcon, { name: "more" })
              }
            ),
            children: [
              selected.claudeSessionId && /* @__PURE__ */ (0, import_jsx_runtime39.jsx)(
                MoreItem,
                {
                  action: { type: "topbar.copy", field: "providerSessionId" },
                  actionName: "copy-claude-session-id",
                  label: selected.provider === "codex" ? "\u590D\u5236 Codex thread ID" : selected.provider === "opencode" ? "\u590D\u5236 OpenCode session ID" : "\u590D\u5236 Claude \u4F1A\u8BDD ID",
                  icon: "copy",
                  onAction: runMoreAction
                }
              ),
              selected.cwd && /* @__PURE__ */ (0, import_jsx_runtime39.jsx)(
                MoreItem,
                {
                  action: { type: "topbar.copy", field: "cwd" },
                  actionName: "copy-cwd",
                  label: "\u590D\u5236\u5DE5\u4F5C\u76EE\u5F55",
                  icon: "file",
                  onAction: runMoreAction
                }
              ),
              /* @__PURE__ */ (0, import_jsx_runtime39.jsx)(
                MoreItem,
                {
                  action: { type: "topbar.copy", field: "sessionId" },
                  actionName: "copy-session-id",
                  label: "\u590D\u5236\u4F1A\u8BDD ID",
                  icon: "hash",
                  onAction: runMoreAction
                }
              ),
              /* @__PURE__ */ (0, import_jsx_runtime39.jsx)("div", { className: "topbar-more-divider", role: "separator" }),
              selectedActions?.merge && /* @__PURE__ */ (0, import_jsx_runtime39.jsx)(
                MoreItem,
                {
                  action: selectedActions.merge,
                  actionName: "worktree-merge",
                  label: "\u5408\u5E76\u5230\u4E3B\u5206\u652F\u2026",
                  icon: "merge",
                  disabled: selected.status === "running" || selected.worktree?.mergeStatus === "merging",
                  onAction: runMoreAction
                }
              ),
              selectedActions?.cleanup && /* @__PURE__ */ (0, import_jsx_runtime39.jsx)(
                MoreItem,
                {
                  action: selectedActions.cleanup,
                  actionName: "worktree-cleanup",
                  label: "\u91CD\u8BD5 worktree \u6E05\u7406",
                  icon: "merge",
                  onAction: runMoreAction
                }
              ),
              selectedActions?.delete && /* @__PURE__ */ (0, import_jsx_runtime39.jsx)(
                MoreItem,
                {
                  action: selectedActions.delete,
                  actionName: "delete-session",
                  label: "\u5220\u9664\u5F53\u524D\u4F1A\u8BDD",
                  icon: "trash",
                  danger: true,
                  onAction: runMoreAction
                }
              )
            ]
          }
        ) })
      ] })
    ] });
  }

  // src/web-ui/react/shell/shell-main-content.tsx
  var import_jsx_runtime40 = __toESM(require_jsx_runtime(), 1);
  function getShellLegacySlotClasses(visibility) {
    return {
      terminal: `terminal-container ${visibility.terminal ? "active" : "hidden"}`,
      chat: `chat-container ${visibility.chat ? "active" : "hidden"}`,
      blank: `blank-chat${visibility.blank ? "" : " hidden"}`,
      composer: `input-panel${visibility.composer ? "" : " hidden"}`
    };
  }
  function getShellWelcomeQuickStartAction(tool) {
    switch (tool) {
      case "claude":
        return { type: "session.quickStart.claude" };
      case "codex":
        return { type: "session.quickStart.codex" };
      case "opencode":
        return { type: "session.quickStart.opencode" };
      case "structured":
        return { type: "session.quickStart.structured" };
    }
  }
  function WelcomeIcon({ name, size: size4 }) {
    const common = {
      width: size4,
      height: size4,
      viewBox: "0 0 24 24",
      fill: "none",
      stroke: "currentColor",
      strokeWidth: 1.8,
      strokeLinecap: "round",
      strokeLinejoin: "round",
      "aria-hidden": true
    };
    switch (name) {
      case "chat":
        return /* @__PURE__ */ (0, import_jsx_runtime40.jsx)("svg", { ...common, children: /* @__PURE__ */ (0, import_jsx_runtime40.jsx)("path", { d: "M21 15a4 4 0 01-4 4H8l-5 3V7a4 4 0 014-4h10a4 4 0 014 4z" }) });
      case "chevron-down":
        return /* @__PURE__ */ (0, import_jsx_runtime40.jsx)("svg", { ...common, strokeWidth: 2, children: /* @__PURE__ */ (0, import_jsx_runtime40.jsx)("path", { d: "M6 9l6 6 6-6" }) });
      case "folder":
        return /* @__PURE__ */ (0, import_jsx_runtime40.jsx)("svg", { ...common, children: /* @__PURE__ */ (0, import_jsx_runtime40.jsx)("path", { d: "M22 19a2 2 0 01-2 2H4a2 2 0 01-2-2V5a2 2 0 012-2h5l2 3h9a2 2 0 012 2z" }) });
      case "terminal":
        return /* @__PURE__ */ (0, import_jsx_runtime40.jsx)("svg", { ...common, children: /* @__PURE__ */ (0, import_jsx_runtime40.jsx)("path", { d: "M4 17l6-5-6-5M12 19h8" }) });
    }
  }
  function ShellBlankChat({ className, cwd, queueRef }) {
    const dispatch = useUiDispatch();
    const quickStart2 = (tool) => {
      void dispatch(getShellWelcomeQuickStartAction(tool));
    };
    const openFolderPicker2 = () => {
      void dispatch({ type: "folderPicker.open" });
    };
    return /* @__PURE__ */ (0, import_jsx_runtime40.jsxs)("div", { id: "blank-chat", className, children: [
      /* @__PURE__ */ (0, import_jsx_runtime40.jsxs)("div", { className: "blank-chat-inner", children: [
        /* @__PURE__ */ (0, import_jsx_runtime40.jsx)("div", { className: "blank-chat-logo", children: "W" }),
        /* @__PURE__ */ (0, import_jsx_runtime40.jsx)("h2", { className: "blank-chat-title", children: "Wand" }),
        /* @__PURE__ */ (0, import_jsx_runtime40.jsx)("p", { className: "blank-chat-subtitle", children: "\u652F\u6301\u7EC8\u7AEF PTY \u4F1A\u8BDD\u4E0E\u7ED3\u6784\u5316 chat \u4F1A\u8BDD\uFF0C\u4E24\u79CD\u6A21\u5F0F\u53EF\u5E76\u5B58\u3002" }),
        /* @__PURE__ */ (0, import_jsx_runtime40.jsxs)("div", { className: "blank-chat-tools", children: [
          /* @__PURE__ */ (0, import_jsx_runtime40.jsxs)(
            "button",
            {
              className: "blank-chat-tool-btn",
              id: "welcome-tool-claude",
              type: "button",
              onClick: () => quickStart2("claude"),
              children: [
                /* @__PURE__ */ (0, import_jsx_runtime40.jsx)("span", { className: "tool-icon", children: /* @__PURE__ */ (0, import_jsx_runtime40.jsx)(WelcomeIcon, { name: "terminal", size: 16 }) }),
                "\u65B0\u5EFA\u7EC8\u7AEF\u4F1A\u8BDD"
              ]
            }
          ),
          /* @__PURE__ */ (0, import_jsx_runtime40.jsxs)(
            "button",
            {
              className: "blank-chat-tool-btn",
              id: "welcome-tool-codex",
              type: "button",
              onClick: () => quickStart2("codex"),
              children: [
                /* @__PURE__ */ (0, import_jsx_runtime40.jsx)("span", { className: "tool-icon tool-icon-text", children: "\u2318" }),
                "\u65B0\u5EFA Codex \u4F1A\u8BDD"
              ]
            }
          ),
          /* @__PURE__ */ (0, import_jsx_runtime40.jsxs)(
            "button",
            {
              className: "blank-chat-tool-btn",
              id: "welcome-tool-opencode",
              type: "button",
              onClick: () => quickStart2("opencode"),
              children: [
                /* @__PURE__ */ (0, import_jsx_runtime40.jsx)("span", { className: "tool-icon tool-icon-text", children: "OC" }),
                "\u65B0\u5EFA OpenCode \u4F1A\u8BDD"
              ]
            }
          ),
          /* @__PURE__ */ (0, import_jsx_runtime40.jsxs)(
            "button",
            {
              className: "blank-chat-tool-btn",
              id: "welcome-tool-structured",
              type: "button",
              onClick: () => quickStart2("structured"),
              children: [
                /* @__PURE__ */ (0, import_jsx_runtime40.jsx)("span", { className: "tool-icon", children: /* @__PURE__ */ (0, import_jsx_runtime40.jsx)(WelcomeIcon, { name: "chat", size: 16 }) }),
                "\u65B0\u5EFA\u7ED3\u6784\u5316\u4F1A\u8BDD"
              ]
            }
          )
        ] }),
        /* @__PURE__ */ (0, import_jsx_runtime40.jsx)("div", { className: "blank-chat-cwd-wrap", children: /* @__PURE__ */ (0, import_jsx_runtime40.jsxs)(
          "div",
          {
            className: "blank-chat-cwd",
            id: "blank-chat-cwd",
            role: "button",
            tabIndex: 0,
            "aria-haspopup": "dialog",
            title: `\u5F53\u524D\u5DE5\u4F5C\u76EE\u5F55\uFF1A${cwd}`,
            onClick: openFolderPicker2,
            onKeyDown: (event) => {
              if (event.key !== "Enter" && event.key !== " ") return;
              event.preventDefault();
              openFolderPicker2();
            },
            children: [
              /* @__PURE__ */ (0, import_jsx_runtime40.jsx)("span", { className: "blank-chat-cwd-icon", children: /* @__PURE__ */ (0, import_jsx_runtime40.jsx)(WelcomeIcon, { name: "folder", size: 13 }) }),
              /* @__PURE__ */ (0, import_jsx_runtime40.jsx)("span", { className: "blank-chat-cwd-path tail-marquee-path", id: "blank-chat-cwd-path", title: cwd, children: /* @__PURE__ */ (0, import_jsx_runtime40.jsx)("span", { className: "tail-marquee-path-inner", children: cwd }) }),
              /* @__PURE__ */ (0, import_jsx_runtime40.jsx)("span", { className: "blank-chat-cwd-arrow", id: "blank-chat-cwd-arrow", children: /* @__PURE__ */ (0, import_jsx_runtime40.jsx)(WelcomeIcon, { name: "chevron-down", size: 11 }) })
            ]
          }
        ) })
      ] }),
      /* @__PURE__ */ (0, import_jsx_runtime40.jsx)("div", { id: "cross-session-queue-host", ref: queueRef })
    ] });
  }
  function ShellMainContent({ legacyRefs } = {}) {
    const snapshot7 = useUiStoreSnapshot();
    const classes = getShellLegacySlotClasses(snapshot7.legacyVisibility);
    const cwd = snapshot7.topbar.cwd || "/";
    return /* @__PURE__ */ (0, import_jsx_runtime40.jsxs)("main", { className: `main-content${snapshot7.layout.filePanelOpen ? " file-panel-open" : ""}`, children: [
      /* @__PURE__ */ (0, import_jsx_runtime40.jsx)(ShellTopbar, {}),
      /* @__PURE__ */ (0, import_jsx_runtime40.jsx)(ShellFilePanel, { explorerRef: legacyRefs?.fileExplorer }),
      /* @__PURE__ */ (0, import_jsx_runtime40.jsx)("div", { id: "output", className: classes.terminal, ref: legacyRefs?.terminal }),
      /* @__PURE__ */ (0, import_jsx_runtime40.jsx)("div", { id: "chat-output", className: classes.chat, ref: legacyRefs?.chat }),
      /* @__PURE__ */ (0, import_jsx_runtime40.jsx)(
        ShellBlankChat,
        {
          className: classes.blank,
          cwd,
          queueRef: legacyRefs?.crossSessionQueue
        }
      ),
      /* @__PURE__ */ (0, import_jsx_runtime40.jsx)("div", { className: classes.composer, ref: legacyRefs?.composer })
    ] });
  }

  // src/web-ui/react/shell/shell-app.tsx
  var import_jsx_runtime41 = __toESM(require_jsx_runtime(), 1);
  function getShellLayoutClassName(layout) {
    const classes = ["main-layout"];
    if (layout.sessionsDrawerOpen) classes.push("sidebar-open");
    if (layout.sidebarAnchored) classes.push("sidebar-pinned");
    if (layout.sidebarPinned && layout.sidebarCollapsed) classes.push("sidebar-collapsed");
    return classes.join(" ");
  }
  function ShellAppFrame({ legacyRefs } = {}) {
    const snapshot7 = useUiStoreSnapshot();
    return /* @__PURE__ */ (0, import_jsx_runtime41.jsx)("div", { className: "app-container", children: /* @__PURE__ */ (0, import_jsx_runtime41.jsxs)("div", { className: getShellLayoutClassName(snapshot7.layout), children: [
      /* @__PURE__ */ (0, import_jsx_runtime41.jsx)(ShellSidebar, {}),
      /* @__PURE__ */ (0, import_jsx_runtime41.jsx)(ShellMainContent, { legacyRefs })
    ] }) });
  }
  function ShellApp({ store, legacyRefs }) {
    return /* @__PURE__ */ (0, import_jsx_runtime41.jsx)(UiStoreProvider, { store, children: /* @__PURE__ */ (0, import_jsx_runtime41.jsx)(ShellAppFrame, { legacyRefs }) });
  }

  // src/web-ui/react/shell/ui-store.ts
  function cloneValue(value, seen = /* @__PURE__ */ new Map()) {
    if (value === null || typeof value !== "object") return value;
    const existing = seen.get(value);
    if (existing !== void 0) return existing;
    if (Array.isArray(value)) {
      const clone2 = [];
      seen.set(value, clone2);
      for (const item of value) clone2.push(cloneValue(item, seen));
      return clone2;
    }
    const clone = {};
    seen.set(value, clone);
    for (const [key, item] of Object.entries(value)) clone[key] = cloneValue(item, seen);
    return clone;
  }
  function freezeValue(value, seen = /* @__PURE__ */ new Set()) {
    if (value === null || typeof value !== "object" || seen.has(value)) return value;
    seen.add(value);
    for (const item of Object.values(value)) freezeValue(item, seen);
    return Object.freeze(value);
  }
  function isolateSnapshot(data, revision, freeze) {
    const snapshot7 = cloneValue({
      auth: data.auth,
      viewport: data.viewport,
      capabilities: data.capabilities,
      layout: data.layout,
      selected: data.selected,
      sidebar: data.sidebar,
      topbar: data.topbar,
      legacyVisibility: data.legacyVisibility,
      revision
    });
    return freeze ? freezeValue(snapshot7) : snapshot7;
  }
  function normalizeBatchMs(value) {
    const batchMs = value ?? 0;
    if (!Number.isFinite(batchMs) || batchMs < 0 || batchMs > 200) {
      throw new RangeError("batchMs must be between 0 and 200 milliseconds");
    }
    return batchMs;
  }
  function reportListenerError(error) {
    const reporter = globalThis.reportError;
    if (typeof reporter === "function") {
      reporter(error);
      return;
    }
    console.error("[wand-ui-store] subscriber failed", error);
  }
  function isPromiseLike2(value) {
    return value !== null && (typeof value === "object" || typeof value === "function") && typeof value.then === "function";
  }
  var UiStoreCore = class {
    constructor(options) {
      __publicField(this, "readSnapshot");
      __publicField(this, "applyAction");
      __publicField(this, "batchMs");
      __publicField(this, "freezeSnapshots");
      __publicField(this, "listeners", /* @__PURE__ */ new Set());
      __publicField(this, "snapshot");
      __publicField(this, "disposed", false);
      __publicField(this, "pending", false);
      __publicField(this, "scheduleGeneration", 0);
      __publicField(this, "timer", null);
      this.readSnapshot = options.readSnapshot;
      this.applyAction = options.applyAction;
      this.batchMs = normalizeBatchMs(options.batchMs);
      this.freezeSnapshots = options.freezeSnapshots !== false;
      this.snapshot = isolateSnapshot(this.readSnapshot(), 0, this.freezeSnapshots);
    }
    getSnapshot() {
      return this.snapshot;
    }
    subscribe(listener) {
      if (this.disposed) return () => {
      };
      this.listeners.add(listener);
      let subscribed = true;
      return () => {
        if (!subscribed) return;
        subscribed = false;
        this.listeners.delete(listener);
      };
    }
    dispatch(action) {
      if (this.disposed) throw new Error("UiStore has been disposed");
      const result = this.applyAction(action);
      this.publish({ sync: true, reason: `action:${action.type}` });
      if (!isPromiseLike2(result)) return;
      return Promise.resolve(result).then(
        (value) => {
          this.publish({ sync: true, reason: `action-settled:${action.type}` });
          return value;
        },
        (error) => {
          this.publish({ sync: true, reason: `action-settled:${action.type}` });
          throw error;
        }
      );
    }
    publish(options = {}) {
      if (this.disposed) return;
      if (options.sync) {
        this.cancelPendingPublish();
        this.flush();
        return;
      }
      if (this.pending) return;
      this.pending = true;
      const generation = ++this.scheduleGeneration;
      if (this.batchMs === 0) {
        queueMicrotask(() => this.flushScheduled(generation));
        return;
      }
      this.timer = setTimeout(() => this.flushScheduled(generation), this.batchMs);
    }
    dispose() {
      if (this.disposed) return;
      this.disposed = true;
      this.cancelPendingPublish();
      this.listeners.clear();
    }
    isDisposed() {
      return this.disposed;
    }
    flushScheduled(generation) {
      if (this.disposed || !this.pending || generation !== this.scheduleGeneration) return;
      this.pending = false;
      this.timer = null;
      this.flush();
    }
    cancelPendingPublish() {
      this.pending = false;
      this.scheduleGeneration += 1;
      if (this.timer !== null) clearTimeout(this.timer);
      this.timer = null;
    }
    flush() {
      if (this.disposed) return;
      this.snapshot = isolateSnapshot(
        this.readSnapshot(),
        this.snapshot.revision + 1,
        this.freezeSnapshots
      );
      for (const listener of [...this.listeners]) {
        try {
          listener();
        } catch (error) {
          reportListenerError(error);
        }
      }
    }
  };
  var LegacyUiAdapter = class {
    constructor(options) {
      __publicField(this, "store");
      __publicField(this, "unsubscribeLegacy");
      this.store = new UiStoreCore({
        readSnapshot: options.readSnapshot,
        applyAction: options.applyAction,
        batchMs: options.batchMs,
        freezeSnapshots: options.freezeSnapshots
      });
      const unsubscribe = options.subscribeLegacy((reason) => this.publish({ reason }));
      this.unsubscribeLegacy = typeof unsubscribe === "function" ? unsubscribe : () => {
      };
    }
    getSnapshot() {
      return this.store.getSnapshot();
    }
    subscribe(listener) {
      return this.store.subscribe(listener);
    }
    dispatch(action) {
      return this.store.dispatch(action);
    }
    publish(options) {
      this.store.publish(options);
    }
    dispose() {
      this.store.dispose();
      const unsubscribe = this.unsubscribeLegacy;
      this.unsubscribeLegacy = () => {
      };
      unsubscribe();
    }
  };

  // src/web-ui/react/index.tsx
  var import_jsx_runtime42 = __toESM(require_jsx_runtime(), 1);
  var OVERLAY_ROOT_ID = "overlay-root";
  var REACT_MOUNT_ID = "wand-react-ui-mount";
  var PORTAL_CONTAINER_ID = "wand-react-ui-portals";
  var activeRoot = null;
  function getOrCreateChild(parent, id, className) {
    const existing = document.getElementById(id);
    if (existing && existing.parentElement === parent) return existing;
    const element = document.createElement("div");
    element.id = id;
    element.className = className;
    parent.appendChild(element);
    return element;
  }
  function getOrCreateOverlayRoot() {
    const existing = document.getElementById(OVERLAY_ROOT_ID);
    if (existing) return existing;
    const root = document.createElement("div");
    root.id = OVERLAY_ROOT_ID;
    root.dataset.wandUiRoot = "";
    document.body.appendChild(root);
    return root;
  }
  function exposeBusinessControllers() {
    window.__wandReactSettings = settingsController;
    window.__wandReactNewSession = newSessionController;
    window.__wandReactFolderPicker = folderPickerController;
    window.__wandReactQuickCommit = quickCommitController;
    window.__wandReactWorktreeMerge = worktreeMergeController;
    window.__wandReactRestartOverlay = restartOverlayController;
    window.__wandReactFilePreview = filePreviewController;
  }
  function startReactUi() {
    const overlayRoot = getOrCreateOverlayRoot();
    const genericUiEnabled = isReactUiEnabled();
    if (!activeRoot) {
      installReactUiStyles();
      const mount = getOrCreateChild(overlayRoot, REACT_MOUNT_ID, "wand-ui-mount");
      const portals = getOrCreateChild(overlayRoot, PORTAL_CONTAINER_ID, "wand-ui-portals");
      activeRoot = (0, import_client.createRoot)(mount);
      activeRoot.render(/* @__PURE__ */ (0, import_jsx_runtime42.jsx)(OverlayHost, { portalContainer: portals }));
    }
    exposeBusinessControllers();
    overlayRoot.dataset.reactUi = genericUiEnabled ? "enabled" : "fallback";
    if (!genericUiEnabled) {
      delete window.__wandReactUi;
      return null;
    }
    window.__wandReactUi = wandOverlay;
    return wandOverlay;
  }

  // src/web-ui/browser/ui-store-bridge.ts
  var AUTOMATION_EXPANDED_KEY = "wand-automation-sessions-expanded";
  var HISTORY_EXPANDED_KEY = "wand-non-wand-sessions-expanded";
  var legacyChangeListeners = /* @__PURE__ */ new Set();
  function notifyLegacyUiChange(reason) {
    for (const listener of [...legacyChangeListeners]) listener(reason);
  }
  function browserEnvironment() {
    const nativeBridge2 = typeof WandNative === "undefined" ? void 0 : WandNative;
    return {
      width: window.innerWidth,
      online: navigator.onLine,
      embedTerminal: document.documentElement.classList.contains("is-wand-embed-terminal"),
      nativeInput: Boolean(window.__wandImeNative || window.__wandIosNative),
      backToNative: typeof nativeBridge2?.backToNative === "function",
      switchServer: typeof nativeBridge2?.switchServer === "function",
      automationExpanded: readStoredBoolean(AUTOMATION_EXPANDED_KEY, false),
      historyExpanded: readStoredBoolean(HISTORY_EXPANDED_KEY, false)
    };
  }
  function subscribeBrowserChanges(listener) {
    legacyChangeListeners.add(listener);
    const onResize = () => listener("viewport:resize");
    const onOnline = () => listener("network:online");
    const onOffline = () => listener("network:offline");
    window.addEventListener("resize", onResize);
    window.addEventListener("online", onOnline);
    window.addEventListener("offline", onOffline);
    return () => {
      legacyChangeListeners.delete(listener);
      window.removeEventListener("resize", onResize);
      window.removeEventListener("online", onOnline);
      window.removeEventListener("offline", onOffline);
    };
  }
  function createBrowserUiStoreBridge(commands2, options = {}) {
    return new LegacyUiAdapter({
      readSnapshot: () => deriveLegacyUiSnapshot(state, browserEnvironment()),
      applyAction: (action) => applyLegacyUiAction(commands2, action),
      subscribeLegacy: subscribeBrowserChanges,
      batchMs: options.batchMs,
      freezeSnapshots: options.freezeSnapshots
    });
  }

  // src/web-ui/browser/shell-runtime.ts
  var commands = null;
  var runtime5 = null;
  function configureBrowserShellCommands(nextCommands) {
    if (runtime5) throw new Error("Cannot replace browser shell commands after mount");
    commands = nextCommands;
  }
  function isBrowserReactShellMounted() {
    return runtime5 !== null;
  }
  function moveChildren(source, target) {
    while (source.firstChild) target.appendChild(source.firstChild);
  }
  function createLegacyRefs(markup) {
    const template = document.createElement("template");
    template.innerHTML = markup;
    const slots = [
      { name: "BrowserTerminalSlot", selector: "#output", key: "terminal" },
      { name: "BrowserChatSlot", selector: "#chat-output", key: "chat" },
      { name: "BrowserComposerSlot", selector: ".input-panel", key: "composer" },
      { name: "BrowserFileExplorerSlot", selector: "#file-explorer", key: "fileExplorer" },
      { name: "BrowserCrossSessionQueueSlot", selector: "#cross-session-queue-host", key: "crossSessionQueue" }
    ];
    const refs = {};
    const hosts = slots.map(({ name, selector, key }) => {
      const source = template.content.querySelector(selector);
      if (!source) throw new Error(`${name} seed is missing ${selector}`);
      const host = new LegacyHost(name, {
        mount(target) {
          moveChildren(source, target);
        }
      });
      refs[key] = (node) => {
        if (node) {
          host.mount(node);
        } else {
          host.unmount();
        }
      };
      return host;
    });
    return { refs, hosts };
  }
  function renderBrowserReactShell(app, createLegacyMarkup) {
    if (!isReactShellEnabled()) {
      unmountBrowserReactShell();
      return "disabled";
    }
    if (!commands) throw new Error("Browser shell commands were not configured");
    if (runtime5) {
      if (runtime5.app !== app) throw new Error("React shell cannot move to a different #app root");
      (0, import_react_dom3.flushSync)(() => runtime5?.store.publish({ sync: true, reason: "legacy:render" }));
      return "updated";
    }
    const legacy = createLegacyRefs(createLegacyMarkup());
    const store = createBrowserUiStoreBridge(commands, { batchMs: 0 });
    app.replaceChildren();
    app.dataset.reactShell = "enabled";
    const root = (0, import_client2.createRoot)(app);
    runtime5 = { app, root, store, hosts: legacy.hosts };
    try {
      (0, import_react_dom3.flushSync)(() => root.render(React41.createElement(ShellApp, { store, legacyRefs: legacy.refs })));
    } catch (error) {
      runtime5 = null;
      store.dispose();
      for (const host of legacy.hosts) host.dispose();
      delete app.dataset.reactShell;
      throw error;
    }
    return "mounted";
  }
  function unmountBrowserReactShell() {
    const active = runtime5;
    if (!active) return;
    runtime5 = null;
    (0, import_react_dom3.flushSync)(() => active.root.unmount());
    active.store.dispose();
    for (const host of active.hosts) host.dispose();
    delete active.app.dataset.reactShell;
  }

  // src/web-ui/browser/file-preview-adapter.ts
  var activeDependencies = null;
  var disposeRuntime = null;
  var competingClose = null;
  var pendingCompetingIntent = null;
  function normalizeSiblings(items) {
    const siblings = [];
    for (const item of items) {
      if (item.type !== "file" || typeof item.path !== "string" || !item.path.trim()) continue;
      siblings.push({
        path: item.path,
        ...typeof item.name === "string" && item.name ? { name: item.name } : {},
        type: "file"
      });
    }
    return siblings;
  }
  function closeCompetingReactOverlays() {
    const controllers = [
      quickCommitController,
      folderPickerController,
      newSessionController,
      worktreeMergeController,
      settingsController
    ];
    for (const controller of controllers) {
      if (controller.isOpen() && !controller.closeIfOpen()) return false;
    }
    return true;
  }
  function installFilePreviewLegacyAdapter(dependencies) {
    disposeRuntime?.();
    activeDependencies = dependencies;
    disposeRuntime = configureFilePreviewRuntime({
      confirmDiscard: dependencies.confirmDiscard,
      async copyText(text5) {
        if (!await dependencies.copyText(text5)) throw new Error("\u590D\u5236\u5931\u8D25\u3002");
      },
      appendToComposer: dependencies.appendToComposer,
      notify: dependencies.notify,
      onSaved: dependencies.onSaved
    });
    let disposed = false;
    return () => {
      if (disposed || activeDependencies !== dependencies) return;
      disposed = true;
      pendingCompetingIntent = null;
      filePreviewController.closeIfOpen();
      disposeRuntime?.();
      disposeRuntime = null;
      activeDependencies = null;
    };
  }
  function openFilePreviewFromLegacy(path) {
    const dependencies = activeDependencies;
    const normalizedPath = typeof path === "string" ? path.trim() : "";
    if (!dependencies || !normalizedPath) return false;
    if (restartOverlayController.isOpen()) return false;
    if (!closeCompetingReactOverlays()) return false;
    void filePreviewController.open({
      path: normalizedPath,
      siblings: normalizeSiblings(dependencies.getSiblings())
    });
    return true;
  }
  function prepareFilePreviewForCompetingOverlay(openIntent) {
    if (!filePreviewController.isOpen()) return true;
    const snapshot7 = filePreviewStore.getSnapshot();
    if (snapshot7.saving) return false;
    if (!snapshot7.dirty) {
      filePreviewController.closeIfOpen();
      return !filePreviewController.isOpen();
    }
    pendingCompetingIntent = openIntent;
    if (!competingClose) {
      competingClose = filePreviewController.execute({ type: "close" });
      void competingClose.then((closed) => {
        const intent = pendingCompetingIntent;
        pendingCompetingIntent = null;
        competingClose = null;
        if (!closed || !intent) return;
        try {
          intent();
        } catch (error) {
          console.warn("[wand] Failed to continue overlay open intent", error);
        }
      });
    }
    return false;
  }

  // src/web-ui/browser/file-browser.ts
  function isMobileLayout() {
    return window.innerWidth <= 768;
  }
  function shouldShowSessionsBackdrop() {
    return !!state.sessionsDrawerOpen && (isMobileLayout() || !state.sidebarPinned);
  }
  function setFilePanelOpen(nextOpen) {
    state.filePanelOpen = nextOpen;
    try {
      localStorage.setItem("wand-file-panel-open", String(state.filePanelOpen));
    } catch (e) {
    }
    if (state.filePanelOpen && isMobileLayout()) {
      state.sessionsDrawerOpen = false;
      writeStoredBoolean("wand-sidebar-open", false);
    }
    updateLayoutState();
    if (state.filePanelOpen) {
      refreshFileExplorer();
    }
  }
  function toggleFilePanel() {
    setFilePanelOpen(!state.filePanelOpen);
  }
  function updateFilePanelState() {
    if (isBrowserReactShellMounted()) {
      notifyLegacyUiChange("layout:files");
      return;
    }
    var panel = document.getElementById("file-side-panel");
    var mainContent = document.querySelector(".main-content");
    var toggleBtn = document.getElementById("file-panel-toggle-btn");
    var backdrop = document.getElementById("file-panel-backdrop");
    if (panel) {
      panel.classList.toggle("open", state.filePanelOpen);
    }
    if (mainContent) {
      mainContent.classList.toggle("file-panel-open", state.filePanelOpen);
    }
    if (backdrop) {
      backdrop.classList.toggle("open", state.filePanelOpen);
    }
    if (toggleBtn) {
      toggleBtn.classList.toggle("active", state.filePanelOpen);
    }
  }
  function updateLayoutState() {
    updateDrawerState();
    updateFilePanelState();
  }
  function updateFilePanelCwd(session) {
    if (isBrowserReactShellMounted()) {
      notifyLegacyUiChange("layout:file-cwd");
      return;
    }
    var cwdEl = document.getElementById("file-explorer-cwd");
    if (!cwdEl) return;
    var cwd = session && session.cwd ? session.cwd : getConfigCwd();
    if (cwdEl.tagName === "INPUT") {
      if (document.activeElement !== cwdEl) {
        cwdEl.value = cwd;
        scrollInputToEnd(cwdEl);
      }
    } else {
      cwdEl.textContent = cwd;
    }
    cwdEl.title = cwd;
    var headerEl = cwdEl.closest(".file-explorer-header");
    if (headerEl) {
      scrollPathElementToEnd(headerEl);
    }
  }
  function closeFilePanel() {
    if (!state.filePanelOpen) return;
    setFilePanelOpen(false);
  }
  function adjustTerminalScale(delta) {
    var newScale = state.terminalScale + delta;
    newScale = Math.max(0.5, Math.min(2, newScale));
    newScale = Math.round(newScale * 4) / 4;
    if (newScale === state.terminalScale) return;
    state.terminalScale = newScale;
    try {
      localStorage.setItem("wand-terminal-scale", String(newScale));
    } catch (e) {
    }
    applyTerminalScale();
    updateScaleLabel();
  }
  function applyTerminalScale() {
    if (!state.terminal || !state.terminal.element) return;
    var rawFontSize = state.terminalBaseFontSize * state.terminalScale;
    var fontPx = Math.max(1, Math.round(rawFontSize));
    var rowPx = Math.max(1, Math.round(rawFontSize * 1.5));
    state.terminal.element.style.setProperty("--term-font-size", fontPx + "px");
    state.terminal.element.style.setProperty("--term-row-height", rowPx + "px");
    if (typeof state.terminal.remeasure === "function") {
      requestAnimationFrame(function() {
        if (state.terminal) state.terminal.remeasure();
      });
    }
  }
  function updateScaleLabel() {
    var label = document.getElementById("terminal-scale-label-top");
    if (label) {
      label.textContent = Math.round(state.terminalScale * 100) + "%";
    }
  }
  var WAND_FILE_ICONS = {
    "chevron-left": '<path d="M15 18l-6-6 6-6"/>',
    "arrow-up": '<path d="M12 19V5"/><path d="M5 12l7-7 7 7"/>',
    "refresh": '<path d="M21 12a9 9 0 1 1-3-6.7"/><path d="M21 4v5h-5"/>',
    "eye": '<path d="M1 12s4-7 11-7 11 7 11 7-4 7-11 7S1 12 1 12z"/><circle cx="12" cy="12" r="3"/>',
    "eye-off": '<path d="M17.94 17.94A10.94 10.94 0 0 1 12 19c-7 0-11-7-11-7a19.86 19.86 0 0 1 4.22-5.18"/><path d="M1 1l22 22"/><path d="M9.9 4.24A10.94 10.94 0 0 1 12 4c7 0 11 7 11 7a19.83 19.83 0 0 1-3.36 4.27"/><path d="M14.12 14.12a3 3 0 1 1-4.24-4.24"/>',
    "x": '<path d="M18 6L6 18"/><path d="M6 6l12 12"/>',
    "search": '<circle cx="11" cy="11" r="7"/><path d="M21 21l-4.3-4.3"/>',
    "copy": '<rect x="9" y="9" width="11" height="11" rx="2"/><path d="M5 15V5a2 2 0 0 1 2-2h10"/>',
    "clipboard": '<rect x="8" y="3" width="8" height="4" rx="1"/><path d="M16 5h2a2 2 0 0 1 2 2v12a2 2 0 0 1-2 2H6a2 2 0 0 1-2-2V7a2 2 0 0 1 2-2h2"/>',
    "download": '<path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><path d="M7 10l5 5 5-5"/><path d="M12 15V3"/>',
    "edit": '<path d="M12 20h9"/><path d="M16.5 3.5a2.121 2.121 0 0 1 3 3L7 19l-4 1 1-4z"/>',
    "save": '<path d="M19 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11l5 5v11a2 2 0 0 1-2 2z"/><path d="M17 21v-8H7v8"/><path d="M7 3v5h8"/>',
    "rotate-ccw": '<path d="M3 12a9 9 0 1 0 3-6.7"/><path d="M3 4v5h5"/>',
    "wrap-text": '<path d="M3 6h18"/><path d="M3 12h15a3 3 0 1 1 0 6h-4"/><path d="M16 16l-2 2 2 2"/><path d="M3 18h6"/>',
    "type": '<path d="M4 7V4h16v3"/><path d="M9 20h6"/><path d="M12 4v16"/>',
    "minus": '<path d="M5 12h14"/>',
    "plus": '<path d="M12 5v14"/><path d="M5 12h14"/>',
    "send-to-input": '<path d="M22 2L11 13"/><path d="M22 2l-7 20-4-9-9-4z"/>',
    "terminal": '<polyline points="4 17 10 11 4 5"/><line x1="12" y1="19" x2="20" y2="19"/>',
    "folder-open": '<path d="M6 14l1.45-2.9A2 2 0 0 1 9.24 10H20a2 2 0 0 1 1.94 2.5l-1.55 6a2 2 0 0 1-1.94 1.5H4a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h3.93a2 2 0 0 1 1.66.9l.82 1.2a2 2 0 0 0 1.66.9H18a2 2 0 0 1 2 2v2"/>',
    "info": '<circle cx="12" cy="12" r="10"/><line x1="12" y1="16" x2="12" y2="12"/><line x1="12" y1="8" x2="12.01" y2="8"/>'
  };
  function wandFileIcon(name, opts) {
    opts = opts || {};
    var body = WAND_FILE_ICONS[name] || "";
    var size4 = opts.size || 16;
    var extraClass = opts.className ? " " + opts.className : "";
    return '<svg class="wand-icon wand-icon-' + name + extraClass + '" width="' + size4 + '" height="' + size4 + '" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">' + body + "</svg>";
  }
  function renderFileExplorer(cwd) {
    var root = cwd || getConfigCwd();
    if (!root) {
      return '<div class="file-explorer empty">\u672A\u914D\u7F6E\u5DE5\u4F5C\u76EE\u5F55\u3002</div>';
    }
    return '<div class="file-tree" id="file-tree" data-cwd="' + escapeHtml(root) + '"><div class="tree-loading">\u52A0\u8F7D\u4E2D\u2026</div></div>';
  }
  var FILE_ICON_MAP = {
    // images
    png: "\u{1F5BC}\uFE0F",
    jpg: "\u{1F5BC}\uFE0F",
    jpeg: "\u{1F5BC}\uFE0F",
    gif: "\u{1F5BC}\uFE0F",
    webp: "\u{1F5BC}\uFE0F",
    svg: "\u{1F5BC}\uFE0F",
    avif: "\u{1F5BC}\uFE0F",
    bmp: "\u{1F5BC}\uFE0F",
    ico: "\u{1F5BC}\uFE0F",
    heic: "\u{1F5BC}\uFE0F",
    heif: "\u{1F5BC}\uFE0F",
    // pdf / doc
    pdf: "\u{1F4D5}",
    doc: "\u{1F4D8}",
    docx: "\u{1F4D8}",
    odt: "\u{1F4D8}",
    xls: "\u{1F4CA}",
    xlsx: "\u{1F4CA}",
    csv: "\u{1F4CA}",
    tsv: "\u{1F4CA}",
    ppt: "\u{1F4D9}",
    pptx: "\u{1F4D9}",
    // video / audio
    mp4: "\u{1F3AC}",
    webm: "\u{1F3AC}",
    mov: "\u{1F3AC}",
    mkv: "\u{1F3AC}",
    m4v: "\u{1F3AC}",
    ogv: "\u{1F3AC}",
    mp3: "\u{1F3B5}",
    wav: "\u{1F3B5}",
    ogg: "\u{1F3B5}",
    m4a: "\u{1F3B5}",
    flac: "\u{1F3B5}",
    aac: "\u{1F3B5}",
    opus: "\u{1F3B5}",
    // archives
    zip: "\u{1F4E6}",
    tar: "\u{1F4E6}",
    gz: "\u{1F4E6}",
    tgz: "\u{1F4E6}",
    bz2: "\u{1F4E6}",
    "7z": "\u{1F4E6}",
    rar: "\u{1F4E6}",
    xz: "\u{1F4E6}",
    // markup / docs
    md: "\u{1F4DD}",
    markdown: "\u{1F4DD}",
    mdx: "\u{1F4DD}",
    rst: "\u{1F4DD}",
    txt: "\u{1F4DD}",
    log: "\u{1F4DD}",
    // web / styles
    html: "\u{1F310}",
    htm: "\u{1F310}",
    xml: "\u{1F310}",
    css: "\u{1F3A8}",
    scss: "\u{1F3A8}",
    less: "\u{1F3A8}",
    // configs
    json: "\u2699\uFE0F",
    jsonc: "\u2699\uFE0F",
    yaml: "\u2699\uFE0F",
    yml: "\u2699\uFE0F",
    toml: "\u2699\uFE0F",
    ini: "\u2699\uFE0F",
    cfg: "\u2699\uFE0F",
    conf: "\u2699\uFE0F",
    env: "\u2699\uFE0F",
    editorconfig: "\u2699\uFE0F",
    // code (default 📜)
    ts: "\u{1F4DC}",
    tsx: "\u{1F4DC}",
    js: "\u{1F4DC}",
    jsx: "\u{1F4DC}",
    mjs: "\u{1F4DC}",
    cjs: "\u{1F4DC}",
    py: "\u{1F4DC}",
    rb: "\u{1F4DC}",
    go: "\u{1F4DC}",
    rs: "\u{1F4DC}",
    java: "\u{1F4DC}",
    c: "\u{1F4DC}",
    cpp: "\u{1F4DC}",
    h: "\u{1F4DC}",
    hpp: "\u{1F4DC}",
    cs: "\u{1F4DC}",
    swift: "\u{1F4DC}",
    kt: "\u{1F4DC}",
    scala: "\u{1F4DC}",
    php: "\u{1F4DC}",
    sh: "\u{1F4DC}",
    bash: "\u{1F4DC}",
    zsh: "\u{1F4DC}",
    fish: "\u{1F4DC}",
    lua: "\u{1F4DC}",
    sql: "\u{1F4DC}",
    graphql: "\u{1F4DC}",
    proto: "\u{1F4DC}",
    vue: "\u{1F4DC}",
    svelte: "\u{1F4DC}",
    diff: "\u{1F4DC}",
    patch: "\u{1F4DC}",
    // fonts / binary
    ttf: "\u{1F524}",
    otf: "\u{1F524}",
    woff: "\u{1F524}",
    woff2: "\u{1F524}",
    eot: "\u{1F524}"
  };
  function getFileIcon(item) {
    if (!item) return "\u{1F4C4}";
    if (item.type === "dir") return "\u{1F4C1}";
    var name = (item.name || "").toLowerCase();
    if (name === "dockerfile") return "\u{1F433}";
    if (name === "makefile") return "\u{1F6E0}\uFE0F";
    if (name === "license") return "\u{1F4DC}";
    if (name === "readme") return "\u{1F4DD}";
    var dot = name.lastIndexOf(".");
    if (dot < 0 || dot === name.length - 1) return "\u{1F4C4}";
    var ext = name.slice(dot + 1);
    return FILE_ICON_MAP[ext] || "\u{1F4C4}";
  }
  function formatFileSize(bytes) {
    if (typeof bytes !== "number" || !isFinite(bytes) || bytes < 0) return "";
    if (bytes < 1024) return bytes + " B";
    var kb = bytes / 1024;
    if (kb < 1024) return (kb >= 10 ? Math.round(kb) : kb.toFixed(1)) + " KB";
    var mb = kb / 1024;
    if (mb < 1024) return (mb >= 10 ? Math.round(mb) : mb.toFixed(1)) + " MB";
    var gb = mb / 1024;
    return (gb >= 10 ? Math.round(gb) : gb.toFixed(1)) + " GB";
  }
  function formatRelativeTime(iso) {
    if (!iso) return "";
    var t6 = Date.parse(iso);
    if (isNaN(t6)) return "";
    return new Date(t6).toLocaleString();
  }
  function getEffectiveExplorerCwd() {
    if (state.fileExplorerCwd) return state.fileExplorerCwd;
    if (state.selectedId) {
      var session = state.sessions.find(function(s) {
        return s.id === state.selectedId;
      });
      if (session && session.cwd) return session.cwd;
    }
    return getConfigCwd();
  }
  function refreshFileExplorer(opts) {
    opts = opts || {};
    var explorer = document.getElementById("file-explorer");
    var cwdEl = document.getElementById("file-explorer-cwd");
    if (!explorer) return;
    var cwd = opts.cwd || getEffectiveExplorerCwd();
    if (!cwd) {
      explorer.innerHTML = '<div class="file-explorer empty">\u6CA1\u6709\u53EF\u663E\u793A\u7684\u5DE5\u4F5C\u76EE\u5F55\u3002</div>';
      return;
    }
    state.fileExplorerCwd = cwd;
    state.fileExplorerLoading = true;
    state.allFiles = [];
    state.fileExplorerTruncated = false;
    state.fileExplorerTotal = 0;
    explorer.innerHTML = '<div class="file-explorer"><div class="tree-loading" style="padding:12px;color:var(--text-muted);font-size:0.8125rem;">\u52A0\u8F7D\u4E2D\u2026</div></div>';
    if (cwdEl && !isBrowserReactShellMounted()) {
      if (cwdEl.tagName === "INPUT") {
        if (document.activeElement !== cwdEl) {
          cwdEl.value = cwd;
        }
      } else {
        cwdEl.textContent = cwd;
      }
      cwdEl.title = cwd;
    }
    var url = "/api/directory?q=" + encodeURIComponent(cwd) + "&gitStatus=true";
    fetch(url, { credentials: "same-origin" }).then(function(res) {
      if (!res.ok) throw new Error("Failed to load directory.");
      return res.json();
    }).then(function(payload) {
      state.fileExplorerLoading = false;
      var items, truncated, total;
      if (Array.isArray(payload)) {
        items = payload;
        truncated = false;
        total = payload.length;
      } else {
        items = payload && payload.items || [];
        truncated = !!(payload && payload.truncated);
        total = payload && payload.total || items.length;
      }
      if (!items || items.length === 0) {
        explorer.innerHTML = '<div class="file-explorer empty">\u7A7A\u76EE\u5F55\u6216\u65E0\u6CD5\u8BBF\u95EE\u3002</div>';
        return;
      }
      state.allFiles = items;
      state.fileExplorerTruncated = truncated;
      state.fileExplorerTotal = total;
      filterFileTree();
    }).catch(function() {
      state.fileExplorerLoading = false;
      explorer.innerHTML = '<div class="file-explorer empty">\u52A0\u8F7D\u5931\u8D25\uFF0C\u8BF7\u68C0\u67E5\u8DEF\u5F84\u6216\u6743\u9650\u3002</div>';
    });
  }
  function filterFileTree() {
    var explorer = document.getElementById("file-explorer");
    if (!explorer) return;
    var cwd = state.fileExplorerCwd || "";
    if (!cwd) return;
    var query = state.fileSearchQuery;
    var items = state.allFiles || [];
    var filtered = items;
    if (query) {
      var lowerQuery = query.toLowerCase();
      filtered = items.filter(function(item) {
        return item.name.toLowerCase().indexOf(lowerQuery) !== -1;
      });
    }
    if (filtered.length === 0) {
      explorer.innerHTML = '<div class="file-explorer empty">' + (query ? "\u6CA1\u6709\u627E\u5230\u5339\u914D\u7684\u6587\u4EF6" : "\u7A7A\u76EE\u5F55") + "</div>";
      return;
    }
    var truncatedNotice = "";
    if (!query && state.fileExplorerTruncated) {
      var shown = items.length;
      truncatedNotice = '<div class="tree-truncated" title="\u540E\u7AEF\u6309\u5B57\u6BCD\u5E8F\u6700\u591A\u8FD4\u56DE ' + shown + ' \u9879">\u663E\u793A\u524D ' + shown + " \u9879 / \u5171 " + state.fileExplorerTotal + " \u9879</div>";
    }
    explorer.innerHTML = '<div class="file-tree" id="file-tree" data-cwd="' + escapeHtml(cwd) + '">' + filtered.map(function(item) {
      return renderFileTreeItem(item);
    }).join("") + "</div>" + truncatedNotice;
    attachFileTreeListeners();
  }
  function renderFileTreeItem(item, depth) {
    depth = depth || 0;
    var name = escapeHtml(item.name);
    var isDir = item.type === "dir";
    var displayIcon = getFileIcon(item);
    var toggleIcon = isDir ? "\u25B8" : "";
    var toggleClass = isDir ? "" : " empty";
    var gitStatus = item.gitStatus;
    var statusBadge = renderGitStatusBadge(gitStatus);
    var meta = "";
    if (!isDir && typeof item.size === "number") {
      meta = '<span class="tree-meta" title="\u5927\u5C0F\uFF1A' + escapeHtml(formatFileSize(item.size)) + (item.mtime ? "\n\u4FEE\u6539\u65F6\u95F4\uFF1A" + escapeHtml(formatRelativeTime(item.mtime)) : "") + '">' + escapeHtml(formatFileSize(item.size)) + "</span>";
    }
    return '<div class="tree-item" data-path="' + escapeHtml(item.path) + '" data-type="' + escapeHtml(item.type) + '" data-name="' + escapeHtml(item.name) + '" tabindex="0"><span class="tree-toggle' + toggleClass + '">' + toggleIcon + '</span><span class="tree-icon">' + displayIcon + '</span><span class="tree-name">' + name + "</span>" + meta + (statusBadge ? '<span class="git-status-badge ' + statusBadge.class + '" title="' + statusBadge.title + '">' + statusBadge.text + "</span>" : "") + "</div>";
  }
  function renderGitStatusBadge(gitStatus) {
    if (!gitStatus) return null;
    if (gitStatus.staged === "added") return { text: "A", class: "git-added", title: "\u5DF2\u6682\u5B58\uFF08\u65B0\u589E\uFF09" };
    if (gitStatus.staged === "modified") return { text: "M", class: "git-modified", title: "\u5DF2\u6682\u5B58\uFF08\u4FEE\u6539\uFF09" };
    if (gitStatus.staged === "deleted") return { text: "D", class: "git-deleted", title: "\u5DF2\u6682\u5B58\uFF08\u5220\u9664\uFF09" };
    if (gitStatus.staged === "renamed") return { text: "R", class: "git-renamed", title: "\u5DF2\u6682\u5B58\uFF08\u91CD\u547D\u540D\uFF09" };
    if (gitStatus.unstaged === "modified") return { text: "M", class: "git-unstaged", title: "\u672A\u6682\u5B58\uFF08\u4FEE\u6539\uFF09" };
    if (gitStatus.unstaged === "deleted") return { text: "D", class: "git-unstaged-deleted", title: "\u672A\u6682\u5B58\uFF08\u5220\u9664\uFF09" };
    if (gitStatus.untracked) return { text: "?", class: "git-untracked", title: "\u672A\u8DDF\u8E2A" };
    return null;
  }
  function attachFileTreeListeners() {
    var tree = document.getElementById("file-tree");
    if (!tree) return;
    tree.querySelectorAll(".tree-item[data-type='dir']").forEach(function(item) {
      item.addEventListener("click", function(e) {
        toggleTreeNode(item);
      });
    });
    tree.querySelectorAll(".tree-item[data-type='file']").forEach(function(item) {
      var openHandler = function() {
        openFilePreview(item.dataset.path);
      };
      item.addEventListener("click", openHandler);
      item.addEventListener("dblclick", openHandler);
    });
    var pressTimer = null;
    var pressFired = false;
    tree.querySelectorAll(".tree-item").forEach(function(item) {
      item.addEventListener("contextmenu", function(e) {
        e.preventDefault();
        showFileContextMenu(e.clientX, e.clientY, item);
      });
      item.addEventListener("touchstart", function(e) {
        pressFired = false;
        pressTimer = setTimeout(function() {
          pressFired = true;
          var t6 = e.touches && e.touches[0];
          showFileContextMenu(t6 ? t6.clientX : 0, t6 ? t6.clientY : 0, item);
        }, 500);
      }, { passive: true });
      item.addEventListener("touchend", function() {
        if (pressTimer) {
          clearTimeout(pressTimer);
          pressTimer = null;
        }
      });
      item.addEventListener("touchmove", function() {
        if (pressTimer) {
          clearTimeout(pressTimer);
          pressTimer = null;
        }
      });
    });
  }
  function toggleTreeNode(item) {
    var p = item.dataset.path;
    var toggle = item.querySelector(".tree-toggle");
    var children = item.nextElementSibling;
    if (children && children.classList.contains("tree-children")) {
      var isOpen = children.classList.contains("open");
      children.classList.toggle("open");
      if (toggle) toggle.classList.toggle("open", !isOpen);
      var iconEl = item.querySelector(".tree-icon");
      if (iconEl) iconEl.textContent = isOpen ? "\u{1F4C1}" : "\u{1F4C2}";
      return;
    }
    if (toggle) toggle.classList.add("open");
    var iconEl2 = item.querySelector(".tree-icon");
    if (iconEl2) iconEl2.textContent = "\u{1F4C2}";
    var url = "/api/directory?q=" + encodeURIComponent(p) + "&gitStatus=true";
    fetch(url, { credentials: "same-origin" }).then(function(res) {
      return res.json();
    }).then(function(payload) {
      var items;
      if (Array.isArray(payload)) items = payload;
      else items = payload && payload.items || [];
      var childrenDiv = document.createElement("div");
      childrenDiv.className = "tree-children open";
      if (!items || items.length === 0) {
        childrenDiv.innerHTML = '<div class="tree-item" style="color:var(--text-muted);cursor:default;"><span class="tree-toggle empty">\u25B8</span><span class="tree-name">\uFF08\u7A7A\u76EE\u5F55\uFF09</span></div>';
      } else {
        childrenDiv.innerHTML = items.map(function(child) {
          return renderFileTreeItem(child);
        }).join("");
      }
      item.parentNode.insertBefore(childrenDiv, item.nextSibling);
      attachFileTreeListeners();
    }).catch(function() {
    });
  }
  function navigateExplorerUp() {
    var cwd = getEffectiveExplorerCwd();
    if (!cwd) return;
    var parent = cwd.replace(/\/+$/, "").replace(/\/[^\/]+$/, "");
    if (!parent) parent = "/";
    if (parent === cwd) return;
    refreshFileExplorer({ cwd: parent });
  }
  function appendToComposer(text5) {
    var inputBox = document.getElementById("input-box");
    if (!inputBox) return false;
    var current = inputBox.value || "";
    var sep = current && !current.endsWith(" ") && !current.endsWith("\n") ? " " : "";
    inputBox.value = current + sep + text5;
    inputBox.dispatchEvent(new Event("input", { bubbles: true }));
    try {
      inputBox.focus();
      inputBox.setSelectionRange(inputBox.value.length, inputBox.value.length);
    } catch (e) {
    }
    return true;
  }
  function copyTextSafely(text5) {
    if (navigator.clipboard && navigator.clipboard.writeText) {
      return navigator.clipboard.writeText(text5).then(function() {
        return true;
      }).catch(function() {
        return fallback();
      });
    }
    return Promise.resolve(fallback());
    function fallback() {
      try {
        var ta = document.createElement("textarea");
        ta.value = text5;
        ta.style.position = "fixed";
        ta.style.left = "-9999px";
        document.body.appendChild(ta);
        ta.select();
        var ok = document.execCommand("copy");
        document.body.removeChild(ta);
        return ok;
      } catch (e) {
        return false;
      }
    }
  }
  function showToastIfPossible(msg) {
    showToast(msg);
  }
  function dismissFileContextMenu() {
    var menu = document.getElementById("file-context-menu");
    if (menu) menu.remove();
    document.removeEventListener("click", dismissFileContextMenu, true);
    document.removeEventListener("scroll", dismissFileContextMenu, true);
  }
  function showFileContextMenu(x, y, item) {
    dismissFileContextMenu();
    var fullPath = item.dataset.path || "";
    var type = item.dataset.type || "file";
    var cwd = state.fileExplorerCwd || "";
    var relativePath = fullPath;
    if (cwd && fullPath.indexOf(cwd) === 0) {
      relativePath = fullPath.slice(cwd.length).replace(/^\/+/, "") || ".";
    }
    var menu = document.createElement("div");
    menu.id = "file-context-menu";
    menu.className = "file-context-menu";
    var actions = [];
    if (type === "file") {
      actions.push({ label: "\u6253\u5F00\u9884\u89C8", icon: "\u{1F441}", run: function() {
        openFilePreview(fullPath);
      } });
    } else {
      actions.push({ label: "\u8FDB\u5165\u6B64\u76EE\u5F55", icon: "\u{1F4C2}", run: function() {
        refreshFileExplorer({ cwd: fullPath });
      } });
    }
    actions.push({ label: "\u590D\u5236\u5B8C\u6574\u8DEF\u5F84", icon: "\u{1F4CB}", run: function() {
      copyTextSafely(fullPath).then(function() {
        showToastIfPossible("\u5DF2\u590D\u5236\u8DEF\u5F84");
      });
    } });
    if (relativePath && relativePath !== fullPath) {
      actions.push({ label: "\u590D\u5236\u76F8\u5BF9\u8DEF\u5F84", icon: "\u{1F4CB}", run: function() {
        copyTextSafely(relativePath).then(function() {
          showToastIfPossible("\u5DF2\u590D\u5236\u76F8\u5BF9\u8DEF\u5F84");
        });
      } });
    }
    actions.push({ label: "\u7C98\u8D34\u8DEF\u5F84\u5230\u8F93\u5165\u6846", icon: "\u270F\uFE0F", run: function() {
      if (appendToComposer(fullPath)) showToastIfPossible("\u5DF2\u7C98\u8D34\u5230\u8F93\u5165\u6846");
    } });
    if (type === "file") {
      actions.push({ label: "\u4E0B\u8F7D\u6587\u4EF6", icon: "\u2B07", run: function() {
        var a = document.createElement("a");
        a.href = "/api/file-raw?download=1&path=" + encodeURIComponent(fullPath);
        a.rel = "noopener";
        document.body.appendChild(a);
        a.click();
        a.remove();
      } });
    }
    menu.innerHTML = actions.map(function(act, i) {
      return '<button type="button" class="file-context-menu-item" data-idx="' + i + '"><span class="ctx-icon">' + act.icon + '</span><span class="ctx-label">' + escapeHtml(act.label) + "</span></button>";
    }).join("");
    document.body.appendChild(menu);
    var vw = window.innerWidth;
    var vh = window.innerHeight;
    var rect = menu.getBoundingClientRect();
    var left = Math.min(x, vw - rect.width - 8);
    var top = Math.min(y, vh - rect.height - 8);
    menu.style.left = Math.max(8, left) + "px";
    menu.style.top = Math.max(8, top) + "px";
    menu.querySelectorAll(".file-context-menu-item").forEach(function(btn) {
      btn.addEventListener("click", function(ev) {
        ev.stopPropagation();
        var idx = parseInt(btn.dataset.idx, 10);
        dismissFileContextMenu();
        if (actions[idx]) actions[idx].run();
      });
    });
    setTimeout(function() {
      document.addEventListener("click", dismissFileContextMenu, true);
      document.addEventListener("scroll", dismissFileContextMenu, true);
    }, 0);
  }
  function openFilePreview(filePath) {
    openFilePreviewFromLegacy(filePath);
  }

  // src/web-ui/browser/git-commit.ts
  function renderTopbarGitBadgeHtml() {
    if (!state.selectedId || !state.gitStatus || !state.gitStatus.isGit) return "";
    if (state.gitStatusSessionId !== state.selectedId) return "";
    var branch = state.gitStatus.branch || "?";
    var count3 = state.gitStatus.modifiedCount || 0;
    var titleText = branch + (count3 ? "  \xB7  " + count3 + " \u4E2A\u6587\u4EF6\u5F85\u63D0\u4EA4" : "  \xB7  \u5DE5\u4F5C\u533A\u5E72\u51C0");
    return '<button id="topbar-git-badge" class="topbar-git-badge" type="button" title="' + escapeHtml(titleText) + '" aria-label="\u5FEB\u6377\u63D0\u4EA4"><svg class="topbar-git-icon" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><circle cx="6" cy="6" r="2"/><circle cx="6" cy="18" r="2"/><circle cx="18" cy="9" r="2"/><path d="M6 8v8"/><path d="M18 11v1a3 3 0 0 1-3 3H9"/></svg><span class="topbar-git-branch">' + escapeHtml(branch) + "</span>" + (count3 > 0 ? '<span class="topbar-git-count">\xB7' + count3 + "</span>" : '<span class="topbar-git-clean" aria-hidden="true">\u2713</span>') + "</button>";
  }
  function updateTopbarGitBadge() {
    if (isBrowserReactShellMounted()) {
      notifyLegacyUiChange("topbar:git");
      return;
    }
    var slot = document.getElementById("topbar-git-slot");
    if (!slot) return;
    slot.innerHTML = renderTopbarGitBadgeHtml();
    var btn = document.getElementById("topbar-git-badge");
    if (btn) {
      btn.addEventListener("click", function(e) {
        e.preventDefault();
        openQuickCommitModal();
      });
    }
  }
  function renderTopbarMoreMenuHtml(session) {
    if (!session) return "";
    var open = state.topbarMoreOpen;
    var hasClaudeId = !!session.claudeSessionId;
    var hasCwd = !!session.cwd;
    var canOpenMerge = session.worktreeEnabled && session.worktree && session.worktree.branch && session.worktree.path;
    var needsCleanup = session.worktreeMergeStatus === "merged" && session.worktreeMergeInfo && session.worktreeMergeInfo.cleanupDone === false;
    var mergeDisabled = session.status === "running" || session.worktreeMergeStatus === "merging";
    var showMerge = canOpenMerge && session.worktreeMergeStatus !== "merged";
    var showCleanup = needsCleanup;
    var hasInfoGroup = hasClaudeId || hasCwd || true;
    var hasActionGroup = showMerge || showCleanup || true;
    var copyIconSvg = '<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="9" y="9" width="13" height="13" rx="2" ry="2"/><path d="M5 15H4a2 2 0 0 1-2-2V4a2 2 0 0 1 2-2h9a2 2 0 0 1 2 2v1"/></svg>';
    var cloudIconSvg = '<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M17.5 19a4.5 4.5 0 1 0-1.5-8.74A6 6 0 1 0 6 14h11.5z"/></svg>';
    var folderIconSvg = '<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 19a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h5l2 3h9a2 2 0 0 1 2 2z"/></svg>';
    var hashIconSvg = '<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="4" y1="9" x2="20" y2="9"/><line x1="4" y1="15" x2="20" y2="15"/><line x1="10" y1="3" x2="8" y2="21"/><line x1="16" y1="3" x2="14" y2="21"/></svg>';
    var mergeIconSvg = '<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M7 7h10"/><path d="M7 12h10"/><path d="M7 17h10"/><path d="M5 7l-2 2 2 2"/><path d="M19 15l2 2-2 2"/></svg>';
    var trashIconSvg = '<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 6h18"/><path d="M8 6V4a2 2 0 012-2h4a2 2 0 012 2v2"/><path d="M19 6l-1 14a2 2 0 01-2 2H8a2 2 0 01-2-2L5 6"/></svg>';
    var infoItems = "";
    if (hasClaudeId) {
      var historyIdLabel = session.provider === "codex" ? "\u590D\u5236 Codex thread ID" : session.provider === "opencode" ? "\u590D\u5236 OpenCode session ID" : "\u590D\u5236 Claude \u4F1A\u8BDD ID";
      infoItems += '<button class="topbar-more-item" data-action="copy-claude-session-id" type="button" role="menuitem">' + cloudIconSvg + "<span>" + historyIdLabel + "</span></button>";
    }
    if (hasCwd) {
      infoItems += '<button class="topbar-more-item" data-action="copy-cwd" type="button" role="menuitem">' + folderIconSvg + "<span>\u590D\u5236\u5DE5\u4F5C\u76EE\u5F55</span></button>";
    }
    infoItems += '<button class="topbar-more-item" data-action="copy-session-id" type="button" role="menuitem">' + hashIconSvg + "<span>\u590D\u5236\u4F1A\u8BDD ID</span></button>";
    var actionItems = "";
    if (showMerge) {
      actionItems += '<button class="topbar-more-item" data-action="worktree-merge" type="button" role="menuitem"' + (mergeDisabled ? " disabled" : "") + ">" + mergeIconSvg + "<span>\u5408\u5E76\u5230\u4E3B\u5206\u652F\u2026</span></button>";
    } else if (showCleanup) {
      actionItems += '<button class="topbar-more-item" data-action="worktree-cleanup" type="button" role="menuitem">' + mergeIconSvg + "<span>\u91CD\u8BD5 worktree \u6E05\u7406</span></button>";
    }
    actionItems += '<button class="topbar-more-item topbar-more-item-danger" data-action="delete-session" type="button" role="menuitem">' + trashIconSvg + "<span>\u5220\u9664\u5F53\u524D\u4F1A\u8BDD</span></button>";
    var divider = hasInfoGroup && hasActionGroup ? '<div class="topbar-more-divider" role="separator"></div>' : "";
    return '<div class="topbar-more-wrap"><button id="topbar-more-button" class="topbar-btn square' + (open ? " active" : "") + '" type="button" aria-label="\u5F53\u524D\u4F1A\u8BDD\u64CD\u4F5C" aria-haspopup="menu" aria-expanded="' + (open ? "true" : "false") + '" title="\u5F53\u524D\u4F1A\u8BDD\u64CD\u4F5C"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="1"/><circle cx="12" cy="5" r="1"/><circle cx="12" cy="19" r="1"/></svg></button><div id="topbar-more-menu" class="topbar-more-menu' + (open ? "" : " hidden") + '" role="menu" aria-label="\u5F53\u524D\u4F1A\u8BDD">' + infoItems + divider + actionItems + "</div></div>";
  }
  function loadGitStatus(sessionId, options) {
    if (!sessionId) return Promise.resolve(null);
    var force = options && options.force;
    var now = Date.now();
    if (!force && state.gitStatusSessionId === sessionId && state.gitStatus && now - state.gitStatusLastFetchAt < 1e3) {
      return Promise.resolve(state.gitStatus);
    }
    if (state.gitStatusInflight && state.gitStatusInflight.sessionId === sessionId) {
      return state.gitStatusInflight.promise;
    }
    state.gitStatusLoading = true;
    var promise = fetch("/api/sessions/" + encodeURIComponent(sessionId) + "/git-status", {
      credentials: "same-origin"
    }).then(function(res) {
      return res.ok ? res.json() : { isGit: false };
    }).then(function(data) {
      state.gitStatus = data || { isGit: false };
      state.gitStatusSessionId = sessionId;
      state.gitStatusLastFetchAt = Date.now();
      updateTopbarGitBadge();
      return data;
    }).catch(function() {
      state.gitStatus = { isGit: false };
      state.gitStatusSessionId = sessionId;
      state.gitStatusLastFetchAt = Date.now();
      updateTopbarGitBadge();
      return null;
    }).finally(function() {
      state.gitStatusLoading = false;
      if (state.gitStatusInflight && state.gitStatusInflight.sessionId === sessionId) {
        state.gitStatusInflight = null;
      }
    });
    state.gitStatusInflight = { sessionId, promise };
    return promise;
  }
  configureQuickCommitRuntime({
    onOpen: function() {
      folderPickerController.closeIfOpen();
      closeWorktreeMergeModal();
      closeSessionModal();
      closeSettingsModal();
    },
    onClose: function() {
    },
    onRepositoryChanged: function(sessionId) {
      void loadGitStatus(sessionId, { force: true });
    },
    toast: function(message, tone) {
      showToast(message, tone);
    }
  });
  function openQuickCommitModal() {
    if (!state.selectedId) return;
    var sessionId = state.selectedId;
    var openIntent = function() {
      if (folderPickerController.isOpen() && !folderPickerController.closeIfOpen()) return;
      var worktree = window.__wandReactWorktreeMerge;
      if (worktree?.isOpen() && !worktree.closeIfOpen()) return;
      var newSession = window.__wandReactNewSession;
      if (newSession?.isOpen() && !newSession.closeIfOpen()) return;
      var settings = window.__wandReactSettings;
      if (settings?.isOpen() && !settings.closeIfOpen()) return;
      quickCommitController.open({ sessionId });
    };
    if (!prepareFilePreviewForCompetingOverlay(openIntent)) return;
    openIntent();
  }

  // src/web-ui/browser/session-ui.ts
  function timeAgo(isoString) {
    if (!isoString) return "";
    var now = Date.now();
    var then = new Date(isoString).getTime();
    var diff = Math.max(0, now - then);
    var seconds = Math.floor(diff / 1e3);
    if (seconds < 60) return "\u521A\u521A";
    var minutes = Math.floor(seconds / 60);
    if (minutes < 60) return minutes + "\u5206\u949F\u524D";
    var hours = Math.floor(minutes / 60);
    if (hours < 24) return hours + "\u5C0F\u65F6\u524D";
    var days = Math.floor(hours / 24);
    if (days < 30) return days + "\u5929\u524D";
    return Math.floor(days / 30) + "\u4E2A\u6708\u524D";
  }
  function elapsedTime(isoString) {
    if (!isoString) return "";
    var now = Date.now();
    var then = new Date(isoString).getTime();
    var diff = Math.max(0, now - then);
    var seconds = Math.floor(diff / 1e3);
    var minutes = Math.floor(seconds / 60);
    var hours = Math.floor(minutes / 60);
    if (hours > 0) return hours + "h" + (minutes % 60 > 0 ? minutes % 60 + "m" : "");
    if (minutes > 0) return minutes + "m";
    return seconds + "s";
  }
  function getSessionStatusLabel(session) {
    if (!session) return "";
    if (session.permissionBlocked) return "\u7B49\u5F85\u6388\u6743";
    if (isStructuredSession2(session) && session.structuredState && session.structuredState.inFlight) return "\u601D\u8003\u4E2D";
    var statusMap = {
      "idle": "\u7A7A\u95F2",
      "stopped": "\u5DF2\u505C\u6B62",
      "running": "\u8FD0\u884C\u4E2D",
      "exited": "\u5DF2\u9000\u51FA",
      "failed": "\u5DF2\u5931\u8D25"
    };
    return statusMap[session.status] || session.status;
  }
  function getSessionStatusClass(session) {
    if (!session) return "";
    if (session.permissionBlocked) return "permission-blocked";
    if (isStructuredSession2(session) && session.structuredState && session.structuredState.inFlight) return "running";
    return session.status || "";
  }
  function getSessionActivityDesc(session) {
    if (!session) return "";
    if (session.permissionBlocked) return "\u7B49\u5F85\u4F60\u7684\u6388\u6743";
    if (session.status !== "running") return "";
    if (session.id === state.selectedId && state.currentTask && state.currentTask.title) {
      return state.currentTask.title;
    }
    if (session.currentTaskTitle) return session.currentTaskTitle;
    return "";
  }
  function getSessionLatestUserText(session) {
    var msgs = session && session.messages;
    if (!msgs || msgs.length === 0) return "";
    for (var i = msgs.length - 1; i >= 0; i--) {
      var msg = msgs[i];
      if (!msg || msg.role !== "user") continue;
      var content = msg.content;
      if (typeof content === "string") {
        var t6 = content.trim();
        if (t6) return t6;
        continue;
      }
      if (Array.isArray(content)) {
        for (var j = 0; j < content.length; j++) {
          var block = content[j];
          if (!block || block.type !== "text" || !block.text) continue;
          if (block.__queued) continue;
          var bt = String(block.text).trim();
          if (bt) return bt;
        }
      }
    }
    return "";
  }
  function getLastAssistantSummary(session) {
    var msgs = session && session.messages;
    if (!msgs || msgs.length === 0) return "";
    for (var i = msgs.length - 1; i >= 0; i--) {
      var msg = msgs[i];
      if (msg.role !== "assistant") continue;
      var blocks = msg.content || [];
      for (var j = 0; j < blocks.length; j++) {
        if (blocks[j].type === "text" && blocks[j].text && blocks[j].text.trim()) {
          var text5 = blocks[j].text.trim();
          text5 = text5.replace(/^#+\s+/gm, "").replace(/\*\*/g, "").replace(/`/g, "");
          var firstLine = text5.split("\n")[0].trim();
          return firstLine.slice(0, 100);
        }
      }
    }
    return "";
  }
  function renderSessionItem(session, kind) {
    var activeClass = session.id === state.selectedId ? " active" : "";
    var selectedClass = state.sessionsManageMode && state.selectedSessionIds[session.id] ? " selected" : "";
    var metaStatus = getSessionStatusLabel(session);
    var metaStatusClass = getSessionStatusClass(session);
    var resumeButton = "";
    var checkbox = renderManageCheckbox("sessions", session.id, "\u9009\u62E9\u4F1A\u8BDD " + session.command);
    if ((session.provider === "claude" || session.provider === "codex") && session.claudeSessionId) {
      if (session.status !== "running" && !state.sessionsManageMode && !isStructuredSession2(session)) {
        var resumeTitle = session.provider === "codex" ? "\u6062\u590D Codex \u4F1A\u8BDD" : "\u6062\u590D Claude \u4F1A\u8BDD";
        resumeButton = '<button class="session-action-btn" data-action="resume" data-session-id="' + session.id + '" type="button" aria-label="' + resumeTitle + '" title="' + resumeTitle + '"><svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M1 4v6h6"/><path d="M3.51 15a9 9 0 105.64-11.36L3 10"/></svg></button>';
      }
    }
    var canOpenMerge = !state.sessionsManageMode && session.worktreeEnabled && session.worktree && session.worktree.branch && session.worktree.path;
    var needsCleanup = session.worktreeMergeStatus === "merged" && session.worktreeMergeInfo && session.worktreeMergeInfo.cleanupDone === false;
    var mergeDisabled = session.status === "running" || session.worktreeMergeStatus === "merging";
    var mergeTitle = needsCleanup ? "\u91CD\u8BD5\u6E05\u7406 worktree" : "\u5408\u5E76\u5230\u4E3B\u5206\u652F";
    var mergeButton = canOpenMerge && session.worktreeMergeStatus !== "merged" ? '<button class="session-action-btn merge-btn" data-action="worktree-merge" data-session-id="' + session.id + '" type="button" aria-label="' + escapeHtml(mergeTitle) + '" title="' + escapeHtml(mergeTitle) + '"' + (mergeDisabled ? " disabled" : "") + '><svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M7 7h10"/><path d="M7 12h10"/><path d="M7 17h10"/><path d="M5 7l-2 2 2 2"/><path d="M19 15l2 2-2 2"/></svg></button>' : needsCleanup ? '<button class="session-action-btn merge-btn" data-action="worktree-cleanup" data-session-id="' + session.id + '" type="button" aria-label="\u91CD\u8BD5\u6E05\u7406 worktree" title="\u91CD\u8BD5\u6E05\u7406 worktree"><svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 6h18"/><path d="M8 6V4a2 2 0 012-2h4a2 2 0 012 2v2"/><path d="M19 6l-1 14a2 2 0 01-2 2H8a2 2 0 01-2-2L5 6"/></svg></button>' : "";
    var deleteButton = state.sessionsManageMode ? "" : '<button class="session-action-btn delete-btn" data-action="delete-session" data-session-id="' + session.id + '" type="button" aria-label="\u5220\u9664\u4F1A\u8BDD" title="\u5220\u9664\u6B64\u4F1A\u8BDD"><svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 6h18"/><path d="M8 6V4a2 2 0 012-2h4a2 2 0 012 2v2"/><path d="M19 6l-1 14a2 2 0 01-2 2H8a2 2 0 01-2-2L5 6"/></svg></button>';
    var actionsHtml = '<span class="session-actions">' + resumeButton + mergeButton + deleteButton + "</span>";
    var titleHtml = session.title || session.summary ? '<div class="session-title">' + escapeHtml(session.title || session.summary) + "</div>" : '<div class="session-command">' + escapeHtml(session.resumedFromSessionId ? session.command.replace(/\s+--resume\s+\S+/, "").replace(/\s+resume\s+[0-9a-f-]+/, "") : session.command) + "</div>";
    var activityDesc = getSessionActivityDesc(session);
    var activityHtml = "";
    if (session.status === "running" && activityDesc) {
      activityHtml = '<div class="session-activity">' + escapeHtml(activityDesc) + "</div>";
    }
    var descriptionHtml = session.description ? '<div class="session-description">' + escapeHtml(session.description) + "</div>" : "";
    var timeDisplay = "";
    if (session.status === "running") {
      timeDisplay = '<span class="session-time" title="\u5DF2\u8FD0\u884C ' + escapeHtml(elapsedTime(session.startedAt)) + '">' + escapeHtml(elapsedTime(session.startedAt)) + "</span>";
    } else if (session.endedAt) {
      timeDisplay = '<span class="session-time" title="' + escapeHtml(new Date(session.endedAt).toLocaleString()) + '">' + escapeHtml(timeAgo(session.endedAt)) + "</span>";
    } else if (session.startedAt) {
      timeDisplay = '<span class="session-time" title="' + escapeHtml(new Date(session.startedAt).toLocaleString()) + '">' + escapeHtml(timeAgo(session.startedAt)) + "</span>";
    }
    var badgesHtml = renderWorktreeBadge(session);
    var recoveryHtml = session.autoRecovered ? '<span class="session-recovery-hint">\u81EA\u52A8\u6062\u590D</span>' : "";
    var swipeBgHtml = state.sessionsManageMode ? "" : '<div class="session-swipe-bg" aria-hidden="true"><button class="session-swipe-delete" data-action="swipe-delete-session" data-session-id="' + session.id + '" type="button" tabindex="-1" aria-label="\u5220\u9664\u4F1A\u8BDD"><svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 6h18"/><path d="M8 6V4a2 2 0 012-2h4a2 2 0 012 2v2"/><path d="M19 6l-1 14a2 2 0 01-2 2H8a2 2 0 01-2-2L5 6"/></svg><span>\u5220\u9664</span></button></div>';
    return '<div class="session-item' + activeClass + selectedClass + '" data-session-id="' + session.id + '" role="button" tabindex="0">' + swipeBgHtml + '<div class="session-item-content"><div class="session-item-row">' + checkbox + '<div class="session-main"><div class="session-title-row">' + titleHtml + timeDisplay + "</div>" + descriptionHtml + activityHtml + '<div class="session-meta"><span class="session-status ' + metaStatusClass + '">' + escapeHtml(metaStatus) + "</span>" + badgesHtml + recoveryHtml + "</div></div>" + actionsHtml + "</div></div></div>";
  }
  function getWorktreeMergeStatusLabel(session) {
    if (!session || !session.worktreeMergeStatus) return "";
    var labels = {
      ready: "\u53EF\u5408\u5E76",
      checking: "\u68C0\u67E5\u4E2D",
      merging: "\u5408\u5E76\u4E2D",
      merged: session.worktreeMergeInfo && session.worktreeMergeInfo.cleanupDone === false ? "\u5DF2\u5408\u5E76\u5F85\u6E05\u7406" : "\u5DF2\u5408\u5E76",
      failed: "\u5408\u5E76\u5931\u8D25"
    };
    return labels[session.worktreeMergeStatus] || "";
  }
  function renderWorktreeMergeBadge(session) {
    var label = getWorktreeMergeStatusLabel(session);
    if (!label) return "";
    return '<span class="session-kind-badge worktree-merge ' + escapeHtml(session.worktreeMergeStatus || "") + '">' + escapeHtml(label) + "</span>";
  }
  function renderWorktreeBadge(session) {
    if (!session || !session.worktreeEnabled) return "";
    var titleParts = [];
    if (session.worktree && session.worktree.branch) {
      titleParts.push("Worktree: " + session.worktree.branch);
    }
    if (session.worktree && session.worktree.path) {
      titleParts.push("Path: " + session.worktree.path);
    }
    var title = titleParts.length > 0 ? ' title="' + escapeHtml(titleParts.join("\n")) + '"' : "";
    return '<span class="session-kind-badge worktree"' + title + ">Worktree</span>" + renderWorktreeMergeBadge(session);
  }

  // src/web-ui/browser/sidebar.ts
  var NON_WAND_SESSIONS_EXPANDED_KEY = "wand-non-wand-sessions-expanded";
  var AUTOMATION_SESSIONS_EXPANDED_KEY = "wand-automation-sessions-expanded";
  function getSecondaryGroupStorageKey(group) {
    if (group.classList.contains("automation-session-group")) return AUTOMATION_SESSIONS_EXPANDED_KEY;
    if (group.classList.contains("non-wand-session-group")) return NON_WAND_SESSIONS_EXPANDED_KEY;
    return "";
  }
  document.addEventListener("toggle", function(event) {
    var group = event.target;
    if (!group || !group.classList) return;
    var storageKey = getSecondaryGroupStorageKey(group);
    if (!storageKey) return;
    if (state.sessionsManageMode) {
      if (!group.open) group.open = true;
      return;
    }
    writeStoredBoolean(storageKey, group.open);
  }, true);
  document.addEventListener("click", function(event) {
    if (isBrowserReactShellMounted()) return;
    var target = event.target;
    if (!target || typeof target.closest !== "function") return;
    if (target.closest(".non-wand-session-group.manage-mode > summary, .automation-session-group.manage-mode > summary")) {
      event.preventDefault();
      return;
    }
    var compactTrigger = target.closest("[data-expand-session-group]");
    if (!compactTrigger) return;
    var targetGroup = compactTrigger.dataset.expandSessionGroup;
    var storageKey = targetGroup === "automation" ? AUTOMATION_SESSIONS_EXPANDED_KEY : targetGroup === "non-wand" ? NON_WAND_SESSIONS_EXPANDED_KEY : "";
    if (!storageKey) return;
    writeStoredBoolean(storageKey, true);
    var collapseButton = document.getElementById("sidebar-collapse-btn");
    if (collapseButton) collapseButton.click();
  });
  function isAutomationSession(session) {
    var source = String(session && session.sessionSource || "").toLowerCase();
    return source === "automation" || source === "startup";
  }
  function sortSessionEntries(entries) {
    return entries.sort(function(a, b) {
      return b.t - a.t;
    });
  }
  function getSessionEntryGroups() {
    var wandEntries = [];
    var automationEntries = [];
    var nonWandEntries = [];
    state.sessions.forEach(function(s) {
      var t6 = s.startedAt ? new Date(s.startedAt).getTime() : 0;
      var entry = { kind: "session", ref: s, t: isFinite(t6) ? t6 : 0 };
      (isAutomationSession(s) ? automationEntries : wandEntries).push(entry);
    });
    if (state.claudeHistoryLoaded) {
      getVisibleClaudeHistorySessions().forEach(function(h) {
        var t6 = h.timestamp ? new Date(h.timestamp).getTime() : Number(h.mtimeMs) || 0;
        if (!isFinite(t6)) t6 = Number(h.mtimeMs) || 0;
        nonWandEntries.push({ kind: "history", ref: h, t: t6 });
      });
    }
    if (state.codexHistoryLoaded) {
      getVisibleCodexHistorySessions().forEach(function(h) {
        var t6 = h.timestamp ? new Date(h.timestamp).getTime() : Number(h.mtimeMs) || 0;
        nonWandEntries.push({ kind: "codex", ref: h, t: isFinite(t6) ? t6 : 0 });
      });
    }
    return {
      wand: sortSessionEntries(wandEntries),
      automation: sortSessionEntries(automationEntries),
      nonWand: sortSessionEntries(nonWandEntries)
    };
  }
  function renderSessions() {
    var groups = [];
    groups.push(renderSessionManageBar());
    var entries = getSessionEntryGroups();
    if (entries.wand.length + entries.automation.length + entries.nonWand.length === 0) {
      return renderSessionManageBar() + '<div class="empty-state"><strong>\u8FD8\u6CA1\u6709\u4F1A\u8BDD\u8BB0\u5F55</strong><br>\u70B9\u51FB\u4E0A\u65B9\u300C\u65B0\u5BF9\u8BDD\u300D\u5F00\u59CB\u4F60\u7684\u7B2C\u4E00\u6B21\u5BF9\u8BDD\u3002</div>';
    }
    if (entries.wand.length > 0) groups.push(renderSessionEntries(entries.wand));
    if (entries.automation.length > 0) groups.push(renderAutomationSessionGroup(entries.automation));
    if (entries.nonWand.length > 0) groups.push(renderNonWandSessionGroup(entries.nonWand));
    return groups.join("");
  }
  function isSidebarNarrow() {
    return !!state.sidebarPinned && !!state.sidebarCollapsed;
  }
  function renderCollapsedSessionTiles() {
    var entries = getSessionEntryGroups();
    var tiles = entries.wand.map(function(e, i) {
      var idx = i + 1;
      var s = e.ref;
      var activeCls = s.id === state.selectedId ? " active" : "";
      var title = s.title || s.description || s.summary || s.command || "\u4F1A\u8BDD " + idx;
      return '<button class="sidebar-collapsed-tile' + activeCls + '" type="button" data-collapsed-session-id="' + escapeHtml(s.id) + '" title="' + escapeHtml(title) + '">' + idx + "</button>";
    }).join("");
    var automationCount = entries.automation.length;
    var automationActive = entries.automation.some(function(entry) {
      return entry.ref.id === state.selectedId;
    });
    var automationTile = automationCount > 0 ? '<button class="sidebar-collapsed-tile automation-count-tile' + (automationActive ? " active-group" : "") + '" type="button" data-expand-session-group="automation" title="\u5C55\u5F00\u67E5\u770B ' + automationCount + ' \u4E2A\u81EA\u52A8\u5316\u4F1A\u8BDD" aria-label="\u5C55\u5F00\u67E5\u770B ' + automationCount + ' \u4E2A\u81EA\u52A8\u5316\u4F1A\u8BDD"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M12 2v4"/><path d="M12 18v4"/><path d="M4.93 4.93l2.83 2.83"/><path d="M16.24 16.24l2.83 2.83"/><path d="M2 12h4"/><path d="M18 12h4"/><path d="M4.93 19.07l2.83-2.83"/><path d="M16.24 7.76l2.83-2.83"/><circle cx="12" cy="12" r="3"/></svg><span class="non-wand-count-badge">' + (automationCount > 99 ? "99+" : automationCount) + "</span></button>" : "";
    var nonWandCount = entries.nonWand.length;
    var nonWandTile = nonWandCount > 0 ? '<button class="sidebar-collapsed-tile non-wand-count-tile" type="button" data-expand-session-group="non-wand" title="\u5C55\u5F00\u67E5\u770B ' + nonWandCount + ' \u4E2A\u975E Wand \u4F1A\u8BDD" aria-label="\u5C55\u5F00\u67E5\u770B ' + nonWandCount + ' \u4E2A\u975E Wand \u4F1A\u8BDD"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M3 12a9 9 0 109-9 9.75 9.75 0 00-6.74 2.74L3 8"/><path d="M3 3v5h5"/><path d="M12 7v5l3 2"/></svg><span class="non-wand-count-badge">' + (nonWandCount > 99 ? "99+" : nonWandCount) + "</span></button>" : "";
    var addTile = '<button class="sidebar-collapsed-tile add" type="button" data-collapsed-new-session="1" title="\u65B0\u5EFA\u4F1A\u8BDD" aria-label="\u65B0\u5EFA\u4F1A\u8BDD"><svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><line x1="12" y1="5" x2="12" y2="19"/><line x1="5" y1="12" x2="19" y2="12"/></svg></button>';
    return '<div class="sidebar-collapsed-tiles">' + tiles + automationTile + nonWandTile + addTile + "</div>";
  }
  function renderSessionsListContent() {
    return isSidebarNarrow() ? renderCollapsedSessionTiles() : renderSessions();
  }
  function renderSessionManageBar() {
    if (!state.sessionsManageMode) {
      return '<div class="session-manage-bar"><span class="sidebar-intro">Wand \u4F1A\u8BDD</span><button class="btn btn-ghost btn-xs session-manage-toggle" data-action="toggle-manage-mode" type="button"><svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M9 11l3 3L22 4"/><path d="M21 12v7a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h11"/></svg><span>\u7BA1\u7406</span></button></div>';
    }
    var sessionCount = getSelectedSessionIds().length;
    var historyCount = getSelectedClaudeHistoryIds().length;
    var codexCount = getSelectedCodexHistoryIds().length;
    var totalCount = sessionCount + historyCount + codexCount;
    var hasAny = totalCount > 0;
    var selectable = countSelectableItems();
    var allSelected = selectable > 0 && totalCount >= selectable;
    var selectAllLabel = allSelected ? "\u53D6\u6D88\u5168\u9009" : "\u5168\u9009";
    var selectAllAction = allSelected ? "clear-selection" : "select-all-visible";
    var selectAllDisabled = selectable === 0 ? " disabled" : "";
    var exitBtn = '<button class="session-manage-exit" data-action="toggle-manage-mode" type="button" aria-label="\u9000\u51FA\u7BA1\u7406\u6A21\u5F0F" title="\u9000\u51FA\u7BA1\u7406\u6A21\u5F0F"><svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" aria-hidden="true"><line x1="6" y1="6" x2="18" y2="18"/><line x1="18" y1="6" x2="6" y2="18"/></svg></button>';
    var summary = hasAny ? '<span class="session-manage-count">' + totalCount + '</span><span class="session-manage-summary-label">\u5DF2\u9009\u62E9</span>' : '<span class="session-manage-summary-label muted">\u9009\u62E9\u8981\u7BA1\u7406\u7684\u9879\u76EE</span>';
    return '<div class="session-manage-bar active">' + exitBtn + '<div class="session-manage-summary">' + summary + '</div><div class="session-manage-actions"><button class="btn btn-ghost btn-xs" data-action="' + selectAllAction + '" type="button"' + selectAllDisabled + ">" + selectAllLabel + '</button><button class="btn btn-danger btn-xs" data-action="delete-selected" type="button"' + (hasAny ? "" : " disabled") + ">\u5220\u9664" + (hasAny ? " " + totalCount : "") + "</button></div></div>";
  }
  function renderSessionEntries(entries, extraClass) {
    var html = '<section class="session-group' + (extraClass ? " " + extraClass : "") + '">';
    html += entries.map(function(e) {
      return e.kind === "session" ? renderSessionItem(e.ref, "sessions") : renderClaudeHistoryItem(e.ref, e.kind);
    }).join("");
    html += "</section>";
    return html;
  }
  function renderNonWandSessionGroup(entries) {
    var expanded = state.sessionsManageMode || readStoredBoolean(NON_WAND_SESSIONS_EXPANDED_KEY, false);
    return '<details class="non-wand-session-group' + (state.sessionsManageMode ? " manage-mode" : "") + '"' + (expanded ? " open" : "") + '><summary class="non-wand-session-summary" title="Claude \u4E0E Codex \u7684\u672C\u673A\u539F\u751F\u4F1A\u8BDD\uFF0C\u4E0D\u53C2\u4E0E Wand \u4F1A\u8BDD\u6392\u5E8F"><span class="non-wand-session-icon" aria-hidden="true"><svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9" stroke-linecap="round" stroke-linejoin="round"><path d="M3 12a9 9 0 109-9 9.75 9.75 0 00-6.74 2.74L3 8"/><path d="M3 3v5h5"/><path d="M12 7v5l3 2"/></svg></span><span class="non-wand-session-title">\u975E Wand \u4F1A\u8BDD</span><span class="non-wand-session-count" aria-label="' + entries.length + ' \u4E2A\u4F1A\u8BDD">' + entries.length + '</span><svg class="non-wand-session-chevron" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><polyline points="6 9 12 15 18 9"/></svg></summary>' + renderSessionEntries(entries, "non-wand-session-list") + "</details>";
  }
  function renderAutomationSessionGroup(entries) {
    var expanded = state.sessionsManageMode || readStoredBoolean(AUTOMATION_SESSIONS_EXPANDED_KEY, false);
    return '<details class="automation-session-group' + (state.sessionsManageMode ? " manage-mode" : "") + '"' + (expanded ? " open" : "") + '><summary class="automation-session-summary" title="\u7531\u81EA\u52A8\u5316\u6216\u542F\u52A8\u4EFB\u52A1\u521B\u5EFA\uFF0C\u4E0D\u53C2\u4E0E\u666E\u901A Wand \u4F1A\u8BDD\u6392\u5E8F"><span class="automation-session-icon" aria-hidden="true"><svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.9" stroke-linecap="round" stroke-linejoin="round"><path d="M12 2v4"/><path d="M12 18v4"/><path d="M4.93 4.93l2.83 2.83"/><path d="M16.24 16.24l2.83 2.83"/><path d="M2 12h4"/><path d="M18 12h4"/><path d="M4.93 19.07l2.83-2.83"/><path d="M16.24 7.76l2.83-2.83"/><circle cx="12" cy="12" r="3"/></svg></span><span class="automation-session-title">\u81EA\u52A8\u5316</span><span class="automation-session-count" aria-label="' + entries.length + ' \u4E2A\u4F1A\u8BDD">' + entries.length + '</span><svg class="automation-session-chevron" width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><polyline points="6 9 12 15 18 9"/></svg></summary>' + renderSessionEntries(entries, "automation-session-list") + "</details>";
  }
  function getVisibleClaudeHistorySessions() {
    var managedIds = /* @__PURE__ */ new Set();
    state.sessions.forEach(function(s) {
      if (s.claudeSessionId) managedIds.add(s.claudeSessionId);
    });
    return state.claudeHistory.filter(function(s) {
      return s.hasConversation && !s.managedByWand && !managedIds.has(s.claudeSessionId);
    });
  }
  function getSelectedSessionIds() {
    return Object.keys(state.selectedSessionIds).filter(function(id) {
      return !!state.selectedSessionIds[id];
    });
  }
  function getSelectedClaudeHistoryIds() {
    return Object.keys(state.selectedClaudeHistoryIds).filter(function(id) {
      return !!state.selectedClaudeHistoryIds[id];
    });
  }
  function getSelectedCodexHistoryIds() {
    return Object.keys(state.selectedCodexHistoryIds).filter(function(id) {
      return !!state.selectedCodexHistoryIds[id];
    });
  }
  function clearManageSelections() {
    state.selectedSessionIds = {};
    state.selectedClaudeHistoryIds = {};
    state.selectedCodexHistoryIds = {};
  }
  function toggleManageMode(force) {
    state.sessionsManageMode = typeof force === "boolean" ? force : !state.sessionsManageMode;
    if (state.sessionsManageMode && (!state.claudeHistoryLoaded || !state.codexHistoryLoaded)) {
      ensureClaudeHistoryLoaded().then(updateSessionsList);
    }
    if (!state.sessionsManageMode) {
      clearManageSelections();
      closeSwipedItem();
    }
    updateSessionsList();
  }
  function getSelectableSessions() {
    return state.sessions.slice();
  }
  function countSelectableItems() {
    return getSelectableSessions().length + getVisibleClaudeHistorySessions().length + getVisibleCodexHistorySessions().length;
  }
  function selectAllVisibleItems() {
    if (!state.claudeHistoryLoaded || !state.codexHistoryLoaded) {
      ensureClaudeHistoryLoaded().then(selectAllVisibleItems);
      return;
    }
    var nextSessionIds = {};
    getSelectableSessions().forEach(function(session) {
      nextSessionIds[session.id] = true;
    });
    var nextHistoryIds = {};
    getVisibleClaudeHistorySessions().forEach(function(session) {
      nextHistoryIds[session.claudeSessionId] = true;
    });
    var nextCodexIds = {};
    getVisibleCodexHistorySessions().forEach(function(session) {
      nextCodexIds[session.claudeSessionId] = true;
    });
    state.selectedSessionIds = nextSessionIds;
    state.selectedClaudeHistoryIds = nextHistoryIds;
    state.selectedCodexHistoryIds = nextCodexIds;
    updateSessionsList();
  }
  function clearSelections() {
    clearManageSelections();
    updateSessionsList();
  }
  function toggleManagedItemSelection(kind, id) {
    if (!state.sessionsManageMode || !id) return;
    var target = kind === "history" ? state.selectedClaudeHistoryIds : kind === "codex" ? state.selectedCodexHistoryIds : state.selectedSessionIds;
    if (target[id]) {
      delete target[id];
    } else {
      target[id] = true;
    }
    updateSessionsList();
  }
  function renderManageCheckbox(kind, id, label) {
    if (!state.sessionsManageMode) return "";
    var selected = kind === "history" ? !!state.selectedClaudeHistoryIds[id] : kind === "codex" ? !!state.selectedCodexHistoryIds[id] : !!state.selectedSessionIds[id];
    return '<label class="session-manage-check"><input type="checkbox" data-action="toggle-selection" data-kind="' + escapeHtml(kind) + '" data-id="' + escapeHtml(id) + '"' + (selected ? " checked" : "") + ' aria-label="' + escapeHtml(label) + '"><span></span></label>';
  }
  function confirmDelete(message, options) {
    return wandConfirm2(message, Object.assign({ type: "danger", danger: true, okLabel: "\u5220\u9664" }, options || {}));
  }
  function batchDeleteSelected() {
    var sessionIds = getSelectedSessionIds();
    var historyIds = getSelectedClaudeHistoryIds();
    var codexIds = getSelectedCodexHistoryIds();
    var managedProviderIds = state.sessions.filter(function(session) {
      return sessionIds.indexOf(session.id) !== -1;
    }).map(function(session) {
      return session.claudeSessionId;
    }).filter(Boolean);
    var total = sessionIds.length + historyIds.length + codexIds.length;
    if (!total) return;
    confirmDelete("\u786E\u8BA4\u5220\u9664\u6240\u9009 " + total + " \u9879\u5417\uFF1F\u6B64\u64CD\u4F5C\u65E0\u6CD5\u64A4\u9500\u3002", {
      title: "\u5220\u9664\u6240\u9009 " + total + " \u9879"
    }).then(function(ok) {
      if (!ok) return;
      var requests = [];
      if (sessionIds.length > 0) {
        requests.push(fetch("/api/sessions/batch-delete", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          credentials: "same-origin",
          body: JSON.stringify({ sessionIds })
        }).then(function(res) {
          return res.json();
        }));
      }
      if (historyIds.length > 0) {
        requests.push(fetch("/api/claude-history/batch-delete", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          credentials: "same-origin",
          body: JSON.stringify({ claudeSessionIds: historyIds })
        }).then(function(res) {
          return res.json();
        }));
      }
      if (codexIds.length > 0) {
        requests.push(fetch("/api/codex-history/batch-delete", {
          method: "POST",
          headers: { "Content-Type": "application/json" },
          credentials: "same-origin",
          body: JSON.stringify({ claudeSessionIds: codexIds })
        }).then(function(res) {
          return res.json();
        }));
      }
      Promise.all(requests).then(function() {
        if (sessionIds.indexOf(state.selectedId) !== -1) {
          state.selectedId = null;
          persistSelectedId();
        }
        state.claudeHistory = state.claudeHistory.filter(function(session) {
          return historyIds.indexOf(session.claudeSessionId) === -1 && managedProviderIds.indexOf(session.claudeSessionId) === -1;
        });
        state.codexHistory = state.codexHistory.filter(function(session) {
          return codexIds.indexOf(session.claudeSessionId) === -1 && managedProviderIds.indexOf(session.claudeSessionId) === -1;
        });
        clearManageSelections();
        return refreshAll();
      }).catch(function() {
        var errorEl = document.getElementById("action-error");
        showError(errorEl, "\u65E0\u6CD5\u6279\u91CF\u5220\u9664\u6240\u9009\u9879\u76EE\u3002");
      });
    });
  }
  function clearAllClaudeHistory() {
    var cutoff = Date.now() - 24 * 60 * 60 * 1e3;
    var visibleHistory2 = getVisibleClaudeHistorySessions().filter(function(s) {
      return !s.timestamp || new Date(s.timestamp).getTime() <= cutoff;
    });
    if (!visibleHistory2.length) return;
    return confirmDelete("\u786E\u8BA4\u6E05\u7A7A\u5F53\u524D\u663E\u793A\u7684 " + visibleHistory2.length + " \u6761 Claude \u5386\u53F2\u5417\uFF1F", {
      title: "\u6E05\u7A7A Claude \u5386\u53F2",
      okLabel: "\u6E05\u7A7A"
    }).then(function(ok) {
      if (!ok) return;
      var deleteIds = visibleHistory2.map(function(session) {
        return session.claudeSessionId;
      });
      return fetch("/api/claude-history/batch-delete", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        credentials: "same-origin",
        body: JSON.stringify({ claudeSessionIds: deleteIds })
      }).then(function(res) {
        return res.json();
      }).then(function(data) {
        if (data && data.error) {
          throw new Error(data.error);
        }
        state.claudeHistory = state.claudeHistory.filter(function(s) {
          return deleteIds.indexOf(s.claudeSessionId) === -1;
        });
        clearManageSelections();
        updateSessionsList();
      }).catch(function() {
        var errorEl = document.getElementById("action-error");
        showError(errorEl, "\u65E0\u6CD5\u6E05\u7A7A\u5386\u53F2\u4F1A\u8BDD\u3002");
      });
    });
  }
  function renderClaudeHistoryItem(session, kind) {
    var isCodex = kind === "codex";
    var rAct = isCodex ? "resume-codex-history" : "resume-history";
    var dAct = isCodex ? "delete-codex-history" : "delete-history";
    var selMap = isCodex ? state.selectedCodexHistoryIds : state.selectedClaudeHistoryIds;
    var shortId = session.claudeSessionId.slice(0, 8);
    var preview = session.firstUserMessage || "(\u7A7A\u4F1A\u8BDD)";
    var timeStr = formatHistoryTime(session.timestamp);
    var checkbox = renderManageCheckbox(kind, session.claudeSessionId, "\u9009\u62E9\u4F1A\u8BDD " + preview);
    var deleteButton = state.sessionsManageMode ? "" : '<button class="session-action-btn delete-btn" data-action="' + dAct + '" data-claude-session-id="' + session.claudeSessionId + '" type="button" aria-label="\u5220\u9664\u4F1A\u8BDD" title="\u5220\u9664\u4F1A\u8BDD"><svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 6h18"/><path d="M8 6V4a2 2 0 012-2h4a2 2 0 012 2v2"/><path d="M19 6l-1 14a2 2 0 01-2 2H8a2 2 0 01-2-2L5 6"/></svg></button>';
    var resumeButton = state.sessionsManageMode ? "" : '<button class="session-action-btn" data-action="' + rAct + '" data-claude-session-id="' + session.claudeSessionId + '" data-cwd="' + escapeHtml(session.cwd) + '" type="button" aria-label="\u6062\u590D\u4F1A\u8BDD" title="' + (isCodex ? "\u6062\u590D\u6B64 Codex \u4F1A\u8BDD" : "\u6062\u590D\u6B64 Claude \u4F1A\u8BDD") + '"><svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M1 4v6h6"/><path d="M3.51 15a9 9 0 105.64-11.36L3 10"/></svg></button>';
    return '<div class="session-item non-wand-session' + (state.sessionsManageMode && selMap[session.claudeSessionId] ? " selected" : "") + '" data-claude-history-id="' + session.claudeSessionId + '" data-provider="' + (isCodex ? "codex" : "claude") + '" data-cwd="' + escapeHtml(session.cwd) + '" role="button" tabindex="0"><div class="session-item-content"><div class="session-item-row">' + checkbox + '<div class="session-main"><div class="session-command claude-history-preview">' + escapeHtml(preview) + '</div><div class="session-meta"><span class="session-id" title="' + escapeHtml(session.claudeSessionId) + '">' + escapeHtml(shortId) + "</span><span>" + escapeHtml(timeStr) + '</span></div></div><span class="session-actions">' + resumeButton + deleteButton + "</span></div></div></div>";
  }
  function formatHistoryTime(isoStr) {
    if (!isoStr) return "";
    try {
      var d = new Date(isoStr);
      var now = /* @__PURE__ */ new Date();
      var diffMs = now - d;
      var diffMin = Math.floor(diffMs / 6e4);
      if (diffMin < 1) return "\u521A\u521A";
      if (diffMin < 60) return diffMin + " \u5206\u949F\u524D";
      var diffHr = Math.floor(diffMin / 60);
      if (diffHr < 24) return diffHr + " \u5C0F\u65F6\u524D";
      var diffDay = Math.floor(diffHr / 24);
      if (diffDay < 30) return diffDay + " \u5929\u524D";
      return d.toLocaleDateString();
    } catch (e) {
      return "";
    }
  }
  function loadClaudeHistory() {
    return fetch("/api/claude-history", { credentials: "same-origin" }).then(function(res) {
      if (!res.ok) return [];
      return res.json();
    }).then(function(sessions) {
      state.claudeHistory = sessions || [];
      state.claudeHistoryLoaded = true;
      updateSessionsList();
    }).catch(function() {
      state.claudeHistoryLoaded = true;
      state.claudeHistory = [];
      updateSessionsList();
    });
  }
  var _claudeHistoryLoadingPromise = null;
  function ensureClaudeHistoryLoaded() {
    var codexPromise = ensureCodexHistoryLoaded();
    if (!state.claudeHistoryLoaded && !_claudeHistoryLoadingPromise) {
      _claudeHistoryLoadingPromise = loadClaudeHistory().then(function() {
        _claudeHistoryLoadingPromise = null;
      }, function() {
        _claudeHistoryLoadingPromise = null;
      });
    }
    var claudePromise = _claudeHistoryLoadingPromise || Promise.resolve();
    return Promise.all([claudePromise, codexPromise]).then(function() {
    });
  }
  function loadCodexHistory() {
    return fetch("/api/codex-history", { credentials: "same-origin" }).then(function(res) {
      if (!res.ok) return [];
      return res.json();
    }).then(function(sessions) {
      state.codexHistory = sessions || [];
      state.codexHistoryLoaded = true;
      updateSessionsList();
    }).catch(function() {
      state.codexHistoryLoaded = true;
      state.codexHistory = [];
      updateSessionsList();
    });
  }
  var _codexHistoryLoadingPromise = null;
  function ensureCodexHistoryLoaded() {
    if (state.codexHistoryLoaded) return Promise.resolve();
    if (_codexHistoryLoadingPromise) return _codexHistoryLoadingPromise;
    _codexHistoryLoadingPromise = loadCodexHistory().then(function() {
      _codexHistoryLoadingPromise = null;
    }, function() {
      _codexHistoryLoadingPromise = null;
    });
    return _codexHistoryLoadingPromise;
  }
  function getVisibleCodexHistorySessions() {
    var managedIds = /* @__PURE__ */ new Set();
    state.sessions.forEach(function(s) {
      if (s.claudeSessionId) managedIds.add(s.claudeSessionId);
    });
    return state.codexHistory.filter(function(s) {
      return s.hasConversation && !s.managedByWand && !managedIds.has(s.claudeSessionId);
    });
  }

  // src/web-ui/browser/websocket.ts
  function startPolling() {
    stopPolling();
    if (initWebSocket()) {
      return;
    }
    state.pollTimer = setInterval(refreshAll, 1600);
  }
  setInterval(function() {
    var timeEls = document.querySelectorAll(".session-time");
    if (timeEls.length > 0) scheduleSessionListUpdate();
  }, 3e4);
  function cancelWsReconnect() {
    if (state.wsReconnectTimer) {
      clearTimeout(state.wsReconnectTimer);
      state.wsReconnectTimer = null;
    }
  }
  var WS_HEARTBEAT_CHECK_MS = 1e4;
  var WS_HEARTBEAT_STALE_MS = 4e4;
  function startWsHeartbeatCheck() {
    stopWsHeartbeatCheck();
    state.wsHeartbeatCheckTimer = setInterval(evaluateWsHeartbeatStale, WS_HEARTBEAT_CHECK_MS);
  }
  function stopWsHeartbeatCheck() {
    if (state.wsHeartbeatCheckTimer) {
      clearInterval(state.wsHeartbeatCheckTimer);
      state.wsHeartbeatCheckTimer = null;
    }
  }
  function evaluateWsHeartbeatStale() {
    if (!state.ws || state.ws.readyState !== WebSocket.OPEN) return;
    if (!state.lastWsMessageAt) return;
    var idle = Date.now() - state.lastWsMessageAt;
    if (idle > WS_HEARTBEAT_STALE_MS) {
      forceReconnectWebSocket("heartbeat-stale-" + Math.round(idle / 1e3) + "s");
    }
  }
  function forceReconnectWebSocket(reason) {
    cancelWsReconnect();
    if (state.ws) {
      var stale = state.ws;
      try {
        stale.onclose = null;
      } catch (e) {
      }
      try {
        stale.onerror = null;
      } catch (e) {
      }
      try {
        stale.onmessage = null;
      } catch (e) {
      }
      try {
        stale.close();
      } catch (e) {
      }
      state.ws = null;
    }
    state.wsConnected = false;
    state.wsReconnectAttempts = 0;
    initWebSocket(reason);
  }
  function scheduleWsReconnect() {
    if (state.wsReconnectTimer) return;
    if (document.hidden) return;
    var attempt = state.wsReconnectAttempts || 0;
    var delays = [500, 1e3, 2e3, 4e3, 8e3];
    var delay = delays[attempt < delays.length ? attempt : delays.length - 1];
    state.wsReconnectAttempts = attempt + 1;
    state.wsReconnectTimer = setTimeout(function() {
      state.wsReconnectTimer = null;
      if (state.config && !state.ws && !document.hidden) {
        initWebSocket("backoff");
      }
    }, delay);
  }
  function initWebSocket(reason) {
    if (!window.WebSocket) return false;
    if (state.ws) {
      try {
        state.ws.close();
      } catch (e) {
      }
      state.ws = null;
    }
    var protocol = window.location.protocol === "https:" ? "wss:" : "ws:";
    var wsUrl = protocol + "//" + window.location.host + "/ws";
    try {
      var ws = new WebSocket(wsUrl);
      ws.onopen = function() {
        state.ws = ws;
        state.wsConnected = true;
        state.lastWsMessageAt = Date.now();
        state.wsReconnectAttempts = 0;
        cancelWsReconnect();
        state.lastSeqBySession = {};
        startWsHeartbeatCheck();
        subscribeToSession(state.selectedId);
        flushPendingMessages();
        ensureTerminalFitWithRetry("ws-reconnect");
      };
      ws.onmessage = function(event) {
        state.lastWsMessageAt = Date.now();
        try {
          var msg = JSON.parse(event.data);
          if (msg && msg.type === "ping") {
            if (state.ws && state.ws.readyState === WebSocket.OPEN) {
              try {
                state.ws.send(JSON.stringify({ type: "pong", t: msg.t }));
              } catch (sendErr) {
              }
            }
            return;
          }
          if (msg && msg.type === "resync_required" && msg.sessionId) {
            if (state.ws && state.ws.readyState === WebSocket.OPEN) {
              try {
                state.ws.send(JSON.stringify({ type: "resync", sessionId: msg.sessionId }));
              } catch (sendErr) {
              }
            }
            if (!state.lastSeqBySession) state.lastSeqBySession = {};
            state.lastSeqBySession[msg.sessionId] = 0;
            return;
          }
          if (msg && (msg.type === "init" || msg.type === "output") && msg.sessionId && typeof msg.seq === "number") {
            if (!state.lastSeqBySession) state.lastSeqBySession = {};
            var prevSeq = state.lastSeqBySession[msg.sessionId] || 0;
            if (msg.type === "init") {
              state.lastSeqBySession[msg.sessionId] = msg.seq;
            } else if (msg.seq === prevSeq + 1) {
              state.lastSeqBySession[msg.sessionId] = msg.seq;
            } else if (msg.seq > prevSeq + 1 && prevSeq > 0) {
              if (state.ws && state.ws.readyState === WebSocket.OPEN) {
                try {
                  state.ws.send(JSON.stringify({ type: "resync", sessionId: msg.sessionId }));
                } catch (sendErr) {
                }
              }
              state.lastSeqBySession[msg.sessionId] = 0;
              return;
            } else {
              if (msg.seq < prevSeq) return;
              state.lastSeqBySession[msg.sessionId] = msg.seq;
            }
          }
          handleWebSocketMessage(msg);
        } catch (e) {
        }
      };
      ws.onclose = function() {
        state.ws = null;
        state.wsConnected = false;
        stopWsHeartbeatCheck();
        scheduleWsReconnect();
      };
      ws.onerror = function() {
        ws.close();
      };
      return true;
    } catch (e) {
      scheduleWsReconnect();
      return false;
    }
  }
  function handleWebSocketMessage(msg) {
    switch (msg.type) {
      case "output":
        if (msg.data && msg.sessionId && Object.prototype.hasOwnProperty.call(msg.data, "isResponding")) {
          if (!state._lastIsResponding) state._lastIsResponding = {};
          state._lastIsResponding[msg.sessionId] = !!msg.data.isResponding;
        }
        if (msg.data && msg.sessionId) {
          var isIncremental = !!msg.data.incremental;
          var snapshot7 = { id: msg.sessionId };
          var topicMetadataChanged = false;
          if (!isIncremental && msg.data.output !== void 0) {
            snapshot7.output = msg.data.output;
          }
          if (Object.prototype.hasOwnProperty.call(msg.data, "permissionBlocked")) {
            snapshot7.permissionBlocked = !!msg.data.permissionBlocked;
          }
          if (Object.prototype.hasOwnProperty.call(msg.data, "queuedMessages")) {
            snapshot7.queuedMessages = msg.data.queuedMessages || [];
            state.queueEpoch++;
          }
          if (msg.data.structuredState) {
            snapshot7.structuredState = msg.data.structuredState;
          }
          if (msg.data.sessionKind) {
            snapshot7.sessionKind = msg.data.sessionKind;
          }
          if (Object.prototype.hasOwnProperty.call(msg.data, "title")) {
            snapshot7.title = msg.data.title;
            topicMetadataChanged = true;
          }
          if (Object.prototype.hasOwnProperty.call(msg.data, "description")) {
            snapshot7.description = msg.data.description;
            topicMetadataChanged = true;
          }
          if (Object.prototype.hasOwnProperty.call(msg.data, "summary")) {
            snapshot7.summary = msg.data.summary;
          }
          if (msg.data.messages) {
            snapshot7.messages = msg.data.messages;
            if (typeof msg.data.messageOffset === "number") snapshot7.messageOffset = msg.data.messageOffset;
            if (typeof msg.data.messageTotal === "number") snapshot7.messageTotal = msg.data.messageTotal;
          } else if (isIncremental && msg.data.lastMessage) {
            var existingSession = state.sessions.find(function(s) {
              return s.id === msg.sessionId;
            });
            if (existingSession) {
              var msgs = Array.isArray(existingSession.messages) ? existingSession.messages.slice() : [];
              var expectedCount = msg.data.messageCount || 0;
              var baseOffset = typeof existingSession.messageOffset === "number" ? existingSession.messageOffset : 0;
              var localLast = msgs.length > 0 ? msgs[msgs.length - 1] : null;
              var incoming = msg.data.lastMessage;
              if (localLast && incoming.role && localLast.role === incoming.role) {
                msgs[msgs.length - 1] = mergeAssistantTurn(localLast, incoming);
              } else if (baseOffset + msgs.length < expectedCount) {
                msgs.push(incoming);
              }
              snapshot7.messages = msgs;
              if (expectedCount > 0) snapshot7.messageTotal = expectedCount;
            }
          }
          var isChunkOnly = isIncremental && msg.data.chunk && !msg.data.lastMessage && !snapshot7.messages && snapshot7.output === void 0 && !msg.data.structuredState && !msg.data.sessionKind;
          if (isChunkOnly) {
            if (msg.data.permissionBlocked !== void 0) {
              var existingPB = state.sessions.find(function(s) {
                return s.id === msg.sessionId;
              });
              if (existingPB && !!existingPB.permissionBlocked !== !!msg.data.permissionBlocked) {
                updateSessionSnapshot(snapshot7);
                if (msg.sessionId === state.selectedId) updateTaskDisplay();
              }
            }
          } else if (snapshot7.output !== void 0 || snapshot7.messages || isIncremental || msg.data.permissionBlocked !== void 0 || snapshot7.title || snapshot7.description) {
            updateSessionSnapshot(snapshot7);
            if (topicMetadataChanged) scheduleSessionListUpdate();
            if (msg.sessionId === state.selectedId) {
              var updatedSession = state.sessions.find(function(s) {
                return s.id === msg.sessionId;
              }) || snapshot7;
              state.currentMessages = buildMessagesForRender(updatedSession, getPreferredMessages(updatedSession, updatedSession.output, false));
              updateTaskDisplay();
              if (updatedSession.sessionKind === "structured" || msg.data.sessionKind === "structured") {
                renderChat2();
              } else {
                scheduleChatRender();
              }
            }
          }
        }
        if (msg.sessionId === state.selectedId && state.terminal && msg.data) {
          if (msg.data.chunk && isCurrentTerminalSession(msg.sessionId)) {
            state.lastChunkAt = Date.now();
            state.terminalLiveStreamSessions[msg.sessionId] = true;
            wandTerminalWrite(state.terminal, msg.data.chunk);
            state.terminalSessionId = msg.sessionId;
            if (msg.data.output) {
              state.terminalOutput = clampClientTerminalOutput(normalizeTerminalOutput(msg.data.output));
              state.terminalOutputMarker = 0;
            } else {
              state.terminalOutput = clampClientTerminalOutput((state.terminalOutput || "") + normalizeTerminalOutput(msg.data.chunk));
            }
            maybeScrollTerminalToBottom("output");
            updateTerminalJumpToBottomButton();
          } else if (!msg.data.incremental && Object.prototype.hasOwnProperty.call(msg.data, "output")) {
            syncTerminalBuffer(msg.sessionId, msg.data.output || "", { mode: "append" });
          }
        }
        break;
      case "started":
        loadSessions();
        break;
      case "ended": {
        var endedStatus = msg.data && msg.data.status ? msg.data.status : "exited";
        var endedPermBlocked = msg.data && Object.prototype.hasOwnProperty.call(msg.data, "permissionBlocked") ? !!msg.data.permissionBlocked : false;
        var endedSnapshot = { id: msg.sessionId, status: endedStatus, permissionBlocked: endedPermBlocked };
        if (msg.data && msg.data.messages) {
          endedSnapshot.messages = msg.data.messages;
          if (typeof msg.data.messageOffset === "number") endedSnapshot.messageOffset = msg.data.messageOffset;
          if (typeof msg.data.messageTotal === "number") endedSnapshot.messageTotal = msg.data.messageTotal;
        }
        if (msg.data && msg.data.structuredState) {
          endedSnapshot.structuredState = msg.data.structuredState;
        }
        if (msg.data && msg.data.queuedMessages) {
          endedSnapshot.queuedMessages = msg.data.queuedMessages;
        }
        updateSessionSnapshot(endedSnapshot);
        if (msg.sessionId === state.selectedId) {
          updateInputHint("Enter \u53D1\u9001 \xB7 Shift+Enter \u6362\u884C");
          scheduleChatRender(true);
        }
        var endedSession = state.sessions.find(function(s) {
          return s.id === msg.sessionId;
        });
        var endedExitCode = msg.data && msg.data.exitCode;
        var endedIsError = endedExitCode !== null && endedExitCode !== void 0 && endedExitCode !== 0;
        var endedTaskSummary = endedSession ? endedSession.summary || "" : "";
        var endedLastReply = endedSession ? getLastAssistantSummary(endedSession) : "";
        var endedNotifTitle = endedIsError ? "\u4EFB\u52A1\u5F02\u5E38\u7ED3\u675F" : "\u4EFB\u52A1\u5DF2\u5B8C\u6210";
        var endedNotifBody = "";
        if (endedTaskSummary) {
          endedNotifBody = endedTaskSummary;
          if (endedLastReply && !endedIsError) {
            endedNotifBody += "\n" + endedLastReply;
          }
        } else {
          endedNotifBody = endedSession ? endedSession.command || msg.sessionId : msg.sessionId;
        }
        _vibrate(endedIsError ? "error" : "success");
        notifyTaskEnded(msg.sessionId, endedNotifTitle, endedNotifBody);
        clearSessionProgressNative(msg.sessionId);
        _syncWakeLock();
        if (msg.sessionId !== state.selectedId || document.hidden) {
          showNotificationBubble({
            title: endedNotifTitle,
            body: endedNotifBody,
            type: endedIsError ? "warning" : "success",
            icon: endedIsError ? "!" : "\u2713",
            duration: 6e3,
            actionLabel: "\u67E5\u770B",
            action: function() {
              selectSession(msg.sessionId);
            }
          });
        }
        state.messageQueue = [];
        state.pendingMessages = [];
        var endedSessionObj = state.sessions.find(function(s) {
          return s.id === msg.sessionId;
        });
        var selectedSessionObj = msg.sessionId === state.selectedId ? state.sessions.find(function(s) {
          return s.id === state.selectedId;
        }) : null;
        var isStructuredEnded = !!(endedSessionObj && endedSessionObj.sessionKind === "structured" || selectedSessionObj && selectedSessionObj.sessionKind === "structured");
        if (isStructuredEnded && msg.sessionId === state.selectedId) {
          flushStructuredInputQueue();
          clearStructuredQueuePersistence(msg.sessionId);
        } else if (!isStructuredEnded) {
          state.structuredInputQueue = [];
          clearStructuredQueuePersistence(state.selectedId);
          updateStructuredQueueCounter();
        }
        if (msg.sessionId === state.selectedId) {
          if (!isStructuredEnded) {
            setTerminalInteractive(false);
          }
          state.currentTask = null;
          updateTaskDisplay();
        }
        if (msg.sessionId === state.selectedId) {
          updateShellChrome();
        }
        loadSessions().then(function() {
          flushCrossSessionQueue();
        });
        if (msg.sessionId === state.selectedId) {
          if (!isStructuredEnded) {
            loadOutput(msg.sessionId);
          }
        }
        break;
      }
      case "init":
        if (msg.sessionId === state.selectedId && msg.data) {
          if (state.chatRenderTimer) {
            clearTimeout(state.chatRenderTimer);
            state.chatRenderTimer = null;
          }
          updateSessionSnapshot(msg.data);
          var initSession = state.sessions.find(function(s) {
            return s.id === msg.sessionId;
          });
          state.currentMessages = buildMessagesForRender(initSession || msg.data, getPreferredMessages(initSession || msg.data, msg.data.output, false));
          renderChat2(true);
          updateTaskDisplay();
          updateApprovalStats();
          var initOutput = msg.data.output || "";
          var sameTerminalSession = state.terminalSessionId === msg.sessionId;
          var currTerminalOutput = state.terminalOutput || "";
          var canAppendDelta = sameTerminalSession && currTerminalOutput.length > 0 && initOutput.length >= currTerminalOutput.length && initOutput.startsWith(currTerminalOutput);
          updateTerminalOutput(initOutput, msg.sessionId, canAppendDelta ? "append" : "replace");
          ensureTerminalFitWithRetry("init");
        }
        break;
      case "usage":
        break;
      case "task":
        if (msg.sessionId === state.selectedId) {
          state.currentTask = msg.data || null;
          updateTaskDisplay();
        }
        notifyTaskProgress(msg.sessionId, msg.data || null);
        syncSessionProgressToNative(msg.sessionId);
        scheduleSessionListUpdate();
        break;
      case "status":
        if (msg.sessionId && msg.data) {
          var statusUpdate = { id: msg.sessionId };
          if (Object.prototype.hasOwnProperty.call(msg.data, "status")) {
            statusUpdate.status = msg.data.status;
          }
          if (Object.prototype.hasOwnProperty.call(msg.data, "exitCode")) {
            statusUpdate.exitCode = msg.data.exitCode;
          }
          if (msg.data.structuredState) {
            statusUpdate.structuredState = msg.data.structuredState;
          } else if (Object.prototype.hasOwnProperty.call(msg.data, "status")) {
            var existingSession = state.sessions.find(function(s) {
              return s.id === msg.sessionId;
            });
            if (existingSession && existingSession.sessionKind === "structured") {
              statusUpdate.structuredState = Object.assign({}, existingSession.structuredState || {}, {
                inFlight: msg.data.status === "running"
              });
            }
          }
          if (Object.prototype.hasOwnProperty.call(msg.data, "queuedMessages")) {
            statusUpdate.queuedMessages = msg.data.queuedMessages || [];
            state.queueEpoch++;
          }
          if (Object.prototype.hasOwnProperty.call(msg.data, "permissionBlocked")) {
            statusUpdate.permissionBlocked = !!msg.data.permissionBlocked;
          }
          if (msg.data.permissionRequest) {
            statusUpdate.pendingEscalation = {
              scope: msg.data.permissionRequest.scope,
              target: msg.data.permissionRequest.target,
              reason: msg.data.permissionRequest.prompt
            };
            var permSession = state.sessions.find(function(s) {
              return s.id === msg.sessionId;
            });
            var permTaskName = permSession ? permSession.summary || permSession.command || msg.sessionId : msg.sessionId;
            var permDetail = msg.data.permissionRequest.prompt || "\u9700\u8981\u6743\u9650\u5BA1\u6279";
            var permTarget = msg.data.permissionRequest.target;
            var permBody = permTaskName;
            if (permTarget) {
              permBody += "\n" + permDetail + " \xB7 " + permTarget;
            } else {
              permBody += "\n" + permDetail;
            }
            _vibrate("medium");
            notifyPermissionRequest(msg.sessionId, permBody);
            if (msg.sessionId !== state.selectedId) {
              showNotificationBubble({
                title: "\u9700\u8981\u4F60\u7684\u6388\u6743",
                body: permBody,
                type: "warning",
                icon: "!",
                duration: 0,
                actionLabel: "\u53BB\u5904\u7406",
                action: function() {
                  selectSession(msg.sessionId);
                }
              });
            }
          }
          if (msg.data.permissionBlocked === false) {
            statusUpdate.pendingEscalation = null;
          }
          if (msg.data.approvalStats) {
            statusUpdate.approvalStats = msg.data.approvalStats;
          }
          updateSessionSnapshot(statusUpdate);
          syncSessionProgressToNative(msg.sessionId);
          _syncWakeLock();
          if (msg.sessionId === state.selectedId) {
            updateTaskDisplay();
            if (msg.data.approvalStats) {
              updateApprovalStats();
            }
            if (statusUpdate.structuredState) {
              if (!statusUpdate.structuredState.inFlight) {
                updateInputHint("Enter \u53D1\u9001 \xB7 Shift+Enter \u6362\u884C");
                flushStructuredInputQueue();
              }
              scheduleChatRender();
            }
          }
        }
        break;
      case "notification":
        if (msg.data) {
          if (msg.data.kind === "update") {
            notifyUpdateAvailable(msg.data.current || "-", msg.data.latest || "-");
          } else if (msg.data.kind === "auto-update-start") {
            showAutoUpdateOverlay(
              msg.data.current || "-",
              msg.data.latest || "-",
              msg.data.previousInstanceId || null
            );
          } else if (msg.data.kind === "auto-update-restart") {
            showRestartOverlay(msg.data.previousInstanceId || null, msg.data.latest || null);
          } else if (msg.data.kind === "restart") {
            showRestartOverlay();
          }
        }
        break;
    }
  }
  function updateTaskDisplay() {
    var reactShellActive = isBrowserReactShellMounted();
    var taskEl = document.getElementById("current-task");
    var permissionActionsEl = document.getElementById("permission-actions");
    var permissionLabel = document.getElementById("permission-actions-label");
    if (!taskEl && !reactShellActive) return;
    notifyLegacyUiChange("task:update");
    var selectedSession = state.sessions.find(function(s) {
      return s.id === state.selectedId;
    });
    if (selectedSession && selectedSession.provider === "codex") {
      if (permissionActionsEl) permissionActionsEl.classList.add("hidden");
      if (!reactShellActive && taskEl) taskEl.classList.remove("permission-blocked");
    }
    var pendingEscalation = selectedSession && selectedSession.pendingEscalation ? selectedSession.pendingEscalation : null;
    var isBlocked = selectedSession && selectedSession.provider !== "codex" ? pendingEscalation || selectedSession.permissionBlocked : false;
    if (isBlocked) {
      var isAutoApprove = selectedSession && selectedSession.autoApprovePermissions;
      if (permissionLabel) {
        if (isAutoApprove) {
          permissionLabel.textContent = "\u81EA\u52A8\u6279\u51C6\u4E2D...";
        } else if (pendingEscalation) {
          var reason = pendingEscalation.reason || "\u7B49\u5F85\u6388\u6743";
          var target = pendingEscalation.target ? " \xB7 " + pendingEscalation.target : "";
          permissionLabel.textContent = reason + target;
        } else {
          permissionLabel.textContent = "\u7B49\u5F85\u6388\u6743";
        }
      }
      if (permissionActionsEl) {
        permissionActionsEl.classList.remove("hidden");
        var approveBtn = document.getElementById("approve-permission-btn");
        var denyBtn = document.getElementById("deny-permission-btn");
        if (approveBtn) approveBtn.classList.toggle("hidden", !!isAutoApprove);
        if (denyBtn) denyBtn.classList.toggle("hidden", !!isAutoApprove);
      }
      if (!reactShellActive && taskEl) {
        taskEl.textContent = "";
        taskEl.classList.add("hidden");
        taskEl.classList.remove("permission-blocked");
      }
      return;
    }
    if (!reactShellActive && taskEl) taskEl.classList.remove("permission-blocked");
    if (permissionActionsEl) permissionActionsEl.classList.add("hidden");
    var task = state.currentTask;
    if (!reactShellActive && taskEl && task && task.title) {
      taskEl.textContent = task.title;
      taskEl.classList.remove("hidden");
    } else if (!reactShellActive && taskEl) {
      taskEl.textContent = "";
      taskEl.classList.add("hidden");
    }
  }
  function updateApprovalStats() {
    var container = document.getElementById("approval-stats");
    if (!container) return;
    var selectedSession = state.sessions.find(function(s) {
      return s.id === state.selectedId;
    });
    var stats = selectedSession && selectedSession.approvalStats;
    if (!stats || stats.total === 0) {
      container.className = "approval-stats hidden";
      container.innerHTML = "";
      return;
    }
    container.className = "approval-stats";
    container.innerHTML = '<span class="approval-stats-divider"></span><span class="approval-stats-badge" id="approval-stats-badge" title="\u672C\u6B21\u4F1A\u8BDD\u81EA\u52A8\u6279\u51C6\u7EDF\u8BA1"><svg class="approval-stats-icon" width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg><span class="approval-stats-total">' + stats.total + '</span></span><span class="approval-stats-popup" id="approval-stats-popup"><span class="approval-stats-popup-title">\u81EA\u52A8\u6279\u51C6\u7EDF\u8BA1</span>' + (stats.command > 0 ? '<span class="approval-stats-row"><span class="approval-stats-row-icon">' + iconSvg("terminal", { size: 12, strokeWidth: 1.8 }) + '</span><span class="approval-stats-row-label">\u547D\u4EE4\u6267\u884C</span><span class="approval-stats-row-count">' + stats.command + "</span></span>" : "") + (stats.file > 0 ? '<span class="approval-stats-row"><span class="approval-stats-row-icon">' + iconSvg("edit", { size: 12, strokeWidth: 1.8 }) + '</span><span class="approval-stats-row-label">\u6587\u4EF6\u5199\u5165</span><span class="approval-stats-row-count">' + stats.file + "</span></span>" : "") + (stats.tool > 0 ? '<span class="approval-stats-row"><span class="approval-stats-row-icon">' + iconSvg("wrench", { size: 12, strokeWidth: 1.8 }) + '</span><span class="approval-stats-row-label">\u5176\u4ED6\u5DE5\u5177</span><span class="approval-stats-row-count">' + stats.tool + "</span></span>" : "") + '<span class="approval-stats-row approval-stats-row-total"><span class="approval-stats-row-icon">' + iconSvg("sigma", { size: 12, strokeWidth: 1.8 }) + '</span><span class="approval-stats-row-label">\u5408\u8BA1</span><span class="approval-stats-row-count">' + stats.total + "</span></span></span>";
    var badge = container.querySelector(".approval-stats-badge");
    if (badge) {
      badge.classList.remove("approval-stats-pulse");
      void badge.offsetWidth;
      badge.classList.add("approval-stats-pulse");
    }
  }
  function approvePermission() {
    _vibrate("light");
    if (!state.selectedId) return;
    var approveBtn = document.getElementById("approve-permission-btn");
    var denyBtn = document.getElementById("deny-permission-btn");
    if (approveBtn) approveBtn.disabled = true;
    if (denyBtn) denyBtn.disabled = true;
    fetch("/api/sessions/" + encodeURIComponent(state.selectedId) + "/approve-permission", {
      method: "POST",
      credentials: "same-origin"
    }).then(function(res) {
      return res.json();
    }).then(function(data) {
      if (data && data.error) {
        showToast(data.error, "error");
        return;
      }
      updateSessionSnapshot(data);
      updateTaskDisplay();
    }).catch(function(error) {
      showToast(error && error.message || "\u65E0\u6CD5\u6279\u51C6\u6388\u6743\u3002", "error");
    }).finally(function() {
      if (approveBtn) approveBtn.disabled = false;
      if (denyBtn) denyBtn.disabled = false;
    });
  }
  function denyPermission() {
    _vibrate("light");
    if (!state.selectedId) return;
    var approveBtn = document.getElementById("approve-permission-btn");
    var denyBtn = document.getElementById("deny-permission-btn");
    if (approveBtn) approveBtn.disabled = true;
    if (denyBtn) denyBtn.disabled = true;
    fetch("/api/sessions/" + encodeURIComponent(state.selectedId) + "/deny-permission", {
      method: "POST",
      credentials: "same-origin"
    }).then(function(res) {
      return res.json();
    }).then(function(data) {
      if (data && data.error) {
        showToast(data.error, "error");
        return;
      }
      updateSessionSnapshot(data);
      updateTaskDisplay();
    }).catch(function(error) {
      showToast(error && error.message || "\u65E0\u6CD5\u62D2\u7EDD\u6388\u6743\u3002", "error");
    }).finally(function() {
      if (approveBtn) approveBtn.disabled = false;
      if (denyBtn) denyBtn.disabled = false;
    });
  }
  function toggleAutoApprove() {
    if (!state.selectedId) return;
    var selectedSession = state.sessions.find(function(s) {
      return s.id === state.selectedId;
    });
    if (selectedSession && selectedSession.provider === "codex") {
      showToast("Codex \u4F1A\u8BDD\u56FA\u5B9A\u4EE5 full-access PTY \u542F\u52A8\uFF0C\u4E0D\u652F\u6301\u5207\u6362\u81EA\u52A8\u6279\u51C6\u3002", "info");
      return;
    }
    var toggle = document.getElementById("auto-approve-toggle");
    if (toggle) toggle.style.opacity = "0.5";
    fetch("/api/sessions/" + encodeURIComponent(state.selectedId) + "/toggle-auto-approve", {
      method: "POST",
      credentials: "same-origin"
    }).then(function(res) {
      return res.json();
    }).then(function(data) {
      if (data && data.error) {
        showToast(data.error, "error");
        return;
      }
      updateSessionSnapshot(data);
      updateAutoApproveIndicator();
      var enabled = data.autoApprovePermissions;
      showToast(enabled ? "\u81EA\u52A8\u6279\u51C6\u5DF2\u5F00\u542F" : "\u81EA\u52A8\u6279\u51C6\u5DF2\u5173\u95ED", "info");
    }).catch(function(error) {
      showToast(error && error.message || "\u65E0\u6CD5\u5207\u6362\u81EA\u52A8\u6279\u51C6\u3002", "error");
    }).finally(function() {
      if (toggle) toggle.style.opacity = "";
    });
  }
  function updateAutoApproveIndicator() {
    var toggle = document.getElementById("auto-approve-toggle");
    var selectedSession = state.sessions.find(function(s) {
      return s.id === state.selectedId;
    });
    if (isAutoApproveImpliedByMode(selectedSession)) {
      if (toggle && toggle.parentNode) toggle.parentNode.removeChild(toggle);
      return;
    }
    if (!toggle) return;
    var base = "composer-pill composer-pill-chip auto-approve-indicator";
    var enabled = selectedSession && selectedSession.autoApprovePermissions;
    if (enabled) {
      toggle.className = base + " active";
      toggle.title = "\u81EA\u52A8\u6279\u51C6\u5DF2\u542F\u7528 \u2014 \u70B9\u51FB\u5173\u95ED";
      toggle.setAttribute("aria-pressed", "true");
      toggle.setAttribute("aria-label", "\u81EA\u52A8\u6279\u51C6\u5DF2\u542F\u7528\uFF0C\u70B9\u51FB\u5173\u95ED");
      toggle.innerHTML = iconSvg("shieldCheck", { size: 12, strokeWidth: 1.7, cls: "composer-pill-icon" }) + '<span class="composer-pill-label">\u81EA\u52A8</span>';
    } else {
      toggle.className = base;
      toggle.title = "\u81EA\u52A8\u6279\u51C6\u5DF2\u5173\u95ED \u2014 \u70B9\u51FB\u5F00\u542F";
      toggle.setAttribute("aria-pressed", "false");
      toggle.setAttribute("aria-label", "\u81EA\u52A8\u6279\u51C6\u5DF2\u5173\u95ED\uFF0C\u70B9\u51FB\u5F00\u542F");
      toggle.innerHTML = iconSvg("shield", { size: 12, strokeWidth: 1.7, cls: "composer-pill-icon" }) + '<span class="composer-pill-label">\u624B\u52A8</span>';
    }
  }
  function updateTerminalOutput(output, sessionId, mode) {
    if (!state.terminal) return false;
    return syncTerminalBuffer(sessionId || state.selectedId, output, { mode: mode || "append" });
  }
  function stopPolling() {
    if (state.pollTimer) {
      clearInterval(state.pollTimer);
      state.pollTimer = null;
    }
  }
  function renderChat2(forceFullRender) {
    if (state.renderPending && !forceFullRender) return;
    state.renderPending = true;
    if (forceFullRender) {
      doRenderChat(true);
      state.renderPending = false;
    } else {
      requestAnimationFrame(function() {
        doRenderChat(false);
        state.renderPending = false;
      });
    }
  }
  state.chatRenderTimer = null;
  function scheduleChatRender(immediate) {
    if (state.chatRenderTimer && !immediate) return;
    if (state.chatRenderTimer) clearTimeout(state.chatRenderTimer);
    if (immediate) {
      state.chatRenderTimer = null;
      renderChat2();
      return;
    }
    var selectedForDelay = state.sessions.find(function(s) {
      return s.id === state.selectedId;
    });
    var isActiveStream = selectedForDelay && selectedForDelay.status === "running" && selectedForDelay.sessionKind !== "structured";
    var delay = isActiveStream ? CHAT_RENDER_LIVE_MS : CHAT_RENDER_IDLE_MS;
    state.chatRenderTimer = setTimeout(function() {
      state.chatRenderTimer = null;
      var selectedSession = state.sessions.find(function(s) {
        return s.id === state.selectedId;
      });
      if (selectedSession) {
        state.currentMessages = buildMessagesForRender(selectedSession, getPreferredMessages(selectedSession, selectedSession.output, true));
      }
      renderChat2();
    }, delay);
  }

  // src/web-ui/browser/viewport.ts
  var appViewportBaselineWidth = 0;
  var appViewportBaselineHeight = 0;
  var closedViewportBaselineUntil = 0;
  var keyboardDismissCooldownUntil = 0;
  function isIosNativeViewportMode() {
    return window.__wandIosNative === true;
  }
  function markClosedViewportBaselineWindow(durationMs) {
    closedViewportBaselineUntil = Math.max(
      closedViewportBaselineUntil,
      Date.now() + (durationMs || 1800)
    );
  }
  function scheduleClosedViewportBaselineWindow(durationMs, blurredEl) {
    setTimeout(function() {
      var activeEl = document.activeElement;
      if (isEditableFocusTarget(activeEl) && activeEl !== blurredEl) return;
      markClosedViewportBaselineWindow(durationMs);
    }, 30);
  }
  function getFullViewportHeight(vv) {
    return Math.max(
      window.innerHeight || 0,
      vv && vv.height || 0,
      document.documentElement ? document.documentElement.clientHeight || 0 : 0,
      document.body ? document.body.clientHeight || 0 : 0
    );
  }
  function refreshAppViewportBaseline(vv) {
    var root = document.documentElement;
    var width = Math.max(
      window.innerWidth || 0,
      vv && vv.width || 0,
      root ? root.clientWidth || 0 : 0
    );
    var height = getFullViewportHeight(vv);
    if (!appViewportBaselineWidth || Math.abs(width - appViewportBaselineWidth) > 8) {
      appViewportBaselineWidth = width;
      appViewportBaselineHeight = height;
    } else if (height > appViewportBaselineHeight) {
      appViewportBaselineHeight = height;
    }
    return Math.max(1, Math.round(appViewportBaselineHeight || height || 1));
  }
  function shouldUseFullViewport(isKeyboardOpen, offsetTop, height, baselineHeight) {
    if (isKeyboardOpen || !isIosNativeViewportMode()) return false;
    return offsetTop > 0 || baselineHeight > height + 1;
  }
  function syncAppViewportHeight(isKeyboardOpen) {
    var vv = window.visualViewport;
    if (!vv) return;
    var root = document.documentElement;
    root.classList.toggle("is-keyboard-open", !!isKeyboardOpen);
    if (window.__wandImeNative) {
      root.style.setProperty("--app-viewport-top", "0px");
      root.style.setProperty("--app-viewport-height", Math.round(vv.height) + "px");
      return;
    }
    var baselineHeight = refreshAppViewportBaseline(vv);
    var offsetTop = Math.max(0, Math.round(vv.offsetTop || 0));
    var height = Math.max(1, Math.round(vv.height));
    if (!isKeyboardOpen && isIosNativeViewportMode()) {
      offsetTop = 0;
      height = Math.max(height, baselineHeight);
    } else if (shouldUseFullViewport(isKeyboardOpen, offsetTop, height, baselineHeight)) {
      offsetTop = 0;
      height = Math.max(height, baselineHeight);
    }
    root.style.setProperty("--app-viewport-top", offsetTop + "px");
    root.style.setProperty("--app-viewport-height", height + "px");
    if (isKeyboardOpen || (window.scrollY || 0) > 0) {
      resetRootViewportScroll();
    }
  }
  function isEditableFocusTarget(el) {
    if (!el) return false;
    var tag = el.tagName;
    if (tag === "TEXTAREA") return true;
    if (tag === "SELECT") return true;
    if (tag === "INPUT") {
      var type = (el.getAttribute("type") || "text").toLowerCase();
      return !/^(button|checkbox|color|file|hidden|image|radio|range|reset|submit)$/i.test(type);
    }
    return !!el.isContentEditable;
  }
  function setupVisualViewportHandlers() {
    if (!("visualViewport" in window)) return;
    if (window.__wandViewportHandlersBound) return;
    window.__wandViewportHandlersBound = true;
    var vv = window.visualViewport;
    var lastHeight = vv.height;
    var keyboardOpen = false;
    var lastViewportWidth = Math.max(window.innerWidth || 0, vv.width || 0);
    var largestViewportHeight = Math.max(window.innerHeight || 0, vv.height || 0);
    var viewportSettleTimers = [];
    function getCurrentViewportHeightBaseline() {
      return Math.max(window.innerHeight || 0, vv.height || 0);
    }
    function refreshViewportBaseline() {
      var width = Math.max(window.innerWidth || 0, vv.width || 0);
      var height = getCurrentViewportHeightBaseline();
      if (Math.abs(width - lastViewportWidth) > 8) {
        lastViewportWidth = width;
        largestViewportHeight = height;
        return;
      }
      if (height > largestViewportHeight) {
        largestViewportHeight = height;
      }
    }
    function detectKeyboardOpen(inputBox, offsetBottom) {
      var activeEl = document.activeElement;
      var hasEditableFocus = activeEl === inputBox || isEditableFocusTarget(activeEl);
      var shrinkFromLargest = largestViewportHeight - vv.height;
      var innerShrinkFromLargest = largestViewportHeight - (window.innerHeight || vv.height || 0);
      var inDismissCooldown = Date.now() < keyboardDismissCooldownUntil;
      if (inDismissCooldown) {
        if (hasEditableFocus && (shrinkFromLargest > 120 || innerShrinkFromLargest > 120)) return true;
        return false;
      }
      if (offsetBottom > 80) return true;
      if (hasEditableFocus && (shrinkFromLargest > 120 || innerShrinkFromLargest > 120)) return true;
      if (keyboardOpen && (shrinkFromLargest > 80 || offsetBottom > 32)) return true;
      return false;
    }
    function scheduleViewportSettle() {
      viewportSettleTimers.forEach(function(timer) {
        clearTimeout(timer);
      });
      viewportSettleTimers = [60, 180, 360, 620, 900].map(function(delay) {
        return setTimeout(function() {
          syncAppViewportHeight(keyboardOpen);
        }, delay);
      });
    }
    function scheduleFocusedInputSettle() {
      [0, 50, 120, 220, 360, 560].forEach(function(delay) {
        setTimeout(function() {
          updateViewport();
          var inputBox = document.getElementById("input-box");
          if (inputBox && document.activeElement === inputBox) {
            syncInputBoxScroll(inputBox);
          }
        }, delay);
      });
    }
    function updateViewport() {
      if (!vv) return;
      var inputBox = document.getElementById("input-box");
      var offsetBottom = window.innerHeight - vv.height - vv.offsetTop;
      refreshViewportBaseline();
      var isKeyboardOpen = detectKeyboardOpen(inputBox, offsetBottom);
      var heightChanged = Math.abs(vv.height - lastHeight) > 8;
      syncAppViewportHeight(isKeyboardOpen);
      if (isKeyboardOpen && (!keyboardOpen || heightChanged) && shouldAdjustForKeyboard(vv, inputBox)) {
        syncInputBoxScroll(inputBox);
      }
      if (!keyboardOpen && isKeyboardOpen) {
        var wasStickToBottom = state.terminalAutoFollow || isTerminalNearBottom();
        ensureTerminalFit("keyboard-open", { forceReplay: true });
        if (!window.__wandImeNative) {
          setTimeout(function() {
            syncAppViewportHeight(true);
          }, 220);
        }
        scheduleViewportSettle();
        if (wasStickToBottom) {
          setTimeout(function() {
            if (!state.terminal) return;
            maybeScrollTerminalToBottom("force");
          }, 220);
        }
      }
      if (keyboardOpen && !isKeyboardOpen) {
        var imeIsNative = !!window.__wandImeNative;
        if (!imeIsNative) {
          markClosedViewportBaselineWindow(2200);
          keyboardDismissCooldownUntil = Date.now() + 1200;
          syncAppViewportHeight(false);
          resetRootViewportScroll();
        }
        scheduleViewportSettle();
        setTimeout(function() {
          if (!imeIsNative) {
            syncAppViewportHeight(false);
            resetRootViewportScroll();
          }
          ensureTerminalFit("keyboard-close", { forceReplay: true });
          maybeScrollTerminalToBottom("keyboard");
        }, 200);
      }
      if (heightChanged && keyboardOpen === isKeyboardOpen) {
        ensureTerminalFit("viewport");
      }
      keyboardOpen = isKeyboardOpen;
      lastHeight = vv.height;
    }
    var viewportFrame = null;
    function debouncedUpdate() {
      if (viewportFrame !== null) cancelAnimationFrame(viewportFrame);
      viewportFrame = requestAnimationFrame(function() {
        viewportFrame = null;
        updateViewport();
      });
    }
    vv.addEventListener("resize", debouncedUpdate);
    vv.addEventListener("scroll", debouncedUpdate);
    document.addEventListener("visibilitychange", function() {
      if (document.visibilityState === "visible") {
        debouncedUpdate();
        setTimeout(debouncedUpdate, 240);
        setTimeout(debouncedUpdate, 720);
      }
    });
    window.addEventListener("pageshow", function(e) {
      if (e && e.persisted) {
        debouncedUpdate();
        setTimeout(debouncedUpdate, 240);
      }
    });
    document.addEventListener("focusout", function(e) {
      if (!e || !e.target) return;
      if (!isEditableFocusTarget(e.target)) return;
      scheduleClosedViewportBaselineWindow(1600, e.target);
      if (isIosNativeViewportMode()) {
        keyboardDismissCooldownUntil = Math.max(
          keyboardDismissCooldownUntil,
          Date.now() + 1200
        );
      }
      setTimeout(debouncedUpdate, 80);
      setTimeout(debouncedUpdate, 420);
    });
    document.addEventListener("focusin", function(e) {
      if (!e || !e.target || !isEditableFocusTarget(e.target)) return;
      scheduleFocusedInputSettle();
    });
    window.addEventListener("wand-ios-ime-state", function(e) {
      var state2 = e && e.detail && e.detail.state;
      if (state2 === "hidden") {
        keyboardDismissCooldownUntil = Date.now() + 900;
      }
      scheduleFocusedInputSettle();
      scheduleViewportSettle();
    });
    updateViewport();
  }
  function initTerminalResizeHandle() {
    var container = document.getElementById("output");
    if (!container) return;
    var resizeHandle = document.createElement("div");
    resizeHandle.className = "terminal-resize-handle";
    resizeHandle.innerHTML = "&#8942;";
    container.appendChild(resizeHandle);
    var isResizing = false;
    var startY = 0;
    var startHeight = 0;
    resizeHandle.addEventListener("mousedown", function(e) {
      isResizing = true;
      startY = e.clientY;
      startHeight = container.getBoundingClientRect().height;
      document.body.style.cursor = "ns-resize";
      document.body.style.userSelect = "none";
      e.preventDefault();
    });
    state.resizeMouseMove = function(e) {
      if (!isResizing) return;
      var deltaY = e.clientY - startY;
      var newHeight = Math.max(200, Math.min(startHeight + deltaY, window.innerHeight - 200));
      container.style.height = newHeight + "px";
      container.style.flex = "none";
      scheduleTerminalResize();
    };
    document.addEventListener("mousemove", state.resizeMouseMove);
    state.resizeMouseUp = function() {
      if (isResizing) {
        isResizing = false;
        document.body.style.cursor = "";
        document.body.style.userSelect = "";
        scheduleTerminalResize();
      }
    };
    document.addEventListener("mouseup", state.resizeMouseUp);
    resizeHandle.addEventListener("touchstart", function(e) {
      isResizing = true;
      startY = e.touches[0].clientY;
      startHeight = container.getBoundingClientRect().height;
      e.preventDefault();
    }, { passive: false });
    state.resizeTouchMove = function(e) {
      if (!isResizing) return;
      var deltaY = e.touches[0].clientY - startY;
      var newHeight = Math.max(200, Math.min(startHeight + deltaY, window.innerHeight - 200));
      container.style.height = newHeight + "px";
      container.style.flex = "none";
      scheduleTerminalResize();
      e.preventDefault();
    };
    document.addEventListener("touchmove", state.resizeTouchMove, { passive: false });
    state.resizeTouchEnd = function() {
      if (isResizing) {
        isResizing = false;
        scheduleTerminalResize();
      }
    };
    document.addEventListener("touchend", state.resizeTouchEnd);
  }
  function isJoystickAvailable() {
    return !!getSelectedSession();
  }
  function clampJoystickPos(pos) {
    var maxRight = Math.max(JOYSTICK_EDGE_MARGIN, window.innerWidth - JOYSTICK_BALL_SIZE - JOYSTICK_EDGE_MARGIN);
    var maxBottom = Math.max(JOYSTICK_EDGE_MARGIN, window.innerHeight - JOYSTICK_BALL_SIZE - JOYSTICK_EDGE_MARGIN);
    return {
      right: Math.min(Math.max(JOYSTICK_EDGE_MARGIN, pos.right), maxRight),
      bottom: Math.min(Math.max(JOYSTICK_EDGE_MARGIN, pos.bottom), maxBottom)
    };
  }
  function applyJoystickPosition() {
    if (!state.joystickBallEl) return;
    var pos = clampJoystickPos(state.joystickPos || { right: 18, bottom: 96 });
    state.joystickBallEl.style.right = pos.right + "px";
    state.joystickBallEl.style.bottom = pos.bottom + "px";
  }
  function saveJoystickPosition(right, bottom) {
    var pos = clampJoystickPos({ right, bottom });
    state.joystickPos = pos;
    try {
      localStorage.setItem("wand-ball-pos", JSON.stringify(pos));
    } catch (e) {
    }
  }
  function renderJoystickPanel() {
    function keyBtn(key, label, cls) {
      return '<button type="button" class="wjp-key' + (cls ? " " + cls : "") + '" data-key="' + key + '">' + label + "</button>";
    }
    var dpad = '<div class="wjp-dpad"><div class="wjp-dpad-row">' + keyBtn("up", "\u2191", "wjp-dir") + '</div><div class="wjp-dpad-row">' + keyBtn("left", "\u2190", "wjp-dir") + keyBtn("down", "\u2193", "wjp-dir") + keyBtn("right", "\u2192", "wjp-dir") + "</div></div>";
    var fnRow = "";
    var i;
    for (i = 0; i < JOYSTICK_ACTION_KEYS.length; i++) {
      fnRow += keyBtn(JOYSTICK_ACTION_KEYS[i].key, JOYSTICK_ACTION_KEYS[i].label, "");
    }
    var html = '<div class="wjp-header"><span class="wjp-title">' + iconSvg("paw", { size: 13, strokeWidth: 1.6, cls: "wjp-title-icon" }) + '<span>\u9065\u63A7\u9762\u677F</span></span><button type="button" class="wjp-close" aria-label="\u5173\u95ED\u9065\u63A7\u9762\u677F">' + iconSvg("x", { size: 13, strokeWidth: 2 }) + "</button></div>" + dpad + '<div class="wjp-grid wjp-fnkeys">' + fnRow + "</div>";
    return html;
  }
  function initTerminalJoystick() {
    if (state.joystickRootEl) return;
    var root = document.createElement("div");
    root.className = "wand-joystick-root";
    ["pointerdown", "pointerup", "touchstart", "touchend", "click"].forEach(function(type) {
      root.addEventListener(type, suppressJoystickKeyboardFocus, true);
    });
    var backdrop = document.createElement("div");
    backdrop.className = "wand-joystick-backdrop";
    root.appendChild(backdrop);
    var panel = document.createElement("div");
    panel.className = "wand-joystick-panel";
    panel.innerHTML = renderJoystickPanel();
    panel.addEventListener("click", onJoystickPanelClick);
    root.appendChild(panel);
    var ball = document.createElement("div");
    ball.className = "wand-joystick-ball";
    ball.setAttribute("role", "button");
    ball.setAttribute("aria-label", "Wand \u9065\u63A7\u9762\u677F");
    ball.setAttribute("title", "\u70B9\u51FB\u6253\u5F00\u9065\u63A7\u9762\u677F\uFF0C\u62D6\u52A8\u53EF\u79FB\u52A8\u4F4D\u7F6E");
    ball.innerHTML = iconSvg("paw", { size: 25, strokeWidth: 1.6, cls: "wand-joystick-logo" });
    root.appendChild(ball);
    document.body.appendChild(root);
    state.joystickRootEl = root;
    state.joystickBackdropEl = backdrop;
    state.joystickPanelEl = panel;
    state.joystickBallEl = ball;
    applyJoystickPosition();
    ball.addEventListener("pointerdown", onJoystickPointerDown);
    ball.addEventListener("click", function(e) {
      e.preventDefault();
      e.stopPropagation();
    });
    backdrop.addEventListener("pointerdown", function(e) {
      if (state.joystickPinnedOpen && state.joystickGesture == null) {
        e.preventDefault();
        closeJoystickPanel();
      }
    });
    backdrop.addEventListener("click", function(e) {
      e.preventDefault();
      e.stopPropagation();
    });
    state.joystickResizeHandler = function() {
      applyJoystickPosition();
    };
    window.addEventListener("resize", state.joystickResizeHandler);
    window.addEventListener("orientationchange", state.joystickResizeHandler);
    updateJoystickVisibility();
  }
  function suppressJoystickKeyboardFocus() {
    if (!document.documentElement.classList.contains("is-wand-native-input")) return;
    var active = document.activeElement;
    if (active && typeof active.blur === "function") {
      try {
        active.blur();
      } catch (err) {
      }
    }
    setTimeout(function() {
      var next = document.activeElement;
      if (next && typeof next.blur === "function") {
        try {
          next.blur();
        } catch (err) {
        }
      }
    }, 0);
  }
  function onJoystickPointerDown(e) {
    if (!isJoystickAvailable()) return;
    if ((e.pointerType === "mouse" || e.pointerType === "pen") && e.button !== 0) return;
    if (state.joystickPointerId !== null) return;
    suppressJoystickKeyboardFocus();
    e.preventDefault();
    e.stopPropagation();
    var canDirectDrag = e.pointerType === "mouse" || e.pointerType === "pen";
    state.joystickPointerId = e.pointerId;
    state.joystickPressStart = { x: e.clientX, y: e.clientY, t: Date.now() };
    state.joystickGesture = "pending";
    try {
      state.joystickBallEl.setPointerCapture(e.pointerId);
    } catch (err) {
    }
    if (!canDirectDrag) {
      state.joystickLongPressTimer = setTimeout(function() {
        if (state.joystickGesture === "pending") enterJoystickMoveMode();
      }, JOYSTICK_LONG_PRESS_MS);
    }
    state.joystickMoveHandler = onJoystickPointerMove;
    state.joystickUpHandler = onJoystickPointerUp;
    document.addEventListener("pointermove", state.joystickMoveHandler);
    document.addEventListener("pointerup", state.joystickUpHandler);
    document.addEventListener("pointercancel", state.joystickUpHandler);
  }
  function enterJoystickMoveMode() {
    state.joystickGesture = "move";
    if (state.joystickPinnedOpen) closeJoystickPanel();
    if (state.joystickBallEl) state.joystickBallEl.classList.add("dragging");
    if (state.joystickBackdropEl) state.joystickBackdropEl.classList.add("active");
  }
  function moveJoystickBallTo(clientX, clientY) {
    if (!state.joystickBallEl) return;
    var pos = clampJoystickPos({
      right: window.innerWidth - clientX - JOYSTICK_BALL_SIZE / 2,
      bottom: window.innerHeight - clientY - JOYSTICK_BALL_SIZE / 2
    });
    state.joystickBallEl.style.right = pos.right + "px";
    state.joystickBallEl.style.bottom = pos.bottom + "px";
  }
  function onJoystickPointerMove(e) {
    if (e.pointerId !== state.joystickPointerId || !state.joystickBallEl) return;
    e.preventDefault();
    if (state.joystickGesture === "move") {
      moveJoystickBallTo(e.clientX, e.clientY);
      return;
    }
    if (state.joystickGesture !== "pending") return;
    var dx = e.clientX - state.joystickPressStart.x;
    var dy = e.clientY - state.joystickPressStart.y;
    if (Math.sqrt(dx * dx + dy * dy) <= JOYSTICK_MOVE_THRESHOLD) return;
    if (e.pointerType === "mouse" || e.pointerType === "pen") {
      enterJoystickMoveMode();
      moveJoystickBallTo(e.clientX, e.clientY);
    } else {
      if (state.joystickLongPressTimer) {
        clearTimeout(state.joystickLongPressTimer);
        state.joystickLongPressTimer = null;
      }
      state.joystickGesture = "cancelled";
    }
  }
  function onJoystickPointerUp(e) {
    if (e.pointerId !== state.joystickPointerId) return;
    if (state.joystickLongPressTimer) {
      clearTimeout(state.joystickLongPressTimer);
      state.joystickLongPressTimer = null;
    }
    var gesture = state.joystickGesture;
    if (gesture === "pending") {
      var dx = e.clientX - state.joystickPressStart.x;
      var dy = e.clientY - state.joystickPressStart.y;
      if (Math.sqrt(dx * dx + dy * dy) <= JOYSTICK_TAP_THRESHOLD) toggleJoystickPanel();
    } else if (gesture === "move") {
      var r = state.joystickBallEl ? state.joystickBallEl.getBoundingClientRect() : null;
      if (r) saveJoystickPosition(window.innerWidth - r.right, window.innerHeight - r.bottom);
    }
    endJoystickGesture();
  }
  function endJoystickGesture() {
    if (state.joystickLongPressTimer) {
      clearTimeout(state.joystickLongPressTimer);
      state.joystickLongPressTimer = null;
    }
    if (state.joystickBallEl && state.joystickPointerId !== null) {
      try {
        state.joystickBallEl.releasePointerCapture(state.joystickPointerId);
      } catch (err) {
      }
    }
    if (state.joystickMoveHandler) {
      document.removeEventListener("pointermove", state.joystickMoveHandler);
      state.joystickMoveHandler = null;
    }
    if (state.joystickUpHandler) {
      document.removeEventListener("pointerup", state.joystickUpHandler);
      document.removeEventListener("pointercancel", state.joystickUpHandler);
      state.joystickUpHandler = null;
    }
    if (state.joystickBallEl) state.joystickBallEl.classList.remove("dragging");
    if (state.joystickBackdropEl && !state.joystickPinnedOpen) {
      state.joystickBackdropEl.classList.remove("active");
    }
    state.joystickPointerId = null;
    state.joystickGesture = null;
    state.joystickPressStart = null;
  }
  function sendJoystickKey(key) {
    if (key === "ctrl" || key === "alt" || key === "shift") {
      state.modifiers[key] = !state.modifiers[key];
      updateJoystickPanelUI();
      return;
    }
    var session = getSelectedSession();
    if (session && isStructuredSession2(session)) {
      if (key === "ctrl_c" || key === "escape") {
        interruptStructuredSessionFromJoystick(session, key);
      }
      clearModifiers();
      updateJoystickPanelUI();
      return;
    }
    var seq = buildPtySequence(key, {
      ctrl: state.modifiers.ctrl,
      alt: state.modifiers.alt,
      shift: state.modifiers.shift
    });
    if (seq) sendTerminalSequence(seq, key);
    clearModifiers();
    updateJoystickPanelUI();
    scheduleShortcutResync();
  }
  function interruptStructuredSessionFromJoystick(session, key) {
    if (!session || !session.id) return;
    fetch("/api/structured-sessions/" + session.id + "/messages", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      credentials: "same-origin",
      body: JSON.stringify({ input: "", interrupt: true, preserveQueue: true })
    }).then(function(res) {
      if (!res.ok) return res.json().catch(function() {
        return {};
      }).then(function(p) {
        throw new Error(p && p.error || "\u4E2D\u65AD\u5931\u8D25 (key=" + key + ")");
      });
      return res.json();
    }).then(function(snapshot7) {
      if (snapshot7 && snapshot7.id) {
        updateSessionSnapshot(snapshot7);
        if (snapshot7.id === state.selectedId) {
          var refreshed = state.sessions.find(function(s) {
            return s.id === snapshot7.id;
          }) || snapshot7;
          state.currentMessages = buildMessagesForRender(refreshed, getPreferredMessages(refreshed, snapshot7.output, false));
          renderChat2(true);
          if (typeof updateQueueBar === "function") updateQueueBar();
        }
      }
    }).catch(function(err) {
      if (err && err.message) console.debug("[wand] joystick interrupt no-op:", err.message);
    });
  }
  function toggleJoystickPanel() {
    if (state.joystickPinnedOpen) closeJoystickPanel();
    else openJoystickPanel();
  }
  function openJoystickPanel() {
    if (!state.joystickPanelEl || !state.joystickBallEl) return;
    state.joystickPinnedOpen = true;
    var r = state.joystickBallEl.getBoundingClientRect();
    state.joystickPanelEl.style.right = Math.max(JOYSTICK_EDGE_MARGIN, window.innerWidth - r.right) + "px";
    state.joystickPanelEl.style.bottom = Math.max(JOYSTICK_EDGE_MARGIN, window.innerHeight - r.top + 10) + "px";
    state.joystickPanelEl.classList.add("active");
    state.joystickBallEl.classList.add("panel-open");
    if (state.joystickBackdropEl) state.joystickBackdropEl.classList.add("active");
    updateJoystickPanelUI();
  }
  function closeJoystickPanel() {
    state.joystickPinnedOpen = false;
    if (state.joystickPanelEl) state.joystickPanelEl.classList.remove("active");
    if (state.joystickBallEl) state.joystickBallEl.classList.remove("panel-open");
    if (state.joystickBackdropEl && state.joystickGesture == null) {
      state.joystickBackdropEl.classList.remove("active");
    }
  }
  function updateJoystickPanelUI() {
    if (!state.joystickPanelEl) return;
    ["ctrl", "alt"].forEach(function(name) {
      var btn = state.joystickPanelEl.querySelector('.wjp-mod[data-key="' + name + '"]');
      if (btn) btn.classList.toggle("active", !!state.modifiers[name]);
    });
  }
  function onJoystickPanelClick(e) {
    var closeBtn = e.target && e.target.closest ? e.target.closest(".wjp-close") : null;
    if (closeBtn) {
      e.preventDefault();
      e.stopPropagation();
      closeJoystickPanel();
      return;
    }
    var btn = e.target && e.target.closest ? e.target.closest(".wjp-key") : null;
    if (!btn) return;
    e.preventDefault();
    e.stopPropagation();
    var key = btn.getAttribute("data-key");
    if (key) sendJoystickKey(key);
  }
  function updateJoystickVisibility() {
    var root = state.joystickRootEl;
    if (!root) return;
    var available = isJoystickAvailable();
    root.classList.toggle("visible", available);
    if (!available) {
      if (state.joystickPointerId !== null || state.joystickGesture) endJoystickGesture();
      if (state.joystickPinnedOpen) closeJoystickPanel();
      if (state.joystickBackdropEl) state.joystickBackdropEl.classList.remove("active");
    }
  }
  function teardownJoystick() {
    if (state.joystickLongPressTimer) {
      clearTimeout(state.joystickLongPressTimer);
      state.joystickLongPressTimer = null;
    }
    if (state.joystickMoveHandler) {
      document.removeEventListener("pointermove", state.joystickMoveHandler);
      state.joystickMoveHandler = null;
    }
    if (state.joystickUpHandler) {
      document.removeEventListener("pointerup", state.joystickUpHandler);
      document.removeEventListener("pointercancel", state.joystickUpHandler);
      state.joystickUpHandler = null;
    }
    if (state.joystickResizeHandler) {
      window.removeEventListener("resize", state.joystickResizeHandler);
      window.removeEventListener("orientationchange", state.joystickResizeHandler);
      state.joystickResizeHandler = null;
    }
    if (state.joystickRootEl && state.joystickRootEl.parentNode) {
      state.joystickRootEl.parentNode.removeChild(state.joystickRootEl);
    }
    state.joystickRootEl = null;
    state.joystickPanelEl = null;
    state.joystickBackdropEl = null;
    state.joystickBallEl = null;
    state.joystickPointerId = null;
    state.joystickGesture = null;
    state.joystickPressStart = null;
    state.joystickPinnedOpen = false;
  }
  function observeTerminalResize() {
    var output = document.getElementById("output");
    if (!output) return;
    var lastKnownDesktop = !isMobileLayout();
    state.resizeHandler = function() {
      scheduleTerminalResize(true);
      var isDesktop = !isMobileLayout();
      if (lastKnownDesktop !== isDesktop) {
        lastKnownDesktop = isDesktop;
        if (!isDesktop && state.sidebarPinned && state.sessionsDrawerOpen) {
          state.sessionsDrawerOpen = false;
          writeStoredBoolean("wand-sidebar-open", false);
          updateDrawerState();
        } else if (isDesktop && state.sidebarPinned && !state.sessionsDrawerOpen) {
          state.sessionsDrawerOpen = true;
          writeStoredBoolean("wand-sidebar-open", true);
          updateDrawerState();
        }
      }
    };
    window.addEventListener("resize", state.resizeHandler);
    if (window.visualViewport) {
      state.visualViewportHandler = function() {
        scheduleTerminalResize(true);
      };
      window.visualViewport.addEventListener("resize", state.visualViewportHandler);
    }
    state.visibilityHandler = function() {
      if (!document.hidden) ensureTerminalFit("visibility", { forceReplay: true });
    };
    document.addEventListener("visibilitychange", state.visibilityHandler);
    state.orientationHandler = function() {
      ensureTerminalFit("orientation", { forceReplay: true });
    };
    window.addEventListener("orientationchange", state.orientationHandler);
    requestAnimationFrame(function() {
      scheduleTerminalResize(true);
    });
  }
  function startTerminalHealthCheck() {
    if (state.terminalHealthTimer) return;
    state.terminalHealthTimer = setInterval(function() {
      if (!state.terminal || state.currentView !== "terminal" || document.hidden) return;
      var selectedSession = state.sessions.find(function(s) {
        return s.id === state.selectedId;
      });
      if (!selectedSession || selectedSession.sessionKind === "structured") return;
      ensureTerminalFit("health");
      var now = Date.now();
      var chunkPause = state.lastChunkAt > 0 && now - state.lastChunkAt > 300;
      var resyncDue = now - state.lastTerminalResyncAt > 3e4;
      var dirtySinceResync = state.lastChunkAt > state.lastTerminalResyncAt;
      if (resyncDue && dirtySinceResync && (chunkPause || selectedSession.status !== "running") && state.terminalOutput) {
        softResyncTerminal2();
      }
    }, 5e3);
  }
  function stopTerminalHealthCheck() {
    if (state.terminalHealthTimer) {
      clearInterval(state.terminalHealthTimer);
      state.terminalHealthTimer = null;
    }
  }
  function teardownTerminal() {
    stopTerminalHealthCheck();
    if (state.resizeTimer) {
      clearTimeout(state.resizeTimer);
      state.resizeTimer = null;
    }
    if (state.resizeObserver) {
      state.resizeObserver.disconnect();
      state.resizeObserver = null;
    }
    if (state.resizeHandler) {
      window.removeEventListener("resize", state.resizeHandler);
      state.resizeHandler = null;
    }
    if (state.visualViewportHandler && window.visualViewport) {
      window.visualViewport.removeEventListener("resize", state.visualViewportHandler);
      state.visualViewportHandler = null;
    }
    if (state.visibilityHandler) {
      document.removeEventListener("visibilitychange", state.visibilityHandler);
      state.visibilityHandler = null;
    }
    if (state.orientationHandler) {
      window.removeEventListener("orientationchange", state.orientationHandler);
      state.orientationHandler = null;
    }
    [
      ["mousemove", "resizeMouseMove"],
      ["mouseup", "resizeMouseUp"],
      ["touchmove", "resizeTouchMove"],
      ["touchend", "resizeTouchEnd"]
    ].forEach(function(pair) {
      if (state[pair[1]]) {
        document.removeEventListener(pair[0], state[pair[1]]);
        state[pair[1]] = null;
      }
    });
    clearTerminalScrollIdleTimer();
    var output = document.getElementById("output");
    if (state.terminalViewportEl) {
      if (state.terminalViewportScrollHandler) {
        state.terminalViewportEl.removeEventListener("scroll", state.terminalViewportScrollHandler);
      }
      if (state.terminalViewportTouchHandler) {
        state.terminalViewportEl.removeEventListener("touchmove", state.terminalViewportTouchHandler);
      }
      if (state.terminalViewportTouchStartHandler) {
        state.terminalViewportEl.removeEventListener("touchstart", state.terminalViewportTouchStartHandler);
      }
    }
    if (output) {
      if (state.terminalWheelHandler) {
        output.removeEventListener("wheel", state.terminalWheelHandler);
      }
      if (state.terminalClickHandler) {
        output.removeEventListener("click", state.terminalClickHandler);
      }
    }
    state.terminalViewportEl = null;
    state.terminalViewportScrollHandler = null;
    state.terminalViewportTouchHandler = null;
    state.terminalViewportTouchStartHandler = null;
    state.terminalWheelHandler = null;
    state.terminalClickHandler = null;
    if (state.terminalScrollbarHideTimer) {
      clearTimeout(state.terminalScrollbarHideTimer);
      state.terminalScrollbarHideTimer = null;
    }
    if (state.terminalScrollbarEl && state.terminalScrollbarEl.parentNode) {
      state.terminalScrollbarEl.parentNode.removeChild(state.terminalScrollbarEl);
    }
    state.terminalScrollbarEl = null;
    state.terminalScrollbarThumbEl = null;
    state.terminalScrollbarDragging = false;
    state.terminalScrollbarRafPending = false;
    if (state.terminal) {
      state.terminal.destroy();
      state.terminal = null;
    }
    if (output) {
      var staleWraps = output.querySelectorAll(".terminal-scroll-wrap");
      for (var i = 0; i < staleWraps.length; i++) {
        var wrap = staleWraps[i];
        if (wrap.parentNode === output) output.removeChild(wrap);
      }
    }
    resetWideParserState();
    state.syncOutputBuffer = null;
    state.syncOutputDeadline = 0;
    state.syncFramingResidue = false;
    state.terminalSessionId = null;
    state.terminalOutput = "";
    state.terminalOutputMarker = 0;
    state.terminalAutoFollow = true;
    state.showTerminalJumpToBottom = false;
    updateTerminalJumpToBottomButton();
    if (state.softResyncTimer) {
      clearTimeout(state.softResyncTimer);
      state.softResyncTimer = null;
    }
    if (state._resyncChunkTailTimer) {
      clearTimeout(state._resyncChunkTailTimer);
      state._resyncChunkTailTimer = null;
    }
    state._resyncChunkLastAt = 0;
    state._resyncStatsWindowStart = 0;
    state._resyncStatsCount = 0;
    state._resyncLastWarnAt = 0;
    state._resyncInProgress = false;
    state.lastResize = { cols: 0, rows: 0 };
    teardownJoystick();
  }
  function sendTerminalResize(cols, rows) {
    if (!state.selectedId) return;
    var selectedSess = state.sessions.find(function(s) {
      return s.id === state.selectedId;
    });
    if (!selectedSess || selectedSess.status !== "running") return;
    if (isStructuredSession2(selectedSess)) return;
    if (cols > 256) cols = 256;
    if (rows > 160) rows = 160;
    var nextSize = { cols, rows };
    if (state.lastResize.cols !== nextSize.cols || state.lastResize.rows !== nextSize.rows) {
      state.lastResize = nextSize;
      fetch("/api/sessions/" + state.selectedId + "/resize", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        credentials: "same-origin",
        body: JSON.stringify(nextSize)
      }).catch(function() {
      });
    }
  }
  function ensureTerminalFit(reason, options) {
    if (!state.terminal) return false;
    var opts = options || {};
    var forceReplay = opts.forceReplay === true;
    var el = document.getElementById("output");
    if (!el || el.offsetWidth === 0 || el.offsetHeight === 0) {
      ensureTerminalFitWithRetry(reason || "fit-retry", { forceReplay });
      return false;
    }
    var shouldStickToBottom = state.terminalAutoFollow || isTerminalNearBottom();
    var prevCols = state.terminal.cols;
    var prevRows = state.terminal.rows;
    requestAnimationFrame(function() {
      requestAnimationFrame(function() {
        if (!state.terminal) return;
        if (typeof state.terminal.remeasure === "function") {
          state.terminal.remeasure();
        }
        sendTerminalResize(state.terminal.cols, state.terminal.rows);
        var didResize = state.terminal.cols !== prevCols || state.terminal.rows !== prevRows;
        if (!didResize && forceReplay && state.terminalOutput) {
          softResyncTerminal2({ skipFit: true });
        }
        if (shouldStickToBottom) {
          maybeScrollTerminalToBottom("force");
        }
      });
    });
    return true;
  }
  function ensureTerminalFitWithRetry(reason, options) {
    if (!state.terminal) return;
    var opts = options || {};
    var forceReplay = opts.forceReplay !== false;
    var attempts = 0;
    var maxAttempts = 8;
    function tryFit() {
      if (!state.terminal) return;
      var el = document.getElementById("output");
      if (el) void el.offsetHeight;
      if (el && el.offsetWidth > 0 && el.offsetHeight > 0) {
        ensureTerminalFit(reason, { forceReplay });
        return;
      }
      if (++attempts >= maxAttempts) return;
      if (attempts <= 4) {
        requestAnimationFrame(tryFit);
      } else {
        setTimeout(tryFit, 32);
      }
    }
    tryFit();
  }
  function scheduleTerminalResize(immediate) {
    if (state.resizeTimer) {
      clearTimeout(state.resizeTimer);
      state.resizeTimer = null;
    }
    var delay = immediate ? 0 : 100;
    state.resizeTimer = setTimeout(function() {
      state.resizeTimer = null;
      requestAnimationFrame(syncTerminalSize);
    }, delay);
  }
  function syncTerminalSize() {
    if (!state.terminal) return;
    var shouldStickToBottom = state.terminalAutoFollow || isTerminalNearBottom();
    if (shouldStickToBottom) {
      maybeScrollTerminalToBottom("force");
    }
    sendTerminalResize(state.terminal.cols, state.terminal.rows);
  }

  // src/web-ui/browser/terminal.ts
  function saveWorkingDir(path) {
    state.workingDir = path;
    try {
      localStorage.setItem("wand-working-dir", path);
    } catch (e) {
    }
    addRecentPath(path);
  }
  function addRecentPath(path) {
    return fetch("/api/recent-paths", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      credentials: "same-origin",
      body: JSON.stringify({ path })
    }).catch(function() {
    });
  }
  function activateSessionItem(sessionId) {
    var session = state.sessions.find(function(s) {
      return s.id === sessionId;
    });
    if (session && session.status !== "running" && !isStructuredSession2(session)) {
      resumeSessionFromList(sessionId);
    } else {
      selectSession(sessionId);
    }
    dismissDrawerIfOverlay();
  }
  function handleSessionItemClick(event) {
    var target = event.target;
    if (!target || !(target instanceof Element)) return;
    var collapsedTile = target.closest(".sidebar-collapsed-tile");
    if (collapsedTile && collapsedTile instanceof HTMLElement) {
      if (collapsedTile.dataset.collapsedNewSession) {
        event.preventDefault();
        event.stopPropagation();
        openSessionModal();
        return;
      }
      if (collapsedTile.dataset.collapsedSessionId) {
        event.preventDefault();
        event.stopPropagation();
        activateSessionItem(collapsedTile.dataset.collapsedSessionId);
        return;
      }
      if (collapsedTile.dataset.collapsedHistoryId) {
        event.preventDefault();
        event.stopPropagation();
        var historyCid = collapsedTile.dataset.collapsedHistoryId;
        var historyCwd = collapsedTile.dataset.cwd || "";
        var resumeCollapsed = collapsedTile.dataset.provider === "codex" ? resumeCodexHistorySession : resumeClaudeHistorySession;
        resumeCollapsed(historyCid, historyCwd).then(function(data) {
          if (data && data.id) {
            state.selectedId = data.id;
            persistSelectedId();
            state.drafts[data.id] = "";
            loadSessions().then(function() {
              selectSession(data.id);
            });
          }
        });
        return;
      }
    }
    var historyToggle = target.closest("#claude-history-toggle");
    if (historyToggle) {
      event.preventDefault();
      event.stopPropagation();
      state.claudeHistoryExpanded = !state.claudeHistoryExpanded;
      if (state.claudeHistoryExpanded && !state.claudeHistoryLoaded) {
        ensureClaudeHistoryLoaded();
      }
      updateSessionsList();
      return;
    }
    var actionButton = target.closest("[data-action]");
    if (actionButton && actionButton instanceof HTMLElement) {
      event.preventDefault();
      event.stopPropagation();
      if (actionButton.dataset.action === "toggle-manage-mode") {
        toggleManageMode();
      } else if (actionButton.dataset.action === "select-all-visible") {
        selectAllVisibleItems();
      } else if (actionButton.dataset.action === "clear-selection") {
        clearSelections();
      } else if (actionButton.dataset.action === "delete-selected") {
        batchDeleteSelected();
      } else if (actionButton.dataset.action === "toggle-selection") {
        toggleManagedItemSelection(actionButton.dataset.kind, actionButton.dataset.id);
      } else if (actionButton.dataset.action === "swipe-delete-session" && actionButton.dataset.sessionId) {
        deleteSession(actionButton.dataset.sessionId);
      } else if (actionButton.dataset.action === "delete-session" && actionButton.dataset.sessionId) {
        (function(sid) {
          confirmDelete("\u786E\u8BA4\u5220\u9664\u8FD9\u4E2A\u4F1A\u8BDD\u5417\uFF1F\u6B64\u64CD\u4F5C\u65E0\u6CD5\u64A4\u9500\u3002", { title: "\u5220\u9664\u4F1A\u8BDD" }).then(function(ok) {
            if (ok) deleteSession(sid);
          });
        })(actionButton.dataset.sessionId);
      } else if (actionButton.dataset.action === "delete-history" && actionButton.dataset.claudeSessionId) {
        (function(cid, item2) {
          confirmDelete("\u786E\u8BA4\u5220\u9664\u8FD9\u6761 Claude \u4F1A\u8BDD\u5417\uFF1F", { title: "\u5220\u9664\u4F1A\u8BDD" }).then(function(ok) {
            if (ok) executeDeleteHistory(cid, item2);
          });
        })(actionButton.dataset.claudeSessionId, actionButton.closest(".session-item"));
      } else if (actionButton.dataset.action === "toggle-history-directory" && actionButton.dataset.cwd) {
        var dirCwd = actionButton.dataset.cwd;
        state.claudeHistoryExpandedDirs[dirCwd] = !state.claudeHistoryExpandedDirs[dirCwd];
        updateSessionsList();
      } else if (actionButton.dataset.action === "delete-history-directory" && actionButton.dataset.cwd) {
        (function(deleteCwd, btn) {
          var items = getHistoryItemsByCwd(deleteCwd);
          var dirCount = getVisibleClaudeHistorySessions().filter(function(s) {
            return s.cwd === deleteCwd;
          }).length;
          confirmDelete("\u786E\u8BA4\u6E05\u7A7A\u6B64\u76EE\u5F55\u4E0B\u7684 " + dirCount + " \u6761 Claude \u5386\u53F2\u5417\uFF1F", {
            title: "\u6E05\u7A7A\u76EE\u5F55\u5386\u53F2",
            okLabel: "\u6E05\u7A7A"
          }).then(function(ok) {
            if (!ok) return;
            setDeletingState(items, true);
            deleteClaudeHistoryDirectory(deleteCwd, btn, items);
          });
        })(actionButton.dataset.cwd, actionButton);
      } else if (actionButton.dataset.action === "clear-all-history") {
        clearAllClaudeHistory();
      } else if (actionButton.dataset.action === "toggle-archived-group") {
        state.archivedExpanded = !state.archivedExpanded;
        updateSessionsList();
      } else if (actionButton.dataset.action === "resume" && actionButton.dataset.sessionId) {
        handleResumeAction(actionButton);
      } else if (actionButton.dataset.action === "resume-history" && actionButton.dataset.claudeSessionId) {
        handleResumeHistoryAction(actionButton);
      } else if (actionButton.dataset.action === "resume-codex-history" && actionButton.dataset.claudeSessionId) {
        handleResumeCodexHistoryAction(actionButton);
      } else if (actionButton.dataset.action === "delete-codex-history" && actionButton.dataset.claudeSessionId) {
        handleDeleteCodexHistoryAction(actionButton);
      } else if (actionButton.dataset.action === "toggle-codex-history-directory" && actionButton.dataset.cwd) {
        var codexDirCwd = actionButton.dataset.cwd;
        state.codexHistoryExpandedDirs[codexDirCwd] = !state.codexHistoryExpandedDirs[codexDirCwd];
        updateSessionsList();
      } else if (actionButton.dataset.action === "worktree-merge" && actionButton.dataset.sessionId) {
        openWorktreeMergeModal(actionButton.dataset.sessionId);
      } else if (actionButton.dataset.action === "worktree-cleanup" && actionButton.dataset.sessionId) {
        retryWorktreeCleanup(actionButton.dataset.sessionId);
      }
      return;
    }
    var item = target.closest(".session-item");
    if (item) {
      if (state.sessionsManageMode) {
        if (item.dataset.sessionId) {
          toggleManagedItemSelection("sessions", item.dataset.sessionId);
        } else if (item.dataset.claudeHistoryId) {
          toggleManagedItemSelection(item.dataset.provider === "codex" ? "codex" : "history", item.dataset.claudeHistoryId);
        }
        return;
      }
      if (item.classList.contains("swiped")) {
        closeSwipedItem();
        return;
      }
      if (_swipeState) return;
      if (item.dataset.sessionId) {
        activateSessionItem(item.dataset.sessionId);
      } else if (item.dataset.claudeHistoryId) {
        var claudeSessionId = item.dataset.claudeHistoryId;
        var cwd = item.dataset.cwd;
        var resumeItem = item.dataset.provider === "codex" ? resumeCodexHistorySession : resumeClaudeHistorySession;
        resumeItem(claudeSessionId, cwd).then(function(data) {
          if (data && data.id) {
            state.selectedId = data.id;
            persistSelectedId();
            state.drafts[data.id] = "";
            loadSessions().then(function() {
              selectSession(data.id);
              dismissDrawerIfOverlay();
            });
          }
        });
      }
    }
  }
  function handleSessionItemKeydown(event) {
    if (event.key !== "Enter" && event.key !== " ") return;
    var item = event.target.closest(".session-item");
    if (!item) return;
    event.preventDefault();
    if (state.sessionsManageMode) {
      if (item.dataset.sessionId) {
        toggleManagedItemSelection("sessions", item.dataset.sessionId);
      } else if (item.dataset.claudeHistoryId) {
        toggleManagedItemSelection(item.dataset.provider === "codex" ? "codex" : "history", item.dataset.claudeHistoryId);
      }
      return;
    }
    if (item.dataset.sessionId) {
      activateSessionItem(item.dataset.sessionId);
    } else if (item.dataset.claudeHistoryId) {
      var claudeSessionId = item.dataset.claudeHistoryId;
      var cwd = item.dataset.cwd;
      var resumeItem = item.dataset.provider === "codex" ? resumeCodexHistorySession : resumeClaudeHistorySession;
      resumeItem(claudeSessionId, cwd).then(function(data) {
        if (data && data.id) {
          state.selectedId = data.id;
          persistSelectedId();
          state.drafts[data.id] = "";
          loadSessions().then(function() {
            selectSession(data.id);
            dismissDrawerIfOverlay();
          });
        }
      });
    }
  }
  function copySelectedSessionField(field, successMsg) {
    var session = state.sessions.find(function(s) {
      return s.id === state.selectedId;
    });
    if (!session) return;
    var value = session[field];
    if (!value) {
      showToast("\u5F53\u524D\u4F1A\u8BDD\u6CA1\u6709\u53EF\u590D\u5236\u7684\u5185\u5BB9\u3002", "error");
      return;
    }
    copyToClipboard(String(value), null, function() {
      showToast(successMsg || "\u5DF2\u590D\u5236", "info");
    });
  }
  function getTerminalViewport() {
    if (!state.terminal || !state.terminal.element) return null;
    state.terminalViewportEl = state.terminal.element;
    return state.terminalViewportEl;
  }
  function clearTerminalScrollIdleTimer() {
    if (state.terminalScrollIdleTimer) {
      clearTimeout(state.terminalScrollIdleTimer);
      state.terminalScrollIdleTimer = null;
    }
  }
  function updateTerminalJumpToBottomButton() {
    var button = document.getElementById("terminal-jump-bottom");
    var shouldShow = !!state.selectedId && state.currentView === "terminal" && !state.terminalAutoFollow && !isTerminalAtBottom();
    state.showTerminalJumpToBottom = shouldShow;
    if (button) {
      button.classList.toggle("visible", shouldShow);
    }
    var termContainer = document.getElementById("output");
    if (termContainer) termContainer.classList.toggle("has-jump-btn", shouldShow);
  }
  function isTerminalNearBottom() {
    var viewport = getTerminalViewport();
    if (!viewport) return true;
    var distance = viewport.scrollHeight - viewport.clientHeight - viewport.scrollTop;
    return distance <= state.terminalScrollThreshold;
  }
  function isTerminalAtBottom() {
    var viewport = getTerminalViewport();
    if (!viewport) return true;
    var distance = viewport.scrollHeight - viewport.clientHeight - viewport.scrollTop;
    return distance <= 2;
  }
  function scrollTerminalToBottom(smooth) {
    if (!state.terminal) return;
    var viewport = getTerminalViewport();
    if (!viewport) return;
    var windowMs = smooth ? 500 : 120;
    state.terminalProgrammaticScrollUntil = Math.max(
      state.terminalProgrammaticScrollUntil,
      Date.now() + windowMs
    );
    if (smooth) {
      viewport.scrollTo({ top: viewport.scrollHeight, behavior: "smooth" });
    } else {
      viewport.scrollTop = viewport.scrollHeight;
    }
  }
  function setTerminalManualScrollActive() {
    state.terminalAutoFollow = false;
    clearTerminalScrollIdleTimer();
    state.terminalProgrammaticScrollUntil = 0;
    if (state.terminal && "_shouldScrollToBottom" in state.terminal) {
      state.terminal._shouldScrollToBottom = false;
    }
    updateTerminalJumpToBottomButton();
  }
  function maybeScrollTerminalToBottom(reason) {
    if (!state.terminal) return;
    var force = reason === "force";
    if (force) {
      state.terminalAutoFollow = true;
      clearTerminalScrollIdleTimer();
      scrollTerminalToBottom(false);
      updateTerminalJumpToBottomButton();
      return;
    }
    if (!state.terminalAutoFollow) {
      updateTerminalJumpToBottomButton();
      return;
    }
    scrollTerminalToBottom(false);
    updateTerminalJumpToBottomButton();
  }
  function initTerminalScrollbar2(container) {
    var scrollbar = document.createElement("div");
    scrollbar.className = "terminal-scrollbar";
    var track = document.createElement("div");
    track.className = "terminal-scrollbar-track";
    var thumb = document.createElement("div");
    thumb.className = "terminal-scrollbar-thumb";
    track.appendChild(thumb);
    scrollbar.appendChild(track);
    container.appendChild(scrollbar);
    state.terminalScrollbarEl = scrollbar;
    state.terminalScrollbarThumbEl = thumb;
    state.terminalScrollbarHideTimer = null;
    state.terminalScrollbarDragging = false;
    state.terminalScrollbarRafPending = false;
    function showScrollbar() {
      if (state.terminalScrollbarHideTimer) {
        clearTimeout(state.terminalScrollbarHideTimer);
        state.terminalScrollbarHideTimer = null;
      }
      scrollbar.classList.add("visible");
    }
    function scheduleHideScrollbar() {
      if (state.terminalScrollbarDragging) return;
      if (state.terminalScrollbarHideTimer) clearTimeout(state.terminalScrollbarHideTimer);
      state.terminalScrollbarHideTimer = setTimeout(function() {
        state.terminalScrollbarHideTimer = null;
        if (!state.terminalScrollbarDragging) {
          scrollbar.classList.remove("visible");
        }
      }, 1500);
    }
    function syncScrollbarThumb() {
      state.terminalScrollbarRafPending = false;
      var viewport2 = getTerminalViewport();
      if (!viewport2) return;
      var sh = viewport2.scrollHeight;
      var ch = viewport2.clientHeight;
      if (sh <= ch) {
        scrollbar.classList.remove("visible");
        return;
      }
      var trackH = track.clientHeight;
      var thumbH = Math.max(28, ch / sh * trackH);
      var maxScroll = sh - ch;
      var scrollRatio = viewport2.scrollTop / maxScroll;
      var thumbTop = scrollRatio * (trackH - thumbH);
      thumb.style.height = thumbH + "px";
      thumb.style.top = thumbTop + "px";
    }
    function requestSyncScrollbar() {
      if (state.terminalScrollbarRafPending) return;
      state.terminalScrollbarRafPending = true;
      requestAnimationFrame(syncScrollbarThumb);
    }
    var viewport = getTerminalViewport();
    if (viewport) {
      viewport.addEventListener("scroll", function() {
        showScrollbar();
        requestSyncScrollbar();
        scheduleHideScrollbar();
      }, { passive: true });
    }
    track.addEventListener("mousedown", function(e) {
      if (e.target === thumb) return;
      e.preventDefault();
      var viewport2 = getTerminalViewport();
      if (!viewport2) return;
      var rect = track.getBoundingClientRect();
      var clickRatio = (e.clientY - rect.top) / rect.height;
      var maxScroll = viewport2.scrollHeight - viewport2.clientHeight;
      viewport2.scrollTop = clickRatio * maxScroll;
    });
    var dragStartY = 0;
    var dragStartScrollTop = 0;
    thumb.addEventListener("mousedown", function(e) {
      e.preventDefault();
      e.stopPropagation();
      state.terminalScrollbarDragging = true;
      thumb.classList.add("dragging");
      dragStartY = e.clientY;
      var viewport2 = getTerminalViewport();
      dragStartScrollTop = viewport2 ? viewport2.scrollTop : 0;
      document.addEventListener("mousemove", onDragMove);
      document.addEventListener("mouseup", onDragEnd);
    });
    function onDragMove(e) {
      e.preventDefault();
      var viewport2 = getTerminalViewport();
      if (!viewport2) return;
      var trackH = track.clientHeight;
      var sh = viewport2.scrollHeight;
      var ch = viewport2.clientHeight;
      var maxScroll = sh - ch;
      if (maxScroll <= 0) return;
      var thumbH = Math.max(28, ch / sh * trackH);
      var scrollableTrack = trackH - thumbH;
      if (scrollableTrack <= 0) return;
      var deltaY = e.clientY - dragStartY;
      var scrollDelta = deltaY / scrollableTrack * maxScroll;
      viewport2.scrollTop = dragStartScrollTop + scrollDelta;
    }
    function onDragEnd() {
      state.terminalScrollbarDragging = false;
      thumb.classList.remove("dragging");
      document.removeEventListener("mousemove", onDragMove);
      document.removeEventListener("mouseup", onDragEnd);
      scheduleHideScrollbar();
    }
    thumb.addEventListener("touchstart", function(e) {
      if (e.touches.length !== 1) return;
      e.stopPropagation();
      state.terminalScrollbarDragging = true;
      thumb.classList.add("dragging");
      dragStartY = e.touches[0].clientY;
      var viewport2 = getTerminalViewport();
      dragStartScrollTop = viewport2 ? viewport2.scrollTop : 0;
      document.addEventListener("touchmove", onTouchDragMove, { passive: false });
      document.addEventListener("touchend", onTouchDragEnd);
      document.addEventListener("touchcancel", onTouchDragEnd);
    }, { passive: false });
    function onTouchDragMove(e) {
      if (e.touches.length !== 1) return;
      e.preventDefault();
      var viewport2 = getTerminalViewport();
      if (!viewport2) return;
      var trackH = track.clientHeight;
      var sh = viewport2.scrollHeight;
      var ch = viewport2.clientHeight;
      var maxScroll = sh - ch;
      if (maxScroll <= 0) return;
      var thumbH = Math.max(28, ch / sh * trackH);
      var scrollableTrack = trackH - thumbH;
      if (scrollableTrack <= 0) return;
      var deltaY = e.touches[0].clientY - dragStartY;
      var scrollDelta = deltaY / scrollableTrack * maxScroll;
      viewport2.scrollTop = dragStartScrollTop + scrollDelta;
    }
    function onTouchDragEnd() {
      state.terminalScrollbarDragging = false;
      thumb.classList.remove("dragging");
      document.removeEventListener("touchmove", onTouchDragMove);
      document.removeEventListener("touchend", onTouchDragEnd);
      document.removeEventListener("touchcancel", onTouchDragEnd);
      scheduleHideScrollbar();
    }
    scrollbar.addEventListener("mouseenter", function() {
      showScrollbar();
    });
    scrollbar.addEventListener("mouseleave", function() {
      if (!state.terminalScrollbarDragging) scheduleHideScrollbar();
    });
    requestSyncScrollbar();
  }
  function isEastAsianWide(cp) {
    if (cp < 4352) return false;
    return cp >= 4352 && cp <= 4447 || cp >= 9001 && cp <= 9002 || cp >= 11904 && cp <= 12350 || cp >= 12353 && cp <= 13311 || cp >= 13312 && cp <= 19903 || cp >= 19968 && cp <= 40959 || cp >= 40960 && cp <= 42191 || cp >= 44032 && cp <= 55203 || cp >= 63744 && cp <= 64255 || cp >= 65072 && cp <= 65103 || cp >= 65280 && cp <= 65376 || cp >= 65504 && cp <= 65510 || cp >= 126976 && cp <= 129535 || cp >= 131072 && cp <= 196605 || cp >= 196608 && cp <= 262141;
  }
  var WAND_WIDE_FILLER = "\u2060";
  function createWideParserState() {
    return { mode: "normal" };
  }
  function isAsciiNonEscape(s) {
    return !/[^\x00-\x7f]/.test(s) && s.indexOf("\x1B") === -1;
  }
  function widePadAnsi2(data, st) {
    if (!data) return "";
    var s = String(data);
    if (st.mode === "normal" && isAsciiNonEscape(s)) return s;
    var out = "";
    for (var i = 0; i < s.length; i++) {
      var code = s.charCodeAt(i);
      var cp = code;
      var consumed = 1;
      if (code >= 55296 && code <= 56319 && i + 1 < s.length) {
        var lo = s.charCodeAt(i + 1);
        if (lo >= 56320 && lo <= 57343) {
          cp = (code - 55296) * 1024 + (lo - 56320) + 65536;
          consumed = 2;
        }
      }
      var ch = consumed === 2 ? s.substr(i, 2) : s.charAt(i);
      switch (st.mode) {
        case "normal":
          if (cp === 27) {
            st.mode = "esc";
            out += ch;
          } else if (cp === 155) {
            st.mode = "csi";
            out += ch;
          } else if (cp === 157 || cp === 144 || cp === 158 || cp === 159) {
            st.mode = "string";
            out += ch;
          } else {
            out += ch;
            if (isEastAsianWide(cp)) out += WAND_WIDE_FILLER;
          }
          break;
        case "esc":
          out += ch;
          if (cp === 91) st.mode = "csi";
          else if (cp === 93 || cp === 80 || cp === 88 || cp === 94 || cp === 95) st.mode = "string";
          else st.mode = "normal";
          break;
        case "csi":
          out += ch;
          if (cp >= 64 && cp <= 126) st.mode = "normal";
          break;
        case "string":
          out += ch;
          if (cp === 7 || cp === 156) st.mode = "normal";
          else if (cp === 27) st.mode = "string-esc";
          break;
        case "string-esc":
          out += ch;
          if (cp === 92) st.mode = "normal";
          else st.mode = "string";
          break;
      }
      i += consumed - 1;
    }
    return out;
  }
  var SYNC_OUTPUT_BEGIN = "\x1B[?2026h";
  var SYNC_OUTPUT_END = "\x1B[?2026l";
  var SYNC_OUTPUT_MAX_BUFFER_MS = 200;
  var SYNC_OUTPUT_MAX_BYTES = 256 * 1024;
  function processSyncOutputFraming(data) {
    if (!data) return data;
    if (state.syncOutputBuffer === null && data.indexOf(SYNC_OUTPUT_BEGIN) === -1) {
      return data;
    }
    var out = "";
    var i = 0;
    while (i < data.length) {
      if (state.syncOutputBuffer !== null) {
        var endIdx = data.indexOf(SYNC_OUTPUT_END, i);
        if (endIdx === -1) {
          state.syncOutputBuffer += data.slice(i);
          if (state.syncOutputBuffer.length > SYNC_OUTPUT_MAX_BYTES || Date.now() > state.syncOutputDeadline) {
            out += state.syncOutputBuffer;
            state.syncOutputBuffer = null;
            state.syncFramingResidue = true;
          }
          return out;
        }
        state.syncOutputBuffer += data.slice(i, endIdx + SYNC_OUTPUT_END.length);
        out += state.syncOutputBuffer;
        state.syncOutputBuffer = null;
        i = endIdx + SYNC_OUTPUT_END.length;
      } else {
        var beginIdx = data.indexOf(SYNC_OUTPUT_BEGIN, i);
        if (beginIdx === -1) {
          out += data.slice(i);
          return out;
        }
        out += data.slice(i, beginIdx);
        state.syncOutputBuffer = SYNC_OUTPUT_BEGIN;
        state.syncOutputDeadline = Date.now() + SYNC_OUTPUT_MAX_BUFFER_MS;
        i = beginIdx + SYNC_OUTPUT_BEGIN.length;
      }
    }
    return out;
  }
  function wandTerminalWrite(terminal, data) {
    if (!terminal || data == null) return;
    if (!state.wideParserState) state.wideParserState = createWideParserState();
    var padded = widePadAnsi2(data, state.wideParserState);
    var framed = processSyncOutputFraming(padded);
    var follow = state.terminalAutoFollow !== false;
    if ("_shouldScrollToBottom" in terminal) {
      terminal._shouldScrollToBottom = follow;
    }
    if (framed) terminal.write(framed);
    if ("_shouldScrollToBottom" in terminal) {
      terminal._shouldScrollToBottom = follow;
    }
    if (follow) {
      state.terminalProgrammaticScrollUntil = Math.max(
        state.terminalProgrammaticScrollUntil,
        Date.now() + 120
      );
    }
    maybeScheduleResyncForChunk(data);
  }
  function resetWideParserState() {
    state.wideParserState = createWideParserState();
  }
  function stripWideFillerForCopy() {
    if (typeof document === "undefined") return;
    document.addEventListener("copy", function(e) {
      var sel = window.getSelection && window.getSelection();
      if (!sel || sel.isCollapsed) return;
      var anchor = sel.anchorNode;
      var node = anchor && anchor.nodeType === 3 ? anchor.parentNode : anchor;
      var output = document.getElementById("output");
      if (!output || !node || !output.contains(node)) return;
      var text5 = sel.toString();
      var cleaned = text5.split("\n").map(function(line) {
        return line.split(WAND_WIDE_FILLER).join("").replace(/[ \t]+$/, "");
      }).join("\n");
      if (cleaned === text5) return;
      if (e.clipboardData) {
        e.clipboardData.setData("text/plain", cleaned);
        e.preventDefault();
      }
    });
  }
  stripWideFillerForCopy();
  var CHAT_RENDER_LIVE_MS = 150;
  var CHAT_RENDER_IDLE_MS = 30;
  var CLIENT_OUTPUT_MAX = 160 * 1024;
  var CLIENT_OUTPUT_TRIM_AT = 192 * 1024;
  function clampClientTerminalOutput(buf) {
    if (!buf || buf.length <= CLIENT_OUTPUT_TRIM_AT) return buf;
    var preTrimLen = buf.length;
    var _adjustMarker = function(trimmedLen) {
      if (typeof state === "undefined" || !state) return;
      var mk = state.terminalOutputMarker | 0;
      if (mk <= 0) return;
      var dropped = preTrimLen - trimmedLen;
      state.terminalOutputMarker = mk > dropped ? mk - dropped : 0;
    };
    var start = buf.length - CLIENT_OUTPUT_MAX;
    if (start > 0 && start < buf.length) {
      var c0 = buf.charCodeAt(start);
      if (c0 >= 56320 && c0 <= 57343) start++;
    }
    var LOOKAHEAD = 4096;
    var upper = Math.min(start + LOOKAHEAD, buf.length);
    for (var i = start; i < upper; i++) {
      if (buf.charCodeAt(i) === 10) {
        var trimmed1 = buf.slice(i + 1);
        _adjustMarker(trimmed1.length);
        return trimmed1;
      }
    }
    var lookback = Math.max(0, start - 256);
    var escAt = -1;
    for (var j = start - 1; j >= lookback; j--) {
      var c = buf.charCodeAt(j);
      if (c === 27) {
        escAt = j;
        break;
      }
      if (c === 7) break;
      if (c >= 64 && c <= 126) break;
    }
    if (escAt !== -1) {
      var terminated = false;
      for (var k = escAt + 1; k < start; k++) {
        var ck = buf.charCodeAt(k);
        if (ck === 7) {
          terminated = true;
          break;
        }
        if (ck >= 64 && ck <= 126) {
          terminated = true;
          break;
        }
      }
      if (!terminated) {
        var ahead = Math.min(start + 256, buf.length);
        for (var m = start; m < ahead; m++) {
          var cm = buf.charCodeAt(m);
          if (cm === 7 || cm >= 64 && cm <= 126) {
            var trimmed2 = buf.slice(m + 1);
            _adjustMarker(trimmed2.length);
            return trimmed2;
          }
        }
      }
    }
    var trimmed3 = buf.slice(start);
    _adjustMarker(trimmed3.length);
    return trimmed3;
  }
  function resetTerminal3() {
    if (!state.terminal) return;
    if (typeof state.terminal.reset === "function") {
      state.terminal.reset();
      resetWideParserState();
      state.syncOutputBuffer = null;
      state.syncOutputDeadline = 0;
      return;
    }
    if (typeof state.terminal.write === "function") {
      state.terminal.write("\x1Bc");
    }
    resetWideParserState();
    state.syncOutputBuffer = null;
    state.syncOutputDeadline = 0;
  }
  state._resyncStatsWindowStart = 0;
  state._resyncStatsCount = 0;
  state._resyncLastWarnAt = 0;
  var RESYNC_BUDGET_WINDOW_MS = 5e3;
  var RESYNC_BUDGET_MAX = 12;
  var RESYNC_WARN_COOLDOWN_MS = 3e4;
  state._resyncInProgress = false;
  function softResyncTerminal2(options) {
    if (!state.terminal || !state.terminalOutput) return false;
    var opts = options || {};
    var marker = state.terminalOutputMarker | 0;
    if (marker < 0) marker = 0;
    if (marker > state.terminalOutput.length) marker = state.terminalOutput.length;
    var replaySource = marker > 0 ? state.terminalOutput.slice(marker) : state.terminalOutput;
    var bufLen = replaySource.length;
    var startedAt = typeof performance !== "undefined" && performance.now ? performance.now() : Date.now();
    state._resyncInProgress = true;
    try {
      resetTerminal3();
      wandTerminalWrite(state.terminal, replaySource);
    } finally {
      state._resyncInProgress = false;
    }
    state.lastTerminalResyncAt = Date.now();
    maybeScrollTerminalToBottom("output");
    if (!opts.skipFit) ensureTerminalFit("refresh");
    var now = Date.now();
    if (now - state._resyncStatsWindowStart > RESYNC_BUDGET_WINDOW_MS) {
      state._resyncStatsWindowStart = now;
      state._resyncStatsCount = 1;
    } else {
      state._resyncStatsCount++;
      if (state._resyncStatsCount > RESYNC_BUDGET_MAX && now - state._resyncLastWarnAt > RESYNC_WARN_COOLDOWN_MS) {
        state._resyncLastWarnAt = now;
        var endedAt = typeof performance !== "undefined" && performance.now ? performance.now() : Date.now();
        console.warn(
          "[wand] softResyncTerminal high frequency",
          "count=" + state._resyncStatsCount + "/" + Math.round((now - state._resyncStatsWindowStart) / 100) / 10 + "s",
          "bufLen=" + bufLen,
          "lastReplayMs=" + Math.round(endedAt - startedAt)
        );
      }
    }
    return true;
  }
  function scheduleSoftResyncTerminal(delayMs) {
    if (state.softResyncTimer) clearTimeout(state.softResyncTimer);
    state.softResyncTimer = setTimeout(function() {
      state.softResyncTimer = null;
      softResyncTerminal2();
    }, typeof delayMs === "number" ? delayMs : 150);
  }
  var IN_PLACE_REDRAW_RE = /\x1b\[\d*(?:;\d*)?[ABCDfHJK]/;
  var RESYNC_THROTTLE_MS = 1500;
  var RESYNC_TAIL_MS = 800;
  state._resyncChunkLastAt = 0;
  state._resyncChunkTailTimer = null;
  function maybeScheduleResyncForChunk(chunk) {
    if (state._resyncInProgress) return;
    if (!state.syncFramingResidue) return;
    state.syncFramingResidue = false;
    if (!chunk || typeof chunk !== "string") return;
    if (chunk.indexOf("\x1B[") === -1) return;
    if (!IN_PLACE_REDRAW_RE.test(chunk)) return;
    var now = Date.now();
    var sinceLast = now - state._resyncChunkLastAt;
    if (sinceLast >= RESYNC_THROTTLE_MS) {
      if (state._resyncChunkTailTimer) {
        clearTimeout(state._resyncChunkTailTimer);
        state._resyncChunkTailTimer = null;
      }
      state._resyncChunkLastAt = now;
      softResyncTerminal2();
      return;
    }
    if (state._resyncChunkTailTimer) return;
    var wait = Math.max(RESYNC_TAIL_MS, RESYNC_THROTTLE_MS - sinceLast);
    state._resyncChunkTailTimer = setTimeout(function() {
      state._resyncChunkTailTimer = null;
      state._resyncChunkLastAt = Date.now();
      softResyncTerminal2();
    }, wait);
  }
  function syncTerminalBuffer(sessionId, output, options) {
    if (!state.terminal) return false;
    var normalizedOutput = normalizeTerminalOutput(output || "");
    var nextSessionId = sessionId || null;
    var opts = options || {};
    var mode = opts.mode || "append";
    var shouldScroll = opts.scroll !== false;
    var sessionChanged = state.terminalSessionId !== nextSessionId;
    var currentOutput = state.terminalOutput || "";
    var liveChunkStream = !!(nextSessionId && state.terminalLiveStreamSessions[nextSessionId]);
    var wrote = false;
    if (normalizedOutput === currentOutput && !sessionChanged) {
      if (shouldScroll) maybeScrollTerminalToBottom("output");
      updateTerminalJumpToBottomButton();
      return false;
    }
    if (sessionChanged) {
      resetTerminal3();
      currentOutput = "";
      state.terminalOutput = "";
      state.terminalOutputMarker = 0;
      state.terminalAutoFollow = true;
      clearTerminalScrollIdleTimer();
      updateTerminalJumpToBottomButton();
    }
    if (mode === "replace") {
      if (normalizedOutput !== currentOutput) {
        resetTerminal3();
        if (normalizedOutput) {
          wandTerminalWrite(state.terminal, normalizedOutput);
        }
        wrote = true;
      }
    } else if (normalizedOutput.length < currentOutput.length && !sessionChanged) {
      return false;
    } else if (liveChunkStream && !sessionChanged && mode !== "replace" && currentOutput && !normalizedOutput.startsWith(currentOutput)) {
      return false;
    } else if (normalizedOutput.startsWith(currentOutput)) {
      var delta = normalizedOutput.slice(currentOutput.length);
      if (delta) {
        wandTerminalWrite(state.terminal, delta);
        wrote = true;
      }
    } else if (currentOutput && currentOutput.startsWith(normalizedOutput)) {
      return false;
    } else {
      resetTerminal3();
      if (normalizedOutput) {
        wandTerminalWrite(state.terminal, normalizedOutput);
      }
      wrote = true;
    }
    state.terminalSessionId = nextSessionId;
    state.terminalOutput = normalizedOutput;
    state.terminalOutputMarker = 0;
    if (shouldScroll && (wrote || sessionChanged || mode === "replace")) {
      maybeScrollTerminalToBottom(sessionChanged || mode === "replace" ? "force" : "output");
    } else {
      updateTerminalJumpToBottomButton();
    }
    if (sessionChanged) {
      sendTerminalResize(state.terminal.cols, state.terminal.rows);
    }
    return wrote || sessionChanged;
  }
  function initTerminal2() {
    var container = document.getElementById("output");
    if (!container || state.terminal || state.terminalInitializing) return;
    if (typeof WTermLib === "undefined" || !WTermLib.WTerm) {
      if (!state.terminalInitRetries) state.terminalInitRetries = 0;
      if (state.terminalInitRetries < 10) {
        state.terminalInitRetries++;
        setTimeout(initTerminal2, 200);
      }
      return;
    }
    state.terminalInitRetries = 0;
    state.terminalInitializing = true;
    if (state.selectedId) {
      container.classList.remove("hidden");
      container.classList.add("active");
    }
    var staleWraps = container.querySelectorAll(".terminal-scroll-wrap");
    for (var i = 0; i < staleWraps.length; i++) {
      var stale = staleWraps[i];
      if (stale.parentNode === container) container.removeChild(stale);
    }
    var termWrap = document.createElement("div");
    termWrap.className = "terminal-scroll-wrap";
    container.appendChild(termWrap);
    var term = new WTermLib.WTerm(termWrap, {
      cols: 120,
      rows: 36,
      autoResize: true,
      cursorBlink: false,
      onData: function() {
        return;
      },
      onResize: function(cols, rows) {
        sendTerminalResize(cols, rows);
        if (state.terminal && state.terminalOutput) {
          softResyncTerminal2({ skipFit: true });
        }
      }
    });
    var fontsReady = document.fonts && typeof document.fonts.ready === "object" ? Promise.race([document.fonts.ready, new Promise(function(r) {
      setTimeout(r, 800);
    })]) : Promise.resolve();
    fontsReady.then(function() {
      return term.init();
    }).then(function() {
      state.terminal = term;
      state.terminalInitializing = false;
      applyTerminalScale();
      if (termWrap.isConnected) {
        void termWrap.offsetHeight;
        if (typeof term.remeasure === "function") {
          try {
            term.remeasure();
          } catch (e) {
          }
        }
      }
      state.terminalAutoFollow = true;
      clearTerminalScrollIdleTimer();
      var viewport = getTerminalViewport();
      if (viewport) {
        state.terminalViewportScrollHandler = function() {
          if (Date.now() < state.terminalProgrammaticScrollUntil) {
            updateTerminalJumpToBottomButton();
            return;
          }
          if (isTerminalAtBottom()) {
            state.terminalAutoFollow = true;
            clearTerminalScrollIdleTimer();
            updateTerminalJumpToBottomButton();
            return;
          }
          setTerminalManualScrollActive();
        };
        state.terminalViewportTouchStartHandler = function(e) {
          if (e.touches && e.touches.length === 1) {
            state.terminalTouchStartY = e.touches[0].clientY;
          }
        };
        state.terminalViewportTouchHandler = function(e) {
          if (!e.touches || e.touches.length !== 1) return;
          if (typeof state.terminalTouchStartY !== "number") return;
          if (e.touches[0].clientY - state.terminalTouchStartY > 4) {
            setTerminalManualScrollActive();
          }
        };
        viewport.addEventListener("scroll", state.terminalViewportScrollHandler, { passive: true });
        viewport.addEventListener("touchstart", state.terminalViewportTouchStartHandler, { passive: true });
        viewport.addEventListener("touchmove", state.terminalViewportTouchHandler, { passive: true });
      }
      state.terminalWheelHandler = function(e) {
        if (e.deltaY < 0) {
          setTerminalManualScrollActive();
        }
        e.stopPropagation();
      };
      container.addEventListener("wheel", state.terminalWheelHandler, { passive: true });
      initTerminalScrollbar2(container);
      if (state.selectedId) {
        var session = state.sessions.find(function(s) {
          return s.id === state.selectedId;
        });
        if (session) {
          syncTerminalBuffer(session.id, session.output || "", { mode: "append", scroll: false });
        }
      } else {
        wandTerminalWrite(term, "\u70B9\u51FB\u4E0A\u65B9\u300C\u65B0\u5BF9\u8BDD\u300D\u5F00\u59CB\u4F60\u7684\u7B2C\u4E00\u6B21\u5BF9\u8BDD\u3002\r\n");
      }
      state.terminalClickHandler = function(e) {
        if (hasActiveTerminalSelection()) return;
        focusInputBox2(e);
      };
      container.addEventListener("click", state.terminalClickHandler);
      updateTerminalJumpToBottomButton();
      initTerminalResizeHandle();
      initTerminalJoystick();
      observeTerminalResize();
      startTerminalHealthCheck();
      ensureTerminalFit("mount", { forceReplay: true });
      if (document.documentElement.classList.contains("is-wand-embed-terminal")) {
        [120, 350, 700].forEach(function(delay) {
          setTimeout(function() {
            if (state.terminal) ensureTerminalFit("embed-settle");
          }, delay);
        });
      }
    }).catch(function(err) {
      state.terminalInitializing = false;
      console.error("[wand] wterm init failed:", err);
    });
  }

  // src/web-ui/browser/events.ts
  function __fetchToolContent(toolUseId, callback) {
    if (!state.selectedId || !toolUseId) return;
    var cacheKey = state.selectedId + ":" + toolUseId;
    if (state.toolContentCache[cacheKey]) {
      callback(null, state.toolContentCache[cacheKey]);
      return;
    }
    fetch("/api/sessions/" + encodeURIComponent(state.selectedId) + "/tool-content/" + encodeURIComponent(toolUseId), { credentials: "same-origin" }).then(function(res) {
      return res.json();
    }).then(function(data) {
      if (data.error) {
        callback(data.error, null);
      } else {
        state.toolContentCache[cacheKey] = data;
        callback(null, data);
      }
    }).catch(function() {
      callback("\u52A0\u8F7D\u5931\u8D25", null);
    });
  }
  function getCardDefault(key) {
    return !!(state.config && state.config.cardDefaults && state.config.cardDefaults[key]);
  }
  function lazyLoadTruncatedToolContent(container, targetEl, renderContent, renderError) {
    if (!container || container.dataset.truncated !== "true" || container.dataset.loaded === "true") return;
    var toolUseId = container.dataset.toolUseId;
    if (!toolUseId) return;
    if (targetEl) targetEl.innerHTML = '<div class="tool-content-loading">\u52A0\u8F7D\u4E2D\u2026</div>';
    container.dataset.loaded = "loading";
    __fetchToolContent(toolUseId, function(err, data) {
      if (err) {
        if (targetEl) targetEl.innerHTML = renderError || '<div class="tool-content-error">\u52A0\u8F7D\u5931\u8D25\uFF0C\u70B9\u51FB\u91CD\u8BD5</div>';
        container.dataset.loaded = "";
        return;
      }
      container.dataset.truncated = "false";
      container.dataset.loaded = "true";
      var content = typeof data.content === "string" ? data.content : JSON.stringify(data.content);
      renderContent(content, data);
    });
  }
  window.__tcToggle = function(e, headerEl) {
    var card = headerEl.closest(".tool-use-card") || headerEl.closest(".inline-diff");
    if (card) {
      var wasCollapsed = card.classList.contains("collapsed");
      card.classList.toggle("collapsed");
      var isExpanded = wasCollapsed;
      headerEl.setAttribute("aria-expanded", isExpanded ? "true" : "false");
      var cardBody = card.querySelector(".tool-use-body, .diff-body");
      if (cardBody) cardBody.setAttribute("aria-hidden", isExpanded ? "false" : "true");
      var expandKind = card.dataset.expandKind || "tool-card";
      persistElementExpandState(card, expandKind);
      if (wasCollapsed) {
        var resultDiv = card.querySelector(".tool-use-result");
        lazyLoadTruncatedToolContent(
          card,
          resultDiv,
          function(content) {
            if (resultDiv) resultDiv.innerHTML = '<pre class="tool-use-result-content">' + escapeHtml(content) + "</pre>";
          },
          `<div class="tool-content-error" onclick="__tcToggle(null, this.closest('.tool-use-card,.inline-diff').querySelector('.tool-use-header,.diff-header'))">\u52A0\u8F7D\u5931\u8D25\uFF0C\u70B9\u51FB\u91CD\u8BD5</div>`
        );
      }
    }
    if (e) {
      e.preventDefault();
      e.stopPropagation();
    }
  };
  window.__thinkingToggle = function(el) {
    var isCollapsed = el.classList.contains("collapsed");
    if (isCollapsed) {
      el.classList.remove("collapsed");
      el.classList.add("expanded");
      el.querySelector(".thinking-inline-preview").textContent = el.dataset.thinking || "";
      var action = el.querySelector(".thinking-inline-action");
      if (action) action.textContent = "\u6536\u8D77";
    } else {
      el.classList.remove("expanded");
      el.classList.add("collapsed");
      var preview = (el.dataset.thinking || "").slice(0, 57) + ((el.dataset.thinking || "").length > 60 ? "\u2026" : "");
      el.querySelector(".thinking-inline-preview").textContent = preview;
      var action = el.querySelector(".thinking-inline-action");
      if (action) action.textContent = "\u5C55\u5F00";
    }
    persistElementExpandState(el, "thinking");
  };
  window.__subagentReplyToggle = function(e, target) {
    if (e) {
      e.preventDefault();
      e.stopPropagation();
    }
    var bubble = target && target.closest ? target.closest(".subagent-reply") : null;
    if (!bubble) return;
    var expanded = bubble.getAttribute("data-expanded") === "true";
    applyExpandedState(bubble, "subagent-reply", !expanded);
    persistElementExpandState(bubble, "subagent-reply");
  };
  window.__subagentPanelToggle = function(e, target) {
    if (e) {
      e.preventDefault();
      e.stopPropagation();
    }
    var panel = target && target.closest ? target.closest(".subagent-panel") : null;
    if (!panel) return;
    panel.setAttribute("data-expanded", "true");
  };
  function snapCollapsedSubagentPanelsToBottom(container) {
    if (!container) return;
    var panels = container.querySelectorAll('.subagent-panel[data-follow-tail="true"]');
    for (var i = 0; i < panels.length; i++) {
      var body = panels[i].querySelector(".subagent-panel-body");
      if (!body) continue;
      body.scrollTop = body.scrollHeight;
    }
  }
  window.__openFilePreview = function(p) {
    if (p) openFilePreview(p);
  };
  window.__inlineToolToggle = function(el) {
    var expanded = el.classList.toggle("inline-tool-open");
    var body = el.querySelector(".inline-tool-expanded");
    if (body) {
      body.style.display = expanded ? "block" : "none";
    }
    var statusSpan = el.querySelector(".inline-tool-status");
    if (statusSpan) {
      if (el.dataset.status === "error") {
        statusSpan.textContent = "\u2717";
      } else if (el.dataset.status === "done") {
        statusSpan.textContent = "\u2713";
      }
    }
    if (expanded) {
      lazyLoadTruncatedToolContent(el, body, function(content) {
        el.dataset.result = content;
        if (body) body.innerHTML = '<div class="inline-tool-result">' + formatInlineResult(content, "") + "</div>";
      });
    }
    persistElementExpandState(el, "inline-tool");
  };
  window.__terminalExpand = function(el) {
    var container = el.closest(".inline-terminal");
    if (!container) return;
    var body = container.querySelector(".term-body");
    if (body) {
      var isHidden2 = body.style.display === "none";
      body.style.display = isHidden2 ? "block" : "none";
      container.dataset.expanded = isHidden2 ? "true" : "false";
      el.setAttribute("aria-expanded", isHidden2 ? "true" : "false");
      body.setAttribute("aria-hidden", isHidden2 ? "false" : "true");
      var toggleIcon = el.querySelector(".term-toggle-icon");
      if (toggleIcon) toggleIcon.textContent = isHidden2 ? "\u25BC" : "\u25B6";
      persistElementExpandState(container, "terminal");
      if (isHidden2) {
        var termOutput = body.querySelector(".term-output");
        lazyLoadTruncatedToolContent(container, termOutput, function(content) {
          if (termOutput) {
            var lines = content.split("\n");
            var html = "";
            for (var i = 0; i < lines.length; i++) {
              if (!lines[i] && i === lines.length - 1) continue;
              html += '<div class="term-line">' + escapeHtml(lines[i]) + "</div>";
            }
            termOutput.innerHTML = html;
          }
        });
      }
    }
  };
  window.__askSelect = function(toolUseId, qIdx, optIdx, isMulti) {
    var sel = state.askUserSelections[toolUseId];
    if (!sel) {
      sel = { submitted: false };
      state.askUserSelections[toolUseId] = sel;
    }
    if (sel.submitted) return;
    var current = sel[qIdx] || [];
    if (isMulti) {
      var pos = current.indexOf(optIdx);
      if (pos === -1) {
        current.push(optIdx);
      } else {
        current.splice(pos, 1);
      }
    } else {
      current = current[0] === optIdx ? [] : [optIdx];
    }
    sel[qIdx] = current;
    window.__askRender(toolUseId);
  };
  window.__askRender = function(toolUseId) {
    var card = document.querySelector('[data-tool-use-id="' + toolUseId + '"]');
    if (!card) return;
    var sel = state.askUserSelections[toolUseId] || {};
    card.querySelectorAll(".ask-user-option").forEach(function(btn) {
      var qIdx = parseInt(btn.dataset.questionIndex, 10);
      var oIdx = parseInt(btn.dataset.optionIndex, 10);
      var chosen = (sel[qIdx] || []).indexOf(oIdx) !== -1;
      btn.classList.toggle("selected", chosen);
    });
    var submitBtn = card.querySelector(".ask-user-submit");
    if (submitBtn) {
      var groups = card.querySelectorAll(".ask-user-question-group");
      var allAnswered = true;
      groups.forEach(function(g, i) {
        if (!sel[i] || sel[i].length === 0) allAnswered = false;
      });
      submitBtn.disabled = !allAnswered || !!sel.submitted;
      if (sel.submitted) {
        submitBtn.textContent = "\u5DF2\u63D0\u4EA4...";
        submitBtn.classList.add("ask-user-submitted");
      }
    }
  };
  window.__askSubmit = function(toolUseId) {
    var sel = state.askUserSelections[toolUseId];
    if (!sel || sel.submitted || !state.selectedId) return;
    var card = document.querySelector('[data-tool-use-id="' + toolUseId + '"]');
    if (!card) return;
    var groups = card.querySelectorAll(".ask-user-question-group");
    var lines = [];
    var allAnswered = true;
    groups.forEach(function(group, qIdx) {
      var selected = sel[qIdx] || [];
      if (selected.length === 0) {
        allAnswered = false;
        return;
      }
      var labels = [];
      selected.forEach(function(optIdx) {
        var btn = group.querySelector('[data-option-index="' + optIdx + '"]');
        if (btn) labels.push(btn.dataset.optionLabel);
      });
      lines.push(labels.join(", "));
    });
    if (!allAnswered) return;
    sel.submitted = true;
    window.__askRender(toolUseId);
    var answerText = lines.join("\n");
    fetch("/api/sessions/" + state.selectedId + "/input", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      credentials: "same-origin",
      body: JSON.stringify({ input: answerText + "\n", view: state.currentView })
    }).catch(function(err) {
      console.error("[wand] Error sending answer:", err);
      sel.submitted = false;
      window.__askRender(toolUseId);
    });
  };
  function bindGlobalListenersOnce() {
    if (state.__globalListenersBound) return;
    state.__globalListenersBound = true;
    document.addEventListener("click", function() {
      if (isBrowserReactShellMounted()) return;
      var el = document.getElementById("sidebar-overflow-menu");
      if (el) el.classList.remove("open");
    });
    window.addEventListener("resize", function() {
      if (isBrowserReactShellMounted()) return;
      var el = document.getElementById("sidebar-overflow-menu");
      if (el) el.classList.remove("open");
    });
    var closeTopbarMore = function() {
      if (isBrowserReactShellMounted()) return;
      state.topbarMoreOpen = false;
      var menu = document.getElementById("topbar-more-menu");
      var btn = document.getElementById("topbar-more-button");
      if (menu) menu.classList.add("hidden");
      if (btn) {
        btn.classList.remove("active");
        btn.setAttribute("aria-expanded", "false");
      }
    };
    document.addEventListener("click", function(e) {
      if (isBrowserReactShellMounted()) return;
      if (!state.topbarMoreOpen) return;
      var menu = document.getElementById("topbar-more-menu");
      var wrap = menu && menu.parentElement;
      if (wrap && !wrap.contains(e.target)) closeTopbarMore();
    });
    document.addEventListener("keydown", function(e) {
      if (isBrowserReactShellMounted()) return;
      if (e.key === "Escape" && state.topbarMoreOpen) closeTopbarMore();
    });
    document.addEventListener("click", function(e) {
      if (!state.plusPopoverOpen) return;
      var pop = document.getElementById("composer-plus-popover");
      var btn = document.getElementById("attach-btn");
      if (pop && pop.contains(e.target)) return;
      if (btn && btn.contains(e.target)) return;
      closePlusPopover();
    });
    document.addEventListener("keydown", function(e) {
      if (e.key === "Escape" && state.plusPopoverOpen) closePlusPopover();
    });
    document.addEventListener("click", function(e) {
      var target = e.target;
      if (!target || typeof target.closest !== "function") return;
      var toggle = target.closest("#auto-approve-toggle");
      if (!toggle) return;
      e.preventDefault();
      toggleAutoApprove();
    });
    document.addEventListener("change", function(e) {
      var target = e.target;
      if (!target || target.nodeType !== 1) return;
      if (typeof target.matches !== "function" || !target.matches("[data-mode-control]")) return;
      var ctrl = target.getAttribute("data-mode-control");
      var value = target.value;
      if (ctrl === "mode") {
        onChatModeChange(value);
      } else if (ctrl === "model") {
        onChatModelChange(value);
      } else if (ctrl === "thinking") {
        onChatThinkingChange(value);
      }
      if (target.closest && target.closest("#composer-plus-popover")) closePlusPopover();
    });
  }
  function attachEventListeners() {
    bindGlobalListenersOnce();
    var loginButton = document.getElementById("login-button");
    if (loginButton) {
      loginButton.addEventListener("click", login);
      var loginForm = document.getElementById("login-form");
      if (loginForm) loginForm.addEventListener("submit", function(e) {
        e.preventDefault();
        login();
      });
      var loginSwitchServerBtn = document.getElementById("login-switch-server-button");
      if (loginSwitchServerBtn) loginSwitchServerBtn.addEventListener("click", switchServer);
      var passwordEl = document.getElementById("password");
      var togglePasswordButton = document.getElementById("toggle-password-button");
      if (togglePasswordButton && passwordEl) {
        togglePasswordButton.addEventListener("click", function() {
          var visible = passwordEl.type === "text";
          passwordEl.type = visible ? "password" : "text";
          togglePasswordButton.textContent = visible ? "\u663E\u793A" : "\u9690\u85CF";
          togglePasswordButton.setAttribute("aria-label", visible ? "\u663E\u793A\u5BC6\u7801" : "\u9690\u85CF\u5BC6\u7801");
          togglePasswordButton.setAttribute("aria-pressed", visible ? "false" : "true");
          passwordEl.focus();
        });
      }
      if (passwordEl) {
        passwordEl.addEventListener("keydown", function(e) {
          if (e.key === "Enter") login();
        });
        passwordEl.addEventListener("input", function() {
          passwordEl.dataset.error = "false";
          passwordEl.setAttribute("aria-invalid", "false");
          var errorEl = document.getElementById("login-error");
          if (errorEl) hideError(errorEl);
        });
        passwordEl.focus();
      }
      return;
    }
    var reactShellActive = isBrowserReactShellMounted();
    if (!reactShellActive) {
      var welcomeInput = document.getElementById("welcome-input");
      if (welcomeInput) {
        welcomeInput.addEventListener("keydown", function(e) {
          if (e.key === "Enter" && !e.shiftKey) {
            e.preventDefault();
            welcomeInputSend();
          }
        });
        welcomeInput.focus();
      }
      var welcomeSendBtn = document.getElementById("welcome-send-btn");
      if (welcomeSendBtn) {
        welcomeSendBtn.addEventListener("click", function() {
          welcomeInputSend();
        });
      }
      var welcomeClaudeBtn = document.getElementById("welcome-tool-claude");
      if (welcomeClaudeBtn) {
        welcomeClaudeBtn.addEventListener("click", function() {
          quickStartSession();
        });
      }
      var welcomeCodexBtn = document.getElementById("welcome-tool-codex");
      if (welcomeCodexBtn) {
        welcomeCodexBtn.addEventListener("click", function() {
          state.sessionTool = "codex";
          state.preferredCommand = "codex";
          state.modeValue = "full-access";
          quickStartSession();
        });
      }
      var welcomeOpenCodeBtn = document.getElementById("welcome-tool-opencode");
      if (welcomeOpenCodeBtn) {
        welcomeOpenCodeBtn.addEventListener("click", function() {
          state.sessionTool = "opencode";
          state.preferredCommand = "opencode";
          state.modeValue = "managed";
          quickStartSession();
        });
      }
      var welcomeStructuredBtn = document.getElementById("welcome-tool-structured");
      if (welcomeStructuredBtn) {
        welcomeStructuredBtn.addEventListener("click", function() {
          createStructuredSession().then(function() {
            focusInputBox2(true);
          }).catch(function(error) {
            showToast(error && error.message || "\u65E0\u6CD5\u542F\u52A8\u7ED3\u6784\u5316\u4F1A\u8BDD\u3002", "error");
          });
        });
      }
      var sessionsList = document.getElementById("sessions-list");
      if (sessionsList) {
        sessionsList.addEventListener("click", handleSessionItemClick);
        sessionsList.addEventListener("keydown", handleSessionItemKeydown);
        sessionsList.addEventListener("mouseover", handleCollapsedTileHover);
        sessionsList.addEventListener("mouseout", handleCollapsedTileLeave);
        initSwipeToDelete(sessionsList);
      }
      window.addEventListener("scroll", hideCollapsedTileBubble, true);
      window.addEventListener("resize", hideCollapsedTileBubble);
      var sessionsToggle = document.getElementById("sessions-toggle-button");
      if (sessionsToggle) sessionsToggle.addEventListener("click", toggleSessionsDrawer);
      var drawerBackdrop = document.getElementById("sessions-drawer-backdrop");
      if (drawerBackdrop) drawerBackdrop.addEventListener("click", closeSessionsDrawer2);
      var closeDrawerBtn = document.getElementById("close-drawer-button");
      if (closeDrawerBtn) closeDrawerBtn.addEventListener("click", closeSessionsDrawer2);
      var collapseBtn = document.getElementById("sidebar-collapse-btn");
      if (collapseBtn) collapseBtn.addEventListener("click", toggleSidebarCollapsed);
      var pinBtn = document.getElementById("sidebar-pin-btn");
      if (pinBtn) pinBtn.addEventListener("click", toggleSidebarPin);
      var sidebarMoreBtn = document.getElementById("sidebar-more-btn");
      var sidebarOverflow = document.getElementById("sidebar-overflow-menu");
      if (sidebarMoreBtn && sidebarOverflow) {
        sidebarMoreBtn.addEventListener("click", function(e) {
          e.stopPropagation();
          var willOpen = !sidebarOverflow.classList.contains("open");
          sidebarOverflow.classList.toggle("open", willOpen);
          if (willOpen) positionSidebarOverflowMenu(sidebarOverflow);
        });
      }
      var homeBtn = document.getElementById("sidebar-home-btn");
      if (homeBtn) homeBtn.addEventListener("click", function() {
        state.selectedId = null;
        persistSelectedId();
        resetChatRenderCache();
        dismissDrawerIfOverlay();
        render();
      });
      var refreshBtn = document.getElementById("sidebar-refresh-btn");
      if (refreshBtn) refreshBtn.addEventListener("click", function() {
        window.location.reload();
      });
      var logoutBtn = document.getElementById("logout-button");
      if (logoutBtn) logoutBtn.addEventListener("click", logout2);
      var switchServerBtn = document.getElementById("switch-server-button");
      if (switchServerBtn) switchServerBtn.addEventListener("click", switchServer);
      var backToNativeBtn = document.getElementById("back-to-native-button");
      if (backToNativeBtn) backToNativeBtn.addEventListener("click", backToNativeApp);
      var settingsBtn = document.getElementById("settings-button");
      if (settingsBtn) settingsBtn.addEventListener("click", openSettingsModal);
      var newSessBtn = document.getElementById("topbar-new-session-button");
      if (newSessBtn) newSessBtn.addEventListener("click", openSessionModal);
      var drawerNewSessBtn = document.getElementById("drawer-new-session-button");
      if (drawerNewSessBtn) drawerNewSessBtn.addEventListener("click", openSessionModal);
    }
    var approvePermissionBtn = document.getElementById("approve-permission-btn");
    if (approvePermissionBtn) approvePermissionBtn.addEventListener("click", approvePermission);
    var denyPermissionBtn = document.getElementById("deny-permission-btn");
    if (denyPermissionBtn) denyPermissionBtn.addEventListener("click", denyPermission);
    var sendBtn = document.getElementById("send-input-button");
    if (sendBtn) sendBtn.addEventListener("click", function() {
      dismissDrawerIfOverlay();
      sendOrStart();
    });
    var stopBtn = document.getElementById("stop-button");
    if (stopBtn) stopBtn.addEventListener("click", stopSession);
    var inputBox = document.getElementById("input-box");
    if (inputBox) {
      bindInputTouchScroll(inputBox);
      inputBox.addEventListener("keydown", handleInputBoxKeydown);
      inputBox.addEventListener("paste", handleInputPaste);
      inputBox.addEventListener("input", function() {
        if (state.terminalComposing) return;
        if (handleInteractiveTextInput(inputBox)) {
          return;
        }
        refreshInputBoxState(inputBox);
        setDraftValue(inputBox.value, true);
        syncComposerHasText(inputBox);
      });
      inputBox.addEventListener("compositionstart", function() {
        if (state.terminalInteractive) state.terminalComposing = true;
      });
      inputBox.addEventListener("compositionend", function() {
        if (!state.terminalComposing) return;
        state.terminalComposing = false;
        if (state.terminalInteractive) handleInteractiveTextInput(inputBox);
      });
      inputBox.addEventListener("focus", function() {
        dismissDrawerIfOverlay();
        handleInputBoxFocus({ target: inputBox });
      });
      inputBox.addEventListener("blur", handleInputBoxBlur2);
    }
    var attachBtn = document.getElementById("attach-btn");
    var fileInput = document.getElementById("file-upload-input");
    var plusPopover = document.getElementById("composer-plus-popover");
    var plusAttachItem = document.getElementById("plus-attach-item");
    if (attachBtn && plusPopover) {
      attachBtn.addEventListener("click", function(e) {
        e.stopPropagation();
        togglePlusPopover();
      });
    }
    if (plusAttachItem && fileInput) {
      plusAttachItem.addEventListener("click", function() {
        closePlusPopover();
        fileInput.click();
      });
    }
    if (fileInput) {
      fileInput.addEventListener("change", function() {
        var files = fileInput.files;
        if (files) {
          for (var i = 0; i < files.length; i++) addPendingAttachment(files[i]);
        }
        fileInput.value = "";
      });
    }
    var voiceRecordBtn = document.getElementById("voice-record-btn");
    if (voiceRecordBtn) {
      voiceRecordBtn.addEventListener("pointerdown", startVoiceRecording);
      voiceRecordBtn.addEventListener("pointermove", handleVoiceMove);
      voiceRecordBtn.addEventListener("pointerup", stopVoiceRecording);
      voiceRecordBtn.addEventListener("pointercancel", cancelVoiceRecording);
      voiceRecordBtn.addEventListener("contextmenu", function(e) {
        e.preventDefault();
      });
    }
    var promptOptimizeBtn = document.getElementById("prompt-optimize-btn");
    if (promptOptimizeBtn) {
      promptOptimizeBtn.addEventListener("click", function() {
        optimizePromptText();
      });
    }
    var composer = document.querySelector(".input-composer");
    if (composer) {
      composer.addEventListener("dragover", function(e) {
        e.preventDefault();
        e.stopPropagation();
        composer.classList.add("drag-over");
      });
      composer.addEventListener("dragleave", function(e) {
        e.preventDefault();
        e.stopPropagation();
        composer.classList.remove("drag-over");
      });
      composer.addEventListener("drop", function(e) {
        e.preventDefault();
        e.stopPropagation();
        composer.classList.remove("drag-over");
        var files = e.dataTransfer && e.dataTransfer.files;
        if (files) {
          for (var i = 0; i < files.length; i++) addPendingAttachment(files[i]);
        }
      });
    }
    var terminalInteractiveToggles = ["terminal-interactive-toggle-top"];
    terminalInteractiveToggles.forEach(function(id) {
      var toggle = document.getElementById(id);
      if (toggle) toggle.addEventListener("click", toggleTerminalInteractive);
    });
    if (!reactShellActive) {
      var filePanelToggle = document.getElementById("file-panel-toggle-btn");
      if (filePanelToggle) filePanelToggle.addEventListener("click", toggleFilePanel);
      var filePanelClose = document.getElementById("file-side-panel-close");
      if (filePanelClose) filePanelClose.addEventListener("click", closeFilePanel);
      var filePanelBackdrop = document.getElementById("file-panel-backdrop");
      if (filePanelBackdrop) filePanelBackdrop.addEventListener("click", closeFilePanel);
      var topbarFileBtn = document.getElementById("topbar-file-button");
      if (topbarFileBtn) topbarFileBtn.addEventListener("click", toggleFilePanel);
      var topbarCwdEl = document.getElementById("topbar-cwd");
      if (topbarCwdEl) {
        topbarCwdEl.addEventListener("click", function() {
          if (!state.filePanelOpen) toggleFilePanel();
        });
        topbarCwdEl.addEventListener("keydown", function(e) {
          if (e.key === "Enter" || e.key === " ") {
            e.preventDefault();
            if (!state.filePanelOpen) toggleFilePanel();
          }
        });
      }
      var topbarMoreBtn = document.getElementById("topbar-more-button");
      var topbarMoreMenu = document.getElementById("topbar-more-menu");
      if (topbarMoreBtn && topbarMoreMenu) {
        topbarMoreBtn.addEventListener("click", function(e) {
          e.stopPropagation();
          state.topbarMoreOpen = !state.topbarMoreOpen;
          topbarMoreMenu.classList.toggle("hidden", !state.topbarMoreOpen);
          topbarMoreBtn.classList.toggle("active", state.topbarMoreOpen);
          topbarMoreBtn.setAttribute("aria-expanded", state.topbarMoreOpen ? "true" : "false");
        });
        topbarMoreMenu.addEventListener("click", function(e) {
          var btn = e.target && e.target.closest ? e.target.closest(".topbar-more-item") : null;
          if (!btn) return;
          var action = btn.getAttribute("data-action");
          state.topbarMoreOpen = false;
          topbarMoreMenu.classList.add("hidden");
          topbarMoreBtn.classList.remove("active");
          topbarMoreBtn.setAttribute("aria-expanded", "false");
          switch (action) {
            case "copy-claude-session-id":
              var copyProvider = getSelectedSession() && getSelectedSession().provider;
              copySelectedSessionField("claudeSessionId", copyProvider === "codex" ? "Codex thread ID \u5DF2\u590D\u5236" : copyProvider === "opencode" ? "OpenCode session ID \u5DF2\u590D\u5236" : "Claude \u4F1A\u8BDD ID \u5DF2\u590D\u5236");
              break;
            case "copy-cwd":
              copySelectedSessionField("cwd", "\u5DE5\u4F5C\u76EE\u5F55\u5DF2\u590D\u5236");
              break;
            case "copy-session-id":
              copySelectedSessionField("id", "\u4F1A\u8BDD ID \u5DF2\u590D\u5236");
              break;
            case "worktree-merge":
              if (state.selectedId) openWorktreeMergeModal(state.selectedId);
              break;
            case "worktree-cleanup":
              if (state.selectedId) retryWorktreeCleanup(state.selectedId);
              break;
            case "delete-session":
              if (state.selectedId) {
                (function(pendingId) {
                  confirmDelete("\u786E\u5B9A\u8981\u5220\u9664\u5F53\u524D\u4F1A\u8BDD\u5417\uFF1F\u6B64\u64CD\u4F5C\u65E0\u6CD5\u64A4\u9500\u3002", { title: "\u5220\u9664\u5F53\u524D\u4F1A\u8BDD" }).then(function(ok) {
                    if (ok) deleteSession(pendingId);
                  });
                })(state.selectedId);
              }
              break;
          }
        });
      }
    }
    var scaleDownBtn = document.getElementById("terminal-scale-down-top");
    var scaleUpBtn = document.getElementById("terminal-scale-up-top");
    if (scaleDownBtn) scaleDownBtn.addEventListener("click", function() {
      adjustTerminalScale(-0.25);
    });
    if (scaleUpBtn) scaleUpBtn.addEventListener("click", function() {
      adjustTerminalScale(0.25);
    });
    var pageRefreshBtn = document.getElementById("page-refresh-btn");
    if (pageRefreshBtn) pageRefreshBtn.addEventListener("click", function(ev) {
      if (ev && ev.shiftKey) {
        location.reload();
        return;
      }
      softResyncTerminal2();
      resetChatRenderCache({ preserveStickState: true });
      scheduleChatRender3(true);
    });
    var jumpBottomBtn = document.getElementById("terminal-jump-bottom");
    if (jumpBottomBtn) jumpBottomBtn.addEventListener("click", function() {
      maybeScrollTerminalToBottom("force");
    });
    var chatUnreadBubble = document.getElementById("chat-unread-bubble");
    if (chatUnreadBubble) chatUnreadBubble.addEventListener("click", function() {
      scrollChatToBottom(true);
    });
    if (!reactShellActive) {
      var fileRefresh = document.getElementById("file-explorer-refresh");
      if (fileRefresh) fileRefresh.addEventListener("click", function() {
        refreshFileExplorer();
      });
      var fileUp = document.getElementById("file-explorer-up");
      if (fileUp) fileUp.addEventListener("click", navigateExplorerUp);
      var fileCwdInput = document.getElementById("file-explorer-cwd");
      if (fileCwdInput && fileCwdInput.tagName === "INPUT") {
        var lastCommittedCwd = fileCwdInput.value;
        var normalizeCwdInput = function(raw) {
          var s = (raw || "").trim();
          if (!s) return "";
          s = s.replace(/\/{2,}/g, "/");
          if (s.length > 1) s = s.replace(/\/+$/, "");
          return s;
        };
        fileCwdInput.addEventListener("focus", function() {
          lastCommittedCwd = fileCwdInput.value;
          setTimeout(function() {
            try {
              fileCwdInput.select();
            } catch (e) {
            }
          }, 0);
        });
        fileCwdInput.addEventListener("keydown", function(e) {
          if (e.key === "Enter") {
            e.preventDefault();
            var next = normalizeCwdInput(fileCwdInput.value);
            if (!next) return;
            lastCommittedCwd = next;
            fileCwdInput.value = next;
            refreshFileExplorer({ cwd: next });
            fileCwdInput.blur();
          } else if (e.key === "Escape") {
            e.preventDefault();
            fileCwdInput.value = lastCommittedCwd;
            fileCwdInput.blur();
          }
        });
        fileCwdInput.addEventListener("blur", function() {
          var next = normalizeCwdInput(fileCwdInput.value);
          if (!next) {
            fileCwdInput.value = lastCommittedCwd;
            return;
          }
          if (next === lastCommittedCwd) {
            fileCwdInput.value = next;
            return;
          }
          lastCommittedCwd = next;
          fileCwdInput.value = next;
          refreshFileExplorer({ cwd: next });
        });
      }
      var fileSearchInput = document.getElementById("file-search-input");
      var fileSearchClear = document.getElementById("file-search-clear");
      if (fileSearchInput) {
        fileSearchInput.addEventListener("input", function(e) {
          state.fileSearchQuery = e.target.value.trim();
          if (fileSearchClear) {
            fileSearchClear.classList.toggle("visible", state.fileSearchQuery.length > 0);
          }
          filterFileTree();
        });
      }
      if (fileSearchClear) {
        fileSearchClear.addEventListener("click", function() {
          state.fileSearchQuery = "";
          if (fileSearchInput) {
            fileSearchInput.value = "";
          }
          fileSearchClear.classList.remove("visible");
        });
      }
      var topbarGitBadge = document.getElementById("topbar-git-badge");
      if (topbarGitBadge) {
        topbarGitBadge.addEventListener("click", function(e) {
          e.preventDefault();
          openQuickCommitModal();
        });
      }
    }
    initTerminal2();
    setupMobileKeyboardHandlers();
    setupVisualViewportHandlers();
    attachQueueBarDelegates();
    updateQueueBar();
  }

  // src/web-ui/browser/render.ts
  function resetChatRenderCache(options) {
    var opts = options || {};
    state.lastRenderedHash = 0;
    state.lastRenderedMsgCount = 0;
    state.lastRenderedEmpty = null;
    state.renderPending = false;
    state.chatRenderedCount = state.chatPageSize;
    state.askUserSelections = {};
    if (state.chatScrollElement) {
      if (state.chatScrollHandler) {
        state.chatScrollElement.removeEventListener("scroll", state.chatScrollHandler);
      }
      if (state.chatScrollWheelHandler) {
        state.chatScrollElement.removeEventListener("wheel", state.chatScrollWheelHandler);
      }
      if (state.chatScrollTouchStartHandler) {
        state.chatScrollElement.removeEventListener("touchstart", state.chatScrollTouchStartHandler);
      }
      if (state.chatScrollTouchMoveHandler) {
        state.chatScrollElement.removeEventListener("touchmove", state.chatScrollTouchMoveHandler);
      }
    }
    state.chatScrollElement = null;
    state.chatScrollHandler = null;
    state.chatScrollWheelHandler = null;
    state.chatScrollTouchStartHandler = null;
    state.chatScrollTouchMoveHandler = null;
    state.chatIsProgrammaticScroll = false;
    if (!opts.preserveStickState) {
      state.chatStickToBottom = true;
      state.chatUnreadCount = 0;
      state.chatUnreadStartIndex = -1;
      state.chatInitialRenderDone = false;
    }
  }
  function getEffectiveCwd() {
    return state.workingDir || getConfigCwd();
  }
  window.addEventListener("online", function() {
    state.isOnline = true;
    updateOfflineBanner();
  });
  window.addEventListener("offline", function() {
    state.isOnline = false;
    updateOfflineBanner();
  });
  function updateOfflineBanner() {
    var banner = document.getElementById("offline-banner");
    if (!state.isOnline && !banner) {
      var el = document.createElement("div");
      el.id = "offline-banner";
      el.className = "offline-banner";
      el.textContent = "You are offline - some features may be limited";
      document.body.appendChild(el);
    } else if (state.isOnline && banner) {
      banner.remove();
    }
  }
  function renderBootLoading() {
    var app = document.getElementById("app");
    if (!app) return;
    app.innerHTML = '<div class="boot-loading"><div class="boot-loading-card"><div class="boot-loading-spinner"></div><div class="boot-loading-text">\u6B63\u5728\u8FDE\u63A5 Wand\u2026</div></div></div>';
  }
  function scheduleForegroundSync(reason, opts) {
    if (!state.config) return;
    if (document.hidden) return;
    var immediate = opts && opts.immediate === true;
    var now = Date.now();
    if (!immediate && now - state.lastForegroundSyncAt < 1500) return;
    state.lastForegroundSyncAt = now;
    if (state.foregroundSyncTimer) {
      clearTimeout(state.foregroundSyncTimer);
      state.foregroundSyncTimer = null;
    }
    syncOnForeground(reason, immediate);
  }
  function syncOnForeground(reason, force) {
    if (!state.config) return Promise.resolve();
    if (document.hidden) return Promise.resolve();
    evaluateWsHeartbeatStale();
    if (force) {
      forceReconnectWebSocket("resume-force");
    } else if (!state.ws || state.ws.readyState !== WebSocket.OPEN && state.ws.readyState !== WebSocket.CONNECTING) {
      initWebSocket();
    }
    if (state.claudeHistoryLoaded) {
      loadClaudeHistory();
    }
    if (state.codexHistoryLoaded) {
      loadCodexHistory();
    }
    return loadSessions({ skipSelectedOutputReload: true }).catch(function(e) {
      console.error("[wand] foreground sync failed:", reason, e);
    });
  }
  function bindForegroundSyncListeners() {
    if (window.__wandForegroundSyncBound) return;
    window.__wandForegroundSyncBound = true;
    document.addEventListener("visibilitychange", function() {
      if (document.hidden) {
        cancelWsReconnect();
      } else {
        scheduleForegroundSync("visibility");
        ensureTerminalFitWithRetry("visibility");
      }
    });
    window.addEventListener("focus", function() {
      scheduleForegroundSync("focus");
    });
    window.addEventListener("pageshow", function() {
      scheduleForegroundSync("pageshow");
    });
    window.addEventListener("resume", function() {
      scheduleForegroundSync("resume");
    });
    window.addEventListener("wand-android-resume", function() {
      scheduleForegroundSync("android-resume", { immediate: true });
      ensureTerminalFitWithRetry("android-resume");
    });
    window.addEventListener("wand-ime-state", function(e) {
      var which = e && e.detail && e.detail.state;
      if (which === "shown" || which === "hidden") {
        try {
          ensureTerminalFit("native-ime-" + which, { forceReplay: true });
          maybeScrollTerminalToBottom("native-ime");
        } catch (_e) {
        }
      }
    });
    window.addEventListener("wand-android-network", function(e) {
      var which = e && e.detail && e.detail.state;
      if (which === "lost") {
        state.isOnline = false;
        try {
          updateOfflineBanner();
        } catch (_e) {
        }
        return;
      }
      if (which === "available" || which === "changed" || which === "validated") {
        state.isOnline = true;
        try {
          updateOfflineBanner();
        } catch (_e) {
        }
        forceReconnectWebSocket("android-network-" + which);
      }
    });
  }
  function restoreLoginSession() {
    fetch("/api/session-check", { credentials: "same-origin" }).then(function(res) {
      return res.ok ? res.json() : { authed: false };
    }).then(function(info) {
      if (!info || !info.authed) {
        state.loginChecked = true;
        render();
        return null;
      }
      return fetch("/api/config", { credentials: "same-origin" }).then(function(res) {
        if (!res.ok) {
          state.loginChecked = true;
          render();
          return null;
        }
        return res.json();
      });
    }).then(function(config) {
      if (!config) return;
      state.config = config;
      state.loginChecked = true;
      requestAnimationFrame(function() {
        try {
          render({ skipShellChrome: true });
        } catch (_e) {
        }
        bindForegroundSyncListeners();
        startPolling();
        refreshAll();
        fetchAvailableModels();
        requestNotificationPermission();
        if (config.updateAvailable && config.latestVersion) {
          notifyUpdateAvailable(config.currentVersion || "-", config.latestVersion);
        }
        if (_apkVersion) {
          checkApkAutoUpdate();
        }
        if (_macAppVersion) {
          checkDmgAutoUpdate();
        }
        if (!state.claudeHistoryLoaded) {
          setTimeout(function() {
            if (!state.claudeHistoryLoaded) ensureClaudeHistoryLoaded();
          }, 600);
        }
      });
    }).catch(function() {
      state.loginChecked = true;
      if (!navigator.onLine) {
        var app = document.getElementById("app");
        if (app) {
          app.innerHTML = '<div class="boot-loading"><div class="boot-loading-card"><div class="boot-loading-text" style="font-size:1.3em;margin-bottom:12px;display:flex;align-items:center;justify-content:center;gap:8px">' + iconSvg("signal", { size: 20, strokeWidth: 1.8 }) + '<span>\u65E0\u6CD5\u8FDE\u63A5\u5230\u670D\u52A1\u5668</span></div><div class="boot-loading-text" style="opacity:0.7;font-size:0.95em">\u8BF7\u68C0\u67E5\u7F51\u7EDC\u8FDE\u63A5\u6216\u786E\u8BA4 Wand \u670D\u52A1\u6B63\u5728\u8FD0\u884C\u3002</div><button onclick="location.reload()" style="margin-top:18px;padding:8px 24px;border-radius:8px;border:1px solid rgba(150,118,85,0.3);background:rgba(255,255,255,0.8);cursor:pointer;font-size:0.95em">\u91CD\u8BD5</button></div></div>';
        }
        window.addEventListener("online", function() {
          location.reload();
        }, { once: true });
        return;
      }
      render();
    });
  }
  document.addEventListener("click", function(e) {
    if (isMobileLayout()) return;
    if (state.sidebarPinned) return;
    if (state.sidebarCollapsed) return;
    if (!state.sessionsDrawerOpen) return;
    var target = e.target;
    if (!target || !(target instanceof Element)) return;
    if (target.closest("#sessions-drawer")) return;
    if (target.closest("#sessions-toggle-button")) return;
    if (target.closest(".floating-sidebar-toggle")) return;
    if (target.closest(".sidebar-tile-bubble")) return;
    if (target.closest(
      ".modal-backdrop, .modal-overlay, .modal-container, [role='dialog'], [role='menu'], .topbar-more-menu, .sidebar-header-overflow, .path-suggestions, .permission-prompt-overlay"
    )) return;
    closeTransientSessionsDrawer();
  }, true);
  renderBootLoading();
  restoreLoginSession();
  function render(options) {
    var skipShellChrome = options && options.skipShellChrome;
    var app = document.getElementById("app");
    if (!app) return;
    var isLoggedIn = state.config !== null;
    var reactShellWasMounted = isBrowserReactShellMounted();
    var shouldResetShell = !isLoggedIn || !reactShellWasMounted && !!document.getElementById("output");
    if (shouldResetShell) {
      teardownTerminal();
    }
    if (!isLoggedIn && reactShellWasMounted) {
      unmountBrowserReactShell();
    }
    document.documentElement.classList.add("no-transition");
    if (state.sidebarPinned && !state.sidebarCollapsed && !isMobileLayout()) {
      state.sessionsDrawerOpen = true;
      writeStoredBoolean("wand-sidebar-open", true);
    }
    var shellRenderResult = "disabled";
    var rebuiltLegacyHosts = false;
    if (isLoggedIn) {
      try {
        shellRenderResult = renderBrowserReactShell(app, renderAppShell);
      } catch (error) {
        console.error("[wand] React shell mount failed; using legacy shell", error);
        unmountBrowserReactShell();
        app.innerHTML = renderAppShell();
        shellRenderResult = "disabled";
      }
      if (shellRenderResult === "disabled") {
        app.innerHTML = renderAppShell();
        rebuiltLegacyHosts = true;
      } else if (shellRenderResult === "mounted") {
        rebuiltLegacyHosts = true;
      }
    } else {
      app.innerHTML = renderLogin();
      rebuiltLegacyHosts = true;
    }
    if (rebuiltLegacyHosts) {
      resetChatRenderCache();
      attachEventListeners();
    }
    updateDrawerState();
    syncComposerModeSelect();
    syncComposerModelSelect(getSelectedSession());
    applyCurrentView();
    if (!skipShellChrome) {
      updateShellChrome();
    }
    if (isLoggedIn && state.filePanelOpen) {
      refreshFileExplorer();
    }
    void document.body.offsetHeight;
    requestAnimationFrame(function() {
      document.documentElement.classList.remove("no-transition");
    });
    if (isLoggedIn && state.selectedId && state.gitStatusSessionId !== state.selectedId) {
      loadGitStatus(state.selectedId);
    }
    if (isLoggedIn) {
      var __sel = state.sessions.find(function(s) {
        return s.id === state.selectedId;
      });
      updateRunningIndicators(__sel);
    }
    scrollPathElementToEnd(document.getElementById("topbar-cwd"));
    scrollPathElementToEnd(document.getElementById("blank-chat-cwd-path"));
    refreshTailMarqueePaths();
  }
  function renderApprovalStatsBadge() {
    var selectedSession = state.sessions.find(function(s) {
      return s.id === state.selectedId;
    });
    var stats = selectedSession && selectedSession.approvalStats;
    if (!stats || stats.total === 0) return '<span class="approval-stats hidden" id="approval-stats"></span>';
    return '<span class="approval-stats" id="approval-stats"><span class="approval-stats-divider"></span><span class="approval-stats-badge" id="approval-stats-badge" title="\u672C\u6B21\u4F1A\u8BDD\u81EA\u52A8\u6279\u51C6\u7EDF\u8BA1"><svg class="approval-stats-icon" width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg><span class="approval-stats-total">' + stats.total + '</span></span><span class="approval-stats-popup" id="approval-stats-popup"><span class="approval-stats-popup-title">\u81EA\u52A8\u6279\u51C6\u7EDF\u8BA1</span>' + (stats.command > 0 ? '<span class="approval-stats-row"><span class="approval-stats-row-icon">' + iconSvg("terminal", { size: 12, strokeWidth: 1.8 }) + '</span><span class="approval-stats-row-label">\u547D\u4EE4\u6267\u884C</span><span class="approval-stats-row-count">' + stats.command + "</span></span>" : "") + (stats.file > 0 ? '<span class="approval-stats-row"><span class="approval-stats-row-icon">' + iconSvg("edit", { size: 12, strokeWidth: 1.8 }) + '</span><span class="approval-stats-row-label">\u6587\u4EF6\u5199\u5165</span><span class="approval-stats-row-count">' + stats.file + "</span></span>" : "") + (stats.tool > 0 ? '<span class="approval-stats-row"><span class="approval-stats-row-icon">' + iconSvg("wrench", { size: 12, strokeWidth: 1.8 }) + '</span><span class="approval-stats-row-label">\u5176\u4ED6\u5DE5\u5177</span><span class="approval-stats-row-count">' + stats.tool + "</span></span>" : "") + '<span class="approval-stats-row approval-stats-row-total"><span class="approval-stats-row-icon">' + iconSvg("sigma", { size: 12, strokeWidth: 1.8 }) + '</span><span class="approval-stats-row-label">\u5408\u8BA1</span><span class="approval-stats-row-count">' + stats.total + "</span></span></span></span>";
  }
  function renderLogin() {
    if (!state.loginChecked) {
      return '<div class="login-container"><div class="login-card login-card-loading"><div class="login-header"><div class="login-logo"><div class="login-logo-icon">W</div><span class="login-logo-text">Wand</span></div><div class="login-subtitle">\u6B63\u5728\u6062\u590D\u767B\u5F55\u72B6\u6001</div></div><div class="login-body"><div class="login-status"><span class="login-spinner" aria-hidden="true"></span><div><p class="login-hint">\u6B63\u5728\u68C0\u67E5\u672C\u5730\u767B\u5F55\u4F1A\u8BDD\uFF0C\u8BF7\u7A0D\u5019\u3002</p><p class="login-muted">\u5982\u679C\u4F60\u521A\u5237\u65B0\u9875\u9762\uFF0C\u8FD9\u662F\u6B63\u5E38\u73B0\u8C61\u3002</p></div></div></div></div></div>';
    }
    return '<div class="login-container"><div class="login-card"><div class="login-header"><div class="login-logo"><div class="login-logo-icon">W</div><span class="login-logo-text">Wand</span></div><div class="login-subtitle">\u5728\u6D4F\u89C8\u5668\u4E2D\u8FD0\u884C\u672C\u673A\u7EC8\u7AEF</div></div><form id="login-form" class="login-body" autocomplete="on"><input type="text" name="username" autocomplete="username" value="wand" tabindex="-1" aria-hidden="true" style="position:absolute;left:-9999px;width:1px;height:1px;opacity:0;pointer-events:none" readonly /><p class="login-hint">\u8F93\u5165 Wand \u8BBF\u95EE\u5BC6\u7801\u4EE5\u8FDB\u5165\u63A7\u5236\u53F0\u3002</p><div class="field"><label class="field-label" for="password">\u5BC6\u7801</label><div class="password-field"><input id="password" type="password" class="field-input password-input" placeholder="\u8F93\u5165\u8BBF\u95EE\u5BC6\u7801" autocomplete="current-password" data-error="false" aria-describedby="password-hint login-error" aria-invalid="false" /><button id="toggle-password-button" type="button" class="password-toggle" aria-label="\u663E\u793A\u5BC6\u7801" aria-pressed="false">\u663E\u793A</button></div><p id="password-hint" class="hint">\u4F7F\u7528\u4F60\u5728 Wand \u4E2D\u8BBE\u7F6E\u7684\u8BBF\u95EE\u5BC6\u7801\u3002</p><p id="login-error" class="error-message hidden" role="alert"></p></div><div id="login-cert-hint" class="login-cert-hint hidden" role="alert"><div class="login-cert-hint-title">\u8BC1\u4E66\u4E0D\u53D7\u4FE1\u4EFB\uFF0C\u767B\u5F55\u6001\u65E0\u6CD5\u4FDD\u5B58</div><p class="login-cert-hint-body">\u5BC6\u7801\u662F\u5BF9\u7684\uFF0C\u4F46\u5F53\u524D HTTPS \u8BC1\u4E66\u4E0D\u53D7\u6D4F\u89C8\u5668\u4FE1\u4EFB\uFF0C\u6D4F\u89C8\u5668\u56E0\u6B64\u62D2\u7EDD\u4FDD\u5B58\u767B\u5F55 Cookie\uFF0C\u6240\u4EE5\u8FDB\u4E0D\u4E86\u63A7\u5236\u53F0\u3002<br/>\u89E3\u51B3\u529E\u6CD5\uFF08\u4EFB\u9009\u5176\u4E00\uFF09\uFF1A\u6539\u7528 HTTP \u8BBF\u95EE\u672C\u670D\u52A1\uFF1B\u6216\u628A\u672C\u670D\u52A1\u8BC1\u4E66\u8BBE\u4E3A\u300C\u53D7\u4FE1\u4EFB\u300D\uFF08\u63A8\u8350 mkcert\uFF09\uFF1B\u6216\u5728\u672C\u673A\u5C06\u8BE5\u81EA\u7B7E\u8BC1\u4E66\u8BBE\u4E3A\u5B8C\u5168\u4FE1\u4EFB\u540E\u91CD\u8BD5\u3002</p><a id="login-cert-http-link" class="btn btn-ghost btn-block" href="#" rel="noopener">\u6539\u7528 HTTP \u8BBF\u95EE</a></div><button id="login-button" type="submit" class="btn btn-primary btn-block">\u8FDB\u5165\u63A7\u5236\u53F0</button>' + (hasNativeSwitchServer() ? '<button id="login-switch-server-button" class="btn btn-ghost btn-block login-switch-server" type="button"><svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="3" width="20" height="8" rx="2"/><rect x="2" y="13" width="20" height="8" rx="2"/><line x1="6" y1="7" x2="6.01" y2="7"/><line x1="6" y1="17" x2="6.01" y2="17"/></svg><span>\u5207\u6362\u670D\u52A1\u5668</span></button>' : "") + "</form></div></div>";
  }
  function renderAppShell() {
    var scriptClose = String.fromCharCode(60) + String.fromCharCode(47) + "script>";
    var selectedSession = state.sessions.find(function(s) {
      return s.id === state.selectedId;
    });
    var terminalTitle = selectedSession ? shortCommand(selectedSession.command) : "\u672A\u9009\u62E9\u4F1A\u8BDD";
    var terminalInfo = selectedSession ? selectedSession.mode + " | " + selectedSession.status : "\u70B9\u51FB\u4E0A\u65B9\u300C\u65B0\u5BF9\u8BDD\u300D\u5F00\u59CB";
    var currentDraft = state.selectedId ? state.drafts[state.selectedId] || "" : "";
    var drawerClass = state.sessionsDrawerOpen ? " open" : "";
    var backdropClass = shouldShowSessionsBackdrop() ? " open" : "";
    var preferredTool = getComposerTool();
    var composerMode = getSafeModeForTool2(preferredTool, state.chatMode);
    var isMobile = isMobileLayout();
    var isCollapsed = !!state.sidebarPinned && !!state.sidebarCollapsed;
    var isAnchored = isCollapsed || !isMobile && (!!state.sidebarPinned || !!state.sessionsDrawerOpen);
    var collapsedCls = isCollapsed ? " sidebar-collapsed" : "";
    var sidebarCollapsedCls = isCollapsed ? " collapsed" : "";
    return '<div class="app-container"><div id="sessions-drawer-backdrop" class="drawer-backdrop' + backdropClass + '"></div><div class="main-layout' + (state.sessionsDrawerOpen ? " sidebar-open" : "") + (isAnchored ? " sidebar-pinned" : "") + collapsedCls + '"><aside id="sessions-drawer" class="sidebar' + drawerClass + (isAnchored ? " pinned" : "") + sidebarCollapsedCls + '"><div class="sidebar-header"><div class="sidebar-header-main"><div class="topbar-logo-icon">W</div><span class="sidebar-title">\u4F1A\u8BDD</span><span class="session-count" id="session-count">' + String(state.sessions.filter(function(session) {
      var source = String(session && session.sessionSource || "").toLowerCase();
      return source !== "automation" && source !== "startup";
    }).length) + '</span></div><div class="sidebar-header-actions"><div class="sidebar-header-more"><button id="sidebar-more-btn" class="btn btn-ghost btn-sm" type="button" title="\u66F4\u591A\u64CD\u4F5C"><svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="5" r="1.5"/><circle cx="12" cy="12" r="1.5"/><circle cx="12" cy="19" r="1.5"/></svg></button><div class="sidebar-header-overflow" id="sidebar-overflow-menu"><button class="overflow-item" id="sidebar-home-btn" type="button"><svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M3 9l9-7 9 7v11a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2z"/><polyline points="9 22 9 12 15 12 15 22"/></svg><span>\u56DE\u5230\u9996\u9875</span></button><button class="overflow-item" id="sidebar-refresh-btn" type="button"><svg width="15" height="15" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="23 4 23 10 17 10"/><path d="M20.49 15a9 9 0 1 1-2.12-9.36L23 10"/></svg><span>\u5237\u65B0\u9875\u9762</span></button></div></div><button id="sidebar-pin-btn" class="btn btn-ghost btn-sm sidebar-pin-toggle' + (state.sidebarPinned ? " pinned" : "") + '" type="button" title="' + (state.sidebarPinned ? "\u5DF2\u56FA\u5B9A\u5E38\u9A7B\uFF08\u70B9\u51FB\u89E3\u9664\u9501\u5B9A\uFF09" : "\u56FA\u5B9A\u4FA7\u680F\u5E38\u9A7B") + '" aria-label="' + (state.sidebarPinned ? "\u89E3\u9664\u56FA\u5B9A\u5E38\u9A7B" : "\u56FA\u5B9A\u4FA7\u680F\u5E38\u9A7B") + '" aria-pressed="' + (state.sidebarPinned ? "true" : "false") + '"><svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><line x1="12" y1="17" x2="12" y2="22"/><path d="M5 17h14v-1.76a2 2 0 0 0-1.11-1.79l-1.78-.9A2 2 0 0 1 15 10.76V6h1a2 2 0 0 0 0-4H8a2 2 0 0 0 0 4h1v4.76a2 2 0 0 1-1.11 1.79l-1.78.9A2 2 0 0 0 5 15.24z"/></svg></button><button id="sidebar-collapse-btn" class="btn btn-ghost btn-sm sidebar-collapse-toggle' + (isCollapsed ? " collapsed" : "") + '" type="button" title="' + (isCollapsed ? "\u5C55\u5F00\u4E3A\u5168\u5C3A\u5BF8" : "\u6536\u8D77\u4E3A\u7A84\u6761") + '" aria-label="' + (isCollapsed ? "\u5C55\u5F00\u4E3A\u5168\u5C3A\u5BF8" : "\u6536\u8D77\u4E3A\u7A84\u6761") + '">' + (isCollapsed ? '<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><polyline points="10 6 16 12 10 18"/><line x1="20" y1="5" x2="20" y2="19"/></svg>' : '<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><polyline points="14 6 8 12 14 18"/><line x1="4" y1="5" x2="4" y2="19"/></svg>') + '</button><button id="close-drawer-button" class="btn btn-ghost btn-icon sidebar-close drawer-close-btn" type="button" aria-label="\u5173\u95ED\u83DC\u5355"><svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" aria-hidden="true"><line x1="6" y1="6" x2="18" y2="18"/><line x1="18" y1="6" x2="6" y2="18"/></svg></button></div></div><div class="sidebar-body"><div id="sessions-panel"><div class="sessions-list" id="sessions-list">' + renderSessionsListContent() + '</div></div></div><div class="sidebar-footer"><button id="drawer-new-session-button" class="btn btn-primary btn-block"><span>+</span> \u65B0\u4F1A\u8BDD</button><div class="sidebar-footer-actions"><button id="file-panel-toggle-btn" class="btn btn-ghost btn-sm' + (state.filePanelOpen ? " active" : "") + '" type="button" title="\u67E5\u770B\u6587\u4EF6"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 19a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h5l2 3h9a2 2 0 0 1 2 2z"/></svg><span>\u6587\u4EF6</span></button><button id="settings-button" class="btn btn-ghost btn-sm" type="button" title="\u8BBE\u7F6E"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="3"/><path d="M19.4 15a1.65 1.65 0 0 0 .33 1.82l.06.06a2 2 0 0 1-2.83 2.83l-.06-.06a1.65 1.65 0 0 0-1.82-.33 1.65 1.65 0 0 0-1 1.51V21a2 2 0 0 1-4 0v-.09A1.65 1.65 0 0 0 9 19.4a1.65 1.65 0 0 0-1.82.33l-.06.06a2 2 0 0 1-2.83-2.83l.06-.06A1.65 1.65 0 0 0 4.68 15a1.65 1.65 0 0 0-1.51-1H3a2 2 0 0 1 0-4h.09A1.65 1.65 0 0 0 4.6 9a1.65 1.65 0 0 0-.33-1.82l-.06-.06a2 2 0 0 1 2.83-2.83l.06.06A1.65 1.65 0 0 0 9 4.68a1.65 1.65 0 0 0 1-1.51V3a2 2 0 0 1 4 0v.09a1.65 1.65 0 0 0 1 1.51 1.65 1.65 0 0 0 1.82-.33l.06-.06a2 2 0 0 1 2.83 2.83l-.06.06A1.65 1.65 0 0 0 19.4 9a1.65 1.65 0 0 0 1.51 1H21a2 2 0 0 1 0 4h-.09a1.65 1.65 0 0 0-1.51 1z"/></svg><span>\u8BBE\u7F6E</span></button>' + (hasNativeBackToApp() ? '<button id="back-to-native-button" class="btn btn-ghost btn-sm sidebar-back-to-native" type="button" title="\u8FD4\u56DE App \u539F\u751F\u754C\u9762"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="10" y="3" width="11" height="18" rx="2"/><line x1="14" y1="17" x2="17" y2="17"/><polyline points="7 8 3 12 7 16"/></svg><span>\u8FD4\u56DEApp</span></button>' : "") + (hasNativeSwitchServer() ? '<button id="switch-server-button" class="btn btn-ghost btn-sm sidebar-switch-server" type="button" title="\u5207\u6362\u670D\u52A1\u5668"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="2" y="3" width="20" height="8" rx="2"/><rect x="2" y="13" width="20" height="8" rx="2"/><line x1="6" y1="7" x2="6.01" y2="7"/><line x1="6" y1="17" x2="6.01" y2="17"/></svg><span>\u5207\u6362</span></button>' : "") + '<button id="logout-button" class="btn btn-ghost btn-sm sidebar-logout" type="button" title="\u9000\u51FA\u767B\u5F55"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"/><polyline points="16 17 21 12 16 7"/><line x1="21" y1="12" x2="9" y2="12"/></svg><span>\u9000\u51FA</span></button></div></div></aside><main class="main-content"><div class="main-header-row"><div class="topbar-left"><button id="sessions-toggle-button" class="floating-sidebar-toggle' + (state.sessionsDrawerOpen ? " active" : "") + '" aria-label="\u5207\u6362\u4F1A\u8BDD\u4FA7\u680F" type="button"><span class="hamburger-icon"><span></span><span></span><span></span></span></button><span class="topbar-brand" aria-hidden="true">W</span></div><div class="topbar-center">' + (selectedSession ? '<span class="topbar-session-title" title="' + escapeHtml(selectedSession.description || selectedSession.command || "") + '">' + escapeHtml(selectedSession.title || shortCommand(selectedSession.command)) + '</span><span class="session-status-pill ' + getSessionStatusClass(selectedSession) + '" title="' + escapeHtml(getSessionStatusLabel(selectedSession)) + '"><span class="session-status-dot"></span><span class="session-status-text">' + escapeHtml(getSessionStatusLabel(selectedSession)) + '</span></span><span class="current-task hidden" id="current-task"></span>' + (selectedSession.cwd ? renderTailMarqueePath(selectedSession.cwd, "topbar-cwd", ' id="topbar-cwd" role="button" tabindex="0"') : "") : '<span class="topbar-tagline">Wand \u63A7\u5236\u53F0</span><span class="current-task hidden" id="current-task"></span>') + '</div><div class="topbar-right"><button id="topbar-file-button" class="topbar-btn square' + (state.filePanelOpen ? " active" : "") + '" type="button" aria-label="\u6587\u4EF6" title="\u67E5\u770B\u6587\u4EF6\uFF08\u53EF\u4FEE\u6539\u8DEF\u5F84\uFF09"><svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 19a2 2 0 0 1-2 2H4a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h5l2 3h9a2 2 0 0 1 2 2z"/></svg></button><span id="topbar-git-slot" class="topbar-git-slot">' + renderTopbarGitBadgeHtml() + "</span>" + (selectedSession ? renderTopbarMoreMenuHtml(selectedSession) : "") + '</div></div><div id="file-panel-backdrop" class="file-panel-backdrop' + (state.filePanelOpen ? " open" : "") + '"></div><div id="file-side-panel" class="file-side-panel' + (state.filePanelOpen ? " open" : "") + '"><div class="file-side-panel-header"><div class="file-side-panel-title-group"><span class="file-side-panel-icon">' + wandFileIcon("folder-open", { size: 16 }) + '</span><span class="file-side-panel-title">\u6587\u4EF6</span></div><div class="file-side-panel-header-actions"><button class="file-side-panel-iconbtn" id="file-explorer-refresh" type="button" title="\u5237\u65B0" aria-label="\u5237\u65B0\u6587\u4EF6\u5217\u8868">' + wandFileIcon("refresh", { size: 15 }) + '</button><button id="file-side-panel-close" class="file-side-panel-iconbtn close" type="button" aria-label="\u5173\u95ED\u6587\u4EF6\u9762\u677F" title="\u5173\u95ED">' + wandFileIcon("x", { size: 16 }) + '</button></div></div><div class="file-side-panel-body"><div class="file-explorer-header"><button class="file-explorer-up" id="file-explorer-up" type="button" title="\u8FD4\u56DE\u4E0A\u7EA7\u76EE\u5F55" aria-label="\u8FD4\u56DE\u4E0A\u7EA7\u76EE\u5F55">' + wandFileIcon("arrow-up", { size: 15 }) + '</button><input type="text" class="file-explorer-path" id="file-explorer-cwd" value="' + escapeHtml(selectedSession && selectedSession.cwd ? selectedSession.cwd : getConfigCwd()) + '" title="' + escapeHtml(selectedSession && selectedSession.cwd ? selectedSession.cwd : getConfigCwd()) + '" placeholder="\u8F93\u5165\u8DEF\u5F84\u5E76\u56DE\u8F66..." spellcheck="false" autocomplete="off" autocapitalize="off" autocorrect="off" aria-label="\u5F53\u524D\u8DEF\u5F84\uFF0C\u53EF\u76F4\u63A5\u4FEE\u6539\u540E\u56DE\u8F66" /></div><div class="file-search-box"><span class="file-search-icon">' + wandFileIcon("search", { size: 14 }) + '</span><input type="text" id="file-search-input" class="file-search-input" placeholder="\u641C\u7D22\u5F53\u524D\u76EE\u5F55\u2026" autocomplete="off" autocorrect="off" autocapitalize="off" spellcheck="false" /><button class="file-search-clear" id="file-search-clear" type="button" aria-label="\u6E05\u9664\u641C\u7D22" title="\u6E05\u9664">' + wandFileIcon("x", { size: 13 }) + '</button></div><div class="file-explorer" id="file-explorer">' + renderFileExplorer(selectedSession && selectedSession.cwd ? selectedSession.cwd : getConfigCwd()) + '</div></div></div><div id="output" class="terminal-container' + (state.selectedId ? "" : " hidden") + ' active"><div class="terminal-scale-overlay" aria-label="\u7EC8\u7AEF\u7F29\u653E\u63A7\u4EF6"><button id="terminal-scale-down-top" class="terminal-scale-overlay-btn terminal-scale-btn" type="button" title="\u7F29\u5C0F">\u2212</button><span class="terminal-scale-overlay-label terminal-scale-label" id="terminal-scale-label-top">' + Math.round(state.terminalScale * 100) + '%</span><button id="terminal-scale-up-top" class="terminal-scale-overlay-btn terminal-scale-btn" type="button" title="\u653E\u5927">+</button><span class="terminal-scale-overlay-divider"></span><button id="page-refresh-btn" class="terminal-scale-overlay-btn" type="button" title="\u5237\u65B0\u9875\u9762">\u21BB</button></div><button id="terminal-jump-bottom" class="terminal-jump-bottom' + (state.showTerminalJumpToBottom ? " visible" : "") + '" type="button" title="\u56DE\u5230\u5E95\u90E8" aria-label="\u56DE\u5230\u5E95\u90E8"><svg viewBox="0 0 16 16" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M8 3.5v9M3.5 8l4.5 4.5L12.5 8"/></svg></button></div><div id="chat-output" class="chat-container hidden"><div id="chat-fold-bar" class="chat-fold-bar hidden" aria-live="polite"></div><button id="chat-unread-bubble" class="chat-unread-bubble" type="button" title="\u56DE\u5230\u6700\u65B0\u6D88\u606F" aria-label="\u56DE\u5230\u6700\u65B0\u6D88\u606F"><span class="chat-unread-bubble-icon"><svg viewBox="0 0 16 16" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M8 3.5v9M3.5 8l4.5 4.5L12.5 8"/></svg></span><span class="chat-unread-bubble-count" aria-hidden="true"></span></button></div><div id="blank-chat" class="blank-chat' + (state.selectedId ? " hidden" : "") + '"><div class="blank-chat-inner"><div class="blank-chat-logo">W</div><h2 class="blank-chat-title">Wand</h2><p class="blank-chat-subtitle">\u652F\u6301\u7EC8\u7AEF PTY \u4F1A\u8BDD\u4E0E\u7ED3\u6784\u5316 chat \u4F1A\u8BDD\uFF0C\u4E24\u79CD\u6A21\u5F0F\u53EF\u5E76\u5B58\u3002</p><div class="blank-chat-tools"><button class="blank-chat-tool-btn" id="welcome-tool-claude" type="button"><span class="tool-icon">' + iconSvg("terminal", { size: 16, strokeWidth: 1.8 }) + '</span>\u65B0\u5EFA\u7EC8\u7AEF\u4F1A\u8BDD</button><button class="blank-chat-tool-btn" id="welcome-tool-codex" type="button"><span class="tool-icon tool-icon-text">\u2318</span>\u65B0\u5EFA Codex \u4F1A\u8BDD</button><button class="blank-chat-tool-btn" id="welcome-tool-opencode" type="button"><span class="tool-icon tool-icon-text">OC</span>\u65B0\u5EFA OpenCode \u4F1A\u8BDD</button><button class="blank-chat-tool-btn" id="welcome-tool-structured" type="button"><span class="tool-icon">' + iconSvg("chat", { size: 16, strokeWidth: 1.8 }) + '</span>\u65B0\u5EFA\u7ED3\u6784\u5316\u4F1A\u8BDD</button></div><div class="blank-chat-cwd-wrap"><div class="blank-chat-cwd" id="blank-chat-cwd" role="button" tabindex="0" aria-haspopup="dialog" aria-expanded="false" title="\u70B9\u51FB\u5207\u6362\u5DE5\u4F5C\u76EE\u5F55"><span class="blank-chat-cwd-icon">' + iconSvg("folder", { size: 13, strokeWidth: 1.8 }) + "</span>" + renderTailMarqueePath(getEffectiveCwd(), "blank-chat-cwd-path", ' id="blank-chat-cwd-path"') + '<span class="blank-chat-cwd-arrow">' + iconSvg("chevronDown", { size: 11, strokeWidth: 2 }) + '</span></div></div></div><div id="cross-session-queue-host"></div></div><div class="input-panel' + (state.selectedId ? "" : " hidden") + '"><div class="composer-top-row"><div id="todo-progress" class="todo-progress hidden"><button class="todo-progress-header" id="todo-progress-toggle" type="button" aria-expanded="false" aria-controls="todo-progress-body" aria-label="\u5C55\u5F00\u5F85\u529E\u5217\u8868"><div class="todo-progress-fill" id="todo-progress-fill" aria-hidden="true" style="--progress:0"></div><div class="todo-progress-left"><span class="todo-progress-ring" id="todo-progress-ring" aria-hidden="true" style="--progress:0"><svg width="16" height="16" viewBox="0 0 36 36"><circle class="todo-ring-track" cx="18" cy="18" r="15.5" fill="none" stroke-width="4"/><circle class="todo-ring-fill" cx="18" cy="18" r="15.5" fill="none" stroke-width="4" stroke-linecap="round"/></svg></span><span class="todo-progress-counter" id="todo-progress-counter"></span></div><div class="todo-progress-task-wrap"><span class="todo-progress-task" id="todo-progress-task"></span></div>' + iconSvg("chevronDown", { size: 14, strokeWidth: 2 }) + '</button></div><div class="todo-progress-body hidden" id="todo-progress-body"><ul class="todo-progress-list" id="todo-progress-list"></ul></div></div><div id="queue-bar-host" class="queue-bar-host" hidden></div><div class="input-composer-row"><div class="input-composer' + (currentDraft ? " has-text" : "") + '"><div class="composer-status-row" id="composer-status-row">' + renderAutoApproveChip(selectedSession) + '<span class="permission-actions hidden" id="permission-actions"><span class="permission-actions-label" id="permission-actions-label">\u7B49\u5F85\u6388\u6743</span><button id="approve-permission-btn" class="btn btn-permission btn-permission-approve" type="button">\u6279\u51C6</button><button id="deny-permission-btn" class="btn btn-permission btn-permission-deny" type="button">\u62D2\u7EDD</button></span>' + renderApprovalStatsBadge() + '</div><div class="composer-main-row"><div class="composer-actions-left"><button id="attach-btn" class="btn-circle btn-circle-action" type="button" title="\u66F4\u591A" aria-label="\u66F4\u591A" aria-haspopup="dialog" aria-expanded="false">' + iconSvg("plus", { size: 18, strokeWidth: 2.2 }) + '</button><input type="file" id="file-upload-input" multiple tabindex="-1" style="position:absolute;width:1px;height:1px;opacity:0;overflow:hidden;clip:rect(0,0,0,0);pointer-events:none"></div><div class="composer-inline-config">' + renderComposerConfigControlsHtml(selectedSession) + '</div><div class="composer-input-wrap"><textarea id="input-box" class="input-textarea" placeholder="' + getComposerPlaceholder(selectedSession, state.terminalInteractive) + '" rows="1" autocomplete="off" autocorrect="off" autocapitalize="off" spellcheck="false" enterkeyhint="send">' + escapeHtml(currentDraft) + '</textarea><button id="prompt-optimize-btn" class="prompt-optimize-btn" type="button" title="\u63D0\u793A\u8BCD\u4F18\u5316\uFF08AI\uFF09" aria-label="\u63D0\u793A\u8BCD\u4F18\u5316"><svg class="prompt-optimize-icon" width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M12 3l1.6 4.4L18 9l-4.4 1.6L12 15l-1.6-4.4L6 9l4.4-1.6z" fill="currentColor" opacity="0.25"/><path d="M12 3l1.6 4.4L18 9l-4.4 1.6L12 15l-1.6-4.4L6 9l4.4-1.6z"/><path d="M19 14l.7 1.9L21.6 17l-1.9.7L19 19.6l-.7-1.9L16.4 17l1.9-.7z" fill="currentColor" opacity="0.35"/><path d="M5 4l.5 1.4L7 6l-1.5.6L5 8l-.5-1.4L3 6l1.5-.6z" fill="currentColor" opacity="0.35"/></svg><span class="prompt-optimize-spinner" aria-hidden="true"></span></button></div><div class="composer-actions-right"><button id="stop-button" class="btn-circle btn-circle-stop hidden" title="\u505C\u6B62"><svg width="12" height="12" viewBox="0 0 16 16" fill="currentColor"><rect x="3" y="3" width="10" height="10" rx="2"/></svg></button><button id="voice-record-btn" class="btn-circle btn-circle-action btn-circle-voice" type="button" title="\u6309\u4F4F\u8BED\u97F3\u8F93\u5165" aria-label="\u6309\u4F4F\u8BED\u97F3\u8F93\u5165" aria-pressed="false"' + (state.terminalInteractive ? " disabled" : "") + ">" + iconSvg("mic", { size: 19, strokeWidth: 2 }) + '</button><button id="send-input-button" class="btn-circle btn-circle-send" title="\u53D1\u9001"><svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round"><line x1="22" y1="2" x2="11" y2="13"/><polygon points="22 2 15 22 11 13 2 9 22 2"/></svg></button></div></div><div id="attachment-preview" class="attachment-preview hidden"></div></div></div><div class="composer-plus-popover hidden" id="composer-plus-popover" role="dialog" aria-modal="false" aria-label="\u66F4\u591A\u64CD\u4F5C"><button class="plus-popover-item" id="plus-attach-item" type="button">' + iconSvg("paperclip", { size: 14, strokeWidth: 1.8, cls: "plus-popover-icon" }) + '<span class="plus-popover-label">\u4E0A\u4F20\u9644\u4EF6</span></button><button class="plus-popover-item' + (state.terminalInteractive ? " is-on" : "") + '" id="terminal-interactive-toggle-top" type="button" aria-pressed="' + (state.terminalInteractive ? "true" : "false") + '">' + iconSvg("keyboard", { size: 14, strokeWidth: 1.8, cls: "plus-popover-icon" }) + '<span class="plus-popover-label">\u7EC8\u7AEF\u4EA4\u4E92</span><span class="plus-popover-toggle-state">' + (state.terminalInteractive ? "\u5F00" : "\u5173") + '</span></button><div class="plus-popover-sep" aria-hidden="true"></div><div class="plus-popover-trio-wrap">' + renderComposerConfigControlsHtml(selectedSession) + '</div></div><div class="voice-transcript-bubble hidden" id="voice-transcript-bubble" aria-live="polite"><div class="voice-transcript-text" id="voice-transcript-text"></div><div class="voice-transcript-hint" id="voice-transcript-hint"><span class="voice-wave" aria-hidden="true"><i></i><i></i><i></i><i></i></span><span class="voice-transcript-status" id="voice-transcript-status">\u6B63\u5728\u8046\u542C\u2026\u4E0A\u6ED1\u53D6\u6D88</span></div><span class="voice-bubble-arrow" aria-hidden="true"></span></div><p id="action-error" class="error-message hidden"></p></div></main></div></div>';
  }

  // src/web-ui/react/legacy-overlays.ts
  var pendingReactLegacyDialogs = 0;
  var reactLegacyFocusOrigin = null;
  var reactLegacyFocusRestoreScheduled = false;
  function activeOverlay() {
    const overlay = window.__wandReactUi;
    if (!overlay || typeof overlay.dialog !== "function" || typeof overlay.toast !== "function") {
      return null;
    }
    return overlay;
  }
  function dialogTone(value) {
    switch (value) {
      case "warning":
      case "danger":
      case "success":
      case "question":
        return value;
      case "error":
        return "danger";
      default:
        return "info";
    }
  }
  function toastTone(value) {
    switch (value) {
      case "success":
      case "warning":
      case "error":
        return value;
      default:
        return "info";
    }
  }
  function buttonKind(value) {
    switch (value) {
      case "primary":
      case "outline":
      case "ghost":
      case "danger":
        return value;
      default:
        return "secondary";
    }
  }
  function dialogButtons(options) {
    return Array.isArray(options.buttons) && options.buttons.length > 0 ? options.buttons : [{ label: "\u597D", value: true, kind: "primary", autofocus: true }];
  }
  function cancelResult(options) {
    if (options.cancelValue !== void 0) return options.cancelValue;
    return options.input ? null : false;
  }
  function readFocusOrigin() {
    if (typeof document === "undefined") return null;
    const active = document.activeElement;
    return active && typeof active.focus === "function" ? active : null;
  }
  function scheduleFocusRestore() {
    if (reactLegacyFocusRestoreScheduled) return;
    reactLegacyFocusRestoreScheduled = true;
    const schedule = typeof window.requestAnimationFrame === "function" ? window.requestAnimationFrame.bind(window) : (callback) => setTimeout(() => callback(Date.now()), 0);
    schedule(() => {
      reactLegacyFocusRestoreScheduled = false;
      if (pendingReactLegacyDialogs > 0) return;
      const target = reactLegacyFocusOrigin;
      reactLegacyFocusOrigin = null;
      if (!target || !target.isConnected || typeof document === "undefined" || !document.contains(target)) return;
      try {
        target.focus();
      } catch {
      }
    });
  }
  function finishReactLegacyDialog() {
    pendingReactLegacyDialogs = Math.max(0, pendingReactLegacyDialogs - 1);
    if (pendingReactLegacyDialogs === 0) scheduleFocusRestore();
  }
  function openReactLegacyDialog(options) {
    const overlay = activeOverlay();
    if (!overlay) return null;
    const focusAtOpen = pendingReactLegacyDialogs === 0 && !reactLegacyFocusRestoreScheduled ? readFocusOrigin() : null;
    const buttons = dialogButtons(options);
    const tone = dialogTone(options.type);
    const hasInput = !!options.input;
    const reactOptions = {
      title: options.title ? String(options.title) : tone === "danger" ? "\u786E\u8BA4\u64CD\u4F5C" : "\u63D0\u793A",
      description: options.message ? String(options.message) : void 0,
      tone,
      icon: options.icon == null ? void 0 : String(options.icon),
      actions: buttons.map((button) => ({
        label: button.label == null ? "" : String(button.label),
        value: { button },
        kind: buttonKind(button.kind),
        autoFocus: button.autofocus === true || button.autoFocus === true
      })),
      input: hasInput ? {
        value: options.inputValue == null ? "" : String(options.inputValue),
        placeholder: options.inputPlaceholder == null ? void 0 : String(options.inputPlaceholder),
        label: options.inputLabel == null ? options.title == null ? "\u8F93\u5165\u5185\u5BB9" : String(options.title) : String(options.inputLabel)
      } : void 0,
      dismissable: options.dismissable !== false
    };
    let result;
    try {
      result = overlay.dialog(reactOptions);
    } catch {
      return null;
    }
    if (pendingReactLegacyDialogs === 0 && !reactLegacyFocusRestoreScheduled) {
      reactLegacyFocusOrigin = focusAtOpen;
    }
    pendingReactLegacyDialogs += 1;
    return result.then((outcome) => {
      if (outcome.dismissed === true) return cancelResult(options);
      const button = outcome.action.button;
      if (hasInput && buttonKind(button.kind) === "primary") {
        return outcome.inputValue ?? "";
      }
      return button.value;
    }).finally(finishReactLegacyDialog);
  }
  function showReactLegacyToast(message, type, duration) {
    const overlay = activeOverlay();
    if (!overlay) return false;
    const options = {
      tone: toastTone(type),
      duration
    };
    try {
      overlay.toast(message, options);
      return true;
    } catch {
      return false;
    }
  }

  // src/web-ui/browser/notifications.ts
  function showError(el, msg) {
    if (!el) return;
    el.textContent = msg;
    el.classList.remove("hidden");
    var inputEl = el.previousElementSibling;
    while (inputEl) {
      if (inputEl.tagName === "INPUT") {
        inputEl.setAttribute("data-error", "true");
        break;
      }
      inputEl = inputEl.previousElementSibling;
    }
  }
  function hideError(el) {
    if (!el) return;
    el.textContent = "";
    el.classList.add("hidden");
    var inputEl = el.previousElementSibling;
    while (inputEl) {
      if (inputEl.tagName === "INPUT") {
        inputEl.setAttribute("data-error", "false");
        break;
      }
      inputEl = inputEl.previousElementSibling;
    }
  }
  function showToast(message, type) {
    var duration = type === "error" ? 4e3 : 2200;
    if (showReactLegacyToast(message, type, duration)) return;
    showNotificationBubble({
      title: message,
      type: type === "error" ? "warning" : "info",
      duration
    });
  }
  function openWandDialog(opts) {
    opts = opts || {};
    var reactDialog = openReactLegacyDialog(opts);
    if (reactDialog) return reactDialog;
    return openNativeWandDialog(opts);
  }
  function openNativeWandDialog(opts) {
    var title = opts.title == null ? "" : String(opts.title);
    var message = opts.message == null ? "" : String(opts.message);
    var text5 = title && message ? title + "\n\n" + message : title || message;
    var buttons = Array.isArray(opts.buttons) && opts.buttons.length ? opts.buttons : [{ label: "\u597D", value: true, kind: "primary" }];
    if (opts.input) {
      return Promise.resolve(window.prompt(text5, opts.inputValue == null ? "" : String(opts.inputValue)));
    }
    if (buttons.length <= 1) {
      window.alert(text5);
      return Promise.resolve(buttons[0]?.value);
    }
    var accepted = window.confirm(text5);
    if (!accepted) {
      return Promise.resolve(opts.cancelValue !== void 0 ? opts.cancelValue : false);
    }
    var primary = buttons.find(function(button) {
      return button.kind === "primary" || button.kind === "danger";
    }) || buttons[buttons.length - 1];
    return Promise.resolve(primary.value);
  }
  function wandAlert(message, options) {
    options = options || {};
    return openWandDialog({
      title: options.title || "\u63D0\u793A",
      message: typeof message === "string" ? message : String(message == null ? "" : message),
      type: options.type || "info",
      dismissable: options.dismissable,
      buttons: [
        { label: options.okLabel || "\u597D", value: void 0, kind: "primary", autofocus: true }
      ]
    });
  }
  function wandConfirm2(message, options) {
    options = options || {};
    var danger = !!options.danger || options.type === "danger";
    return openWandDialog({
      title: options.title || (danger ? "\u786E\u8BA4\u64CD\u4F5C" : "\u8BF7\u786E\u8BA4"),
      message: typeof message === "string" ? message : String(message == null ? "" : message),
      type: options.type || (danger ? "danger" : "question"),
      cancelValue: false,
      dismissable: options.dismissable,
      buttons: [
        { label: options.cancelLabel || "\u53D6\u6D88", value: false, kind: "secondary" },
        {
          label: options.okLabel || (danger ? "\u5220\u9664" : "\u786E\u5B9A"),
          value: true,
          kind: danger ? "danger" : "primary",
          autofocus: !danger
        }
      ]
    }).then(function(v) {
      return v === true;
    });
  }
  function wandPrompt(message, defaultValue, options) {
    options = options || {};
    return openWandDialog({
      title: options.title || "\u8BF7\u8F93\u5165",
      message: typeof message === "string" ? message : String(message == null ? "" : message),
      type: options.type || "question",
      input: true,
      inputValue: defaultValue == null ? "" : String(defaultValue),
      inputPlaceholder: options.placeholder || "",
      cancelValue: null,
      dismissable: options.dismissable,
      buttons: [
        { label: options.cancelLabel || "\u53D6\u6D88", value: null, kind: "secondary" },
        { label: options.okLabel || "\u786E\u5B9A", value: void 0, kind: "primary" }
      ]
    });
  }
  window.wandAlert = wandAlert;
  window.wandConfirm = wandConfirm2;
  window.wandPrompt = wandPrompt;
  window.openWandDialog = openWandDialog;
  window.showToast = showToast;
  var notificationStack = [];
  var notificationIdCounter = 0;
  var NOTIFICATION_GAP = 6;
  var NOTIFICATION_TOP = 16;
  function showNotificationBubble(opts) {
    if (opts.actionLabel || opts.playSound) playNotificationSound();
    if (!state.notifBubble) return { dismiss: function() {
    } };
    var id = ++notificationIdCounter;
    var type = opts.type || "info";
    var icon = opts.icon || (type === "warning" ? "!" : type === "success" ? "\u2713" : "i");
    var duration = opts.duration !== void 0 ? opts.duration : 8e3;
    var bubble = document.createElement("div");
    bubble.className = "notification-bubble";
    bubble.setAttribute("data-nid", String(id));
    var headerHtml = '<div class="notification-bubble-header"><span class="notification-bubble-icon ' + type + '">' + icon + '</span><span class="notification-bubble-title">' + escapeHtml(opts.title) + '</span><button class="notification-bubble-close" title="\u5173\u95ED">\xD7</button></div>';
    var bodyHtml = opts.body ? '<div class="notification-bubble-body">' + escapeHtml(opts.body).replace(/\n/g, "<br>") + "</div>" : "";
    var actionsHtml = opts.actionLabel ? '<div class="notification-bubble-actions"><button class="primary">' + escapeHtml(opts.actionLabel) + "</button></div>" : "";
    bubble.innerHTML = headerHtml + bodyHtml + actionsHtml;
    document.body.appendChild(bubble);
    var entry = { id, el: bubble };
    notificationStack.push(entry);
    repositionNotifications();
    var closeBtn = bubble.querySelector(".notification-bubble-close");
    if (closeBtn) closeBtn.onclick = function() {
      dismissNotification(id);
    };
    if (opts.actionLabel && opts.action) {
      var actionBtn = bubble.querySelector(".notification-bubble-actions button");
      if (actionBtn) actionBtn.onclick = function() {
        opts.action();
        dismissNotification(id);
      };
    }
    var timer = null;
    if (duration > 0) {
      timer = setTimeout(function() {
        dismissNotification(id);
      }, duration);
    }
    return {
      dismiss: function() {
        dismissNotification(id);
      }
    };
  }
  function dismissNotification(id) {
    var idx = -1;
    for (var i = 0; i < notificationStack.length; i++) {
      if (notificationStack[i].id === id) {
        idx = i;
        break;
      }
    }
    if (idx === -1) return;
    var entry = notificationStack[idx];
    entry.el.classList.add("slide-out");
    notificationStack.splice(idx, 1);
    repositionNotifications();
    setTimeout(function() {
      if (entry.el.parentNode) entry.el.parentNode.removeChild(entry.el);
    }, 300);
  }
  function repositionNotifications() {
    var top = NOTIFICATION_TOP;
    for (var i = 0; i < notificationStack.length; i++) {
      notificationStack[i].el.style.top = top + "px";
      top += notificationStack[i].el.offsetHeight + NOTIFICATION_GAP;
    }
  }
  try {
    _macUaMatch = navigator.userAgent.match(/WandPlatform\/macOS/);
    _macHandler = window.webkit && window.webkit.messageHandlers && window.webkit.messageHandlers.wandNative || null;
    if (_macUaMatch && _macHandler && typeof window.WandNative === "undefined") {
      window.WandNative = {
        // Only downloadUpdate is wired for now; other Android-specific
        // methods (notifications, haptics, screen wake) intentionally
        // omitted so feature detection falls back to web APIs on macOS.
        downloadUpdate: function(url, fileName, source) {
          try {
            _macHandler.postMessage({
              type: "downloadUpdate",
              url: String(url || ""),
              fileName: String(fileName || "wand-update.dmg"),
              source: String(source || "local")
            });
          } catch (_e) {
          }
        }
      };
    }
  } catch (_e) {
  }
  var _macUaMatch;
  var _macHandler;
  var _hasNativeBridge = typeof WandNative !== "undefined" && typeof WandNative.sendNotification === "function";
  var _wandAppMatch = navigator.userAgent.match(/WandApp\/([^\s]+)/);
  var _isMacApp = /WandPlatform\/macOS/.test(navigator.userAgent);
  var _isAndroidApp = !!_wandAppMatch && !_isMacApp;
  var _apkVersion = _wandAppMatch && _isAndroidApp ? _wandAppMatch[1] : null;
  var _macAppVersion = _wandAppMatch && _isMacApp ? _wandAppMatch[1] : null;
  function _vibrate(pattern) {
    if (!_hasNativeBridge || typeof WandNative.vibrate !== "function") return;
    try {
      WandNative.vibrate(pattern || "light");
    } catch (_e) {
    }
  }
  function _syncWakeLock() {
    if (!_hasNativeBridge) return;
    var anyActive = state.sessions.some(function(s) {
      return !s.archived && (s.status === "running" || s.status === "thinking" || s.status === "initializing");
    });
    if (typeof WandNative.setKeepScreenOn === "function") {
      try {
        WandNative.setKeepScreenOn(anyActive);
      } catch (_e) {
      }
    }
    if (anyActive) {
      if (typeof WandNative.startKeepAlive === "function") {
        try {
          WandNative.startKeepAlive();
        } catch (_e) {
        }
      }
    } else {
      if (typeof WandNative.stopKeepAlive === "function") {
        try {
          WandNative.stopKeepAlive();
        } catch (_e) {
        }
      }
    }
  }
  function _getNativePermission() {
    if (_hasNativeBridge && typeof WandNative.getPermission === "function") {
      try {
        return WandNative.getPermission();
      } catch (_e) {
      }
    }
    return null;
  }
  function requestNotificationPermission() {
    if (_hasNativeBridge) {
      var perm = _getNativePermission();
      if (perm === "default" || perm === "denied") {
        try {
          WandNative.requestPermission();
        } catch (_e) {
        }
      }
      return;
    }
    if (typeof Notification !== "undefined" && Notification.permission === "default") {
      Notification.requestPermission();
    }
  }
  function _shouldSendSystemNotification(opts) {
    var options = opts || {};
    if (options.onlyWhenHidden && !document.hidden) return false;
    if (options.skipWhenSelectedSessionId && options.skipWhenSelectedSessionId === state.selectedId && !document.hidden) {
      return false;
    }
    return true;
  }
  function _isNotificationThrottled(tag, minIntervalMs) {
    if (!tag || !minIntervalMs || minIntervalMs <= 0) return false;
    var lastAt = state.notificationHistory[tag] || 0;
    var now = Date.now();
    if (now - lastAt < minIntervalMs) return true;
    state.notificationHistory[tag] = now;
    return false;
  }
  function sendBrowserNotification(title, body, opts) {
    var options = opts || {};
    var tag = options.tag || "";
    if (!_shouldSendSystemNotification(options)) return;
    if (_isNotificationThrottled(tag, options.minIntervalMs || 0)) return;
    if (_hasNativeBridge) {
      var perm = _getNativePermission();
      if (perm !== "granted") return;
      try {
        var nativeTag = tag;
        if (options.kind) {
          nativeTag = options.kind + (tag ? ":" + tag : "");
        }
        WandNative.sendNotification(title || "Wand", body || "", nativeTag || "");
      } catch (_e) {
      }
      return;
    }
    if (typeof Notification === "undefined" || Notification.permission !== "granted") return;
    if (!document.hidden) return;
    try {
      var n = new Notification(title, {
        body: body || "",
        icon: options.icon || "/favicon.ico",
        tag: tag || void 0
      });
      n.onclick = function() {
        window.focus();
        n.close();
        if (options.onClick) options.onClick();
      };
      setTimeout(function() {
        n.close();
      }, 1e4);
    } catch (_e) {
    }
  }
  function notifyTaskProgress(sessionId, task) {
    if (!task || !task.title) return;
    var session = state.sessions.find(function(s) {
      return s.id === sessionId;
    });
    if (!session) return;
    var sessionLabel = session.summary || session.command || sessionId;
    sendBrowserNotification(
      "\u4EFB\u52A1\u8FDB\u884C\u4E2D",
      sessionLabel + "\n" + task.title,
      {
        kind: "task",
        tag: "wand-task-" + sessionId + "-" + task.title,
        minIntervalMs: 9e4,
        onlyWhenHidden: true,
        skipWhenSelectedSessionId: sessionId,
        onClick: function() {
          if (sessionId !== state.selectedId) selectSession(sessionId);
        }
      }
    );
  }
  function notifyUpdateAvailable(currentVersion, latestVersion) {
    showUpdateBubble(currentVersion || "-", latestVersion || "-");
    sendBrowserNotification(
      "Wand \u53D1\u73B0\u65B0\u7248\u672C",
      "\u5F53\u524D " + (currentVersion || "-") + " \u2192 \u6700\u65B0 " + (latestVersion || "-"),
      {
        kind: "update",
        tag: "wand-update",
        minIntervalMs: 3e5
      }
    );
  }
  function notifyPermissionRequest(sessionId, body) {
    sendBrowserNotification(
      "\u9700\u8981\u4F60\u7684\u6388\u6743",
      body,
      {
        kind: "permission",
        tag: "wand-perm-" + sessionId,
        minIntervalMs: 6e4,
        onlyWhenHidden: true,
        skipWhenSelectedSessionId: sessionId,
        onClick: function() {
          if (sessionId !== state.selectedId) {
            selectSession(sessionId);
          }
        }
      }
    );
  }
  function notifyTaskEnded(sessionId, title, body) {
    sendBrowserNotification(
      title,
      body,
      {
        kind: "task-ended",
        tag: "wand-ended-" + sessionId,
        minIntervalMs: 1e4,
        onClick: function() {
          if (sessionId !== state.selectedId) selectSession(sessionId);
        }
      }
    );
  }
  var _progressSyncTimers = {};
  var _PROGRESS_SYNC_DEBOUNCE_MS = 30;
  function _compactNotificationText(text5) {
    if (!text5) return "";
    var t6 = String(text5).replace(/^#+\s+/gm, "").replace(/\*\*/g, "").replace(/`/g, "").trim();
    var firstLine = t6.split("\n")[0].trim();
    if (firstLine.length > 100) firstLine = firstLine.slice(0, 100) + "\u2026";
    return firstLine;
  }
  function syncSessionProgressToNative(sessionId) {
    if (!_hasNativeBridge || typeof WandNative.updateSessionProgress !== "function") return;
    if (!sessionId) return;
    if (_progressSyncTimers[sessionId]) {
      clearTimeout(_progressSyncTimers[sessionId]);
    }
    _progressSyncTimers[sessionId] = setTimeout(function() {
      delete _progressSyncTimers[sessionId];
      _doSyncSessionProgress(sessionId);
    }, _PROGRESS_SYNC_DEBOUNCE_MS);
  }
  function _doSyncSessionProgress(sessionId) {
    var session = state.sessions.find(function(s) {
      return s.id === sessionId;
    });
    if (!session) return;
    var sessionLabel = session.summary || session.command || sessionId;
    var sessionStatus = session.status || "running";
    if (sessionStatus === "idle" || sessionStatus === "archived" || sessionStatus === "exited") {
      clearSessionProgressNative(sessionId);
      return;
    }
    var todos = null;
    var latestUserText = "";
    var latestAssistantText = "";
    var recentUserTexts = [];
    var messages = session.messages || [];
    for (var i = messages.length - 1; i >= 0; i--) {
      var msg = messages[i];
      if (!msg.content || !Array.isArray(msg.content)) continue;
      if (!latestAssistantText && msg.role === "assistant") {
        for (var ai = msg.content.length - 1; ai >= 0; ai--) {
          var ablock = msg.content[ai];
          if (ablock && ablock.type === "text" && ablock.text && ablock.text.trim()) {
            latestAssistantText = _compactNotificationText(ablock.text);
            break;
          }
        }
      }
      if (msg.role === "user") {
        var isPlaceholder = msg.content.some(function(b) {
          return b && b.__queued;
        });
        if (!isPlaceholder) {
          for (var ui = 0; ui < msg.content.length; ui++) {
            var ublock = msg.content[ui];
            if (ublock && ublock.type === "text" && ublock.text && ublock.text.trim()) {
              var utext = _compactNotificationText(ublock.text);
              if (!latestUserText) latestUserText = utext;
              if (recentUserTexts.length < 4) recentUserTexts.push(utext);
              break;
            }
          }
        }
      }
      if (!todos) {
        for (var j = msg.content.length - 1; j >= 0; j--) {
          var block = msg.content[j];
          if (block && block.type === "tool_use" && block.name === "TodoWrite" && block.input && block.input.todos) {
            todos = block.input.todos;
            break;
          }
        }
      }
      if (todos && recentUserTexts.length >= 4 && latestAssistantText) break;
    }
    recentUserTexts.reverse();
    var currentTask = "";
    if (sessionId === state.selectedId && state.currentTask && state.currentTask.title) {
      currentTask = state.currentTask.title;
    }
    var data = {
      sessionLabel,
      status: sessionStatus,
      currentTask,
      latestUserText,
      latestAssistantText,
      todos: todos || [],
      recentUserTexts
    };
    try {
      WandNative.updateSessionProgress(sessionId, JSON.stringify(data));
    } catch (_e) {
    }
  }
  function clearSessionProgressNative(sessionId) {
    if (!_hasNativeBridge || typeof WandNative.clearSessionProgress !== "function") return;
    if (_progressSyncTimers[sessionId]) {
      clearTimeout(_progressSyncTimers[sessionId]);
      delete _progressSyncTimers[sessionId];
    }
    try {
      WandNative.clearSessionProgress(sessionId);
    } catch (_e) {
    }
  }
  window.handleNativeBack = function() {
    if (restartOverlayController.isOpen()) return true;
    var reactOverlay = window.__wandReactUi;
    if (reactOverlay && typeof reactOverlay.closeTopmost === "function") {
      try {
        if (reactOverlay.closeTopmost()) return true;
      } catch (_e) {
      }
    }
    var reactFilePreview = window.__wandReactFilePreview;
    if (reactFilePreview && typeof reactFilePreview.closeTopmost === "function") {
      try {
        if (reactFilePreview.closeTopmost()) return true;
      } catch (_e) {
      }
    }
    var reactQuickCommit = window.__wandReactQuickCommit;
    if (reactQuickCommit && typeof reactQuickCommit.closeTopmost === "function") {
      try {
        if (reactQuickCommit.closeTopmost()) return true;
      } catch (_e) {
      }
    }
    var reactNewSession = window.__wandReactNewSession;
    if (reactNewSession && typeof reactNewSession.closeTopmost === "function") {
      try {
        if (reactNewSession.closeTopmost()) return true;
      } catch (_e) {
      }
    }
    var reactFolderPicker = window.__wandReactFolderPicker;
    if (reactFolderPicker && typeof reactFolderPicker.closeTopmost === "function") {
      try {
        if (reactFolderPicker.closeTopmost()) return true;
      } catch (_e) {
      }
    }
    var reactWorktreeMerge = window.__wandReactWorktreeMerge;
    if (reactWorktreeMerge && typeof reactWorktreeMerge.closeTopmost === "function") {
      try {
        if (reactWorktreeMerge.closeTopmost()) return true;
      } catch (_e) {
      }
    }
    var reactSettings = window.__wandReactSettings;
    if (reactSettings && typeof reactSettings.closeTopmost === "function") {
      try {
        if (reactSettings.closeTopmost()) return true;
      } catch (_e) {
      }
    }
    if (state.filePanelOpen && isMobileLayout()) {
      setFilePanelOpen(false);
      return true;
    }
    if (state.sessionsDrawerOpen && isMobileLayout()) {
      closeSessionsDrawer2();
      return true;
    }
    if (isMobileLayout() && state.selectedId) {
      state.selectedId = null;
      persistSelectedId();
      state.sessionsDrawerOpen = true;
      writeStoredBoolean("wand-sidebar-open", true);
      render();
      return true;
    }
    return false;
  };
  function playNotificationSound() {
    if (!state.notifSound) return;
    _doPlaySound();
  }
  function _doPlaySound() {
    try {
      let tone2 = function(freq, start, dur) {
        var osc = ctx.createOscillator();
        var gain = ctx.createGain();
        osc.type = "sine";
        osc.frequency.value = freq;
        gain.gain.setValueAtTime(0, ctx.currentTime + start);
        gain.gain.linearRampToValueAtTime(0.5 * vol, ctx.currentTime + start + 0.04);
        gain.gain.exponentialRampToValueAtTime(1e-3, ctx.currentTime + start + dur);
        osc.connect(gain);
        gain.connect(ctx.destination);
        osc.start(ctx.currentTime + start);
        osc.stop(ctx.currentTime + start + dur);
      };
      var tone = tone2;
      var AudioCtx = window.AudioContext || window.webkitAudioContext;
      if (!AudioCtx) return false;
      var ctx = new AudioCtx();
      if (ctx.state === "suspended") ctx.resume();
      var vol = (state.notifVolume || 0) / 100;
      tone2(523, 0, 0.25);
      tone2(659, 0.12, 0.3);
      setTimeout(function() {
        ctx.close();
      }, 600);
      return true;
    } catch (_e) {
      return false;
    }
  }
  function showUpdateBubble(currentVer, latestVer) {
    if (state._updateBubbleShown) return;
    state._updateBubbleShown = true;
    playNotificationSound();
    var id = ++notificationIdCounter;
    var card = document.createElement("div");
    card.className = "notification-bubble update-card";
    card.setAttribute("data-nid", String(id));
    card.innerHTML = '<div class="update-card-shine" aria-hidden="true"></div><div class="update-card-header"><div class="update-card-icon" aria-hidden="true"><svg viewBox="0 0 24 24" width="16" height="16" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><path d="M12 19V5"/><path d="M5 12l7-7 7 7"/></svg></div><div class="update-card-heading"><div class="update-card-title">\u53D1\u73B0\u65B0\u7248\u672C</div><div class="update-card-subtitle" id="update-card-subtitle">\u70B9\u51FB\u4E0B\u65B9\u6309\u94AE\u4E00\u952E\u66F4\u65B0</div></div><button class="update-card-close" title="\u7A0D\u540E\u63D0\u9192" aria-label="\u5173\u95ED"><svg viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round"><path d="M18 6L6 18"/><path d="M6 6l12 12"/></svg></button></div><div class="update-card-version"><span class="update-card-version-chip update-card-version-current">v' + escapeHtml(String(currentVer).replace(/^v/, "")) + '</span><svg class="update-card-version-arrow" viewBox="0 0 24 24" width="14" height="14" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><path d="M5 12h14"/><path d="M13 5l7 7-7 7"/></svg><span class="update-card-version-chip update-card-version-latest">v' + escapeHtml(String(latestVer).replace(/^v/, "")) + '</span></div><div class="update-card-progress" id="update-card-progress" aria-hidden="true"><div class="update-card-progress-track"><div class="update-card-progress-fill"></div></div></div><div class="update-card-status hidden" id="update-card-status"></div><div class="update-card-actions"><button class="update-card-action update-card-action-primary" id="update-bubble-action" type="button"><span class="update-card-action-label">\u7ACB\u5373\u66F4\u65B0</span></button></div>';
    document.body.appendChild(card);
    var entry = { id, el: card };
    notificationStack.push(entry);
    repositionNotifications();
    var closeBtn = card.querySelector(".update-card-close");
    if (closeBtn) closeBtn.onclick = function() {
      dismissNotification(id);
      state._updateBubbleShown = false;
    };
    var actionBtn = card.querySelector("#update-bubble-action");
    var actionLabel = card.querySelector(".update-card-action-label");
    var subtitleEl = card.querySelector("#update-card-subtitle");
    var statusEl = card.querySelector("#update-card-status");
    var progressEl = card.querySelector("#update-card-progress");
    function setStatus(text5, kind) {
      if (!statusEl) return;
      statusEl.textContent = text5 || "";
      statusEl.classList.remove("hidden", "error", "success");
      if (!text5) {
        statusEl.classList.add("hidden");
        return;
      }
      if (kind) statusEl.classList.add(kind);
    }
    function setSubtitle(text5) {
      if (subtitleEl) subtitleEl.textContent = text5 || "";
    }
    function setProgress(active) {
      if (!progressEl) return;
      progressEl.classList.toggle("active", !!active);
    }
    if (actionBtn) actionBtn.onclick = function() {
      actionBtn.disabled = true;
      card.classList.add("is-busy");
      if (actionLabel) actionLabel.textContent = "\u66F4\u65B0\u4E2D\u2026";
      setSubtitle("\u6B63\u5728\u4E0B\u8F7D\u5E76\u5B89\u88C5\u65B0\u7248\u672C\u2026");
      setProgress(true);
      setStatus("");
      fetch("/api/update", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        credentials: "same-origin"
      }).then(function(res) {
        return res.json();
      }).then(function(data) {
        if (data.error) {
          setProgress(false);
          card.classList.remove("is-busy");
          setSubtitle("\u66F4\u65B0\u672A\u5B8C\u6210");
          setStatus(data.error, "error");
          actionBtn.disabled = false;
          if (actionLabel) actionLabel.textContent = "\u91CD\u8BD5";
          return;
        }
        card.classList.add("is-success");
        setSubtitle((data.message || "\u66F4\u65B0\u5B8C\u6210") + "\uFF0C\u6B63\u5728\u91CD\u542F\u670D\u52A1\u2026");
        setStatus("");
        if (actionLabel) actionLabel.textContent = "\u6B63\u5728\u91CD\u542F\u2026";
        if (data.detachedUpdate) {
          showRestartOverlay();
          return;
        }
        if (data.restartRequired === false) {
          setProgress(false);
          card.classList.remove("is-busy");
          actionBtn.disabled = false;
          if (actionLabel) actionLabel.textContent = "\u5DF2\u5B8C\u6210";
          return;
        }
        performRestartCard(actionBtn, actionLabel, subtitleEl, statusEl, progressEl);
      }).catch(function() {
        setProgress(false);
        card.classList.remove("is-busy");
        setSubtitle("\u66F4\u65B0\u672A\u5B8C\u6210");
        setStatus("\u8BF7\u68C0\u67E5\u7F51\u7EDC\u8FDE\u63A5\u540E\u91CD\u8BD5", "error");
        actionBtn.disabled = false;
        if (actionLabel) actionLabel.textContent = "\u91CD\u8BD5";
      });
    };
  }
  function performRestartCard(btn, labelEl, subtitleEl, statusEl, progressEl) {
    fetch("/api/restart", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      credentials: "same-origin"
    }).then(function(res) {
      return res.json();
    }).then(function() {
      showRestartOverlay();
    }).catch(function() {
      showRestartOverlay();
    });
  }
  function showRestartOverlay(previousInstanceId, expectedVersion) {
    showRestart(previousInstanceId, expectedVersion);
  }
  function showAutoUpdateOverlay(currentVer, latestVer, previousInstanceId) {
    showAutoUpdate(currentVer, latestVer, previousInstanceId);
  }

  // src/web-ui/browser/input.ts
  var voiceState = { recording: false, canceling: false, transcript: "", startY: 0 };
  var VOICE_CANCEL_THRESHOLD = 60;
  function updateVoiceTranscript(text5) {
    voiceState.transcript = text5 || "";
    var textEl = document.getElementById("voice-transcript-text");
    if (textEl) textEl.textContent = voiceState.transcript;
    var bubble = document.getElementById("voice-transcript-bubble");
    if (bubble) bubble.classList.toggle("has-text", !!voiceState.transcript);
  }
  function startVoiceRecording(e) {
    if (state.terminalInteractive || voiceState.recording) return;
    if (e) {
      e.preventDefault();
      voiceState.startY = typeof e.clientY === "number" ? e.clientY : 0;
      try {
        if (e.pointerId !== void 0 && e.currentTarget && e.currentTarget.setPointerCapture) {
          e.currentTarget.setPointerCapture(e.pointerId);
        }
      } catch (_) {
      }
    }
    voiceState.recording = true;
    voiceState.canceling = false;
    voiceState.transcript = "";
    var btn = document.getElementById("voice-record-btn");
    if (btn) {
      btn.classList.add("is-recording");
      btn.setAttribute("aria-pressed", "true");
      btn.setAttribute("title", "\u677E\u5F00\u7ED3\u675F \xB7 \u4E0A\u6ED1\u53D6\u6D88");
      var label = btn.querySelector(".voice-record-label");
      if (label) label.textContent = "\u677E\u5F00 \u53D1\u9001";
    }
    var bubble = document.getElementById("voice-transcript-bubble");
    if (bubble) bubble.classList.remove("hidden", "is-canceling", "has-text");
    updateVoiceTranscript("");
    var status = document.getElementById("voice-transcript-status");
    if (status) status.textContent = "\u7F51\u9875\u7AEF\u6682\u4E0D\u652F\u6301\u8BED\u97F3\u8F93\u5165\uFF0C\u8BF7\u4F7F\u7528 App";
  }
  function handleVoiceMove(e) {
    if (!voiceState.recording || !e) return;
    var dy = voiceState.startY - (typeof e.clientY === "number" ? e.clientY : voiceState.startY);
    var shouldCancel = dy > VOICE_CANCEL_THRESHOLD;
    if (shouldCancel === voiceState.canceling) return;
    voiceState.canceling = shouldCancel;
    var bubble = document.getElementById("voice-transcript-bubble");
    if (bubble) bubble.classList.toggle("is-canceling", shouldCancel);
    var btn = document.getElementById("voice-record-btn");
    var label = btn && btn.querySelector(".voice-record-label");
    if (label) label.textContent = shouldCancel ? "\u677E\u5F00 \u53D6\u6D88" : "\u677E\u5F00 \u53D1\u9001";
    var status = document.getElementById("voice-transcript-status");
    if (status) status.textContent = shouldCancel ? "\u677E\u5F00\u624B\u6307 \u53D6\u6D88" : "\u6B63\u5728\u8046\u542C\u2026\u4E0A\u6ED1\u53D6\u6D88";
  }
  function stopVoiceRecording(e) {
    if (!voiceState.recording) return;
    if (e) e.preventDefault();
    voiceState.recording = false;
    var commit = !voiceState.canceling && !!voiceState.transcript.trim();
    var text5 = voiceState.transcript;
    resetVoiceRecordingUI();
    if (commit) {
      commitVoiceTranscript(text5);
    }
  }
  function cancelVoiceRecording(e) {
    if (!voiceState.recording) return;
    voiceState.canceling = true;
    stopVoiceRecording(e);
  }
  function resetVoiceRecordingUI() {
    voiceState.canceling = false;
    var btn = document.getElementById("voice-record-btn");
    if (btn) {
      btn.classList.remove("is-recording");
      btn.setAttribute("aria-pressed", "false");
      btn.setAttribute("title", "\u6309\u4F4F\u8BED\u97F3\u8F93\u5165");
      var label = btn.querySelector(".voice-record-label");
      if (label) label.textContent = "\u6309\u4F4F \u8BF4\u8BDD";
    }
    var bubble = document.getElementById("voice-transcript-bubble");
    if (bubble) bubble.classList.add("hidden");
  }
  function commitVoiceTranscript(text5) {
    var clean = (text5 || "").trim();
    if (!clean) return;
    var box = document.getElementById("input-box");
    if (!box) return;
    var existing = box.value || "";
    var joined = existing ? existing.replace(/\s+$/, "") + " " + clean : clean;
    box.value = joined;
    setDraftValue(joined, true);
    autoResizeInput(box);
    try {
      box.setSelectionRange(joined.length, joined.length);
    } catch (_) {
    }
  }
  function autoResizeInput(el) {
    if (!el) return;
    var minHeight = 36;
    var computedMaxHeight = parseFloat(window.getComputedStyle(el).maxHeight || "");
    var maxHeight = Number.isFinite(computedMaxHeight) ? Math.max(minHeight, computedMaxHeight) : 120;
    var touchDevice = isTouchDevice();
    if (!el.value || el.value.trim() === "") {
      el.style.height = minHeight + "px";
      el.style.minHeight = minHeight + "px";
      el.style.overflowY = touchDevice ? "auto" : "hidden";
      el.scrollTop = 0;
      el.classList.remove("has-multiline-draft", "has-clipped-draft");
      el.closest(".input-composer")?.classList.remove("is-expanded");
      syncComposerHasText(el);
      return;
    }
    var prevOverflow = el.style.overflowY;
    el.style.overflowY = "hidden";
    el.style.height = minHeight + "px";
    var contentHeight = el.scrollHeight;
    var newHeight = Math.max(minHeight, Math.min(contentHeight, maxHeight));
    var shouldScrollInside = contentHeight > maxHeight;
    var needsExpandedHeight = contentHeight > minHeight + 1;
    el.style.height = newHeight + "px";
    el.style.minHeight = minHeight + "px";
    el.style.overflowY = shouldScrollInside || touchDevice ? "auto" : "hidden";
    el.classList.toggle("has-multiline-draft", needsExpandedHeight);
    el.classList.toggle("has-clipped-draft", shouldScrollInside);
    el.closest(".input-composer")?.classList.toggle("is-expanded", needsExpandedHeight);
    if (shouldScrollInside) {
      syncInputBoxScroll(el);
    } else {
      el.scrollTop = 0;
    }
    syncComposerHasText(el);
  }
  function isSelectedSessionRunning() {
    if (!state.selectedId) return false;
    var selectedSession = state.sessions.find(function(session) {
      return session.id === state.selectedId;
    });
    if (isStructuredSession2(selectedSession)) {
      return !!(selectedSession.structuredState && selectedSession.structuredState.inFlight);
    }
    return !!selectedSession && selectedSession.status === "running";
  }
  var _queueLaunching = false;
  function sessionIsBusyForQueue(s) {
    if (!s || s.archived) return false;
    if (isStructuredSession2(s)) {
      return !!(s.structuredState && s.structuredState.inFlight);
    }
    return s.status === "running";
  }
  function hasAnyBusySession() {
    return state.sessions.some(sessionIsBusyForQueue);
  }
  function getContinuationTargetSession() {
    var candidates = state.sessions.filter(sessionIsBusyForQueue);
    if (candidates.length === 0) return null;
    if (state.selectedId) {
      var sel = candidates.find(function(s) {
        return s.id === state.selectedId;
      });
      if (sel) return sel;
    }
    candidates.sort(function(a, b) {
      return (Date.parse(b.startedAt) || 0) - (Date.parse(a.startedAt) || 0);
    });
    return candidates[0];
  }
  function getLastStructuredSubmittedInput(session) {
    if (!session) return "";
    var queue = Array.isArray(session.queuedMessages) ? session.queuedMessages : [];
    for (var qi = queue.length - 1; qi >= 0; qi--) {
      var queued = typeof queue[qi] === "string" ? queue[qi].trim() : "";
      if (queued) return queued;
    }
    var messages = Array.isArray(session.messages) ? session.messages : [];
    for (var mi = messages.length - 1; mi >= 0; mi--) {
      var turn = messages[mi];
      if (!turn || turn.role !== "user" || !Array.isArray(turn.content)) continue;
      var textParts = turn.content.filter(function(block2) {
        return block2 && block2.type === "text" && typeof block2.text === "string";
      }).map(function(block2) {
        return block2.text;
      });
      if (textParts.length) return textParts.join("\n").trim();
      for (var bi = 0; bi < turn.content.length; bi++) {
        var block = turn.content[bi];
        if (block && block.type === "tool_result" && typeof block.content === "string") {
          return block.content.trim();
        }
      }
      return "";
    }
    return "";
  }
  function continueStructuredSession(session, text5) {
    var normalizedText2 = typeof text5 === "string" ? text5.trim() : "";
    if (normalizedText2 && getLastStructuredSubmittedInput(session) === normalizedText2) {
      showToast("\u4E0E\u4E0A\u4E00\u6761\u6D88\u606F\u76F8\u540C\uFF0C\u5DF2\u5FFD\u7565\uFF0C\u4E0D\u4F1A\u52A0\u5165\u6392\u961F\u3002", "warning");
      return Promise.resolve();
    }
    var idempotencyKey = typeof crypto !== "undefined" && crypto.randomUUID ? crypto.randomUUID() : Date.now().toString(36) + "-" + Math.random().toString(36).slice(2, 10);
    var prevQueue = Array.isArray(session.queuedMessages) ? session.queuedMessages.slice() : [];
    var nextQueue = prevQueue.slice();
    nextQueue.push(text5);
    updateSessionSnapshot({ id: session.id, queuedMessages: nextQueue });
    if (session.id === state.selectedId) updateQueueBar();
    var label = session.title || shortCommand(session.command) || "\u5F53\u524D\u4F1A\u8BDD";
    showToast("\u5DF2\u52A0\u5165\u300C" + label + "\u300D\u7684\u6392\u961F\uFF0C\u56DE\u590D\u7ED3\u675F\u540E\u81EA\u52A8\u53D1\u9001\uFF08\u542B\u4E0A\u4E0B\u6587\uFF09\u3002", "info");
    return fetch("/api/structured-sessions/" + session.id + "/messages", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      credentials: "same-origin",
      body: JSON.stringify({ input: text5, idempotencyKey })
    }).then(function(res) {
      if (!res.ok) {
        return res.json().catch(function() {
          return { error: "\u8BF7\u6C42\u5931\u8D25" };
        }).then(function(p) {
          throw new Error(p && p.error || "\u65E0\u6CD5\u6392\u961F\u6D88\u606F\u3002");
        });
      }
      return res.json();
    }).then(function(snapshot7) {
      if (snapshot7 && snapshot7.id) {
        updateSessionSnapshot(snapshot7);
        if (snapshot7.id === state.selectedId) updateQueueBar();
      }
    }).catch(function(err) {
      updateSessionSnapshot({ id: session.id, queuedMessages: prevQueue });
      if (session.id === state.selectedId) updateQueueBar();
      showToast(err && err.message || "\u6392\u961F\u5931\u8D25\uFF0C\u8BF7\u91CD\u8BD5\u3002", "error");
    });
  }
  function enqueueCrossSessionMessage(text5) {
    var target = getContinuationTargetSession();
    if (target && isStructuredSession2(target)) {
      continueStructuredSession(target, text5);
      return;
    }
    if (state.crossSessionQueue.length >= 10) {
      showToast("\u6392\u961F\u6D88\u606F\u5DF2\u6EE1\uFF08\u6700\u591A 10 \u6761\uFF09\uFF0C\u8BF7\u7B49\u5F85\u5F53\u524D\u4F1A\u8BDD\u5B8C\u6210\u3002", "warning");
      return;
    }
    var id = "csq-" + Date.now() + "-" + Math.random().toString(36).slice(2, 8);
    state.crossSessionQueue.push({
      id,
      text: text5,
      cwd: getEffectiveCwd(),
      mode: state.chatMode || "managed",
      tool: getPreferredTool(),
      queuedAt: Date.now()
    });
    persistCrossSessionQueue();
    renderCrossSessionQueue();
    showToast("\u5DF2\u6392\u961F\uFF0C\u5C06\u5728\u7A7A\u95F2\u540E\u81EA\u52A8\u5F00\u59CB\u65B0\u4F1A\u8BDD\u3002", "info");
  }
  function launchQueueItem(item) {
    if (_queueLaunching) return;
    _queueLaunching = true;
    fetch("/api/commands", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      credentials: "same-origin",
      body: JSON.stringify(withTerminalDimensions({
        command: item.tool,
        cwd: item.cwd,
        mode: item.mode,
        initialInput: item.text
      }))
    }).then(function(res) {
      return res.json();
    }).then(function(data) {
      _queueLaunching = false;
      if (data.error) {
        showToast(data.error, "error");
        state.crossSessionQueue.unshift(item);
        persistCrossSessionQueue();
        renderCrossSessionQueue();
        return null;
      }
      return activateSession(data);
    }).catch(function(error) {
      _queueLaunching = false;
      showToast(error && error.message || "\u65E0\u6CD5\u542F\u52A8\u6392\u961F\u4F1A\u8BDD\u3002", "error");
      state.crossSessionQueue.unshift(item);
      persistCrossSessionQueue();
      renderCrossSessionQueue();
    });
  }
  function sendQueueItemNow(queueId) {
    var idx = state.crossSessionQueue.findIndex(function(q) {
      return q.id === queueId;
    });
    if (idx < 0) return;
    var item = state.crossSessionQueue.splice(idx, 1)[0];
    persistCrossSessionQueue();
    renderCrossSessionQueue();
    fetch("/api/commands", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      credentials: "same-origin",
      body: JSON.stringify(withTerminalDimensions({
        command: item.tool,
        cwd: item.cwd,
        mode: item.mode,
        initialInput: item.text
      }))
    }).then(function(res) {
      return res.json();
    }).then(function(data) {
      if (data.error) {
        showToast(data.error, "error");
        state.crossSessionQueue.splice(idx, 0, item);
        persistCrossSessionQueue();
        renderCrossSessionQueue();
        return null;
      }
      return activateSession(data);
    }).catch(function(error) {
      showToast(error && error.message || "\u65E0\u6CD5\u542F\u52A8\u6392\u961F\u4F1A\u8BDD\u3002", "error");
      state.crossSessionQueue.splice(idx, 0, item);
      persistCrossSessionQueue();
      renderCrossSessionQueue();
    });
  }
  function cancelQueueItem(queueId) {
    var idx = state.crossSessionQueue.findIndex(function(q) {
      return q.id === queueId;
    });
    if (idx < 0) return;
    state.crossSessionQueue.splice(idx, 1);
    persistCrossSessionQueue();
    renderCrossSessionQueue();
    if (state.crossSessionQueue.length === 0) {
      showToast("\u6392\u961F\u5DF2\u6E05\u7A7A\u3002", "info");
    }
  }
  function flushCrossSessionQueue() {
    if (state.crossSessionQueue.length === 0) return;
    if (hasAnyBusySession()) return;
    if (_queueLaunching) return;
    var item = state.crossSessionQueue.shift();
    renderCrossSessionQueue();
    launchQueueItem(item);
  }
  function formatQueueAge(queuedAt) {
    var sec = Math.floor((Date.now() - queuedAt) / 1e3);
    if (sec < 60) return sec + "s";
    var min2 = Math.floor(sec / 60);
    if (min2 < 60) return min2 + "m";
    return Math.floor(min2 / 60) + "h";
  }
  function renderCrossSessionQueue() {
    var container = document.querySelector(".cross-session-queue");
    var inputPanel = document.querySelector(".input-panel");
    var statusBar = document.querySelector(".structured-status-bar");
    var composer = document.querySelector(".input-composer");
    var blankQueueHost = document.getElementById("cross-session-queue-host");
    if (state.crossSessionQueue.length === 0) {
      if (container) container.remove();
      persistCrossSessionQueue();
      return;
    }
    var isInputPanelVisible = inputPanel && !inputPanel.classList.contains("hidden");
    var parent = isInputPanelVisible ? inputPanel : blankQueueHost;
    var insertBefore = isInputPanelVisible ? statusBar || composer : null;
    if (!parent) return;
    if (container && container.parentNode !== parent) {
      container.remove();
      container = null;
    }
    if (!container) {
      container = document.createElement("div");
      container.className = "cross-session-queue";
      if (insertBefore) {
        parent.insertBefore(container, insertBefore);
      } else {
        parent.appendChild(container);
      }
    } else if (isInputPanelVisible && insertBefore && container.nextSibling !== insertBefore) {
      parent.insertBefore(container, insertBefore);
    }
    var total = state.crossSessionQueue.length;
    var items = state.crossSessionQueue.map(function(item, i) {
      var preview = item.text.length > 60 ? item.text.slice(0, 60) + "\u2026" : item.text;
      var age = formatQueueAge(item.queuedAt);
      return '<div class="queue-item" data-queue-id="' + escapeHtml(item.id) + '"><span class="queue-item-dot"></span><span class="queue-item-text" title="' + escapeHtml(item.text) + '">' + escapeHtml(preview) + '</span><span class="queue-item-age">' + age + '</span><button class="queue-item-send-now" data-queue-id="' + escapeHtml(item.id) + '" title="\u7ACB\u5373\u53D1\u9001" type="button">\u53D1\u9001</button><button class="queue-item-cancel" data-queue-id="' + escapeHtml(item.id) + '" title="\u53D6\u6D88" type="button">\xD7</button></div>';
    }).join("");
    var header = total > 1 ? '<div class="queue-header"><span class="queue-header-label">\u6392\u961F ' + total + ' \u6761</span><button class="queue-header-clear" id="queue-clear-all" type="button" title="\u6E05\u7A7A\u6392\u961F">\u6E05\u7A7A</button></div>' : "";
    container.innerHTML = header + items;
  }
  setInterval(function() {
    if (state.crossSessionQueue.length > 0) {
      var ages = document.querySelectorAll(".queue-item-age");
      state.crossSessionQueue.forEach(function(item, i) {
        if (ages[i]) ages[i].textContent = formatQueueAge(item.queuedAt);
      });
      flushCrossSessionQueue();
    }
  }, 5e3);
  document.addEventListener("click", function(e) {
    var target = e.target;
    if (target.closest("#queue-clear-all")) {
      e.preventDefault();
      state.crossSessionQueue = [];
      persistCrossSessionQueue();
      renderCrossSessionQueue();
      showToast("\u6392\u961F\u5DF2\u6E05\u7A7A\u3002", "info");
      return;
    }
    var sendNow = target.closest(".queue-item-send-now");
    if (sendNow) {
      e.preventDefault();
      sendQueueItemNow(sendNow.dataset.queueId);
      return;
    }
    var cancel = target.closest(".queue-item-cancel");
    if (cancel) {
      e.preventDefault();
      cancelQueueItem(cancel.dataset.queueId);
      return;
    }
  });
  function welcomeInputSend() {
    var welcomeInput = document.getElementById("welcome-input");
    var value = welcomeInput ? welcomeInput.value.trim() : "";
    if (!value) return;
    if (hasAnyBusySession()) {
      welcomeInput.value = "";
      enqueueCrossSessionMessage(value);
      return;
    }
    var todoEl = document.getElementById("todo-progress");
    if (todoEl) todoEl.classList.add("hidden");
    welcomeInput.value = "";
    welcomeInput.placeholder = "\u6B63\u5728\u542F\u52A8\u2026";
    welcomeInput.disabled = true;
    var mode = state.chatMode || "managed";
    var defaultCwd2 = getEffectiveCwd();
    var preferredTool = getPreferredTool();
    fetch("/api/commands", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      credentials: "same-origin",
      body: JSON.stringify(withTerminalDimensions({
        command: preferredTool,
        provider: preferredTool,
        cwd: defaultCwd2,
        mode,
        initialInput: value
      }))
    }).then(function(res) {
      return res.json();
    }).then(function(data) {
      if (data.error) {
        showToast(data.error, "error");
        welcomeInput.placeholder = "\u8F93\u5165\u6D88\u606F";
        welcomeInput.disabled = false;
        return;
      }
      state.selectedId = data.id;
      persistSelectedId();
      state.drafts[data.id] = "";
      resetChatRenderCache();
      updateSessionSnapshot(data);
      updateSessionsList();
      switchToSessionView2(data.id);
      subscribeToSession(data.id);
      loadOutput(data.id).then(function() {
        welcomeInput.placeholder = "\u8F93\u5165\u6D88\u606F";
        welcomeInput.disabled = false;
        focusInputBox2(true);
      });
    }).catch(function(error) {
      showToast(error && error.message || (preferredTool === "codex" ? "\u65E0\u6CD5\u542F\u52A8 Codex \u4F1A\u8BDD\u3002" : "\u65E0\u6CD5\u542F\u52A8 Claude \u4F1A\u8BDD\u3002"), "error");
      welcomeInput.placeholder = "\u8F93\u5165\u6D88\u606F";
      welcomeInput.disabled = false;
    });
  }
  function sendOrStart(opts) {
    opts = opts || {};
    var welcomeInput = document.getElementById("welcome-input");
    var inputBox = document.getElementById("input-box");
    var value = welcomeInput && welcomeInput.value.trim() ? welcomeInput.value.trim() : inputBox ? inputBox.value.trim() : "";
    if (state.selectedId) {
      if (value) {
        sendInputFromBox(opts);
      }
      return;
    }
    if (value && hasAnyBusySession()) {
      if (inputBox) inputBox.value = "";
      if (welcomeInput) welcomeInput.value = "";
      syncComposerHasText(inputBox);
      enqueueCrossSessionMessage(value);
      return;
    }
    var mode = state.chatMode || "managed";
    var defaultCwd2 = getEffectiveCwd();
    var preferredTool = getPreferredTool();
    fetch("/api/commands", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      credentials: "same-origin",
      body: JSON.stringify(withTerminalDimensions({
        command: preferredTool,
        provider: preferredTool,
        cwd: defaultCwd2,
        mode,
        initialInput: value || void 0
      }))
    }).then(function(res) {
      return res.json();
    }).then(function(data) {
      if (data.error) {
        showToast(data.error, "error");
        return null;
      }
      state.selectedId = data.id;
      persistSelectedId();
      state.drafts[data.id] = "";
      resetChatRenderCache();
      if (inputBox) inputBox.value = "";
      if (welcomeInput) welcomeInput.value = "";
      updateSessionSnapshot(data);
      updateSessionsList();
      switchToSessionView2(data.id);
      subscribeToSession(data.id);
      return loadOutput(data.id);
    }).catch(function(error) {
      showToast(error && error.message || (preferredTool === "codex" ? "\u65E0\u6CD5\u542F\u52A8 Codex \u4F1A\u8BDD\u3002" : "\u65E0\u6CD5\u542F\u52A8 Claude \u4F1A\u8BDD\u3002"), "error");
    });
  }
  function switchToSessionView2(sessionId) {
    var reactShellActive = isBrowserReactShellMounted();
    var session = state.sessions.find(function(s) {
      return s.id === sessionId;
    });
    var blankChat = document.getElementById("blank-chat");
    var terminalContainer = document.getElementById("output");
    var chatContainer = document.getElementById("chat-output");
    var stopBtn = document.getElementById("stop-button");
    var terminalTitle = document.getElementById("terminal-title");
    var terminalInfo = document.getElementById("terminal-info");
    var sessionSummary = document.querySelector(".session-summary-value");
    var structured = isStructuredSession2(session);
    if (!reactShellActive && blankChat) blankChat.classList.add("hidden");
    if (!reactShellActive && terminalContainer) {
      terminalContainer.classList.toggle("hidden", structured);
    }
    if (!reactShellActive && chatContainer) {
      chatContainer.classList.remove("hidden");
    }
    if (structured) {
      state.currentView = "chat";
    } else {
      state.currentView = "terminal";
    }
    var title = session ? shortCommand(session.command) : "Wand";
    var info = session ? getSessionStatusLabel(session) : "\u5F00\u59CB\u5BF9\u8BDD";
    if (terminalTitle) terminalTitle.textContent = title;
    if (terminalInfo) terminalInfo.textContent = info;
    if (sessionSummary) sessionSummary.textContent = title;
    if (!structured) {
      if (!state.terminal) initTerminal2();
    }
    applyCurrentView();
    focusInputBox2(true);
    if (!structured) ensureTerminalFit("view-switch", { forceReplay: true });
    notifyLegacyUiChange("session:view");
  }
  function sendInputFromBox(opts) {
    opts = opts || {};
    var interruptFlag = !!opts.interrupt;
    var embedTerminal = document.documentElement.classList.contains("is-wand-embed-terminal");
    if (state.terminalInteractive && !embedTerminal) {
      showToast("\u7EC8\u7AEF\u4EA4\u4E92\u6A21\u5F0F\u5F00\u542F\u65F6\uFF0C\u8BF7\u76F4\u63A5\u5728\u7EC8\u7AEF\u4E2D\u8F93\u5165\u3002", "info");
      return Promise.resolve();
    }
    var inputBox = document.getElementById("input-box");
    var value = inputBox ? inputBox.value : "";
    var selectedSession = getSelectedSession();
    var hasAttachments = state.pendingAttachments.length > 0;
    if (value || hasAttachments) {
      var attachUpload = hasAttachments && state.selectedId ? uploadAttachments(state.selectedId).catch(function(err) {
        showToast("\u9644\u4EF6\u4E0A\u4F20\u5931\u8D25: " + (err && err.message || err), "error");
        var marked = err instanceof Error ? err : new Error(String(err));
        marked.__wandToasted = true;
        throw marked;
      }) : Promise.resolve([]);
      return attachUpload.then(function(uploadedFiles) {
        var prefix = buildAttachmentPrefix(uploadedFiles);
        var finalValue = prefix + (value || (uploadedFiles.length ? "\u8BF7\u67E5\u770B\u9644\u4EF6\u3002" : ""));
        if (uploadedFiles.length) clearAttachments();
        var todoEl = document.getElementById("todo-progress");
        if (todoEl) todoEl.classList.add("hidden");
        if (isStructuredSession2(selectedSession)) {
          return postStructuredInput(finalValue, inputBox, selectedSession, { interrupt: interruptFlag });
        }
        var submitChunks = getTerminalSubmitChunks(selectedSession, finalValue);
        var isOffline = !state.wsConnected;
        if (isOffline) {
          queueOfflineTerminalChunks(submitChunks);
          if (inputBox) {
            inputBox.value = "";
            autoResizeInput(inputBox);
          }
          setDraftValue("");
          return Promise.resolve();
        }
        return ensureSessionReadyForInput(selectedSession).then(function(readySession) {
          if (!readySession) {
            return null;
          }
          var submitView = state.currentView;
          if (readySession && readySession.provider === "codex" && state.selectedId !== readySession.id) {
            throw new Error("Codex session changed before input send.");
          }
          prepareChatBottomFollow();
          return sendTerminalChunks(submitChunks, "enter_text", 30, submitView).then(function() {
            if (inputBox && inputBox.value === value) {
              inputBox.value = "";
              autoResizeInput(inputBox);
            }
            setDraftValue("");
          });
        }).catch(function(err) {
          showToast(getInputErrorMessage(err), "error");
          if (err) err.__wandToasted = true;
          throw err;
        });
      }).catch(function(err) {
        if (!(err && err.__wandToasted)) {
          showToast(getInputErrorMessage(err), "error");
        }
        throw err;
      });
    }
    return Promise.resolve();
  }
  var _structuredLastSubmitAt = {};
  var DUPLICATE_SUBMIT_WINDOW_MS = 350;
  function postStructuredInput(input, inputBox, session, opts) {
    opts = opts || {};
    var requestedInterrupt = !!opts.interrupt;
    if (!state.selectedId || !input) return Promise.resolve();
    if (!session) {
      showToast("\u4F1A\u8BDD\u4E0D\u5B58\u5728\uFF0C\u8BF7\u91CD\u65B0\u9009\u62E9\u6216\u65B0\u5EFA\u4F1A\u8BDD\u3002", "error");
      return Promise.resolve();
    }
    var sessionInFlight = !!(session.structuredState && session.structuredState.inFlight && session.status === "running");
    if (sessionInFlight && !requestedInterrupt && getLastStructuredSubmittedInput(session) === input.trim()) {
      if (inputBox) {
        inputBox.value = "";
        autoResizeInput(inputBox);
      }
      setDraftValue("");
      showToast("\u4E0E\u4E0A\u4E00\u6761\u6D88\u606F\u76F8\u540C\uFF0C\u5DF2\u5FFD\u7565\uFF0C\u4E0D\u4F1A\u52A0\u5165\u6392\u961F\u3002", "warning");
      updateInputHint("Enter \u53D1\u9001 \xB7 Shift+Enter \u6362\u884C");
      return Promise.resolve();
    }
    var nowTs = Date.now();
    var lastTs = _structuredLastSubmitAt[session.id] || 0;
    if (nowTs - lastTs < DUPLICATE_SUBMIT_WINDOW_MS) {
      console.log("[wand] postStructuredInput: duplicate submit (within " + DUPLICATE_SUBMIT_WINDOW_MS + "ms) ignored for session", session.id);
      return Promise.resolve();
    }
    _structuredLastSubmitAt[session.id] = nowTs;
    var isInterrupting = sessionInFlight && requestedInterrupt;
    var isQueueing = sessionInFlight && !requestedInterrupt;
    var userMsgs = stripRenderOnlyStructuredMessages(Array.isArray(session.messages) ? session.messages.slice() : []);
    var optimisticPatch;
    if (isQueueing) {
      var nextQueue = Array.isArray(session.queuedMessages) ? session.queuedMessages.slice() : [];
      nextQueue.push(input);
      optimisticPatch = {
        id: session.id,
        queuedMessages: nextQueue
      };
      updateSessionSnapshot(optimisticPatch);
      var queueRefreshed = state.sessions.find(function(s) {
        return s.id === session.id;
      }) || session;
      state.currentMessages = buildMessagesForRender(queueRefreshed, getPreferredMessages(queueRefreshed, queueRefreshed.output, false));
      updateInputHint("\u5DF2\u52A0\u5165\u6392\u961F\u2026");
      renderChat(true);
      updateStructuredQueueCounter();
      showToast(nextQueue.length > 1 ? "\u5DF2\u52A0\u5165\u6392\u961F\uFF08\u5171 " + nextQueue.length + " \u6761\u7B49\u5F85\uFF09" : "\u5DF2\u52A0\u5165\u6392\u961F\uFF0C\u7B49\u5F53\u524D\u56DE\u590D\u5B8C\u6210\u4F1A\u81EA\u52A8\u53D1\u9001\u3002", "info");
    } else {
      var userTurn = { role: "user", content: [{ type: "text", text: input }] };
      userMsgs.push(userTurn);
      var optimisticStructuredState = Object.assign({}, session.structuredState || {}, { inFlight: true });
      updateSessionSnapshot({
        id: session.id,
        status: "running",
        messages: userMsgs,
        structuredState: optimisticStructuredState
      });
      state.currentMessages = buildMessagesForRender(Object.assign({}, session, {
        status: "running",
        messages: userMsgs,
        structuredState: optimisticStructuredState
      }), userMsgs);
      updateInputHint(isInterrupting ? "\u5DF2\u4E2D\u65AD\uFF0C\u6B63\u5728\u5904\u7406\u65B0\u6D88\u606F\u2026" : "\u601D\u8003\u4E2D\u2026");
      prepareChatBottomFollow();
      renderChat(true);
      if (isInterrupting) {
        showToast("\u5DF2\u4E2D\u65AD\u4E0A\u4E00\u6761\u56DE\u590D\uFF0C\u6B63\u5728\u5904\u7406\u65B0\u6D88\u606F\u2026", "info");
      }
    }
    if (inputBox) {
      inputBox.value = "";
      autoResizeInput(inputBox);
    }
    setDraftValue("");
    var epochBeforePost = state.queueEpoch;
    var idempotencyKey = typeof crypto !== "undefined" && crypto.randomUUID ? crypto.randomUUID() : Date.now().toString(36) + "-" + Math.random().toString(36).slice(2, 10);
    return fetch("/api/structured-sessions/" + session.id + "/messages", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      credentials: "same-origin",
      body: JSON.stringify({ input, interrupt: isInterrupting || void 0, idempotencyKey })
    }).then(function(res) {
      if (!res.ok) {
        return res.json().catch(function() {
          return { error: "\u8BF7\u6C42\u5931\u8D25" };
        }).then(function(payload) {
          var err = new Error(payload && payload.error || "\u65E0\u6CD5\u53D1\u9001\u7ED3\u6784\u5316\u6D88\u606F\u3002");
          err.errorCode = payload && payload.errorCode;
          err.httpStatus = res.status;
          throw err;
        });
      }
      return res.json();
    }).then(function(snapshot7) {
      if (snapshot7 && snapshot7.error) {
        throw new Error(snapshot7.error);
      }
      if (snapshot7 && snapshot7.id) {
        if (state.queueEpoch > epochBeforePost && snapshot7.queuedMessages) {
          delete snapshot7.queuedMessages;
        }
        updateSessionSnapshot(snapshot7);
        if (snapshot7.id === state.selectedId) {
          var refreshedSession = state.sessions.find(function(s) {
            return s.id === snapshot7.id;
          }) || snapshot7;
          state.currentMessages = buildMessagesForRender(refreshedSession, getPreferredMessages(refreshedSession, snapshot7.output, false));
          renderChat(true);
          updateStructuredQueueCounter();
        }
      }
    }).catch(function(error) {
      if (error && error.errorCode === "duplicate_idempotency_key") {
        showToast(error.message || "\u68C0\u6D4B\u5230\u91CD\u590D\u53D1\u9001\uFF0C\u5DF2\u62E6\u622A\u3002", "warning");
        updateInputHint("Enter \u53D1\u9001 \xB7 Shift+Enter \u6362\u884C");
        return;
      }
      if (isQueueing) {
        var prevQueue = Array.isArray(session.queuedMessages) ? session.queuedMessages.slice() : [];
        updateSessionSnapshot({
          id: session.id,
          queuedMessages: prevQueue
        });
        if (session.id === state.selectedId) {
          var rolledQueueSession = state.sessions.find(function(s) {
            return s.id === session.id;
          }) || session;
          state.currentMessages = buildMessagesForRender(rolledQueueSession, getPreferredMessages(rolledQueueSession, rolledQueueSession.output, false));
          renderChat(true);
          updateStructuredQueueCounter();
        }
      } else {
        var rollbackMsgs = userMsgs.slice(0, -1);
        updateSessionSnapshot({
          id: session.id,
          status: session.status,
          messages: rollbackMsgs,
          structuredState: Object.assign({}, session.structuredState || {}, { inFlight: false })
        });
        if (session.id === state.selectedId) {
          state.currentMessages = buildMessagesForRender(
            Object.assign({}, session, { messages: rollbackMsgs, structuredState: Object.assign({}, session.structuredState || {}, { inFlight: false }) }),
            rollbackMsgs
          );
          renderChat(true);
        }
      }
      var message = error && error.message || "";
      var isTransientAbort = message === "Failed to fetch" || message === "NetworkError when attempting to fetch resource." || message === "Load failed" || /aborted|aborterror|networkerror|failed to fetch/i.test(message);
      if (!isTransientAbort) {
        showToast(error && error.message || "\u65E0\u6CD5\u53D1\u9001\u7ED3\u6784\u5316\u6D88\u606F\u3002", "error");
      }
      updateInputHint("Enter \u53D1\u9001 \xB7 Shift+Enter \u6362\u884C");
    });
  }
  function updateInputHint(text5) {
    var hint = document.querySelector(".input-hint");
    if (hint) hint.textContent = text5;
  }
  function updateStructuredQueueCounter() {
    updateQueueBar();
  }
  var QUEUE_BAR_MAX = 10;
  var QUEUE_CHIP_MAX_TEXT = 26;
  function queueChipTruncate(text5) {
    if (typeof text5 !== "string") return "";
    var s = text5.replace(/\s+/g, " ").trim();
    if (s.length <= QUEUE_CHIP_MAX_TEXT) return s;
    return s.slice(0, QUEUE_CHIP_MAX_TEXT) + "\u2026";
  }
  function isQueueBarExpanded() {
    return !!state.queueBarExpanded;
  }
  function setQueueBarExpanded(expanded) {
    if (!!state.queueBarExpanded === !!expanded) return;
    state.queueBarExpanded = !!expanded;
    var bar = document.querySelector(".queue-bar");
    if (bar) bar.classList.toggle("expanded", !!expanded);
  }
  function renderQueueBarHtml(items, inFlight, atCapacity) {
    var n = items.length;
    var barClass = "queue-bar";
    if (atCapacity) barClass += " queue-bar-capacity";
    if (inFlight) barClass += " queue-bar-inflight";
    var promoteTitle = inFlight ? "\u4E2D\u65AD\u5F53\u524D\u56DE\u590D\uFF0C\u7ACB\u5373\u53D1\u9001\u8FD9\u6761" : "\u7ACB\u5373\u53D1\u9001\u8FD9\u6761";
    var chipNodes = "";
    for (var i = 0; i < n; i++) {
      var raw = items[i] == null ? "" : String(items[i]);
      var displayText = queueChipTruncate(raw);
      var titleAttr = raw + "\uFF08\u6309\u4F4F\u53EF\u62D6\u52A8\u8C03\u5E8F\uFF09";
      chipNodes += '<li class="queue-bar-item" data-index="' + i + '" data-action="drag" title="' + escapeHtml(titleAttr) + '"><span class="queue-bar-item-index" aria-hidden="true">' + (i + 1) + '</span><span class="queue-bar-item-text">' + escapeHtml(displayText) + '</span><button type="button" class="queue-bar-item-promote" data-action="promote-item" title="' + escapeHtml(promoteTitle) + '" aria-label="\u7ACB\u5373\u53D1\u9001\u7B2C ' + (i + 1) + ' \u6761"><svg width="11" height="11" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true"><path d="M13 2 L4 14 L11 14 L10 22 L20 9 L13 9 Z"/></svg></button><button type="button" class="queue-bar-item-delete" data-action="delete" aria-label="\u5220\u9664\u7B2C ' + (i + 1) + ' \u6761\u6392\u961F\u6D88\u606F" title="\u5220\u9664"><svg width="11" height="11" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.6" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><line x1="6" y1="6" x2="18" y2="18"/><line x1="6" y1="18" x2="18" y2="6"/></svg></button></li>';
    }
    var headerBar = "";
    if (n >= 2) {
      headerBar = '<div class="queue-bar-head"><span class="queue-bar-head-count">' + n + ' \u6761\u6392\u961F</span><button type="button" class="queue-bar-clear-all" data-action="clear-all" title="\u6E05\u7A7A\u5168\u90E8\u6392\u961F" aria-label="\u6E05\u7A7A\u5168\u90E8 ' + n + ' \u6761\u6392\u961F\u6D88\u606F">\u6E05\u7A7A</button></div>';
    }
    return '<div class="' + barClass + '" data-queue-bar="1" title="\u6392\u961F ' + n + ' \u6761\uFF08\u6309\u4F4F\u6C14\u6CE1\u53EF\u8C03\u5E8F\uFF09">' + headerBar + '<ol class="queue-bar-list" data-queue-list="1">' + chipNodes + "</ol></div>";
  }
  function updateQueueBar() {
    var host = document.getElementById("queue-bar-host");
    if (!host) return;
    var session = state.sessions.find(function(s) {
      return s.id === state.selectedId;
    });
    var isStructured = session && session.sessionKind === "structured";
    var queue = isStructured ? getStructuredQueuedInputs(session) : [];
    queue = Array.isArray(queue) ? queue : [];
    if (!isStructured || queue.length === 0) {
      host.hidden = true;
      host.innerHTML = "";
      state.queueBarExpanded = false;
      return;
    }
    if (state.queueBarDrag) return;
    host.hidden = false;
    var inFlight = !!(session.structuredState && session.structuredState.inFlight && session.status === "running");
    var atCapacity = queue.length >= QUEUE_BAR_MAX;
    host.innerHTML = renderQueueBarHtml(queue, inFlight, atCapacity);
  }
  function rollbackQueueOptimistic(session, prevQueue) {
    updateSessionSnapshot({ id: session.id, queuedMessages: prevQueue });
    var refreshed = state.sessions.find(function(s) {
      return s.id === session.id;
    }) || session;
    state.currentMessages = buildMessagesForRender(refreshed, getPreferredMessages(refreshed, refreshed.output, false));
    renderChat(true);
    updateQueueBar();
  }
  function queueBarDeleteItem(index2) {
    var session = state.sessions.find(function(s) {
      return s.id === state.selectedId;
    });
    if (!session) return;
    var queue = Array.isArray(session.queuedMessages) ? session.queuedMessages.slice() : [];
    if (index2 < 0 || index2 >= queue.length) return;
    var prev = queue.slice();
    var next = queue.slice(0, index2).concat(queue.slice(index2 + 1));
    updateSessionSnapshot({ id: session.id, queuedMessages: next });
    var refreshed = state.sessions.find(function(s) {
      return s.id === session.id;
    }) || session;
    state.currentMessages = buildMessagesForRender(refreshed, getPreferredMessages(refreshed, refreshed.output, false));
    renderChat(true);
    updateQueueBar();
    fetch("/api/structured-sessions/" + session.id + "/queued/" + index2, {
      method: "DELETE",
      credentials: "same-origin"
    }).then(function(res) {
      if (!res.ok) {
        return res.json().catch(function() {
          return {};
        }).then(function(p) {
          throw new Error(p && p.error || "\u5220\u9664\u5931\u8D25");
        });
      }
    }).catch(function(err) {
      rollbackQueueOptimistic(session, prev);
      showToast(err && err.message || "\u5220\u9664\u6392\u961F\u6D88\u606F\u5931\u8D25\u3002", "error");
    });
  }
  function queueBarClearAll() {
    var session = state.sessions.find(function(s) {
      return s.id === state.selectedId;
    });
    if (!session) return;
    var prev = Array.isArray(session.queuedMessages) ? session.queuedMessages.slice() : [];
    if (prev.length === 0) return;
    state.queueBarExpanded = false;
    updateSessionSnapshot({ id: session.id, queuedMessages: [] });
    var refreshed = state.sessions.find(function(s) {
      return s.id === session.id;
    }) || session;
    state.currentMessages = buildMessagesForRender(refreshed, getPreferredMessages(refreshed, refreshed.output, false));
    renderChat(true);
    updateQueueBar();
    fetch("/api/structured-sessions/" + session.id + "/queued", {
      method: "DELETE",
      credentials: "same-origin"
    }).then(function(res) {
      if (!res.ok) {
        return res.json().catch(function() {
          return {};
        }).then(function(p) {
          throw new Error(p && p.error || "\u6E05\u7A7A\u5931\u8D25");
        });
      }
      showToast("\u5DF2\u6E05\u7A7A " + prev.length + " \u6761\u6392\u961F\u6D88\u606F\u3002", "info");
    }).catch(function(err) {
      rollbackQueueOptimistic(session, prev);
      showToast(err && err.message || "\u6E05\u7A7A\u6392\u961F\u6D88\u606F\u5931\u8D25\u3002", "error");
    });
  }
  function queueBarPromoteIndex(index2) {
    if (state.queueBarPromoting) return;
    var session = state.sessions.find(function(s) {
      return s.id === state.selectedId;
    });
    if (!session) return;
    var queue = Array.isArray(session.queuedMessages) ? session.queuedMessages.slice() : [];
    if (index2 < 0 || index2 >= queue.length) return;
    var picked = queue[index2];
    var rest = queue.slice(0, index2).concat(queue.slice(index2 + 1));
    var prev = queue.slice();
    var inFlight = !!(session.structuredState && session.structuredState.inFlight && session.status === "running");
    state.queueBarPromoting = true;
    if (rest.length === 0) {
      state.queueBarExpanded = false;
    }
    updateSessionSnapshot({ id: session.id, queuedMessages: rest });
    var idempotencyKey = typeof crypto !== "undefined" && crypto.randomUUID ? crypto.randomUUID() : Date.now().toString(36) + "-" + Math.random().toString(36).slice(2, 10);
    showToast(inFlight ? "\u5DF2\u8BF7\u6C42\u4E2D\u65AD\u5F53\u524D\u56DE\u590D\uFF0C\u7ACB\u5373\u53D1\u9001\u8FD9\u6761\u3002" : "\u5DF2\u7ACB\u5373\u53D1\u9001\u8FD9\u6761\u6D88\u606F\u3002", "info");
    fetch("/api/structured-sessions/" + session.id + "/queued/" + index2 + "/promote", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      credentials: "same-origin",
      body: JSON.stringify({ expectedText: picked, idempotencyKey })
    }).then(function(res) {
      if (!res.ok) {
        return res.json().catch(function() {
          return {};
        }).then(function(p) {
          throw new Error(p && p.error || "\u7ACB\u5373\u53D1\u9001\u5931\u8D25");
        });
      }
      return res.json();
    }).then(function(snapshot7) {
      if (snapshot7 && snapshot7.id) {
        updateSessionSnapshot(snapshot7);
        if (snapshot7.id === state.selectedId) {
          var refreshed = state.sessions.find(function(s) {
            return s.id === snapshot7.id;
          }) || snapshot7;
          state.currentMessages = buildMessagesForRender(refreshed, getPreferredMessages(refreshed, snapshot7.output, false));
          renderChat(true);
          updateQueueBar();
        }
      }
      state.queueBarPromoting = false;
    }).catch(function(err) {
      state.queueBarPromoting = false;
      rollbackQueueOptimistic(session, prev);
      showToast(err && err.message || "\u7ACB\u5373\u53D1\u9001\u5931\u8D25\u3002", "error");
    });
  }
  function queueBarDragStart(ev, chipEl) {
    var session = state.sessions.find(function(s) {
      return s.id === state.selectedId;
    });
    if (!session) return;
    var queue = Array.isArray(session.queuedMessages) ? session.queuedMessages.slice() : [];
    if (queue.length <= 1) return;
    if (!chipEl) return;
    var listEl = chipEl.parentElement;
    if (!listEl) return;
    var origIndex = Number(chipEl.getAttribute("data-index"));
    var siblings = Array.prototype.slice.call(listEl.children);
    var rects = siblings.map(function(el) {
      return el.getBoundingClientRect();
    });
    var gap = 3;
    if (rects.length >= 2) gap = Math.max(0, rects[1].top - rects[0].top - rects[0].height);
    ev.preventDefault();
    try {
      chipEl.setPointerCapture(ev.pointerId);
    } catch (_e) {
    }
    if (navigator && navigator.vibrate) {
      try {
        navigator.vibrate(8);
      } catch (_e2) {
      }
    }
    state.queueBarDrag = {
      pointerId: ev.pointerId,
      handleEl: chipEl,
      itemEl: chipEl,
      listEl,
      siblings,
      rects,
      origIndex,
      targetIndex: origIndex,
      startY: ev.clientY,
      gap,
      queueSnapshot: queue
    };
    chipEl.classList.add("dragging");
    siblings.forEach(function(el) {
      if (el !== chipEl) el.classList.add("queue-bar-item-sliding");
    });
    var move = function(e) {
      queueBarDragMove(e);
    };
    var up = function(e) {
      queueBarDragEnd(e);
    };
    state.queueBarDrag.moveHandler = move;
    state.queueBarDrag.upHandler = up;
    chipEl.addEventListener("pointermove", move);
    chipEl.addEventListener("pointerup", up);
    chipEl.addEventListener("pointercancel", up);
  }
  function queueBarComputeNewTops(origIndex, target, rects, gap) {
    var n = rects.length;
    var order = [];
    for (var i = 0; i < n; i++) order.push(i);
    order.splice(origIndex, 1);
    order.splice(target, 0, origIndex);
    var top = rects[0].top;
    var listTop = rects[0].top;
    for (var k = 1; k < n; k++) if (rects[k].top < listTop) listTop = rects[k].top;
    var newTops = {};
    var cursor = listTop;
    for (var newPos = 0; newPos < n; newPos++) {
      var oldIdx = order[newPos];
      newTops[oldIdx] = cursor;
      cursor += rects[oldIdx].height + gap;
    }
    return newTops;
  }
  function queueBarDragMove(ev) {
    var d = state.queueBarDrag;
    if (!d || ev.pointerId !== d.pointerId) return;
    ev.preventDefault();
    var deltaY = ev.clientY - d.startY;
    d.itemEl.style.transform = "translateY(" + deltaY + "px)";
    var centerY = d.rects[d.origIndex].top + d.rects[d.origIndex].height / 2 + deltaY;
    var target = d.origIndex;
    for (var i = 0; i < d.rects.length; i++) {
      if (i === d.origIndex) continue;
      var midY = d.rects[i].top + d.rects[i].height / 2;
      if (i < d.origIndex && centerY < midY) {
        target = Math.min(target, i);
      } else if (i > d.origIndex && centerY > midY) {
        target = Math.max(target, i);
      }
    }
    if (target !== d.targetIndex) {
      d.targetIndex = target;
      var newTops = queueBarComputeNewTops(d.origIndex, target, d.rects, d.gap);
      d.siblings.forEach(function(el, idx) {
        if (idx === d.origIndex) return;
        var move = newTops[idx] - d.rects[idx].top;
        el.style.transform = move ? "translateY(" + move + "px)" : "";
      });
    }
  }
  function queueBarDragEnd(ev) {
    var d = state.queueBarDrag;
    if (!d || ev && ev.pointerId !== d.pointerId) return;
    try {
      d.handleEl.releasePointerCapture(d.pointerId);
    } catch (_e) {
    }
    d.handleEl.removeEventListener("pointermove", d.moveHandler);
    d.handleEl.removeEventListener("pointerup", d.upHandler);
    d.handleEl.removeEventListener("pointercancel", d.upHandler);
    var origIndex = d.origIndex;
    var targetIndex = d.targetIndex;
    var queueSnapshot = d.queueSnapshot;
    d.siblings.forEach(function(el) {
      el.style.transform = "";
      el.classList.remove("queue-bar-item-sliding");
    });
    d.itemEl.classList.remove("dragging");
    state.queueBarDrag = null;
    if (origIndex === targetIndex) {
      updateQueueBar();
      return;
    }
    var order = [];
    for (var i = 0; i < queueSnapshot.length; i++) order.push(i);
    order.splice(origIndex, 1);
    order.splice(targetIndex, 0, origIndex);
    var nextQueue = order.map(function(i2) {
      return queueSnapshot[i2];
    });
    var session = state.sessions.find(function(s) {
      return s.id === state.selectedId;
    });
    if (!session) {
      updateQueueBar();
      return;
    }
    updateSessionSnapshot({ id: session.id, queuedMessages: nextQueue });
    updateQueueBar();
    fetch("/api/structured-sessions/" + session.id + "/queued", {
      method: "PATCH",
      headers: { "Content-Type": "application/json" },
      credentials: "same-origin",
      body: JSON.stringify({ order })
    }).then(function(res) {
      if (!res.ok) {
        return res.json().catch(function() {
          return {};
        }).then(function(p) {
          throw new Error(p && p.error || "\u6392\u5E8F\u5931\u8D25");
        });
      }
    }).catch(function(err) {
      rollbackQueueOptimistic(session, queueSnapshot);
      showToast(err && err.message || "\u8C03\u6574\u6392\u961F\u987A\u5E8F\u5931\u8D25\u3002", "error");
    });
  }
  function attachQueueBarDelegates() {
    var host = document.getElementById("queue-bar-host");
    if (!host || host.__queueDelegated) return;
    host.__queueDelegated = true;
    host.addEventListener("click", function(ev) {
      var evTarget = ev.target;
      var actionEl = evTarget && evTarget.closest ? evTarget.closest("[data-action]") : null;
      if (actionEl && host.contains(actionEl)) {
        var action = actionEl.getAttribute("data-action");
        if (action === "drag") return;
        ev.preventDefault();
        ev.stopPropagation();
        if (action === "promote-item") {
          var pItem = actionEl.closest(".queue-bar-item");
          if (pItem) queueBarPromoteIndex(Number(pItem.getAttribute("data-index")));
        } else if (action === "delete") {
          var itemEl = actionEl.closest(".queue-bar-item");
          if (itemEl) queueBarDeleteItem(Number(itemEl.getAttribute("data-index")));
        } else if (action === "clear-all") {
          queueBarClearAll();
        }
        return;
      }
    });
    host.addEventListener("pointerdown", function(ev) {
      if (ev.button !== void 0 && ev.button !== 0) return;
      var evTarget = ev.target;
      if (evTarget && evTarget.closest && evTarget.closest(
        '[data-action="delete"], [data-action="promote-item"], [data-action="clear-all"], [data-action="expand"]'
      )) return;
      var chip = evTarget && evTarget.closest ? evTarget.closest(".queue-bar-item") : null;
      if (!chip) return;
      queueBarDragStart(ev, chip);
    });
    host.addEventListener("keydown", function(ev) {
      if (ev.key === "Escape" && isQueueBarExpanded()) {
        ev.stopPropagation();
        setQueueBarExpanded(false);
      }
    });
  }
  function buildMessagesForRender(session, messages) {
    var sanitized = Array.isArray(messages) ? stripRenderOnlyStructuredMessages(messages) : [];
    var base = Array.isArray(sanitized) ? sanitized.slice() : [];
    if (!session || session.sessionKind !== "structured") {
      return base;
    }
    if (session.structuredState && session.structuredState.inFlight) {
      var last = base[base.length - 1];
      if (!last || last.role !== "assistant") {
        base.push({ role: "assistant", content: [{ type: "text", text: "", __processing: true }] });
      }
    }
    return base;
  }
  function flushStructuredInputQueue() {
    var session = state.sessions.find(function(s) {
      return s.id === state.selectedId;
    });
    syncStructuredQueueFromSession(session);
    updateStructuredQueueCounter();
  }
  function getInputErrorMessage(error) {
    var selectedSession = getSelectedSession();
    var isCodex = selectedSession && selectedSession.provider === "codex";
    if (error && (error.errorCode === "SESSION_NOT_RUNNING" || error.errorCode === "SESSION_NO_PTY")) {
      return isCodex ? "Codex \u4F1A\u8BDD\u5DF2\u7ED3\u675F\uFF1B\u82E5\u5B58\u5728 Codex \u5386\u53F2\u4F1A\u8BDD\uFF0C\u5C06\u5728\u4F60\u4E0B\u6B21\u53D1\u9001\u6D88\u606F\u65F6\u81EA\u52A8\u6062\u590D\u3002" : "\u4F1A\u8BDD\u5DF2\u7ED3\u675F\uFF1B\u82E5\u5B58\u5728 Claude \u5386\u53F2\u4F1A\u8BDD\uFF0C\u5C06\u5728\u4F60\u4E0B\u6B21\u53D1\u9001\u6D88\u606F\u65F6\u81EA\u52A8\u6062\u590D\u3002";
    }
    if (error && error.errorCode === "SESSION_NOT_FOUND") {
      return "\u4F1A\u8BDD\u4E0D\u5B58\u5728\uFF0C\u8BF7\u91CD\u65B0\u9009\u62E9\u6216\u65B0\u5EFA\u4F1A\u8BDD\u3002";
    }
    return error && error.message || (isCodex ? "Codex \u4F1A\u8BDD\u6682\u4E0D\u53EF\u7528\uFF1B\u82E5\u5B58\u5728 Codex \u5386\u53F2\u4F1A\u8BDD\uFF0C\u5C06\u81EA\u52A8\u5C1D\u8BD5\u6062\u590D\u3002" : "\u4F1A\u8BDD\u6682\u4E0D\u53EF\u7528\uFF1B\u82E5\u5B58\u5728 Claude \u5386\u53F2\u4F1A\u8BDD\uFF0C\u5C06\u81EA\u52A8\u5C1D\u8BD5\u6062\u590D\u3002");
  }
  function buildInputError(payload) {
    var err = new Error(payload && payload.error || "\u4F1A\u8BDD\u5DF2\u7ED3\u675F\u3002");
    if (payload && typeof payload === "object") {
      err.errorCode = payload.errorCode || null;
      err.sessionId = payload.sessionId || state.selectedId || null;
      err.sessionStatus = Object.prototype.hasOwnProperty.call(payload, "sessionStatus") ? payload.sessionStatus : null;
    }
    return err;
  }
  function isSessionUnavailableError(error) {
    return error && (error.errorCode === "SESSION_NOT_RUNNING" || error.errorCode === "SESSION_NO_PTY" || error.errorCode === "SESSION_NOT_FOUND");
  }
  function markSessionStopped(sessionId, status) {
    if (!sessionId) return;
    updateSessionSnapshot({ id: sessionId, status: status || "exited" });
  }
  function canAutoResumeSession(session) {
    return !!(session && !isStructuredSession2(session) && (session.provider === "claude" || session.provider === "codex") && session.status !== "running" && session.claudeSessionId);
  }
  function ensureSessionReadyForInput(session, errorEl) {
    if (!session) {
      showToast("\u4F1A\u8BDD\u4E0D\u5B58\u5728\uFF0C\u8BF7\u91CD\u65B0\u9009\u62E9\u6216\u65B0\u5EFA\u4F1A\u8BDD\u3002", "error");
      return Promise.resolve(null);
    }
    if (session.status === "running") {
      return Promise.resolve(session);
    }
    if (!canAutoResumeSession(session)) {
      var providerLabel = session && session.provider === "codex" ? "Codex" : "Claude";
      showToast("\u8BE5\u4F1A\u8BDD\u6CA1\u6709\u53EF\u6062\u590D\u7684 " + providerLabel + " \u5386\u53F2\u4E0A\u4E0B\u6587\uFF0C\u8BF7\u65B0\u5EFA\u4F1A\u8BDD\u3002", "error");
      return Promise.resolve(null);
    }
    return resumeSession(session.id, errorEl).then(function(data) {
      if (!data) return null;
      updateSessionSnapshot(data);
      updateSessionsList();
      subscribeToSession(data.id);
      return loadOutput(data.id).then(function() {
        focusInputBox2(true);
        return data;
      });
    });
  }
  function getTerminalSubmitChunks(session, text5) {
    return [text5, String.fromCharCode(13)];
  }
  function sendTerminalChunks(chunks, shortcutKey, delayMs, viewOverride) {
    var sequence = Array.isArray(chunks) ? chunks.filter(function(chunk) {
      return !!chunk;
    }) : [];
    if (sequence.length === 0) {
      return Promise.resolve();
    }
    var delay = typeof delayMs === "number" ? delayMs : 0;
    return sequence.reduce(function(promise, chunk, index2) {
      return promise.then(function() {
        if (index2 > 0 && delay > 0) {
          return new Promise(function(resolve) {
            setTimeout(resolve, delay);
          }).then(function() {
            return queueDirectInput4(chunk, index2 === sequence.length - 1 ? shortcutKey : void 0, viewOverride);
          });
        }
        return queueDirectInput4(chunk, index2 === sequence.length - 1 ? shortcutKey : void 0, viewOverride);
      });
    }, Promise.resolve());
  }
  var PENDING_INPUT_TTL_MS = 5e3;
  var PENDING_INPUT_MAX = 100;
  function enqueuePendingInput(input) {
    if (!input) return;
    if (state.pendingMessages.length >= PENDING_INPUT_MAX) {
      state.pendingMessages.shift();
    }
    state.pendingMessages.push({ input, at: Date.now() });
  }
  function queueOfflineTerminalChunks(chunks) {
    var sequence = Array.isArray(chunks) ? chunks.filter(function(chunk) {
      return !!chunk;
    }) : [];
    sequence.forEach(function(chunk) {
      enqueuePendingInput(chunk);
    });
  }
  function _detectAndMarkClear(input) {
    if (typeof input !== "string" || !input) return;
    var stripped = input.replace(/\x1b\[200~/g, "").replace(/\x1b\[201~/g, "");
    if (/(?:^|\n)\s*\/clear\s*(?:\r|\n|$)/.test(stripped)) {
      if (typeof state !== "undefined" && state) {
        state.terminalOutputMarker = (state.terminalOutput && state.terminalOutput.length) | 0;
      }
    }
  }
  function queueDirectInput4(input, shortcutKey, viewOverride) {
    if (!input || !state.selectedId) return Promise.resolve();
    _detectAndMarkClear(input);
    state.messageQueue.push(input);
    state.inputQueue = state.inputQueue.then(function() {
      return postInput3(input, shortcutKey, viewOverride).finally(function() {
        var idx = state.messageQueue.indexOf(input);
        if (idx > -1) state.messageQueue.splice(idx, 1);
      });
    });
    return state.inputQueue;
  }
  function postInput3(input, shortcutKey, viewOverride) {
    if (!state.selectedId) return Promise.resolve();
    var requestSessionId = state.selectedId;
    var effectiveView = viewOverride || state.currentView;
    if (!isSelectedSessionRunning()) {
      if (!state.wsConnected) {
        enqueuePendingInput(input);
        console.log("[wand] postInput: session not running, queued for reconnect", {
          sessionId: state.selectedId,
          inputLength: input.length
        });
        return Promise.resolve();
      }
      console.warn("[wand] postInput: session not running, skipping send", {
        sessionId: state.selectedId
      });
      showToast("\u4F1A\u8BDD\u672A\u8FD0\u884C\uFF0C\u6B63\u5728\u7B49\u5F85\u81EA\u52A8\u6062\u590D\u540E\u91CD\u8BD5\u3002", "info");
      return Promise.resolve();
    }
    if (!state.wsConnected) {
      enqueuePendingInput(input);
      console.log("[wand] postInput: WebSocket disconnected, queued message", {
        sessionId: state.selectedId,
        inputLength: input.length
      });
      return Promise.resolve();
    }
    return fetch("/api/sessions/" + requestSessionId + "/input", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      credentials: "same-origin",
      body: JSON.stringify({ input, view: effectiveView, shortcutKey: shortcutKey || void 0 })
    }).then(function(res) {
      if (!res.ok) {
        return res.json().catch(function() {
          return { error: "\u8BF7\u6C42\u5931\u8D25" };
        }).then(function(payload) {
          var error = buildInputError(payload);
          error.httpStatus = res.status;
          console.error("[wand] postInput: request failed", {
            status: res.status,
            errorCode: error.errorCode,
            message: error.message,
            sessionId: requestSessionId
          });
          if (isSessionUnavailableError(error)) {
            markSessionStopped(requestSessionId, error.sessionStatus || "exited");
          }
          throw error;
        });
      }
      return res.json();
    }).then(function(snapshot7) {
      if (snapshot7 && snapshot7.id) {
        updateSessionSnapshot(snapshot7);
        if (snapshot7.id === state.selectedId) {
          if (snapshot7.messages && snapshot7.messages.length > 0) {
            state.currentMessages = snapshot7.messages;
          }
          renderChat(true);
        }
      }
      return snapshot7;
    });
  }
  function getSelectedSession() {
    return state.sessions.find(function(session) {
      return session.id === state.selectedId;
    }) || null;
  }
  function isTerminalInteractionAvailable2() {
    return !!state.selectedId && state.currentView === "terminal";
  }
  function isCurrentTerminalSession(sessionId) {
    if (!state.terminal || !sessionId) return false;
    if (sessionId !== state.selectedId) return false;
    if (state.terminalSessionId && state.terminalSessionId !== sessionId) return false;
    return true;
  }
  function shouldCaptureTerminalEvent(event) {
    if (!state.terminalInteractive || !isTerminalInteractionAvailable2()) return false;
    if (event.defaultPrevented || event.isComposing) return false;
    var target = event.target;
    if (!target) return true;
    if (document.documentElement.classList.contains("is-wand-embed-terminal") && target.closest && target.closest("#input-box")) {
      return false;
    }
    if (target.closest && target.closest("#mini-keyboard")) return false;
    if (shouldIgnoreInteractiveTarget(target)) return false;
    return true;
  }
  var keyboardEventKeyMap = {
    Esc: "escape",
    ArrowUp: "up",
    ArrowDown: "down",
    ArrowLeft: "left",
    ArrowRight: "right",
    Enter: "enter",
    Tab: "tab",
    Backspace: "backspace",
    Home: "home",
    End: "end",
    PageUp: "pageup",
    PageDown: "pagedown",
    Delete: "delete",
    Insert: "insert",
    " ": "space"
  };
  var ptySpecialKeyMap = {
    space: " ",
    tab: String.fromCharCode(9),
    shift_tab: String.fromCharCode(27) + "[Z",
    backspace: String.fromCharCode(127),
    home: String.fromCharCode(27) + "[H",
    end: String.fromCharCode(27) + "[F",
    pageup: String.fromCharCode(27) + "[5~",
    pagedown: String.fromCharCode(27) + "[6~",
    delete: String.fromCharCode(27) + "[3~",
    insert: String.fromCharCode(27) + "[2~"
  };
  var ctrlSymbolMap = {
    " ": 0,
    "[": 27,
    "\\": 28,
    "]": 29,
    "^": 30,
    "_": 31
  };
  var JOYSTICK_LONG_PRESS_MS = 400;
  var JOYSTICK_MOVE_THRESHOLD = 10;
  var JOYSTICK_TAP_THRESHOLD = 8;
  var JOYSTICK_BALL_SIZE = 54;
  var JOYSTICK_EDGE_MARGIN = 8;
  var JOYSTICK_ACTION_KEYS = [
    { key: "enter", label: "Enter" },
    { key: "ctrl_c", label: "Ctrl+C" },
    { key: "escape", label: "Esc" },
    { key: "shift_tab", label: "Shift+Tab" }
  ];
  var ignoredInteractiveTargetIds = /* @__PURE__ */ new Set([
    "mini-keyboard-fab",
    "mini-keyboard-toggle",
    "terminal-interactive-toggle"
  ]);
  function shouldIgnoreInteractiveTarget(target) {
    if (!target) return false;
    if (ignoredInteractiveTargetIds.has(target.id)) return true;
    return !!(target.closest && target.closest('[role="dialog"], [role="alertdialog"]'));
  }
  var modifierKeySet = /* @__PURE__ */ new Set(["ctrl", "alt", "shift"]);
  function isModifierKey(key) {
    return modifierKeySet.has(key);
  }
  function getPtySpecialSequence(key) {
    return ptySpecialKeyMap[key] || "";
  }
  function getCtrlSequence(text5) {
    var lower = text5.toLowerCase();
    if (lower >= "a" && lower <= "z") {
      return String.fromCharCode(lower.charCodeAt(0) - 96);
    }
    if (Object.prototype.hasOwnProperty.call(ctrlSymbolMap, lower)) {
      return String.fromCharCode(ctrlSymbolMap[lower]);
    }
    return "";
  }
  function keyFromKeyboardEvent(event) {
    return keyboardEventKeyMap[event.key] || event.key;
  }
  function getModifierStateFromEvent(event, key) {
    return {
      ctrl: event.ctrlKey,
      alt: event.altKey,
      // 仅对单字符键保留 shift（控制 toUpperCase 路径），
      // 但 Tab 特例：物理 Shift+Tab 要走 buildPtySequence 的 back-tab 分支。
      shift: event.shiftKey && (key.length === 1 || key === "tab"),
      meta: event.metaKey
    };
  }
  function sendTerminalSequence(sequence, shortcutKey) {
    if (!sequence) return;
    queueDirectInput4(sequence, shortcutKey).catch(function() {
    });
  }
  function focusTerminalInteractionTarget() {
    focusTerminalContainer();
  }
  function hideMiniKeyboard(clearModifiersOnHide) {
    state.keyboardPopupOpen = false;
    if (clearModifiersOnHide !== false) {
      clearModifiers();
    }
    updateKeyboardPopupUI();
  }
  function toggleTerminalInteractive() {
    if (!isTerminalInteractionAvailable2()) return;
    setTerminalInteractive(!state.terminalInteractive);
  }
  function setTerminalInteractive(enabled) {
    var next = !!enabled && isTerminalInteractionAvailable2();
    if (state.terminalInteractive === next) return;
    state.terminalInteractive = next;
    if (next) {
      enableTerminalCapture();
      hideMiniKeyboard(false);
      focusTerminalInteractionTarget();
      showToast("\u7EC8\u7AEF\u4EA4\u4E92\u6A21\u5F0F\u5DF2\u5F00\u542F", "info");
    } else {
      disableTerminalCapture();
      clearModifiers();
    }
    updateInteractiveControls3();
  }
  function reconcileInteractiveState() {
    var selectedSession = state.sessions.find(function(session) {
      return session.id === state.selectedId;
    });
    var shouldDisableInteractive = !selectedSession || selectedSession.status !== "running" || state.currentView !== "terminal";
    if (shouldDisableInteractive && state.terminalInteractive) {
      setTerminalInteractive(false);
      return;
    }
    if ((!selectedSession || state.currentView !== "terminal") && state.keyboardPopupOpen) {
      state.keyboardPopupOpen = false;
    }
    updateInteractiveControls3();
  }
  function updateInteractiveControls3() {
    var selectedSession = state.sessions.find(function(session) {
      return session.id === state.selectedId;
    });
    var structured = isStructuredSession2(selectedSession);
    var isCodex = selectedSession && selectedSession.provider === "codex";
    var isRunning = structured ? !!(selectedSession && selectedSession.structuredState && selectedSession.structuredState.inFlight) : !!selectedSession && selectedSession.status === "running";
    var composer = document.getElementById("input-box");
    var toggles = ["terminal-interactive-toggle-top"];
    toggles.forEach(function(id) {
      var toggle = document.getElementById(id);
      if (toggle) {
        toggle.classList.toggle("active", state.terminalInteractive);
        toggle.classList.toggle("is-on", state.terminalInteractive);
        toggle.classList.toggle("hidden", structured || state.currentView !== "terminal" || !selectedSession);
        toggle.setAttribute("aria-pressed", state.terminalInteractive ? "true" : "false");
        var stateLabel = toggle.querySelector(".plus-popover-toggle-state");
        if (stateLabel) stateLabel.textContent = state.terminalInteractive ? "\u5F00" : "\u5173";
      }
    });
    var inputHint = document.querySelector(".input-hint");
    if (inputHint) {
      inputHint.classList.toggle("hidden", structured ? true : state.currentView === "terminal");
      if (!structured && selectedSession) {
        inputHint.textContent = isCodex ? "Enter \u53D1\u9001 \xB7 chat \u4E3A\u89E3\u6790\u89C6\u56FE\uFF0Cterminal \u4E3A\u539F\u59CB\u8F93\u51FA" : "Enter \u53D1\u9001 \xB7 Shift+Enter \u6362\u884C";
      }
    }
    var canResumeOnSend = !structured && !isRunning && canAutoResumeSession(selectedSession);
    if (composer) {
      composer.placeholder = getComposerPlaceholder(selectedSession, state.terminalInteractive);
      composer.disabled = !structured && !!selectedSession && !isRunning && !canResumeOnSend;
      composer.setAttribute("aria-disabled", composer.disabled ? "true" : "false");
      composer.readOnly = false;
      composer.classList.toggle(
        "is-terminal-passthrough",
        !!state.terminalInteractive && !document.documentElement.classList.contains("is-wand-embed-terminal")
      );
    }
    var voiceBtn = document.getElementById("voice-record-btn");
    if (voiceBtn) {
      voiceBtn.disabled = state.terminalInteractive;
      voiceBtn.setAttribute("aria-disabled", voiceBtn.disabled ? "true" : "false");
    }
    if (state.terminalInteractive && voiceState.recording) {
      voiceState.recording = false;
      resetVoiceRecordingUI();
    }
    var sendBtn = document.getElementById("send-input-button");
    var structuredInFlight = structured && isRunning;
    if (sendBtn) {
      sendBtn.disabled = !structured && !!selectedSession && !isRunning && !canResumeOnSend;
      sendBtn.setAttribute("title", structured ? structuredInFlight ? "\u6392\u961F\u53D1\u9001\uFF08\u5F53\u524D\u56DE\u590D\u7ED3\u675F\u540E\u5904\u7406\uFF09" : "\u53D1\u9001" : isCodex ? isRunning ? "\u53D1\u9001\u7ED9 Codex" : "Codex \u4F1A\u8BDD\u5DF2\u7ED3\u675F" : !selectedSession || isRunning || canResumeOnSend ? "\u53D1\u9001" : "\u4F1A\u8BDD\u5DF2\u7ED3\u675F");
      sendBtn.classList.toggle("queue-mode", structuredInFlight);
    }
    var stopBtn = document.getElementById("stop-button");
    if (stopBtn) {
      var sig = computeRunningSignal2(selectedSession);
      stopBtn.classList.toggle("hidden", !sig.active);
    }
    var stopBtnEl = document.getElementById("stop-button");
    if (stopBtnEl) {
      var runSig = computeRunningSignal2(selectedSession);
      stopBtnEl.classList.toggle("hidden", !runSig.active);
    }
    var container = document.getElementById("output");
    if (container) container.classList.toggle("interactive", !structured && state.terminalInteractive);
    updateJoystickVisibility();
  }
  function hasActiveTerminalSelection() {
    var sel = window.getSelection && window.getSelection();
    if (!sel || sel.isCollapsed) return false;
    var output = document.getElementById("output");
    if (!output) return false;
    var node = sel.anchorNode;
    if (node && node.nodeType === 3) node = node.parentNode;
    return !!(node && output.contains(node));
  }
  function captureTerminalInput2(event) {
    if (!shouldCaptureTerminalEvent(event)) return;
    if (event.metaKey) return;
    var key = keyFromKeyboardEvent(event);
    if (!key) return;
    var mods = getModifierStateFromEvent(event, key);
    if (isModifierKey(key)) return;
    if (mods.ctrl && key.length === 1 && key.toLowerCase() === "c" && hasActiveTerminalSelection()) {
      return;
    }
    var sequence = buildPtySequence(key, mods);
    if (!sequence) return;
    event.preventDefault();
    sendTerminalSequence(sequence, key);
  }
  function scheduleShortcutResync() {
    if (!state.terminal) return;
    scheduleSoftResyncTerminal(500);
  }
  function updateKeyboardPopupUI() {
    updateJoystickPanelUI();
  }
  function closeKeyboardPopup() {
    state.keyboardPopupOpen = false;
    updateInteractiveControls3();
  }
  function enableTerminalCapture() {
    document.addEventListener("keydown", captureTerminalInput2, true);
  }
  function disableTerminalCapture() {
    document.removeEventListener("keydown", captureTerminalInput2, true);
  }
  function buildPtySequence(key, modifiers) {
    var mods = modifiers || { ctrl: false, alt: false, shift: false };
    if (isModifierKey(key)) return "";
    if (key === "tab" && mods.shift) return String.fromCharCode(27) + "[Z";
    var specialSequence = getPtySpecialSequence(key);
    if (specialSequence) return specialSequence;
    if (key.indexOf("ctrl_") === 0) {
      return String.fromCharCode(key.charCodeAt(key.length - 1) - 96);
    }
    var mapped = getControlInput(key);
    if (mapped) return mapped;
    if (!key) return "";
    var text5 = key.length === 1 ? key : "";
    if (!text5) return "";
    if (mods.shift) text5 = text5.toUpperCase();
    if (mods.ctrl) {
      return getCtrlSequence(text5);
    }
    if (mods.alt) return String.fromCharCode(27) + text5;
    return text5;
  }
  function clearModifiers() {
    state.modifiers.ctrl = false;
    state.modifiers.alt = false;
    state.modifiers.shift = false;
    updateModifierUI();
  }
  function updateModifierUI() {
    var keyboard = document.getElementById("mini-keyboard");
    if (!keyboard) return;
    ["ctrl", "alt", "shift"].forEach(function(name) {
      var btn = keyboard.querySelector('[data-key="' + name + '"]');
      if (btn) btn.classList.toggle("active", !!state.modifiers[name]);
    });
  }
  function getControlInput(key) {
    switch (key) {
      case "yes":
        return "y" + String.fromCharCode(13);
      case "no":
        return "n" + String.fromCharCode(13);
      case "up":
        return String.fromCharCode(27) + "[A";
      case "down":
        return String.fromCharCode(27) + "[B";
      case "left":
        return String.fromCharCode(27) + "[D";
      case "right":
        return String.fromCharCode(27) + "[C";
      case "enter":
        return String.fromCharCode(13);
      case "ctrl_c":
        return String.fromCharCode(3);
      case "ctrl_d":
        return String.fromCharCode(4);
      case "ctrl_l":
        return String.fromCharCode(12);
      case "ctrl_u":
        return String.fromCharCode(21);
      case "ctrl_k":
        return String.fromCharCode(11);
      case "ctrl_w":
        return String.fromCharCode(23);
      case "ctrl_z":
        return String.fromCharCode(26);
      case "escape":
        return String.fromCharCode(27);
      default:
        return "";
    }
  }
  function flushPendingMessages() {
    if (state.pendingMessages.length === 0) return;
    var selectedSession = getSelectedSession();
    if (isStructuredSession2(selectedSession)) {
      state.pendingMessages = [];
      return;
    }
    var now = Date.now();
    var queue = [];
    var dropped = 0;
    state.pendingMessages.forEach(function(item) {
      if (typeof item === "string") {
        queue.push(item);
        return;
      }
      if (!item || typeof item.input !== "string") return;
      if (now - (item.at || 0) > PENDING_INPUT_TTL_MS) {
        dropped++;
        return;
      }
      queue.push(item.input);
    });
    state.pendingMessages = [];
    if (dropped > 0) {
      console.log("[wand] flushPendingMessages: dropped " + dropped + " stale input(s)");
    }
    var sendPromise = Promise.resolve();
    queue.forEach(function(input) {
      sendPromise = sendPromise.then(function() {
        return sendInputDirect(input).catch(function() {
        });
      });
    });
  }
  function sendInputDirect(input) {
    if (!input || !state.selectedId) return Promise.resolve();
    var requestSessionId = state.selectedId;
    return fetch("/api/sessions/" + requestSessionId + "/input", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      credentials: "same-origin",
      body: JSON.stringify({ input, view: state.currentView })
    }).then(function(res) {
      if (!res.ok) {
        return res.json().catch(function() {
          return { error: "\u8BF7\u6C42\u5931\u8D25" };
        }).then(function(payload) {
          var error = buildInputError(payload);
          error.httpStatus = res.status;
          if (isSessionUnavailableError(error)) {
            console.log("[wand] sendInputDirect: session unavailable, dropping", {
              sessionId: requestSessionId,
              errorCode: error.errorCode
            });
            return null;
          }
          throw error;
        });
      }
      return res.json();
    }).then(function(snapshot7) {
      if (snapshot7 && snapshot7.id) {
        updateSessionSnapshot(snapshot7);
        if (snapshot7.id === state.selectedId) {
          if (snapshot7.messages && snapshot7.messages.length > 0) {
            state.currentMessages = snapshot7.messages;
          }
          renderChat(true);
        }
      }
      return snapshot7;
    });
  }
  function stopSession() {
    if (!state.selectedId) return;
    var id = state.selectedId;
    wandConfirm2(t("stop.confirm.message"), {
      title: t("stop.confirm.title"),
      danger: true,
      okLabel: t("stop.confirm.ok"),
      cancelLabel: t("stop.confirm.cancel")
    }).then(function(ok) {
      if (!ok) return;
      if (state.selectedId !== id) return;
      fetch("/api/sessions/" + id + "/stop", { method: "POST", credentials: "same-origin" }).then(refreshAll);
    });
  }
  function deleteSession(id) {
    var item = isBrowserReactShellMounted() ? null : document.querySelector('.session-item[data-session-id="' + id + '"]');
    var session = state.sessions.find(function(candidate) {
      return candidate.id === id;
    });
    var providerSessionId = session && session.claudeSessionId;
    if (item) {
      item.classList.add("deleting");
    }
    setTimeout(function() {
      fetch("/api/sessions/" + id, { method: "DELETE", credentials: "same-origin" }).then(function(res) {
        return res.json();
      }).then(function(data) {
        if (data && data.error) {
          throw new Error(data.error);
        }
        if (state.selectedId === id) {
          state.selectedId = null;
          persistSelectedId();
        }
        if (providerSessionId) {
          state.claudeHistory = state.claudeHistory.filter(function(history2) {
            return history2.claudeSessionId !== providerSessionId;
          });
          state.codexHistory = state.codexHistory.filter(function(history2) {
            return history2.claudeSessionId !== providerSessionId;
          });
        }
        return refreshAll();
      }).catch(function() {
        if (item) item.classList.remove("deleting");
        var errorEl = document.getElementById("action-error");
        showError(errorEl, "\u65E0\u6CD5\u5220\u9664\u4F1A\u8BDD\u3002");
      });
    }, 250);
  }
  function executeDeleteHistory(claudeSessionId, item) {
    if (item) {
      item.classList.add("deleting");
    }
    setTimeout(function() {
      fetch("/api/claude-history/" + encodeURIComponent(claudeSessionId), { method: "DELETE", credentials: "same-origin" }).then(function(res) {
        return res.json();
      }).then(function(data) {
        if (data && data.error) {
          throw new Error(data.error);
        }
        state.claudeHistory = state.claudeHistory.filter(function(s) {
          return s.claudeSessionId !== claudeSessionId;
        });
        delete state.selectedClaudeHistoryIds[claudeSessionId];
        updateSessionsList();
      }).catch(function() {
        if (item) item.classList.remove("deleting");
        var errorEl = document.getElementById("action-error");
        showError(errorEl, "\u65E0\u6CD5\u5220\u9664\u4F1A\u8BDD\u3002");
      });
    }, 250);
  }
  function deleteClaudeHistorySession2(claudeSessionId, item) {
    executeDeleteHistory(claudeSessionId, item);
  }
  function deleteClaudeHistoryDirectory(cwd, btn, items) {
    if (!cwd) {
      return;
    }
    fetch("/api/claude-history?cwd=" + encodeURIComponent(cwd), { method: "DELETE", credentials: "same-origin" }).then(function(res) {
      return res.json();
    }).then(function(data) {
      if (data && data.error) {
        throw new Error(data.error);
      }
      state.claudeHistory = state.claudeHistory.filter(function(s) {
        return s.cwd !== cwd;
      });
      updateSessionsList();
    }).catch(function() {
      setDeletingState(items, false);
      var errorEl = document.getElementById("action-error");
      showError(errorEl, "\u65E0\u6CD5\u6E05\u7406\u8BE5\u76EE\u5F55\u7684\u5386\u53F2\u4F1A\u8BDD\u3002");
    });
  }
  function setDeletingState(items, deleting) {
    items.forEach(function(item) {
      item.classList.toggle("deleting", deleting);
    });
  }
  function getHistoryItemsByCwd(cwd) {
    return Array.prototype.slice.call(document.querySelectorAll('.claude-history-item[data-cwd="' + window.CSS.escape(String(cwd)) + '"]'));
  }
  var _swipeState = null;
  var _swipedItem = null;
  function closeSwipedItem() {
    if (_swipedItem) {
      _swipedItem.classList.remove("swiped");
      var content = _swipedItem.querySelector(".session-item-content");
      if (content) content.style.transform = "";
      _swipedItem = null;
    }
  }
  function initSwipeToDelete(_container) {
    _swipeState = null;
    _swipedItem = null;
  }
  var _resumeInProgress = false;
  function resumeSession(sessionId, errorEl) {
    if (!sessionId || _resumeInProgress) return Promise.resolve(null);
    _resumeInProgress = true;
    return fetch("/api/sessions/" + encodeURIComponent(sessionId) + "/resume", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      credentials: "same-origin",
      body: JSON.stringify(withTerminalDimensions({
        mode: state.chatMode || state.config.defaultMode || "default"
      }))
    }).then(function(res) {
      return res.json();
    }).then(function(data) {
      if (data.error) {
        if (errorEl) showError(errorEl, data.error);
        else showToast(data.error, "error");
        return null;
      }
      state.selectedId = data.id;
      persistSelectedId();
      state.drafts[data.id] = "";
      return data;
    }).catch(function(error) {
      var message = error && error.message || "\u65E0\u6CD5\u6062\u590D\u4F1A\u8BDD\u3002";
      if (errorEl) showError(errorEl, message);
      else showToast(message, "error");
      return null;
    }).finally(function() {
      _resumeInProgress = false;
    });
  }
  function activateSession(data) {
    if (!data || !data.id) return Promise.resolve();
    state.selectedId = data.id;
    persistSelectedId();
    state.currentMessages = [];
    teardownTerminal();
    resetChatRenderCache();
    switchToSessionView2(data.id);
    updateSessionSnapshot(data);
    updateSessionsList();
    subscribeToSession(data.id);
    return loadOutput(data.id).then(function() {
      focusInputBox2(true);
    });
  }
  function resumeSessionFromList(sessionId) {
    return resumeSession(sessionId).then(function(data) {
      if (!data) return null;
      if (data.claudeSessionId) {
        if (data.provider === "codex") {
          state.codexHistory = state.codexHistory.filter(function(s) {
            return s.claudeSessionId !== data.claudeSessionId;
          });
        } else {
          state.claudeHistory = state.claudeHistory.filter(function(s) {
            return s.claudeSessionId !== data.claudeSessionId;
          });
        }
      }
      return activateSession(data).then(function() {
        return data;
      });
    });
  }
  function handleResumeAction(actionButton) {
    actionButton.disabled = true;
    resumeSessionFromList(actionButton.dataset.sessionId).finally(function() {
      actionButton.disabled = false;
    });
  }
  function handleResumeCodexHistoryAction(actionButton) {
    var threadId = actionButton.dataset.claudeSessionId;
    var cwd = actionButton.dataset.cwd;
    if (!threadId) return;
    actionButton.disabled = true;
    resumeHistoryFromList("codex", threadId, cwd).finally(function() {
      actionButton.disabled = false;
    });
  }
  function resumeCodexHistorySession(threadId, cwd) {
    return fetch("/api/codex-sessions/" + encodeURIComponent(threadId) + "/resume", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      credentials: "same-origin",
      body: JSON.stringify(withTerminalDimensions({
        mode: state.chatMode || state.config && state.config.defaultMode || "default",
        cwd
      }))
    }).then(function(res) {
      return res.json();
    }).then(function(data) {
      if (data.error) {
        showToast(data.error, "error");
        return null;
      }
      return data;
    }).catch(function(error) {
      showToast(error && error.message || "\u65E0\u6CD5\u6062\u590D\u4F1A\u8BDD\u3002", "error");
      return null;
    });
  }
  function handleDeleteCodexHistoryAction(actionButton) {
    var threadId = actionButton.dataset.claudeSessionId;
    if (!threadId) return;
    confirmDelete("\u786E\u8BA4\u5220\u9664\u8FD9\u6761 Codex \u4F1A\u8BDD\u5417\uFF1F", {
      title: "\u5220\u9664\u4F1A\u8BDD"
    }).then(function(ok) {
      if (!ok) return;
      var item = actionButton.closest(".session-item");
      if (item) item.style.opacity = "0.5";
      deleteCodexHistorySession(threadId).catch(function() {
        if (item) item.style.opacity = "1";
      });
    });
  }
  function handleResumeHistoryAction(actionButton) {
    var claudeSessionId = actionButton.dataset.claudeSessionId;
    var cwd = actionButton.dataset.cwd;
    if (!claudeSessionId) return;
    actionButton.disabled = true;
    resumeHistoryFromList("claude", claudeSessionId, cwd).finally(function() {
      actionButton.disabled = false;
    });
  }
  function resumeClaudeHistorySession(claudeSessionId, cwd) {
    return fetch("/api/claude-sessions/" + encodeURIComponent(claudeSessionId) + "/resume", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      credentials: "same-origin",
      body: JSON.stringify(withTerminalDimensions({
        mode: state.chatMode || state.config && state.config.defaultMode || "default",
        cwd
      }))
    }).then(function(res) {
      return res.json();
    }).then(function(data) {
      if (data.error) {
        showToast(data.error, "error");
        return null;
      }
      return data;
    }).catch(function(error) {
      showToast(error && error.message || "\u65E0\u6CD5\u6062\u590D\u4F1A\u8BDD\u3002", "error");
      return null;
    });
  }
  function resumeHistoryFromList(provider, providerSessionId, cwd) {
    var request2 = provider === "codex" ? resumeCodexHistorySession(providerSessionId, cwd) : resumeClaudeHistorySession(providerSessionId, cwd);
    return request2.then(function(data) {
      if (!data || !data.id) return null;
      if (provider === "codex") {
        state.codexHistory = state.codexHistory.filter(function(s) {
          return s.claudeSessionId !== providerSessionId;
        });
      } else {
        state.claudeHistory = state.claudeHistory.filter(function(s) {
          return s.claudeSessionId !== providerSessionId;
        });
      }
      state.selectedId = data.id;
      persistSelectedId();
      state.drafts[data.id] = "";
      return activateSession(data).then(function() {
        dismissDrawerIfOverlay();
        return data;
      });
    });
  }
  function deleteCodexHistorySession(threadId) {
    return fetch("/api/codex-history/" + encodeURIComponent(threadId), {
      method: "DELETE",
      credentials: "same-origin"
    }).then(function(res) {
      return res.json();
    }).then(function(data) {
      if (!data || data.ok !== true) throw new Error(data && data.error || "\u65E0\u6CD5\u5220\u9664\u4F1A\u8BDD\u3002");
      state.codexHistory = state.codexHistory.filter(function(s) {
        return s.claudeSessionId !== threadId;
      });
      updateSessionsList();
      return data;
    });
  }
  function isTouchDevice() {
    return "ontouchstart" in window || navigator.maxTouchPoints > 0;
  }
  function focusInputBox2(skipMobile) {
    if (state.terminalInteractive) return;
    var inputBox = document.getElementById("input-box");
    if (!inputBox || !state.selectedId) return;
    if (document.activeElement === inputBox) return;
    if (skipMobile && isTouchDevice()) return;
    focusInputWithSelection(inputBox);
  }
  function updateInputPanelViewportSpacing() {
    var inputPanel = document.querySelector(".input-panel");
    if (!inputPanel) return;
    inputPanel.style.removeProperty("--keyboard-offset");
  }
  function resetInputPanelViewportSpacing() {
    var inputPanel = document.querySelector(".input-panel");
    if (!inputPanel) return;
    inputPanel.style.removeProperty("--keyboard-offset");
  }
  function restoreInputBoxViewport(inputBox) {
    if (!inputBox) return;
    var start = inputBox.selectionStart;
    var end = inputBox.selectionEnd;
    syncInputBoxScroll(inputBox);
    if (typeof start === "number" && typeof end === "number") {
      inputBox.setSelectionRange(start, end);
    }
  }
  function bindInputTouchScroll(inputBox) {
    if (!inputBox || inputBox.dataset.touchScrollBound === "true") return;
    inputBox.dataset.touchScrollBound = "true";
    inputBox.addEventListener("touchstart", function() {
      if (inputBox.scrollHeight <= inputBox.clientHeight + 1) return;
      if (inputBox.scrollTop <= 0) {
        inputBox.scrollTop = 1;
      } else if (inputBox.scrollTop + inputBox.clientHeight >= inputBox.scrollHeight) {
        inputBox.scrollTop = Math.max(1, inputBox.scrollHeight - inputBox.clientHeight - 1);
      }
    }, { passive: true });
  }
  function syncInputBoxLayout(inputBox) {
    if (!inputBox) return;
    autoResizeInput(inputBox);
    restoreInputBoxViewport(inputBox);
  }
  function handleInputBoxFocus(event) {
    var inputBox = event && event.target ? event.target : document.getElementById("input-box");
    if (!inputBox) return;
    updateInputPanelViewportSpacing();
    syncInputBoxLayout(inputBox);
  }
  function handleInputBoxBlur2(event) {
    var blurredEl = event && event.target ? event.target : document.getElementById("input-box");
    resetInputPanelViewportSpacing();
    if (blurredEl) autoResizeInput(blurredEl);
    scheduleClosedViewportBaselineWindow(2200, blurredEl);
    var dismissTicks = [80, 200, 380, 620, 900];
    dismissTicks.forEach(function(delay, idx) {
      setTimeout(function() {
        syncAppViewportHeight(false);
        if (blurredEl && blurredEl.value) autoResizeInput(blurredEl);
        if (idx === 1 && isTouchDevice()) {
          ensureTerminalFit("keyboard-blur", { forceReplay: true });
          maybeScrollTerminalToBottom("keyboard");
        }
      }, delay);
    });
  }
  function adjustInputBoxSelection(inputBox) {
    if (!inputBox) return;
    inputBox.setSelectionRange(inputBox.value.length, inputBox.value.length);
    restoreInputBoxViewport(inputBox);
  }
  function focusInputWithSelection(inputBox) {
    if (!inputBox) return;
    inputBox.focus({ preventScroll: true });
    adjustInputBoxSelection(inputBox);
  }
  function syncInputBoxForCurrentState(inputBox) {
    bindInputTouchScroll(inputBox);
    syncInputBoxLayout(inputBox);
  }
  function refreshInputBoxState(inputBox) {
    syncInputBoxForCurrentState(inputBox);
  }
  function shouldAdjustForKeyboard(vv, inputBox) {
    if (!vv || !inputBox || document.activeElement !== inputBox) return false;
    var offsetBottom = window.innerHeight - vv.height - vv.offsetTop;
    if (offsetBottom <= 50) return false;
    var rect = inputBox.getBoundingClientRect();
    return rect.bottom > vv.height - 12;
  }
  function syncInputBoxScroll(inputBox) {
    if (!inputBox) return;
    var isScrollable = inputBox.scrollHeight > inputBox.clientHeight + 1;
    if (!isScrollable) {
      inputBox.scrollTop = 0;
      return;
    }
    inputBox.scrollTop = inputBox.scrollHeight;
  }
  function focusInputFromTap() {
    if (state.terminalInteractive) {
      focusTerminalContainer();
      return;
    }
    if (isTouchDevice()) return;
    var inputBox = document.getElementById("input-box");
    if (!inputBox || !state.selectedId || document.activeElement === inputBox) return;
    focusInputWithSelection(inputBox);
  }
  function focusTerminalContainer() {
    var output = document.getElementById("output");
    if (!output) return;
    output.setAttribute("tabindex", "0");
    output.focus();
    if (state.terminal && state.terminal.focus) {
      state.terminal.focus();
    }
  }
  function setupMobileKeyboardHandlers() {
    var inputPanel = document.querySelector(".input-panel");
    var chatMessages = document.querySelector(".chat-messages");
    if ("virtualKeyboard" in navigator) {
      var vk = navigator.virtualKeyboard;
      vk.addEventListener("geometrychange", function() {
        if (!inputPanel) return;
        inputPanel.style.removeProperty("padding-bottom");
      });
    }
    var output = document.getElementById("output");
    if (output) {
      output.addEventListener("click", function() {
        focusInputFromTap();
      });
    }
    if (chatMessages) {
      chatMessages.addEventListener("click", function(e) {
        var target = e.target;
        if (target.tagName !== "A" && target.tagName !== "BUTTON" && !target.closest("button") && !target.closest("[data-tool-toggle]")) {
          focusInputFromTap();
        }
      });
    }
    document.addEventListener("click", function(e) {
      if (!isTouchDevice()) return;
      var inputBox = document.getElementById("input-box");
      if (!inputBox || document.activeElement !== inputBox) return;
      var target = e.target;
      if (!target || typeof target.closest !== "function") return;
      if (target.closest(".input-panel") || target.closest("#mini-keyboard") || target.closest("#mini-keyboard-fab") || target.closest("#mini-keyboard-toggle") || target.closest("#terminal-interactive-toggle") || target.closest(".wand-joystick-root")) {
        return;
      }
      inputBox.blur();
    }, true);
  }
  function resetRootViewportScroll() {
    try {
      window.scrollTo(0, 0);
    } catch (e) {
    }
    if (document.scrollingElement) document.scrollingElement.scrollTop = 0;
    if (document.documentElement) document.documentElement.scrollTop = 0;
    if (document.body) document.body.scrollTop = 0;
  }

  // src/web-ui/browser/pty-system-info.ts
  var STRUCTURED_RUNNERS = /* @__PURE__ */ new Set([
    "claude-sdk",
    "claude-cli-print",
    "codex-cli-exec",
    "opencode-cli-run",
    "grok-cli-headless",
    "qoder-cli-print"
  ]);
  function shouldExtractPtySystemInfo(session) {
    if (!session || session.sessionKind === "structured") return false;
    return typeof session.runner !== "string" || !STRUCTURED_RUNNERS.has(session.runner);
  }

  // src/web-ui/browser/chat-render.ts
  function renderChat(forceFullRender) {
    if (state.renderPending && !forceFullRender) return;
    state.renderPending = true;
    if (forceFullRender) {
      doRenderChat(true);
      state.renderPending = false;
    } else {
      requestAnimationFrame(function() {
        doRenderChat(false);
        state.renderPending = false;
      });
    }
  }
  state.chatRenderTimer = null;
  function scheduleChatRender3(immediate) {
    if (state.chatRenderTimer && !immediate) return;
    if (state.chatRenderTimer) clearTimeout(state.chatRenderTimer);
    if (immediate) {
      state.chatRenderTimer = null;
      renderChat();
      return;
    }
    try {
      window.__scheduleChatRender = function() {
        scheduleChatRender3(true);
      };
    } catch (e) {
    }
    var selectedForDelay = state.sessions.find(function(s) {
      return s.id === state.selectedId;
    });
    var isActiveStream = selectedForDelay && selectedForDelay.status === "running" && selectedForDelay.sessionKind !== "structured";
    var delay = isActiveStream ? CHAT_RENDER_LIVE_MS : CHAT_RENDER_IDLE_MS;
    state.chatRenderTimer = setTimeout(function() {
      state.chatRenderTimer = null;
      var selectedSession = state.sessions.find(function(s) {
        return s.id === state.selectedId;
      });
      if (selectedSession) {
        state.currentMessages = buildMessagesForRender(selectedSession, getPreferredMessages(selectedSession, selectedSession.output, true));
      }
      renderChat();
    }, delay);
  }
  function extractPtySystemInfo(output, messages) {
    if (!output || !messages || messages.length === 0) return [];
    function stripAnsi2(text5) {
      return text5.replace(/\x1B(?:[@-Z\\-_]|\[[0-?]*[ -/]*[@-~])/g, "");
    }
    var clean = stripAnsi2(output);
    var systemInfo = [];
    var userInputs = [];
    for (var i = 0; i < messages.length; i++) {
      if (messages[i].role === "user") {
        var userText = "";
        var content = messages[i].content;
        if (typeof content === "string") {
          userText = content;
        } else if (Array.isArray(content)) {
          for (var j = 0; j < content.length; j++) {
            if (content[j].type === "text") {
              userText = content[j].text;
              break;
            }
          }
        }
        if (userText) {
          userInputs.push({ text: userText, index: i });
        }
      }
    }
    var lastPos = 0;
    for (var i = 0; i < userInputs.length; i++) {
      var userInput = userInputs[i];
      var pos = clean.indexOf("\u276F " + userInput.text, lastPos);
      if (pos === -1) {
        pos = clean.indexOf("\n\u276F " + userInput.text, lastPos);
        if (pos !== -1) pos += 1;
      }
      if (pos > lastPos) {
        var segment = clean.substring(lastPos, pos);
        var lines = segment.split("\n");
        var infoLines = [];
        for (var j = 0; j < lines.length; j++) {
          var line = lines[j].trim();
          if (!line || line.startsWith("\u2500\u2500\u2500\u2500") || line === "\u276F" || line === "?" || line === "") continue;
          if (line.includes("Claude Code v") || line.includes("Opus") && line.includes("with") || line.includes("Sonnet") && line.includes("with") || line.includes("API Usage") || line.includes("Billing") || line.includes("for shortcuts") || line.includes("/effort") || line.match(/^[▸▐▝▘▗▖█▌▍▎▏▔▁▂▃▄▅▆▇██]/) || line.match(/^[▸▐▝▘▗▖█▌▍▎▏▔▁▂▃▄▅▆▇██]{3,}/)) {
            continue;
          }
          if (line.length > 3) {
            infoLines.push(line);
          }
        }
        if (infoLines.length > 0) {
          systemInfo.push({
            beforeMessage: userInput.index,
            content: infoLines.join("\n")
          });
        }
      }
      lastPos = pos + userInput.text.length + 2;
    }
    return systemInfo;
  }
  function ensureChatMessagesContainer(chatOutput) {
    if (!chatOutput) return null;
    var chatMessages = chatOutput.querySelector(".chat-messages");
    if (chatMessages) return chatMessages;
    chatMessages = document.createElement("div");
    chatMessages.className = "chat-messages";
    chatOutput.appendChild(chatMessages);
    return chatMessages;
  }
  function renderChatEmptyState2(chatOutput, html) {
    var chatMessages = ensureChatMessagesContainer(chatOutput);
    if (!chatMessages) return null;
    chatMessages.innerHTML = html;
    refreshTailMarqueePaths(chatMessages);
    bindChatScrollListener();
    updateChatUnreadBubble();
    return chatMessages;
  }
  function doRenderChat(forceFullRender) {
    var chatOutput = document.getElementById("chat-output");
    if (!chatOutput) return;
    var selectedSession = state.sessions.find(function(s) {
      return s.id === state.selectedId;
    });
    if (!selectedSession) {
      if (state.lastRenderedEmpty !== "none") {
        renderChatEmptyState2(chatOutput, '<div class="empty-state"><strong>\u672A\u9009\u62E9\u4F1A\u8BDD</strong><br>\u70B9\u51FB\u4E0A\u65B9\u300C\u65B0\u5BF9\u8BDD\u300D\u5F00\u59CB\u4F60\u7684\u7B2C\u4E00\u6B21\u5BF9\u8BDD\u3002</div>');
        state.lastRenderedEmpty = "none";
        state.lastRenderedMsgCount = 0;
      }
      return;
    }
    var allMessages = state.currentMessages;
    var legacyTaskMap = collectLegacyTaskIdMap(allMessages);
    _subagentSuffixMap = collectSubagentSuffixMap(allMessages);
    if (allMessages.length === 0) {
      if (state.lastRenderedEmpty !== "empty") {
        var emptyTrioHtml = "";
        if (isStructuredSession2(selectedSession)) {
          emptyTrioHtml = '<div class="empty-state-trio-wrap"><div class="empty-state-trio-hint">\u9ED8\u8BA4\u4F1A\u6309\u4EE5\u4E0B\u8BBE\u7F6E\u53D1\u9001\uFF0C\u53EF\u70B9\u51FB\u8C03\u6574\uFF1A</div>' + renderChatModeTrioHtml(selectedSession, { kind: "dropdown" }) + "</div>";
        }
        renderChatEmptyState2(
          chatOutput,
          '<div class="empty-state"><strong>\u5BF9\u8BDD\u5DF2\u5F00\u59CB</strong><br>\u5728\u4E0B\u65B9\u8F93\u5165\u6846\u53D1\u9001\u6D88\u606F\uFF0CClaude \u4F1A\u81EA\u52A8\u56DE\u590D\u3002</div>' + emptyTrioHtml
        );
        state.lastRenderedEmpty = "empty";
        state.lastRenderedMsgCount = 0;
      }
      renderStructuredStatusBar(null, selectedSession);
      updateTodoProgress([]);
      return;
    }
    var totalMsgCount = allMessages.length;
    if (totalMsgCount > state.chatRenderedCount) {
      state.chatRenderedCount = totalMsgCount;
    }
    var visibleOffset = Math.max(0, totalMsgCount - state.chatRenderedCount);
    var messages = visibleOffset > 0 ? allMessages.slice(visibleOffset) : allMessages;
    var hasServerOlder = typeof selectedSession.messageOffset === "number" && selectedSession.messageOffset > 0;
    var hasOlderMessages = visibleOffset > 0 || hasServerOlder;
    var msgCount = messages.length;
    var outputHash = selectedSession.output ? selectedSession.output.length : 0;
    if (selectedSession.messages && selectedSession.messages.length > 0) {
      var totalBlocks = 0;
      var contentLen = 0;
      for (var bi = 0; bi < selectedSession.messages.length; bi++) {
        var msgContent = selectedSession.messages[bi].content;
        if (msgContent) {
          if (Array.isArray(msgContent)) {
            totalBlocks += msgContent.length;
            for (var bj = 0; bj < msgContent.length; bj++) {
              var block = msgContent[bj];
              if (block.text) contentLen += block.text.length;
              if (block.thinking) contentLen += block.thinking.length;
              if (block.content) contentLen += block.content.length;
              if (block.id) contentLen += block.id.length;
              if (block.tool_use_id) contentLen += block.tool_use_id.length;
              if (block.description) contentLen += block.description.length;
              if (block.input) contentLen += JSON.stringify(block.input).length;
            }
            if (selectedSession.messages[bi].usage) {
              var hashUsage = selectedSession.messages[bi].usage;
              contentLen += (hashUsage.inputTokens || 0) + (hashUsage.outputTokens || 0) + (hashUsage.cacheReadInputTokens || 0) + (hashUsage.cacheCreationInputTokens || 0) + (hashUsage.reasoningOutputTokens || 0) + Math.round((hashUsage.totalCostUsd || 0) * 1e6) + (hashUsage.estimated === true ? 1 : 0);
            }
          } else {
            totalBlocks += 1;
            contentLen = String(msgContent).length;
          }
        }
      }
      outputHash = msgCount * 1e5 + totalBlocks * 1e3 + contentLen;
    }
    var forceRender = forceFullRender || msgCount !== state.lastRenderedMsgCount;
    if (!forceRender && msgCount === state.lastRenderedMsgCount && outputHash === state.lastRenderedHash) {
      var chatMessages = chatOutput.querySelector(".chat-messages");
      if (chatMessages) renderStructuredStatusBar(chatMessages, selectedSession);
      updateTodoProgress(allMessages);
      return;
    }
    var prevHash = state.lastRenderedHash;
    var prevMsgCount = state.lastRenderedMsgCount;
    state.lastRenderedMsgCount = msgCount;
    state.lastRenderedHash = outputHash;
    chatMessages = ensureChatMessagesContainer(chatOutput);
    if (!chatMessages) return;
    var renderWasAtBottom = isChatNearBottom(chatMessages);
    var renderIsInitial = !state.chatInitialRenderDone;
    var existingCount = chatMessages.querySelectorAll(".chat-message:not(.system-info)").length;
    var needsFullRender = forceRender || existingCount === 0 || msgCount !== existingCount;
    function fullRenderChat() {
      var systemInfo = shouldExtractPtySystemInfo(selectedSession) ? extractPtySystemInfo(selectedSession.output, messages) : [];
      var html = "";
      var reversedMessages2 = messages.slice().reverse();
      var visibleCount = messages.length;
      for (var i2 = 0; i2 < reversedMessages2.length; i2++) {
        var msg = reversedMessages2[i2];
        var localIndex = visibleCount - 1 - i2;
        var originalIndex = localIndex + visibleOffset;
        var sysInfo = null;
        for (var j = 0; j < systemInfo.length; j++) {
          if (systemInfo[j].beforeMessage === localIndex) {
            sysInfo = systemInfo[j];
            break;
          }
        }
        if (sysInfo) {
          html += '<div class="chat-message system-info"><div class="system-info-card"><div class="system-info-header">\u2139\uFE0F \u7CFB\u7EDF\u4FE1\u606F</div><div class="system-info-content">' + escapeHtml(sysInfo.content) + "</div></div></div>";
        }
        html += renderChatMessage(msg, roundUsageByIndex[originalIndex] || null, originalIndex, legacyTaskMap);
      }
      if (hasOlderMessages) {
        var loadMoreLabel = visibleOffset > 0 ? "\u52A0\u8F7D\u66F4\u65E9\u7684 " + Math.min(state.chatPageSize, visibleOffset) + " \u6761\u6D88\u606F" : "\u52A0\u8F7D\u66F4\u65E9\u7684\u6D88\u606F";
        html += '<div class="chat-load-more" id="chat-load-more-sentinel"><button class="chat-load-more-btn" type="button">' + loadMoreLabel + "</button></div>";
      }
      var anchorMsgIndex = -1;
      var anchorOffset = 0;
      if (existingCount > 0 && !renderWasAtBottom) {
        var containerTop = chatMessages.getBoundingClientRect().top;
        var preEls = chatMessages.querySelectorAll(".chat-message:not(.system-info)");
        for (var pi = 0; pi < preEls.length; pi++) {
          var rect = preEls[pi].getBoundingClientRect();
          if (rect.bottom >= containerTop) {
            var idxAttr = preEls[pi].getAttribute("data-msg-index");
            if (idxAttr != null) {
              anchorMsgIndex = parseInt(idxAttr, 10);
              anchorOffset = rect.top - containerTop;
            }
            break;
          }
        }
      }
      chatMessages.innerHTML = html;
      (function() {
        var msgEls = chatMessages.querySelectorAll(".chat-message:not(.system-info)");
        var totalVisible = msgEls.length;
        for (var idx = 0; idx < totalVisible; idx++) {
          msgEls[idx].setAttribute("data-msg-index", String(visibleOffset + totalVisible - 1 - idx));
        }
      })();
      refreshTailMarqueePaths(chatMessages);
      if (prevMsgCount === 0 && !state.chatInitialRenderDone) {
        chatMessages.scrollTop = 0;
        state.chatStickToBottom = true;
        clearChatUnread({ removeDivider: true });
        state.chatInitialRenderDone = true;
      } else if (prevMsgCount === 0 && state.chatStickToBottom) {
        chatMessages.scrollTop = 0;
      } else if (renderWasAtBottom) {
        chatMessages.scrollTop = 0;
      } else if (anchorMsgIndex >= 0) {
        var newAnchor = chatMessages.querySelector(
          '.chat-message[data-msg-index="' + anchorMsgIndex + '"]'
        );
        if (newAnchor) {
          var newContainerTop = chatMessages.getBoundingClientRect().top;
          var newRect = newAnchor.getBoundingClientRect();
          var delta = newRect.top - newContainerTop - anchorOffset;
          if (Math.abs(delta) > 0.5) {
            state.chatIsProgrammaticScroll = true;
            chatMessages.scrollTop += delta;
            requestAnimationFrame(function() {
              state.chatIsProgrammaticScroll = false;
            });
          }
        }
      }
      attachAllCopyHandlers(chatMessages);
      bindChatScrollListener();
      applyPersistedExpandState(chatMessages);
      requestAnimationFrame(function() {
        refreshChatUnreadDivider(chatMessages);
        updateChatUnreadBubble();
        observeLoadMoreSentinel();
      });
    }
    var roundUsageByIndex = {};
    (function() {
      var acc = { inputTokens: 0, outputTokens: 0, cacheReadInputTokens: 0, cacheCreationInputTokens: 0, reasoningOutputTokens: 0, totalCostUsd: 0, estimated: false };
      var hasUsage = false;
      var lastAssistantIdx = -1;
      for (var mi2 = 0; mi2 < allMessages.length; mi2++) {
        var m = allMessages[mi2];
        if (m.role === "user") {
          if (lastAssistantIdx >= 0 && hasUsage) {
            roundUsageByIndex[lastAssistantIdx] = acc;
          }
          acc = { inputTokens: 0, outputTokens: 0, cacheReadInputTokens: 0, cacheCreationInputTokens: 0, reasoningOutputTokens: 0, totalCostUsd: 0, estimated: false };
          hasUsage = false;
          lastAssistantIdx = -1;
        } else if (m.role === "assistant" && m.usage) {
          var u = m.usage;
          hasUsage = true;
          acc.inputTokens += u.inputTokens || 0;
          acc.outputTokens += u.outputTokens || 0;
          acc.cacheReadInputTokens += u.cacheReadInputTokens || 0;
          acc.cacheCreationInputTokens += u.cacheCreationInputTokens || 0;
          acc.reasoningOutputTokens += u.reasoningOutputTokens || 0;
          acc.totalCostUsd += u.totalCostUsd || 0;
          acc.estimated = acc.estimated || u.estimated === true;
          lastAssistantIdx = mi2;
        } else if (m.role === "assistant") {
          lastAssistantIdx = mi2;
        }
      }
      if (lastAssistantIdx >= 0 && hasUsage) {
        roundUsageByIndex[lastAssistantIdx] = acc;
      }
    })();
    if (needsFullRender) {
      fullRenderChat();
    } else if (msgCount > existingCount) {
      var newMessages = messages.slice(existingCount);
      newMessages.reverse();
      var fragment = document.createDocumentFragment();
      var insertedEls = [];
      var insertedOrigIdx = [];
      var firstNewOrigIdx = visibleOffset + existingCount;
      for (var i = 0; i < newMessages.length; i++) {
        var div = document.createElement("div");
        var nmOrigIdx = visibleOffset + existingCount + (newMessages.length - 1 - i);
        div.innerHTML = renderChatMessage(newMessages[i], roundUsageByIndex[nmOrigIdx] || null, nmOrigIdx, legacyTaskMap);
        var el = div.firstElementChild;
        if (el) {
          el.classList.add("animate-in");
          el.setAttribute("data-msg-index", String(nmOrigIdx));
          insertedEls.push(el);
          insertedOrigIdx.push(nmOrigIdx);
          fragment.appendChild(el);
        }
      }
      chatMessages.insertBefore(fragment, chatMessages.firstChild);
      bindChatScrollListener();
      attachAllCopyHandlers(chatMessages);
      applyPersistedExpandState(chatMessages);
      if (renderWasAtBottom) {
        requestAnimationFrame(function() {
          if (chatMessages.isConnected && Math.abs(chatMessages.scrollTop) > 1) {
            state.chatIsProgrammaticScroll = true;
            chatMessages.scrollTop = 0;
            requestAnimationFrame(function() {
              state.chatIsProgrammaticScroll = false;
            });
          }
          clearChatUnread({ removeDivider: true });
          updateChatUnreadBubble();
        });
      } else {
        if (state.chatUnreadStartIndex < 0) {
          state.chatUnreadStartIndex = firstNewOrigIdx;
        }
        state.chatUnreadCount += insertedEls.length;
        refreshChatUnreadDivider(chatMessages);
        updateChatUnreadBubble();
      }
    } else if (msgCount === existingCount && outputHash !== prevHash) {
      var existingEls = Array.from(chatMessages.querySelectorAll(".chat-message:not(.system-info)"));
      var reversedMessages = messages.slice().reverse();
      var replacedAny = false;
      var MAX_STREAMING_SCAN = Math.min(4, reversedMessages.length, existingEls.length);
      for (var mi = 0; mi < MAX_STREAMING_SCAN; mi++) {
        var currentEl = existingEls[mi];
        var tmpWrap = document.createElement("div");
        var srOrigIdx = visibleOffset + reversedMessages.length - 1 - mi;
        tmpWrap.innerHTML = renderChatMessage(reversedMessages[mi], roundUsageByIndex[srOrigIdx] || null, srOrigIdx, legacyTaskMap);
        var replacementEl = tmpWrap.firstElementChild;
        if (!replacementEl) continue;
        if (currentEl.innerHTML !== replacementEl.innerHTML || currentEl.className !== replacementEl.className) {
          chatMessages.replaceChild(replacementEl, currentEl);
          attachCopyHandler(replacementEl);
          replacedAny = true;
        } else if (mi > 0) {
          break;
        }
      }
      if (!replacedAny && reversedMessages.length > MAX_STREAMING_SCAN) {
        fullRenderChat();
      }
      if (replacedAny) {
        bindChatScrollListener();
        applyPersistedExpandState(chatMessages);
        requestAnimationFrame(function() {
          if (renderWasAtBottom && chatMessages.isConnected && Math.abs(chatMessages.scrollTop) > 1) {
            state.chatIsProgrammaticScroll = true;
            chatMessages.scrollTop = 0;
            requestAnimationFrame(function() {
              state.chatIsProgrammaticScroll = false;
            });
          }
          refreshChatUnreadDivider(chatMessages);
          updateChatUnreadBubble();
        });
        var newestMsgEl = chatMessages.querySelector(".chat-message");
        var allCards = chatMessages.querySelectorAll(".tool-use-card, .inline-diff[data-expand-key]");
        var newestCard = null;
        allCards.forEach(function(c) {
          var cardKey = getElementExpandKey(c);
          if (getPersistedExpandState(cardKey) !== null) return;
          if (c.classList.contains("ask-user") && !c.classList.contains("ask-user-answered")) return;
          if (newestMsgEl && newestMsgEl.contains(c)) {
            if (!newestCard) newestCard = c;
            else c.classList.add("collapsed");
          } else {
            c.classList.add("collapsed");
          }
        });
      }
    } else if (msgCount < existingCount) {
      fullRenderChat();
    }
    snapCollapsedSubagentPanelsToBottom(chatMessages);
    applyHistoryCollapse(chatMessages, selectedSession);
    applyAutoFoldBar(chatOutput, chatMessages, allMessages, renderIsInitial);
    renderStructuredStatusBar(chatMessages, selectedSession);
    updateTodoProgress(allMessages);
  }
  var todoExpanded = false;
  document.addEventListener("click", function(e) {
    var target = e.target;
    if (!target || !(target instanceof Element)) return;
    var toggle = target.closest("#todo-progress-toggle");
    if (!toggle) return;
    e.preventDefault();
    e.stopPropagation();
    todoExpanded = !todoExpanded;
    toggle.setAttribute("aria-expanded", todoExpanded ? "true" : "false");
    toggle.setAttribute("aria-label", todoExpanded ? "\u6536\u8D77\u5F85\u529E\u5217\u8868" : "\u5C55\u5F00\u5F85\u529E\u5217\u8868");
    var prog = document.getElementById("todo-progress");
    var body = document.getElementById("todo-progress-body");
    if (prog && body) {
      if (todoExpanded) {
        prog.classList.add("expanded");
        body.classList.add("expanded");
      } else {
        prog.classList.remove("expanded");
        body.classList.remove("expanded");
      }
    }
    syncChatMessagesPaddingForTodoBody();
  });
  function syncChatMessagesPaddingForTodoBody() {
    var chatMessages = document.querySelector("#chat-output .chat-messages");
    var todoBody = document.getElementById("todo-progress-body");
    if (!chatMessages || !todoBody) return;
    var chatMessagesEl = chatMessages;
    var isVisible = !todoBody.classList.contains("hidden");
    var isExpanded = todoBody.classList.contains("expanded");
    var bodyHeight = todoBody.offsetHeight;
    if (isVisible && isExpanded && bodyHeight > 0) {
      chatMessagesEl.style.paddingBottom = bodyHeight + 8 + "px";
    } else {
      chatMessagesEl.style.paddingBottom = "";
    }
  }
  function flattenToolResultContent(content) {
    if (typeof content === "string") return content;
    if (Array.isArray(content)) {
      var parts = [];
      for (var i = 0; i < content.length; i++) {
        var piece = content[i];
        if (typeof piece === "string") parts.push(piece);
        else if (piece && typeof piece.text === "string") parts.push(piece.text);
      }
      return parts.join("");
    }
    return "";
  }
  function reconstructTodosFromTaskTools(messages, startIdx) {
    var resultById = {};
    for (var i = startIdx; i < messages.length; i++) {
      var msg = messages[i];
      if (!msg || !Array.isArray(msg.content)) continue;
      for (var j = 0; j < msg.content.length; j++) {
        var b = msg.content[j];
        if (b && b.type === "tool_result" && b.tool_use_id) {
          resultById[b.tool_use_id] = flattenToolResultContent(b.content);
        }
      }
    }
    var taskMap = {};
    var order = 0;
    var createFallback = 0;
    var sawTaskTool = false;
    for (var m = startIdx; m < messages.length; m++) {
      var msg2 = messages[m];
      if (!msg2 || !Array.isArray(msg2.content)) continue;
      for (var k = 0; k < msg2.content.length; k++) {
        var blk = msg2.content[k];
        if (!blk || blk.type !== "tool_use") continue;
        var input = blk.input || {};
        if (blk.name === "TaskCreate") {
          sawTaskTool = true;
          createFallback++;
          var res = resultById[blk.id] || "";
          var match = res.match(/#(\d+)/);
          var cid = match ? match[1] : String(createFallback);
          taskMap[cid] = {
            id: cid,
            content: input.subject || "",
            activeForm: input.activeForm || "",
            status: "pending",
            order: order++
          };
        } else if (blk.name === "TaskUpdate") {
          sawTaskTool = true;
          var uid = String(input.taskId);
          var task = taskMap[uid];
          if (!task) {
            task = { id: uid, content: "", activeForm: "", status: "pending", order: order++ };
            taskMap[uid] = task;
          }
          if (input.status) task.status = input.status;
          if (input.subject) task.content = input.subject;
          if (input.activeForm) task.activeForm = input.activeForm;
        }
      }
    }
    if (!sawTaskTool) return null;
    var list = [];
    for (var key in taskMap) {
      if (!Object.prototype.hasOwnProperty.call(taskMap, key)) continue;
      if (taskMap[key].status === "deleted") continue;
      list.push(taskMap[key]);
    }
    list.sort(function(a, b2) {
      return a.order - b2.order;
    });
    return list.length ? list : null;
  }
  function updateTodoProgress(messages) {
    var startIdx = 0;
    for (var ui = messages.length - 1; ui >= 0; ui--) {
      if (messages[ui] && messages[ui].role === "user") {
        startIdx = ui + 1;
        break;
      }
    }
    var todos = null;
    for (var i = messages.length - 1; i >= startIdx; i--) {
      var msg = messages[i];
      if (!msg.content || !Array.isArray(msg.content)) continue;
      for (var j = msg.content.length - 1; j >= 0; j--) {
        var block = msg.content[j];
        if (block.type === "tool_use" && block.semantic && block.semantic.kind === "task_list") {
          todos = block.semantic.items;
          break;
        }
        if (block.type === "tool_use" && block.name === "TodoWrite" && block.input && block.input.todos) {
          todos = block.input.todos;
          break;
        }
      }
      if (todos) break;
    }
    if (!todos) {
      todos = reconstructTodosFromTaskTools(messages, startIdx);
    }
    var container = document.getElementById("todo-progress");
    var bodyEl = document.getElementById("todo-progress-body");
    if (!container) return;
    if (!todos || todos.length === 0) {
      container.classList.add("hidden");
      if (bodyEl) bodyEl.classList.add("hidden");
      syncChatMessagesPaddingForTodoBody();
      return;
    }
    var completed = 0;
    var inProgress = 0;
    var activeTask = "";
    for (var k = 0; k < todos.length; k++) {
      if (todos[k].status === "completed") completed++;
      if (todos[k].status === "in_progress") {
        inProgress++;
        if (!activeTask) {
          activeTask = todos[k].activeForm || todos[k].content || "";
        }
      }
    }
    var allDone = completed === todos.length;
    var selectedSession = state.sessions.find(function(s) {
      return s.id === state.selectedId;
    });
    var sessionActive = !!selectedSession && selectedSession.status === "running";
    if (!sessionActive || allDone) {
      container.classList.add("hidden");
      if (bodyEl) bodyEl.classList.add("hidden");
      syncChatMessagesPaddingForTodoBody();
      return;
    }
    container.classList.remove("hidden");
    container.classList.remove("all-done");
    if (bodyEl) bodyEl.classList.remove("hidden");
    var counter = document.getElementById("todo-progress-counter");
    if (counter) counter.textContent = completed + " / " + todos.length;
    var task = document.getElementById("todo-progress-task");
    if (task) {
      if (!activeTask) {
        for (var p = 0; p < todos.length; p++) {
          if (todos[p].status === "pending" && (todos[p].activeForm || todos[p].content)) {
            activeTask = todos[p].activeForm || todos[p].content;
            break;
          }
        }
      }
      task.textContent = activeTask || "\u51C6\u5907\u4E2D\u2026";
    }
    var ratio = todos.length > 0 ? completed / todos.length : 0;
    var ring = document.getElementById("todo-progress-ring");
    if (ring) {
      ring.style.setProperty("--progress", ratio.toFixed(3));
    }
    var fill = document.getElementById("todo-progress-fill");
    if (fill) {
      fill.style.setProperty("--progress", ratio.toFixed(3));
    }
    var list = document.getElementById("todo-progress-list");
    if (list) {
      var html = "";
      for (var m = 0; m < todos.length; m++) {
        var t6 = todos[m];
        var st = t6.status || "pending";
        var itemClass = st === "in_progress" ? "active" : st === "completed" ? "done" : "";
        var iconClass = st === "in_progress" ? "active" : st === "completed" ? "done" : "pending";
        var icon = st === "completed" ? "\u2713" : st === "in_progress" ? "\u203A" : "\u25CB";
        html += '<li class="todo-progress-item ' + itemClass + '"><span class="todo-item-icon ' + iconClass + '">' + icon + "</span><span>" + escapeHtml(t6.content || "") + "</span></li>";
      }
      list.innerHTML = html;
    }
    if (state.selectedId) {
      syncSessionProgressToNative(state.selectedId);
    }
    syncChatMessagesPaddingForTodoBody();
  }
  function attachCopyHandler(el) {
    el.querySelectorAll(".code-copy").forEach(function(btn) {
      btn.addEventListener("click", function() {
        var codeBlock = btn.closest(".code-block");
        var code = codeBlock ? codeBlock.querySelector("code") : null;
        if (code) {
          copyToClipboard(code.textContent || "", null, function() {
            btn.textContent = "Copied!";
            btn.classList.add("copied");
            setTimeout(function() {
              btn.textContent = "Copy";
              btn.classList.remove("copied");
            }, 2e3);
          });
        }
      });
    });
  }
  function attachAllCopyHandlers(container) {
    container.querySelectorAll(".code-copy").forEach(function(btn) {
      var clone = btn.cloneNode(true);
      btn.parentNode.replaceChild(clone, btn);
      clone.addEventListener("click", function() {
        var codeBlock = clone.closest(".code-block");
        var code = codeBlock ? codeBlock.querySelector("code") : null;
        if (code) {
          copyToClipboard(code.textContent || "", null, function() {
            clone.textContent = "Copied!";
            clone.classList.add("copied");
            setTimeout(function() {
              clone.textContent = "Copy";
              clone.classList.remove("copied");
            }, 2e3);
          });
        }
      });
    });
    attachMessageCopyButtons(container);
  }
  function attachMessageCopyButtons(container) {
    var isTouch = window.matchMedia("(pointer: coarse)").matches;
    if (!isTouch) return;
    container.querySelectorAll(".chat-message").forEach(function(msgEl) {
      if (msgEl.querySelector(".msg-copy-btn")) return;
      var bubble = msgEl.querySelector(".chat-message-bubble");
      if (!bubble) return;
      var btn = document.createElement("button");
      btn.className = "msg-copy-btn";
      btn.textContent = "\u590D\u5236";
      btn.addEventListener("click", function(e) {
        e.stopPropagation();
        var text5 = bubble.innerText || bubble.textContent || "";
        copyToClipboard(text5.trim(), null, function() {
          btn.textContent = "\u5DF2\u590D\u5236";
          btn.classList.add("copied");
          setTimeout(function() {
            btn.textContent = "\u590D\u5236";
            btn.classList.remove("copied");
            btn.classList.remove("visible");
          }, 1500);
        });
      });
      msgEl.appendChild(btn);
    });
  }
  (function initMobileCopyLongPress() {
    var isTouch = window.matchMedia("(pointer: coarse)").matches;
    if (!isTouch) return;
    var longPressTimer = null;
    var touchStartY = 0;
    document.addEventListener("touchstart", function(e) {
      var msgEl = e.target.closest(".chat-message");
      if (!msgEl) return;
      var bubble = msgEl.querySelector(".chat-message-bubble");
      if (!bubble) return;
      touchStartY = e.touches[0].clientY;
      longPressTimer = setTimeout(function() {
        var btn = msgEl.querySelector(".msg-copy-btn");
        if (btn) {
          document.querySelectorAll(".msg-copy-btn.visible").forEach(function(b) {
            b.classList.remove("visible");
          });
          btn.classList.add("visible");
        }
      }, 500);
    }, { passive: true });
    document.addEventListener("touchmove", function(e) {
      if (longPressTimer && Math.abs(e.touches[0].clientY - touchStartY) > 10) {
        clearTimeout(longPressTimer);
        longPressTimer = null;
      }
    }, { passive: true });
    document.addEventListener("touchend", function() {
      if (longPressTimer) {
        clearTimeout(longPressTimer);
        longPressTimer = null;
      }
    }, { passive: true });
    document.addEventListener("click", function(e) {
      if (!e.target.closest(".msg-copy-btn")) {
        document.querySelectorAll(".msg-copy-btn.visible").forEach(function(b) {
          b.classList.remove("visible");
        });
      }
    });
  })();
  function isNoiseLine(line) {
    if (!line) return false;
    var trimmed = String(line).trim();
    if (!trimmed) return false;
    if (trimmed.indexOf("\u2500\u2500\u2500\u2500") === 0) return true;
    if (trimmed === "\u276F" || trimmed === "\u203A") return true;
    if (/^[╭╰│┌└┐┘├┤┬┴┼─═]{2,}$/.test(trimmed)) return true;
    if (/^[▁▂▃▄▅▆▇█▔▕▏▐]+$/.test(trimmed)) return true;
    if (trimmed.indexOf("esc to interrupt") !== -1) return true;
    if (trimmed.indexOf("Claude Code v") !== -1) return true;
    if (/^Sonnet\b/.test(trimmed)) return true;
    if (trimmed.indexOf("Failed to install Anthropic") !== -1) return true;
    if (trimmed.indexOf("Claude Code has switched") !== -1) return true;
    if (trimmed.indexOf("? for shortcuts") !== -1) return true;
    if (trimmed.indexOf("Claude is waiting") !== -1) return true;
    if (trimmed.indexOf("[wand]") !== -1) return true;
    if (trimmed.indexOf("0;") === 0 || trimmed.indexOf("9;") === 0) return true;
    if (trimmed.indexOf("ctrl+g") !== -1) return true;
    if (trimmed.indexOf("/effort") !== -1) return true;
    if (/^Using .* for .* session/.test(trimmed)) return true;
    if (trimmed.indexOf("Press ") === 0 && trimmed.indexOf(" for") !== -1) return true;
    if (trimmed.indexOf("type ") === 0 && trimmed.indexOf(" to ") !== -1) return true;
    if (trimmed.indexOf("auto mode is unavailable") !== -1) return true;
    if (/MCP server.*failed/i.test(trimmed)) return true;
    if (trimmed.indexOf("Germinating") !== -1 || trimmed.indexOf("Doodling") !== -1 || trimmed.indexOf("Brewing") !== -1) return true;
    if (trimmed.indexOf("Permissions") !== -1 && trimmed.indexOf("mode") !== -1) return true;
    if (trimmed.indexOf("\u25CF") === 0 && trimmed.indexOf("\xB7") !== -1) return true;
    if (trimmed.indexOf("[>") === 0 || trimmed.indexOf("[<") === 0) return true;
    if (trimmed.indexOf("Captured Claude session ID") !== -1) return true;
    if (/^>_\s*OpenAI Codex\b/.test(trimmed)) return true;
    if (/^OpenAI Codex\b/i.test(trimmed)) return true;
    if (/^(model|directory):\s+/i.test(trimmed)) return true;
    if (/^(tip|context):\s+/i.test(trimmed)) return true;
    if (/^work(tree|space):\s+/i.test(trimmed)) return true;
    if (/^(approvals?|sandbox|provider|session id):\s+/i.test(trimmed)) return true;
    if (/^(thinking|working)(\.\.\.|…)?$/i.test(trimmed)) return true;
    if (/^[•◦·]\s+Working\b/i.test(trimmed)) return true;
    if (/^[•◦·]\s+(Running|Planning|Applying|Reading|Searching)\b/i.test(trimmed)) return true;
    if (/^[•◦·]\s+(Inspecting|Reviewing|Summarizing|Editing|Updating|Writing)\b/i.test(trimmed)) return true;
    if (/^[•◦·]\s+Completed\b/i.test(trimmed)) return true;
    if (/^(ctrl|enter|tab|shift|esc|alt)\+/i.test(trimmed)) return true;
    if (/\b(open|close|toggle) (chat|terminal)\b/i.test(trimmed)) return true;
    if (/\b(approve|deny)\b.*\b(permission|approval)\b/i.test(trimmed)) return true;
    if (/^(use|press) .* (to|for) .*/i.test(trimmed)) return true;
    if (/^(?:token|context window|remaining context|conversation):\s+/i.test(trimmed)) return true;
    if (/^(?:cwd|path):\s+\//i.test(trimmed)) return true;
    if (/^[<>│┆╎].*[<>│┆╎]$/.test(trimmed) && trimmed.length < 8) return true;
    return false;
  }
  function stripAnsi(text5) {
    return String(text5 || "").replace(/\x1b\][^\x07]*(\x07|\x1b\\)/g, "").replace(/\x1b\[(\d+)C/g, function(_match, count3) {
      return " ".repeat(Number(count3) || 1);
    }).replace(/\x1b\[[0-9;?]*[AB]/g, "\n").replace(/\x1b\[[0-9;?]*[su]/g, "").replace(/\x1b\[[0-9;?]*[HfJKr]/g, "\n").replace(/\x1bM/g, "\n").replace(/\x1b\[[0-9;?]*[ST]/g, "\n").replace(/\x1b\[[0-9;?]*[a-zA-Z]/g, "").replace(/\x1b[><=ePX^_]/g, "").replace(/[\u00a0\u200b-\u200d\ufeff]/g, " ").replace(/[\x00-\x08\x0b\x0c\x0e-\x1f]/g, "").replace(/\r\n?/g, "\n").replace(/[ \t]+\n/g, "\n").replace(/\n{3,}/g, "\n\n");
  }
  function parseMessages(output, command) {
    var messages = [];
    if (!output) return messages;
    var text5 = String(output || "");
    var newline = String.fromCharCode(10);
    var carriageReturn = String.fromCharCode(13);
    var esc = String.fromCharCode(27);
    if (/^codex\b/.test(String(command || "").trim())) {
      let stripCodexSegment2 = function(raw) {
        return String(raw || "").replace(/\x1b\][^\x07]*(\x07|\x1b\\)/g, "").replace(/\x1b\[(\d+)C/g, function(_match, count3) {
          return " ".repeat(Number(count3) || 1);
        }).replace(/\x1b\[[0-9;?]*[AB]/g, newline).replace(/\x1b\[[0-9;?]*[su]/g, "").replace(/\x1b\[[0-9;?]*[HfJKr]/g, newline).replace(/\x1bM/g, newline).replace(/\x1b\[[0-9;?]*[ST]/g, newline).replace(/\x1b\[[0-9;?]*[a-zA-Z]/g, "").replace(/\x1b[><=ePX^_]/g, "").replace(/[\u00a0\u200b-\u200d\ufeff]/g, " ").replace(/[\x00-\x08\x0b\x0c\x0e-\x1f]/g, "").replace(/[ \t]+\n/g, newline);
      }, normalizeCodexText2 = function(value) {
        return String(value || "").replace(/\s+/g, " ").replace(/[M]+$/g, "").trim();
      }, normalizeCodexPromptLine2 = function(line2) {
        return String(line2 || "").replace(/^›\s*/, "").replace(/^>\s*/, "").trim();
      }, shouldIgnoreCodexLine2 = function(line2) {
        var trimmed = String(line2 || "").trim();
        if (!trimmed) return true;
        if (isNoiseLine(trimmed)) return true;
        if (codexFooterRe.test(trimmed)) return true;
        if (/^[╭╰│┌└┐┘├┤┬┴┼─═]/.test(trimmed)) return true;
        if (/^\[>[0-9;?]*u$/i.test(trimmed)) return true;
        if (/^M+$/i.test(trimmed)) return true;
        if (/^(?:OpenAI Codex|Codex)\b/i.test(trimmed)) return true;
        if (/^(?:tokens?|context window|remaining context|approvals?|sandbox|provider|session id):\s*/i.test(trimmed)) return true;
        if (/^(?:thinking|working)\s*(?:\.\.\.|…)?$/i.test(trimmed)) return true;
        if (/^[•◦·]\s+(?:thinking|working|running|planning|applying|reading|searching|inspecting|reviewing|summarizing|editing|updating|writing|completed)\b/i.test(trimmed)) return true;
        if (/^(?:model|directory|tip|context|cwd|path):\s+/i.test(trimmed)) return true;
        return false;
      }, extractCodexPromptCandidate2 = function(line2) {
        var trimmed = String(line2 || "").trim();
        if (!/^›(?:\s|$)/.test(trimmed)) return null;
        if (codexFooterRe.test(trimmed)) return null;
        var prompt = normalizeCodexText2(normalizeCodexPromptLine2(trimmed));
        if (!prompt || shouldIgnoreCodexLine2(prompt)) return null;
        return prompt;
      }, extractCodexAssistantCandidate2 = function(line2) {
        var trimmed = String(line2 || "").trim();
        if (!/^[•◦·⏺]/.test(trimmed)) return null;
        var assistant = trimmed.replace(/^[•◦·]\s*/, "").replace(/^⏺\s+/, "").replace(/^│\s*/, "").trim();
        if (!assistant || /^[•◦·⏺]$/.test(assistant)) return null;
        assistant = assistant.replace(/\s*\(\d+[smh]?\s*•\s*esc to interrupt\)[\s\S]*$/i, "").replace(/(?:[a-z]{1,6})?›[\s\S]*$/, "").replace(/\s{2,}gpt-\d[\s\S]*$/i, "").replace(/\b(?:OpenAI Codex|model:|directory:|Tip:)\b[\s\S]*$/i, "");
        assistant = normalizeCodexText2(assistant);
        if (!assistant || assistant.length < 2 || codexActivityRe.test(assistant) || shouldIgnoreCodexLine2(assistant)) {
          return null;
        }
        return assistant;
      }, extractCodexEchoCandidate2 = function(line2) {
        var trimmed = normalizeCodexText2(line2);
        if (!trimmed || shouldIgnoreCodexLine2(trimmed)) return null;
        if (/^[•◦·⏺›]/.test(trimmed)) return null;
        if (/^[\[\]<>0-9;?]+u?$/i.test(trimmed)) return null;
        if (/^[╭╰│┌└┐┘├┤┬┴┼─═]/.test(trimmed)) return null;
        if (trimmed.length > 500) return null;
        return trimmed;
      }, isLikelyAssistantTailArtifact2 = function(longer, shorter) {
        if (longer.indexOf(shorter) !== 0) return false;
        var suffix = longer.slice(shorter.length);
        return /^[a-z]{1,4}$/i.test(suffix);
      }, coalesceAssistantLines2 = function(lines2) {
        var collected = [];
        for (var i2 = 0; i2 < lines2.length; i2++) {
          var normalized = normalizeCodexText2(lines2[i2]);
          if (!normalized || normalized.length < 2 || shouldIgnoreCodexLine2(normalized)) continue;
          var previous = collected[collected.length - 1];
          if (!previous) {
            collected.push(normalized);
            continue;
          }
          if (normalized === previous) continue;
          if (normalized.indexOf(previous) === 0) {
            collected[collected.length - 1] = normalized;
            continue;
          }
          if (previous.indexOf(normalized) === 0) {
            if (isLikelyAssistantTailArtifact2(previous, normalized)) {
              collected[collected.length - 1] = normalized;
            }
            continue;
          }
          collected.push(normalized);
        }
        return collected.join(newline).trim();
      }, extractVisiblePrompt2 = function(lines2) {
        for (var i2 = 0; i2 < lines2.length; i2++) {
          var line2 = String(lines2[i2] || "").trim();
          if (!line2) continue;
          var inlinePrompt = extractCodexPromptCandidate2(line2);
          if (inlinePrompt) return inlinePrompt;
          if (line2 === "\u203A") {
            for (var j = i2 + 1; j < lines2.length; j++) {
              var nextLine = normalizeCodexText2(lines2[j]);
              if (!nextLine || codexFooterRe.test(nextLine) || shouldIgnoreCodexLine2(nextLine)) continue;
              return nextLine;
            }
          }
        }
        return null;
      }, extractVisibleAssistantLines2 = function(lines2) {
        var assistantLines = [];
        var collecting = false;
        for (var i2 = 0; i2 < lines2.length; i2++) {
          var line2 = String(lines2[i2] || "").trim();
          if (!line2) {
            if (collecting) break;
            continue;
          }
          var assistant = extractCodexAssistantCandidate2(line2);
          if (assistant) {
            assistantLines.push(assistant);
            collecting = true;
            continue;
          }
          if (collecting) {
            if (line2 === "\u203A" || /^›(?:\s|$)/.test(line2) || codexFooterRe.test(line2) || shouldIgnoreCodexLine2(line2)) {
              break;
            }
            assistantLines.push(normalizeCodexText2(line2));
          }
        }
        return assistantLines;
      };
      var stripCodexSegment = stripCodexSegment2, normalizeCodexText = normalizeCodexText2, normalizeCodexPromptLine = normalizeCodexPromptLine2, shouldIgnoreCodexLine = shouldIgnoreCodexLine2, extractCodexPromptCandidate = extractCodexPromptCandidate2, extractCodexAssistantCandidate = extractCodexAssistantCandidate2, extractCodexEchoCandidate = extractCodexEchoCandidate2, isLikelyAssistantTailArtifact = isLikelyAssistantTailArtifact2, coalesceAssistantLines = coalesceAssistantLines2, extractVisiblePrompt = extractVisiblePrompt2, extractVisibleAssistantLines = extractVisibleAssistantLines2;
      var codexFooterRe = /\bgpt-\d+(?:\.\d+)?(?:\s+[a-z0-9.-]+)?\s+·\s+\d+%\s+left\s+·\s+(?:\/|~\/).+/i;
      var codexActivityRe = /^(?:thinking|working|running|planning|applying|reading|searching|inspecting|reviewing|summarizing|editing|updating|writing|completed)\b/i;
      var rawCandidates = [];
      var candidateOrder = 0;
      var rawSegments = text5.replace(/\r\n?/g, newline).split(newline);
      for (var rs = 0; rs < rawSegments.length; rs++) {
        var cleanedSegment = stripCodexSegment2(rawSegments[rs]);
        var pieces = cleanedSegment.split(newline);
        for (var pi = 0; pi < pieces.length; pi++) {
          var piece = String(pieces[pi] || "").trim();
          if (!piece) continue;
          var promptCandidate = extractCodexPromptCandidate2(piece);
          if (promptCandidate) {
            rawCandidates.push({ kind: "user", order: candidateOrder++, text: promptCandidate });
            continue;
          }
          var assistantCandidate = extractCodexAssistantCandidate2(piece);
          if (assistantCandidate) {
            rawCandidates.push({ kind: "assistant", order: candidateOrder++, text: assistantCandidate });
            continue;
          }
          var echoCandidate = extractCodexEchoCandidate2(piece);
          if (echoCandidate) {
            rawCandidates.push({ kind: "echo", order: candidateOrder++, text: echoCandidate });
          }
        }
      }
      var candidates = rawCandidates.filter(function(candidate, index2, list) {
        var previous = list[index2 - 1];
        return !previous || previous.kind !== candidate.kind || previous.text !== candidate.text;
      });
      var explicitUsers = candidates.filter(function(candidate) {
        return candidate.kind === "user";
      });
      var assistantCandidates = candidates.filter(function(candidate) {
        return candidate.kind === "assistant";
      });
      var echoCandidates = candidates.filter(function(candidate) {
        return candidate.kind === "echo";
      });
      var strippedOutput = stripAnsi(text5);
      var strippedLines = strippedOutput.split(newline).map(function(line2) {
        return String(line2 || "").trimEnd();
      });
      var visiblePrompt = extractVisiblePrompt2(strippedLines);
      var latestExplicitUser = explicitUsers.length ? explicitUsers[explicitUsers.length - 1] : null;
      var echoedUserCandidates = echoCandidates.map(function(candidate) {
        return candidate.text;
      }).filter(function(value) {
        return value.length >= 3;
      });
      var latestEchoUser = null;
      for (var eu = echoedUserCandidates.length - 1; eu >= 0; eu--) {
        if (echoedUserCandidates[eu] !== visiblePrompt) {
          latestEchoUser = echoedUserCandidates[eu];
          break;
        }
      }
      if (!latestEchoUser && echoedUserCandidates.length) {
        latestEchoUser = echoedUserCandidates[echoedUserCandidates.length - 1];
      }
      var currentUser = latestExplicitUser ? latestExplicitUser.text : latestEchoUser;
      var rawAssistantLines = assistantCandidates.filter(function(candidate) {
        return !latestExplicitUser || candidate.order > latestExplicitUser.order;
      }).map(function(candidate) {
        return candidate.text;
      });
      var visibleAssistantFallback = [];
      var bulletMatches = strippedOutput.match(/^[ \t]*[•◦·⏺][ \t]*(.+)$/gm) || [];
      for (var bm = 0; bm < bulletMatches.length; bm++) {
        var bulletContent = normalizeCodexText2(bulletMatches[bm].replace(/^[ \t]*[•◦·⏺][ \t]*/, ""));
        if (!bulletContent) continue;
        if (codexActivityRe.test(bulletContent)) continue;
        if (codexFooterRe.test(bulletContent)) continue;
        if (/\b(?:OpenAI Codex|model:|directory:|Tip:|esc to interrupt)\b/i.test(bulletContent)) continue;
        visibleAssistantFallback.push(bulletContent);
      }
      var assistantText = coalesceAssistantLines2(rawAssistantLines) || coalesceAssistantLines2(extractVisibleAssistantLines2(strippedLines)) || (visibleAssistantFallback.length ? visibleAssistantFallback[visibleAssistantFallback.length - 1] : null);
      if (currentUser) {
        messages.push({ role: "user", content: currentUser });
      }
      if (assistantText) {
        messages.push({ role: "assistant", content: assistantText });
      }
      if (!messages.length && latestExplicitUser) {
        messages.push({ role: "user", content: latestExplicitUser.text });
      } else if (!messages.length && latestEchoUser) {
        messages.push({ role: "user", content: latestEchoUser });
      }
      return messages;
    }
    var nul = String.fromCharCode(0);
    var bs = String.fromCharCode(8);
    var vt = String.fromCharCode(11);
    var ff = String.fromCharCode(12);
    var so = String.fromCharCode(14);
    var us = String.fromCharCode(31);
    var nbsp = String.fromCharCode(160);
    var bel = String.fromCharCode(7);
    var ansiRegex = new RegExp(
      esc + "\\[[0-9;?]*[a-zA-Z]|" + // CSI sequences
      esc + "\\][^" + bel + "]*(" + bel + "|" + esc + "\\\\\\\\)|" + // OSC sequences - matches ESC ] ... (BEL or ESC \)
      esc + "[><=eP_X^]|[" + nul + "-" + bs + vt + ff + so + "-" + us + "]|" + // Control chars: 0-8, 11, 12, 14-31
      nbsp + "|" + carriageReturn,
      "g"
    );
    var ansiStripped = text5.replace(
      ansiRegex,
      function(m) {
        return m === nbsp ? " " : m === carriageReturn ? newline : "";
      }
    ).split(carriageReturn).join(newline);
    var lines = ansiStripped.split(newline).map(function(line2) {
      return line2.trim();
    }).filter(Boolean);
    var thinkingPatterns = [
      /thinking with high effort/i,
      /thinking with medium effort/i,
      /thinking with low effort/i,
      new RegExp("thought for [0-9]+s", "i"),
      new RegExp("Sauteed for [0-9]+m", "i"),
      /Germinating/i,
      /Doodling/i,
      /Brewing/i
    ];
    var lastThinkingLine = null;
    var userCmdIndex = -1;
    var promptLines = [];
    var contentLines = [];
    var thinkingLines = [];
    for (var i = 0; i < lines.length; i++) {
      var line = lines[i];
      var lineForPromptCheck = line.replace(/^❯\s*/, "");
      if (lineForPromptCheck.indexOf('Try"') === 0 || lineForPromptCheck.indexOf('Try "') === 0) {
        promptLines.push(lineForPromptCheck);
        continue;
      }
      var isThinking = false;
      for (var p = 0; p < thinkingPatterns.length; p++) {
        if (thinkingPatterns[p].test(line)) {
          isThinking = true;
          thinkingLines.push(line);
          break;
        }
      }
      if (isThinking) continue;
      if (!line) continue;
      if (line.indexOf("\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500\u2500") === 0) continue;
      if (line === "\u276F") continue;
      if (line.indexOf("esc to interrupt") !== -1) continue;
      if (line.indexOf("Claude Code v") !== -1) continue;
      if (line.indexOf("Sonnet") !== -1) continue;
      if (line.indexOf("~/") === 0) continue;
      if (line.indexOf("\u25CF high") !== -1) continue;
      if (line.indexOf("Failed to install Anthropic marketplace") !== -1) continue;
      if (line.indexOf("Claude Code has switched from npm to native installer") !== -1) continue;
      if (line.indexOf("Fluttering") !== -1) continue;
      if (line.indexOf("? for shortcuts") !== -1) continue;
      if (line.indexOf("0;") === 0) continue;
      if (line.indexOf("9;") === 0) continue;
      if (line.indexOf("Claude is waiting") !== -1) continue;
      if (line.indexOf("\u2722") !== -1 || line.indexOf("\u2733") !== -1 || line.indexOf("\u2736") !== -1 || line.indexOf("\u273B") !== -1 || line.indexOf("\u273D") !== -1) continue;
      if (line.indexOf("\u2590") === 0 || line.indexOf("\u259D") === 0 || line.indexOf("\u2598") === 0) continue;
      if ((line === "lu" || line === "ue" || line === "tr" || line === "ti" || line === "g" || line === "n" || line === "i\u2026" || line === "\u2026" || line === "uts" || line === "lt" || line === "rg" || line === "\xB7") && line.length < 4) continue;
      if (line.indexOf("\u273DF") === 0 || line.indexOf("\u273BF") === 0) continue;
      if (line.indexOf("npm WARN") !== -1) continue;
      if (line.indexOf("npm notice") !== -1) continue;
      if (line.indexOf("added ") !== -1 && line.indexOf(" packages") !== -1) continue;
      if (line.indexOf("audited ") !== -1) continue;
      if (line.indexOf("found ") !== -1 && line.indexOf(" vulnerabilities") !== -1) continue;
      if (line.indexOf("Using ") !== -1 && line.indexOf(" for ") !== -1 && line.indexOf("session") !== -1) continue;
      if (line.indexOf("You can use") !== -1) continue;
      if (line.indexOf("Press ") !== -1 && line.indexOf(" for") !== -1) continue;
      if (line.indexOf("type ") === 0 && line.indexOf(" to ") !== -1) continue;
      if (line.indexOf("[wand]") === 0) continue;
      if (line.indexOf("Captured Claude session ID") !== -1) continue;
      if (line.indexOf("\u23F5") !== -1) continue;
      if (line.indexOf("acceptedit") !== -1) continue;
      if (line.indexOf("shift+tab") !== -1) continue;
      if (line.indexOf("tabtocycle") !== -1) continue;
      if (line.indexOf("ctrl+g") !== -1) continue;
      if (line.indexOf("/effort") !== -1) continue;
      if (line.indexOf("Opus") !== -1 && line.indexOf("model") !== -1) continue;
      if (line.indexOf("Haiku") !== -1) continue;
      if (line.indexOf("to cycle") !== -1) continue;
      if (line.indexOf("high \xB7") !== -1 || line.indexOf("high\xB7") !== -1) continue;
      if (line.indexOf("medium \xB7") !== -1 || line.indexOf("medium\xB7") !== -1) continue;
      if (line.indexOf("low \xB7") !== -1 || line.indexOf("low\xB7") !== -1) continue;
      if (line.indexOf("\u25CF") === 0) {
        line = line.slice(1).trim();
        if (!line) continue;
        contentLines.push(line);
        continue;
      }
      if (line.length < 3 && !/^[a-zA-Z]{3}$/.test(line)) continue;
      contentLines.push(line);
    }
    if (thinkingLines.length > 0) {
      var lastThinking = thinkingLines[thinkingLines.length - 1];
      var durationMatch = lastThinking.match(new RegExp("for ([0-9]+[ms]+| [0-9]+m [0-9]+s)", "i"));
      var thinkingText = durationMatch ? "\u6DF1\u5EA6\u601D\u8003 " + durationMatch[0].replace(/for /i, "") : "\u6DF1\u5EA6\u601D\u8003\u4E2D...";
      messages.push({ role: "thinking", content: thinkingText, type: "deep-thought" });
    }
    if (promptLines.length > 0) {
      var promptText = promptLines[promptLines.length - 1].replace(/^Try\s*/, "").trim();
      messages.push({ role: "prompt", content: promptText, type: "suggestion" });
    }
    if (!contentLines.length) return messages;
    var turns = [];
    var currentUserText = null;
    var currentAssistantLines = [];
    for (var i = 0; i < contentLines.length; i++) {
      line = contentLines[i];
      if (line.indexOf("\u276F") === 0) {
        var afterPrompt = line.replace(/^❯\s*/, "").trim();
        if (afterPrompt.indexOf('Try"') === 0 || afterPrompt.indexOf('Try "') === 0) continue;
        if (currentUserText !== null && currentAssistantLines.length > 0) {
          turns.push({ user: currentUserText, assistantLines: currentAssistantLines });
          currentAssistantLines = [];
        }
        if (afterPrompt) {
          currentUserText = afterPrompt;
        } else {
          if (currentUserText !== null && currentAssistantLines.length > 0) {
            turns.push({ user: currentUserText, assistantLines: currentAssistantLines });
            currentAssistantLines = [];
          }
          currentUserText = null;
        }
      } else if (currentUserText !== null) {
        if (line.indexOf("\u23FA") !== -1 && (line.indexOf("Hi!") !== -1 || line.indexOf("Hello") !== -1 || line.indexOf("What") !== -1 || line.indexOf("working") !== -1)) {
          currentAssistantLines.push(line);
        } else if (line.indexOf("\u23FA") === 0) {
          currentAssistantLines.push(line.slice(1).trim() || line);
        } else if (line.length >= 8) {
          if (line.indexOf("\u2722") === -1 && line.indexOf("\u2733") === -1 && line.indexOf("\u2736") === -1 && line.indexOf("\u273B") === -1 && line.indexOf("\u273D") === -1 && line.indexOf("\u2590") !== 0 && line.indexOf("\u259D") !== 0 && line.indexOf("\u2598") !== 0 && line.indexOf("esctointerrupt") === -1 && line.indexOf("?for") !== 0 && line.indexOf("? for") !== 0) {
            currentAssistantLines.push(line);
          }
        }
      }
    }
    if (currentUserText !== null && currentAssistantLines.length > 0) {
      turns.push({ user: currentUserText, assistantLines: currentAssistantLines });
    }
    if (turns.length === 0) {
      var fallbackUserText = "";
      var fallbackUserIdx = -1;
      for (var i = 0; i < contentLines.length; i++) {
        line = contentLines[i];
        if (line.indexOf('Try"') === 0 || line.indexOf('Try "') === 0) continue;
        if (line.indexOf("Failed to install") !== -1) continue;
        if (line.indexOf("ctrl+g") !== -1) continue;
        if (line.indexOf("\u25CF ") === 0) continue;
        if (line.length < 2 || line.length > 100) continue;
        if (/^[a-zA-Z]/.test(line)) {
          fallbackUserText = line.trim();
          fallbackUserIdx = i;
          break;
        }
      }
      if (fallbackUserText && fallbackUserIdx >= 0) {
        var fallbackAssistant = contentLines.slice(fallbackUserIdx + 1).filter(function(l) {
          return l.length >= 8;
        });
        if (fallbackAssistant.length > 0) {
          turns.push({ user: fallbackUserText, assistantLines: fallbackAssistant });
        }
      }
    }
    for (var t6 = 0; t6 < turns.length; t6++) {
      messages.push({ role: "user", content: turns[t6].user });
      if (turns[t6].assistantLines.length > 0) {
        var formattedContent = formatAssistantResponse(turns[t6].assistantLines.join(newline));
        messages.push({ role: "assistant", content: formattedContent });
      }
    }
    return messages;
  }
  var _AVATAR_T = "transparent";
  function buildPixelSvg(grid, size4) {
    var s = size4 || 3;
    var w = grid[0].length * s;
    var h = grid.length * s;
    var rects = "";
    for (var y = 0; y < grid.length; y++) {
      for (var x = 0; x < grid[y].length; x++) {
        if (grid[y][x] !== _AVATAR_T) {
          rects += '<rect x="' + x * s + '" y="' + y * s + '" width="' + s + '" height="' + s + '" fill="' + grid[y][x] + '"/>';
        }
      }
    }
    return '<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 ' + w + " " + h + '" class="pixel-avatar-svg">' + rects + "</svg>";
  }
  function buildCatGrid(palette) {
    var T = _AVATAR_T;
    var b = palette.base;
    var d = palette.dark;
    var l = palette.light || palette.base;
    var w = palette.accent || "#FFFFFF";
    var k = palette.eye || "#2D2D2D";
    var p = palette.mouth || "#F28B9A";
    var n = palette.nose || palette.dark;
    return [
      [T, d, T, T, T, T, T, T, d, T],
      [d, b, d, T, T, T, T, d, b, d],
      [d, b, b, b, b, b, b, b, b, d],
      [b, b, w, k, b, b, w, k, b, b],
      [b, b, w, w, b, b, w, w, b, b],
      [b, b, b, b, p, p, b, b, b, b],
      [b, n, b, l, b, b, l, b, n, b],
      [T, b, b, b, b, b, b, b, b, T],
      [T, T, b, d, b, b, d, b, T, T],
      [T, T, T, b, T, T, b, T, T, T]
    ];
  }
  var GARFIELD_PALETTE = {
    base: "#F0923A",
    dark: "#C46A1A",
    light: "#F0923A",
    accent: "#FFFFFF",
    eye: "#2D2D2D",
    mouth: "#F28B9A",
    nose: "#E87D5A"
  };
  var SHORTHAIR_PALETTE = {
    base: "#9EAAB8",
    dark: "#6B7B8D",
    light: "#C5CED8",
    accent: "#FFFFFF",
    eye: "#7EC88B",
    mouth: "#F28B9A"
  };
  var SUBAGENT_PALETTES = [
    { base: "#5A8FE0", dark: "#2E5BB3", light: "#9CC0F2", accent: "#FFFFFF", eye: "#FFD66E", mouth: "#F28B9A", primary: "#5A8FE0" },
    // 蓝猫
    { base: "#A06FE0", dark: "#6B45A8", light: "#C8A4F2", accent: "#FFFFFF", eye: "#FFE36E", mouth: "#F28B9A", primary: "#A06FE0" },
    // 紫猫
    { base: "#7BB76B", dark: "#4F8A40", light: "#A9D49C", accent: "#FFFFFF", eye: "#2D2D2D", mouth: "#F28B9A", primary: "#7BB76B" },
    // 抹茶猫
    { base: "#D86A88", dark: "#9C3A57", light: "#E8A4B5", accent: "#FFFFFF", eye: "#2D2D2D", mouth: "#FFFFFF", primary: "#D86A88" },
    // 樱花猫
    { base: "#5BB7B0", dark: "#2E7873", light: "#9CD6D2", accent: "#FFFFFF", eye: "#FFD66E", mouth: "#F28B9A", primary: "#5BB7B0" },
    // 青苔猫
    { base: "#4A4A60", dark: "#1F1F2E", light: "#6E6E84", accent: "#F5F5F5", eye: "#FFD66E", mouth: "#F28B9A", primary: "#4A4A60" },
    // 黑猫
    { base: "#D8A85A", dark: "#9C7028", light: "#EBC78A", accent: "#FFFFFF", eye: "#2D2D2D", mouth: "#F28B9A", primary: "#D8A85A" }
    // 焦糖猫
  ];
  function hashStringToIndex(str, mod) {
    var s = String(str || "");
    var h = 0;
    for (var i = 0; i < s.length; i++) h = (h << 5) - h + s.charCodeAt(i) | 0;
    return Math.abs(h) % mod;
  }
  var SUBAGENT_NAME_MAP = {
    "general-purpose": "\u4E07\u80FD\u732B",
    "Explore": "\u4FA6\u63A2\u732B",
    "code-explorer": "\u4FA6\u63A2\u732B",
    "code-reviewer": "\u5BA1\u67E5\u732B",
    "code-architect": "\u67B6\u6784\u732B",
    "code-simplifier": "\u7B80\u5316\u732B",
    "code-guide": "\u5411\u5BFC\u732B",
    "Plan": "\u7B56\u5212\u732B",
    "feature-dev": "\u5F00\u53D1\u732B",
    "pr-test-analyzer": "\u6D4B\u8BD5\u732B",
    "silent-failure-hunter": "\u62A4\u536B\u732B",
    "type-design-analyzer": "\u7C7B\u578B\u732B",
    "comment-analyzer": "\u6CE8\u91CA\u732B"
  };
  var _subagentSuffixMap = null;
  function getSubagentBaseName(sub) {
    if (!sub) return "";
    var agentType = sub.agentType || "";
    if (agentType && SUBAGENT_NAME_MAP[agentType]) return SUBAGENT_NAME_MAP[agentType];
    if (agentType) return agentType;
    return getActiveLang() === "English" ? "Subtask" : "\u5B50\u4EFB\u52A1";
  }
  function catPrefixedSubagentName(name) {
    var text5 = String(name || "").trim();
    if (!text5) return "\u732B\u732B\u5B50 Agent";
    return text5.indexOf("\u732B\u732B") === 0 ? text5 : "\u732B\u732B " + text5;
  }
  function getSubagentDisplayName(sub) {
    var base = getSubagentBaseName(sub);
    if (!base) return base;
    var suffix = _subagentSuffixMap && sub && sub.taskId ? _subagentSuffixMap.get(sub.taskId) : null;
    return catPrefixedSubagentName(suffix ? base + suffix : base);
  }
  function getSubagentPalette(sub) {
    var seed = sub && (sub.agentType || sub.taskId) || "subagent";
    return SUBAGENT_PALETTES[hashStringToIndex(seed, SUBAGENT_PALETTES.length)];
  }
  function renderSubagentReplyBubble(block, role) {
    if (!block || block.type !== "tool_result") return "";
    var text5 = extractToolResultText(block.content);
    var isError = block.is_error === true;
    var rawText = typeof text5 === "string" ? text5 : text5 == null ? "" : String(text5);
    if (!isError && !rawText.trim()) {
      return '<div class="subagent-reply pending"><span class="subagent-reply-marker pending">' + escapeHtml(t("subagent.running")) + '</span><span class="typing-indicator"><span></span><span></span><span></span></span></div>';
    }
    var displayText = rawText.trim() ? rawText : t("subagent.no_output");
    var bodyHtml = rawText.trim() ? renderMarkdown(displayText) : escapeHtml(displayText);
    var markerLabel = isError ? t("subagent.task.failed") : t("subagent.task.done");
    var markerSymbol = isError ? "\u2717" : "\u2713";
    return '<div class="subagent-reply final' + (isError ? " error" : "") + '"><div class="subagent-reply-marker ' + (isError ? "error" : "done") + '"><span class="subagent-reply-marker-icon" aria-hidden="true">' + markerSymbol + '</span><span class="subagent-reply-marker-label">' + escapeHtml(markerLabel) + '</span></div><div class="subagent-reply-content">' + bodyHtml + "</div></div>";
  }
  var PIXEL_AVATAR = {
    assistant: buildPixelSvg(buildCatGrid(GARFIELD_PALETTE)),
    user: buildPixelSvg(buildCatGrid(SHORTHAIR_PALETTE))
  };
  var DEFAULT_CHAT_PERSONA = {
    user: {
      name: "\u8D5B\u535A\u864E\u599E",
      avatarSvg: PIXEL_AVATAR.user
    },
    assistant: {
      name: "\u52E4\u52B3\u521D\u4E8C",
      avatarSvg: PIXEL_AVATAR.assistant
    }
  };
  function getStructuredChatPersona(role) {
    var configPersona = state.config && state.config.structuredChatPersona;
    var roleConfig = configPersona && configPersona[role] ? configPersona[role] : null;
    var defaults = DEFAULT_CHAT_PERSONA[role] || DEFAULT_CHAT_PERSONA.assistant;
    return {
      name: roleConfig && typeof roleConfig.name === "string" && roleConfig.name.trim() ? roleConfig.name.trim() : defaults.name,
      avatar: roleConfig && typeof roleConfig.avatar === "string" && roleConfig.avatar.trim() ? roleConfig.avatar.trim() : null,
      avatarSvg: defaults.avatarSvg
    };
  }
  function renderAvatarFallback(svg) {
    return '<div class="pixel-avatar">' + svg + "</div>";
  }
  function chatAvatar(role) {
    var personaRole = role === "user" ? "user" : "assistant";
    var persona = getStructuredChatPersona(personaRole);
    var avatarInner = persona.avatar ? '<img class="pixel-avatar-image" src="' + escapeHtml(persona.avatar) + '" alt="' + escapeHtml(persona.name) + '" onerror="handleChatAvatarImageError(this, ' + JSON.stringify(personaRole) + ')" />' : renderAvatarFallback(persona.avatarSvg);
    return '<div class="chat-message-avatar ' + role + '">' + avatarInner + '<span class="avatar-name">' + escapeHtml(persona.name) + "</span></div>";
  }
  function renderChatMessage(msg, roundUsage, messageIndex, legacyTaskMap) {
    if (msg.role === "thinking") {
      var ptyThinkingText = typeof msg.content === "string" ? msg.content : "";
      if (!ptyThinkingText.trim()) return "";
      var thinkingKey = buildExpandKey("thinking", [getMessageKey(msg, messageIndex), "pty"]);
      var thinkingPersisted = getPersistedExpandState(thinkingKey);
      var thinkingExpanded = thinkingPersisted === null ? getCardDefault("thinking") : thinkingPersisted;
      return '<div class="chat-message thinking"><div class="thinking-inline thinking-pty ' + (thinkingExpanded ? "expanded" : "collapsed") + '" data-expand-kind="thinking" data-expand-key="' + escapeHtml(thinkingKey) + '" data-thinking="' + escapeHtml(ptyThinkingText) + '" onclick="__thinkingToggle(this)"><span class="thinking-inline-icon">\u29BF</span><span class="thinking-inline-preview">' + escapeHtml(ptyThinkingText) + '</span><span class="thinking-inline-action">' + (thinkingExpanded ? "\u6536\u8D77" : "\u5C55\u5F00") + "</span></div></div>";
    }
    if (msg.role === "prompt") {
      return '<div class="chat-message prompt"><div class="prompt-card"><div class="prompt-icon">\u2192</div><div class="prompt-content">\u8BD5\u8BD5\uFF1A<span class="prompt-text">' + escapeHtml(msg.content) + "</span></div></div></div>";
    }
    if (Array.isArray(msg.content)) {
      return renderStructuredMessage(msg, roundUsage, messageIndex, legacyTaskMap);
    }
    var avatar = chatAvatar(msg.role);
    var bubbleContent = msg.role === "assistant" ? renderMarkdown(msg.content) : msg.role === "user" ? renderUserText(msg.content) : escapeHtml(msg.content);
    return '<div class="chat-message ' + msg.role + '">' + avatar + '<div class="chat-message-bubble">' + bubbleContent + "</div></div>";
  }
  function buildToolResultMap(contentBlocks) {
    var toolResults = {};
    if (!Array.isArray(contentBlocks)) return toolResults;
    for (var i = 0; i < contentBlocks.length; i++) {
      var block = contentBlocks[i];
      if (block && block.type === "tool_result") {
        var toolUseId = block.tool_use_id;
        if (!toolUseId) continue;
        if (!toolResults[toolUseId]) {
          toolResults[toolUseId] = [];
        }
        toolResults[toolUseId].push(block);
      }
    }
    return toolResults;
  }
  function pickToolResultForDisplay(toolResults, toolUseId) {
    var entries = toolResults && toolUseId ? toolResults[toolUseId] : null;
    if (!entries || !entries.length) return null;
    for (var i = 0; i < entries.length - 1; i++) {
      if (isRecoverableToolError(entries[i], entries[i + 1])) {
        return entries[i + 1];
      }
    }
    return entries[entries.length - 1];
  }
  function hasRecoveredToolNoise(toolResults, toolUseId) {
    var entries = toolResults && toolUseId ? toolResults[toolUseId] : null;
    if (!entries || entries.length < 2) return false;
    for (var i = 0; i < entries.length - 1; i++) {
      if (isRecoverableToolError(entries[i], entries[i + 1])) {
        return true;
      }
    }
    return false;
  }
  function renderRecoveredToolHint(toolName) {
    return '<div class="structured-tool-hint">\u5DF2\u81EA\u52A8\u6062\u590D\u4E00\u6B21 ' + escapeHtml(getToolDisplayName(toolName)) + " \u53C2\u6570\u95EE\u9898</div>";
  }
  var GROUPABLE_TOOLS = { Read: 1, Glob: 1, Grep: 1, WebFetch: 1, WebSearch: 1, TodoRead: 1 };
  function isGroupableToolBlock(block) {
    if (!block || block.type !== "tool_use" || !GROUPABLE_TOOLS[block.name]) return false;
    if (block.name === "Read") {
      var input = block.input || {};
      if (isImagePath(input.file_path || input.path || "")) return false;
    }
    return true;
  }
  function groupConsecutiveTools(content) {
    var groups = [];
    var i = 0;
    while (i < content.length) {
      var block = content[i];
      if (block.type === "tool_result") {
        i++;
        continue;
      }
      if (isGroupableToolBlock(block)) {
        var run2 = [{ block, index: i }];
        var j = i + 1;
        while (j < content.length) {
          if (content[j].type === "tool_result") {
            j++;
            continue;
          }
          if (isGroupableToolBlock(content[j])) {
            run2.push({ block: content[j], index: j });
            j++;
          } else {
            break;
          }
        }
        if (run2.length >= 2) {
          groups.push({ type: "group", items: run2, endIndex: j });
        } else {
          groups.push({ type: "single", block, index: i });
        }
        i = j;
      } else {
        groups.push({ type: "single", block, index: i });
        i++;
      }
    }
    return groups;
  }
  var TOOL_GROUP_LABELS = { Read: "\u8BFB\u53D6", Glob: "\u641C\u7D22", Grep: "\u641C\u7D22", WebFetch: "\u6293\u53D6", WebSearch: "\u641C\u7D22", TodoRead: "\u5F85\u529E" };
  function renderToolGroup(items, role, toolResults, messageKey, options) {
    var opts = options || {};
    var counts = {};
    for (var k = 0; k < items.length; k++) {
      var n = items[k].block.name;
      counts[n] = (counts[n] || 0) + 1;
    }
    var allDone = true;
    var anyError = false;
    for (var k = 0; k < items.length; k++) {
      var b = items[k].block;
      var tr = pickToolResultForDisplay(toolResults, b.id);
      if (!tr) {
        allDone = false;
      } else if (tr.is_error) {
        anyError = true;
      }
    }
    var statusIcon = !allDone ? "\u2026" : anyError ? "\u2717" : "\u2713";
    var statusClass = !allDone ? "pending" : anyError ? "error" : "done";
    var parts = [];
    for (var name in counts) {
      parts.push(counts[name] + " " + (TOOL_GROUP_LABELS[name] || name));
    }
    var summaryText = parts.join(" \xB7 ");
    var groupKey = buildExpandKey("tool-group", [messageKey, items[0] && items[0].index, items.length]);
    var persistedExpanded = getPersistedExpandState(groupKey);
    var shouldExpand = opts.forceExpandedToolBodies ? true : persistedExpanded === null ? getCardDefault("toolGroup") : persistedExpanded;
    var innerHtml = "";
    for (var k = 0; k < items.length; k++) {
      try {
        innerHtml += renderContentBlock(items[k].block, role, toolResults, items[k].index, messageKey, opts);
      } catch (e) {
        innerHtml += '<div class="render-error">\u5DE5\u5177\u6E32\u67D3\u5931\u8D25</div>';
      }
    }
    return '<div class="tool-group" data-expand-kind="tool-group" data-expand-key="' + escapeHtml(groupKey) + '" data-expanded="' + (shouldExpand ? "true" : "false") + '" data-status="' + statusClass + '"><div class="tool-group-summary" onclick="__toolGroupToggle(this.parentNode)"><span class="tool-group-status">' + statusIcon + '</span><span class="tool-group-text">' + escapeHtml(summaryText) + '</span><span class="tool-group-count">' + items.length + ' \u4E2A\u8C03\u7528</span><svg class="tool-group-chevron" width="12" height="12" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="transform:' + (shouldExpand ? "rotate(180deg)" : "") + '"><polyline points="6 9 12 15 18 9"/></svg></div><div class="tool-group-body" style="display:' + (shouldExpand ? "block" : "none") + ';">' + innerHtml + "</div></div>";
  }
  window.__toolGroupToggle = function(el) {
    if (!el) return;
    var expanded = el.getAttribute("data-expanded") === "true";
    el.setAttribute("data-expanded", expanded ? "false" : "true");
    var body = el.querySelector(".tool-group-body");
    if (body) body.style.display = expanded ? "none" : "block";
    var chevron = el.querySelector(".tool-group-chevron");
    if (chevron) chevron.style.transform = expanded ? "" : "rotate(180deg)";
    persistElementExpandState(el, "tool-group");
  };
  function applyHistoryHiddenState(summaryEl, expanded) {
    var node = summaryEl.nextElementSibling;
    while (node) {
      if (!node.classList.contains("chat-load-more")) {
        if (expanded) node.classList.remove("chat-history-hidden");
        else node.classList.add("chat-history-hidden");
      }
      node = node.nextElementSibling;
    }
  }
  function applyAutoFoldBar(chatOutput, chatMessages, allMessages, renderIsInitial) {
    void allMessages;
    void renderIsInitial;
    if (!chatOutput || !chatMessages) return;
    setAutoFoldMode(chatOutput, chatMessages, false);
    clearAutoFoldHistoryHidden(chatMessages);
    clearAutoFoldBar(chatOutput);
  }
  function setAutoFoldMode(chatOutput, chatMessages, enabled) {
    if (!chatOutput) return;
    chatOutput.classList.toggle("auto-fold", !!enabled);
  }
  function clearAutoFoldHistoryHidden(chatMessages) {
    if (!chatMessages) return;
    var hidden = chatMessages.querySelectorAll(".chat-auto-fold-hidden");
    for (var i = 0; i < hidden.length; i++) hidden[i].classList.remove("chat-auto-fold-hidden");
  }
  function followAutoFoldLatest(chatMessages) {
    if (!chatMessages || !chatMessages.isConnected) return;
    clearChatUnread({ removeDivider: true });
    scrollChatToBottom(true);
  }
  function clearAutoFoldBar(chatOutput) {
    if (!chatOutput) return;
    var bar = chatOutput.querySelector("#chat-fold-bar");
    if (!bar) return;
    bar.innerHTML = "";
    bar.classList.add("hidden");
    state.chatAutoFoldSnapshot = null;
  }
  function getMessagePreviewText(msg) {
    if (!msg) return "";
    var parts = [];
    function pushText(value) {
      if (typeof value !== "string") return;
      var cleaned = value.replace(/\s+/g, " ").trim();
      if (cleaned) parts.push(cleaned);
    }
    if (typeof msg.content === "string") {
      pushText(msg.content);
    } else if (Array.isArray(msg.content)) {
      for (var i = 0; i < msg.content.length && parts.join(" ").length < 180; i++) {
        var block = msg.content[i];
        if (!block) continue;
        if (block.type === "text") pushText(block.text);
        else if (block.type === "thinking") pushText(block.thinking);
        else if (block.type === "tool_use") pushText(block.name ? "\u8C03\u7528 " + block.name : "\u5DE5\u5177\u8C03\u7528");
        else if (block.type === "tool_result") pushText(block.is_error ? "\u5DE5\u5177\u8FD4\u56DE\u9519\u8BEF" : "\u5DE5\u5177\u8FD4\u56DE\u7ED3\u679C");
        else if (block.text) pushText(block.text);
      }
    }
    var text5 = parts.join(" \xB7 ");
    return text5.length > 180 ? text5.slice(0, 177) + "..." : text5;
  }
  window.__chatFoldJumpToLatest = function() {
    var chatMessages = document.querySelector("#chat-output .chat-messages");
    if (chatMessages) followAutoFoldLatest(chatMessages);
  };
  window.__chatFoldToggleHistory = function() {
    var summary = document.querySelector("#chat-output .chat-history-summary");
    var btn = summary ? summary.querySelector(".chat-history-summary-btn") : null;
    if (summary) summary.classList.remove("chat-auto-fold-hidden");
    if (btn && window.__historySummaryToggle) {
      window.__historySummaryToggle(btn);
    }
  };
  function applyHistoryCollapse(chatMessages, selectedSession) {
    if (!chatMessages) return;
    var allMessages = state.currentMessages || [];
    var lastUserIdx = -1;
    for (var i = allMessages.length - 1; i >= 0; i--) {
      if (allMessages[i] && allMessages[i].role === "user") {
        lastUserIdx = i;
        break;
      }
    }
    function clearAll() {
      var prev = chatMessages.querySelector(".chat-history-summary");
      if (prev) prev.remove();
      var hidden = chatMessages.querySelectorAll(".chat-history-hidden");
      for (var h = 0; h < hidden.length; h++) hidden[h].classList.remove("chat-history-hidden");
      chatMessages.removeAttribute("data-history-sig");
    }
    clearAll();
    var msgEls = chatMessages.querySelectorAll(".chat-message.assistant[data-msg-index]");
    for (var m = 0; m < msgEls.length; m++) {
      var el = msgEls[m];
      var idx = parseInt(el.getAttribute("data-msg-index") || "", 10);
      if (isNaN(idx) || !allMessages[idx] || allMessages[idx].role !== "assistant") continue;
      var historical = idx < lastUserIdx;
      var key = buildExpandKey(historical ? "assistant-reply-history" : "assistant-reply-current", [getMessageKey(allMessages[idx], idx)]);
      var persisted = getPersistedExpandState(key);
      var expanded = persisted === null ? !historical : persisted;
      var disclosure = el.querySelector(":scope > .assistant-reply-disclosure");
      if (!disclosure) {
        disclosure = document.createElement("button");
        disclosure.className = "assistant-reply-disclosure";
        disclosure.setAttribute("type", "button");
        el.insertBefore(disclosure, el.firstChild);
      }
      disclosure.setAttribute("data-expand-key", key);
      disclosure.setAttribute("aria-expanded", expanded ? "true" : "false");
      disclosure.innerHTML = '<span class="assistant-reply-label">\u56DE\u590D</span><span class="assistant-reply-preview">' + escapeHtml(getMessagePreviewText(allMessages[idx]) || "\u52A9\u624B\u56DE\u590D") + '</span><span class="assistant-reply-action">' + (expanded ? "\u6536\u8D77" : "\u5C55\u5F00") + '</span><span class="assistant-reply-chevron">' + iconSvg("chevronDown", { size: 15 }) + "</span>";
      el.classList.toggle("assistant-reply-collapsed", !expanded);
      el.classList.toggle("assistant-reply-expanded", expanded);
      disclosure.onclick = function() {
        var parent = this.parentElement;
        var nextExpanded = parent.classList.contains("assistant-reply-collapsed");
        parent.classList.toggle("assistant-reply-collapsed", !nextExpanded);
        parent.classList.toggle("assistant-reply-expanded", nextExpanded);
        this.setAttribute("aria-expanded", nextExpanded ? "true" : "false");
        var action = this.querySelector(".assistant-reply-action");
        if (action) action.textContent = nextExpanded ? "\u6536\u8D77" : "\u5C55\u5F00";
        setPersistedExpandState(this.getAttribute("data-expand-key"), nextExpanded);
      };
    }
  }
  window.__historySummaryToggle = function(btn) {
    var wrap = btn && btn.closest ? btn.closest(".chat-history-summary") : null;
    if (!wrap) return;
    var key = wrap.getAttribute("data-expand-key");
    var nowExpanded = wrap.getAttribute("data-expanded") !== "true";
    wrap.setAttribute("data-expanded", nowExpanded ? "true" : "false");
    btn.setAttribute("aria-expanded", nowExpanded ? "true" : "false");
    var title = wrap.querySelector(".chat-history-summary-title");
    if (title) title.textContent = nowExpanded ? t("history.collapse") : t("history.expand");
    if (key) setPersistedExpandState(key, nowExpanded);
    applyHistoryHiddenState(wrap, nowExpanded);
    var container = wrap.parentElement;
    if (nowExpanded) {
      clearAutoFoldHistoryHidden(container);
    }
    if (container) {
      var sig = container.getAttribute("data-history-sig");
      if (sig) {
        var segs = sig.split(":");
        if (segs.length >= 3) {
          segs[2] = nowExpanded ? "1" : "0";
          container.setAttribute("data-history-sig", segs.join(":"));
        }
      }
    }
  };
  function deriveLegacySubagent(block) {
    if (!block || block.type !== "tool_use") return null;
    var input = block.input || {};
    var agentType = typeof input.subagent_type === "string" ? input.subagent_type : null;
    if (!agentType && block.name !== "Task" && block.name !== "Agent") return null;
    return {
      taskId: block.id,
      agentType: agentType || void 0,
      taskDescription: typeof input.description === "string" ? input.description : void 0
    };
  }
  function collectLegacyTaskIdMap(allMessages) {
    var map = /* @__PURE__ */ new Map();
    if (!Array.isArray(allMessages)) return map;
    for (var i = 0; i < allMessages.length; i++) {
      var m = allMessages[i];
      if (!m || m.role !== "assistant" || !Array.isArray(m.content)) continue;
      for (var j = 0; j < m.content.length; j++) {
        var b = m.content[j];
        if (!b || b.type !== "tool_use") continue;
        var derived = b.__subagent || deriveLegacySubagent(b);
        if (derived) map.set(b.id, derived);
      }
    }
    return map;
  }
  function collectSubagentSuffixMap(allMessages) {
    var suffix = /* @__PURE__ */ new Map();
    if (!Array.isArray(allMessages)) return suffix;
    var bucketsByName = /* @__PURE__ */ new Map();
    var seenTaskIds = /* @__PURE__ */ new Set();
    function record2(sub2) {
      if (!sub2 || !sub2.taskId) return;
      if (seenTaskIds.has(sub2.taskId)) return;
      seenTaskIds.add(sub2.taskId);
      var name = getSubagentBaseName(sub2);
      if (!name) return;
      if (!bucketsByName.has(name)) bucketsByName.set(name, []);
      bucketsByName.get(name).push(sub2.taskId);
    }
    for (var i = 0; i < allMessages.length; i++) {
      var m = allMessages[i];
      if (!m || !Array.isArray(m.content)) continue;
      for (var j = 0; j < m.content.length; j++) {
        var b = m.content[j];
        if (!b) continue;
        var sub = b.__subagent || (b.type === "tool_use" ? deriveLegacySubagent(b) : null);
        if (sub) record2(sub);
      }
    }
    bucketsByName.forEach(function(taskIds) {
      if (taskIds.length < 2) return;
      for (var k = 0; k < taskIds.length; k++) {
        suffix.set(taskIds[k], " #" + (k + 1));
      }
    });
    return suffix;
  }
  function splitTurnBySubagent(blocks, legacyTaskMap) {
    var segs = [];
    if (!Array.isArray(blocks) || !blocks.length) return segs;
    var current = null;
    for (var i = 0; i < blocks.length; i++) {
      var b = blocks[i];
      var isPlaceholder = b && b.type === "text" && b.__processing === true;
      if (isPlaceholder && current) {
        current.blocks.push(b);
        continue;
      }
      var sub = b && b.__subagent ? b.__subagent : null;
      if (!sub) sub = deriveLegacySubagent(b);
      if (!sub && b && b.type === "tool_result" && legacyTaskMap && legacyTaskMap.has(b.tool_use_id)) {
        sub = legacyTaskMap.get(b.tool_use_id);
      }
      var key = sub ? sub.taskId : null;
      if (!current || current.key !== key) {
        current = { key, subagent: sub, blocks: [], firstIndex: i };
        segs.push(current);
      }
      current.blocks.push(b);
    }
    return segs;
  }
  function buildSegmentBlocksHtml(segmentBlocks, segmentFirstIndex, role, toolResults, messageKey, options) {
    var html = "";
    var opts = options || {};
    try {
      var groups = groupConsecutiveTools(segmentBlocks);
      for (var g = 0; g < groups.length; g++) {
        var grp = groups[g];
        try {
          if (grp.type === "group") {
            var shifted = [];
            for (var k = 0; k < grp.items.length; k++) {
              shifted.push({ block: grp.items[k].block, index: grp.items[k].index + segmentFirstIndex });
            }
            html += renderToolGroup(shifted, role, toolResults, messageKey, opts);
          } else {
            html += renderContentBlock(grp.block, role, toolResults, grp.index + segmentFirstIndex, messageKey, opts);
          }
        } catch (e) {
          html += '<div class="render-error">\u6D88\u606F\u5757\u6E32\u67D3\u5931\u8D25</div>';
        }
      }
    } catch (e) {
      html += '<div class="render-error">\u6D88\u606F\u6E32\u67D3\u5931\u8D25</div>';
    }
    return html;
  }
  function buildMultiAgentHtml(segments, role, parentPersonaName, toolResults, messageKey, options) {
    var opts = options || {};
    var showHandoff = opts.showHandoff !== false;
    var html = "";
    var lastSubId = null;
    for (var si = 0; si < segments.length; si++) {
      var seg = segments[si];
      var segmentOptions = seg.subagent ? { inSubagentPanel: true } : {};
      var segHtml = buildSegmentBlocksHtml(seg.blocks, seg.firstIndex, role, toolResults, messageKey, segmentOptions);
      if (!segHtml || !segHtml.trim()) continue;
      if (seg.subagent) {
        var includeHandoff = showHandoff && lastSubId !== seg.subagent.taskId;
        html += buildSubagentPanelHtml(seg, parentPersonaName, segHtml, messageKey, includeHandoff);
        lastSubId = seg.subagent.taskId;
      } else {
        html += '<div class="chat-message-segment parent">' + chatAvatar(role) + '<div class="chat-message-content">' + segHtml + "</div></div>";
        lastSubId = null;
      }
    }
    return html;
  }
  function buildSubagentPanelHtml(seg, parentPersonaName, segHtml, messageKey, includeHandoff) {
    var sub = seg.subagent;
    var subPalette = getSubagentPalette(sub);
    var subName = getSubagentDisplayName(sub);
    var taskId = sub.taskId || "";
    var avatarSvg = buildPixelSvg(buildCatGrid(subPalette));
    var itemCount = countRenderableSegmentBlocks(seg.blocks);
    var titleHtml;
    if (includeHandoff) {
      var hasDesc = !!(sub.taskDescription && String(sub.taskDescription).trim());
      var descSpan = hasDesc ? '<span class="subagent-panel-task-desc">' + escapeHtml(sub.taskDescription) + "</span>" : '<span class="subagent-panel-task-desc">' + escapeHtml(t("subagent.continued")) + "</span>";
      titleHtml = '<span class="subagent-panel-attribution"><strong class="subagent-panel-name">' + escapeHtml(subName) + '</strong><span class="subagent-panel-tag" title="' + escapeHtml(t("subagent.tag_title")) + '">' + escapeHtml(t("subagent.tag")) + "</span>" + descSpan + "</span>";
    } else {
      titleHtml = '<span class="subagent-panel-attribution"><strong class="subagent-panel-name">' + escapeHtml(subName) + '</strong><span class="subagent-panel-task-desc"> ' + escapeHtml(t("subagent.continued")) + "</span></span>";
    }
    var expandKey = buildExpandKey("subagent-panel", [messageKey, taskId]);
    return '<div class="subagent-panel" data-expand-kind="subagent-panel" data-expand-key="' + escapeHtml(expandKey) + '" data-agent-id="' + escapeHtml(taskId) + '" data-expanded="true" style="--agent-color:' + subPalette.primary + '"><div class="subagent-panel-header" aria-label="' + escapeHtml(t("subagent.title_aria")) + '"><span class="subagent-panel-avatar" aria-hidden="true">' + avatarSvg + "</span>" + titleHtml + '<span class="subagent-panel-count">' + escapeHtml(itemCount + " \u6761\u5185\u5BB9") + '</span></div><div class="subagent-panel-body">' + segHtml + "</div></div>";
  }
  function countRenderableSegmentBlocks(blocks) {
    if (!Array.isArray(blocks)) return 0;
    var count3 = 0;
    for (var i = 0; i < blocks.length; i++) {
      var block = blocks[i];
      if (!block || !block.type) continue;
      if (block.type === "tool_result") continue;
      if (block.type === "text" && !String(block.text || "").trim() && !block.__processing) continue;
      if (block.type === "thinking" && !String(block.thinking || "").trim()) continue;
      count3++;
    }
    return Math.max(1, count3);
  }
  function renderStructuredMessage(msg, roundUsage, messageIndex, legacyTaskMap) {
    var role = msg.role;
    var messageKey = getMessageKey(msg, messageIndex);
    var usageHtml = role === "assistant" ? renderUsageSummaryHtml(roundUsage) : "";
    var isQueued = role === "user" && msg.content && msg.content.some(function(b) {
      return b.__queued;
    });
    if (!msg.content || msg.content.length === 0) {
      if (role === "assistant") {
        return '<div class="chat-message ' + role + '">' + chatAvatar(role) + '<div class="chat-message-content"><div class="typing-indicator"><span></span><span></span><span></span></div>' + usageHtml + "</div></div>";
      }
      return '<div class="chat-message ' + role + ' empty-message" data-message-key="' + escapeHtml(messageKey) + '">' + chatAvatar(role) + '<div class="chat-message-content"><span class="empty-message-hint">\uFF08\u7A7A\u6D88\u606F\uFF09</span></div></div>';
    }
    var toolResults = buildToolResultMap(msg.content);
    var parentPersona = getStructuredChatPersona("assistant");
    if (role !== "assistant") {
      var userSegments = splitTurnBySubagent(msg.content, legacyTaskMap);
      var userHasSub = userSegments.some(function(s) {
        return s.subagent;
      });
      var queuedClass = isQueued ? " queued" : "";
      var queuedBadge = isQueued ? '<span class="queued-badge">\u6392\u961F\u4E2D</span>' : "";
      if (userHasSub) {
        var userMultiHtml = buildMultiAgentHtml(userSegments, role, parentPersona.name, toolResults, messageKey, { showHandoff: false });
        return '<div class="chat-message ' + role + queuedClass + ' multi-agent" data-message-key="' + escapeHtml(messageKey) + '">' + userMultiHtml + queuedBadge + "</div>";
      }
      var userHtml = buildSegmentBlocksHtml(msg.content, 0, role, toolResults, messageKey);
      return '<div class="chat-message ' + role + queuedClass + '" data-message-key="' + escapeHtml(messageKey) + '">' + chatAvatar(role) + '<div class="chat-message-content">' + userHtml + queuedBadge + "</div></div>";
    }
    var segments = splitTurnBySubagent(msg.content, legacyTaskMap);
    var hasSubagent = segments.some(function(s) {
      return s.subagent;
    });
    if (!hasSubagent) {
      var html = buildSegmentBlocksHtml(msg.content, 0, role, toolResults, messageKey);
      return '<div class="chat-message ' + role + '" data-message-key="' + escapeHtml(messageKey) + '">' + chatAvatar(role) + '<div class="chat-message-content">' + html + usageHtml + "</div></div>";
    }
    var multiHtml = '<div class="chat-message ' + role + ' multi-agent" data-message-key="' + escapeHtml(messageKey) + '">';
    multiHtml += buildMultiAgentHtml(segments, role, parentPersona.name, toolResults, messageKey, { showHandoff: true });
    multiHtml += usageHtml;
    multiHtml += "</div>";
    return multiHtml;
  }
  function compactUsageNumber(value) {
    if (value >= 1e6) return (value / 1e6).toFixed(1).replace(/\.0$/, "") + "M";
    if (value >= 1e3) return (value / 1e3).toFixed(1).replace(/\.0$/, "") + "k";
    return String(Math.max(0, Math.round(value || 0)));
  }
  function renderUsageSummaryHtml(usage) {
    if (!usage) return "";
    var estimated = usage.estimated === true;
    var parts = [];
    if ((usage.inputTokens || 0) > 0) parts.push("\u8F93\u5165 " + compactUsageNumber(usage.inputTokens));
    if ((usage.cacheReadInputTokens || 0) > 0) parts.push("\u7F13\u5B58\u547D\u4E2D " + compactUsageNumber(usage.cacheReadInputTokens));
    if ((usage.cacheCreationInputTokens || 0) > 0) parts.push("\u7F13\u5B58\u5199\u5165 " + compactUsageNumber(usage.cacheCreationInputTokens));
    if ((usage.outputTokens || 0) > 0) parts.push("\u8F93\u51FA " + (estimated ? "\u2248" : "") + compactUsageNumber(usage.outputTokens));
    if ((usage.reasoningOutputTokens || 0) > 0) parts.push("\u63A8\u7406 " + (estimated ? "\u2248" : "") + compactUsageNumber(usage.reasoningOutputTokens));
    if ((usage.totalCostUsd || 0) > 0) parts.push("$" + Number(usage.totalCostUsd).toFixed(4).replace(/0+$/, "").replace(/\.$/, ""));
    if (parts.length === 0 && estimated) parts.push("\u6B63\u5728\u7EDF\u8BA1\u7528\u91CF\u2026");
    if (parts.length === 0) return "";
    return '<div class="turn-usage-summary' + (estimated ? " is-estimated" : "") + '" role="status" aria-live="polite" aria-label="\u672C\u8F6E\u7528\u91CF ' + escapeHtml(parts.join("\uFF0C")) + '"><svg class="turn-usage-icon" viewBox="0 0 16 16" aria-hidden="true"><path d="M2.5 13.5h11M4 11V7.5M8 11V3M12 11V5.5"/></svg><span>' + escapeHtml(parts.join(" \xB7 ")) + "</span></div>";
  }
  var ATTACHMENT_PREFIX_RE = /^\s*\[附件已上传，请查看以下文件:\n([\s\S]*?)\]\n+/;
  function renderUserAttachmentBlock(rawPath) {
    var p = (rawPath || "").trim();
    if (!p) return "";
    var name = p.split("/").pop() || p;
    if (isImagePath(p)) {
      var src = "/api/file-raw?path=" + encodeURIComponent(p);
      return '<div class="user-attachment-image"><img class="user-attachment-thumb" loading="lazy" src="' + src + '" alt="' + escapeHtml(name) + '" data-path="' + escapeHtml(p) + `" onclick="event.stopPropagation(); if(window.__openFilePreview)window.__openFilePreview(this.getAttribute('data-path'));" onerror="var w=this.closest('.user-attachment-image'); if(w)w.style.display='none';" /></div>`;
    }
    return '<div class="user-attachment-file" data-path="' + escapeHtml(p) + `" onclick="event.stopPropagation(); if(window.__openFilePreview)window.__openFilePreview(this.getAttribute('data-path'));"><span class="user-attachment-file-icon"><svg viewBox="0 0 16 16" width="13" height="13" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M9 1.5H4a1 1 0 0 0-1 1v11a1 1 0 0 0 1 1h8a1 1 0 0 0 1-1V5.5L9 1.5z"/><path d="M9 1.5V5.5h4"/></svg></span><span class="user-attachment-file-name">` + escapeHtml(name) + "</span></div>";
  }
  function renderUserText(text5) {
    var raw = text5 || "";
    var m = raw.match(ATTACHMENT_PREFIX_RE);
    if (!m) return escapeHtml(raw);
    var attachHtml = "";
    var lines = m[1].split("\n");
    for (var i = 0; i < lines.length; i++) {
      attachHtml += renderUserAttachmentBlock(lines[i]);
    }
    var rest = raw.slice(m[0].length);
    var wrap = attachHtml ? '<div class="user-attachments">' + attachHtml + "</div>" : "";
    var body = rest.trim() ? '<div class="user-attachment-text">' + escapeHtml(rest) + "</div>" : "";
    return wrap + body;
  }
  function renderContentBlock(block, role, toolResults, index2, messageKey, options) {
    var opts = options || {};
    if (!block || !block.type) return "";
    if (!opts.inSubagentPanel && block.type === "tool_use" && block.__subagent && block.__subagent.taskId === block.id) {
      return "";
    }
    if (block.type === "tool_result" && block.__subagent && block.__subagent.taskId === block.tool_use_id) {
      return renderSubagentReplyBubble(block, role);
    }
    switch (block.type) {
      case "text":
        if (role === "assistant" && block.__processing) {
          return '<div class="typing-indicator"><span></span><span></span><span></span></div>';
        }
        return role === "assistant" ? renderMarkdown(block.text || "") : renderUserText(block.text || "");
      case "thinking":
        var thinkingText = block.thinking || "";
        var isStreaming = block.thinking === void 0 && block.type === "thinking";
        if (isStreaming) {
          return '<div class="thinking-inline thinking-streaming" data-thinking=""><div class="thinking-streaming-inner"><span class="thinking-streaming-icon spinning">\u29BF</span><div class="thinking-streaming-text"></div></div></div>';
        }
        if (!thinkingText.trim()) return "";
        var preview = thinkingText.length > 60 ? thinkingText.slice(0, 57) + "\u2026" : thinkingText;
        var thinkingKey = buildExpandKey("thinking", [messageKey, index2]);
        var thinkingPersisted = getPersistedExpandState(thinkingKey);
        var thinkingExpanded = thinkingPersisted === null ? getCardDefault("thinking") : thinkingPersisted;
        return '<div class="thinking-inline ' + (thinkingExpanded ? "expanded" : "collapsed") + '" data-expand-kind="thinking" data-expand-key="' + escapeHtml(thinkingKey) + '" data-thinking="' + escapeHtml(thinkingText) + '" onclick="__thinkingToggle(this)"><span class="thinking-inline-icon">\u29BF</span><span class="thinking-inline-preview">' + escapeHtml(thinkingExpanded ? thinkingText : preview) + '</span><span class="thinking-inline-action">' + (thinkingExpanded ? "\u6536\u8D77" : "\u5C55\u5F00") + "</span></div>";
      case "tool_use":
        var toolResult = pickToolResultForDisplay(toolResults, block.id);
        var rendered = renderToolUseCard(block, toolResult, index2, messageKey, opts);
        if (hasRecoveredToolNoise(toolResults, block.id)) {
          rendered = renderRecoveredToolHint(block.name || "\u5DE5\u5177") + rendered;
        }
        return rendered;
      case "tool_result":
        return "";
      default:
        var unknownType = block && block.type ? String(block.type) : "\u672A\u77E5";
        var unknownJson = "";
        try {
          unknownJson = JSON.stringify(block, null, 2);
        } catch (_e) {
          unknownJson = "{}";
        }
        return `<div class="unknown-block collapsed" onclick="this.classList.toggle('collapsed')"><div class="unknown-block-header"><span class="unknown-block-icon">?</span><span class="unknown-block-label">\u672A\u8BC6\u522B\u7684\u5185\u5BB9\u5757\uFF1A` + escapeHtml(unknownType) + '</span><span class="unknown-block-toggle">\u25BC</span></div><pre class="unknown-block-body">' + escapeHtml(unknownJson) + "</pre></div>";
    }
  }
  function renderInlineTool(block, toolResult, toolName, fileInfo, extraInfo, messageKey, index2, options) {
    var opts = options || {};
    var toolId = block.id || "tool-" + toolName;
    var expandKey = buildExpandKey("inline-tool", [messageKey, toolId || index2, index2]);
    var persistedExpanded = getPersistedExpandState(expandKey);
    var inputData = block.input || {};
    var resultContent = extractToolResultText(toolResult && toolResult.content);
    var isError = toolResult && toolResult.is_error;
    var hasResult = resultContent.length > 0;
    var statusIcon = isError ? "\u2717" : hasResult ? "\u2713" : "\u2026";
    var icon = "";
    var title = "";
    var meta = "";
    var preview = "";
    if (toolName === "Read") {
      icon = '<svg class="inline-tool-icon" viewBox="0 0 16 16" width="14" height="14" fill="none" stroke="currentColor" stroke-width="1.5"><path d="M1 3.5A1.5 1.5 0 0 1 2.5 2h2.764c.958 0 1.76.56 2.311 1.184C8.405 3.77 9.146 4 10 4h3.5A1.5 1.5 0 0 1 15 5.5v7a1.5 1.5 0 0 1-1.5 1.5h-11A1.5 1.5 0 0 1 1 12.5v-9z"/><path d="M2 5.5h12M2 8h8M2 10.5h5"/></svg>';
      var path = inputData.file_path || inputData.path || fileInfo || "";
      var lineCount = "";
      if (inputData.limit) {
        lineCount = " " + inputData.offset + "-" + (inputData.offset + inputData.limit);
      }
      title = path;
      meta = lineCount;
    } else if (toolName === "Glob") {
      icon = '<svg class="inline-tool-icon" viewBox="0 0 16 16" width="14" height="14" fill="none" stroke="currentColor" stroke-width="1.5"><circle cx="7" cy="7" r="5"/><path d="M10.5 10.5L14 14"/></svg>';
      var pattern = inputData.pattern || "";
      var gPath = inputData.path || fileInfo || "";
      title = pattern;
      meta = gPath;
    } else if (toolName === "Grep") {
      icon = '<svg class="inline-tool-icon" viewBox="0 0 16 16" width="14" height="14" fill="none" stroke="currentColor" stroke-width="1.5"><circle cx="6.5" cy="6.5" r="4.5"/><path d="M11.5 11.5L15 15"/></svg>';
      var pattern = inputData.pattern || "";
      var gPath = inputData.path || fileInfo || "";
      title = pattern;
      meta = gPath;
      if (inputData.context) meta += " -C" + inputData.context;
    } else if (toolName === "WebFetch") {
      icon = '<svg class="inline-tool-icon" viewBox="0 0 16 16" width="14" height="14" fill="none" stroke="currentColor" stroke-width="1.5"><circle cx="8" cy="8" r="6.5"/><path d="M8 1.5v13M1.5 8h13"/></svg>';
      var url = inputData.url || "";
      title = url;
      meta = extraInfo || "";
    } else if (toolName === "WebSearch") {
      icon = '<svg class="inline-tool-icon" viewBox="0 0 16 16" width="14" height="14" fill="none" stroke="currentColor" stroke-width="1.5"><circle cx="7" cy="7" r="5"/><path d="M10.5 10.5L14 14"/><path d="M5 7h4M7 5v4"/></svg>';
      var query = inputData.query || "";
      title = query;
      meta = extraInfo || "";
    } else if (toolName === "TodoRead") {
      icon = '<svg class="inline-tool-icon" viewBox="0 0 16 16" width="14" height="14" fill="none" stroke="currentColor" stroke-width="1.5"><rect x="2" y="2" width="12" height="12" rx="2"/><path d="M5 8l2 2 4-4"/></svg>';
      title = "\u8BFB\u53D6\u5F85\u529E\u5217\u8868";
      meta = extraInfo || "";
    } else {
      icon = '<svg class="inline-tool-icon" viewBox="0 0 16 16" width="14" height="14" fill="none" stroke="currentColor" stroke-width="1.5"><circle cx="8" cy="8" r="6.5"/><path d="M8 5v3.5M8 11h.01"/></svg>';
      title = getToolDisplayName(toolName);
      meta = extraInfo || "";
    }
    if (hasResult) {
      var lines = resultContent.split("\n");
      if (lines.length > 10) {
        preview = lines.slice(0, 10).join("\n") + "\n\u2026";
      } else {
        preview = resultContent;
      }
    }
    var resultDataAttr = escapeHtml(resultContent);
    var previewDataAttr = escapeHtml(preview);
    var fullResult = resultContent;
    var expandedHtml = "";
    var shouldExpand = opts.forceExpandedToolBodies ? true : persistedExpanded === null ? getCardDefault("inlineTools") : persistedExpanded;
    if (hasResult) {
      expandedHtml = '<div class="inline-tool-expanded" style="display: ' + (shouldExpand ? "block" : "none") + ';"><div class="inline-tool-result">' + formatInlineResult(resultContent, toolName) + "</div></div>";
    } else if (isError) {
      expandedHtml = '<div class="inline-tool-expanded" style="display: ' + (shouldExpand ? "block" : "none") + ';"><div class="inline-tool-result inline-tool-error">' + escapeHtml(resultContent || "\u64CD\u4F5C\u5931\u8D25") + "</div></div>";
    } else if (!toolResult) {
      expandedHtml = '<div class="inline-tool-expanded" style="display: ' + (shouldExpand ? "block" : "none") + ';"><div class="inline-tool-loading">\u7B49\u5F85\u54CD\u5E94\u2026</div></div>';
    }
    var isTruncated = toolResult && toolResult._truncated === true;
    var imageHtml = "";
    if (toolName === "Read") {
      var imgPath = inputData.file_path || inputData.path || fileInfo || "";
      if (imgPath && isImagePath(imgPath)) {
        var imgSrc = "/api/file-raw?path=" + encodeURIComponent(imgPath);
        imageHtml = '<div class="inline-tool-image" onclick="event.stopPropagation();"><img class="inline-tool-image-thumb" loading="lazy" src="' + imgSrc + '" alt="' + escapeHtml(path) + '" data-path="' + escapeHtml(imgPath) + `" onclick="event.stopPropagation(); if(window.__openFilePreview)window.__openFilePreview(this.getAttribute('data-path'));" onerror="var w=this.closest('.inline-tool-image'); if(w)w.style.display='none';" /></div>`;
      }
    }
    var extraInfoHtml = meta ? '<span class="inline-tool-meta">' + escapeHtml(meta) + "</span>" : "";
    var extraClass = isError ? "inline-tool-error-inline" : "";
    if (shouldExpand) extraClass += " inline-tool-open";
    var truncatedAttrs = isTruncated ? 'data-truncated="true" data-tool-use-id="' + escapeHtml(block.id || "") + '" ' : "";
    return '<div class="inline-tool ' + extraClass + '" data-expand-kind="inline-tool" data-expand-key="' + escapeHtml(expandKey) + '" data-result="' + escapeHtml(fullResult) + '" data-preview="' + previewDataAttr + '" data-status="' + (isError ? "error" : hasResult ? "done" : "pending") + '" ' + truncatedAttrs + 'onclick="__inlineToolToggle(this)"><div class="inline-tool-row"><span class="inline-tool-status">' + statusIcon + "</span>" + icon + '<span class="inline-tool-title">' + escapeHtml(title) + "</span>" + extraInfoHtml + "</div>" + imageHtml + expandedHtml + "</div>";
  }
  function renderTerminalTool(block, toolResult, toolName, messageKey, index2, options) {
    var opts = options || {};
    var inputData = block.input || {};
    var command = inputData.command || inputData.cmd || "";
    var resultContent = extractToolResultText(toolResult && toolResult.content);
    var toolId = block.id || "tool-" + toolName;
    var expandKey = buildExpandKey("terminal", [messageKey, toolId || index2, index2]);
    var persistedExpanded = getPersistedExpandState(expandKey);
    var isError = toolResult && toolResult.is_error;
    var exitCode = inputData.exitCode;
    var hasResult = resultContent.length > 0;
    var statusDot = "";
    if (toolResult) {
      if (isError) {
        statusDot = '<span class="term-status-dot term-error"></span>';
      } else if (exitCode === 0 || exitCode === void 0) {
        statusDot = '<span class="term-status-dot term-success"></span>';
      } else {
        statusDot = '<span class="term-status-dot term-warn"></span>';
      }
    } else {
      statusDot = '<span class="term-status-dot term-running"></span>';
    }
    var prompt = '<span class="term-prompt">$</span>';
    var cmdDisplay = escapeHtml(command);
    var outputLines = resultContent.split("\n");
    var outputHtml = "";
    for (var oi = 0; oi < outputLines.length; oi++) {
      var line = outputLines[oi];
      if (!line && oi === outputLines.length - 1) continue;
      outputHtml += '<div class="term-line">' + escapeHtml(line) + "</div>";
    }
    var exitCodeHtml = "";
    if (toolResult && exitCode !== void 0) {
      var codeClass = exitCode === 0 ? "term-exit-success" : "term-exit-error";
      exitCodeHtml = '<div class="term-exit ' + codeClass + '">exit ' + exitCode + "</div>";
    }
    var cmdPreview = command.length > 80 ? command.slice(0, 77) + "\u2026" : command;
    var shouldExpand = opts.forceExpandedToolBodies ? true : persistedExpanded === null ? getCardDefault("terminal") : persistedExpanded;
    var termTruncated = toolResult && toolResult._truncated === true;
    var termTruncAttrs = termTruncated ? ' data-truncated="true" data-tool-use-id="' + escapeHtml(block.id || "") + '"' : "";
    return '<div class="inline-terminal" data-expand-kind="terminal" data-expand-key="' + escapeHtml(expandKey) + '" data-expanded="' + (shouldExpand ? "true" : "false") + '"' + termTruncAttrs + '><div class="term-header" role="button" tabindex="0" aria-expanded="' + (shouldExpand ? "true" : "false") + `" onclick="__terminalExpand(this)" onkeydown="if(event.key==='Enter'||event.key===' '){event.preventDefault();__terminalExpand(this);}">` + statusDot + '<span class="term-cmd-preview"><span class="term-prompt">$</span> ' + escapeHtml(cmdPreview) + '</span><span class="term-toggle-icon">' + (shouldExpand ? "\u25BC" : "\u25B6") + '</span></div><div class="term-body" aria-hidden="' + (shouldExpand ? "false" : "true") + '" style="display:' + (shouldExpand ? "block" : "none") + ';"><div class="term-command"><span class="term-prompt">$</span> ' + cmdDisplay + "</div>" + (outputHtml ? '<div class="term-output">' + outputHtml + "</div>" : "") + exitCodeHtml + "</div></div>";
  }
  function extractToolResultText(content) {
    if (!content) return "";
    if (typeof content === "string") return content;
    if (Array.isArray(content)) {
      return content.map(function(item) {
        if (!item || typeof item !== "object") return "";
        if (item.type === "text" && typeof item.text === "string") return item.text;
        try {
          return JSON.stringify(item);
        } catch (e) {
          return "";
        }
      }).filter(Boolean).join("\n");
    }
    return "";
  }
  function renderDiffTool(block, toolResult, toolName, messageKey, index2, options) {
    var opts = options || {};
    var inputData = block.input || {};
    var path = inputData.file_path || inputData.path || "";
    var fileName = path.split("/").pop() || path;
    var toolId = block.id || "tool-" + toolName + "-" + (typeof index2 === "number" ? index2 : 0);
    var oldStr = inputData.old_string || "";
    var newStr = inputData.new_string || inputData.content || "";
    var oldContent = inputData.old_content || "";
    var newContent = inputData.new_content || "";
    var unifiedDiff = inputData.unified_diff || inputData.diff || "";
    var changeKind = inputData.kind || "";
    var isWrite = toolName === "Write" || toolName === "MultiEdit";
    var isError = toolResult && toolResult.is_error;
    var toolResultText = extractToolResultText(toolResult && toolResult.content);
    var hasResult = !!(toolResultText && toolResultText.trim().length > 0);
    var leftCol = "";
    var rightCol = "";
    var unifiedCol = "";
    if (unifiedDiff) {
      unifiedCol = '<div class="diff-col diff-col-full"><div class="diff-col-label">Diff</div>' + renderUnifiedDiffLines(unifiedDiff) + "</div>";
    } else if (isWrite) {
      rightCol = '<div class="diff-line diff-add">+ ' + escapeHtml(newContent) + "</div>";
    } else {
      if (oldStr) {
        leftCol = '<div class="diff-line diff-remove">- ' + escapeHtml(oldStr) + "</div>";
      }
      if (newStr) {
        rightCol = '<div class="diff-line diff-add">+ ' + escapeHtml(newStr) + "</div>";
      }
    }
    var statusClass = "";
    var statusText = "";
    if (toolResult) {
      if (isError) {
        statusClass = "diff-error";
        statusText = toolResultText.indexOf("haven't granted") !== -1 || toolResultText.indexOf("permission") !== -1 ? "\u7B49\u5F85\u6388\u6743" : "\u5931\u8D25";
      } else {
        statusClass = "diff-success";
        statusText = changeKind === "add" ? "\u5DF2\u65B0\u589E" : changeKind === "delete" ? "\u5DF2\u5220\u9664" : changeKind === "move" ? "\u5DF2\u79FB\u52A8" : "\u5DF2\u4FEE\u6539";
      }
    } else {
      statusClass = "diff-pending";
      statusText = "\u6267\u884C\u4E2D";
    }
    var expandKey = buildExpandKey("diff", [messageKey, toolId || index2, index2]);
    var persistedExpanded = getPersistedExpandState(expandKey);
    var cardDefaultExpand = getCardDefault("editCards");
    var shouldExpand = opts.forceExpandedToolBodies ? true : persistedExpanded === null ? cardDefaultExpand : persistedExpanded;
    var collapsedClass = shouldExpand ? "" : " collapsed";
    var bothCols = !unifiedCol && leftCol && rightCol;
    var colClass = bothCols ? "diff-col-half" : "diff-col-full";
    var columnsHtml = unifiedCol || (bothCols ? '<div class="diff-col ' + colClass + '"><div class="diff-col-label">\u65E7</div>' + leftCol + "</div>" : "") + '<div class="diff-col ' + colClass + '"><div class="diff-col-label">' + (bothCols ? "\u65B0" : "") + "</div>" + (rightCol || leftCol || renderEmptyDiff(path)) + "</div>";
    var openButton = path ? '<button class="diff-open-file" type="button" data-path="' + escapeHtml(path) + `" title="\u6253\u5F00\u6587\u4EF6" onclick="event.stopPropagation(); if(window.__openFilePreview)window.__openFilePreview(this.getAttribute('data-path'));">\u6253\u5F00</button>` : "";
    return '<div class="inline-diff' + collapsedClass + '" data-tool-name="' + escapeHtml(toolName) + '" data-expand-kind="diff" data-expand-key="' + escapeHtml(expandKey) + '" data-tool-use-id="' + escapeHtml(toolId) + '" data-path="' + escapeHtml(path) + '"><div class="diff-header" role="button" tabindex="0" aria-expanded="' + (shouldExpand ? "true" : "false") + `" onclick="__tcToggle(event,this)" onkeydown="if(event.key==='Enter'||event.key===' '){event.preventDefault();__tcToggle(event,this);}"><span class="diff-file-icon"></span><span class="diff-file-name">` + escapeHtml(fileName) + "</span>" + renderTailMarqueePath(path, "diff-path") + '<span class="diff-status ' + statusClass + '">' + statusText + "</span>" + openButton + '<span class="diff-toggle">\u25BC</span></div><div class="diff-body" aria-hidden="' + (shouldExpand ? "false" : "true") + '"><div class="diff-columns">' + columnsHtml + "</div></div></div>";
  }
  function renderUnifiedDiffLines(diff) {
    var lines = String(diff || "").split("\n");
    var limit = 600;
    var html = "";
    for (var i = 0; i < lines.length && i < limit; i++) {
      var line = lines[i];
      var cls = "diff-context";
      if (/^@@/.test(line)) cls = "diff-hunk";
      else if (/^\+/.test(line) && !/^\+\+\+/.test(line)) cls = "diff-add";
      else if (/^-/.test(line) && !/^---/.test(line)) cls = "diff-remove";
      html += '<div class="diff-line ' + cls + '">' + escapeHtml(line || " ") + "</div>";
    }
    if (lines.length > limit) {
      html += '<div class="diff-line diff-context">\u2026\uFF08\u5DF2\u622A\u65AD ' + (lines.length - limit) + " \u884C\uFF09</div>";
    }
    return html || renderEmptyDiff("");
  }
  function renderEmptyDiff(path) {
    var suffix = path ? "\uFF0C\u53EF\u6253\u5F00\u6587\u4EF6\u67E5\u770B\u5F53\u524D\u5185\u5BB9" : "";
    return '<div class="diff-empty">Codex \u672A\u63D0\u4F9B\u5185\u8054 diff' + suffix + "\u3002</div>";
  }
  function formatInlineResult(content, toolName) {
    if (!content) return '<span class="inline-tool-empty">\u65E0\u8F93\u51FA</span>';
    return '<pre class="inline-tool-result-text" style="max-height: 300px; overflow-y: auto;">' + escapeHtml(content) + "</pre>";
  }
  function renderToolUseCard(block, toolResult, index2, messageKey, options) {
    var opts = options || {};
    var toolName = block.name || "unknown";
    var toolId = block.id || "tool-" + toolName + "-" + (typeof index2 === "number" ? index2 : 0);
    var fileInfo = extractFileInfo(toolName, block.input);
    if (toolName === "Read" || toolName === "Glob" || toolName === "Grep" || toolName === "WebFetch" || toolName === "WebSearch" || toolName === "TodoRead") {
      return renderInlineTool(block, toolResult, toolName, fileInfo, "", messageKey, index2, opts);
    }
    if (toolName === "Bash") {
      return renderTerminalTool(block, toolResult, toolName, messageKey, index2, opts);
    }
    if (toolName === "Edit" || toolName === "Write" || toolName === "MultiEdit") {
      return renderDiffTool(block, toolResult, toolName, messageKey, index2, opts);
    }
    var semanticQuestions = block.semantic && block.semantic.kind === "question_request" ? block.semantic.questions : null;
    if (semanticQuestions || toolName === "AskUserQuestion" && block.input && block.input.questions) {
      var questions = semanticQuestions || block.input.questions;
      if (questions && questions.length > 0) {
        var isAnswered = !!toolResult;
        var sel = state.askUserSelections[toolId] || {};
        var isSubmitted = !!sel.submitted;
        var answerText = isAnswered ? extractToolResultText(toolResult.content) : "";
        var answerLines = answerText ? answerText.trim().split("\n") : [];
        var headerLabel = "";
        for (var hi = 0; hi < questions.length; hi++) {
          if (questions[hi].header) {
            headerLabel = questions[hi].header;
            break;
          }
        }
        var headerSummary = headerLabel ? '<span class="tool-use-summary">' + escapeHtml(headerLabel) + "</span>" : "";
        var questionsHtml = "";
        questions.forEach(function(question, qIdx) {
          var isMulti = !!question.multiSelect;
          var questionText = question.question ? '<div class="ask-user-title">' + escapeHtml(question.question) + "</div>" : "";
          var optionsHtml = "";
          if (question.options && question.options.length > 0) {
            optionsHtml = '<div class="ask-user-options" data-multi-select="' + isMulti + '">';
            question.options.forEach(function(opt, idx) {
              var label = opt.label ? escapeHtml(opt.label) : "\u9009\u9879 " + (idx + 1);
              var descHtml = opt.description ? '<div class="ask-user-option-desc">' + escapeHtml(opt.description) + "</div>" : "";
              if (isAnswered) {
                var answerLine = answerLines[qIdx] || answerLines[0] || "";
                var chosenLabels = answerLine.split(",").map(function(s) {
                  return s.trim();
                });
                var isChosen = chosenLabels.indexOf(opt.label || "") !== -1;
                optionsHtml += '<div class="ask-user-option ask-user-option-readonly' + (isChosen ? " ask-user-option-chosen" : "") + '"><span class="ask-user-indicator"></span><div class="ask-user-option-content"><div class="ask-user-option-label">' + label + "</div>" + descHtml + "</div></div>";
              } else {
                var isSelected = (sel[qIdx] || []).indexOf(idx) !== -1;
                var disabledAttr = isSubmitted ? " disabled" : "";
                optionsHtml += '<button class="ask-user-option' + (isSelected ? " selected" : "") + '" data-option-index="' + idx + '" data-question-index="' + qIdx + '" data-option-label="' + escapeHtml(opt.label || "\u9009\u9879 " + (idx + 1)) + `" onclick="__askSelect('` + escapeHtml(toolId) + "'," + qIdx + "," + idx + "," + isMulti + ')"' + disabledAttr + '><span class="ask-user-indicator"></span><div class="ask-user-option-content"><div class="ask-user-option-label">' + label + "</div>" + descHtml + "</div></button>";
              }
            });
            optionsHtml += "</div>";
          }
          questionsHtml += '<div class="ask-user-question-group" data-question-index="' + qIdx + '">' + questionText + optionsHtml + "</div>";
        });
        var actionsHtml = "";
        if (!isAnswered) {
          var allAnsweredCheck = true;
          for (var qi = 0; qi < questions.length; qi++) {
            if (!sel[qi] || sel[qi].length === 0) {
              allAnsweredCheck = false;
              break;
            }
          }
          var submitDisabled = !allAnsweredCheck || isSubmitted ? " disabled" : "";
          var submitClass = isSubmitted ? " ask-user-submitted" : "";
          var submitText = isSubmitted ? "\u5DF2\u63D0\u4EA4..." : "\u786E\u8BA4\u63D0\u4EA4";
          actionsHtml = '<div class="ask-user-actions"><button class="ask-user-submit' + submitClass + '" data-tool-use-id="' + escapeHtml(toolId) + `" onclick="__askSubmit('` + escapeHtml(toolId) + `')"` + submitDisabled + ">" + submitText + "</button></div>";
        }
        var answeredSummary = "";
        if (isAnswered && answerText) {
          var shortAnswer = answerText.trim().replace(/\n/g, ", ");
          if (shortAnswer.length > 40) shortAnswer = shortAnswer.slice(0, 37) + "...";
          answeredSummary = '<span class="tool-use-file">' + escapeHtml(shortAnswer) + "</span>";
        }
        var askExpandKey = buildExpandKey("tool-card", [messageKey, toolId]);
        var askPersisted = getPersistedExpandState(askExpandKey);
        var askShouldExpand = opts.forceExpandedToolBodies ? true : askPersisted === null ? !isAnswered : askPersisted;
        var askCollapsed = askShouldExpand ? "" : " collapsed";
        var answeredClass = isAnswered ? " ask-user-answered" : "";
        return '<div class="tool-use-card ask-user' + answeredClass + askCollapsed + '" data-tool-use-id="' + escapeHtml(toolId) + '" data-expand-kind="tool-card" data-expand-key="' + escapeHtml(askExpandKey) + '"><div class="tool-use-header" data-tool-toggle onclick="__tcToggle(event,this)"><span class="tool-use-icon">' + (isAnswered ? "\u2713" : "?") + '</span><span class="tool-use-name">\u63D0\u95EE</span>' + headerSummary + answeredSummary + '<span class="tool-use-toggle">\u25BC</span></div><div class="tool-use-body ask-user-body">' + questionsHtml + actionsHtml + "</div></div>";
      }
    }
    var description = block.description || block.input && block.input.description || "";
    var summary = generateInputSummary(block.name, block.input);
    var titleText = "";
    var subtitleHtml = "";
    if (description) {
      titleText = description.length > 80 ? description.slice(0, 77) + "..." : description;
      if (fileInfo) {
        subtitleHtml = '<span class="tool-use-file">' + escapeHtml(fileInfo) + "</span>";
      }
    } else {
      titleText = getToolDisplayName(toolName);
      if (fileInfo) {
        subtitleHtml = '<span class="tool-use-file">' + escapeHtml(fileInfo) + "</span>";
      }
      if (summary) {
        subtitleHtml += '<span class="tool-use-summary">' + escapeHtml(summary) + "</span>";
      }
    }
    var fullJson = block.input ? JSON.stringify(block.input, null, 2) : "{}";
    var statusClass = "loading";
    var headerIcon = '<span class="tool-use-spinner"></span>';
    var resultHtml = "";
    if (toolResult) {
      var isError = toolResult.is_error;
      var content = extractToolResultText(toolResult.content);
      statusClass = isError ? "error" : "success";
      headerIcon = getToolIcon(toolName);
      var hasContent = content && content.trim().length > 0;
      if (hasContent) {
        resultHtml = '<pre class="tool-use-result-content">' + escapeHtml(content) + "</pre>";
      } else {
        resultHtml = '<span class="tool-use-result-empty">\u65E0\u8F93\u51FA</span>';
      }
    } else {
      headerIcon = getToolIcon(toolName);
    }
    var expandKey = buildExpandKey("tool-card", [messageKey, toolId]);
    var persistedExpanded = getPersistedExpandState(expandKey);
    var cardDefaultExpand = getCardDefault("editCards");
    var shouldExpand = opts.forceExpandedToolBodies ? true : persistedExpanded === null ? cardDefaultExpand : persistedExpanded;
    var tcTruncated = toolResult && toolResult._truncated === true;
    var collapsedClass = shouldExpand ? "" : " collapsed";
    var toggleHtml = '<span class="tool-use-toggle">\u25BC</span>';
    return '<div class="tool-use-card ' + statusClass + collapsedClass + '" data-expand-kind="tool-card" data-expand-key="' + escapeHtml(expandKey) + '" data-tool-use-id="' + escapeHtml(toolId) + '"' + (tcTruncated ? ' data-truncated="true"' : "") + '><div class="tool-use-header" role="button" tabindex="0" aria-expanded="' + (shouldExpand ? "true" : "false") + `" data-tool-toggle onclick="__tcToggle(event,this)" onkeydown="if(event.key==='Enter'||event.key===' '){event.preventDefault();__tcToggle(event,this);}"><span class="tool-use-icon">` + headerIcon + '</span><span class="tool-use-name">' + escapeHtml(titleText) + "</span>" + subtitleHtml + toggleHtml + '</div><div class="tool-use-body" aria-hidden="' + (shouldExpand ? "false" : "true") + '">' + (description ? '<div class="tool-use-meta"><span class="tool-use-meta-label">\u5DE5\u5177\uFF1A</span>' + escapeHtml(toolName) + "</div>" : "") + '<pre class="tool-use-content">' + escapeHtml(fullJson) + "</pre>" + (resultHtml ? '<div class="tool-use-result">' + resultHtml + "</div>" : "") + "</div></div>";
  }
  function getToolDisplayName(toolName) {
    var names = {
      "Read": "\u8BFB\u53D6\u6587\u4EF6",
      "Write": "\u5199\u5165\u6587\u4EF6",
      "Edit": "\u7F16\u8F91\u6587\u4EF6",
      "MultiEdit": "\u591A\u5904\u7F16\u8F91",
      "Bash": "\u6267\u884C\u547D\u4EE4",
      "Grep": "\u641C\u7D22\u5185\u5BB9",
      "Glob": "\u67E5\u627E\u6587\u4EF6",
      "WebFetch": "\u83B7\u53D6\u7F51\u9875",
      "WebSearch": "\u641C\u7D22\u7F51\u9875",
      "Task": "\u4EFB\u52A1",
      "TodoWrite": "\u66F4\u65B0\u5F85\u529E",
      "TodoRead": "\u8BFB\u53D6\u5F85\u529E",
      "NotebookEdit": "\u7F16\u8F91\u7B14\u8BB0\u672C",
      "Agent": "\u5B50\u4EE3\u7406",
      "AskUserQuestion": "\u63D0\u95EE",
      "Exit": "\u9000\u51FA"
    };
    return names[toolName] || toolName;
  }
  function getToolIcon(toolName) {
    var icons = {
      "Read": "R",
      "Write": "W",
      "Edit": "E",
      "MultiEdit": "E",
      "Bash": "$",
      "Grep": "G",
      "Glob": "F",
      "WebFetch": "\u21E3",
      "WebSearch": "\u21E2",
      "Task": "T",
      "TodoWrite": "\u2610",
      "TodoRead": "\u2611",
      "NotebookEdit": "N",
      "Agent": "A",
      "Exit": "\xD7"
    };
    return icons[toolName] || "\xB7";
  }
  function generateInputSummary(toolName, input) {
    if (!input || typeof input !== "object") return "";
    var keys = Object.keys(input);
    if (keys.length === 0) return "{}";
    if (toolName === "Read") {
      return "\u8BFB\u53D6\u6587\u4EF6";
    }
    if (toolName === "Write") {
      return "\u5199\u5165\u6587\u4EF6";
    }
    if (toolName === "Edit") {
      var edits = input.edits ? input.edits.length : 0;
      return "\u7F16\u8F91 (" + edits + " \u5904\u4FEE\u6539)";
    }
    if (toolName === "Bash") {
      var cmd = input.command || "";
      if (cmd) {
        var cmdPreview = cmd.length > 60 ? cmd.slice(0, 60) + "..." : cmd;
        return "\u547D\u4EE4\uFF1A" + cmdPreview;
      }
    }
    if (toolName === "Grep") {
      var pattern = input.pattern || "";
      var path = input.path || "";
      if (pattern) {
        return "\u641C\u7D22\uFF1A" + pattern + (path ? " (\u5728 " + path + ")" : "");
      }
    }
    if (toolName === "Glob") {
      var pattern = input.pattern || "";
      if (pattern) return "\u67E5\u627E\uFF1A" + pattern;
    }
    if (toolName === "Agent") {
      var task = input.prompt || input.task || "";
      if (task) {
        var taskPreview = task.length > 40 ? task.slice(0, 40) + "..." : task;
        return "\u4EFB\u52A1\uFF1A" + taskPreview;
      }
    }
    if (toolName === "Task") {
      var task = input.task || input.description || "";
      if (task) {
        var taskPreview = task.length > 40 ? task.slice(0, 40) + "..." : task;
        return "\u4EFB\u52A1\uFF1A" + taskPreview;
      }
    }
    if (toolName === "TodoWrite") {
      var todos = input.todos || [];
      return "\u66F4\u65B0\u5F85\u529E (" + todos.length + " \u9879)";
    }
    if (toolName === "WebSearch") {
      var query = input.query || "";
      if (query) return "\u641C\u7D22\uFF1A" + query;
    }
    var firstKey = keys[0];
    var firstVal = input[firstKey];
    if (typeof firstVal === "string") {
      var valPreview = firstVal.length > 50 ? firstVal.slice(0, 50) + "..." : firstVal;
      return firstKey + ": " + valPreview;
    }
    return keys.length + " \u4E2A\u53C2\u6570";
  }
  function extractFileInfo(toolName, input) {
    if (!input) return null;
    var path = input.file_path || input.path || input.cwd;
    if (path) {
      if (path.length > 50) {
        return "..." + path.slice(-47);
      }
      return path;
    }
    return null;
  }
  function formatAssistantResponse(text5) {
    if (!text5) return "";
    var newline = String.fromCharCode(10);
    var lines = text5.split(newline);
    var cleanLines = [];
    var started = false;
    for (var i = 0; i < lines.length; i++) {
      var line = lines[i];
      var trimmed = line.trim();
      if (!started && !trimmed) continue;
      started = true;
      if (trimmed.indexOf("\u23FA") === 0 && trimmed.length > 2) {
        cleanLines.push(trimmed.slice(1).trim());
        continue;
      }
      if (trimmed.indexOf("\u25CF") === 0) {
        trimmed = trimmed.slice(1).trim();
        if (!trimmed) continue;
        line = trimmed;
      }
      cleanLines.push(line);
    }
    while (cleanLines.length > 0 && !cleanLines[cleanLines.length - 1].trim()) {
      cleanLines.pop();
    }
    var deduped = [];
    var seenNorm = {};
    for (var j = 0; j < cleanLines.length; j++) {
      var normalized = cleanLines[j].replace(/\s+/g, "");
      if (normalized.length > 5 && seenNorm[normalized]) continue;
      if (normalized.length > 5) seenNorm[normalized] = true;
      deduped.push(cleanLines[j]);
    }
    return deduped.join(newline);
  }
  function parseMarkdownTables(source) {
    var NL = "\n";
    var lines = source.split(NL);
    var out = [];
    var i = 0;
    function splitRow(line) {
      var s = line.trim();
      if (s.charAt(0) === "|") s = s.slice(1);
      if (s.charAt(s.length - 1) === "|") s = s.slice(0, -1);
      return s.split("|");
    }
    function styleAttr(a) {
      return a ? ' style="text-align:' + a + '"' : "";
    }
    function buildTable(headers2, aligns2, rows2) {
      var thead = "<thead><tr>" + headers2.map(function(c, idx) {
        return "<th" + styleAttr(aligns2[idx]) + ">" + c.trim() + "</th>";
      }).join("") + "</tr></thead>";
      var tbody = rows2.length ? "<tbody>" + rows2.map(function(r) {
        return "<tr>" + r.map(function(c, idx) {
          return "<td" + styleAttr(aligns2[idx]) + ">" + c.trim() + "</td>";
        }).join("") + "</tr>";
      }).join("") + "</tbody>" : "";
      return '<div class="md-table-wrap"><table class="md-table">' + thead + tbody + "</table></div>";
    }
    while (i < lines.length) {
      var header = lines[i];
      if (header.indexOf("|") !== -1 && i + 1 < lines.length) {
        var sep = lines[i + 1].trim();
        if (/^\|?\s*:?-+:?(\s*\|\s*:?-+:?)+\s*\|?$/.test(sep)) {
          var headers = splitRow(header);
          var aligns = splitRow(sep).map(function(c) {
            var t6 = c.trim();
            var L = t6.charAt(0) === ":";
            var R = t6.charAt(t6.length - 1) === ":";
            if (L && R) return "center";
            if (R) return "right";
            if (L) return "left";
            return "";
          });
          var rows = [];
          var j = i + 2;
          while (j < lines.length) {
            var trimmed = lines[j].trim();
            if (!trimmed || trimmed.indexOf("|") === -1) break;
            rows.push(splitRow(lines[j]));
            j += 1;
          }
          out.push("", buildTable(headers, aligns, rows), "");
          i = j;
          continue;
        }
      }
      out.push(header);
      i += 1;
    }
    return out.join(NL);
  }
  function renderMarkdown(text5) {
    if (!text5) return "";
    var markdownLinks = [];
    var result = escapeHtml(stashMarkdownLinks(String(text5)));
    var bt = String.fromCharCode(96);
    var newline = String.fromCharCode(10);
    function serverFilePathFromLink(target) {
      var value = String(target || "").trim();
      if (!value) return null;
      if (value.charAt(0) === "<" && value.charAt(value.length - 1) === ">") {
        value = value.slice(1, -1).trim();
      }
      if (/^file:\/\//i.test(value)) {
        try {
          var fileUrl = new URL(value);
          if (fileUrl.protocol !== "file:") return null;
          value = decodeURIComponent(fileUrl.pathname || "");
        } catch (_) {
          value = value.replace(/^file:\/\/(?:localhost)?/i, "");
          try {
            value = decodeURIComponent(value);
          } catch (_2) {
          }
        }
      } else if (value.charAt(0) !== "/" || value.indexOf("//") === 0) {
        return null;
      }
      if (/^\/(?:api|android|macos)(?:\/|$)/.test(value)) return null;
      value = value.replace(/#L\d+(?:C\d+)?$/i, "");
      value = value.replace(/:\d+(?::\d+)?$/, "");
      return value.charAt(0) === "/" ? value : null;
    }
    function safeExternalLink(target, label) {
      var value = String(target || "").trim();
      if (value.charAt(0) === "<" && value.charAt(value.length - 1) === ">") {
        value = value.slice(1, -1).trim();
      }
      if (!/^(?:https?:\/\/|mailto:|#)/i.test(value)) return label;
      var escapedTarget = escapeHtml(value);
      var opensNewWindow = /^https?:\/\//i.test(value);
      return '<a href="' + escapedTarget + '"' + (opensNewWindow ? ' target="_blank"' : "") + ' rel="noopener">' + label + "</a>";
    }
    function stashMarkdownLinks(source) {
      function findTargetEnd(start2) {
        if (source.charAt(start2) === "<") {
          var closeAngle = source.indexOf(">", start2 + 1);
          return closeAngle >= 0 && source.charAt(closeAngle + 1) === ")" ? closeAngle + 1 : -1;
        }
        var depth = 0;
        for (var i = start2; i < source.length; i += 1) {
          if (source.charAt(i) === "\\") {
            i += 1;
            continue;
          }
          if (source.charAt(i) === "(") depth += 1;
          else if (source.charAt(i) === ")") {
            if (depth === 0) return i;
            depth -= 1;
          }
        }
        return -1;
      }
      var output = "";
      var cursor = 0;
      var inFence = false;
      var inInlineCode = false;
      while (cursor < source.length) {
        if (source.slice(cursor, cursor + 3) === "```") {
          inFence = !inFence;
          output += "```";
          cursor += 3;
          continue;
        }
        if (!inFence && source.charAt(cursor) === "`") {
          inInlineCode = !inInlineCode;
          output += "`";
          cursor += 1;
          continue;
        }
        if (!inFence && !inInlineCode && source.charAt(cursor) === "[" && source.charAt(cursor - 1) !== "!") {
          var closeText = source.indexOf("](", cursor + 1);
          var closeTarget = closeText >= 0 ? findTargetEnd(closeText + 2) : -1;
          if (closeText > cursor + 1 && closeTarget > closeText + 2) {
            var label = escapeHtml(source.slice(cursor + 1, closeText));
            var target = source.slice(closeText + 2, closeTarget).trim();
            var serverPath = serverFilePathFromLink(target);
            var linkHtml;
            if (serverPath) {
              var rawUrl2 = "/api/file-raw?download=1&amp;path=" + encodeURIComponent(serverPath);
              linkHtml = '<a class="server-file-link" href="' + rawUrl2 + '" data-server-file-path="' + escapeHtml(serverPath) + `" title="\u6253\u5F00\u6216\u4E0B\u8F7D\u670D\u52A1\u7AEF\u6587\u4EF6" onclick="if(window.__openFilePreview){event.preventDefault();window.__openFilePreview(this.getAttribute('data-server-file-path'));}">` + label + "</a>";
            } else {
              linkHtml = safeExternalLink(target, label);
            }
            var token = "WANDMARKDOWNLINKTOKEN" + markdownLinks.length + "END";
            markdownLinks.push(linkHtml);
            output += token;
            cursor = closeTarget + 1;
            continue;
          }
        }
        output += source.charAt(cursor);
        cursor += 1;
      }
      return output;
    }
    function restoreMarkdownLinks(source) {
      for (var i = 0; i < markdownLinks.length; i += 1) {
        source = source.split("WANDMARKDOWNLINKTOKEN" + i + "END").join(markdownLinks[i]);
      }
      return source;
    }
    function replacePair(source, marker, openTag, closeTag) {
      var cursor = 0;
      while (true) {
        var start2 = source.indexOf(marker, cursor);
        if (start2 === -1) break;
        var end = source.indexOf(marker, start2 + marker.length);
        if (end === -1) break;
        var inner = source.slice(start2 + marker.length, end);
        if (!inner) {
          cursor = end + marker.length;
          continue;
        }
        var replacement2 = openTag + inner + closeTag;
        source = source.slice(0, start2) + replacement2 + source.slice(end + marker.length);
        cursor = start2 + replacement2.length;
      }
      return source;
    }
    function isWordChar(code2) {
      return code2 >= 48 && code2 <= 57 || code2 >= 65 && code2 <= 90 || code2 >= 97 && code2 <= 122 || code2 === 95;
    }
    function replaceUnderscoreEmphasis(source, openTag, closeTag) {
      var cursor = 0;
      while (cursor < source.length) {
        var start2 = source.indexOf("_", cursor);
        if (start2 === -1) break;
        var leftCode = start2 > 0 ? source.charCodeAt(start2 - 1) : 0;
        if (isWordChar(leftCode)) {
          cursor = start2 + 1;
          continue;
        }
        var searchFrom = start2 + 1;
        var end = -1;
        while (searchFrom < source.length) {
          var candidate = source.indexOf("_", searchFrom);
          if (candidate === -1) break;
          var rightIdx = candidate + 1;
          var rightCode = rightIdx < source.length ? source.charCodeAt(rightIdx) : 0;
          if (!isWordChar(rightCode)) {
            end = candidate;
            break;
          }
          searchFrom = candidate + 1;
        }
        if (end === -1) break;
        var inner = source.slice(start2 + 1, end);
        if (!inner) {
          cursor = end + 1;
          continue;
        }
        var replacement2 = openTag + inner + closeTag;
        source = source.slice(0, start2) + replacement2 + source.slice(end + 1);
        cursor = start2 + replacement2.length;
      }
      return source;
    }
    function replaceLinePrefix(source, marker, openTag, closeTag) {
      return source.split(newline).map(function(line) {
        if (line.indexOf(marker) !== 0) return line;
        return openTag + line.slice(marker.length) + closeTag;
      }).join(newline);
    }
    function replaceOrderedList(source) {
      return source.split(newline).map(function(line) {
        var dotIndex = line.indexOf(". ");
        if (dotIndex <= 0) return line;
        for (var i = 0; i < dotIndex; i += 1) {
          var code2 = line.charCodeAt(i);
          if (code2 < 48 || code2 > 57) return line;
        }
        return "<li>" + line.slice(dotIndex + 2) + "</li>";
      }).join(newline);
    }
    function wrapParagraphs(source) {
      return source.split(newline + newline).map(function(part) {
        var block = part.trim();
        if (!block) return "";
        if (block.indexOf("<div") === 0 || block.indexOf("<h1") === 0 || block.indexOf("<h2") === 0 || block.indexOf("<h3") === 0 || block.indexOf("<h4") === 0 || block.indexOf("<h5") === 0 || block.indexOf("<h6") === 0 || block.indexOf("<ul") === 0 || block.indexOf("<ol") === 0 || block.indexOf("<li") === 0 || block.indexOf("<blockquote") === 0 || block.indexOf("<pre") === 0) {
          return block;
        }
        return "<p>" + block.split(newline).join("<br>") + "</p>";
      }).join("");
    }
    var pos = 0;
    while (true) {
      var start = result.indexOf(bt + bt + bt, pos);
      if (start === -1) break;
      var endTag = result.indexOf(bt + bt + bt, start + 3);
      if (endTag === -1) break;
      var codeBlock = result.slice(start + 3, endTag);
      var langLineEnd = codeBlock.indexOf(newline);
      var lang = "";
      var code = codeBlock;
      if (langLineEnd !== -1 && langLineEnd < 30) {
        var potentialLang = codeBlock.slice(0, langLineEnd).trim();
        var isSimpleLang = potentialLang.length > 0;
        for (var j = 0; j < potentialLang.length; j += 1) {
          var langCode = potentialLang.charCodeAt(j);
          var isDigit = langCode >= 48 && langCode <= 57;
          var isUpper = langCode >= 65 && langCode <= 90;
          var isLower = langCode >= 97 && langCode <= 122;
          if (!isDigit && !isUpper && !isLower) {
            isSimpleLang = false;
            break;
          }
        }
        if (isSimpleLang) {
          lang = potentialLang;
          code = codeBlock.slice(langLineEnd + 1);
        }
      }
      var highlighted = highlightCode(code.trim(), lang);
      var protectedHighlighted = highlighted.replace(/_/g, "&#95;").replace(/\*/g, "&#42;");
      var replacement = '<div class="code-block"><div class="code-block-header"><span class="code-lang">' + (lang || "code") + '</span><button class="code-copy">Copy</button></div><pre><code>' + protectedHighlighted + "</code></pre></div>";
      result = result.slice(0, start) + replacement + result.slice(endTag + 3);
      pos = start + replacement.length;
    }
    pos = 0;
    while (true) {
      var inlineStart = result.indexOf(bt, pos);
      if (inlineStart === -1) break;
      var inlineEnd = result.indexOf(bt, inlineStart + 1);
      if (inlineEnd === -1) break;
      if (inlineEnd === inlineStart + 1) {
        pos = inlineEnd + 1;
        continue;
      }
      var inlineCode = result.slice(inlineStart + 1, inlineEnd);
      var protectedInlineCode = inlineCode.replace(/_/g, "&#95;").replace(/\*/g, "&#42;");
      var inlineReplacement = '<code class="code-inline">' + protectedInlineCode + "</code>";
      result = result.slice(0, inlineStart) + inlineReplacement + result.slice(inlineEnd + 1);
      pos = inlineStart + inlineReplacement.length;
    }
    result = replacePair(result, "**", "<strong>", "</strong>");
    result = replacePair(result, "*", "<em>", "</em>");
    result = replaceUnderscoreEmphasis(result, "<em>", "</em>");
    result = replaceLinePrefix(result, "### ", "<h3>", "</h3>");
    result = replaceLinePrefix(result, "## ", "<h2>", "</h2>");
    result = replaceLinePrefix(result, "# ", "<h1>", "</h1>");
    result = replaceLinePrefix(result, "&gt; ", "<blockquote>", "</blockquote>");
    result = replaceLinePrefix(result, "- ", "<li>", "</li>");
    result = replaceLinePrefix(result, "* ", "<li>", "</li>");
    result = replaceOrderedList(result);
    result = parseMarkdownTables(result);
    var lines = result.split(newline);
    var grouped = [];
    var listBuffer = [];
    function flushListBuffer() {
      if (!listBuffer.length) return;
      grouped.push("<ul>" + listBuffer.join("") + "</ul>");
      listBuffer = [];
    }
    lines.forEach(function(line) {
      if (line.indexOf("<li>") === 0 && line.lastIndexOf("</li>") === line.length - 5) {
        listBuffer.push(line);
        return;
      }
      flushListBuffer();
      grouped.push(line);
    });
    flushListBuffer();
    result = wrapParagraphs(grouped.join(newline));
    result = restoreMarkdownLinks(result);
    return '<div class="markdown-content">' + result + "</div>";
  }
  function highlightCode(code, lang) {
    code = code.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;");
    return code;
  }
  function shortCommand(cmd) {
    var s = String(cmd || "").trim();
    return s.length <= 24 ? s || "\u672A\u9009\u62E9\u4F1A\u8BDD" : s.slice(0, 21) + "...";
  }
  function normalizeTerminalOutput(value) {
    return String(value || "").replace(/\r\r\n/g, "\r\n").replace(/\u0000/g, "");
  }

  // src/web-ui/browser/worktree-merge-adapter.ts
  var MERGE_STATUSES = /* @__PURE__ */ new Set([
    "ready",
    "checking",
    "merging",
    "merged",
    "failed"
  ]);
  var activeInstallation = null;
  function isRecord6(value) {
    return !!value && typeof value === "object" && !Array.isArray(value);
  }
  function optionalText(value) {
    if (typeof value !== "string") return void 0;
    const normalized = value.trim();
    return normalized || void 0;
  }
  function optionalMergeStatus(value) {
    return typeof value === "string" && MERGE_STATUSES.has(value) ? value : void 0;
  }
  function buildWorktreeMergeOpenContext(session, intent) {
    if (!session) return null;
    const sessionId = optionalText(session.id);
    if (!sessionId) return null;
    const worktree = isRecord6(session.worktree) ? session.worktree : {};
    const mergeInfo = isRecord6(session.worktreeMergeInfo) ? session.worktreeMergeInfo : {};
    const sourceBranch = optionalText(worktree.branch) ?? optionalText(session.worktreeBranch);
    const worktreePath = optionalText(worktree.path) ?? optionalText(session.worktreePath);
    const targetBranch = optionalText(mergeInfo.targetBranch);
    const sessionStatus = optionalText(session.status);
    const mergeStatus = optionalMergeStatus(session.worktreeMergeStatus);
    const cleanupPending = mergeStatus === "merged" && mergeInfo.cleanupDone === false;
    return {
      sessionId,
      intent: intent ?? (cleanupPending ? "cleanup" : "merge"),
      ...sourceBranch ? { sourceBranch } : {},
      ...worktreePath ? { worktreePath } : {},
      ...targetBranch ? { targetBranch } : {},
      ...sessionStatus ? { sessionStatus } : {},
      ...mergeStatus ? { mergeStatus } : {},
      cleanupPending
    };
  }
  function closeCompetingReactOverlays2() {
    for (const controller of [
      quickCommitController,
      folderPickerController,
      newSessionController,
      settingsController
    ]) {
      if (controller.isOpen() && !controller.closeIfOpen()) return false;
    }
    return true;
  }
  function reportRefreshFailure(dependencies, error) {
    const suffix = error instanceof Error && error.message ? `\uFF1A${error.message}` : "";
    dependencies.toast(`Worktree \u5DF2\u66F4\u65B0\uFF0C\u4F46\u4F1A\u8BDD\u5217\u8868\u5237\u65B0\u5931\u8D25${suffix}`, "error");
  }
  function refreshSessions(dependencies, sessionId) {
    try {
      void Promise.resolve(dependencies.refreshSessions(sessionId)).catch((error) => {
        reportRefreshFailure(dependencies, error);
      });
    } catch (error) {
      reportRefreshFailure(dependencies, error);
    }
  }
  function createRuntime(dependencies) {
    return {
      onOpen(context) {
        closeCompetingReactOverlays2();
        dependencies.onOpen?.(context);
      },
      onClose(context) {
        dependencies.onClose?.(context);
      },
      onRepositoryChanged(sessionId) {
        refreshSessions(dependencies, sessionId);
      },
      toast(message, tone) {
        dependencies.toast(message, tone);
      }
    };
  }
  function installWorktreeMergeLegacyAdapter(dependencies) {
    if (activeInstallation?.dependencies === dependencies) return activeInstallation.dispose;
    activeInstallation?.dispose();
    const uninstallRuntime3 = configureWorktreeMergeRuntime(createRuntime(dependencies));
    let disposed = false;
    const installation = {
      dependencies,
      dispose() {
        if (disposed) return;
        disposed = true;
        uninstallRuntime3();
        if (activeInstallation === installation) activeInstallation = null;
      }
    };
    activeInstallation = installation;
    return installation.dispose;
  }
  function openWorktreeMergeForSession(sessionId, intent) {
    const installation = activeInstallation;
    const normalizedId = sessionId.trim();
    if (!installation || !normalizedId) return false;
    const openIntent = () => {
      openWorktreeMergeNow(installation, normalizedId, intent);
    };
    if (!prepareFilePreviewForCompetingOverlay(openIntent)) return false;
    return openWorktreeMergeNow(installation, normalizedId, intent);
  }
  function openWorktreeMergeNow(installation, normalizedId, intent) {
    if (!closeCompetingReactOverlays2()) return false;
    const context = buildWorktreeMergeOpenContext(
      installation.dependencies.getSession(normalizedId),
      intent
    );
    if (!context) {
      installation.dependencies.toast("\u672A\u627E\u5230\u53EF\u64CD\u4F5C\u7684 worktree \u4F1A\u8BDD\u3002", "error");
      return false;
    }
    if (worktreeMergeController.open(context)) return true;
    installation.dependencies.toast("Worktree \u5408\u5E76\u754C\u9762\u5C1A\u672A\u5C31\u7EEA\uFF0C\u8BF7\u5237\u65B0\u540E\u91CD\u8BD5\u3002", "error");
    return false;
  }
  function closeWorktreeMergeFromLegacy() {
    return worktreeMergeController.closeIfOpen();
  }

  // src/web-ui/browser/session-engine.ts
  function showLoginCertHint() {
    var hint = document.getElementById("login-cert-hint");
    if (hint) hint.classList.remove("hidden");
    var httpLink = document.getElementById("login-cert-http-link");
    if (httpLink) {
      httpLink.href = "http://" + location.host + location.pathname;
      httpLink.style.display = location.protocol === "https:" ? "" : "none";
    }
  }
  function hideLoginCertHint() {
    var hint = document.getElementById("login-cert-hint");
    if (hint) hint.classList.add("hidden");
  }
  function login() {
    if (state.loginPending) return;
    var passwordEl = document.getElementById("password");
    var loginButton = document.getElementById("login-button");
    var errorEl = document.getElementById("login-error");
    if (!passwordEl || !loginButton || !errorEl) return;
    hideError(errorEl);
    hideLoginCertHint();
    passwordEl.dataset.error = "false";
    passwordEl.setAttribute("aria-invalid", "false");
    state.loginPending = true;
    loginButton.disabled = true;
    loginButton.textContent = "\u767B\u5F55\u4E2D...";
    fetch("/api/login", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify({ password: passwordEl.value }),
      credentials: "same-origin"
    }).then(function(res) {
      if (res.status === 429) {
        showError(errorEl, "\u767B\u5F55\u5C1D\u8BD5\u6B21\u6570\u8FC7\u591A\uFF0C\u8BF7\u7A0D\u540E\u518D\u8BD5\u3002");
        return Promise.reject("handled");
      }
      if (!res.ok) {
        passwordEl.dataset.error = "true";
        passwordEl.setAttribute("aria-invalid", "true");
        showError(errorEl, "\u5BC6\u7801\u9519\u8BEF\uFF0C\u8BF7\u91CD\u8BD5\u3002");
        return Promise.reject("handled");
      }
      return fetch("/api/config", { credentials: "same-origin" });
    }).then(function(res) {
      if (!res.ok) {
        if (location.protocol === "https:") {
          showLoginCertHint();
        } else {
          showError(errorEl, "\u767B\u5F55\u5931\u8D25\uFF0C\u8BF7\u91CD\u8BD5\u3002");
        }
        return Promise.reject("handled");
      }
      return res.json();
    }).then(function(config) {
      state.config = config;
      var statusDot = document.getElementById("status-dot");
      var statusText = document.getElementById("status-text");
      if (statusDot) statusDot.classList.add("active");
      if (statusText) statusText.textContent = "\u5DF2\u767B\u5F55";
      return refreshAll();
    }).then(function() {
      startPolling();
      render();
      if (!state.claudeHistoryLoaded) {
        setTimeout(function() {
          if (!state.claudeHistoryLoaded) ensureClaudeHistoryLoaded();
        }, 600);
      }
    }).catch(function(error) {
      if (error === "handled") return;
      console.error("[wand] Login error:", error);
      showError(errorEl, "\u767B\u5F55\u5931\u8D25\uFF0C\u8BF7\u91CD\u8BD5\u3002");
    }).finally(function() {
      state.loginPending = false;
      loginButton.disabled = false;
      loginButton.textContent = "\u8FDB\u5165\u63A7\u5236\u53F0";
    });
  }
  function hasNativeSwitchServer() {
    return typeof WandNative !== "undefined" && typeof WandNative.switchServer === "function";
  }
  function switchServer() {
    if (!hasNativeSwitchServer()) return;
    try {
      WandNative.switchServer();
    } catch (e) {
    }
  }
  function hasNativeBackToApp() {
    if (typeof WandNative !== "undefined" && typeof WandNative.backToNative === "function") return true;
    return typeof window.__wandBackToNative === "function";
  }
  function backToNativeApp() {
    try {
      if (typeof WandNative !== "undefined" && typeof WandNative.backToNative === "function") {
        WandNative.backToNative();
        return;
      }
      if (typeof window.__wandBackToNative === "function") window.__wandBackToNative();
    } catch (e) {
    }
  }
  function logout2() {
    fetch("/api/logout", { method: "POST", credentials: "same-origin" }).catch(function() {
    });
    stopPolling();
    setTerminalInteractive(false);
    hideMiniKeyboard();
    teardownTerminal();
    state.config = null;
    state.selectedId = null;
    persistSelectedId();
    state.sessions = [];
    state.claudeHistory = [];
    state.claudeHistoryLoaded = false;
    state.claudeHistoryExpanded = false;
    state.claudeHistoryExpandedDirs = {};
    state.sessionsDrawerOpen = false;
    writeStoredBoolean("wand-sidebar-open", false);
    render();
  }
  function refreshAll() {
    return loadSessions();
  }
  function getModeLabel(mode) {
    return mode === "full-access" ? "\u5168\u6743\u9650" : mode === "default" ? "\u9ED8\u8BA4" : mode === "native" ? "\u539F\u751F" : mode === "auto-edit" ? "\u81EA\u52A8\u7F16\u8F91" : mode === "managed" ? "\u6258\u7BA1" : mode;
  }
  function getPreferredTool() {
    return state.sessionTool || state.preferredCommand || "claude";
  }
  function getComposerTool() {
    var selected = state.sessions.find(function(s) {
      return s.id === state.selectedId;
    });
    return selected && selected.provider || state.preferredCommand || "claude";
  }
  function getComposerPlaceholder(session, terminalInteractive) {
    if (terminalInteractive) return "\u952E\u76D8\u8F93\u5165\u5C06\u53D1\u9001\u5230\u7EC8\u7AEF";
    if (session && (session.status === "exited" || session.status === "failed" || session.status === "stopped")) {
      if (canAutoResumeSession(session)) return "\u8F93\u5165\u6D88\u606F\u4EE5\u7EE7\u7EED\u4F1A\u8BDD";
      return "\u4F1A\u8BDD\u5DF2\u7ED3\u675F";
    }
    if (isStructuredSession2(session) && session.structuredState && session.structuredState.inFlight) {
      return "\u56DE\u590D\u4E2D\uFF0C\u53EF\u7EE7\u7EED\u8F93\u5165";
    }
    return "\u6253\u5B57\u6216\u6309\u4F4F\u8BF4\u8BDD";
  }
  function getSupportedModes(tool) {
    if (tool === "codex") {
      return ["full-access"];
    }
    if (tool === "opencode" || tool === "grok") return ["default", "full-access", "managed"];
    if (tool === "qoder") return ["default", "full-access", "auto-edit", "managed"];
    return ["default", "full-access", "auto-edit", "native", "managed"];
  }
  function getSafeModeForTool2(tool, mode) {
    var supported = getSupportedModes(tool);
    if (supported.indexOf(mode) !== -1) return mode;
    var fallback = state.config && state.config.defaultMode ? state.config.defaultMode : "default";
    if (supported.indexOf(fallback) !== -1) return fallback;
    return supported[0];
  }
  function getModeHint(mode) {
    var hints = {
      "default": "\u6807\u51C6\u6A21\u5F0F - \u9700\u8981\u786E\u8BA4\u6587\u4EF6\u4FEE\u6539",
      "full-access": "\u5B8C\u5168\u8BBF\u95EE - \u81EA\u52A8\u786E\u8BA4\u6743\u9650\u4E0E\u64CD\u4F5C",
      "auto-edit": "\u81EA\u52A8\u7F16\u8F91 - \u81EA\u52A8\u786E\u8BA4\u6587\u4EF6\u4FEE\u6539",
      "native": "\u539F\u751F\u6A21\u5F0F - \u8FD4\u56DE\u7ED3\u6784\u5316\u8F93\u51FA",
      "managed": "\u6258\u7BA1\u6A21\u5F0F - AI \u81EA\u52A8\u5B8C\u6210\u6240\u6709\u5DE5\u4F5C"
    };
    return hints[mode] || "";
  }
  function getSessionKindLabel(session) {
    var provider = session && session.provider ? session.provider : "claude";
    return (isStructuredSession2(session) ? "\u7ED3\u6784\u5316" : "\u7EC8\u7AEF") + " \xB7 " + provider;
  }
  function getSessionKindDescription(session) {
    return isStructuredSession2(session) ? "\u7ED3\u6784\u5316 \xB7 \u5757\u7EA7\u8BB0\u5F55" : session && session.provider === "codex" ? "\u7EC8\u7AEF \xB7 Codex PTY\uFF08chat \u4E3A\u89E3\u6790\u89C6\u56FE\uFF09" : "\u7EC8\u7AEF \xB7 PTY \u4F1A\u8BDD";
  }
  function shouldRequestChatFormat3(session) {
    if (!session) return false;
    return isStructuredSession2(session) || session.provider === "codex";
  }
  function isRecoverableToolError(toolResult, nextResult) {
    if (!toolResult || !toolResult.is_error || !nextResult || nextResult.is_error) {
      return false;
    }
    var currentText = extractToolResultText(toolResult.content).toLowerCase();
    var nextText = extractToolResultText(nextResult.content).toLowerCase();
    if (!currentText) return false;
    if (currentText.indexOf("invalid pages parameter") !== -1 && nextText.length > 0) {
      return true;
    }
    return false;
  }
  function isStructuredSession2(session) {
    return !!session && (session.sessionKind === "structured" || session.runner === "claude-cli-print");
  }
  function syncComposerModeSelect() {
    state.chatMode = getSafeModeForTool2("claude", state.chatMode);
    var modeHint2 = document.getElementById("mode-hint");
    if (modeHint2) modeHint2.textContent = getModeHint(state.chatMode);
    refreshAllChatModeTrios2();
  }
  var sessionConfigMutationTails = /* @__PURE__ */ Object.create(null);
  var sessionConfigMutationRevisions = /* @__PURE__ */ Object.create(null);
  var pendingSessionConfig = /* @__PURE__ */ Object.create(null);
  function getPendingSessionConfig(sessionId) {
    return sessionId ? pendingSessionConfig[sessionId] || null : null;
  }
  function setPendingSessionConfig(sessionId, key, value) {
    if (!sessionId) return;
    var pending = pendingSessionConfig[sessionId] || (pendingSessionConfig[sessionId] = {});
    pending[key] = value;
  }
  function clearPendingSessionConfig(sessionId) {
    if (sessionId) delete pendingSessionConfig[sessionId];
  }
  function enqueueSessionConfigMutation(sessionId, task) {
    var revision = (sessionConfigMutationRevisions[sessionId] || 0) + 1;
    sessionConfigMutationRevisions[sessionId] = revision;
    var previous = sessionConfigMutationTails[sessionId] || Promise.resolve();
    var request2 = previous.catch(function() {
    }).then(task);
    sessionConfigMutationTails[sessionId] = request2.catch(function() {
    });
    return request2.then(function(data) {
      return { data, latest: sessionConfigMutationRevisions[sessionId] === revision };
    }, function(error) {
      return Promise.reject({ error, latest: sessionConfigMutationRevisions[sessionId] === revision });
    });
  }
  function recoverLatestSessionConfigMutation(sessionId, message) {
    clearPendingSessionConfig(sessionId);
    Promise.resolve(loadSessions()).catch(function() {
    }).finally(function() {
      refreshAllChatModeTrios2();
    });
    showToast(message, "error");
  }
  function applyLatestSessionConfigSnapshot(sessionId, outcome) {
    if (!outcome.latest) return false;
    var data = outcome.data;
    if (data && data.error) {
      recoverLatestSessionConfigMutation(sessionId, data.error);
      return false;
    }
    if (!data || !data.id) {
      recoverLatestSessionConfigMutation(sessionId, "\u4F1A\u8BDD\u8BBE\u7F6E\u66F4\u65B0\u5931\u8D25");
      return false;
    }
    updateSessionSnapshot(data);
    clearPendingSessionConfig(sessionId);
    refreshAllChatModeTrios2();
    return true;
  }
  function renderChatModeTrioHtml(session, opts) {
    opts = opts || {};
    var kind = opts.kind === "compact" || opts.kind === "popover" ? opts.kind : "dropdown";
    var preferredTool = getPreferredTool();
    var composerMode = state.chatMode || "default";
    var modelText = getEffectiveModel(session) || "";
    var modelLabel = getShortModelLabel(modelText, session);
    var thinkingText = getEffectiveThinking(session);
    function pill(ctrl, label, value, optionsHtml) {
      var tagHtml = kind === "compact" ? "" : '<span class="chat-mode-trio-tag">' + escapeHtml(label) + "</span>";
      return '<span class="composer-text-pill chat-mode-trio-pill" data-mode-control-pill="' + ctrl + '" title="' + escapeHtml(label) + '">' + tagHtml + '<span class="composer-text-label">' + escapeHtml(value) + '</span><select class="composer-text-hidden-select" data-mode-control="' + ctrl + '" aria-label="' + escapeHtml(label) + '">' + optionsHtml + "</select></span>";
    }
    return '<div class="chat-mode-trio chat-mode-trio-' + kind + '" role="group" aria-label="\u4F1A\u8BDD\u8BBE\u7F6E">' + pill("mode", "\u6A21\u5F0F", composerMode, renderChatModeOptionsRaw(preferredTool, composerMode)) + '<span class="composer-text-sep" aria-hidden="true">\xB7</span>' + pill("model", "\u6A21\u578B", modelLabel, renderChatModelOptionsRaw(modelText, session)) + '<span class="composer-text-sep" aria-hidden="true">\xB7</span>' + pill("thinking", "\u601D\u8003", getThinkingCompactLabel(thinkingText, session), renderThinkingOptions(thinkingText, session)) + "</div>";
  }
  function getThinkingCompactLabel(id, session) {
    var effort = getThinkingLabel(id, session);
    if (effort === "low") return "\u4F4E";
    if (effort === "medium") return "\u4E2D";
    if (effort === "high") return "\u9AD8";
    if (effort === "xhigh") return "\u8D85\u9AD8";
    if (effort === "max") return "\u6781\u9AD8";
    if (effort === "ultra") return "\u6781\u9650";
    return "\u81EA\u52A8";
  }
  function getModelDisplayLabel(model, session) {
    var selected = model || "";
    var models = getModelsForCurrentProvider(session);
    if (!selected || selected === "default") {
      for (var j = 0; j < models.length; j++) {
        if (models[j].id === "default") return models[j].label || models[j].id;
      }
      return "\u9ED8\u8BA4";
    }
    for (var i = 0; i < models.length; i++) {
      if (models[i].id === selected) return models[i].label || models[i].id;
    }
    return selected;
  }
  function getShortModelLabel(model, session) {
    var label = getModelDisplayLabel(model, session);
    if (!label || label === "\u8DDF\u968F\u670D\u52A1\u7AEF\u9ED8\u8BA4") return "\u9ED8\u8BA4";
    var cutAt = label.search(/[（(]/);
    if (cutAt > 0) label = label.slice(0, cutAt).trim();
    var slash = label.lastIndexOf("/");
    if (slash >= 0 && slash < label.length - 1) label = label.slice(slash + 1).trim();
    var lower = label.toLowerCase();
    if (lower.indexOf("opus") !== -1) return "Opus";
    if (lower.indexOf("sonnet") !== -1) return "Sonnet";
    if (lower.indexOf("haiku") !== -1) return "Haiku";
    if (lower.indexOf("gpt-5.5") !== -1) return "GPT-5.5";
    if (lower.indexOf("gpt-5") !== -1) return "GPT-5";
    if (lower.indexOf("gpt-4") !== -1) return "GPT-4";
    if (lower.indexOf("grok-4.5") !== -1) return "Grok 4.5";
    if (lower.indexOf("grok-4") !== -1) return "Grok 4";
    if (lower.indexOf("grok-3") !== -1) return "Grok 3";
    if (lower.indexOf("grok") !== -1) {
      if (label.length > 12) return label.slice(0, 10) + "\u2026";
      return label;
    }
    if (label.length > 12) return label.slice(0, 10) + "\u2026";
    return label;
  }
  function renderThinkingOptions(selected, session) {
    var levels = getThinkingLevels(session);
    var normalized = levels.some(function(level) {
      return level.id === selected;
    }) ? selected : "off";
    return levels.map(function(level) {
      var label = level.id === "off" ? "\u81EA\u52A8\uFF08\u6A21\u578B\u9ED8\u8BA4\uFF09" : getThinkingCompactLabel(level.id, session);
      return '<option value="' + escapeHtml(level.id) + '"' + (level.id === normalized ? " selected" : "") + ">" + escapeHtml(label) + "</option>";
    }).join("");
  }
  function syncThinkingSelect(container, selected, session) {
    var select = container.querySelector('[data-mode-control="thinking"]');
    if (!select) return;
    select.innerHTML = renderThinkingOptions(selected, session);
    select.value = getThinkingLevels(session).some(function(level) {
      return level.id === selected;
    }) ? selected : "off";
  }
  function renderComposerConfigControlsHtml(session) {
    var preferredTool = getPreferredTool();
    var mode = state.chatMode || "default";
    var model = getEffectiveModel(session) || "";
    var thinking = getEffectiveThinking(session);
    var modeLabel = getModeLabel(mode);
    var modelLabel = getShortModelLabel(model, session);
    var thinkingLabel = getThinkingCompactLabel(thinking, session);
    var title = "\u6A21\u5F0F " + modeLabel + " \xB7 \u6A21\u578B " + modelLabel + " \xB7 \u601D\u8003 " + thinkingLabel;
    return '<div class="composer-config-controls" role="group" aria-label="\u4F1A\u8BDD\u8BBE\u7F6E" title="' + escapeHtml(title) + '"><span class="composer-config-chip composer-config-chip-mode" data-mode-control-pill="mode" title="\u6A21\u5F0F\uFF1A' + escapeHtml(modeLabel) + '">' + iconSvg("shield", { size: 13, strokeWidth: 1.8, cls: "composer-config-icon" }) + '<span class="composer-config-label">' + escapeHtml(modeLabel) + '</span><select class="composer-text-hidden-select" data-mode-control="mode" aria-label="\u6A21\u5F0F">' + renderChatModeOptionsRaw(preferredTool, mode) + '</select></span><span class="composer-config-chip composer-config-model" data-mode-control-pill="model" title="\u6A21\u578B\uFF1A' + escapeHtml(modelLabel) + '">' + iconSvg("cpu", { size: 13, strokeWidth: 1.8, cls: "composer-config-icon" }) + '<span class="composer-config-label">' + escapeHtml(modelLabel) + '</span><select class="composer-text-hidden-select" data-mode-control="model" aria-label="\u6A21\u578B">' + renderChatModelOptions(model, session) + '</select></span><span class="composer-config-chip composer-config-thinking" data-mode-control-pill="thinking" data-thinking="' + escapeHtml(thinking) + '" title="\u601D\u8003\u6DF1\u5EA6\uFF1A' + escapeHtml(thinkingLabel) + '">' + iconSvg("brain", { size: 13, strokeWidth: 1.8, cls: "composer-config-icon" }) + '<span class="composer-config-label">' + escapeHtml(thinkingLabel) + '</span><select class="composer-text-hidden-select" data-mode-control="thinking" aria-label="\u601D\u8003\u6DF1\u5EA6">' + renderThinkingOptions(thinking, session) + "</select></span></div>";
  }
  function refreshAllChatModeTrios2() {
    var session = getSelectedSession4();
    var preferredTool = getPreferredTool();
    var mode = state.chatMode || "default";
    var model = getEffectiveModel(session) || "";
    var modelLabel = getShortModelLabel(model, session);
    var thinking = getEffectiveThinking(session);
    var trios = document.querySelectorAll(".chat-mode-trio");
    trios.forEach(function(trio) {
      function setPair(ctrl, value, optionsHtml, labelText) {
        var pillNode = trio.querySelector('[data-mode-control-pill="' + ctrl + '"]');
        if (!pillNode) return;
        var label = pillNode.querySelector(".composer-text-label");
        if (label) label.textContent = labelText || value;
        if (ctrl === "thinking") {
          syncThinkingSelect(pillNode, value, session);
          return;
        }
        var sel = pillNode.querySelector('[data-mode-control="' + ctrl + '"]');
        if (!sel) return;
        if (optionsHtml) sel.innerHTML = optionsHtml;
        if (sel.value !== value) sel.value = value;
      }
      setPair("mode", mode, renderChatModeOptionsRaw(preferredTool, mode));
      setPair("model", model, renderChatModelOptionsRaw(model, session), modelLabel);
      setPair("thinking", thinking, "", getThinkingCompactLabel(thinking, session));
    });
    refreshComposerConfigControls(session, mode, getEffectiveModel(session) || "", thinking);
  }
  function refreshComposerConfigControls(session, mode, model, thinking) {
    var preferredTool = getPreferredTool();
    var modeLabel = getModeLabel(mode);
    var modelLabel = getShortModelLabel(model, session);
    var thinkingLabel = getThinkingCompactLabel(thinking, session);
    var controls = document.querySelectorAll(".composer-config-controls");
    controls.forEach(function(control) {
      var title = "\u6A21\u5F0F " + modeLabel + " \xB7 \u6A21\u578B " + modelLabel + " \xB7 \u601D\u8003 " + thinkingLabel;
      control.setAttribute("title", title);
      function updateSelect(part, value, optionsHtml) {
        var sel = part.querySelector("[data-mode-control]");
        if (!sel) return;
        sel.innerHTML = optionsHtml;
        sel.value = value;
      }
      var modePart = control.querySelector('[data-mode-control-pill="mode"]');
      if (modePart) {
        var modeText = modePart.querySelector(".composer-config-label");
        if (modeText) modeText.textContent = modeLabel;
        modePart.setAttribute("title", "\u6A21\u5F0F\uFF1A" + modeLabel);
        updateSelect(modePart, mode, renderChatModeOptionsRaw(preferredTool, mode));
      }
      var modelPart = control.querySelector('[data-mode-control-pill="model"]');
      if (modelPart) {
        var modelText = modelPart.querySelector(".composer-config-label");
        if (modelText) modelText.textContent = modelLabel;
        modelPart.setAttribute("title", "\u6A21\u578B\uFF1A" + modelLabel);
        updateSelect(modelPart, model, renderChatModelOptions(model, session));
      }
      var thinkingPart = control.querySelector('[data-mode-control-pill="thinking"]');
      if (thinkingPart) {
        var thinkingText = thinkingPart.querySelector(".composer-config-label");
        if (thinkingText) thinkingText.textContent = thinkingLabel;
        thinkingPart.setAttribute("data-thinking", thinking);
        thinkingPart.setAttribute("title", "\u601D\u8003\u6DF1\u5EA6\uFF1A" + thinkingLabel);
        syncThinkingSelect(thinkingPart, thinking, session);
      }
    });
  }
  function renderChatModeOptionsRaw(tool, selectedMode) {
    return getSupportedModes(tool).map(function(mode) {
      return '<option value="' + escapeHtml(mode) + '"' + (mode === selectedMode ? " selected" : "") + ">" + escapeHtml(mode) + "</option>";
    }).join("");
  }
  function getProviderKey(provider) {
    return provider === "codex" || provider === "opencode" || provider === "grok" || provider === "qoder" ? provider : "claude";
  }
  function getProviderForSession(session) {
    return getProviderKey(session && session.provider || state.sessionTool || "claude");
  }
  function getConfigDefaultModels() {
    var configured = state.config && state.config.defaultModels && typeof state.config.defaultModels === "object" ? state.config.defaultModels : {};
    return {
      claude: typeof configured.claude === "string" ? configured.claude : state.config && state.config.defaultModel || "",
      codex: typeof configured.codex === "string" ? configured.codex : state.config && state.config.defaultCodexModel || "",
      opencode: typeof configured.opencode === "string" ? configured.opencode : state.config && state.config.defaultOpenCodeModel || "",
      grok: typeof configured.grok === "string" ? configured.grok : state.config && state.config.defaultGrokModel || "",
      qoder: typeof configured.qoder === "string" ? configured.qoder : state.config && state.config.defaultQoderModel || ""
    };
  }
  function getConfigDefaultModelForProvider(provider) {
    var defaults = getConfigDefaultModels();
    var key = getProviderKey(provider);
    if (key === "codex") return defaults.codex || "";
    if (key === "opencode") return defaults.opencode || "";
    if (key === "grok") return defaults.grok || "";
    if (key === "qoder") return defaults.qoder || "";
    return defaults.claude || "";
  }
  function getChatModelForProvider(provider) {
    var key = getProviderKey(provider);
    var selected = state.chatModels && typeof state.chatModels[key] === "string" ? state.chatModels[key] : "";
    if (!selected && key === "claude") selected = state.chatModel || "";
    return selected || "";
  }
  function setChatModelForProvider(provider, model) {
    var key = getProviderKey(provider);
    var normalized = (model || "").trim();
    if (!state.chatModels) state.chatModels = { claude: "", codex: "", opencode: "", grok: "", qoder: "" };
    state.chatModels[key] = normalized;
    state.chatModel = normalized;
    try {
      localStorage.setItem("wand-chat-model-" + key, normalized);
      localStorage.removeItem("wand-chat-model");
    } catch (e) {
    }
  }
  function getEffectiveModel(session) {
    var pending = session && session.id ? getPendingSessionConfig(session.id) : null;
    if (pending && Object.prototype.hasOwnProperty.call(pending, "model")) return pending.model || "";
    if (session && session.selectedModel) return session.selectedModel;
    var provider = getProviderForSession(session);
    var selected = getChatModelForProvider(provider);
    return selected || getConfigDefaultModelForProvider(provider) || "";
  }
  function getModelsForCurrentProvider(session) {
    var provider = getProviderKey(session && session.provider || state.sessionTool || "claude");
    if (provider === "codex") return state.availableCodexModels || [];
    if (provider === "opencode") return state.availableOpenCodeModels || [];
    if (provider === "grok") return state.availableGrokModels || [];
    if (provider === "qoder") return state.availableQoderModels || [];
    return state.availableModels || [];
  }
  function renderChatModelOptions(selected, session) {
    var models = getModelsForCurrentProvider(session);
    var normalized = selected === "default" ? "" : selected || "";
    var html = '<option value=""' + (!normalized ? " selected" : "") + ">\u9ED8\u8BA4 \xB7 " + escapeHtml(getModelDisplayLabel("", session)) + "</option>";
    for (var i = 0; i < models.length; i++) {
      var m = models[i];
      if (m.id === "default") continue;
      var label = m.label || m.id;
      var suffix = getProviderForSession(session) === "claude" ? m.availability === "verified" ? " \xB7 \u5DF2\u9A8C\u8BC1" : m.availability === "stale" ? " \xB7 \u5F85\u5237\u65B0" : m.source === "models-api" ? " \xB7 API \u5019\u9009" : " \xB7 \u5019\u9009" : "";
      html += '<option value="' + escapeHtml(m.id) + '"' + (m.id === normalized ? " selected" : "") + ">" + escapeHtml(label + suffix) + "</option>";
    }
    if (normalized && !models.some(function(m2) {
      return m2.id === normalized;
    })) {
      html += '<option value="' + escapeHtml(normalized) + '" selected>' + escapeHtml(normalized) + "\uFF08\u81EA\u5B9A\u4E49\uFF09</option>";
    }
    return html;
  }
  function renderChatModelOptionsRaw(selected, session) {
    var models = getModelsForCurrentProvider(session);
    var normalized = selected === "default" ? "" : selected || "";
    var html = '<option value=""' + (!normalized ? " selected" : "") + ">" + escapeHtml(getModelDisplayLabel("", session)) + "</option>";
    for (var i = 0; i < models.length; i++) {
      var m = models[i];
      if (m.id === "default") continue;
      var rawSuffix = getProviderForSession(session) === "claude" ? m.availability === "verified" ? " \xB7 verified" : m.availability === "stale" ? " \xB7 stale" : m.source === "models-api" ? " \xB7 API candidate" : " \xB7 candidate" : "";
      html += '<option value="' + escapeHtml(m.id) + '"' + (m.id === normalized ? " selected" : "") + ">" + escapeHtml(m.id + rawSuffix) + "</option>";
    }
    if (normalized && !models.some(function(m2) {
      return m2.id === normalized;
    })) {
      html += '<option value="' + escapeHtml(normalized) + '" selected>' + escapeHtml(normalized) + "</option>";
    }
    return html;
  }
  function syncComposerModelSelect(session) {
    refreshAllChatModeTrios2();
  }
  var THINKING_LEVELS = [
    { id: "off", label: "auto", hint: "\u4F7F\u7528 provider / \u6A21\u578B\u9ED8\u8BA4\u601D\u8003\u6863\u4F4D" },
    { id: "standard", label: "low", hint: "Claude/Codex: low \xB7 OpenCode variant: low" },
    { id: "deep", label: "medium", hint: "Claude/Codex: medium \xB7 OpenCode variant: high" },
    { id: "max", label: "max", hint: "Claude: max \xB7 Codex: xhigh \xB7 OpenCode variant: max" }
  ];
  function codexThinkingId(effort) {
    if (effort === "low") return "standard";
    if (effort === "medium") return "deep";
    if (effort === "xhigh") return "max";
    return "codex:" + effort;
  }
  function getThinkingLevels(session) {
    if (getProviderForSession(session) !== "codex") return THINKING_LEVELS;
    var model = getEffectiveModel(session) || "default";
    var models = state.availableCodexModels || [];
    var info = models.find(function(item) {
      return item.id === model;
    }) || models.find(function(item) {
      return item.id === "default";
    });
    var efforts = info && Array.isArray(info.reasoningEfforts) ? info.reasoningEfforts : [];
    if (!efforts.length) return THINKING_LEVELS;
    var defaultEffort = info.defaultReasoningEffort || "";
    return [{
      id: "off",
      label: "auto",
      hint: defaultEffort ? "\u4F7F\u7528\u6A21\u578B\u9ED8\u8BA4\u6863\u4F4D\uFF08" + defaultEffort + "\uFF09" : "\u4F7F\u7528\u6A21\u578B\u9ED8\u8BA4\u6863\u4F4D"
    }].concat(efforts.map(function(level) {
      var effort = String(level.effort || "").toLowerCase();
      return {
        id: codexThinkingId(effort),
        label: effort,
        hint: level.description || effort
      };
    }).filter(function(level) {
      return level.label;
    }));
  }
  function getThinkingLabel(id, session) {
    var levels = getThinkingLevels(session);
    for (var i = 0; i < levels.length; i++) {
      if (levels[i].id === id) return levels[i].label;
    }
    if (typeof id === "string" && id.indexOf("codex:") === 0) return id.slice(6);
    for (var j = 0; j < THINKING_LEVELS.length; j++) {
      if (THINKING_LEVELS[j].id === id) return THINKING_LEVELS[j].label;
    }
    return "auto";
  }
  function getEffectiveThinking(session) {
    var pending = session && session.id ? getPendingSessionConfig(session.id) : null;
    if (pending && Object.prototype.hasOwnProperty.call(pending, "thinking")) return pending.thinking || "off";
    if (session && session.thinkingEffort) return session.thinkingEffort;
    if (state.chatThinking) return state.chatThinking;
    return "off";
  }
  function persistThinkingPreference(value) {
    state.chatThinking = value;
    try {
      localStorage.setItem("wand-thinking-effort", value);
    } catch (e) {
    }
  }
  function submitThinkingMutation(session, normalized, successMessage, prerequisite) {
    return enqueueSessionConfigMutation(session.id, function() {
      if (prerequisite && !prerequisite.ok) {
        return { error: prerequisite.error || "\u5207\u6362\u6A21\u578B\u5931\u8D25" };
      }
      return fetch("/api/sessions/" + encodeURIComponent(session.id) + "/thinking-effort", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        credentials: "same-origin",
        body: JSON.stringify({ thinkingEffort: normalized })
      }).then(function(res) {
        return res.json();
      });
    }).then(function(outcome) {
      if (applyLatestSessionConfigSnapshot(session.id, outcome) && typeof showToast === "function") {
        showToast(successMessage || "\u5DF2\u5207\u6362\u601D\u8003\u6DF1\u5EA6 \u2192 " + getThinkingCompactLabel(normalized, getSelectedSession4()), "success");
      }
    }).catch(function(failure) {
      if (failure && failure.latest) recoverLatestSessionConfigMutation(session.id, "\u5207\u6362\u601D\u8003\u6DF1\u5EA6\u5931\u8D25");
    });
  }
  function onChatThinkingChange(value) {
    var normalized = (value || "off").trim();
    var session = getSelectedSession4();
    var supported = getThinkingLevels(session).some(function(level) {
      return level.id === normalized;
    });
    if (!supported) {
      normalized = "off";
    }
    persistThinkingPreference(normalized);
    if (session) setPendingSessionConfig(session.id, "thinking", normalized);
    refreshAllChatModeTrios2();
    if (!session) return;
    submitThinkingMutation(session, normalized, "\u5DF2\u5207\u6362\u601D\u8003\u6DF1\u5EA6 \u2192 " + getThinkingCompactLabel(normalized, session));
  }
  function isAutoApproveImpliedByMode(session) {
    if (!session) return false;
    var m = session.mode;
    return m === "managed" || m === "full-access";
  }
  function renderAutoApproveChip(session) {
    if (!session) return "";
    if (isAutoApproveImpliedByMode(session)) return "";
    var enabled = !!session.autoApprovePermissions;
    return enabled ? '<button id="auto-approve-toggle" class="composer-pill composer-pill-chip auto-approve-indicator active" type="button" aria-pressed="true" aria-label="\u81EA\u52A8\u6279\u51C6\u5DF2\u542F\u7528\uFF0C\u70B9\u51FB\u5173\u95ED" title="\u81EA\u52A8\u6279\u51C6\u5DF2\u542F\u7528 \u2014 \u70B9\u51FB\u5173\u95ED">' + iconSvg("shieldCheck", { size: 12, strokeWidth: 1.7, cls: "composer-pill-icon" }) + '<span class="composer-pill-label">\u81EA\u52A8</span></button>' : '<button id="auto-approve-toggle" class="composer-pill composer-pill-chip auto-approve-indicator" type="button" aria-pressed="false" aria-label="\u81EA\u52A8\u6279\u51C6\u5DF2\u5173\u95ED\uFF0C\u70B9\u51FB\u5F00\u542F" title="\u81EA\u52A8\u6279\u51C6\u5DF2\u5173\u95ED \u2014 \u70B9\u51FB\u5F00\u542F">' + iconSvg("shield", { size: 12, strokeWidth: 1.7, cls: "composer-pill-icon" }) + '<span class="composer-pill-label">\u624B\u52A8</span></button>';
  }
  function fetchAvailableModels() {
    return fetch("/api/models", { credentials: "same-origin" }).then(function(res) {
      return res.json();
    }).then(function(data) {
      if (data && Array.isArray(data.models)) {
        state.availableModels = data.models;
        state.availableCodexModels = Array.isArray(data.codexModels) ? data.codexModels : [];
        state.availableOpenCodeModels = Array.isArray(data.opencodeModels) ? data.opencodeModels : [];
        state.availableGrokModels = Array.isArray(data.grokModels) ? data.grokModels : [];
        state.availableQoderModels = Array.isArray(data.qoderModels) ? data.qoderModels : [];
        syncComposerModelSelect(getSelectedSession4());
      }
      return data;
    }).catch(function() {
      return null;
    });
  }
  function getSelectedSession4() {
    if (!state.selectedId) return null;
    for (var i = 0; i < state.sessions.length; i++) {
      if (state.sessions[i].id === state.selectedId) return state.sessions[i];
    }
    return null;
  }
  function onChatModelChange(value) {
    var normalized = (value || "").trim();
    var session = getSelectedSession4();
    var provider = getProviderForSession(session);
    setChatModelForProvider(provider, normalized);
    if (session) setPendingSessionConfig(session.id, "model", normalized);
    var thinkingFallback = false;
    if (!getThinkingLevels(session).some(function(level) {
      return level.id === getEffectiveThinking(session);
    })) {
      thinkingFallback = true;
      persistThinkingPreference("off");
      if (session) setPendingSessionConfig(session.id, "thinking", "off");
    }
    refreshAllChatModeTrios2();
    if (!session) return;
    var prerequisite = { ok: false, error: "" };
    enqueueSessionConfigMutation(session.id, function() {
      return fetch("/api/sessions/" + encodeURIComponent(session.id) + "/model", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        credentials: "same-origin",
        body: JSON.stringify({ model: normalized || null })
      }).then(function(res) {
        return res.json();
      }).then(function(data) {
        prerequisite.ok = !!(data && data.id && !data.error);
        prerequisite.error = data && data.error ? data.error : "";
        return data;
      });
    }).then(function(outcome) {
      if (thinkingFallback) return;
      if (applyLatestSessionConfigSnapshot(session.id, outcome) && typeof showToast === "function") {
        var display2 = getModelDisplayLabel(normalized, getSelectedSession4() || session);
        var hint = session.provider === "codex" ? "\uFF08\u4E0B\u6B21\u5BF9\u8BDD\u751F\u6548\uFF09" : "";
        showToast("\u5DF2\u5207\u6362\u6A21\u578B \u2192 " + display2 + hint, "success");
      }
    }).catch(function(failure) {
      prerequisite.ok = false;
      if (!thinkingFallback && failure && failure.latest) {
        recoverLatestSessionConfigMutation(session.id, "\u5207\u6362\u6A21\u578B\u5931\u8D25");
      }
    });
    if (thinkingFallback) {
      var display = getModelDisplayLabel(normalized, session);
      submitThinkingMutation(session, "off", "\u5DF2\u5207\u6362\u6A21\u578B \u2192 " + display + "\uFF1B\u601D\u8003\u6DF1\u5EA6\u5DF2\u56DE\u843D\u4E3A\u81EA\u52A8", prerequisite);
    }
  }
  function onChatModeChange(value) {
    var normalized = getSafeModeForTool2(getPreferredTool(), (value || "default").trim());
    state.chatMode = normalized;
    refreshAllChatModeTrios2();
    var session = getSelectedSession4();
    if (!session || !session.id) {
      showToast && showToast("\u65B0\u4F1A\u8BDD\u6A21\u5F0F\uFF1A" + normalized, "info");
      return;
    }
    setPendingSessionConfig(session.id, "mode", normalized);
    enqueueSessionConfigMutation(session.id, function() {
      return fetch("/api/sessions/" + encodeURIComponent(session.id) + "/mode", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        credentials: "same-origin",
        body: JSON.stringify({ mode: normalized })
      }).then(function(res) {
        return res.json();
      });
    }).then(function(outcome) {
      if (applyLatestSessionConfigSnapshot(session.id, outcome) && typeof showToast === "function") {
        var updated = state.sessions.find(function(item) {
          return item.id === session.id;
        }) || session;
        var effectiveMode = updated.mode || normalized;
        state.chatMode = getSafeModeForTool2(updated.provider || getPreferredTool(), effectiveMode);
        refreshAllChatModeTrios2();
        var hint = session.provider === "codex" ? "\uFF08Codex \u56FA\u5B9A\u5168\u6743\u9650\uFF09" : "";
        showToast("\u5DF2\u5207\u6362\u6A21\u5F0F \u2192 " + effectiveMode + hint, "success");
      }
    }).catch(function(failure) {
      if (failure && failure.latest) recoverLatestSessionConfigMutation(session.id, "\u5207\u6362\u6A21\u5F0F\u5931\u8D25");
    });
  }
  function createStructuredSession(prompt, cwdOverride, modeOverride, worktreeEnabled) {
    var provider = getProviderKey(state.sessionTool);
    var modelPref = getChatModelForProvider(provider) || getConfigDefaultModelForProvider(provider);
    var thinkingPref = state.chatThinking || "off";
    var structuredRunner2 = provider === "codex" ? "codex-cli-exec" : provider === "opencode" ? "opencode-cli-run" : provider === "grok" ? "grok-cli-headless" : provider === "qoder" ? "qoder-cli-print" : state.config && state.config.structuredRunner === "sdk" ? "claude-sdk" : state.structuredRunner || "claude-cli-print";
    var payload = {
      cwd: cwdOverride || getEffectiveCwd(),
      mode: modeOverride || state.chatMode || state.config && state.config.defaultMode || "default",
      provider,
      runner: structuredRunner2,
      prompt: prompt || void 0,
      worktreeEnabled: worktreeEnabled === true,
      model: modelPref || void 0,
      thinkingEffort: thinkingPref,
      sessionSource: "interactive"
    };
    return fetch("/api/structured-sessions", {
      method: "POST",
      headers: { "Content-Type": "application/json" },
      credentials: "same-origin",
      body: JSON.stringify(payload)
    }).then(function(res) {
      return res.json();
    }).then(function(data) {
      if (data.error) {
        throw new Error(data.error);
      }
      state.selectedId = data.id;
      persistSelectedId();
      state.drafts[data.id] = "";
      resetChatRenderCache();
      updateSessionSnapshot(data);
      updateSessionsList();
      switchToSessionView2(data.id);
      subscribeToSession(data.id);
      return loadOutput(data.id).then(function() {
        return data;
      });
    });
  }
  function applyCurrentView() {
    var reactShellActive = isBrowserReactShellMounted();
    var hasSession = !!state.selectedId;
    var terminalContainer = document.getElementById("output");
    var chatContainer = document.getElementById("chat-output");
    var blankChat = document.getElementById("blank-chat");
    var selectedSession = state.sessions.find(function(s) {
      return s.id === state.selectedId;
    });
    var structured = isStructuredSession2(selectedSession);
    var showTerminal = hasSession && !structured && state.currentView === "terminal";
    var showChat = hasSession && (structured || state.currentView !== "terminal");
    if (structured) {
      state.currentView = "chat";
    } else if (!hasSession) {
      state.currentView = "terminal";
    }
    if (!reactShellActive && terminalContainer) {
      terminalContainer.classList.toggle("active", showTerminal);
      terminalContainer.classList.toggle("hidden", !showTerminal);
    }
    if (!reactShellActive && chatContainer) {
      chatContainer.classList.toggle("active", showChat);
      chatContainer.classList.toggle("hidden", !showChat);
    }
    if (!reactShellActive && blankChat) {
      blankChat.classList.toggle("hidden", hasSession);
    }
    if (chatContainer && showChat) {
      ensureChatMessagesContainer(chatContainer);
    }
    bindChatScrollListener();
    updateChatUnreadBubble();
    updateInteractiveControls3();
    notifyLegacyUiChange("shell:view");
  }
  function updateSessionSnapshot(snapshot7) {
    if (!snapshot7 || !snapshot7.id) return;
    var currentSession = state.sessions.find(function(session) {
      return session.id === snapshot7.id;
    }) || null;
    var normalizedSnapshot = normalizeStructuredSnapshot(snapshot7, currentSession);
    if (Array.isArray(normalizedSnapshot.messages) && typeof normalizedSnapshot.messageOffset === "number") {
      var mw = mergeWindowedMessages(currentSession, normalizedSnapshot.messages, normalizedSnapshot.messageOffset, normalizedSnapshot.messageTotal);
      normalizedSnapshot.messages = mw.messages;
      normalizedSnapshot.messageOffset = mw.messageOffset;
      normalizedSnapshot.messageTotal = mw.messageTotal;
    }
    var updated = false;
    var prevSession = null;
    state.sessions = state.sessions.map(function(session) {
      if (session.id !== normalizedSnapshot.id) return session;
      prevSession = session;
      updated = true;
      return Object.assign({}, session, normalizedSnapshot);
    });
    if (!updated) {
      state.sessions.unshift(normalizedSnapshot);
    }
    var updatedSession = state.sessions.find(function(session) {
      return session.id === normalizedSnapshot.id;
    }) || normalizedSnapshot;
    if (updatedSession && Array.isArray(updatedSession.queuedMessages) && normalizedSnapshot.id === state.selectedId) {
      syncStructuredQueueFromSession(updatedSession);
      saveStructuredQueue();
      updateStructuredQueueCounter();
    }
    if (normalizedSnapshot.id === state.selectedId) {
      reconcileInteractiveState();
      updateTaskDisplay();
    }
    if (normalizedSnapshot.status && normalizedSnapshot.status !== "running" && state.crossSessionQueue.length > 0) {
      setTimeout(flushCrossSessionQueue, 0);
    }
  }
  function subscribeToSession(sessionId) {
    if (!sessionId || !state.ws || state.ws.readyState !== WebSocket.OPEN) return;
    state.ws.send(JSON.stringify({ type: "subscribe", sessionId }));
  }
  function mergeServerSession(localSession, serverSession) {
    if (!localSession) return serverSession;
    var merged = Object.assign({}, localSession, serverSession);
    var localOutput = localSession.output || "";
    var serverOutput = serverSession.output || "";
    var keepLocalOutput = localOutput.length > serverOutput.length;
    var localStructuredState = localSession.structuredState || null;
    var serverStructuredState = serverSession.structuredState || null;
    var structuredSession = localSession.sessionKind === "structured" || serverSession.sessionKind === "structured";
    var localHasPendingAssistant = !!(localSession.messages && localSession.messages.length && (function() {
      var last = localSession.messages[localSession.messages.length - 1];
      return last && last.role === "assistant" && Array.isArray(last.content) && last.content.some(function(block) {
        return block && block.__processing;
      });
    })());
    var localMessages = Array.isArray(localSession.messages) ? structuredSession ? stripRenderOnlyStructuredMessages(localSession.messages) : localSession.messages : [];
    var serverMessages = Array.isArray(serverSession.messages) ? structuredSession ? stripRenderOnlyStructuredMessages(serverSession.messages) : serverSession.messages : [];
    var serverHasCompletedAssistant = serverMessages.length > 0 && (function() {
      var last = serverMessages[serverMessages.length - 1];
      return last && last.role === "assistant" && Array.isArray(last.content) && !last.content.some(function(b) {
        return b && b.__processing;
      });
    })();
    var preserveLocalStructuredProgress = localSession.sessionKind === "structured" && !!localStructuredState && localStructuredState.inFlight === true && (!serverStructuredState || serverStructuredState.inFlight !== true) && localHasPendingAssistant && !!localStructuredState.activeRequestId && !!serverStructuredState && !!serverStructuredState.activeRequestId && serverStructuredState.activeRequestId === localStructuredState.activeRequestId && !serverHasCompletedAssistant;
    var preserveLocalMessages = localMessages.length > serverMessages.length || localMessages.length > 0 && serverMessages.length > 0 && JSON.stringify(localMessages[localMessages.length - 1]) !== JSON.stringify(serverMessages[serverMessages.length - 1]) && JSON.stringify(localMessages).length > JSON.stringify(serverMessages).length;
    if (keepLocalOutput) {
      merged.output = localOutput;
    }
    if (preserveLocalStructuredProgress) {
      merged.status = localSession.status || merged.status;
      merged.structuredState = Object.assign({}, serverStructuredState || {}, localStructuredState, { inFlight: true });
      merged.messages = localSession.messages;
    }
    if (preserveLocalMessages) {
      merged.messages = localMessages;
    }
    if (localSession.id === state.selectedId) {
      if (localSession.permissionBlocked && serverSession.permissionBlocked === false) {
      } else if (localSession.permissionBlocked && !serverSession.permissionBlocked) {
        merged.permissionBlocked = true;
      }
      if (localSession.pendingEscalation && !serverSession.pendingEscalation && serverSession.permissionBlocked !== false) {
        merged.pendingEscalation = localSession.pendingEscalation;
      }
      if (localSession.messages && localSession.messages.length > 0 && (!serverSession.messages || serverSession.messages.length === 0)) {
        merged.messages = localSession.messages;
      }
    }
    return merged;
  }
  function getPreferredMessages(session, fallbackOutput, allowFallback) {
    if (session && session.messages && session.messages.length > 0) {
      return session.messages;
    }
    if (session && session.sessionKind === "structured") {
      return [];
    }
    if (!allowFallback) {
      return [];
    }
    var output = typeof fallbackOutput === "string" ? fallbackOutput : session && session.output || "";
    if (!output) {
      return [];
    }
    return parseMessages(output, session && session.command);
  }
  function getPreferredSessionId(sessions) {
    if (!sessions || !sessions.length) return null;
    if (state.selectedId) {
      var stillExists = sessions.find(function(session) {
        return session.id === state.selectedId;
      });
      if (stillExists) return stillExists.id;
      return null;
    }
    var runningSession = sessions.find(function(session) {
      return session.status === "running";
    });
    if (runningSession) return runningSession.id;
    var recent = sessions.find(function(session) {
      return !session.archived;
    });
    return recent ? recent.id : sessions[0].id;
  }
  function loadSessions(options) {
    var opts = options || {};
    return fetch("/api/sessions", { credentials: "same-origin" }).then(function(res) {
      if (res.status === 401) {
        logout2();
        return;
      }
      return res.json();
    }).then(function(sessions) {
      var serverSessions = sessions || [];
      var sessionIds = new Set(serverSessions.map(function(s) {
        return s.id;
      }));
      Object.keys(state.drafts).forEach(function(id) {
        if (!sessionIds.has(id)) delete state.drafts[id];
      });
      state.sessions = serverSessions.map(function(serverSession) {
        var localSession = state.sessions.find(function(s) {
          return s.id === serverSession.id;
        });
        return mergeServerSession(localSession, serverSession);
      });
      var preferredSessionId = getPreferredSessionId(state.sessions);
      if (preferredSessionId !== void 0) {
        state.selectedId = preferredSessionId;
      }
      restoreStructuredQueue();
      updateStructuredQueueCounter();
      state.bootstrapping = false;
      persistSelectedId();
      if (!isBrowserReactShellMounted()) {
        var listEl = document.getElementById("sessions-list");
        var rendered = renderSessionsListContent();
        if (listEl && listEl.innerHTML === rendered) {
          var countEl = document.getElementById("session-count");
          if (countEl) countEl.textContent = String(state.sessions.length);
        } else {
          if (listEl) {
            listEl.innerHTML = rendered;
            refreshTailMarqueePaths(listEl);
          }
          var countEl = document.getElementById("session-count");
          if (countEl) countEl.textContent = String(state.sessions.length);
        }
      }
      updateShellChrome();
      if (state.selectedId && state.gitStatusSessionId !== state.selectedId) {
        loadGitStatus(state.selectedId);
      }
      var reloadPromise = Promise.resolve();
      if (!opts.skipSelectedOutputReload && state.selectedId) {
        reloadPromise = loadOutput(state.selectedId);
      } else if (state.selectedId) {
        var sel = state.sessions.find(function(s) {
          return s.id === state.selectedId;
        });
        if (isStructuredSession2(sel)) {
          resetChatRenderCache({ preserveStickState: true });
          scheduleChatRender3(true);
        }
      }
      return reloadPromise.then(function() {
        if (state.crossSessionQueue.length > 0) {
          flushCrossSessionQueue();
        }
        renderCrossSessionQueue();
        _syncWakeLock();
      });
    }).catch(function(e) {
      var message = e && e.message || "";
      var isTransientAbort = message === "Failed to fetch" || message === "NetworkError when attempting to fetch resource." || message === "Load failed" || /aborted|aborterror|networkerror|failed to fetch/i.test(message);
      if (!isTransientAbort) {
        console.error("[wand] loadSessions failed:", e);
      }
    });
  }
  var _sessionListUpdateTimer = null;
  function scheduleSessionListUpdate() {
    if (_sessionListUpdateTimer) return;
    _sessionListUpdateTimer = setTimeout(function() {
      _sessionListUpdateTimer = null;
      updateSessionsList();
    }, 200);
  }
  function updateSessionsList() {
    if (!isBrowserReactShellMounted()) {
      var listEl = document.getElementById("sessions-list");
      var countEl = document.getElementById("session-count");
      if (listEl) {
        listEl.innerHTML = renderSessionsListContent();
        refreshTailMarqueePaths(listEl);
      }
      if (countEl) countEl.textContent = String(state.sessions.length);
    }
    notifyLegacyUiChange("sessions:update");
    if (typeof hideCollapsedTileBubble === "function") hideCollapsedTileBubble();
    updateShellChrome();
    if (state.crossSessionQueue.length > 0) renderCrossSessionQueue();
  }
  function updateShellChrome() {
    var reactShellActive = isBrowserReactShellMounted();
    var selectedSession = state.sessions.find(function(s) {
      return s.id === state.selectedId;
    });
    if (!selectedSession) {
      setTerminalInteractive(false);
      hideMiniKeyboard();
      closeKeyboardPopup();
    }
    var terminalTitle = selectedSession ? shortCommand(selectedSession.command) : "Wand";
    var terminalInfo = selectedSession ? getSessionStatusLabel(selectedSession) : "\u5F00\u59CB\u5BF9\u8BDD";
    var summaryEl = document.querySelector(".session-summary-value");
    var titleEl = document.getElementById("terminal-title");
    var infoEl = document.getElementById("terminal-info");
    var topbarTitleEl = document.querySelector(".topbar-session-title, .topbar-tagline");
    if (!reactShellActive && topbarTitleEl && selectedSession) {
      topbarTitleEl.classList.remove("topbar-tagline");
      topbarTitleEl.classList.add("topbar-session-title");
      topbarTitleEl.textContent = selectedSession.title || shortCommand(selectedSession.command);
      topbarTitleEl.setAttribute("title", selectedSession.description || selectedSession.command || "");
    }
    var blankChat = document.getElementById("blank-chat");
    var terminalContainer = document.getElementById("output");
    var chatContainer = document.getElementById("chat-output");
    var stopBtn = document.getElementById("stop-button");
    if (summaryEl && summaryEl.textContent !== terminalTitle) summaryEl.textContent = terminalTitle;
    if (titleEl && titleEl.textContent !== terminalTitle) titleEl.textContent = terminalTitle;
    if (infoEl) infoEl.textContent = selectedSession ? terminalInfo + " \xB7 " + getSessionKindDescription(selectedSession) : terminalInfo;
    var kindEl = document.getElementById("session-kind-display");
    var kindText = selectedSession ? getSessionKindLabel(selectedSession) : "\u7EC8\u7AEF";
    if (kindEl && kindEl.textContent !== kindText) kindEl.textContent = kindText;
    updateAutoApproveIndicator();
    if (!state.terminal && terminalContainer && selectedSession) {
      initTerminal2();
    }
    if (state.terminal && terminalContainer && !terminalContainer.contains(state.terminal.element)) {
      teardownTerminal();
      initTerminal2();
    }
    if (!selectedSession) {
      state.terminalSessionId = null;
      state.terminalOutput = "";
      state.terminalOutputMarker = 0;
    }
    if (state.terminal && selectedSession && state.currentView === "terminal") {
      maybeScrollTerminalToBottom("view");
    }
    var inputPanel = document.querySelector(".input-panel");
    if (selectedSession) {
      if (!reactShellActive && blankChat) blankChat.classList.add("hidden");
      if (!reactShellActive && terminalContainer) terminalContainer.classList.remove("hidden");
      if (!reactShellActive && chatContainer) chatContainer.classList.remove("hidden");
      if (!reactShellActive && inputPanel) inputPanel.classList.remove("hidden");
    } else {
      if (!reactShellActive && blankChat) blankChat.classList.remove("hidden");
      if (!reactShellActive && terminalContainer) terminalContainer.classList.add("hidden");
      if (!reactShellActive && chatContainer) chatContainer.classList.add("hidden");
      if (stopBtn) stopBtn.classList.add("hidden");
      if (!reactShellActive && inputPanel) inputPanel.classList.add("hidden");
    }
    syncComposerModeSelect();
    syncComposerModelSelect(getSelectedSession4());
    applyCurrentView();
    reconcileInteractiveState();
    notifyLegacyUiChange("shell:chrome");
  }
  function loadOutput(id) {
    if (state.chatRenderTimer) {
      clearTimeout(state.chatRenderTimer);
      state.chatRenderTimer = null;
    }
    var sess = state.sessions.find(function(s) {
      return s.id === id;
    });
    var url = "/api/sessions/" + id;
    if (shouldRequestChatFormat3(sess)) {
      url += "?format=chat";
    }
    return fetch(url, { credentials: "same-origin" }).then(function(res) {
      return res.json();
    }).then(function(data) {
      if (data.error) {
        if (state.selectedId === id) {
          state.selectedId = null;
          persistSelectedId();
        }
        loadSessions();
        return;
      }
      updateSessionSnapshot(data);
      updateShellChrome();
      if (state.terminal && id === state.selectedId && data.output !== void 0) {
        var wsLikelyTakingOver = !!state.ws && (state.ws.readyState === WebSocket.OPEN || state.ws.readyState === WebSocket.CONNECTING);
        if (!wsLikelyTakingOver) {
          syncTerminalBuffer(id, data.output, { mode: "append" });
          ensureTerminalFit("session-switch", { forceReplay: true });
        } else {
          ensureTerminalFit("session-switch");
        }
      }
      var selectedSession = state.sessions.find(function(s) {
        return s.id === id;
      });
      state.currentMessages = buildMessagesForRender(selectedSession, getPreferredMessages(selectedSession, data.output, false));
      renderChat(false);
    });
  }
  var _fetchingEarlierMessages = false;
  function fetchEarlierMessages() {
    if (_fetchingEarlierMessages) return false;
    var id = state.selectedId;
    if (!id) return false;
    var sess = state.sessions.find(function(s) {
      return s.id === id;
    });
    if (!sess) return false;
    var offset4 = typeof sess.messageOffset === "number" ? sess.messageOffset : 0;
    if (offset4 <= 0) return false;
    var pageSize = 40;
    var newOffset = Math.max(0, offset4 - pageSize);
    var limit = offset4 - newOffset;
    _fetchingEarlierMessages = true;
    fetch(
      "/api/sessions/" + encodeURIComponent(id) + "/messages?offset=" + newOffset + "&limit=" + limit,
      { credentials: "same-origin" }
    ).then(function(res) {
      return res.json();
    }).then(function(data) {
      if (data && Array.isArray(data.messages) && sess.messageOffset === offset4) {
        var existing = Array.isArray(sess.messages) ? sess.messages : [];
        sess.messages = data.messages.concat(existing);
        sess.messageOffset = newOffset;
        if (typeof data.total === "number") sess.messageTotal = data.total;
        if (id === state.selectedId) {
          state.currentMessages = buildMessagesForRender(sess, getPreferredMessages(sess, sess.output, false));
          state.chatRenderedCount = state.currentMessages.length;
          renderChat(true);
        }
      }
    }).catch(function() {
    }).finally(function() {
      _fetchingEarlierMessages = false;
    });
    return true;
  }
  function selectSession(id) {
    var foundSession = state.sessions.find(function(item) {
      return item.id === id;
    });
    if (!foundSession) {
      return;
    }
    if (state.selectedId !== id) {
      teardownTerminal();
    }
    state.selectedId = id;
    persistSelectedId();
    state.toolContentCache = {};
    state.messageQueue = [];
    state.pendingMessages = [];
    syncStructuredQueueFromSession(foundSession);
    restoreStructuredQueue();
    updateStructuredQueueCounter();
    resetChatRenderCache();
    state.currentMessages = [];
    if (state.chatRenderTimer) {
      clearTimeout(state.chatRenderTimer);
      state.chatRenderTimer = null;
    }
    var todoEl = document.getElementById("todo-progress");
    if (todoEl) todoEl.classList.add("hidden");
    var staleStatusBar = document.querySelector(".structured-status-bar");
    if (staleStatusBar) staleStatusBar.remove();
    if (state._statusBarTimerId) {
      clearInterval(state._statusBarTimerId);
      state._statusBarTimerId = null;
    }
    state._statusBarStartTime = 0;
    var staleComposer = document.querySelector(".input-composer");
    if (staleComposer) staleComposer.classList.remove("in-flight");
    var session = foundSession;
    state.preferredCommand = getPreferredTool();
    state.chatMode = getSafeModeForTool2("claude", session && session.mode ? session.mode : state.chatMode);
    if (state.terminalInteractive && session && session.status !== "running") {
      setTerminalInteractive(false);
    }
    updateSessionsList();
    switchToSessionView2(id);
    if (state.filePanelOpen) {
      updateFilePanelCwd(session);
      refreshFileExplorer();
    }
    loadOutput(id).then(function() {
      focusInputBox2(true);
    });
    subscribeToSession(id);
    state.gitStatus = null;
    state.gitStatusSessionId = null;
    updateTopbarGitBadge();
    loadGitStatus(id, { force: true });
  }
  function goHome() {
    if (!state.selectedId) return;
    state.selectedId = null;
    state.currentTask = null;
    state.currentMessages = [];
    state.gitStatus = null;
    state.gitStatusSessionId = null;
    persistSelectedId();
    resetChatRenderCache();
    dismissDrawerIfOverlay();
    updateSessionsList();
  }
  function updatePinState() {
    if (isBrowserReactShellMounted()) {
      notifyLegacyUiChange("layout:pin");
      return;
    }
    var drawer = document.getElementById("sessions-drawer");
    var mainLayout = document.querySelector(".main-layout");
    var isMobile = isMobileLayout();
    var isCollapsed = !!state.sidebarPinned && !!state.sidebarCollapsed;
    var isAnchored = isCollapsed || !isMobile && (!!state.sidebarPinned || !!state.sessionsDrawerOpen);
    if (drawer) {
      drawer.classList.toggle("pinned", isAnchored);
      drawer.classList.toggle("collapsed", isCollapsed);
    }
    if (mainLayout) {
      mainLayout.classList.toggle("sidebar-pinned", isAnchored);
      mainLayout.classList.toggle("sidebar-collapsed", isCollapsed);
    }
    var pinBtn = document.getElementById("sidebar-pin-btn");
    if (pinBtn) {
      pinBtn.classList.toggle("pinned", !!state.sidebarPinned);
      pinBtn.title = state.sidebarPinned ? "\u5DF2\u56FA\u5B9A\u5E38\u9A7B\uFF08\u70B9\u51FB\u89E3\u9664\u9501\u5B9A\uFF09" : "\u56FA\u5B9A\u4FA7\u680F\u5E38\u9A7B";
      pinBtn.setAttribute("aria-label", state.sidebarPinned ? "\u89E3\u9664\u56FA\u5B9A\u5E38\u9A7B" : "\u56FA\u5B9A\u4FA7\u680F\u5E38\u9A7B");
      pinBtn.setAttribute("aria-pressed", state.sidebarPinned ? "true" : "false");
    }
  }
  function updateDrawerState() {
    if (isBrowserReactShellMounted()) {
      notifyLegacyUiChange("layout:drawer");
      return;
    }
    var drawer = document.getElementById("sessions-drawer");
    var backdrop = document.getElementById("sessions-drawer-backdrop");
    var mainLayout = document.querySelector(".main-layout");
    if (drawer) {
      drawer.classList.toggle("open", state.sessionsDrawerOpen);
    }
    if (backdrop) {
      backdrop.classList.toggle("open", shouldShowSessionsBackdrop());
    }
    if (mainLayout) {
      mainLayout.classList.toggle("sidebar-open", state.sessionsDrawerOpen);
    }
    var toggleBtn = document.getElementById("sessions-toggle-button");
    if (toggleBtn) {
      toggleBtn.classList.toggle("active", state.sessionsDrawerOpen);
    }
    updatePinState();
  }
  function toggleSessionsDrawer() {
    var isMobile = isMobileLayout();
    if (!isMobile) {
      var willOpen = state.sidebarPinned ? false : !state.sessionsDrawerOpen;
      state.sessionsDrawerOpen = willOpen;
      if (willOpen) {
        state.sidebarCollapsed = false;
        writeStoredBoolean("wand-sidebar-collapsed", false);
      }
      writeStoredBoolean("wand-sidebar-open", state.sessionsDrawerOpen);
      updateLayoutState();
      scheduleTerminalRefitAfterPaddingTransition();
      return;
    }
    state.sessionsDrawerOpen = !state.sessionsDrawerOpen;
    writeStoredBoolean("wand-sidebar-open", state.sessionsDrawerOpen);
    if (state.sessionsDrawerOpen) {
      state.filePanelOpen = false;
      try {
        localStorage.setItem("wand-file-panel-open", "false");
      } catch (e) {
      }
    }
    updateLayoutState();
  }
  function closeSessionsDrawer2() {
    var isMobile = isMobileLayout();
    if (!isMobile) {
      if (!state.sidebarPinned && !state.sessionsDrawerOpen) return;
      closeSwipedItem();
      state.sidebarPinned = false;
      state.sessionsDrawerOpen = false;
      writeStoredBoolean("wand-sidebar-pinned", false);
      writeStoredBoolean("wand-sidebar-open", false);
      updateLayoutState();
      scheduleTerminalRefitAfterPaddingTransition();
      return;
    }
    if (!state.sessionsDrawerOpen) return;
    closeSwipedItem();
    state.sessionsDrawerOpen = false;
    writeStoredBoolean("wand-sidebar-open", false);
    updateLayoutState();
  }
  function closeTransientSessionsDrawer() {
    if (isMobileLayout()) {
      closeSessionsDrawer2();
      return;
    }
    if (state.sidebarPinned || state.sidebarCollapsed || !state.sessionsDrawerOpen) return;
    closeSwipedItem();
    state.sessionsDrawerOpen = false;
    writeStoredBoolean("wand-sidebar-open", false);
    updateLayoutState();
    scheduleTerminalRefitAfterPaddingTransition();
  }
  function setPlusPopoverOpen(open) {
    var popover = document.getElementById("composer-plus-popover");
    var btn = document.getElementById("attach-btn");
    state.plusPopoverOpen = !!open;
    if (popover) popover.classList.toggle("hidden", !open);
    if (btn) {
      btn.classList.toggle("active", !!open);
      btn.setAttribute("aria-expanded", open ? "true" : "false");
    }
  }
  function togglePlusPopover() {
    setPlusPopoverOpen(!state.plusPopoverOpen);
  }
  function closePlusPopover() {
    if (state.plusPopoverOpen) setPlusPopoverOpen(false);
  }
  function dismissDrawerIfOverlay() {
    if (isMobileLayout() && state.sessionsDrawerOpen) {
      closeSessionsDrawer2();
    }
  }
  function scheduleTerminalRefitAfterPaddingTransition() {
    var mainLayout = document.querySelector(".main-layout");
    if (mainLayout) {
      var onEnd = function(e) {
        if (e.propertyName === "padding-left") {
          mainLayout.removeEventListener("transitionend", onEnd);
          scheduleTerminalResize(true);
        }
      };
      mainLayout.addEventListener("transitionend", onEnd);
    }
    setTimeout(function() {
      scheduleTerminalResize(true);
    }, 350);
  }
  var collapsedTileBubbleEl = null;
  function ensureCollapsedTileBubble() {
    if (collapsedTileBubbleEl && document.body.contains(collapsedTileBubbleEl)) {
      return collapsedTileBubbleEl;
    }
    collapsedTileBubbleEl = document.createElement("div");
    collapsedTileBubbleEl.className = "sidebar-tile-bubble";
    collapsedTileBubbleEl.setAttribute("role", "tooltip");
    document.body.appendChild(collapsedTileBubbleEl);
    return collapsedTileBubbleEl;
  }
  function hideCollapsedTileBubble() {
    if (collapsedTileBubbleEl) collapsedTileBubbleEl.classList.remove("visible");
  }
  function showCollapsedTileBubble(tile, text5) {
    if (!text5) {
      hideCollapsedTileBubble();
      return;
    }
    var bubble = ensureCollapsedTileBubble();
    bubble.textContent = text5.length > 400 ? text5.slice(0, 400) + "\u2026" : text5;
    var rect = tile.getBoundingClientRect();
    bubble.classList.add("visible");
    var bubbleRect = bubble.getBoundingClientRect();
    var centerY = rect.top + rect.height / 2;
    var top = centerY - bubbleRect.height / 2;
    var minTop = 8;
    var maxTop = window.innerHeight - bubbleRect.height - 8;
    if (top < minTop) top = minTop;
    if (top > maxTop) top = Math.max(minTop, maxTop);
    bubble.style.left = rect.right + 12 + "px";
    bubble.style.top = top + "px";
    bubble.style.setProperty("--bubble-tail-y", centerY - top + "px");
  }
  function getCollapsedTileBubbleText(tile) {
    if (tile.dataset.collapsedSessionId) {
      var session = state.sessions.find(function(s) {
        return s.id === tile.dataset.collapsedSessionId;
      });
      if (!session) return "";
      var latest = getSessionLatestUserText(session);
      if (latest) return latest;
      return session.summary || session.command || "";
    }
    if (tile.dataset.collapsedHistoryId) {
      var hist = state.claudeHistory.find(function(s) {
        return s.claudeSessionId === tile.dataset.collapsedHistoryId;
      });
      if (hist && hist.firstUserMessage) return hist.firstUserMessage;
    }
    return "";
  }
  function handleCollapsedTileHover(event) {
    var target = event.target;
    if (!target || !(target instanceof Element)) return;
    var tile = target.closest(".sidebar-collapsed-tile");
    if (!tile) {
      hideCollapsedTileBubble();
      return;
    }
    var text5 = getCollapsedTileBubbleText(tile);
    if (!text5) {
      hideCollapsedTileBubble();
      return;
    }
    showCollapsedTileBubble(tile, text5);
  }
  function handleCollapsedTileLeave(event) {
    var related = event.relatedTarget;
    if (related && related instanceof Element && related.closest(".sidebar-collapsed-tile")) {
      return;
    }
    hideCollapsedTileBubble();
  }
  function toggleSidebarCollapsed() {
    var isMobile = isMobileLayout();
    if (!state.sidebarPinned) {
      state.sidebarPinned = true;
      writeStoredBoolean("wand-sidebar-pinned", true);
    }
    state.sidebarCollapsed = !state.sidebarCollapsed;
    writeStoredBoolean("wand-sidebar-collapsed", state.sidebarCollapsed);
    if (state.sidebarCollapsed) {
      state.sessionsDrawerOpen = false;
    } else if (isMobile) {
      state.sidebarPinned = false;
      state.sessionsDrawerOpen = true;
      writeStoredBoolean("wand-sidebar-pinned", false);
    } else {
      state.sessionsDrawerOpen = true;
    }
    writeStoredBoolean("wand-sidebar-open", state.sessionsDrawerOpen);
    updateLayoutState();
    updateSessionsList();
    updateSidebarCollapseButton();
    hideCollapsedTileBubble();
    scheduleTerminalRefitAfterPaddingTransition();
  }
  function toggleSidebarPin() {
    if (isMobileLayout()) return;
    state.sidebarPinned = !state.sidebarPinned;
    state.sessionsDrawerOpen = true;
    writeStoredBoolean("wand-sidebar-pinned", state.sidebarPinned);
    writeStoredBoolean("wand-sidebar-open", true);
    updateLayoutState();
    scheduleTerminalRefitAfterPaddingTransition();
  }
  function updateSidebarCollapseButton() {
    if (isBrowserReactShellMounted()) {
      notifyLegacyUiChange("layout:collapse-button");
      return;
    }
    var btn = document.getElementById("sidebar-collapse-btn");
    if (!btn) return;
    var isCollapsed = !!state.sidebarPinned && !!state.sidebarCollapsed;
    btn.classList.toggle("collapsed", isCollapsed);
    btn.title = isCollapsed ? "\u5C55\u5F00\u4FA7\u680F" : "\u6536\u8D77\u4E3A\u7A84\u6761";
    btn.setAttribute("aria-label", isCollapsed ? "\u5C55\u5F00\u4FA7\u680F" : "\u6536\u8D77\u4E3A\u7A84\u6761");
    btn.innerHTML = isCollapsed ? '<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><polyline points="10 6 16 12 10 18"/><line x1="20" y1="5" x2="20" y2="19"/></svg>' : '<svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.4" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true"><polyline points="14 6 8 12 14 18"/><line x1="4" y1="5" x2="4" y2="19"/></svg>';
  }
  function positionSidebarOverflowMenu(menu) {
    if (!menu) return;
    menu.style.left = "";
    menu.style.right = "";
    var parent = menu.offsetParent || menu.parentElement;
    if (!parent) return;
    var margin = 8;
    var parentRect = parent.getBoundingClientRect();
    var rect = menu.getBoundingClientRect();
    var vw = window.innerWidth;
    if (rect.left < margin) {
      menu.style.right = "auto";
      menu.style.left = margin - parentRect.left + "px";
    } else if (rect.right > vw - margin) {
      menu.style.left = "auto";
      menu.style.right = parentRect.right - (vw - margin) + "px";
    }
  }
  function closeControllerForCompetingOverlay(controller) {
    return !controller?.isOpen() || controller.closeIfOpen();
  }
  function openSessionModalNow() {
    if (!closeControllerForCompetingOverlay(folderPickerController) || !closeControllerForCompetingOverlay(quickCommitController) || !closeControllerForCompetingOverlay(worktreeMergeController) || !closeControllerForCompetingOverlay(window.__wandReactSettings)) return;
    var reactNewSession = window.__wandReactNewSession;
    if (reactNewSession && typeof reactNewSession.open === "function") {
      try {
        if (reactNewSession.open()) return;
      } catch (error) {
        console.warn("[wand] React new-session host unavailable", error);
      }
    }
    try {
      var queryDisabled = new URL(window.location.href).searchParams.get("reactUi") === "0";
      if (queryDisabled) {
        showToast("\u65B0\u5EFA\u4F1A\u8BDD\u754C\u9762\u5DF2\u88AB ?reactUi=0 \u7981\u7528\uFF1B\u79FB\u9664\u8BE5\u53C2\u6570\u5E76\u5237\u65B0\u540E\u5373\u53EF\u4F7F\u7528\u3002", "error");
        return;
      }
      if (window.localStorage.getItem("wand.reactUi.enabled") === "false") {
        window.localStorage.setItem("wand.reactUi.enabled", "true");
        showToast("\u6B63\u5728\u542F\u7528\u65B0\u5EFA\u4F1A\u8BDD\u754C\u9762\u2026", "info");
        window.setTimeout(function() {
          window.location.reload();
        }, 120);
        return;
      }
    } catch (_error) {
    }
    showToast("\u65B0\u5EFA\u4F1A\u8BDD\u754C\u9762\u672A\u80FD\u542F\u52A8\uFF0C\u8BF7\u5237\u65B0\u9875\u9762\u540E\u91CD\u8BD5\u3002", "error");
  }
  function openSessionModal() {
    if (!prepareFilePreviewForCompetingOverlay(openSessionModalNow)) return;
    openSessionModalNow();
  }
  function closeSessionModal() {
    var reactNewSession = window.__wandReactNewSession;
    if (reactNewSession && typeof reactNewSession.closeIfOpen === "function") {
      try {
        if (reactNewSession.closeIfOpen()) return;
      } catch (_error) {
      }
    }
  }
  function openWorktreeMergeModal(sessionId) {
    openWorktreeMergeForSession(sessionId, "merge");
  }
  function closeWorktreeMergeModal() {
    return closeWorktreeMergeFromLegacy();
  }
  function retryWorktreeCleanup(sessionId) {
    openWorktreeMergeForSession(sessionId, "cleanup");
  }
  function openSettingsModalNow() {
    if (!closeControllerForCompetingOverlay(folderPickerController) || !closeControllerForCompetingOverlay(quickCommitController) || !closeControllerForCompetingOverlay(worktreeMergeController) || !closeControllerForCompetingOverlay(window.__wandReactNewSession)) return;
    var controller = window.__wandReactSettings;
    if (controller && typeof controller.open === "function") {
      var settingsTrigger = document.getElementById("settings-button");
      if (settingsTrigger) settingsTrigger.focus({ preventScroll: true });
      controller.open("about");
      return;
    }
    try {
      var queryDisabled = new URL(window.location.href).searchParams.get("reactUi") === "0";
      if (queryDisabled) {
        showToast("\u8BBE\u7F6E\u754C\u9762\u5DF2\u88AB ?reactUi=0 \u7981\u7528\uFF1B\u79FB\u9664\u8BE5\u53C2\u6570\u5E76\u5237\u65B0\u540E\u5373\u53EF\u4F7F\u7528\u3002", "error");
        return;
      }
      if (window.localStorage.getItem("wand.reactUi.enabled") === "false") {
        window.localStorage.setItem("wand.reactUi.enabled", "true");
        showToast("\u6B63\u5728\u542F\u7528\u8BBE\u7F6E\u754C\u9762\u2026", "info");
        window.setTimeout(function() {
          window.location.reload();
        }, 120);
        return;
      }
    } catch (_error) {
    }
    showToast("\u8BBE\u7F6E\u754C\u9762\u672A\u80FD\u542F\u52A8\uFF0C\u8BF7\u5237\u65B0\u9875\u9762\u540E\u91CD\u8BD5\u3002", "error");
  }
  function openSettingsModal() {
    if (!prepareFilePreviewForCompetingOverlay(openSettingsModalNow)) return;
    openSettingsModalNow();
  }
  function closeSettingsModal() {
    var controller = window.__wandReactSettings;
    if (controller && typeof controller.closeIfOpen === "function") controller.closeIfOpen();
  }
  function copyToClipboard(text5, triggerBtn, successCallback) {
    if (!text5) return;
    function onSuccess() {
      _vibrate("light");
      if (successCallback) {
        successCallback();
        return;
      }
      if (triggerBtn) {
        var orig = triggerBtn.textContent;
        triggerBtn.textContent = "\u5DF2\u590D\u5236";
        setTimeout(function() {
          triggerBtn.textContent = orig;
        }, 1500);
      }
    }
    if (_hasNativeBridge && typeof WandNative.copyToClipboard === "function") {
      try {
        if (WandNative.copyToClipboard(text5) === "ok") {
          onSuccess();
          return;
        }
      } catch (_e) {
      }
    }
    navigator.clipboard.writeText(text5).then(onSuccess).catch(function() {
      var ta = document.createElement("textarea");
      ta.value = text5;
      ta.style.position = "fixed";
      ta.style.opacity = "0";
      document.body.appendChild(ta);
      ta.select();
      document.execCommand("copy");
      document.body.removeChild(ta);
      onSuccess();
    });
  }
  function checkApkAutoUpdate() {
    fetch("/api/auto-update", { credentials: "same-origin" }).then(function(res) {
      return res.json();
    }).then(function(autoData) {
      if (!autoData.apk) return;
      return fetch("/api/android-apk-update?currentVersion=" + encodeURIComponent(_apkVersion), { credentials: "same-origin" }).then(function(res) {
        return res.json();
      }).then(function(data) {
        if (!data.updateAvailable || !data.downloadUrl) return;
        try {
          WandNative.downloadUpdate(data.downloadUrl, data.fileName || "wand-update.apk", data.source || "local");
        } catch (_e) {
        }
      });
    }).catch(function() {
    });
  }
  function checkDmgAutoUpdate() {
    if (!_macAppVersion) return;
    fetch("/api/auto-update", { credentials: "same-origin" }).then(function(res) {
      return res.json();
    }).then(function(autoData) {
      if (!autoData.dmg) return;
      return fetch("/api/macos-dmg-update?currentVersion=" + encodeURIComponent(_macAppVersion), { credentials: "same-origin" }).then(function(res) {
        return res.json();
      }).then(function(data) {
        if (!data.updateAvailable || !data.downloadUrl) return;
        try {
          WandNative.downloadUpdate(data.downloadUrl, data.fileName || "wand-update.dmg", data.source || "local");
        } catch (_e) {
        }
      });
    }).catch(function() {
    });
  }
  function withTerminalDimensions(body) {
    if (!body || typeof body !== "object") return body;
    if (!state.terminal) return body;
    try {
      if (typeof state.terminal.remeasure === "function") {
        state.terminal.remeasure();
      }
    } catch (e) {
    }
    var cols = state.terminal.cols;
    var rows = state.terminal.rows;
    if (typeof cols === "number" && typeof rows === "number" && Number.isFinite(cols) && Number.isFinite(rows) && cols > 0 && rows > 0) {
      body.cols = cols;
      body.rows = rows;
    }
    return body;
  }
  function ensureTerminalReady() {
    if (state.terminal && state.terminal.cols) return Promise.resolve();
    return new Promise(function(resolve) {
      var done = false;
      var settle = function() {
        if (!done) {
          done = true;
          resolve();
        }
      };
      var hardTimeout = setTimeout(settle, 2e3);
      try {
        initTerminal2();
      } catch (e) {
      }
      requestAnimationFrame(function() {
        requestAnimationFrame(function() {
          if (state.terminal && state.terminal.cols) {
            clearTimeout(hardTimeout);
            settle();
          }
        });
      });
    });
  }
  function quickStartSession() {
    var command = getPreferredTool();
    var defaultCwd2 = getEffectiveCwd();
    var defaultMode = getSafeModeForTool2(command, state.config && state.config.defaultMode ? state.config.defaultMode : "default");
    state.preferredCommand = command;
    state.chatMode = getSafeModeForTool2(command, state.chatMode);
    return ensureTerminalReady().then(function() {
      return fetch("/api/commands", {
        method: "POST",
        headers: { "Content-Type": "application/json" },
        credentials: "same-origin",
        body: JSON.stringify(withTerminalDimensions({ command, provider: command, cwd: defaultCwd2, mode: defaultMode, sessionSource: "interactive" }))
      });
    }).then(function(res) {
      return res.json();
    }).then(function(data) {
      if (data.error) {
        showToast(data.error, "error");
        return;
      }
      state.selectedId = data.id;
      persistSelectedId();
      state.drafts[data.id] = "";
      resetChatRenderCache();
      return refreshAll();
    }).then(function() {
      focusInputBox2(true);
    }).catch(function() {
      showToast("\u65E0\u6CD5\u542F\u52A8\u4F1A\u8BDD\u3002", "error");
    });
  }
  function handleInputBoxKeydown(event) {
    if (event.isComposing) return;
    if (shouldCaptureTerminalEvent(event)) {
      captureTerminalInput2(event);
      return;
    }
    if (event.key === "Enter") {
      if (event.shiftKey) {
        event.preventDefault();
        var inputBox = document.getElementById("input-box");
        if (inputBox) {
          var start = inputBox.selectionStart || 0;
          var current = inputBox.value;
          var newValue = current.slice(0, start) + String.fromCharCode(10) + current.slice(start);
          inputBox.value = newValue;
          inputBox.selectionStart = start + 1;
          inputBox.selectionEnd = start + 1;
          setDraftValue(newValue, true);
          autoResizeInput(inputBox);
        }
        return;
      }
      event.preventDefault();
      var interruptShortcut = !!(event.metaKey || event.ctrlKey);
      sendInputFromBox(interruptShortcut ? { interrupt: true } : void 0);
      return;
    }
    if (event.key === "Backspace") {
      setTimeout(function() {
        var inputBox3 = document.getElementById("input-box");
        if (inputBox3) {
          setDraftValue(inputBox3.value, true);
        }
      }, 0);
      return;
    }
    if (event.key === "Tab") {
      event.preventDefault();
      var inputBox = document.getElementById("input-box");
      if (inputBox) {
        var start = inputBox.selectionStart || 0;
        var current = inputBox.value;
        var newValue = current.slice(0, start) + String.fromCharCode(9) + current.slice(start);
        inputBox.value = newValue;
        setDraftValue(newValue, true);
      }
      return;
    }
    if (event.key === "Escape") {
      event.preventDefault();
      var escSess = getSelectedSession4();
      if (isStructuredSession2(escSess)) {
        if (escSess && escSess.structuredState && escSess.structuredState.inFlight) {
          stopSession();
        }
      } else {
        queueDirectInput4(getControlInput("escape"), "escape");
      }
      return;
    }
    if ((event.metaKey || event.ctrlKey) && event.key.toLowerCase() === "c") {
      var inputBoxC = document.getElementById("input-box");
      var hasSelectionC = inputBoxC && inputBoxC.selectionStart !== inputBoxC.selectionEnd || hasActiveTerminalSelection();
      if (hasSelectionC) {
        return;
      }
      var ccSess = getSelectedSession4();
      if (isStructuredSession2(ccSess)) {
        event.preventDefault();
        if (ccSess && ccSess.structuredState && ccSess.structuredState.inFlight) {
          stopSession();
        }
        return;
      }
      event.preventDefault();
      queueDirectInput4(getControlInput("ctrl_c"), "ctrl_c");
      return;
    }
    if ((event.metaKey || event.ctrlKey) && event.key.toLowerCase() === "d") {
      var inputBox2 = document.getElementById("input-box");
      var hasSelection2 = inputBox2 && inputBox2.selectionStart !== inputBox2.selectionEnd || hasActiveTerminalSelection();
      if (hasSelection2) {
        return;
      }
      var cdSess = getSelectedSession4();
      if (isStructuredSession2(cdSess)) {
        event.preventDefault();
        return;
      }
      event.preventDefault();
      queueDirectInput4(getControlInput("ctrl_d"), "ctrl_d");
      return;
    }
    if ((event.metaKey || event.ctrlKey) && event.key.toLowerCase() === "l") {
      event.preventDefault();
      var clSess = getSelectedSession4();
      if (isStructuredSession2(clSess)) {
        return;
      }
      queueDirectInput4(getControlInput("ctrl_l"), "ctrl_l");
      return;
    }
    if ((event.metaKey || event.ctrlKey) && event.key.toLowerCase() === "a") {
      return;
    }
    if ((event.metaKey || event.ctrlKey) && event.key.toLowerCase() === "v") {
      return;
    }
    if ((event.metaKey || event.ctrlKey) && event.key.toLowerCase() === "x") {
      var inputBox = document.getElementById("input-box");
      var hasSelection = inputBox && inputBox.selectionStart !== inputBox.selectionEnd;
      if (hasSelection) {
        return;
      }
      event.preventDefault();
      queueDirectInput4(String.fromCharCode(24), "ctrl_x");
      return;
    }
    if (event.key.length === 1 && !event.ctrlKey && !event.metaKey && !event.altKey) {
      setTimeout(function() {
        var inputBox3 = document.getElementById("input-box");
        if (inputBox3) {
          setDraftValue(inputBox3.value);
        }
      }, 0);
    }
  }
  var ATTACH_MAX_SIZE = 10 * 1024 * 1024;
  function formatFileSize2(bytes) {
    if (bytes < 1024) return bytes + " B";
    if (bytes < 1024 * 1024) return (bytes / 1024).toFixed(1) + " KB";
    return (bytes / (1024 * 1024)).toFixed(1) + " MB";
  }
  function isImageType(type) {
    return /^image\/(png|jpe?g|gif|webp|bmp|svg\+xml)/.test(type);
  }
  function addPendingAttachment(file) {
    if (!file) return;
    if (file.size > ATTACH_MAX_SIZE) {
      showToast("\u6587\u4EF6\u8FC7\u5927\uFF08\u4E0A\u9650 10 MB\uFF09: " + file.name, "error");
      return;
    }
    var entry = { file, name: file.name, size: file.size, previewUrl: null };
    if (isImageType(file.type)) {
      entry.previewUrl = URL.createObjectURL(file);
    }
    state.pendingAttachments.push(entry);
    renderAttachmentPreview();
  }
  function removePendingAttachment(index2) {
    var removed = state.pendingAttachments.splice(index2, 1);
    if (removed.length && removed[0].previewUrl) {
      URL.revokeObjectURL(removed[0].previewUrl);
    }
    renderAttachmentPreview();
  }
  function clearAttachments() {
    state.pendingAttachments.forEach(function(a) {
      if (a.previewUrl) URL.revokeObjectURL(a.previewUrl);
    });
    state.pendingAttachments = [];
    renderAttachmentPreview();
  }
  function renderAttachmentPreview() {
    var bar = document.getElementById("attachment-preview");
    if (!bar) return;
    var items = state.pendingAttachments;
    if (items.length === 0) {
      bar.classList.add("hidden");
      bar.innerHTML = "";
      return;
    }
    bar.classList.remove("hidden");
    var html = "";
    for (var i = 0; i < items.length; i++) {
      var a = items[i];
      var thumb = a.previewUrl ? '<img src="' + escapeHtml(a.previewUrl) + '" alt="">' : '<span class="att-icon">' + iconSvg("file", { size: 13, strokeWidth: 1.7 }) + "</span>";
      html += '<span class="attachment-pill" data-index="' + i + '">' + thumb + '<span class="att-name" title="' + escapeHtml(a.name) + '">' + escapeHtml(a.name) + '</span><span class="att-size">' + formatFileSize2(a.size) + '</span><button class="att-remove" data-index="' + i + '" title="\u79FB\u9664">\xD7</button></span>';
    }
    bar.innerHTML = html;
    bar.querySelectorAll(".att-remove").forEach(function(btn) {
      btn.addEventListener("click", function(e) {
        e.preventDefault();
        e.stopPropagation();
        removePendingAttachment(parseInt(btn.getAttribute("data-index"), 10));
      });
    });
  }
  function uploadAttachments(sessionId) {
    if (!state.pendingAttachments.length) return Promise.resolve([]);
    var formData = new FormData();
    state.pendingAttachments.forEach(function(a) {
      formData.append("files", a.file, a.name);
    });
    return fetch("/api/sessions/" + encodeURIComponent(sessionId) + "/upload", {
      method: "POST",
      body: formData,
      credentials: "same-origin"
    }).then(function(resp) {
      if (!resp.ok) return resp.json().then(function(e) {
        throw new Error(e.error || "\u4E0A\u4F20\u5931\u8D25");
      });
      return resp.json();
    }).then(function(data) {
      return data.files || [];
    });
  }
  function buildAttachmentPrefix(uploadedFiles) {
    if (!uploadedFiles || !uploadedFiles.length) return "";
    var paths = uploadedFiles.map(function(f) {
      return f.savedPath;
    });
    return "[\u9644\u4EF6\u5DF2\u4E0A\u4F20\uFF0C\u8BF7\u67E5\u770B\u4EE5\u4E0B\u6587\u4EF6:\n" + paths.join("\n") + "]\n\n";
  }
  function handleInteractiveTextInput(inputBox) {
    if (!state.terminalInteractive || !inputBox) return false;
    if (document.documentElement.classList.contains("is-wand-embed-terminal")) return false;
    var value = inputBox.value || "";
    if (!value) return false;
    queueDirectInput4(value, "interactive_text").catch(function() {
    });
    inputBox.value = "";
    autoResizeInput(inputBox);
    setDraftValue("", true);
    return true;
  }
  function handleInputPaste(event) {
    var items = event.clipboardData && event.clipboardData.items;
    if (items && !state.terminalInteractive) {
      for (var i = 0; i < items.length; i++) {
        if (items[i].type.indexOf("image/") === 0) {
          event.preventDefault();
          var file = items[i].getAsFile();
          if (file) addPendingAttachment(file);
          return;
        }
      }
    }
    var pasted = event.clipboardData && event.clipboardData.getData("text");
    if (!pasted) return;
    event.preventDefault();
    if (state.terminalInteractive) {
      queueDirectInput4(pasted, "paste").catch(function() {
      });
      return;
    }
    var inputBox = document.getElementById("input-box");
    if (inputBox) {
      var start = inputBox.selectionStart || 0;
      var end = inputBox.selectionEnd || 0;
      var current = inputBox.value;
      var newValue = current.slice(0, start) + pasted + current.slice(end);
      inputBox.value = newValue;
      setDraftValue(newValue);
    }
  }
  function setDraftValue(value, skipDom) {
    if (!state.selectedId) return;
    state.drafts[state.selectedId] = value;
    try {
      localStorage.setItem("wand-draft-" + state.selectedId, value);
    } catch (e) {
    }
    if (!skipDom) {
      var inputBox = document.getElementById("input-box");
      if (inputBox) inputBox.value = value;
    }
  }
  var promptOptimizeInFlight = false;
  function optimizePromptText() {
    if (promptOptimizeInFlight) return;
    var inputBox = document.getElementById("input-box");
    var btn = document.getElementById("prompt-optimize-btn");
    var composer = document.querySelector(".input-composer");
    if (!inputBox) return;
    var raw = (inputBox.value || "").trim();
    if (!raw) {
      if (typeof showToast === "function") showToast("\u8BF7\u5148\u8F93\u5165\u8981\u4F18\u5316\u7684\u5185\u5BB9\u3002", "info");
      inputBox.focus();
      return;
    }
    promptOptimizeInFlight = true;
    if (btn) {
      btn.classList.add("is-loading");
      btn.disabled = true;
      btn.setAttribute("title", "\u6B63\u5728\u4F18\u5316\u2026");
    }
    if (composer) composer.classList.add("is-optimizing");
    inputBox.setAttribute("aria-busy", "true");
    var prevReadOnly = inputBox.readOnly;
    inputBox.readOnly = true;
    var payload = { text: raw };
    if (state && state.selectedId) payload.sessionId = state.selectedId;
    fetch("/api/optimize-prompt", {
      method: "POST",
      credentials: "same-origin",
      headers: { "Content-Type": "application/json" },
      body: JSON.stringify(payload)
    }).then(function(res) {
      return res.json().then(function(data) {
        return { ok: res.ok, data };
      });
    }).then(function(result) {
      if (!result.ok) throw new Error(result.data && result.data.error || "\u63D0\u793A\u8BCD\u4F18\u5316\u5931\u8D25\u3002");
      var optimized = result.data && result.data.optimized || "";
      if (!optimized) throw new Error("Claude \u8FD4\u56DE\u4E3A\u7A7A\u3002");
      animateOptimizedReplace(inputBox, optimized);
    }).catch(function(error) {
      if (typeof showToast === "function") showToast(error && error.message || "\u63D0\u793A\u8BCD\u4F18\u5316\u5931\u8D25\u3002", "error");
      if (btn) {
        btn.classList.remove("is-loading");
        btn.classList.add("is-shake");
        setTimeout(function() {
          if (btn) btn.classList.remove("is-shake");
        }, 400);
      }
    }).finally(function() {
      promptOptimizeInFlight = false;
      if (btn) {
        btn.classList.remove("is-loading");
        btn.disabled = false;
        btn.setAttribute("title", "\u63D0\u793A\u8BCD\u4F18\u5316\uFF08AI\uFF09");
      }
      if (composer) composer.classList.remove("is-optimizing");
      inputBox.removeAttribute("aria-busy");
      inputBox.readOnly = prevReadOnly;
    });
  }
  function animateOptimizedReplace(inputBox, finalText) {
    if (!inputBox) return;
    var chars = Array.from(finalText);
    var total = chars.length;
    if (total === 0) {
      inputBox.value = "";
      setDraftValue("", true);
      autoResizeInput(inputBox);
      return;
    }
    var totalDuration = Math.min(700, Math.max(220, total * 8));
    var stepCount = Math.min(total, 60);
    var charsPerStep = Math.ceil(total / stepCount);
    var stepDelay = totalDuration / stepCount;
    var i = 0;
    inputBox.value = "";
    autoResizeInput(inputBox);
    function tick() {
      i = Math.min(total, i + charsPerStep);
      inputBox.value = chars.slice(0, i).join("");
      autoResizeInput(inputBox);
      if (i < total) {
        setTimeout(tick, stepDelay);
      } else {
        setDraftValue(finalText, true);
        try {
          inputBox.setSelectionRange(finalText.length, finalText.length);
        } catch (e) {
        }
      }
    }
    tick();
  }
  function syncComposerHasText(el) {
    var composer = document.querySelector(".input-composer");
    if (!composer) return;
    var inputBox = el || document.getElementById("input-box");
    var hasText = !!(inputBox && inputBox.value && inputBox.value.length > 0);
    composer.classList.toggle("has-text", hasText);
  }

  // src/web-ui/browser/utils.ts
  state._statusBarTimerId = null;
  state._statusBarStartTime = 0;
  var _runningIndicatorsTimerId = null;
  var _runningIndicatorsStartTime = 0;
  function computeRunningSignal2(session) {
    if (!session) return { active: false };
    if (session.archived) return { active: false };
    var permBlocked = !!session.permissionBlocked;
    var inFlight = !!(isStructuredSession2(session) && session.structuredState && session.structuredState.inFlight);
    var ptyRunning = !isStructuredSession2(session) && session.status === "running";
    return {
      active: inFlight || ptyRunning || permBlocked,
      inFlight,
      ptyRunning,
      permissionBlocked: permBlocked
    };
  }
  function formatElapsedShort3(ms) {
    var s = Math.max(0, Math.floor(ms / 1e3));
    if (s < 60) return s + "s";
    var m = Math.floor(s / 60);
    var rs = s % 60;
    if (m < 60) return m + "m" + (rs ? " " + rs + "s" : "");
    var h = Math.floor(m / 60);
    var rm = m % 60;
    return h + "h" + (rm ? " " + rm + "m" : "");
  }
  function updateRunningIndicators(session) {
    var sig = computeRunningSignal2(session);
    var headerRow = document.querySelector(".main-header-row");
    var pill = headerRow ? headerRow.querySelector(".session-status-pill") : null;
    var chatMessages = document.querySelector(".chat-messages");
    if (headerRow) {
      headerRow.classList.toggle("is-running", sig.active);
      headerRow.classList.toggle("is-permission-blocked", sig.permissionBlocked);
    }
    if (pill) {
      var elapsedEl = pill.querySelector(".session-status-elapsed");
      if (sig.inFlight) {
        if (!_runningIndicatorsStartTime) {
          _runningIndicatorsStartTime = state._statusBarStartTime > 0 ? state._statusBarStartTime : Date.now();
        }
        var label = formatElapsedShort3(Date.now() - _runningIndicatorsStartTime);
        if (!elapsedEl) {
          elapsedEl = document.createElement("span");
          elapsedEl.className = "session-status-elapsed";
          pill.appendChild(elapsedEl);
        }
        elapsedEl.textContent = label;
      } else {
        _runningIndicatorsStartTime = 0;
        if (elapsedEl) elapsedEl.remove();
      }
    }
    if (sig.active) {
      if (!_runningIndicatorsTimerId) {
        _runningIndicatorsTimerId = setInterval(function() {
          var sel = state.sessions.find(function(s) {
            return s.id === state.selectedId;
          });
          updateRunningIndicators(sel);
        }, 1e3);
      }
    } else if (_runningIndicatorsTimerId) {
      clearInterval(_runningIndicatorsTimerId);
      _runningIndicatorsTimerId = null;
    }
  }
  function renderStructuredStatusBar(chatMessages, session) {
    updateRunningIndicators(session);
    var topRow = document.querySelector(".composer-top-row");
    var existing = document.querySelector(".structured-status-bar");
    var composer = document.querySelector(".input-composer");
    if (!session || !isStructuredSession2(session)) {
      if (existing) existing.remove();
      if (composer) composer.classList.remove("in-flight");
      clearInterval(state._statusBarTimerId);
      state._statusBarTimerId = null;
      return;
    }
    var isInFlight = session.structuredState && session.structuredState.inFlight;
    if (isInFlight) {
      if (!state._statusBarTimerId) {
        state._statusBarStartTime = Date.now();
      }
      if (composer) composer.classList.add("in-flight");
      if (!existing && topRow) {
        var bar = document.createElement("div");
        bar.className = "structured-status-bar";
        bar.innerHTML = '<span class="status-bar-dot"></span><span class="status-bar-label">\u56DE\u590D\u4E2D</span><span class="status-bar-timer">0.0s</span>';
        topRow.appendChild(bar);
        existing = bar;
      } else if (existing && existing.classList.contains("completed")) {
        existing.classList.remove("completed");
        existing.style.animation = "none";
        existing.querySelector(".status-bar-label").textContent = "\u56DE\u590D\u4E2D";
        var dot = existing.querySelector(".status-bar-dot");
        if (dot) dot.style.display = "";
        state._statusBarStartTime = Date.now();
      }
      if (!state._statusBarTimerId) {
        state._statusBarTimerId = setInterval(function() {
          var bar2 = document.querySelector(".structured-status-bar:not(.completed)");
          if (!bar2) {
            clearInterval(state._statusBarTimerId);
            state._statusBarTimerId = null;
            return;
          }
          var elapsed2 = ((Date.now() - state._statusBarStartTime) / 1e3).toFixed(1);
          var timerEl = bar2.querySelector(".status-bar-timer");
          if (timerEl) timerEl.textContent = elapsed2 + "s";
        }, 100);
      }
    } else {
      clearInterval(state._statusBarTimerId);
      state._statusBarTimerId = null;
      if (composer) composer.classList.remove("in-flight");
      if (existing && !existing.classList.contains("completed")) {
        var elapsed = state._statusBarStartTime ? ((Date.now() - state._statusBarStartTime) / 1e3).toFixed(1) : "0.0";
        existing.classList.add("completed");
        existing.querySelector(".status-bar-label").textContent = "\u5B8C\u6210";
        existing.querySelector(".status-bar-timer").textContent = elapsed + "s";
        var dot = existing.querySelector(".status-bar-dot");
        if (dot) dot.style.display = "none";
        state._statusBarStartTime = 0;
        setTimeout(function() {
          if (existing.parentNode) existing.remove();
        }, 3e3);
      }
    }
  }
  function escapeHtml(value) {
    return String(value).replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/>/g, "&gt;").replace(/"/g, "&quot;").replace(/'/g, "&#39;");
  }
  function renderTailMarqueePath(value, className, attrs) {
    var text5 = String(value || "");
    return '<span class="' + className + ' tail-marquee-path" title="' + escapeHtml(text5) + '"' + (attrs || "") + '><span class="tail-marquee-path-inner">' + escapeHtml(text5) + "</span></span>";
  }
  var IMAGE_PATH_RE = /\.(png|jpe?g|gif|webp|svg|avif|bmp|ico|heic|heif)$/i;
  function isImagePath(value) {
    if (typeof value !== "string") return false;
    var clean = value.trim().split(/[?#]/)[0];
    return IMAGE_PATH_RE.test(clean);
  }
  function scrollPathElementToEnd(el) {
    if (!el) return;
    var apply = function() {
      try {
        var inner = el.firstElementChild && el.firstElementChild.classList && el.firstElementChild.classList.contains("tail-marquee-path-inner") ? el.firstElementChild : null;
        if (inner) {
          var overflow = Math.max(0, inner.scrollWidth - el.clientWidth);
          el.classList.toggle("is-overflowing", overflow > 1);
          el.style.setProperty("--tail-marquee-shift", overflow + "px");
          var travelSeconds = Math.max(4.8, overflow / 18);
          el.style.setProperty("--tail-marquee-duration", Math.max(6.8, travelSeconds / 0.68) + "s");
          return;
        }
        if (el.scrollWidth > el.clientWidth) {
          el.scrollLeft = el.scrollWidth;
        }
      } catch (e) {
      }
    };
    apply();
    if (typeof requestAnimationFrame === "function") {
      requestAnimationFrame(apply);
    }
  }
  function refreshTailMarqueePaths(root) {
    var scope = root || document;
    if (!scope || typeof scope.querySelectorAll !== "function") return;
    scope.querySelectorAll(".tail-marquee-path").forEach(function(el) {
      scrollPathElementToEnd(el);
    });
  }
  function setTailMarqueePathText(el, value) {
    if (!el) return;
    var text5 = String(value || "");
    var inner = el.querySelector && el.querySelector(".tail-marquee-path-inner");
    if (inner) inner.textContent = text5;
    else el.textContent = text5;
    if (el.setAttribute) el.setAttribute("title", text5);
    scrollPathElementToEnd(el);
  }
  function scrollInputToEnd(input) {
    if (!input) return;
    var apply = function() {
      try {
        if (typeof input.setSelectionRange === "function" && input.value != null) {
          var len = input.value.length;
          try {
            input.setSelectionRange(len, len);
          } catch (e) {
          }
        }
        if (input.scrollWidth > input.clientWidth) {
          input.scrollLeft = input.scrollWidth;
        }
      } catch (e) {
      }
    };
    apply();
    if (typeof requestAnimationFrame === "function") {
      requestAnimationFrame(apply);
    }
  }

  // src/web-ui/browser/folder-picker-adapter.ts
  var uninstallRuntime = null;
  function setTriggerExpanded(expanded) {
    if (isBrowserReactShellMounted()) return;
    document.getElementById("blank-chat-cwd")?.setAttribute("aria-expanded", expanded ? "true" : "false");
  }
  function syncWorkingDirectoryUi(path) {
    if (isBrowserReactShellMounted()) {
      notifyLegacyUiChange("working-dir");
      return;
    }
    const trigger = document.getElementById("blank-chat-cwd");
    if (trigger) trigger.title = `\u5F53\u524D\u5DE5\u4F5C\u76EE\u5F55\uFF1A${path}`;
    setTailMarqueePathText(document.getElementById("blank-chat-cwd-path"), path);
    const filePathInput = document.getElementById("file-explorer-cwd");
    if (!state.selectedId && filePathInput && document.activeElement !== filePathInput) {
      filePathInput.value = path;
      filePathInput.title = path;
    }
  }
  var legacyRuntime = {
    getInitialPath: getEffectiveCwd,
    onOpen() {
      newSessionController.closeIfOpen();
      quickCommitController.closeIfOpen();
      settingsController.closeIfOpen();
      worktreeMergeController.closeIfOpen();
      setTriggerExpanded(true);
    },
    onClose() {
      setTriggerExpanded(false);
      window.requestAnimationFrame(() => {
        document.getElementById("blank-chat-cwd")?.focus();
      });
    },
    applySelection(path) {
      saveWorkingDir(path);
      syncWorkingDirectoryUi(path);
    }
  };
  function openFolderPickerNow() {
    for (const controller of [
      newSessionController,
      quickCommitController,
      settingsController,
      worktreeMergeController
    ]) {
      if (controller.isOpen() && !controller.closeIfOpen()) return false;
    }
    if (!folderPickerController.open(getEffectiveCwd())) {
      showToast("\u5DE5\u4F5C\u76EE\u5F55\u9009\u62E9\u5668\u5C1A\u672A\u5C31\u7EEA\uFF0C\u8BF7\u5237\u65B0\u9875\u9762\u540E\u91CD\u8BD5\u3002", "error");
      return false;
    }
    return true;
  }
  function openFolderPickerFromLegacy() {
    if (!prepareFilePreviewForCompetingOverlay(() => {
      openFolderPickerNow();
    })) return false;
    return openFolderPickerNow();
  }
  function openFolderPicker(event) {
    event.preventDefault();
    openFolderPickerFromLegacy();
  }
  function findTrigger(target) {
    return target instanceof Element ? target.closest("#blank-chat-cwd") : null;
  }
  function handleClick(event) {
    if (isBrowserReactShellMounted()) return;
    if (!findTrigger(event.target)) return;
    openFolderPicker(event);
  }
  function handleKeyDown(event) {
    if (isBrowserReactShellMounted()) return;
    if (event.key !== "Enter" && event.key !== " ") return;
    if (!findTrigger(event.target)) return;
    openFolderPicker(event);
  }
  function installFolderPickerLegacyAdapter() {
    if (uninstallRuntime) return;
    uninstallRuntime = configureFolderPickerRuntime(legacyRuntime);
    document.addEventListener("click", handleClick);
    document.addEventListener("keydown", handleKeyDown);
  }

  // src/web-ui/browser/new-session-adapter.ts
  var uninstallRuntime2 = null;
  var legacyRuntime2 = {
    onOpen() {
      folderPickerController.closeIfOpen();
      quickCommitController.closeIfOpen();
      worktreeMergeController.closeIfOpen();
      closeSettingsModal();
      state.sessionsDrawerOpen = false;
      writeStoredBoolean("wand-sidebar-open", false);
      updateDrawerState();
    },
    onClose() {
    },
    getContext() {
      return {
        effectiveCwd: getEffectiveCwd(),
        selectedModels: {
          claude: getChatModelForProvider("claude"),
          codex: getChatModelForProvider("codex"),
          opencode: getChatModelForProvider("opencode"),
          grok: getChatModelForProvider("grok")
        },
        thinkingEffort: state.chatThinking || "off"
      };
    },
    async prepareCreate(kind) {
      if (kind !== "pty") return {};
      await ensureTerminalReady();
      try {
        state.terminal?.remeasure?.();
      } catch (_error) {
      }
      const cols = state.terminal?.cols;
      const rows = state.terminal?.rows;
      return {
        cols: typeof cols === "number" && Number.isFinite(cols) && cols > 0 ? cols : void 0,
        rows: typeof rows === "number" && Number.isFinite(rows) && rows > 0 ? rows : void 0
      };
    },
    async completeCreate(request2, created) {
      state.modeValue = request2.mode;
      state.chatMode = request2.mode;
      state.sessionTool = request2.provider;
      state.preferredCommand = request2.provider;
      state.selectedId = created.id;
      state.drafts[created.id] = "";
      persistSelectedId();
      saveWorkingDir(request2.cwd);
      resetChatRenderCache();
      syncComposerModeSelect();
      await loadSessions();
      syncComposerModelSelect(state.sessions.find((session) => session.id === created.id) || null);
      selectSession(created.id);
      dismissDrawerIfOverlay();
      window.setTimeout(() => focusInputBox2(true), 0);
    }
  };
  function installNewSessionLegacyAdapter() {
    if (uninstallRuntime2) return;
    uninstallRuntime2 = configureNewSessionRuntime(legacyRuntime2);
  }

  // src/web-ui/browser/settings-runtime-bridge.ts
  function installSettingsRuntimeBridge() {
    window.addEventListener("wand-settings-notifications-changed", (event) => {
      const detail = event.detail || {};
      if (typeof detail.sound === "boolean") state.notifSound = detail.sound;
      if (typeof detail.volume === "number") state.notifVolume = detail.volume;
      if (typeof detail.bubble === "boolean") state.notifBubble = detail.bubble;
    });
    window.addEventListener("wand-settings-config-saved", (event) => {
      const detail = event.detail;
      if (!detail) return;
      if (!state.config) state.config = {};
      Object.assign(state.config, detail);
    });
  }

  // src/web-ui/browser/shell-commands.ts
  function managedKind(target) {
    if (target === "claude-history") return "history";
    if (target === "codex-history") return "codex";
    return "sessions";
  }
  function confirmAndDelete(target, id) {
    const provider = target === "codex-history" ? "Codex" : target === "claude-history" ? "Claude" : "Wand";
    return confirmDelete(`\u786E\u8BA4\u5220\u9664\u8FD9\u6761 ${provider} \u4F1A\u8BDD\u5417\uFF1F`, { title: "\u5220\u9664\u4F1A\u8BDD" }).then((confirmed) => {
      if (!confirmed) return void 0;
      if (target === "codex-history") return deleteCodexHistorySession(id);
      if (target === "claude-history") return deleteClaudeHistorySession2(id, null);
      return deleteSession(id);
    });
  }
  function setQuickStartProvider(provider) {
    state.sessionTool = provider;
    state.preferredCommand = provider;
    if (provider === "codex") state.modeValue = "full-access";
    if (provider === "opencode") state.modeValue = "managed";
  }
  function quickStart(provider) {
    setQuickStartProvider(provider);
    return quickStartSession();
  }
  function quickStartStructured() {
    setQuickStartProvider("claude");
    return createStructuredSession().then((created) => {
      focusInputBox2(true);
      return created;
    });
  }
  function copyTopbarField(field) {
    const selected = state.sessions.find((session) => session.id === state.selectedId);
    if (field === "cwd") return copySelectedSessionField("cwd", "\u5DE5\u4F5C\u76EE\u5F55\u5DF2\u590D\u5236");
    if (field === "sessionId") return copySelectedSessionField("id", "\u4F1A\u8BDD ID \u5DF2\u590D\u5236");
    const provider = selected?.provider;
    const label = provider === "codex" ? "Codex thread ID \u5DF2\u590D\u5236" : provider === "opencode" ? "OpenCode session ID \u5DF2\u590D\u5236" : provider === "grok" ? "Grok \u4F1A\u8BDD ID \u5DF2\u590D\u5236" : provider === "qoder" ? "Qoder \u4F1A\u8BDD ID \u5DF2\u590D\u5236" : "Claude \u4F1A\u8BDD ID \u5DF2\u590D\u5236";
    return copySelectedSessionField("claudeSessionId", label);
  }
  function createBrowserShellCommands() {
    return {
      goHome,
      refreshPage: () => window.location.reload(),
      openNewSession: openSessionModal,
      quickStartClaudeTerminal: () => quickStart("claude"),
      quickStartCodexTerminal: () => quickStart("codex"),
      quickStartOpenCodeTerminal: () => quickStart("opencode"),
      quickStartStructuredSession: quickStartStructured,
      selectSession: (id) => {
        selectSession(id);
        dismissDrawerIfOverlay();
      },
      resumeSession: (id) => resumeSessionFromList(id).then((value) => {
        dismissDrawerIfOverlay();
        return value;
      }),
      resumeHistory: (provider, id, cwd) => resumeHistoryFromList(provider, id, cwd),
      deleteItem: confirmAndDelete,
      mergeSession: openWorktreeMergeModal,
      cleanupSession: retryWorktreeCleanup,
      toggleManageMode,
      toggleManagedSelection: (target, id) => toggleManagedItemSelection(managedKind(target), id),
      selectAllManaged: selectAllVisibleItems,
      clearManagedSelection: clearSelections,
      deleteManagedSelection: batchDeleteSelected,
      toggleSessionsDrawer,
      closeSessionsDrawer: closeSessionsDrawer2,
      toggleSidebarPin,
      toggleSidebarCollapsed,
      expandSidebarGroup: (group) => {
        writeStoredBoolean(
          group === "automation" ? "wand-automation-sessions-expanded" : "wand-non-wand-sessions-expanded",
          true
        );
        toggleSidebarCollapsed();
      },
      setSidebarGroupExpanded: (group, expanded) => {
        writeStoredBoolean(
          group === "automation" ? "wand-automation-sessions-expanded" : "wand-non-wand-sessions-expanded",
          expanded
        );
      },
      toggleFilePanel,
      closeFilePanel,
      refreshFiles: () => refreshFileExplorer(),
      navigateFiles: (cwd) => refreshFileExplorer({ cwd }),
      navigateFilesUp: navigateExplorerUp,
      searchFiles: (query) => {
        state.fileSearchQuery = query.trim();
        filterFileTree();
      },
      clearFileSearch: () => {
        state.fileSearchQuery = "";
        filterFileTree();
      },
      openFolderPicker: () => {
        openFolderPickerFromLegacy();
      },
      toggleTopbarMenu: () => {
        state.topbarMoreOpen = !state.topbarMoreOpen;
      },
      copyTopbarField,
      openGitCommit: openQuickCommitModal,
      openSettings: openSettingsModal,
      backToNative: backToNativeApp,
      switchServer,
      logout: logout2
    };
  }

  // src/web-ui/browser/main.ts
  installSettingsRuntimeBridge();
  configureBrowserShellCommands(createBrowserShellCommands());
  (function() {
    try {
      var ua = navigator && navigator.userAgent || "";
      if (/WandApp\//.test(ua)) {
        document.documentElement.classList.add("is-wand-app");
      }
      if (/WandPlatform\/iOS/.test(ua)) {
        document.documentElement.classList.add("is-wand-ios");
      }
      if (/WandPlatform\/Android/.test(ua)) {
        document.documentElement.classList.add("is-wand-android");
      }
      if (/WandPlatform\/macOS/.test(ua)) {
        document.documentElement.classList.add("is-wand-macos");
      }
    } catch (e) {
    }
    try {
      const params = new URL(window.location.href).searchParams;
      if (params.get("embed") === "terminal") {
        document.documentElement.classList.add("is-wand-embed-terminal");
        if (params.get("nativeInput") === "1") {
          document.documentElement.classList.add("is-wand-native-input");
        }
      }
    } catch (e) {
    }
    try {
      window.__wandNativeBackHooked = true;
    } catch (e) {
    }
    try {
      window.__wandConfigPath = configPath;
    } catch (e) {
    }
  })();
  startReactUi();
  installNewSessionLegacyAdapter();
  installFolderPickerLegacyAdapter();
  installWorktreeMergeLegacyAdapter({
    getSession(sessionId) {
      return state.sessions.find((session) => session.id === sessionId) ?? null;
    },
    refreshSessions() {
      return refreshAll();
    },
    toast(message, tone) {
      showToast(message, tone);
    }
  });
  installFilePreviewLegacyAdapter({
    getSiblings() {
      return state.allFiles;
    },
    confirmDiscard(reason) {
      const replacing = reason === "replace";
      return openWandDialog({
        type: "warning",
        title: replacing ? "\u5207\u6362\u9884\u89C8\u6587\u4EF6\uFF1F" : "\u653E\u5F03\u672A\u4FDD\u5B58\u7684\u4FEE\u6539\uFF1F",
        message: replacing ? "\u5F53\u524D\u6587\u4EF6\u6709\u672A\u4FDD\u5B58\u7684\u6539\u52A8\uFF0C\u5207\u6362\u540E\u4F1A\u4E22\u5931\u3002" : reason === "exit-edit" ? "\u5F53\u524D\u6587\u4EF6\u6709\u672A\u4FDD\u5B58\u7684\u6539\u52A8\uFF0C\u9000\u51FA\u7F16\u8F91\u540E\u4F1A\u4E22\u5931\u3002" : "\u5F53\u524D\u6587\u4EF6\u6709\u672A\u4FDD\u5B58\u7684\u6539\u52A8\uFF0C\u5173\u95ED\u540E\u4F1A\u4E22\u5931\u3002",
        buttons: [
          { label: "\u7EE7\u7EED\u7F16\u8F91", value: false, kind: "secondary", autofocus: true },
          { label: "\u653E\u5F03\u4FEE\u6539", value: true, kind: "danger" }
        ],
        cancelValue: false
      }).then((result) => result === true);
    },
    copyText: copyTextSafely,
    appendToComposer,
    notify(message, tone) {
      showToast(message, tone);
    },
    onSaved() {
      refreshFileExplorer();
    }
  });
})();
/*! Bundled license information:

react/cjs/react.production.js:
  (**
   * @license React
   * react.production.js
   *
   * Copyright (c) Meta Platforms, Inc. and affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)

react-dom/cjs/react-dom.production.js:
  (**
   * @license React
   * react-dom.production.js
   *
   * Copyright (c) Meta Platforms, Inc. and affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)

scheduler/cjs/scheduler.production.js:
  (**
   * @license React
   * scheduler.production.js
   *
   * Copyright (c) Meta Platforms, Inc. and affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)

react-dom/cjs/react-dom-client.production.js:
  (**
   * @license React
   * react-dom-client.production.js
   *
   * Copyright (c) Meta Platforms, Inc. and affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)

react/cjs/react-jsx-runtime.production.js:
  (**
   * @license React
   * react-jsx-runtime.production.js
   *
   * Copyright (c) Meta Platforms, Inc. and affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)
*/
